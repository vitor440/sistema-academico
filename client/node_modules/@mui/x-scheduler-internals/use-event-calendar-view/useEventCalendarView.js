"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.useEventCalendarView = useEventCalendarView;
var _useOnMount = require("@base-ui/utils/useOnMount");
var _store = require("@base-ui/utils/store");
var _useEventCalendarStoreContext = require("../use-event-calendar-store-context");
/**
 * Initializes the view on the event calendar.
 * The initialization is only done during the 1st render, the definition should be defined statically.
 * ```
 * @param parameters Parameters for the view.
 */
function useEventCalendarView(definition) {
  // Context hooks
  const store = (0, _useEventCalendarStoreContext.useEventCalendarStoreContext)();

  // Selector hooks
  const days = (0, _store.useStore)(store, definition.visibleDaysSelector);

  // Feature hooks
  (0, _useOnMount.useOnMount)(() => store.setViewDefinition(definition));
  return {
    days
  };
}