'use client';

import _extends from "@babel/runtime/helpers/esm/extends";
import * as React from 'react';
import { useStore } from '@base-ui/utils/store';
import { useEventCalendarView } from '@mui/x-scheduler-internals/use-event-calendar-view';
import { useEventCalendarStoreContext } from '@mui/x-scheduler-internals/use-event-calendar-store-context';
import { eventCalendarViewSelectors } from '@mui/x-scheduler-internals/event-calendar-selectors';
import { DayTimeGrid } from "../internals/components/day-time-grid/DayTimeGrid.mjs";
import { createDayTimeGridViewDefinition } from "../internals/utils/day-time-grid-view-definition.mjs";
import { jsx as _jsx } from "react/jsx-runtime";
const DAY_VIEW_DEFINITION = createDayTimeGridViewDefinition(1);

/**
 * A Day View to use inside the Event Calendar.
 *
 * Renders the desktop event variant, which `DayTimeGrid` resolves from the default value of
 * `DayTimeGridInternalRenderersContext` — no provider is needed here.
 */
export const DayView = /*#__PURE__*/React.memo(/*#__PURE__*/React.forwardRef(function DayView(props, forwardedRef) {
  // Context hooks
  const store = useEventCalendarStoreContext();

  // Feature hooks
  const {
    days
  } = useEventCalendarView(DAY_VIEW_DEFINITION);

  // Selector hooks
  const config = useStore(store, eventCalendarViewSelectors.timeGridConfig, 'day');
  return /*#__PURE__*/_jsx(DayTimeGrid, _extends({
    ref: forwardedRef,
    days: days,
    startTime: config?.startTime,
    endTime: config?.endTime
  }, props));
}));
if (process.env.NODE_ENV !== "production") DayView.displayName = "DayView";