'use client';

import _objectWithoutPropertiesLoose from "@babel/runtime/helpers/esm/objectWithoutPropertiesLoose";
import _extends from "@babel/runtime/helpers/esm/extends";
const _excluded = ["open", "onClose", "container", "className"];
import * as React from 'react';
import clsx from 'clsx';
import { useStore } from '@base-ui/utils/store';
import { styled } from '@mui/material/styles';
import Close from '@mui/icons-material/Close';
import ExpandMoreOutlined from '@mui/icons-material/ExpandMoreOutlined';
import ExpandLessOutlined from '@mui/icons-material/ExpandLessOutlined';
import SettingsOutlined from '@mui/icons-material/SettingsOutlined';
import CalendarViewMonthOutlined from '@mui/icons-material/CalendarViewMonthOutlined';
import CalendarViewWeekOutlined from '@mui/icons-material/CalendarViewWeekOutlined';
import CalendarViewDayOutlined from '@mui/icons-material/CalendarViewDayOutlined';
import ViewAgendaOutlined from '@mui/icons-material/ViewAgendaOutlined';
import Collapse from '@mui/material/Collapse';
import Drawer from '@mui/material/Drawer';
import IconButton from '@mui/material/IconButton';
import MenuItem from '@mui/material/MenuItem';
import MenuList from '@mui/material/MenuList';
import ListItemButton from '@mui/material/ListItemButton';
import ListItemIcon from '@mui/material/ListItemIcon';
import ListItemText from '@mui/material/ListItemText';
import { useAdapterContext } from '@mui/x-scheduler-internals/use-adapter-context';
import { useEventCalendarStoreContext } from '@mui/x-scheduler-internals/use-event-calendar-store-context';
import { eventCalendarViewSelectors } from '@mui/x-scheduler-internals/event-calendar-selectors';
import { schedulerOtherSelectors } from '@mui/x-scheduler-internals/scheduler-selectors';
import { ResourcesTree } from "../resources-tree/index.mjs";
import { usePreferencesMenuItems } from "../header-toolbar/preferences-menu/index.mjs";
import { useEventCalendarStyledContext } from "../EventCalendarStyledContext.mjs";
import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
const SidePanelDrawerRoot = styled(Drawer, {
  name: 'MuiEventCalendar',
  slot: 'SidePanelDrawerViewport'
})(({
  theme
}) => ({
  // Scope the modal to the calendar root (which is `position: relative` and owns the
  // root container query), instead of the default fixed, full-viewport overlay.
  position: 'absolute',
  zIndex: theme.zIndex.drawer,
  '& .MuiBackdrop-root': {
    position: 'absolute',
    backgroundColor: 'rgba(0, 0, 0, 0.5)'
  },
  '& .MuiDrawer-paper': {
    position: 'absolute',
    boxSizing: 'border-box',
    width: 'min(20rem, 85%)',
    height: '100%',
    display: 'flex',
    flexDirection: 'column',
    backgroundColor: (theme.vars || theme).palette.background.paper,
    borderRight: '1px solid',
    borderColor: (theme.vars || theme).palette.divider,
    overflowY: 'auto',
    overscrollBehavior: 'contain'
  }
}));
const SidePanelDrawerHeader = styled('div', {
  name: 'MuiEventCalendar',
  slot: 'SidePanelDrawerHeader'
})(({
  theme
}) => ({
  display: 'flex',
  alignItems: 'center',
  gap: theme.spacing(1),
  borderBottom: '1px solid',
  borderColor: (theme.vars || theme).palette.divider,
  padding: theme.spacing(2)
}));
const SidePanelDrawerTitle = styled('h2', {
  name: 'MuiEventCalendar',
  slot: 'SidePanelDrawerTitle'
})(({
  theme
}) => _extends({
  flex: 1,
  margin: 0
}, theme.typography.h6, {
  fontWeight: theme.typography.fontWeightBold
}));
const SidePanelDrawerViewList = styled(MenuList, {
  name: 'MuiEventCalendar',
  slot: 'SidePanelDrawerViewList'
})(({
  theme
}) => ({
  borderBottom: '1px solid',
  borderColor: (theme.vars || theme).palette.divider
}));
const SidePanelDrawerResources = styled('div', {
  name: 'MuiEventCalendar',
  slot: 'SidePanelDrawerResources'
})(({
  theme
}) => ({
  flexShrink: 0,
  display: 'flex',
  flexDirection: 'column',
  borderBottom: '1px solid',
  borderColor: (theme.vars || theme).palette.divider
}));
const VIEW_ICONS = {
  day: /*#__PURE__*/_jsx(CalendarViewDayOutlined, {
    fontSize: "small"
  }),
  week: /*#__PURE__*/_jsx(CalendarViewWeekOutlined, {
    fontSize: "small"
  }),
  month: /*#__PURE__*/_jsx(CalendarViewMonthOutlined, {
    fontSize: "small"
  }),
  agenda: /*#__PURE__*/_jsx(ViewAgendaOutlined, {
    fontSize: "small"
  })
};

/**
 * Compact-layout side panel (MUI temporary `Drawer`) with view switching, resources, and
 * preferences (inline `Collapse`). Portaled into the calendar root to stay container-scoped.
 */
export const SidePanelDrawer = /*#__PURE__*/React.forwardRef(function SidePanelDrawer(props, forwardedRef) {
  const {
      open,
      onClose,
      container,
      className
    } = props,
    other = _objectWithoutPropertiesLoose(props, _excluded);
  const store = useEventCalendarStoreContext();
  const adapter = useAdapterContext();
  const {
    schedulerId,
    classes,
    localeText
  } = useEventCalendarStyledContext();
  const views = useStore(store, eventCalendarViewSelectors.views);
  const view = useStore(store, eventCalendarViewSelectors.view);
  const visibleDate = useStore(store, schedulerOtherSelectors.visibleDate);
  const {
    hasAnyOption,
    items: preferencesItems
  } = usePreferencesMenuItems();
  const [preferencesOpen, setPreferencesOpen] = React.useState(false);
  const showViewSwitcher = views.length > 1;
  const titleId = `${schedulerId}-side-panel-drawer-title`;

  // Scope the drawer to the calendar root so it renders inside its bounds and
  // container query rather than portaling to `document.body`.
  const portalContainer = container?.current ?? undefined;

  // Collapse the preferences section when the drawer closes, so the next open
  // always starts with it collapsed.
  React.useEffect(() => {
    if (!open) {
      setPreferencesOpen(false);
    }
  }, [open]);
  return /*#__PURE__*/_jsxs(SidePanelDrawerRoot, {
    anchor: "left",
    open: open,
    onClose: onClose,
    className: classes.sidePanelDrawerViewport
    // Hide the drawer in the expanded layout via the root container query, so an
    // open compact drawer can never linger over the expanded calendar after a resize.
    // `EventCalendarRoot` also resets the open state so the modal fully unmounts.
    ,
    "data-compact-only": "",
    slotProps: {
      root: {
        container: portalContainer,
        disableScrollLock: true
      },
      backdrop: {
        className: classes.sidePanelDrawerBackdrop
      },
      paper: _extends({
        ref: forwardedRef
      }, other, {
        className: clsx(className, classes.sidePanelDrawer),
        role: 'dialog',
        'aria-modal': true,
        'aria-labelledby': titleId
      })
    },
    children: [/*#__PURE__*/_jsxs(SidePanelDrawerHeader, {
      className: classes.sidePanelDrawerHeader,
      children: [/*#__PURE__*/_jsxs(SidePanelDrawerTitle, {
        id: titleId,
        className: classes.sidePanelDrawerTitle,
        children: [adapter.format(visibleDate, 'monthFullLetter'), ' ', adapter.format(visibleDate, 'yearPadded')]
      }), /*#__PURE__*/_jsx(IconButton, {
        className: classes.sidePanelDrawerCloseButton,
        "aria-label": localeText.closeSidePanel,
        onClick: onClose,
        children: /*#__PURE__*/_jsx(Close, {})
      })]
    }), showViewSwitcher && /*#__PURE__*/_jsx(SidePanelDrawerViewList, {
      className: classes.sidePanelDrawerViewList,
      role: "listbox",
      children: views.map(viewItem => /*#__PURE__*/_jsxs(MenuItem, {
        className: classes.sidePanelDrawerViewItem,
        role: "option",
        selected: view === viewItem,
        "aria-selected": view === viewItem,
        onClick: event => store.setView(viewItem, event.nativeEvent),
        children: [/*#__PURE__*/_jsx(ListItemIcon, {
          children: VIEW_ICONS[viewItem]
        }), /*#__PURE__*/_jsx(ListItemText, {
          children: localeText[viewItem]
        })]
      }, viewItem))
    }), /*#__PURE__*/_jsx(SidePanelDrawerResources, {
      children: /*#__PURE__*/_jsx(ResourcesTree, {})
    }), hasAnyOption && /*#__PURE__*/_jsxs(React.Fragment, {
      children: [/*#__PURE__*/_jsx("div", {
        className: classes.sidePanelDrawerPreferences,
        children: /*#__PURE__*/_jsxs(ListItemButton, {
          className: classes.sidePanelDrawerPreferencesButton,
          "aria-expanded": preferencesOpen,
          onClick: () => setPreferencesOpen(prev => !prev),
          children: [/*#__PURE__*/_jsx(ListItemIcon, {
            children: /*#__PURE__*/_jsx(SettingsOutlined, {})
          }), /*#__PURE__*/_jsx(ListItemText, {
            children: localeText.preferencesMenu
          }), /*#__PURE__*/_jsx(ListItemIcon, {
            children: preferencesOpen ? /*#__PURE__*/_jsx(ExpandLessOutlined, {}) : /*#__PURE__*/_jsx(ExpandMoreOutlined, {})
          })]
        })
      }), /*#__PURE__*/_jsx(Collapse, {
        in: preferencesOpen,
        unmountOnExit: true,
        children: /*#__PURE__*/_jsx(MenuList, {
          className: classes.sidePanelDrawerPreferencesList,
          "aria-label": localeText.preferencesMenu,
          children: preferencesItems
        })
      })]
    })]
  });
});
if (process.env.NODE_ENV !== "production") SidePanelDrawer.displayName = "SidePanelDrawer";