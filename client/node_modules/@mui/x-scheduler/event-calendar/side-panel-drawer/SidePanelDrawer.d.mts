import * as React from 'react';
import type { SidePanelDrawerProps } from "./SidePanelDrawer.types.mjs";
/**
 * Compact-layout side panel (MUI temporary `Drawer`) with view switching, resources, and
 * preferences (inline `Collapse`). Portaled into the calendar root to stay container-scoped.
 */
export declare const SidePanelDrawer: React.ForwardRefExoticComponent<SidePanelDrawerProps & React.RefAttributes<HTMLDivElement>>;