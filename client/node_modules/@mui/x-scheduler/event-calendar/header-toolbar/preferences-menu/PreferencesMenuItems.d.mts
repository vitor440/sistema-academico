import * as React from 'react';
import type { CalendarView, EventCalendarPreferences, EventCalendarPreferencesMenuConfig } from '@mui/x-scheduler-internals/models';
/**
 * Computes which preference options are visible from the menu config and current view.
 * Shared by the expanded-layout `PreferencesMenu` and the compact-layout `SidePanelDrawer`.
 */
export declare function usePreferencesMenuModel(): {
  currentView: CalendarView;
  visibleOptions: {
    configKey: keyof EventCalendarPreferencesMenuConfig;
    preferenceKey: keyof EventCalendarPreferences;
    label: string;
  }[];
  visibleViewSpecificOptions: {
    configKey: keyof EventCalendarPreferencesMenuConfig;
    preferenceKey: keyof EventCalendarPreferences;
    view: CalendarView;
    label: string;
  }[];
  showTimeFormatSubmenu: boolean;
  showWeekStartsOnSubmenu: boolean;
  showSpecificOptions: boolean;
  hasAnyOption: boolean;
};
/**
 * Builds the preference options as a flat array of elements, so they spread into a
 * `Menu` (expanded layout) or `MenuList` (drawer). `hasAnyOption` is `false` when none show.
 */
export declare function usePreferencesMenuItems(): {
  hasAnyOption: boolean;
  items: React.ReactNode[];
};