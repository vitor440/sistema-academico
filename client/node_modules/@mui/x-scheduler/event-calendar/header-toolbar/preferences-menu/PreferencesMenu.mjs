'use client';

import _extends from "@babel/runtime/helpers/esm/extends";
import * as React from 'react';
import { styled } from '@mui/material/styles';
import { useMergedRefs } from '@base-ui/utils/useMergedRefs';
import SettingsOutlined from '@mui/icons-material/SettingsOutlined';
import IconButton from '@mui/material/IconButton';
import Menu from '@mui/material/Menu';
import clsx from 'clsx';
import { useEventCalendarStyledContext } from "../../EventCalendarStyledContext.mjs";
import { usePreferencesMenuItems } from "./PreferencesMenuItems.mjs";
import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
const PreferencesMenuRoot = styled('div', {
  name: 'MuiEventCalendar',
  slot: 'PreferencesMenu'
})({});
export const PreferencesMenu = /*#__PURE__*/React.forwardRef(function PreferencesMenu(props, forwardedRef) {
  // Context hooks
  const {
    schedulerId,
    classes,
    localeText
  } = useEventCalendarStyledContext();

  // Ref hooks
  const containerRef = React.useRef(null);
  const handleRef = useMergedRefs(forwardedRef, containerRef);

  // Model hooks
  const {
    hasAnyOption,
    items
  } = usePreferencesMenuItems();

  // State hooks
  const [anchorEl, setAnchorEl] = React.useState(null);
  const open = Boolean(anchorEl);
  const handleClick = event => {
    setAnchorEl(event.currentTarget);
  };
  const handleClose = () => {
    setAnchorEl(null);
  };

  // Early return if there is nothing to show
  if (!hasAnyOption) {
    return null;
  }
  return /*#__PURE__*/_jsxs(PreferencesMenuRoot, _extends({
    ref: handleRef
  }, props, {
    className: clsx(props.className, classes.preferencesMenu),
    children: [/*#__PURE__*/_jsx(IconButton, {
      className: classes.preferencesMenuButton,
      "aria-label": localeText.preferencesMenu,
      onClick: handleClick,
      "aria-controls": open ? `${schedulerId}-preferences-menu` : undefined,
      "aria-haspopup": "true",
      "aria-expanded": open ? 'true' : undefined,
      children: /*#__PURE__*/_jsx(SettingsOutlined, {})
    }), /*#__PURE__*/_jsx(Menu, {
      className: classes.preferencesMenuList,
      id: `${schedulerId}-preferences-menu`,
      anchorEl: anchorEl,
      open: open,
      onClose: handleClose,
      container: containerRef.current,
      anchorOrigin: {
        vertical: 'bottom',
        horizontal: 'right'
      },
      transformOrigin: {
        vertical: 'top',
        horizontal: 'right'
      },
      slotProps: {
        list: {
          'aria-label': localeText.preferencesMenu
        },
        paper: {
          sx: {
            minWidth: 220
          }
        }
      },
      children: items
    })]
  }));
});
if (process.env.NODE_ENV !== "production") PreferencesMenu.displayName = "PreferencesMenu";