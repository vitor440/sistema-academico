import { r as __toESM } from "./chunk-B-1-B7_t.js";
import { t as require_react } from "./react.js";
import { t as require_react_dom } from "./react-dom.js";
import { t as require_jsx_runtime } from "./react_jsx-runtime.js";
import { $ as generateUtilityClass, A as useId, Ct as composeClasses, N as useRtl, Q as shouldForwardProp, t as styled, wt as require_prop_types } from "./styled-CNP9jUFH.js";
import { t as clsx } from "./clsx-DQoRUQAf.js";
import { t as _extends } from "./extends-DrH2PCIy.js";
import { f as useThemeProps } from "./styles-BeNPniuI.js";
import { t as generateUtilityClasses } from "./generateUtilityClasses-BLzP2xdp.js";
import { t as useTheme } from "./useTheme-BYWxh-zq.js";
import { t as useForkRef } from "./useForkRef-CtfkQ7a-.js";
import { t as useSlotProps } from "./useSlotProps-on91qybo.js";
import { $ as ChartsTooltip, $n as useChartTooltip, $t as useChartGradientId, A as scatterSeriesConfig, An as selectorChartsInteractionRotationAxisIndex, Ar as selectorChartDrawingArea, At as useFocusedItem, B as createGetBarDimensions, Bn as selectorChartPolarCenter, Br as deg2rad, C as chartsSvgLayerClasses, Cn as useRotationAxis, Cr as selectorChartZoomOptionsLookup, Ct as ChartsText, D as selectorProgressiveBatchSize, Dn as useYAxis, Dr as DEFAULT_PIE_CHART_MARGIN, Dt as tickFrequencies, E as useProgressiveRendering, En as useYAxes, Er as isOrdinalScale, Et as useTicks, F as getSeriesLegendItems, Fn as generatePolar2svg, Fr as rainbowSurgePaletteDark, Ft as abs, G as useSkipAnimation, Gn as useChartId, Gr as DEFAULT_MARGINS, Gt as sin, H as getColor$1, Hn as selectorChartRotationAxis, Hr as AXIS_LABEL_DEFAULT_HEIGHT, Ht as max, I as ChartsSlotsProvider, In as generateSvg2polar, Ir as rainbowSurgePaletteLight, It as acos, J as chartsAxisHighlightClasses, Jn as useChartsContext, Jr as DEFAULT_X_AXIS_KEY, Jt as constant_default, K as ChartsAxisHighlight, Kn as useDrawingArea, Kr as DEFAULT_RADIUS_AXIS_KEY, Kt as sqrt, L as useChartsSlots, Ln as generateSvg2radius, Lr as selectorChartSeriesConfig, Lt as asin, M as identifierCleanerSeriesIdDataIndex, Mn as getRadiusAxisIndex, Mr as selectorChartsDataset, Mt as ChartsLocalizationProvider, N as identifierSerializerSeriesIdDataIndex, Nn as getRotationAxisIndex, Nr as useChartVisibilityManager, Nt as useChartRootRef, O as selectorProgressiveSeriesRevealedBatches, On as selectorChartsInteractionRadius, Or as selectorChartSeriesLayout, Ot as defaultProps, P as createCommonKeyboardFocusHandler, Pn as clampAngle, Pr as rainbowSurgePalette, Pt as withPath, Q as labelClasses, Qn as useChartKeyboardNavigation, Qt as ANIMATION_TIMING_FUNCTION, R as defaultSlotsMaterial, Rn as generateSvg2rotation, Rr as getScale, Rt as atan2, S as ChartsSvgLayer, Sn as useRotationAxes, Sr as selectorChartZoomMap, St as degToRad, T as ChartsDataProvider, Tn as useXAxis, Tr as useChartZAxis, Tt as useChartClosestPoint, U as getSeriesColorFn, Un as getPercentageValue, Ur as DEFAULT_AXIS_SIZE_HEIGHT, Ut as min, V as createGetBucketBarDimensions, Vn as selectorChartRadiusAxis, Vr as rad2deg, Vt as halfPi, W as useInternalIsZoomInteracting, Wn as isPolarSeriesType, Wr as DEFAULT_AXIS_SIZE_WIDTH, Wt as pi, X as legendClasses, Xn as useChartHighlight, Xt as useAnimateArea, Y as ChartsLegend, Yn as ChartsProvider, Yr as DEFAULT_Y_AXIS_KEY, Yt as useAnimateLine, Z as ChartsLabel, Zn as selectorChartsTooltipItem, Zt as useAnimate, _ as ChartsWrapper, _n as useChartsLayerContainerRef, _r as useChartInteraction, _t as ChartsAxis, a as LineHighlightElement, an as createIsFaded, ar as selectorChartsHighlightYAxisValue, at as ChartsLabelMark, b as ChartsSurface, bn as useRadiusAxes, br as selectorChartYAxis, bt as getDefaultBaseline, c as LinePlot, cn as selectorChartIsSeriesHighlighted, cr as selectorBrushCurrentX, ct as ChartsTooltipCell, d as AreaPlot, dn as selectorChartsHighlightedItem, dr as selectorBrushStartY, dt as ChartsTooltipTable, en as useChartGradientIdObjectBound, er as getFullRangeZoom, et as ChartsTooltipContainer, f as AreaElement, fn as useLineSeries, fr as selectorBrushState, ft as useItemTooltip, g as useInteractionItemProps, gn as useSeries, gr as selectorChartsInteractionIsInitialized, gt as chartsGridClasses, h as getInteractionItemProps, hn as useSeriesOfType, hr as checkHasInteractionPlugin, ht as ChartsGrid, i as LineHighlightPlot, in as useItemHighlightState, ir as selectorChartsHighlightXAxisValue, it as ChartsItemTooltipContent, j as getPieCoordinates, jn as selectorChartsInteractionRotationAxisValue, jr as selectorChartSeriesProcessed, jt as useChartsLocalization, k as selectorShouldUseProgressiveRenderer, kn as selectorChartsInteractionRadiusAxisValue, kr as defaultizeMargin, kt as axisClasses, l as LineElement, ln as selectorChartSeriesHighlightedItem, lr as selectorBrushCurrentY, lt as ChartsTooltipPaper, m as AnimatedArea, mn as useAllSeriesOfType, mr as useChartCartesianAxis, mt as chartsTooltipClasses, n as FocusedLineMark, nn as useLegend, nr as getValueToPositionMapper, nt as useAxesTooltip, o as MarkPlot, on as createIsHighlighted, or as useChartBrush, ot as consumeThemeProps, p as lineClasses, pn as useLineSeriesContext, pr as selectorChartsItemIsFocused, pt as useRadarItemTooltip, q as ChartsAxisHighlightPath, qn as useStore, qr as DEFAULT_ROTATION_AXIS_KEY, qt as tau, r as LINE_CHART_PLUGINS, rn as useItemHighlightStateGetter, rr as selectorBarItemAtPosition, rt as getLabel, s as MarkElement, sn as selectorChartIsSeriesFaded, sr as selectorBrushConfig, st as labelMarkClasses, t as LineChart, tn as useChartGradientIdObjectBoundBuilder, tr as selectorChartSamplingPyramids, tt as ChartsAxisTooltipContent, u as AnimatedLine, un as selectorChartSeriesUnfadedItem, ur as selectorBrushStartX, ut as ChartsTooltipRow, v as chartsToolbarClasses, vn as useZAxes, vr as getChartPoint, vt as ChartsYAxis, w as chartsSurfaceClasses, wn as useXAxes, wr as getMarkerSize, wt as ChartsClipPath, x as ChartsLayerContainer, xn as useRadiusAxis, xr as selectorChartZoomIsInteracting, xt as getDefaultTextAnchor, y as useChartsContainerProps, yn as useZAxis, yr as selectorChartXAxis, yt as ChartsXAxis, z as ChartsOverlay, zn as getDrawingAreaCenter, zr as number_default, zt as cos } from "./LineChart-2r3Zznj0.js";
import { a as createSelector, o as createSelectorMemoized, t as fastObjectShallowCompare } from "./fastObjectShallowCompare-BusicMYR.js";
import { t as _objectWithoutPropertiesLoose } from "./objectWithoutPropertiesLoose-BwcsosU7.js";
import { t as warnOnce } from "./warning-ci_nFDcC.js";
import { t as useComponentRenderer } from "./useComponentRenderer-D7Svmsoj.js";
//#region node_modules/@mui/x-charts/colorPalettes/categorical/blueberryTwilight.mjs
var blueberryTwilightPaletteLight = [
	"#02B2AF",
	"#2E96FF",
	"#B800D8",
	"#60009B",
	"#2731C8",
	"#03008D"
];
var blueberryTwilightPaletteDark = [
	"#02B2AF",
	"#72CCFF",
	"#DA00FF",
	"#9001CB",
	"#2E96FF",
	"#3B48E0"
];
var blueberryTwilightPalette = (mode) => mode === "dark" ? blueberryTwilightPaletteDark : blueberryTwilightPaletteLight;
//#endregion
//#region node_modules/@mui/x-charts/colorPalettes/categorical/mangoFusion.mjs
var mangoFusionPaletteLight = [
	"#173A5E",
	"#00A3A0",
	"#C91B63",
	"#EF5350",
	"#FFA726",
	"#B800D8",
	"#60009B",
	"#2E96FF",
	"#2731C8",
	"#03008D"
];
var mangoFusionPaletteDark = [
	"#41698F",
	"#19D0CD",
	"#DE196B",
	"#FC5F5C",
	"#FFD771",
	"#DA00FF",
	"#9001CB",
	"#72CCFF",
	"#2E96FF",
	"#3B48E0"
];
var mangoFusionPalette = (mode) => mode === "dark" ? mangoFusionPaletteDark : mangoFusionPaletteLight;
//#endregion
//#region node_modules/@mui/x-charts/colorPalettes/categorical/cheerfulFiesta.mjs
var cheerfulFiestaPaletteDark = [
	"#0059B2",
	"#2E96FF",
	"#FFC24C",
	"#FF9F0E",
	"#F38200",
	"#2ABFDE",
	"#1F94AD",
	"#BD2C38",
	"#FF3143",
	"#FF8282"
];
var cheerfulFiestaPaletteLight = [
	"#003A75",
	"#007FFF",
	"#FFC24C",
	"#FF9D09",
	"#CA6C00",
	"#127D94",
	"#1F94AD",
	"#C82634",
	"#FF3143",
	"#FF7E7E"
];
var cheerfulFiestaPalette = (mode) => mode === "dark" ? cheerfulFiestaPaletteDark : cheerfulFiestaPaletteLight;
//#endregion
//#region node_modules/@mui/x-charts/colorPalettes/sequential/strawberrySky.mjs
var strawberrySkyPaletteLight = [
	"#6877FF",
	"#694FFD",
	"#A94FFD",
	"#DA4FFD",
	"#F050A5",
	"#FF5E6C"
];
var strawberrySkyPaletteDark = strawberrySkyPaletteLight;
var strawberrySkyPalette = (mode) => mode === "dark" ? strawberrySkyPaletteDark : strawberrySkyPaletteLight;
//#endregion
//#region node_modules/@mui/x-charts/colorPalettes/sequential/blue.mjs
var bluePaletteLight = [
	"#BDDEFF",
	"#99CCFF",
	"#66B2FF",
	"#2E96FF",
	"#0064D6",
	"#0D47A1",
	"#0A367B",
	"#072555"
];
var bluePaletteDark = bluePaletteLight;
var bluePalette = (mode) => mode === "dark" ? bluePaletteDark : bluePaletteLight;
//#endregion
//#region node_modules/@mui/x-charts/colorPalettes/sequential/cyan.mjs
var cyanPaletteLight = [
	"#CFE9E8",
	"#A3DAD8",
	"#7ED0CE",
	"#44BDBA",
	"#299896",
	"#137370",
	"#0E5A58",
	"#073938"
];
var cyanPaletteDark = cyanPaletteLight;
var cyanPalette = (mode) => mode === "dark" ? cyanPaletteDark : cyanPaletteLight;
//#endregion
//#region node_modules/@mui/x-charts/colorPalettes/sequential/green.mjs
var greenPaletteLight = [
	"#CDEBDD",
	"#B2E2CB",
	"#8FD8B5",
	"#54C690",
	"#31B375",
	"#359F6D",
	"#0F7746",
	"#065731"
];
var greenPaletteDark = greenPaletteLight;
var greenPalette = (mode) => mode === "dark" ? greenPaletteDark : greenPaletteLight;
//#endregion
//#region node_modules/@mui/x-charts/colorPalettes/sequential/orange.mjs
var orangePaletteLight = [
	"#FBDBC3",
	"#F9BD92",
	"#F99F5D",
	"#FF7A19",
	"#FD620B",
	"#E15100",
	"#AC3E00",
	"#822F00"
];
var orangePaletteDark = orangePaletteLight;
var orangePalette = (mode) => mode === "dark" ? orangePaletteDark : orangePaletteLight;
//#endregion
//#region node_modules/@mui/x-charts/colorPalettes/sequential/pink.mjs
var pinkPaletteLight = [
	"#F7D2E1",
	"#F6BED5",
	"#F4A0C3",
	"#F6619F",
	"#EE448B",
	"#E32977",
	"#B6215F",
	"#8B1F4C"
];
var pinkPaletteDark = pinkPaletteLight;
var pinkPalette = (mode) => mode === "dark" ? pinkPaletteDark : pinkPaletteLight;
//#endregion
//#region node_modules/@mui/x-charts/colorPalettes/sequential/purple.mjs
var purplePaletteLight = [
	"#CAD4EE",
	"#98ADE5",
	"#577EE3",
	"#4254FB",
	"#2638DF",
	"#222FA6",
	"#111C7F",
	"#091159"
];
var purplePaletteDark = purplePaletteLight;
var purplePalette = (mode) => mode === "dark" ? purplePaletteDark : purplePaletteLight;
//#endregion
//#region node_modules/@mui/x-charts/colorPalettes/sequential/red.mjs
var redPaletteLight = [
	"#FAE0E0",
	"#F7C0BF",
	"#F3A2A0",
	"#EF5350",
	"#E53935",
	"#DC2B27",
	"#860B08",
	"#560503 "
];
var redPaletteDark = redPaletteLight;
var redPalette = (mode) => mode === "dark" ? redPaletteDark : redPaletteLight;
//#endregion
//#region node_modules/@mui/x-charts/colorPalettes/sequential/yellow.mjs
var yellowPaletteLight = [
	"#FBEFD6",
	"#F5DEB0",
	"#F3CD80",
	"#FAC14F",
	"#FFB219",
	"#EF9801",
	"#DA7D0B",
	"#AB6208"
];
var yellowPaletteDark = yellowPaletteLight;
var yellowPalette = (mode) => mode === "dark" ? yellowPaletteDark : yellowPaletteLight;
//#endregion
//#region node_modules/@mui/x-charts/context/useChartApiContext.mjs
var import_react = /* @__PURE__ */ __toESM(require_react(), 1);
/**
* The `useChartApiContext` hook provides access to the chart API.
* This is only available when the chart is rendered within a chart or a `ChartsDataProvider` component.
* If you want to access the chart API outside those components, you should use the `apiRef` prop instead.
* @example
* const apiRef = useChartApiContext<ChartApi<'bar'>>();
*/
function useChartApiContext() {
	const { publicAPI } = useChartsContext();
	const apiRef = import_react.useRef(publicAPI);
	import_react.useEffect(() => {
		apiRef.current = publicAPI;
	}, [publicAPI]);
	return apiRef;
}
//#endregion
//#region node_modules/@mui/x-charts/internals/plugins/featurePlugins/useChartPolarAxis/defaultizeAxis.mjs
function defaultizeAxis(inAxis, dataset, axisName) {
	const DEFAULT_AXIS_KEY = axisName === "rotation" ? DEFAULT_ROTATION_AXIS_KEY : DEFAULT_RADIUS_AXIS_KEY;
	return (inAxis && inAxis.length > 0 ? inAxis : [{ id: DEFAULT_AXIS_KEY }]).map((axisConfig, index) => {
		const id = `defaultized-${axisName}-axis-${index}`;
		const dataKey = axisConfig.dataKey;
		if (axisConfig.data !== void 0 || dataKey === void 0 && !axisConfig.valueGetter) return _extends({ id }, axisConfig);
		if (dataset === void 0) throw new Error(`MUI X Charts: The ${axisName}-axis uses \`dataKey\` or \`valueGetter\` but no \`dataset\` is provided. When using dataKey or valueGetter, a dataset must be provided to retrieve the axis data. Either provide a dataset prop or use the data property directly on the ${axisName}-axis.`);
		return _extends({
			id,
			data: axisConfig.valueGetter ? dataset.map((d) => axisConfig.valueGetter(d)) : dataset.map((d) => d[dataKey])
		}, axisConfig);
	});
}
//#endregion
//#region node_modules/@mui/x-charts/internals/plugins/featurePlugins/useChartPolarAxis/useChartPolarAxis.mjs
var useChartPolarAxis = ({ params, store, instance }) => {
	const { chartsLayerContainerRef } = instance;
	const { rotationAxis, radiusAxis, dataset } = params;
	{
		const ids = [...rotationAxis ?? [], ...radiusAxis ?? []].filter((axis) => axis.id).map((axis) => axis.id);
		const duplicates = new Set(ids.filter((id, index) => ids.indexOf(id) !== index));
		if (duplicates.size > 0) warnOnce([`MUI X Charts: The following axis ids are duplicated: ${Array.from(duplicates).join(", ")}.`, `Please make sure that each axis has a unique id.`].join("\n"), "error");
	}
	const drawingArea = store.use(selectorChartDrawingArea);
	const processedSeries = store.use(selectorChartSeriesProcessed);
	const center = store.use(selectorChartPolarCenter);
	const isInteractionEnabled = store.use(selectorChartsInteractionIsInitialized);
	const { axis: rotationAxisWithScale, axisIds: rotationAxisIds } = store.use(selectorChartRotationAxis);
	const { axis: radiusAxisWithScale, axisIds: radiusAxisIds } = store.use(selectorChartRadiusAxis);
	const isFirstRender = import_react.useRef(true);
	import_react.useEffect(() => {
		if (isFirstRender.current) {
			isFirstRender.current = false;
			return;
		}
		store.set("polarAxis", _extends({}, store.state.polarAxis, {
			rotation: defaultizeAxis(rotationAxis, dataset, "rotation"),
			radius: defaultizeAxis(radiusAxis, dataset, "radius")
		}));
	}, [
		drawingArea,
		rotationAxis,
		radiusAxis,
		dataset,
		store
	]);
	const svg2rotation = import_react.useMemo(() => generateSvg2rotation({
		cx: center.cx,
		cy: center.cy
	}), [center.cx, center.cy]);
	const svg2polar = import_react.useMemo(() => generateSvg2polar({
		cx: center.cx,
		cy: center.cy
	}), [center.cx, center.cy]);
	const polar2svg = import_react.useMemo(() => generatePolar2svg({
		cx: center.cx,
		cy: center.cy
	}), [center.cx, center.cy]);
	const usedRotationAxisId = rotationAxisIds[0];
	const usedRadiusAxisId = radiusAxisIds[0];
	const mousePosition = import_react.useRef({ isInChart: false });
	const hasInteractionPlugin = checkHasInteractionPlugin(instance);
	import_react.useEffect(() => {
		const element = chartsLayerContainerRef.current;
		if (!isInteractionEnabled || !hasInteractionPlugin || element === null || params.disableAxisListener) return () => {};
		const moveEndHandler = instance.addInteractionListener("moveEnd", (event) => {
			if (!event.detail.activeGestures.pan) {
				mousePosition.current.isInChart = false;
				instance.cleanInteraction();
			}
		});
		const panEndHandler = instance.addInteractionListener("panEnd", (event) => {
			if (!event.detail.activeGestures.move) {
				mousePosition.current.isInChart = false;
				instance.cleanInteraction?.();
			}
		});
		const pressEndHandler = instance.addInteractionListener("quickPressEnd", (event) => {
			if (!event.detail.activeGestures.move && !event.detail.activeGestures.pan) {
				mousePosition.current.isInChart = false;
				instance.cleanInteraction?.();
			}
		});
		const gestureHandler = (event) => {
			const srcEvent = event.detail.srcEvent;
			if (event.detail.srcEvent.pointerType === "touch") {
				const svgRect = element.getBoundingClientRect();
				if (srcEvent.clientX < svgRect.left || srcEvent.clientX > svgRect.right || srcEvent.clientY < svgRect.top || srcEvent.clientY > svgRect.bottom) {
					mousePosition.current.isInChart = false;
					instance.cleanInteraction?.();
					return;
				}
				const svgPoint = getChartPoint(element, srcEvent);
				mousePosition.current.isInChart = true;
				instance.setPointerCoordinate?.(svgPoint);
				return;
			}
			const svgPoint = getChartPoint(element, srcEvent);
			if (!instance.isPointInside(svgPoint.x, svgPoint.y, event.detail.target)) {
				if (mousePosition.current.isInChart) {
					instance.cleanInteraction?.();
					mousePosition.current.isInChart = false;
				}
				return;
			}
			if ((center.cx - svgPoint.x) ** 2 + (center.cy - svgPoint.y) ** 2 > radiusAxisWithScale[usedRadiusAxisId].scale.range()[1] ** 2) {
				if (mousePosition.current.isInChart) {
					instance.cleanInteraction?.();
					mousePosition.current.isInChart = false;
				}
				return;
			}
			mousePosition.current.isInChart = true;
			instance.setPointerCoordinate?.(svgPoint);
		};
		const moveHandler = instance.addInteractionListener("move", gestureHandler);
		const panHandler = instance.addInteractionListener("pan", gestureHandler);
		const pressHandler = instance.addInteractionListener("quickPress", gestureHandler);
		return () => {
			moveHandler.cleanup();
			moveEndHandler.cleanup();
			panHandler.cleanup();
			panEndHandler.cleanup();
			pressHandler.cleanup();
			pressEndHandler.cleanup();
		};
	}, [
		chartsLayerContainerRef,
		store,
		center,
		radiusAxisWithScale,
		usedRadiusAxisId,
		rotationAxisWithScale,
		usedRotationAxisId,
		instance,
		params.disableAxisListener,
		isInteractionEnabled,
		svg2rotation,
		hasInteractionPlugin
	]);
	import_react.useEffect(() => {
		const element = chartsLayerContainerRef.current;
		const onAxisClick = params.onAxisClick;
		if (element === null || !onAxisClick) return () => {};
		const axisClickHandler = instance.addInteractionListener("tap", (event) => {
			let dataIndex = null;
			let isRotationAxis = false;
			const svgPoint = getChartPoint(element, event.detail.srcEvent);
			const rotation = generateSvg2rotation(center)(svgPoint.x, svgPoint.y);
			const rotationIndex = getRotationAxisIndex(rotationAxisWithScale[usedRotationAxisId], rotation);
			const radius = generateSvg2radius(center)(svgPoint.x, svgPoint.y);
			const radiusIndex = getRadiusAxisIndex(radiusAxisWithScale[usedRadiusAxisId], radius);
			isRotationAxis = rotationIndex !== -1;
			dataIndex = isRotationAxis ? rotationIndex : radiusIndex;
			const USED_AXIS_ID = isRotationAxis ? usedRotationAxisId : usedRadiusAxisId;
			if (dataIndex == null || dataIndex === -1) return;
			const axisValue = (isRotationAxis ? rotationAxisWithScale : radiusAxisWithScale)[USED_AXIS_ID].data[dataIndex];
			const seriesValues = {};
			Object.keys(processedSeries).filter(isPolarSeriesType).forEach((seriesType) => {
				processedSeries[seriesType]?.seriesOrder.forEach((seriesId) => {
					seriesValues[seriesId] = processedSeries[seriesType].series[seriesId].data[dataIndex];
				});
			});
			onAxisClick(event.detail.srcEvent, {
				dataIndex,
				axisValue,
				seriesValues
			});
		});
		return () => {
			axisClickHandler.cleanup();
		};
	}, [
		center,
		instance,
		params.onAxisClick,
		processedSeries,
		radiusAxisWithScale,
		rotationAxisWithScale,
		chartsLayerContainerRef,
		usedRadiusAxisId,
		usedRotationAxisId
	]);
	return { instance: {
		svg2polar,
		svg2rotation,
		polar2svg
	} };
};
useChartPolarAxis.params = {
	rotationAxis: true,
	radiusAxis: true,
	dataset: true,
	disableAxisListener: true,
	onAxisClick: true
};
useChartPolarAxis.getInitialState = (params) => ({ polarAxis: {
	rotation: defaultizeAxis(params.rotationAxis, params.dataset, "rotation"),
	radius: defaultizeAxis(params.radiusAxis, params.dataset, "radius")
} });
//#endregion
//#region node_modules/@mui/x-charts/hooks/useScale.mjs
var import_jsx_runtime = require_jsx_runtime();
var import_prop_types = /* @__PURE__ */ __toESM(require_prop_types(), 1);
/**
* Get the X scale.
*
* @param axisId - The axis identifier. Can be:
*   - A string or number matching the axis ID defined in the chart's `xAxis` prop
*   - Undefined to get the default (first) X axis
* @returns The X axis scale
*/
function useXScale(axisId) {
	return useXAxis(axisId).scale;
}
/**
* Get the Y scale.
*
* @param axisId - The axis identifier. Can be:
*   - A string or number matching the axis ID defined in the chart's `yAxis` prop
*   - Undefined to get the default (first) Y axis
* @returns The Y axis scale
*/
function useYScale(axisId) {
	return useYAxis(axisId).scale;
}
/**
* Get the rotation scale.
*
* @param axisId - The axis identifier. Can be:
*   - A string or number matching the axis ID defined in the chart's `rotationAxis` prop
*   - Undefined to get the default rotation axis
* @returns The rotation axis scale, or undefined if not found
*/
function useRotationScale(axisId) {
	return useRotationAxis(axisId)?.scale;
}
/**
* Get the radius scale.
* @param axisId - The axis identifier. Can be:
*   - A string or number matching the axis ID defined in the chart's `radiusAxis` prop
*   - Undefined to get the default radius axis
* @returns The radius axis scale, or undefined if not found
*/
function useRadiusScale(axisId) {
	return useRadiusAxis(axisId)?.scale;
}
//#endregion
//#region node_modules/@mui/x-charts/hooks/useColorScale.mjs
/**
* Get the X axis color scale.
*
* @param {AxisId | undefined} axisId - If provided returns color scale for axisId, else returns the values for the default axis.
* @returns {AxisScaleComputedConfig[S]['colorScale'] | undefined} The color scale for the specified X axis, or undefined if not found.
*/
function useXColorScale(axisId) {
	return useXAxis(axisId).colorScale;
}
/**
* Get the Y axis color scale.
*
* @param {AxisId | undefined} axisId - If provided returns color scale for axisId, else returns the values for the default axis.
* @returns {AxisScaleComputedConfig[S]['colorScale'] | undefined} The color scale for the specified Y axis, or undefined if not found.
*/
function useYColorScale(axisId) {
	return useYAxis(axisId).colorScale;
}
/**
* Get the Z axis color scale.
*
* @param {AxisId | undefined} axisId - If provided returns color scale for axisId, else returns the values for the default axis.
* @returns {AxisScaleComputedConfig[S]['colorScale'] | undefined} The color scale for the specified Z axis, or undefined if not found.
*/
function useZColorScale(axisId) {
	return useZAxis(axisId).colorScale;
}
//#endregion
//#region node_modules/@mui/x-charts/hooks/useDataset.mjs
/**
* Get access to the dataset used to populate series and axes data.
* @returns {DatasetType | undefined} The dataset array if provided, otherwise undefined.
*/
function useDataset() {
	return useStore().use(selectorChartsDataset);
}
//#endregion
//#region node_modules/@mui/x-charts/hooks/useScatterSeries.mjs
/**
* Get access to the internal state of scatter series.
*
* @param {SeriesId} seriesId The id of the series to get.
* @returns {UseScatterSeriesReturnValue} the scatter series
*/
/**
* Get access to the internal state of scatter series.
*
* When called without arguments, it returns all scatter series.
*
* @returns {UseScatterSeriesReturnValue[]} the scatter series
*/
/**
* Get access to the internal state of scatter series.
*
* @param {SeriesId[]} seriesIds The ids of the series to get. Order is preserved.
* @returns {UseScatterSeriesReturnValue[]} the scatter series
*/
function useScatterSeries(seriesIds) {
	return useSeriesOfType("scatter", seriesIds);
}
/**
* Get access to the internal state of scatter series.
* The returned object contains:
* - series: a mapping from ids to series attributes.
* - seriesOrder: the array of series ids.
* @returns the scatter series
*/
function useScatterSeriesContext() {
	return useAllSeriesOfType("scatter");
}
//#endregion
//#region node_modules/@mui/x-charts/hooks/usePieSeries.mjs
/**
* Get access to the internal state of pie series.
*
* @param {SeriesId} seriesId The id of the series to get.
* @returns {UsePieSeriesReturnValue} the pie series
*/
/**
* Get access to the internal state of pie series.
*
* When called without arguments, it returns all pie series.
*
* @returns {UsePieSeriesReturnValue[]} the pie series
*/
/**
* Get access to the internal state of pie series.
*
* @param {SeriesId[]} seriesIds The ids of the series to get. Order is preserved.
* @returns {UsePieSeriesReturnValue[]} the pie series
*/
function usePieSeries(seriesIds) {
	return useSeriesOfType("pie", seriesIds);
}
/**
* Get access to the internal state of pie series.
* The returned object contains:
* - series: a mapping from ids to series attributes.
* - seriesOrder: the array of series ids.
* @returns the pie series
*/
function usePieSeriesContext() {
	return useAllSeriesOfType("pie");
}
/**
* Get access to the pie layout.
* @returns {Record<SeriesId, PieSeriesLayout>} the pie layout
*/
function usePieSeriesLayout() {
	return useStore().use(selectorChartSeriesLayout).pie ?? {};
}
//#endregion
//#region node_modules/@mui/x-charts/hooks/useBarSeries.mjs
/**
* Get access to the internal state of bar series.
*
* @param {SeriesId} seriesId The id of the series to get.
* @returns {UseBarSeriesReturnValue} the bar series
*/
/**
* Get access to the internal state of bar series.
*
* When called without arguments, it returns all bar series.
*
* @returns {UseBarSeriesReturnValue[]} the bar series
*/
/**
* Get access to the internal state of bar series.
*
* @param {SeriesId[]} seriesIds The ids of the series to get. Order is preserved.
* @returns {UseBarSeriesReturnValue[]} the bar series
*/
function useBarSeries(seriesIds) {
	return useSeriesOfType("bar", seriesIds);
}
/**
* Get access to the internal state of bar series.
* The returned object contains:
* - series: a mapping from ids to series attributes.
* - seriesOrder: the array of series ids.
* - stackingGroups: the array of stacking groups. Each group contains the series ids stacked and the strategy to use.
* @returns the bar series
*/
function useBarSeriesContext() {
	return useAllSeriesOfType("bar");
}
//#endregion
//#region node_modules/@mui/x-charts/hooks/useRadarSeries.mjs
/**
* Get access to the internal state of radar series.
*
* @param {SeriesId} seriesId The id of the series to get.
* @returns {UseRadarSeriesReturnValue} the radar series
*/
/**
* Get access to the internal state of radar series.
*
* When called without arguments, it returns all radar series.
*
* @returns {UseRadarSeriesReturnValue[]} the radar series
*/
/**
* Get access to the internal state of radar series.
*
* @param {SeriesId[]} seriesIds The ids of the series to get. Order is preserved.
* @returns {UseRadarSeriesReturnValue[]} the radar series
*/
function useRadarSeries(seriesIds) {
	return useSeriesOfType("radar", seriesIds);
}
/**
* Get access to the internal state of radar series.
* The returned object contains:
* - series: a mapping from ids to series attributes.
* - seriesOrder: the array of series ids.
* @returns the radar series
*/
function useRadarSeriesContext() {
	return useAllSeriesOfType("radar");
}
//#endregion
//#region node_modules/@mui/x-charts/hooks/animation/useAnimateBar.mjs
function barPropsInterpolator(from, to) {
	const interpolateX = number_default(from.x, to.x);
	const interpolateY = number_default(from.y, to.y);
	const interpolateWidth = number_default(from.width, to.width);
	const interpolateHeight = number_default(from.height, to.height);
	return (t) => {
		return {
			x: interpolateX(t),
			y: interpolateY(t),
			width: interpolateWidth(t),
			height: interpolateHeight(t)
		};
	};
}
/**
* Animates a bar from the start of the axis (x-axis for vertical layout, y-axis for horizontal layout) to its
* final position.
*
* The props object also accepts a `ref` which will be merged with the ref returned from this hook. This means you can
* pass the ref returned by this hook to the `path` element and the `ref` provided as argument will also be called.
*/
function useAnimateBar(props) {
	const initialProps = {
		x: props.layout === "vertical" ? props.x : props.xOrigin,
		y: props.layout === "vertical" ? props.yOrigin : props.y,
		width: props.layout === "vertical" ? props.width : 0,
		height: props.layout === "vertical" ? 0 : props.height
	};
	return useAnimate({
		x: props.x,
		y: props.y,
		width: props.width,
		height: props.height
	}, {
		createInterpolator: barPropsInterpolator,
		applyProps(element, animatedProps) {
			element.setAttribute("x", animatedProps.x.toString());
			element.setAttribute("y", animatedProps.y.toString());
			element.setAttribute("width", animatedProps.width.toString());
			element.setAttribute("height", animatedProps.height.toString());
		},
		transformProps: (p) => p,
		initialProps,
		skip: props.skipAnimation,
		ref: props.ref
	});
}
//#endregion
//#region node_modules/@mui/x-charts/hooks/animation/useAnimateBarLabel.mjs
function barLabelPropsInterpolator(from, to) {
	const interpolateX = number_default(from.x, to.x);
	const interpolateY = number_default(from.y, to.y);
	const interpolateWidth = number_default(from.width, to.width);
	const interpolateHeight = number_default(from.height, to.height);
	return (t) => ({
		x: interpolateX(t),
		y: interpolateY(t),
		width: interpolateWidth(t),
		height: interpolateHeight(t)
	});
}
/**
* Animates a bar label from the start of the axis (x-axis for vertical layout, y-axis for horizontal layout) to the
* center of the bar it belongs to.
* The label is horizontally centered within the bar when the layout is vertical, and vertically centered for laid out
* horizontally.
*
* The props object also accepts a `ref` which will be merged with the ref returned from this hook. This means you can
* pass the ref returned by this hook to the `path` element and the `ref` provided as argument will also be called.
*/
function useAnimateBarLabel(props) {
	const { initialX, currentX, initialY, currentY } = props.placement === "outside" ? getOutsidePlacement(props) : getCenterPlacement(props);
	const initialProps = {
		x: initialX,
		y: initialY,
		width: props.width,
		height: props.height
	};
	return useAnimate({
		x: currentX,
		y: currentY,
		width: props.width,
		height: props.height
	}, {
		createInterpolator: barLabelPropsInterpolator,
		transformProps: (p) => p,
		applyProps(element, animatedProps) {
			element.setAttribute("x", animatedProps.x.toString());
			element.setAttribute("y", animatedProps.y.toString());
			element.setAttribute("width", animatedProps.width.toString());
			element.setAttribute("height", animatedProps.height.toString());
		},
		initialProps,
		skip: props.skipAnimation,
		ref: props.ref
	});
}
var LABEL_OFFSET = 4;
function getCenterPlacement(props) {
	return {
		initialX: props.layout === "vertical" ? props.x + props.width / 2 : props.xOrigin,
		initialY: props.layout === "vertical" ? props.yOrigin : props.y + props.height / 2,
		currentX: props.x + props.width / 2,
		currentY: props.y + props.height / 2
	};
}
function getOutsidePlacement(props) {
	let initialY = 0;
	let currentY = 0;
	let initialX = 0;
	let currentX = 0;
	if (props.layout === "vertical") {
		if (props.y < props.yOrigin) {
			initialY = props.yOrigin - LABEL_OFFSET;
			currentY = props.y - LABEL_OFFSET;
		} else {
			initialY = props.yOrigin + LABEL_OFFSET;
			currentY = props.y + props.height + LABEL_OFFSET;
		}
		return {
			initialX: props.x + props.width / 2,
			currentX: props.x + props.width / 2,
			initialY,
			currentY
		};
	}
	if (props.x < props.xOrigin) {
		initialX = props.xOrigin;
		currentX = props.x - LABEL_OFFSET;
	} else {
		initialX = props.xOrigin;
		currentX = props.x + props.width + LABEL_OFFSET;
	}
	return {
		initialX,
		currentX,
		initialY: props.y + props.height / 2,
		currentY: props.y + props.height / 2
	};
}
//#endregion
//#region node_modules/d3-shape/src/arc.js
function arcInnerRadius(d) {
	return d.innerRadius;
}
function arcOuterRadius(d) {
	return d.outerRadius;
}
function arcStartAngle(d) {
	return d.startAngle;
}
function arcEndAngle(d) {
	return d.endAngle;
}
function arcPadAngle(d) {
	return d && d.padAngle;
}
function intersect(x0, y0, x1, y1, x2, y2, x3, y3) {
	var x10 = x1 - x0, y10 = y1 - y0, x32 = x3 - x2, y32 = y3 - y2, t = y32 * x10 - x32 * y10;
	if (t * t < 1e-12) return;
	t = (x32 * (y0 - y2) - y32 * (x0 - x2)) / t;
	return [x0 + t * x10, y0 + t * y10];
}
function cornerTangents(x0, y0, x1, y1, r1, rc, cw) {
	var x01 = x0 - x1, y01 = y0 - y1, lo = (cw ? rc : -rc) / sqrt(x01 * x01 + y01 * y01), ox = lo * y01, oy = -lo * x01, x11 = x0 + ox, y11 = y0 + oy, x10 = x1 + ox, y10 = y1 + oy, x00 = (x11 + x10) / 2, y00 = (y11 + y10) / 2, dx = x10 - x11, dy = y10 - y11, d2 = dx * dx + dy * dy, r = r1 - rc, D = x11 * y10 - x10 * y11, d = (dy < 0 ? -1 : 1) * sqrt(max(0, r * r * d2 - D * D)), cx0 = (D * dy - dx * d) / d2, cy0 = (-D * dx - dy * d) / d2, cx1 = (D * dy + dx * d) / d2, cy1 = (-D * dx + dy * d) / d2, dx0 = cx0 - x00, dy0 = cy0 - y00, dx1 = cx1 - x00, dy1 = cy1 - y00;
	if (dx0 * dx0 + dy0 * dy0 > dx1 * dx1 + dy1 * dy1) cx0 = cx1, cy0 = cy1;
	return {
		cx: cx0,
		cy: cy0,
		x01: -ox,
		y01: -oy,
		x11: cx0 * (r1 / r - 1),
		y11: cy0 * (r1 / r - 1)
	};
}
function arc_default() {
	var innerRadius = arcInnerRadius, outerRadius = arcOuterRadius, cornerRadius = constant_default(0), padRadius = null, startAngle = arcStartAngle, endAngle = arcEndAngle, padAngle = arcPadAngle, context = null, path = withPath(arc);
	function arc() {
		var buffer, r, r0 = +innerRadius.apply(this, arguments), r1 = +outerRadius.apply(this, arguments), a0 = startAngle.apply(this, arguments) - halfPi, a1 = endAngle.apply(this, arguments) - halfPi, da = abs(a1 - a0), cw = a1 > a0;
		if (!context) context = buffer = path();
		if (r1 < r0) r = r1, r1 = r0, r0 = r;
		if (!(r1 > 1e-12)) context.moveTo(0, 0);
		else if (da > tau - 1e-12) {
			context.moveTo(r1 * cos(a0), r1 * sin(a0));
			context.arc(0, 0, r1, a0, a1, !cw);
			if (r0 > 1e-12) {
				context.moveTo(r0 * cos(a1), r0 * sin(a1));
				context.arc(0, 0, r0, a1, a0, cw);
			}
		} else {
			var a01 = a0, a11 = a1, a00 = a0, a10 = a1, da0 = da, da1 = da, ap = padAngle.apply(this, arguments) / 2, rp = ap > 1e-12 && (padRadius ? +padRadius.apply(this, arguments) : sqrt(r0 * r0 + r1 * r1)), rc = min(abs(r1 - r0) / 2, +cornerRadius.apply(this, arguments)), rc0 = rc, rc1 = rc, t0, t1;
			if (rp > 1e-12) {
				var p0 = asin(rp / r0 * sin(ap)), p1 = asin(rp / r1 * sin(ap));
				if ((da0 -= p0 * 2) > 1e-12) p0 *= cw ? 1 : -1, a00 += p0, a10 -= p0;
				else da0 = 0, a00 = a10 = (a0 + a1) / 2;
				if ((da1 -= p1 * 2) > 1e-12) p1 *= cw ? 1 : -1, a01 += p1, a11 -= p1;
				else da1 = 0, a01 = a11 = (a0 + a1) / 2;
			}
			var x01 = r1 * cos(a01), y01 = r1 * sin(a01), x10 = r0 * cos(a10), y10 = r0 * sin(a10);
			if (rc > 1e-12) {
				var x11 = r1 * cos(a11), y11 = r1 * sin(a11), x00 = r0 * cos(a00), y00 = r0 * sin(a00), oc;
				if (da < pi) if (oc = intersect(x01, y01, x00, y00, x11, y11, x10, y10)) {
					var ax = x01 - oc[0], ay = y01 - oc[1], bx = x11 - oc[0], by = y11 - oc[1], kc = 1 / sin(acos((ax * bx + ay * by) / (sqrt(ax * ax + ay * ay) * sqrt(bx * bx + by * by))) / 2), lc = sqrt(oc[0] * oc[0] + oc[1] * oc[1]);
					rc0 = min(rc, (r0 - lc) / (kc - 1));
					rc1 = min(rc, (r1 - lc) / (kc + 1));
				} else rc0 = rc1 = 0;
			}
			if (!(da1 > 1e-12)) context.moveTo(x01, y01);
			else if (rc1 > 1e-12) {
				t0 = cornerTangents(x00, y00, x01, y01, r1, rc1, cw);
				t1 = cornerTangents(x11, y11, x10, y10, r1, rc1, cw);
				context.moveTo(t0.cx + t0.x01, t0.cy + t0.y01);
				if (rc1 < rc) context.arc(t0.cx, t0.cy, rc1, atan2(t0.y01, t0.x01), atan2(t1.y01, t1.x01), !cw);
				else {
					context.arc(t0.cx, t0.cy, rc1, atan2(t0.y01, t0.x01), atan2(t0.y11, t0.x11), !cw);
					context.arc(0, 0, r1, atan2(t0.cy + t0.y11, t0.cx + t0.x11), atan2(t1.cy + t1.y11, t1.cx + t1.x11), !cw);
					context.arc(t1.cx, t1.cy, rc1, atan2(t1.y11, t1.x11), atan2(t1.y01, t1.x01), !cw);
				}
			} else context.moveTo(x01, y01), context.arc(0, 0, r1, a01, a11, !cw);
			if (!(r0 > 1e-12) || !(da0 > 1e-12)) context.lineTo(x10, y10);
			else if (rc0 > 1e-12) {
				t0 = cornerTangents(x10, y10, x11, y11, r0, -rc0, cw);
				t1 = cornerTangents(x01, y01, x00, y00, r0, -rc0, cw);
				context.lineTo(t0.cx + t0.x01, t0.cy + t0.y01);
				if (rc0 < rc) context.arc(t0.cx, t0.cy, rc0, atan2(t0.y01, t0.x01), atan2(t1.y01, t1.x01), !cw);
				else {
					context.arc(t0.cx, t0.cy, rc0, atan2(t0.y01, t0.x01), atan2(t0.y11, t0.x11), !cw);
					context.arc(0, 0, r0, atan2(t0.cy + t0.y11, t0.cx + t0.x11), atan2(t1.cy + t1.y11, t1.cx + t1.x11), cw);
					context.arc(t1.cx, t1.cy, rc0, atan2(t1.y11, t1.x11), atan2(t1.y01, t1.x01), !cw);
				}
			} else context.arc(0, 0, r0, a10, a00, cw);
		}
		context.closePath();
		if (buffer) return context = null, buffer + "" || null;
	}
	arc.centroid = function() {
		var r = (+innerRadius.apply(this, arguments) + +outerRadius.apply(this, arguments)) / 2, a = (+startAngle.apply(this, arguments) + +endAngle.apply(this, arguments)) / 2 - pi / 2;
		return [cos(a) * r, sin(a) * r];
	};
	arc.innerRadius = function(_) {
		return arguments.length ? (innerRadius = typeof _ === "function" ? _ : constant_default(+_), arc) : innerRadius;
	};
	arc.outerRadius = function(_) {
		return arguments.length ? (outerRadius = typeof _ === "function" ? _ : constant_default(+_), arc) : outerRadius;
	};
	arc.cornerRadius = function(_) {
		return arguments.length ? (cornerRadius = typeof _ === "function" ? _ : constant_default(+_), arc) : cornerRadius;
	};
	arc.padRadius = function(_) {
		return arguments.length ? (padRadius = _ == null ? null : typeof _ === "function" ? _ : constant_default(+_), arc) : padRadius;
	};
	arc.startAngle = function(_) {
		return arguments.length ? (startAngle = typeof _ === "function" ? _ : constant_default(+_), arc) : startAngle;
	};
	arc.endAngle = function(_) {
		return arguments.length ? (endAngle = typeof _ === "function" ? _ : constant_default(+_), arc) : endAngle;
	};
	arc.padAngle = function(_) {
		return arguments.length ? (padAngle = typeof _ === "function" ? _ : constant_default(+_), arc) : padAngle;
	};
	arc.context = function(_) {
		return arguments.length ? (context = _ == null ? null : _, arc) : context;
	};
	return arc;
}
//#endregion
//#region node_modules/@mui/x-charts/hooks/animation/useAnimatePieArc.mjs
function pieArcPropsInterpolator(from, to) {
	const interpolateStartAngle = number_default(from.startAngle, to.startAngle);
	const interpolateEndAngle = number_default(from.endAngle, to.endAngle);
	const interpolateInnerRadius = number_default(from.innerRadius, to.innerRadius);
	const interpolateOuterRadius = number_default(from.outerRadius, to.outerRadius);
	const interpolatePaddingAngle = number_default(from.paddingAngle, to.paddingAngle);
	const interpolateCornerRadius = number_default(from.cornerRadius, to.cornerRadius);
	return (t) => {
		return {
			startAngle: interpolateStartAngle(t),
			endAngle: interpolateEndAngle(t),
			innerRadius: interpolateInnerRadius(t),
			outerRadius: interpolateOuterRadius(t),
			paddingAngle: interpolatePaddingAngle(t),
			cornerRadius: interpolateCornerRadius(t)
		};
	};
}
/** Animates a slice of a pie chart by increasing the start and end angles from the middle angle to their final values.
* The props object also accepts a `ref` which will be merged with the ref returned from this hook. This means you can
* pass the ref returned by this hook to the `path` element and the `ref` provided as argument will also be called. */
function useAnimatePieArc(props) {
	const initialProps = {
		startAngle: (props.startAngle + props.endAngle) / 2,
		endAngle: (props.startAngle + props.endAngle) / 2,
		innerRadius: props.innerRadius,
		outerRadius: props.outerRadius,
		paddingAngle: props.paddingAngle,
		cornerRadius: props.cornerRadius
	};
	return useAnimate({
		startAngle: props.startAngle,
		endAngle: props.endAngle,
		innerRadius: props.innerRadius,
		outerRadius: props.outerRadius,
		paddingAngle: props.paddingAngle,
		cornerRadius: props.cornerRadius
	}, {
		createInterpolator: pieArcPropsInterpolator,
		transformProps: (p) => ({
			d: arc_default().cornerRadius(p.cornerRadius)({
				padAngle: p.paddingAngle,
				innerRadius: p.innerRadius,
				outerRadius: p.outerRadius,
				startAngle: p.startAngle,
				endAngle: p.endAngle
			}),
			visibility: p.startAngle === p.endAngle ? "hidden" : "visible"
		}),
		applyProps(element, p) {
			element.setAttribute("d", p.d);
			element.setAttribute("visibility", p.visibility);
		},
		initialProps,
		skip: props.skipAnimation,
		ref: props.ref
	});
}
//#endregion
//#region node_modules/@mui/x-charts/hooks/animation/useAnimatePieArcLabel.mjs
function pieArcLabelPropsInterpolator(from, to) {
	const interpolateStartAngle = number_default(from.startAngle, to.startAngle);
	const interpolateEndAngle = number_default(from.endAngle, to.endAngle);
	const interpolateArcLabelRadius = number_default(from.arcLabelRadius, to.arcLabelRadius);
	const interpolatePaddingAngle = number_default(from.paddingAngle, to.paddingAngle);
	const interpolateCornerRadius = number_default(from.cornerRadius, to.cornerRadius);
	return (t) => {
		return {
			startAngle: interpolateStartAngle(t),
			endAngle: interpolateEndAngle(t),
			arcLabelRadius: interpolateArcLabelRadius(t),
			paddingAngle: interpolatePaddingAngle(t),
			cornerRadius: interpolateCornerRadius(t)
		};
	};
}
/** Animates the label of pie slice from its middle point to the centroid of the slice.
* The props object also accepts a `ref` which will be merged with the ref returned from this hook. This means you can
* pass the ref returned by this hook to the `path` element and the `ref` provided as argument will also be called. */
function useAnimatePieArcLabel(props) {
	const initialProps = {
		startAngle: (props.startAngle + props.endAngle) / 2,
		endAngle: (props.startAngle + props.endAngle) / 2,
		arcLabelRadius: props.arcLabelRadius,
		paddingAngle: props.paddingAngle,
		cornerRadius: props.cornerRadius
	};
	return useAnimate({
		startAngle: props.startAngle,
		endAngle: props.endAngle,
		arcLabelRadius: props.arcLabelRadius,
		paddingAngle: props.paddingAngle,
		cornerRadius: props.cornerRadius
	}, {
		createInterpolator: pieArcLabelPropsInterpolator,
		transformProps: (animatedProps) => {
			const [x, y] = arc_default().cornerRadius(animatedProps.cornerRadius).centroid({
				padAngle: animatedProps.paddingAngle,
				startAngle: animatedProps.startAngle,
				endAngle: animatedProps.endAngle,
				innerRadius: animatedProps.arcLabelRadius,
				outerRadius: animatedProps.arcLabelRadius
			});
			return {
				x,
				y
			};
		},
		applyProps(element, { x, y }) {
			element.setAttribute("x", x.toString());
			element.setAttribute("y", y.toString());
		},
		initialProps,
		skip: props.skipAnimation,
		ref: props.ref
	});
}
//#endregion
//#region node_modules/@mui/x-charts/hooks/useBrush.mjs
/**
* Get the current brush state.
*
* - `start` is the starting point of the brush selection.
* - `current` is the current point of the brush selection.
*
* @returns `{ start, current }` - The brush state.
*/
function useBrush() {
	return useStore().use(selectorBrushState);
}
//#endregion
//#region node_modules/@mui/x-charts/hooks/useAxisCoordinates.mjs
function getXAxisCoordinates(drawingArea, computedAxis) {
	const { position, offset, height: axisHeight } = computedAxis;
	if (position === "none") return null;
	let top;
	if (position === "top") top = drawingArea.top - axisHeight - offset;
	else top = drawingArea.top + drawingArea.height + offset;
	const left = drawingArea.left;
	const bottom = top + axisHeight;
	const right = drawingArea.left + drawingArea.width;
	return {
		left,
		top,
		right,
		bottom
	};
}
/**
* Get the coordinates of the given X axis. The coordinates are relative to the SVG's origin.
* @param axisId The id of the X axis.
* @returns {AxisCoordinates | null} The coordinates of the X axis or null if the axis does not exist or has position: 'none'.
*/
function useXAxisCoordinates(axisId) {
	const { xAxis: xAxes } = useXAxes();
	const drawingArea = useDrawingArea();
	const axis = xAxes[axisId];
	const themedProps = useThemeProps({
		props: axis,
		name: "MuiChartsXAxis"
	});
	if (!axis) return null;
	return getXAxisCoordinates(drawingArea, _extends({}, defaultProps, themedProps));
}
function getYAxisCoordinates(drawingArea, computedAxis) {
	const { position, offset, width: axisWidth } = computedAxis;
	if (position === "none") return null;
	let left;
	if (position === "right") left = drawingArea.left + drawingArea.width + offset;
	else left = drawingArea.left - axisWidth - offset;
	const top = drawingArea.top;
	const bottom = drawingArea.top + drawingArea.height;
	const right = left + axisWidth;
	return {
		left,
		top,
		right,
		bottom
	};
}
/**
* Returns the coordinates of the given Y axis. The coordinates are relative to the SVG's origin.
* @param axisId The id of the Y axis.
* @returns {AxisCoordinates | null} The coordinates of the Y axis or null if the axis does not exist or has position: 'none'.
*/
function useYAxisCoordinates(axisId) {
	const { yAxis: yAxes } = useYAxes();
	const drawingArea = useDrawingArea();
	const axis = yAxes[axisId];
	const themedProps = useThemeProps({
		props: axis,
		name: "MuiChartsYAxis"
	});
	if (!axis) return null;
	return getYAxisCoordinates(drawingArea, _extends({}, defaultProps, themedProps));
}
//#endregion
//#region node_modules/@mui/x-charts/hooks/useAxisTicks.mjs
/**
* Returns the ticks for the given X axis. Ticks outside the drawing area are not included.
* The ticks returned from this hook are not grouped, i.e., they don't follow the `groups` prop of the axis.
* @param axisId The id of the X axis.
*/
function useXAxisTicks(axisId) {
	const { xAxis: xAxes } = useXAxes();
	const axis = xAxes[axisId];
	const defaultizedProps = _extends({}, defaultProps, useThemeProps({
		props: axis,
		name: "MuiChartsXAxis"
	}));
	return useTicks({
		scale: axis.scale,
		tickNumber: axis.tickNumber,
		valueFormatter: defaultizedProps.valueFormatter,
		tickInterval: defaultizedProps.tickInterval,
		tickPlacement: defaultizedProps.tickPlacement,
		tickLabelPlacement: defaultizedProps.tickLabelPlacement,
		tickSpacing: defaultizedProps.tickSpacing,
		direction: "x",
		ordinalTimeTicks: "ordinalTimeTicks" in defaultizedProps ? defaultizedProps.ordinalTimeTicks : void 0
	});
}
/**
* Returns the ticks for the given Y axis. Ticks outside the drawing area are not included.
* The ticks returned from this hook are not grouped, i.e., they don't follow the `groups` prop of the axis.
* @param axisId The id of the Y axis.
*/
function useYAxisTicks(axisId) {
	const { yAxis: yAxes } = useYAxes();
	const axis = yAxes[axisId];
	const defaultizedProps = _extends({}, defaultProps, useThemeProps({
		props: axis,
		name: "MuiChartsYAxis"
	}));
	return useTicks({
		scale: axis.scale,
		tickNumber: axis.tickNumber,
		valueFormatter: defaultizedProps.valueFormatter,
		tickInterval: defaultizedProps.tickInterval,
		tickPlacement: defaultizedProps.tickPlacement,
		tickLabelPlacement: defaultizedProps.tickLabelPlacement,
		tickSpacing: defaultizedProps.tickSpacing,
		direction: "y",
		ordinalTimeTicks: defaultizedProps.ordinalTimeTicks
	});
}
//#endregion
//#region node_modules/@mui/x-charts/hooks/usePolarGeometry.mjs
/**
* Provides access to the geometry of a polar chart.
*
* It can be used to position custom elements and convert between polar and Cartesian coordinates.
*
* @example
* ```tsx
* const geo = usePolarGeometry();
*
* const [x, y] = geo.point(100, Math.PI / 2);
*
* return <circle cx={geo.cx + x} cy={geo.cy + y} r={4} />
* ```
*
* @returns The polar geometry helpers, or `null` if the chart scales are not ready.
*/
function usePolarGeometry() {
	const { left, top, width, height } = useDrawingArea();
	const rotationAxis = useRotationAxis();
	const radiusAxis = useRadiusAxis();
	if (!rotationAxis || !radiusAxis) return null;
	const angleScale = rotationAxis.scale;
	const radiusScale = radiusAxis.scale;
	return {
		cx: left + width / 2,
		cy: top + height / 2,
		angleScale,
		radiusScale,
		point: (radius, angle) => [radius * Math.sin(angle), -radius * Math.cos(angle)],
		pointInverse: (x, y) => [Math.sqrt(x * x + y * y), Math.atan2(x, -y)]
	};
}
//#endregion
//#region node_modules/@mui/x-charts/internals/plugins/featurePlugins/useChartClosestPoint/useChartClosestPoint.selectors.mjs
var selectVoronoi = (state) => state.voronoi;
var selectorChartsIsVoronoiEnabled = createSelector(selectVoronoi, (voronoi) => voronoi?.isVoronoiEnabled);
//#endregion
//#region node_modules/@mui/x-charts/ChartsReferenceLine/chartsReferenceLineClasses.mjs
function getReferenceLineUtilityClass(slot) {
	return generateUtilityClass("MuiChartsReferenceLine", slot);
}
var referenceLineClasses = generateUtilityClasses("MuiChartsReferenceLine", [
	"root",
	"vertical",
	"horizontal",
	"line",
	"label"
]);
//#endregion
//#region node_modules/@mui/x-charts/ChartsReferenceLine/common.mjs
var ReferenceLineRoot = styled("g", {
	slot: "internal",
	shouldForwardProp: void 0
})(({ theme }) => ({
	[`& .${referenceLineClasses.line}`]: {
		fill: "none",
		stroke: (theme.vars || theme).palette.text.primary,
		shapeRendering: "crispEdges",
		strokeWidth: 1,
		pointerEvents: "none"
	},
	[`& .${referenceLineClasses.label}`]: _extends({
		fill: (theme.vars || theme).palette.text.primary,
		stroke: "none",
		pointerEvents: "none",
		fontSize: 12
	}, theme.typography.body1)
}));
//#endregion
//#region node_modules/@mui/x-charts/ChartsReferenceLine/ChartsXReferenceLine.mjs
var getTextParams$1 = ({ top, height, spacing, position, labelAlign = "middle" }) => {
	const defaultSpacingOtherAxis = labelAlign === "middle" ? 0 : 5;
	const spacingX = (typeof spacing === "object" ? spacing.x : spacing) ?? 5;
	const spacingY = (typeof spacing === "object" ? spacing.y : defaultSpacingOtherAxis) ?? defaultSpacingOtherAxis;
	switch (labelAlign) {
		case "start": return {
			x: position + spacingX,
			y: top + spacingY,
			style: {
				dominantBaseline: "hanging",
				textAnchor: "start"
			}
		};
		case "end": return {
			x: position + spacingX,
			y: top + height - spacingY,
			style: {
				dominantBaseline: "auto",
				textAnchor: "start"
			}
		};
		default: return {
			x: position + spacingX,
			y: top + height / 2 + spacingY,
			style: {
				dominantBaseline: "central",
				textAnchor: "start"
			}
		};
	}
};
function getXReferenceLineClasses(classes) {
	return composeClasses({
		root: ["root", "vertical"],
		line: ["line"],
		label: ["label"]
	}, getReferenceLineUtilityClass, classes);
}
function ChartsXReferenceLine(props) {
	const { x, label = "", spacing, classes: inClasses, labelAlign = "middle", lineStyle, labelStyle, axisId } = props;
	const { top, height, left, width } = useDrawingArea();
	const xPosition = useXScale(axisId)(x);
	if (xPosition === void 0) {
		warnOnce(`MUI X Charts: the value ${x} does not exist in the data of x axis with id ${axisId}.`, "error");
		return null;
	}
	if (xPosition < left || xPosition > left + width) return null;
	const d = `M ${xPosition} ${top} l 0 ${height}`;
	const classes = getXReferenceLineClasses(inClasses);
	const textParams = _extends({
		text: label,
		fontSize: 12
	}, getTextParams$1({
		top,
		height,
		spacing,
		position: xPosition,
		labelAlign
	}), { className: classes.label });
	return /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(ReferenceLineRoot, {
		className: classes.root,
		children: [/*#__PURE__*/ (0, import_jsx_runtime.jsx)("path", {
			d,
			className: classes.line,
			style: lineStyle
		}), /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ChartsText, _extends({}, textParams, { style: _extends({}, textParams.style, labelStyle) }))]
	});
}
ChartsXReferenceLine.propTypes = {
	/**
	* The id of the axis used for the reference value.
	* @default The `id` of the first defined axis.
	*/
	axisId: import_prop_types.default.oneOfType([import_prop_types.default.number, import_prop_types.default.string]),
	/**
	* Override or extend the styles applied to the component.
	*/
	classes: import_prop_types.default.object,
	/**
	* The label to display along the reference line.
	*/
	label: import_prop_types.default.string,
	/**
	* The alignment if the label is in the chart drawing area.
	* @default 'middle'
	*/
	labelAlign: import_prop_types.default.oneOf([
		"end",
		"middle",
		"start"
	]),
	/**
	* The style applied to the label.
	*/
	labelStyle: import_prop_types.default.object,
	/**
	* The style applied to the line.
	*/
	lineStyle: import_prop_types.default.object,
	/**
	* Additional space around the label in px.
	* Can be a number or an object `{ x, y }` to distinguish space with the reference line and space with axes.
	* @default 5
	*/
	spacing: import_prop_types.default.oneOfType([import_prop_types.default.number, import_prop_types.default.shape({
		x: import_prop_types.default.number,
		y: import_prop_types.default.number
	})]),
	/**
	* The x value associated with the reference line.
	* If defined the reference line will be vertical.
	*/
	x: import_prop_types.default.oneOfType([
		import_prop_types.default.instanceOf(Date),
		import_prop_types.default.number,
		import_prop_types.default.string
	]).isRequired
};
//#endregion
//#region node_modules/@mui/x-charts/ChartsReferenceLine/ChartsYReferenceLine.mjs
var getTextParams = ({ left, width, spacing, position, labelAlign = "middle" }) => {
	const defaultSpacingOtherAxis = labelAlign === "middle" ? 0 : 5;
	const spacingX = (typeof spacing === "object" ? spacing.x : defaultSpacingOtherAxis) ?? defaultSpacingOtherAxis;
	const spacingY = (typeof spacing === "object" ? spacing.y : spacing) ?? 5;
	switch (labelAlign) {
		case "start": return {
			y: position - spacingY,
			x: left + spacingX,
			style: {
				dominantBaseline: "auto",
				textAnchor: "start"
			}
		};
		case "end": return {
			y: position - spacingY,
			x: left + width - spacingX,
			style: {
				dominantBaseline: "auto",
				textAnchor: "end"
			}
		};
		default: return {
			y: position - spacingY,
			x: left + width / 2 + spacingX,
			style: {
				dominantBaseline: "auto",
				textAnchor: "middle"
			}
		};
	}
};
function getYReferenceLineClasses(classes) {
	return composeClasses({
		root: ["root", "horizontal"],
		line: ["line"],
		label: ["label"]
	}, getReferenceLineUtilityClass, classes);
}
function ChartsYReferenceLine(props) {
	const { y, label = "", spacing, classes: inClasses, labelAlign = "middle", lineStyle, labelStyle, axisId } = props;
	const { top, height, left, width } = useDrawingArea();
	const yPosition = useYScale(axisId)(y);
	if (yPosition === void 0) {
		warnOnce(`MUI X Charts: the value ${y} does not exist in the data of y axis with id ${axisId}.`, "error");
		return null;
	}
	if (yPosition < top || yPosition > top + height) return null;
	const d = `M ${left} ${yPosition} l ${width} 0`;
	const classes = getYReferenceLineClasses(inClasses);
	const textParams = _extends({
		text: label,
		fontSize: 12
	}, getTextParams({
		left,
		width,
		spacing,
		position: yPosition,
		labelAlign
	}), { className: classes.label });
	return /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(ReferenceLineRoot, {
		className: classes.root,
		children: [/*#__PURE__*/ (0, import_jsx_runtime.jsx)("path", {
			d,
			className: classes.line,
			style: lineStyle
		}), /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ChartsText, _extends({}, textParams, { style: _extends({}, textParams.style, labelStyle) }))]
	});
}
ChartsYReferenceLine.propTypes = {
	/**
	* The id of the axis used for the reference value.
	* @default The `id` of the first defined axis.
	*/
	axisId: import_prop_types.default.oneOfType([import_prop_types.default.number, import_prop_types.default.string]),
	/**
	* Override or extend the styles applied to the component.
	*/
	classes: import_prop_types.default.object,
	/**
	* The label to display along the reference line.
	*/
	label: import_prop_types.default.string,
	/**
	* The alignment if the label is in the chart drawing area.
	* @default 'middle'
	*/
	labelAlign: import_prop_types.default.oneOf([
		"end",
		"middle",
		"start"
	]),
	/**
	* The style applied to the label.
	*/
	labelStyle: import_prop_types.default.object,
	/**
	* The style applied to the line.
	*/
	lineStyle: import_prop_types.default.object,
	/**
	* Additional space around the label in px.
	* Can be a number or an object `{ x, y }` to distinguish space with the reference line and space with axes.
	* @default 5
	*/
	spacing: import_prop_types.default.oneOfType([import_prop_types.default.number, import_prop_types.default.shape({
		x: import_prop_types.default.number,
		y: import_prop_types.default.number
	})]),
	/**
	* The y value associated with the reference line.
	* If defined the reference line will be horizontal.
	*/
	y: import_prop_types.default.oneOfType([
		import_prop_types.default.instanceOf(Date),
		import_prop_types.default.number,
		import_prop_types.default.string
	]).isRequired
};
//#endregion
//#region node_modules/@mui/x-charts/ChartsReferenceLine/ChartsReferenceLine.mjs
function ChartsReferenceLine(props) {
	const { x, y } = props;
	if (x !== void 0 && y !== void 0) throw new Error("MUI X Charts: The ChartsReferenceLine cannot have both `x` and `y` props set. A reference line can only be vertical (x) or horizontal (y), not both. Remove either the `x` or `y` prop.");
	if (x === void 0 && y === void 0) throw new Error("MUI X Charts: The ChartsReferenceLine requires either an `x` or `y` prop. Without a value, the reference line cannot be positioned on the chart. Provide either an `x` value for a vertical line or a `y` value for a horizontal line.");
	if (x !== void 0) return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ChartsXReferenceLine, _extends({}, props));
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ChartsYReferenceLine, _extends({}, props));
}
ChartsReferenceLine.propTypes = {
	/**
	* The id of the axis used for the reference value.
	* @default The `id` of the first defined axis.
	*/
	axisId: import_prop_types.default.oneOfType([import_prop_types.default.number, import_prop_types.default.string]),
	/**
	* Override or extend the styles applied to the component.
	*/
	classes: import_prop_types.default.object,
	/**
	* The label to display along the reference line.
	*/
	label: import_prop_types.default.string,
	/**
	* The alignment if the label is in the chart drawing area.
	* @default 'middle'
	*/
	labelAlign: import_prop_types.default.oneOf([
		"end",
		"middle",
		"start"
	]),
	/**
	* The style applied to the label.
	*/
	labelStyle: import_prop_types.default.object,
	/**
	* The style applied to the line.
	*/
	lineStyle: import_prop_types.default.object,
	/**
	* Additional space around the label in px.
	* Can be a number or an object `{ x, y }` to distinguish space with the reference line and space with axes.
	* @default { x: 0, y: 5 } on a horizontal line and { x: 5, y: 0 } on a vertical line.
	*/
	spacing: import_prop_types.default.oneOfType([import_prop_types.default.number, import_prop_types.default.shape({
		x: import_prop_types.default.number,
		y: import_prop_types.default.number
	})]),
	/**
	* The x value associated with the reference line.
	* If defined the reference line will be vertical.
	*/
	x: import_prop_types.default.oneOfType([
		import_prop_types.default.instanceOf(Date),
		import_prop_types.default.number,
		import_prop_types.default.string
	]),
	/**
	* The y value associated with the reference line.
	* If defined the reference line will be horizontal.
	*/
	y: import_prop_types.default.oneOfType([
		import_prop_types.default.instanceOf(Date),
		import_prop_types.default.number,
		import_prop_types.default.string
	])
};
//#endregion
//#region node_modules/@mui/x-charts/ChartsAxisHighlightValue/useAxisHighlightValue.mjs
function getAxisValueFormatter(axis, valueFormatter) {
	if (valueFormatter) return valueFormatter;
	if (axis.valueFormatter) return (v) => axis.valueFormatter(v, {
		location: "tooltip",
		scale: axis.scale
	});
	return (v) => `${v}`;
}
var EMPTY_ARRAY = [];
function useAxisHighlightValue(params) {
	const { axisDirection, axisId, labelPosition = "end", value, valueFormatter } = params;
	const { top, left, width, height, right, bottom } = useDrawingArea();
	const store = useStore();
	const highlightSelector = axisDirection === "x" ? selectorChartsHighlightXAxisValue : selectorChartsHighlightYAxisValue;
	const axisSelector = axisDirection === "x" ? selectorChartXAxis : selectorChartYAxis;
	const axisHighlightValues = store.use(highlightSelector);
	const axes = store.use(axisSelector);
	if (labelPosition === "none") return EMPTY_ARRAY;
	let items;
	if (value !== void 0 && value !== null) items = [{
		axisId: axisId ?? axes.axisIds[0],
		value
	}];
	else items = axisHighlightValues.filter((item) => (axisId === void 0 || item.axisId === axisId) && item.value !== void 0).map((item) => ({
		axisId: item.axisId,
		value: item.value
	}));
	const result = [];
	for (const { axisId: itemAxisId, value: itemValue } of items) {
		const axis = axes.axis[itemAxisId];
		if (!axis) continue;
		const scale = axis.scale;
		const position = getValueToPositionMapper(scale)(itemValue);
		if (!Number.isFinite(position)) continue;
		const formattedValue = getAxisValueFormatter(axis, valueFormatter)(itemValue);
		if (axisDirection === "x") {
			if (labelPosition === "start" || labelPosition === "both") result.push({
				key: `${itemAxisId}-${String(itemValue)}-start`,
				x: position,
				y: top,
				formattedValue,
				position: "top",
				minCoord: left,
				maxCoord: left + width,
				space: top
			});
			if (labelPosition === "end" || labelPosition === "both") result.push({
				key: `${itemAxisId}-${String(itemValue)}-end`,
				x: position,
				y: top + height,
				formattedValue,
				position: "bottom",
				minCoord: left,
				maxCoord: left + width,
				space: bottom
			});
		} else {
			if (labelPosition === "start" || labelPosition === "both") result.push({
				key: `${itemAxisId}-${String(itemValue)}-start`,
				x: left,
				y: position,
				formattedValue,
				position: "left",
				minCoord: top,
				maxCoord: top + height,
				space: left
			});
			if (labelPosition === "end" || labelPosition === "both") result.push({
				key: `${itemAxisId}-${String(itemValue)}-end`,
				x: left + width,
				y: position,
				formattedValue,
				position: "right",
				minCoord: top,
				maxCoord: top + height,
				space: right
			});
		}
	}
	return result;
}
//#endregion
//#region node_modules/@mui/x-charts/ChartsAxisHighlightValue/chartsAxisHighlightValueClasses.mjs
function getChartsAxisHighlightValueUtilityClass(slot) {
	return generateUtilityClass("MuiChartsAxisHighlightValue", slot);
}
var useUtilityClasses$12 = (ownerState) => {
	const { position } = ownerState;
	return composeClasses({ root: ["root", position] }, getChartsAxisHighlightValueUtilityClass);
};
generateUtilityClasses("MuiChartsAxisHighlightValue", [
	"root",
	"top",
	"bottom",
	"left",
	"right"
]);
//#endregion
//#region node_modules/@mui/x-charts/ChartsAxisHighlightValue/ChartsAxisHighlightValueItem.mjs
var ChartsAxisHighlightValueText = styled("div", {
	name: "MuiChartsAxisHighlightValue",
	slot: "Root",
	shouldForwardProp: (prop) => shouldForwardProp(prop) && prop !== "position"
})(({ theme }) => _extends({}, theme.typography.caption, {
	padding: theme.spacing(.5, 1),
	border: `solid ${theme.palette.divider} 1px`,
	backgroundColor: theme.palette.background.paper,
	"--clamped-offset": "calc(-1*min(var(--min),max(calc(100% - var(--max)),50%)))",
	variants: [
		{
			props: { position: "top" },
			style: { translate: "var(--clamped-offset) -100%" }
		},
		{
			props: { position: "bottom" },
			style: { translate: "var(--clamped-offset) 0" }
		},
		{
			props: { position: "left" },
			style: { translate: "-100% var(--clamped-offset)" }
		},
		{
			props: { position: "right" },
			style: { translate: "0 var(--clamped-offset)" }
		}
	]
}));
function ChartsAxisHighlightValueItem(props) {
	const { x, y, position, formattedValue, minCoord, maxCoord, space, sx } = props;
	const classes = useUtilityClasses$12({ position });
	const isXAxis = position === "top" || position === "bottom";
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ChartsAxisHighlightValueText, {
		className: classes.root,
		position,
		style: {
			position: "absolute",
			top: y,
			left: x,
			"--min": `${isXAxis ? x - minCoord : y - minCoord}px`,
			"--max": `${isXAxis ? maxCoord - x : maxCoord - y}px`,
			"--space": `${space}px`
		},
		sx,
		children: formattedValue
	});
}
//#endregion
//#region node_modules/@mui/x-charts/ChartsAxisHighlightValue/ChartsAxisHighlightValue.mjs
var import_react_dom = /* @__PURE__ */ __toESM(require_react_dom(), 1);
/**
* A component that displays the axis value at the edge of the drawing area,
* aligned with the current axis highlight position.
*
* Renders as a portal into the ChartsLayerContainer, appearing as the last child.
*
* Demos:
*
* - [Custom components](https://mui.com/x/react-charts/components/)
*
* API:
*
* - [ChartsAxisHighlightValue API](https://mui.com/x/api/charts/charts-axis-highlight-value/)
*/
function ChartsAxisHighlightValue(props) {
	const { axisDirection, axisId, labelPosition, value, valueFormatter, sx } = props;
	const chartsLayerContainerRef = useChartsLayerContainerRef();
	const items = useAxisHighlightValue({
		axisDirection,
		axisId,
		labelPosition,
		value,
		valueFormatter
	});
	if (items.length === 0) return null;
	const content = items.map((itemProps) => /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ChartsAxisHighlightValueItem, _extends({}, itemProps, { sx })));
	if (!chartsLayerContainerRef.current) return content;
	return /*#__PURE__*/ import_react_dom.createPortal(content, chartsLayerContainerRef.current);
}
ChartsAxisHighlightValue.propTypes = {
	/**
	* The axis direction.
	*/
	axisDirection: import_prop_types.default.oneOf(["x", "y"]).isRequired,
	/**
	* The id of the axis.
	* If not provided, defaults to the first axis.
	*/
	axisId: import_prop_types.default.oneOfType([import_prop_types.default.number, import_prop_types.default.string]),
	/**
	* The position of the label relative to the drawing area.
	* - `'start'`: at the beginning of the drawing area (top for x-axis, left for y-axis).
	* - `'end'`: at the end of the drawing area (bottom for x-axis, right for y-axis).
	* - `'both'`: at both positions.
	* - `'none'`: no label is displayed.
	* @default 'end'
	*/
	labelPosition: import_prop_types.default.oneOf([
		"both",
		"end",
		"none",
		"start"
	]),
	/**
	* The value to display.
	* If not defined, tracks the axis highlight value from user interaction.
	*/
	value: import_prop_types.default.oneOfType([
		import_prop_types.default.instanceOf(Date),
		import_prop_types.default.number,
		import_prop_types.default.string
	]),
	/**
	* A function to format the displayed value.
	* If not provided, uses the axis `valueFormatter`, or falls back to a default formatter.
	* @param {number | Date | string} value The value to format.
	* @returns {string} The formatted string.
	*/
	valueFormatter: import_prop_types.default.func
};
//#endregion
//#region node_modules/@mui/x-charts/ChartsLabel/labelGradientClasses.mjs
function getLabelGradientUtilityClass(slot) {
	return generateUtilityClass("MuiChartsLabelGradient", slot);
}
var labelGradientClasses = generateUtilityClasses("MuiChartsLabelGradient", [
	"root",
	"vertical",
	"horizontal",
	"mask",
	"fill"
]);
var useUtilityClasses$11 = (props) => {
	const { direction } = props;
	return composeClasses({
		root: ["root", direction],
		mask: ["mask"],
		fill: ["fill"]
	}, getLabelGradientUtilityClass, props.classes);
};
//#endregion
//#region node_modules/@mui/x-charts/ChartsLegend/useAxis.mjs
/**
* Helper to select an axis definition according to its direction and id.
*/
function useAxis({ axisDirection, axisId }) {
	const { xAxis, xAxisIds } = useXAxes();
	const { yAxis, yAxisIds } = useYAxes();
	const { zAxis, zAxisIds } = useZAxes();
	switch (axisDirection) {
		case "x": return xAxis[typeof axisId === "string" ? axisId : xAxisIds[axisId ?? 0]];
		case "y": return yAxis[typeof axisId === "string" ? axisId : yAxisIds[axisId ?? 0]];
		default: return zAxis[typeof axisId === "string" ? axisId : zAxisIds[axisId ?? 0]];
	}
}
//#endregion
//#region node_modules/@mui/x-charts/ChartsLabel/ChartsLabelGradient.mjs
var _excluded$37 = [
	"gradientId",
	"direction",
	"classes",
	"className",
	"rotate",
	"reverse",
	"thickness"
];
var getRotation = (direction, reverse, rotate, isRtl) => {
	const angle = (direction === "vertical" ? -90 : 0) + (rotate ? 90 : 0) + (reverse ? 180 : 0);
	if (isRtl && direction !== "vertical") return angle + 180;
	return angle;
};
var Root = styled("div", {
	name: "MuiChartsLabelGradient",
	slot: "Root"
})(({ ownerState }) => {
	const rotation = getRotation(ownerState.direction, ownerState.reverse, ownerState.rotate, ownerState.isRtl);
	return {
		display: "flex",
		alignItems: "center",
		justifyContent: "center",
		[`.${labelGradientClasses.mask}`]: {
			borderRadius: 2,
			overflow: "hidden"
		},
		[`&.${labelGradientClasses.horizontal}`]: {
			width: "100%",
			[`.${labelGradientClasses.mask}`]: {
				height: ownerState.thickness,
				width: "100%"
			}
		},
		[`&.${labelGradientClasses.vertical}`]: {
			height: "100%",
			[`.${labelGradientClasses.mask}`]: {
				width: ownerState.thickness,
				height: "100%",
				"> svg": { height: "100%" }
			}
		},
		svg: {
			transform: `rotate(${rotation}deg)`,
			display: "block"
		}
	};
});
/**
* Generates the label Gradient for the tooltip and legend.
* @ignore - internal component.
*/
var ChartsLabelGradient = consumeThemeProps("MuiChartsLabelGradient", {
	defaultProps: {
		direction: "horizontal",
		thickness: 12
	},
	classesResolver: useUtilityClasses$11
}, function ChartsLabelGradient(props, ref) {
	const { gradientId, classes, className } = props, other = _objectWithoutPropertiesLoose(props, _excluded$37);
	const isRtl = useRtl();
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(Root, _extends({
		className: clsx(classes?.root, className),
		ownerState: _extends({}, props, { isRtl }),
		"aria-hidden": "true",
		ref
	}, other, { children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)("div", {
		className: classes?.mask,
		children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)("svg", {
			viewBox: "0 0 24 24",
			children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)("rect", {
				className: classes?.fill,
				width: "24",
				height: "24",
				fill: `url(#${gradientId})`
			})
		})
	}) }));
});
ChartsLabelGradient.propTypes = {
	/**
	* Override or extend the styles applied to the component.
	*/
	classes: import_prop_types.default.object,
	/**
	* The direction of the gradient.
	* @default 'horizontal'
	*/
	direction: import_prop_types.default.oneOf(["vertical", "horizontal"]),
	/**
	* A unique identifier for the gradient.
	* The `gradientId` will be used as `fill="url(#gradientId)"`.
	*/
	gradientId: import_prop_types.default.string.isRequired,
	/**
	* If `true`, the gradient will be reversed.
	*/
	reverse: import_prop_types.default.bool,
	/**
	* If provided, the gradient will be rotated by 90deg.
	* Useful for linear gradients that are not in the correct orientation.
	*/
	rotate: import_prop_types.default.bool,
	/**
	* The thickness of the gradient
	* @default 12
	*/
	thickness: import_prop_types.default.number
};
//#endregion
//#region node_modules/@mui/x-charts/ChartsLegend/continuousColorLegendClasses.mjs
function getLegendUtilityClass$1(slot) {
	return generateUtilityClass("MuiContinuousColorLegend", slot);
}
var useUtilityClasses$10 = (props) => {
	const { classes, direction, labelPosition } = props;
	return composeClasses({
		root: [
			"root",
			direction,
			labelPosition
		],
		minLabel: ["minLabel"],
		maxLabel: ["maxLabel"],
		gradient: ["gradient"],
		mark: ["mark"],
		label: ["label"]
	}, getLegendUtilityClass$1, classes);
};
var continuousColorLegendClasses = generateUtilityClasses("MuiContinuousColorLegend", [
	"root",
	"minLabel",
	"maxLabel",
	"gradient",
	"vertical",
	"horizontal",
	"start",
	"end",
	"extremes",
	"label"
]);
//#endregion
//#region node_modules/@mui/x-charts/ChartsLegend/ContinuousColorLegend.mjs
var _excluded$36 = [
	"minLabel",
	"maxLabel",
	"direction",
	"axisDirection",
	"axisId",
	"rotateGradient",
	"reverse",
	"classes",
	"className",
	"gradientId",
	"labelPosition",
	"thickness"
];
var templateAreas = (reverse) => {
	const startLabel = reverse ? "max-label" : "min-label";
	const endLabel = reverse ? "min-label" : "max-label";
	return {
		row: {
			start: `
    '${startLabel} . ${endLabel}'
    'gradient gradient gradient'
  `,
			end: `
      'gradient gradient gradient'
      '${startLabel} . ${endLabel}'
    `,
			extremes: `
      '${startLabel} gradient ${endLabel}'
    `
		},
		column: {
			start: `
      '${endLabel} gradient'
      '. gradient'
      '${startLabel} gradient'
    `,
			end: `
      'gradient ${endLabel}'
      'gradient .'
      'gradient ${startLabel}'
    `,
			extremes: `
      '${endLabel}'
      'gradient'
      '${startLabel}'
    `
		}
	};
};
var RootElement$1 = styled("ul", {
	name: "MuiContinuousColorLegend",
	slot: "Root"
})(({ theme, ownerState }) => _extends({}, theme.typography.caption, {
	color: (theme.vars || theme).palette.text.primary,
	lineHeight: "100%",
	display: "grid",
	flexShrink: 0,
	gap: theme.spacing(.5),
	listStyleType: "none",
	paddingInlineStart: 0,
	marginBlock: theme.spacing(1),
	marginInline: theme.spacing(1),
	gridArea: "legend",
	[`&.${continuousColorLegendClasses.horizontal}`]: {
		gridTemplateRows: "min-content min-content",
		gridTemplateColumns: "min-content auto min-content",
		[`&.${continuousColorLegendClasses.start}`]: { gridTemplateAreas: templateAreas(ownerState.reverse).row.start },
		[`&.${continuousColorLegendClasses.end}`]: { gridTemplateAreas: templateAreas(ownerState.reverse).row.end },
		[`&.${continuousColorLegendClasses.extremes}`]: {
			gridTemplateAreas: templateAreas(ownerState.reverse).row.extremes,
			gridTemplateRows: "min-content",
			alignItems: "center"
		}
	},
	[`&.${continuousColorLegendClasses.vertical}`]: {
		gridTemplateRows: "min-content auto min-content",
		gridTemplateColumns: "min-content min-content",
		[`&.${continuousColorLegendClasses.start}`]: {
			gridTemplateAreas: templateAreas(ownerState.reverse).column.start,
			[`.${continuousColorLegendClasses.maxLabel}, .${continuousColorLegendClasses.minLabel}`]: { justifySelf: "end" }
		},
		[`&.${continuousColorLegendClasses.end}`]: {
			gridTemplateAreas: templateAreas(ownerState.reverse).column.end,
			[`.${continuousColorLegendClasses.maxLabel}, .${continuousColorLegendClasses.minLabel}`]: { justifySelf: "start" }
		},
		[`&.${continuousColorLegendClasses.extremes}`]: {
			gridTemplateAreas: templateAreas(ownerState.reverse).column.extremes,
			gridTemplateColumns: "min-content",
			[`.${continuousColorLegendClasses.maxLabel}, .${continuousColorLegendClasses.minLabel}`]: { justifySelf: "center" }
		}
	},
	[`.${continuousColorLegendClasses.gradient}`]: { gridArea: "gradient" },
	[`.${continuousColorLegendClasses.maxLabel}`]: { gridArea: "max-label" },
	[`.${continuousColorLegendClasses.minLabel}`]: { gridArea: "min-label" }
}));
var getText = (label, value, formattedValue) => {
	if (typeof label === "string") return label;
	return label?.({
		value,
		formattedValue
	}) ?? formattedValue;
};
var isZAxis = (axis) => axis.scale === void 0;
var ContinuousColorLegend = consumeThemeProps("MuiContinuousColorLegend", {
	defaultProps: {
		direction: "horizontal",
		labelPosition: "end",
		axisDirection: "z"
	},
	classesResolver: useUtilityClasses$10
}, function ContinuousColorLegend(props, ref) {
	const { minLabel, maxLabel, direction, axisDirection, axisId, rotateGradient, reverse, classes, className, gradientId, thickness } = props, other = _objectWithoutPropertiesLoose(props, _excluded$36);
	const generateGradientId = useChartGradientIdObjectBoundBuilder();
	const axisItem = useAxis({
		axisDirection,
		axisId
	});
	const colorMap = axisItem?.colorMap;
	if (!colorMap || !colorMap.type || colorMap.type !== "continuous") return null;
	const minValue = colorMap.min ?? 0;
	const maxValue = colorMap.max ?? 100;
	const valueFormatter = isZAxis(axisItem) ? void 0 : axisItem.valueFormatter;
	const formattedMin = valueFormatter ? valueFormatter(minValue, { location: "legend" }) : minValue.toLocaleString();
	const formattedMax = valueFormatter ? valueFormatter(maxValue, { location: "legend" }) : maxValue.toLocaleString();
	const minText = getText(minLabel, minValue, formattedMin);
	const maxText = getText(maxLabel, maxValue, formattedMax);
	const minComponent = /*#__PURE__*/ (0, import_jsx_runtime.jsx)("li", {
		className: classes?.minLabel,
		children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ChartsLabel, {
			className: classes?.label,
			children: minText
		})
	});
	const maxComponent = /*#__PURE__*/ (0, import_jsx_runtime.jsx)("li", {
		className: classes?.maxLabel,
		children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ChartsLabel, {
			className: classes?.label,
			children: maxText
		})
	});
	return /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(RootElement$1, _extends({
		className: clsx(classes?.root, className),
		ref
	}, other, {
		ownerState: props,
		children: [
			reverse ? maxComponent : minComponent,
			/*#__PURE__*/ (0, import_jsx_runtime.jsx)("li", {
				className: classes?.gradient,
				children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ChartsLabelGradient, {
					direction,
					rotate: rotateGradient,
					reverse,
					thickness,
					gradientId: gradientId ?? generateGradientId(axisItem.id)
				})
			}),
			reverse ? minComponent : maxComponent
		]
	}));
});
ContinuousColorLegend.propTypes = {
	/**
	* The axis direction containing the color configuration to represent.
	* @default 'z'
	*/
	axisDirection: import_prop_types.default.oneOf([
		"x",
		"y",
		"z"
	]),
	/**
	* The id of the axis item with the color configuration to represent.
	* @default The first axis item.
	*/
	axisId: import_prop_types.default.oneOfType([import_prop_types.default.number, import_prop_types.default.string]),
	/**
	* Override or extend the styles applied to the component.
	*/
	classes: import_prop_types.default.object,
	className: import_prop_types.default.string,
	/**
	* The direction of the legend layout.
	* @default 'horizontal'
	*/
	direction: import_prop_types.default.oneOf(["horizontal", "vertical"]),
	/**
	* The id for the gradient to use.
	* If not provided, it will use the generated gradient from the axis configuration.
	* The `gradientId` will be used as `fill="url(#gradientId)"`.
	* @default auto-generated id
	*/
	gradientId: import_prop_types.default.string,
	/**
	* Where to position the labels relative to the gradient.
	* @default 'end'
	*/
	labelPosition: import_prop_types.default.oneOf([
		"start",
		"end",
		"extremes"
	]),
	/**
	* The label to display at the maximum side of the gradient.
	* Can either be a string, or a function.
	* If not defined, the formatted maximal value is display.
	* @default formattedValue
	*/
	maxLabel: import_prop_types.default.oneOfType([import_prop_types.default.func, import_prop_types.default.string]),
	/**
	* The label to display at the minimum side of the gradient.
	* Can either be a string, or a function.
	* @default formattedValue
	*/
	minLabel: import_prop_types.default.oneOfType([import_prop_types.default.func, import_prop_types.default.string]),
	/**
	* If `true`, the gradient and labels will be reversed.
	* @default false
	*/
	reverse: import_prop_types.default.bool,
	/**
	* If provided, the gradient will be rotated by 90deg.
	* Useful for linear gradients that are not in the correct orientation.
	*/
	rotateGradient: import_prop_types.default.bool,
	/**
	* The thickness of the gradient
	* @default 12
	*/
	thickness: import_prop_types.default.number,
	sx: import_prop_types.default.oneOfType([
		import_prop_types.default.arrayOf(import_prop_types.default.oneOfType([
			import_prop_types.default.func,
			import_prop_types.default.object,
			import_prop_types.default.bool
		])),
		import_prop_types.default.func,
		import_prop_types.default.object
	])
};
//#endregion
//#region node_modules/@mui/x-charts/ChartsLegend/piecewiseColorLegendClasses.mjs
var CORRECT_PREFIX = "MuiPiecewiseColorLegend";
var LEGACY_PREFIX = "MuiPiecewiseColorLegendClasses";
function getLegendUtilityClass(slot) {
	return `${generateUtilityClass(CORRECT_PREFIX, slot)} ${generateUtilityClass(LEGACY_PREFIX, slot)}`;
}
var useUtilityClasses$9 = (props) => {
	const { classes, direction, labelPosition } = props;
	return composeClasses({
		root: [
			"root",
			direction,
			labelPosition?.replaceAll(/-(\w)/g, (match) => match[1].toUpperCase())
		],
		minLabel: ["minLabel"],
		maxLabel: ["maxLabel"],
		item: ["item"],
		mark: ["mark"],
		label: ["label"]
	}, getLegendUtilityClass, classes);
};
var piecewiseColorLegendClasses = generateUtilityClasses(CORRECT_PREFIX, [
	"root",
	"minLabel",
	"maxLabel",
	"item",
	"vertical",
	"horizontal",
	"start",
	"end",
	"extremes",
	"inlineStart",
	"inlineEnd",
	"mark",
	"label"
]);
//#endregion
//#region node_modules/@mui/x-charts/ChartsLegend/piecewiseColorDefaultLabelFormatter.mjs
function piecewiseColorDefaultLabelFormatter(params) {
	if (params.min === null) return `<${params.formattedMax}`;
	if (params.max === null) return `>${params.formattedMin}`;
	return `${params.formattedMin}-${params.formattedMax}`;
}
//#endregion
//#region node_modules/@mui/x-charts/ChartsLegend/PiecewiseColorLegend.mjs
var _excluded$35 = [
	"direction",
	"classes",
	"className",
	"markType",
	"labelPosition",
	"axisDirection",
	"axisId",
	"labelFormatter",
	"onItemClick"
];
var RootElement = styled("ul", {
	name: "MuiPiecewiseColorLegend",
	slot: "Root"
})(({ theme, ownerState }) => {
	const classes = piecewiseColorLegendClasses;
	return _extends({}, theme.typography.caption, {
		color: (theme.vars || theme).palette.text.primary,
		lineHeight: "100%",
		display: "flex",
		flexDirection: ownerState.direction === "vertical" ? "column" : "row",
		flexShrink: 0,
		gap: theme.spacing(.5),
		listStyleType: "none",
		paddingInlineStart: 0,
		marginBlock: theme.spacing(1),
		marginInline: theme.spacing(1),
		width: "fit-content",
		gridArea: "legend",
		[`button.${classes.item}`]: {
			background: "none",
			border: "none",
			padding: 0,
			cursor: ownerState.onItemClick ? "pointer" : "unset",
			fontFamily: "inherit",
			fontWeight: "inherit",
			fontSize: "inherit",
			letterSpacing: "inherit",
			color: "inherit"
		},
		[`.${classes.item}`]: {
			display: "flex",
			gap: theme.spacing(.5)
		},
		[`li :not(.${classes.minLabel}, .${classes.maxLabel}) .${classes?.mark}`]: { alignSelf: "center" },
		[`&.${classes.start}`]: { alignItems: "end" },
		[`&.${classes.end}`]: { alignItems: "start" },
		[`&.${classes.horizontal}`]: {
			alignItems: "center",
			[`.${classes.item}`]: { flexDirection: "column" },
			[`&.${classes.inlineStart}, &.${classes.inlineEnd}`]: {
				gap: theme.spacing(1.5),
				flexWrap: "wrap",
				[`.${classes.item}`]: { flexDirection: "row" }
			},
			[`&.${classes.start}`]: { alignItems: "end" },
			[`&.${classes.end}`]: { alignItems: "start" },
			[`.${classes.minLabel}`]: { alignItems: "end" },
			[`&.${classes.extremes}`]: { [`.${classes.minLabel}, .${classes.maxLabel}`]: {
				alignItems: "center",
				display: "flex",
				flexDirection: "row"
			} }
		},
		[`&.${classes.vertical}`]: {
			[`.${classes.item}`]: {
				flexDirection: "row",
				alignItems: "center"
			},
			[`&.${classes.start}, &.${classes.inlineStart}`]: { alignItems: "end" },
			[`&.${classes.end}, &.${classes.inlineEnd}`]: { alignItems: "start" },
			[`&.${classes.extremes}`]: {
				alignItems: "center",
				[`.${classes.minLabel}, .${classes.maxLabel}`]: {
					alignItems: "center",
					display: "flex",
					flexDirection: "column"
				}
			}
		}
	});
});
var PiecewiseColorLegend = consumeThemeProps("MuiPiecewiseColorLegend", {
	defaultProps: {
		direction: "horizontal",
		labelPosition: "extremes",
		labelFormatter: piecewiseColorDefaultLabelFormatter
	},
	classesResolver: useUtilityClasses$9
}, function PiecewiseColorLegend(props, ref) {
	const { direction, classes, className, markType, labelPosition, axisDirection, axisId, labelFormatter, onItemClick } = props, other = _objectWithoutPropertiesLoose(props, _excluded$35);
	const isReverse = direction === "vertical";
	const axisItem = useAxis({
		axisDirection,
		axisId
	});
	const colorMap = axisItem?.colorMap;
	if (!colorMap || !colorMap.type || colorMap.type !== "piecewise") return null;
	const valueFormatter = (v) => axisItem.valueFormatter?.(v, { location: "legend" }) ?? v.toLocaleString();
	const formattedLabels = colorMap.thresholds.map(valueFormatter);
	const startClass = isReverse ? classes?.maxLabel : classes?.minLabel;
	const endClass = isReverse ? classes?.minLabel : classes?.maxLabel;
	const colors = colorMap.colors.map((color, colorIndex) => ({
		color,
		colorIndex
	}));
	const orderedColors = isReverse ? colors.reverse() : colors;
	const isStart = labelPosition === "start";
	const isEnd = labelPosition === "end";
	const isExtremes = labelPosition === "extremes";
	const isInlineStart = labelPosition === "inline-start";
	const isInlineEnd = labelPosition === "inline-end";
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(RootElement, _extends({
		className: clsx(classes?.root, className),
		ref
	}, other, {
		ownerState: props,
		children: orderedColors.map(({ color, colorIndex }, index) => {
			const isFirst = index === 0;
			const isLast = index === colorMap.colors.length - 1;
			const isFirstColor = colorIndex === 0;
			const isLastColor = colorIndex === colorMap.colors.length - 1;
			const data = _extends({
				index: colorIndex,
				length: formattedLabels.length
			}, isFirstColor ? {
				min: null,
				formattedMin: null
			} : {
				min: colorMap.thresholds[colorIndex - 1],
				formattedMin: formattedLabels[colorIndex - 1]
			}, isLastColor ? {
				max: null,
				formattedMax: null
			} : {
				max: colorMap.thresholds[colorIndex],
				formattedMax: formattedLabels[colorIndex]
			});
			const label = labelFormatter?.(data);
			if (label === null || label === void 0) return null;
			const isTextBefore = isStart || isExtremes && isFirst || isInlineStart;
			const isTextAfter = isEnd || isExtremes && isLast || isInlineEnd;
			const clickObject = {
				type: "piecewiseColor",
				color,
				label,
				minValue: data.min,
				maxValue: data.max
			};
			return /*#__PURE__*/ (0, import_jsx_runtime.jsx)("li", { children: /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(onItemClick ? "button" : "div", {
				role: onItemClick ? "button" : void 0,
				type: onItemClick ? "button" : void 0,
				onClick: onItemClick ? (event) => onItemClick(event, clickObject, index) : void 0,
				className: clsx(classes?.item, index === 0 && `${startClass}`, index === orderedColors.length - 1 && `${endClass}`),
				children: [
					isTextBefore && /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ChartsLabel, {
						className: classes?.label,
						children: label
					}),
					/*#__PURE__*/ (0, import_jsx_runtime.jsx)(ChartsLabelMark, {
						className: classes?.mark,
						type: markType,
						color
					}),
					isTextAfter && /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ChartsLabel, {
						className: classes?.label,
						children: label
					})
				]
			}) }, colorIndex);
		})
	}));
});
PiecewiseColorLegend.propTypes = {
	/**
	* The axis direction containing the color configuration to represent.
	* @default 'z'
	*/
	axisDirection: import_prop_types.default.oneOf([
		"x",
		"y",
		"z"
	]),
	/**
	* The id of the axis item with the color configuration to represent.
	* @default The first axis item.
	*/
	axisId: import_prop_types.default.oneOfType([import_prop_types.default.number, import_prop_types.default.string]),
	/**
	* Override or extend the styles applied to the component.
	*/
	classes: import_prop_types.default.object,
	className: import_prop_types.default.string,
	/**
	* The direction of the legend layout.
	* @default 'horizontal'
	*/
	direction: import_prop_types.default.oneOf(["horizontal", "vertical"]),
	/**
	* Format the legend labels.
	* @param {PiecewiseLabelFormatterParams} params The bound of the piece to format.
	* @returns {string|null} The displayed label, `''` to skip the label but show the color mark, or `null` to skip it entirely.
	*/
	labelFormatter: import_prop_types.default.func,
	/**
	* Where to position the labels relative to the gradient.
	* @default 'extremes'
	*/
	labelPosition: import_prop_types.default.oneOf([
		"start",
		"end",
		"extremes",
		"inline-start",
		"inline-end"
	]),
	/**
	* The type of the mark.
	* @default 'square'
	*/
	markType: import_prop_types.default.oneOf([
		"square",
		"circle",
		"line"
	]),
	/**
	* Callback fired when a legend item is clicked.
	* @param {React.MouseEvent<HTMLButtonElement, MouseEvent>} event The click event.
	* @param {PiecewiseColorLegendItemContext} legendItem The legend item data.
	* @param {number} index The index of the clicked legend item.
	*/
	onItemClick: import_prop_types.default.func,
	sx: import_prop_types.default.oneOfType([
		import_prop_types.default.arrayOf(import_prop_types.default.oneOfType([
			import_prop_types.default.func,
			import_prop_types.default.object,
			import_prop_types.default.bool
		])),
		import_prop_types.default.func,
		import_prop_types.default.object
	])
};
//#endregion
//#region node_modules/@mui/x-charts/BarChart/barClasses.mjs
function getBarUtilityClass(slot) {
	return generateUtilityClass("MuiBarChart", slot);
}
var barClasses = generateUtilityClasses("MuiBarChart", [
	"root",
	"series",
	"seriesLabels",
	"element",
	"label",
	"labelAnimate"
]);
var useUtilityClasses$8 = (options) => {
	const { skipAnimation, classes } = options ?? {};
	return composeClasses({
		root: ["root"],
		series: ["series"],
		seriesLabels: ["seriesLabels"],
		element: ["element"],
		label: ["label", !skipAnimation && "labelAnimate"]
	}, getBarUtilityClass, classes);
};
//#endregion
//#region node_modules/@mui/x-charts/BarChart/BarLabel/getBarLabel.mjs
function getBarLabel(options) {
	const { barLabel, value, dataIndex, seriesId, height, width } = options;
	if (barLabel === "value") return value ? value?.toString() : null;
	return barLabel({
		seriesId,
		dataIndex,
		value
	}, { bar: {
		height,
		width
	} });
}
//#endregion
//#region node_modules/@mui/x-charts/BarChart/BarLabel/BarLabel.mjs
var _excluded$34 = [
	"seriesId",
	"dataIndex",
	"color",
	"isFaded",
	"isHighlighted",
	"classes",
	"skipAnimation",
	"layout",
	"xOrigin",
	"yOrigin",
	"placement",
	"hidden"
];
var BarLabelComponent = styled("text", {
	name: "MuiBarLabel",
	slot: "Root"
})(({ theme }) => _extends({}, theme?.typography?.body2, {
	stroke: "none",
	fill: (theme.vars || theme)?.palette?.text?.primary,
	transitionProperty: "opacity, fill",
	transitionDuration: `300ms`,
	transitionTimingFunction: ANIMATION_TIMING_FUNCTION,
	pointerEvents: "none"
}));
function BarLabel(inProps) {
	const props = useThemeProps({
		props: inProps,
		name: "MuiBarLabel"
	});
	const { isFaded, hidden } = props, otherProps = _objectWithoutPropertiesLoose(props, _excluded$34);
	const animatedProps = useAnimateBarLabel(props);
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(BarLabelComponent, _extends({
		textAnchor: getTextAnchor$1(props),
		dominantBaseline: getDominantBaseline$1(props),
		opacity: hidden ? 0 : isFaded ? .3 : 1
	}, otherProps, animatedProps));
}
function getTextAnchor$1({ placement, layout, xOrigin, x }) {
	if (placement === "outside") {
		if (layout === "horizontal") return x < xOrigin ? "end" : "start";
		return "middle";
	}
	return "middle";
}
function getDominantBaseline$1({ placement, layout, yOrigin, y }) {
	if (placement === "outside") {
		if (layout === "horizontal") return "central";
		return y < yOrigin ? "auto" : "hanging";
	}
	return "central";
}
BarLabel.propTypes = {
	classes: import_prop_types.default.object,
	dataIndex: import_prop_types.default.number.isRequired,
	/**
	* Height of the bar this label belongs to.
	*/
	height: import_prop_types.default.number.isRequired,
	/**
	* If true, the bar label is hidden.
	*/
	hidden: import_prop_types.default.bool,
	isFaded: import_prop_types.default.bool.isRequired,
	isHighlighted: import_prop_types.default.bool.isRequired,
	layout: import_prop_types.default.oneOf(["horizontal", "vertical"]).isRequired,
	/**
	* The placement of the bar label.
	* It controls whether the label is rendered in the center or outside the bar.
	* @default 'center'
	*/
	placement: import_prop_types.default.oneOf(["center", "outside"]),
	seriesId: import_prop_types.default.string.isRequired,
	skipAnimation: import_prop_types.default.bool.isRequired,
	/**
	* Width of the bar this label belongs to.
	*/
	width: import_prop_types.default.number.isRequired,
	/**
	* Position in the x-axis of the bar this label belongs to.
	*/
	x: import_prop_types.default.number.isRequired,
	/**
	* The x-coordinate of the stack this bar label belongs to.
	*/
	xOrigin: import_prop_types.default.number.isRequired,
	/**
	* Position in the y-axis of the bar this label belongs to.
	*/
	y: import_prop_types.default.number.isRequired,
	/**
	* The y-coordinate of the stack this bar label belongs to.
	*/
	yOrigin: import_prop_types.default.number.isRequired
};
//#endregion
//#region node_modules/@mui/x-charts/BarChart/BarLabel/BarLabelItem.mjs
var _excluded$33 = [
	"seriesId",
	"classes",
	"color",
	"dataIndex",
	"barLabel",
	"slots",
	"slotProps",
	"xOrigin",
	"yOrigin",
	"x",
	"y",
	"width",
	"height",
	"value",
	"skipAnimation",
	"layout",
	"barLabelPlacement",
	"hidden"
], _excluded2$3 = ["ownerState"];
/**
* @ignore - internal component.
*/
function BarLabelItem(props) {
	const { seriesId, classes: innerClasses, color, dataIndex, barLabel, slots, slotProps, xOrigin, yOrigin, x, y, width, height, value, skipAnimation, layout, barLabelPlacement, hidden } = props, other = _objectWithoutPropertiesLoose(props, _excluded$33);
	const highlightState = useItemHighlightState({
		type: "bar",
		seriesId,
		dataIndex
	});
	const isHighlighted = highlightState === "highlighted";
	const isFaded = highlightState === "faded";
	const ownerState = {
		seriesId,
		classes: innerClasses,
		color,
		isFaded,
		isHighlighted,
		dataIndex,
		skipAnimation,
		layout
	};
	const classes = useUtilityClasses$8(ownerState);
	const Component = slots?.barLabel ?? BarLabel;
	const _useSlotProps = useSlotProps({
		elementType: Component,
		externalSlotProps: slotProps?.barLabel,
		additionalProps: _extends({}, other, {
			xOrigin,
			yOrigin,
			x,
			y,
			width,
			height,
			placement: barLabelPlacement,
			className: classes.label,
			"data-highlighted": isHighlighted || void 0,
			"data-faded": isFaded || void 0
		}),
		ownerState
	}), { ownerState: barLabelOwnerState } = _useSlotProps, barLabelProps = _objectWithoutPropertiesLoose(_useSlotProps, _excluded2$3);
	if (!barLabel) return null;
	const formattedLabelText = getBarLabel({
		barLabel,
		value,
		dataIndex,
		seriesId,
		height,
		width
	});
	if (!formattedLabelText) return null;
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(Component, _extends({}, barLabelProps, barLabelOwnerState, {
		hidden,
		children: formattedLabelText
	}));
}
BarLabelItem.propTypes = {
	/**
	* If provided, the function will be used to format the label of the bar.
	* It can be set to 'value' to display the current value.
	* @param {BarItem} item The item to format.
	* @param {BarLabelContext} context data about the bar.
	* @returns {string} The formatted label.
	*/
	barLabel: import_prop_types.default.oneOfType([import_prop_types.default.oneOf(["value"]), import_prop_types.default.func]),
	classes: import_prop_types.default.object,
	color: import_prop_types.default.string.isRequired,
	dataIndex: import_prop_types.default.number.isRequired,
	/**
	* The height of the bar.
	*/
	height: import_prop_types.default.number.isRequired,
	seriesId: import_prop_types.default.oneOfType([import_prop_types.default.number, import_prop_types.default.string]).isRequired,
	/**
	* The props used for each component slot.
	* @default {}
	*/
	slotProps: import_prop_types.default.object,
	/**
	* Overridable component slots.
	* @default {}
	*/
	slots: import_prop_types.default.object,
	/**
	* The value of the data point.
	*/
	value: import_prop_types.default.number,
	/**
	* The width of the bar.
	*/
	width: import_prop_types.default.number.isRequired
};
//#endregion
//#region node_modules/@mui/x-charts/BarChart/BarLabel/BarLabelPlot.mjs
var _excluded$32 = [
	"processedSeries",
	"className",
	"skipAnimation"
];
/**
* @ignore - internal component.
*/
function BarLabelPlot(props) {
	const { processedSeries, className, skipAnimation } = props, other = _objectWithoutPropertiesLoose(props, _excluded$32);
	const { seriesId, data, layout, xOrigin, yOrigin } = processedSeries;
	if (!processedSeries.barLabel) return null;
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)("g", {
		className,
		"data-series": seriesId,
		children: data.map(({ x, y, dataIndex, color, value, width, height, hidden }) => /*#__PURE__*/ (0, import_jsx_runtime.jsx)(BarLabelItem, _extends({
			seriesId,
			dataIndex,
			value,
			color,
			xOrigin,
			yOrigin,
			x,
			y,
			width,
			height,
			skipAnimation: skipAnimation ?? false,
			layout: layout ?? "vertical",
			hidden
		}, other, {
			barLabel: processedSeries.barLabel,
			barLabelPlacement: processedSeries.barLabelPlacement || "center"
		}), dataIndex))
	}, seriesId);
}
//#endregion
//#region node_modules/@mui/x-charts/BarChart/checkBarChartScaleErrors.mjs
var getAxisMessage = (axisDirection, axisId) => {
	const axisName = `${axisDirection}-axis`;
	const axisIdName = `${axisDirection}Axis`;
	return axisId === (axisDirection === "x" ? "DEFAULT_X_AXIS_KEY" : "DEFAULT_Y_AXIS_KEY") ? `The first \`${axisIdName}\`` : `The ${axisName} with id "${axisId}"`;
};
function checkBarChartScaleErrors(verticalLayout, seriesId, seriesDataLength, xAxisId, xAxis, yAxisId, yAxis) {
	const xAxisConfig = xAxis[xAxisId];
	const yAxisConfig = yAxis[yAxisId];
	const discreteAxisConfig = verticalLayout ? xAxisConfig : yAxisConfig;
	const continuousAxisConfig = verticalLayout ? yAxisConfig : xAxisConfig;
	const discreteAxisId = verticalLayout ? xAxisId : yAxisId;
	const continuousAxisId = verticalLayout ? yAxisId : xAxisId;
	const discreteAxisDirection = verticalLayout ? "x" : "y";
	const continuousAxisDirection = verticalLayout ? "y" : "x";
	if (discreteAxisConfig.scaleType !== "band") throw new Error(`MUI X Charts: ${getAxisMessage(discreteAxisDirection, discreteAxisId)} should be of type "band" to display the bar series of id "${seriesId}". Bar charts require a band scale for the category axis to properly position and size the bars. Set the scaleType to "band" for this axis.`);
	if (discreteAxisConfig.data === void 0) throw new Error(`MUI X Charts: ${getAxisMessage(discreteAxisDirection, discreteAxisId)} should have a data property. The axis needs data to define the categories for the bar chart. Provide a data array to the axis configuration.`);
	if (continuousAxisConfig.scaleType === "band" || continuousAxisConfig.scaleType === "point") throw new Error(`MUI X Charts: ${getAxisMessage(continuousAxisDirection, continuousAxisId)} should be a continuous type to display the bar series of id "${seriesId}". Bar charts require a continuous scale (like "linear" or "log") for the value axis. Change the scaleType to a continuous type such as "linear".`);
	if (discreteAxisConfig.data.length < seriesDataLength) warnOnce([`MUI X Charts: ${getAxisMessage(discreteAxisDirection, discreteAxisId)} has less data (${discreteAxisConfig.data.length} values) than the bar series of id "${seriesId}" (${seriesDataLength} values).`, "The axis data should have at least the same length than the series using it."], "error");
}
//#endregion
//#region node_modules/@mui/x-charts/BarChart/useBarPlotData.mjs
function useBarPlotData(drawingArea, xAxes, yAxes) {
	const seriesData = useBarSeriesContext() ?? {
		series: {},
		stackingGroups: [],
		seriesOrder: []
	};
	const defaultXAxisId = useXAxes().xAxisIds[0];
	const defaultYAxisId = useYAxes().yAxisIds[0];
	const chartId = useChartId();
	const store = useStore();
	const samplingPyramids = store.use(selectorChartSamplingPyramids);
	const zoomMap = store.use(selectorChartZoomMap);
	const zoomOptions = store.use(selectorChartZoomOptionsLookup);
	const sampler = store.use(selectorChartSeriesConfig).bar?.sampler;
	return import_react.useMemo(() => processBarDataForPlot(drawingArea, chartId, seriesData.stackingGroups, seriesData.series, xAxes, yAxes, defaultXAxisId, defaultYAxisId, samplingPyramids, zoomMap, sampler, zoomOptions), [
		drawingArea,
		chartId,
		seriesData.stackingGroups,
		seriesData.series,
		xAxes,
		yAxes,
		defaultXAxisId,
		defaultYAxisId,
		samplingPyramids,
		zoomMap,
		sampler,
		zoomOptions
	]);
}
function processBarDataForPlot(drawingArea, chartId, stackingGroups, series, xAxes, yAxes, defaultXAxisId, defaultYAxisId, samplingPyramids = {}, zoomMap, sampler, zoomOptions) {
	const masks = {};
	return {
		completedData: stackingGroups.flatMap(({ ids: seriesIds }, groupIndex) => {
			const xMin = drawingArea.left;
			const xMax = drawingArea.left + drawingArea.width;
			const yMin = drawingArea.top;
			const yMax = drawingArea.top + drawingArea.height;
			const lastNegativePerIndex = /* @__PURE__ */ new Map();
			const lastPositivePerIndex = /* @__PURE__ */ new Map();
			return seriesIds.map((seriesId) => {
				const xAxisId = series[seriesId].xAxisId ?? defaultXAxisId;
				const yAxisId = series[seriesId].yAxisId ?? defaultYAxisId;
				const layout = series[seriesId].layout;
				const xAxisConfig = xAxes[xAxisId];
				const yAxisConfig = yAxes[yAxisId];
				const verticalLayout = series[seriesId].layout === "vertical";
				const reverse = (verticalLayout ? yAxisConfig.reverse : xAxisConfig.reverse) ?? false;
				checkBarChartScaleErrors(verticalLayout, seriesId, series[seriesId].stackedData.length, xAxisId, xAxes, yAxisId, yAxes);
				const baseScaleConfig = verticalLayout ? xAxisConfig : yAxisConfig;
				const xScale = xAxisConfig.scale;
				const yScale = yAxisConfig.scale;
				const xOrigin = Math.round(xScale(0) ?? 0);
				const yOrigin = Math.round(yScale(0) ?? 0);
				const colorGetter = getColor$1(series[seriesId], xAxes[xAxisId], yAxes[yAxisId]);
				const stackId = series[seriesId].stack;
				const seriesDataPoints = [];
				const registerResult = (result, dataIndex) => {
					if (result.x > xMax || result.x + result.width < xMin || result.y > yMax || result.y + result.height < yMin) return;
					const lastNegative = lastNegativePerIndex.get(dataIndex);
					const lastPositive = lastPositivePerIndex.get(dataIndex);
					const sign = (reverse ? -1 : 1) * Math.sign(result.value ?? 0);
					if (sign > 0) {
						if (lastPositive) delete lastPositive.borderRadiusSide;
						result.borderRadiusSide = verticalLayout ? "top" : "right";
						lastPositivePerIndex.set(dataIndex, result);
					} else if (sign < 0) {
						if (lastNegative) delete lastNegative.borderRadiusSide;
						result.borderRadiusSide = verticalLayout ? "bottom" : "left";
						lastNegativePerIndex.set(dataIndex, result);
					}
					if (!masks[result.maskId]) masks[result.maskId] = {
						id: result.maskId,
						width: 0,
						height: 0,
						hasNegative: false,
						hasPositive: false,
						layout,
						xOrigin,
						yOrigin,
						x: 0,
						y: 0
					};
					const mask = masks[result.maskId];
					mask.width = layout === "vertical" ? result.width : mask.width + result.width;
					mask.height = layout === "vertical" ? mask.height + result.height : result.height;
					mask.x = Math.min(mask.x === 0 ? Infinity : mask.x, result.x);
					mask.y = Math.min(mask.y === 0 ? Infinity : mask.y, result.y);
					const value = result.value ?? 0;
					mask.hasNegative = mask.hasNegative || (reverse ? value > 0 : value < 0);
					mask.hasPositive = mask.hasPositive || (reverse ? value < 0 : value > 0);
					seriesDataPoints.push(result);
				};
				const makeResult = (dataIndex, barDimensions) => _extends({
					seriesId,
					dataIndex,
					hidden: series[seriesId].hidden
				}, barDimensions, {
					color: colorGetter(dataIndex),
					value: series[seriesId].data[dataIndex],
					maskId: `${chartId}_${stackId || seriesId}_${groupIndex}_${dataIndex}`
				});
				const pyramid = samplingPyramids[seriesId];
				const baseAxisId = verticalLayout ? xAxisId : yAxisId;
				const zoom = zoomMap?.get(baseAxisId) ?? (pyramid ? getFullRangeZoom(baseAxisId) : void 0);
				const availableSize = verticalLayout ? drawingArea.width : drawingArea.height;
				const minSpan = zoomOptions?.[baseAxisId]?.minSpan ?? 0;
				const sampledBuckets = pyramid && zoom && sampler?.sample ? sampler.sample({
					built: pyramid,
					zoom,
					availableSize,
					minSpan
				}) : null;
				if (sampledBuckets) {
					const getBucketBarDimensions = createGetBucketBarDimensions({
						verticalLayout,
						xAxisConfig,
						yAxisConfig,
						series: series[seriesId],
						numberOfGroups: stackingGroups.length
					});
					const stacked = series[seriesId].visibleStackedData;
					for (const { startIndex, endIndex, indices } of sampledBuckets) {
						let low = Infinity;
						let high = -Infinity;
						for (let k = 0; k < indices.length; k += 1) {
							const point = stacked[indices[k]];
							low = Math.min(low, point[0], point[1]);
							high = Math.max(high, point[0], point[1]);
						}
						registerResult(makeResult(startIndex, getBucketBarDimensions(startIndex, endIndex, low, high, groupIndex)), startIndex);
					}
				} else {
					const getBarDimensions = createGetBarDimensions({
						verticalLayout,
						xAxisConfig,
						yAxisConfig,
						series: series[seriesId],
						numberOfGroups: stackingGroups.length
					});
					const baseData = baseScaleConfig.data;
					const dataLength = baseData.length;
					let firstIndex = 0;
					let lastIndex = dataLength - 1;
					if (dataLength > 1) {
						const baseScale = baseScaleConfig.scale;
						const p0 = baseScale(baseData[0]);
						const slope = baseScale(baseData[1]) - p0;
						if (slope !== 0) {
							const winLo = verticalLayout ? xMin : yMin;
							const winHi = verticalLayout ? xMax : yMax;
							const ia = (winLo - p0) / slope;
							const ib = (winHi - p0) / slope;
							firstIndex = Math.max(0, Math.floor(Math.min(ia, ib)) - 1);
							lastIndex = Math.min(dataLength - 1, Math.ceil(Math.max(ia, ib)) + 1);
						}
					}
					for (let dataIndex = firstIndex; dataIndex <= lastIndex; dataIndex += 1) {
						const barDimensions = getBarDimensions(dataIndex, groupIndex);
						if (barDimensions == null) continue;
						registerResult(makeResult(dataIndex, barDimensions), dataIndex);
					}
				}
				return {
					seriesId,
					barLabel: series[seriesId].barLabel,
					barLabelPlacement: series[seriesId].barLabelPlacement,
					data: seriesDataPoints,
					layout,
					xOrigin,
					yOrigin
				};
			});
		}),
		masksData: Object.values(masks)
	};
}
//#endregion
//#region node_modules/@mui/x-charts/BarChart/AnimatedBarElement.mjs
var _excluded$31 = [
	"ownerState",
	"skipAnimation",
	"seriesId",
	"dataIndex",
	"xOrigin",
	"yOrigin"
];
function AnimatedBarElement(props) {
	const { ownerState } = props, other = _objectWithoutPropertiesLoose(props, _excluded$31);
	const animatedProps = useAnimateBar(props);
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)("rect", _extends({}, other, {
		filter: ownerState.isHighlighted ? "brightness(120%)" : void 0,
		opacity: ownerState.isFaded ? .3 : 1,
		"data-highlighted": ownerState.isHighlighted || void 0,
		"data-faded": ownerState.isFaded || void 0
	}, animatedProps));
}
//#endregion
//#region node_modules/@mui/x-charts/hooks/useIsItemFocused.mjs
/**
* A hook to check if an item has the focus.
*
* If you need to process multiple points, use the `useIsItemFocusedGetter` hook instead.
*
* @param {FocusedItemIdentifier} item is the item to check
* @returns {boolean} the focus state
*/
function useIsItemFocused(item) {
	return useStore().use(selectorChartsItemIsFocused, item);
}
//#endregion
//#region node_modules/@mui/x-charts/BarChart/BarElement.mjs
var _excluded$30 = [
	"seriesId",
	"dataIndex",
	"classes",
	"color",
	"slots",
	"slotProps",
	"style",
	"onClick",
	"skipAnimation",
	"layout",
	"x",
	"xOrigin",
	"y",
	"yOrigin",
	"width",
	"height",
	"hidden"
];
function BarElement(props) {
	const { seriesId, dataIndex, classes: innerClasses, color, slots, slotProps, style, onClick, skipAnimation, layout, x, xOrigin, y, yOrigin, width, height, hidden } = props, other = _objectWithoutPropertiesLoose(props, _excluded$30);
	const itemIdentifier = import_react.useMemo(() => ({
		type: "bar",
		seriesId,
		dataIndex
	}), [seriesId, dataIndex]);
	const highlightState = useItemHighlightState(itemIdentifier);
	const ownerState = {
		seriesId,
		dataIndex,
		classes: innerClasses,
		color,
		isFaded: highlightState === "faded",
		isHighlighted: highlightState === "highlighted",
		isFocused: useIsItemFocused(itemIdentifier)
	};
	const classes = useUtilityClasses$8(ownerState);
	const Bar = slots?.bar ?? AnimatedBarElement;
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(Bar, _extends({}, useSlotProps({
		elementType: Bar,
		externalSlotProps: slotProps?.bar,
		externalForwardedProps: other,
		additionalProps: {
			seriesId,
			dataIndex,
			color,
			x,
			xOrigin,
			y,
			yOrigin,
			width,
			height,
			style,
			onClick,
			cursor: onClick ? "pointer" : void 0,
			stroke: "none",
			fill: color,
			skipAnimation,
			layout,
			hidden
		},
		className: classes.element,
		ownerState
	})));
}
BarElement.propTypes = {
	classes: import_prop_types.default.object,
	dataIndex: import_prop_types.default.number.isRequired,
	hidden: import_prop_types.default.bool,
	layout: import_prop_types.default.oneOf(["horizontal", "vertical"]).isRequired,
	seriesId: import_prop_types.default.string.isRequired,
	skipAnimation: import_prop_types.default.bool.isRequired,
	/**
	* The props used for each component slot.
	* @default {}
	*/
	slotProps: import_prop_types.default.object,
	/**
	* Overridable component slots.
	* @default {}
	*/
	slots: import_prop_types.default.object,
	xOrigin: import_prop_types.default.number.isRequired,
	yOrigin: import_prop_types.default.number.isRequired
};
//#endregion
//#region node_modules/@mui/x-charts/BarChart/BarClipPath.mjs
function barClipPathPropsInterpolator(from, to) {
	const interpolateX = number_default(from.x, to.x);
	const interpolateY = number_default(from.y, to.y);
	const interpolateWidth = number_default(from.width, to.width);
	const interpolateHeight = number_default(from.height, to.height);
	const interpolateBorderRadius = number_default(from.borderRadius, to.borderRadius);
	return (t) => {
		return {
			x: interpolateX(t),
			y: interpolateY(t),
			width: interpolateWidth(t),
			height: interpolateHeight(t),
			borderRadius: interpolateBorderRadius(t)
		};
	};
}
function useAnimateBarClipPath(props) {
	const initialProps = {
		x: props.layout === "vertical" ? props.x : props.xOrigin,
		y: props.layout === "vertical" ? props.yOrigin : props.y,
		width: props.layout === "vertical" ? props.width : 0,
		height: props.layout === "vertical" ? 0 : props.height,
		borderRadius: props.borderRadius
	};
	return useAnimate({
		x: props.x,
		y: props.y,
		width: props.width,
		height: props.height,
		borderRadius: props.borderRadius
	}, {
		createInterpolator: barClipPathPropsInterpolator,
		transformProps: (p) => ({ d: generateClipPath(props.hasNegative, props.hasPositive, props.layout, p.x, p.y, p.width, p.height, props.xOrigin, props.yOrigin, p.borderRadius) }),
		applyProps(element, { d }) {
			if (d) element.setAttribute("d", d);
		},
		initialProps,
		skip: props.skipAnimation,
		ref: props.ref
	});
}
/**
* @ignore - internal component.
*/
function BarClipPath(props) {
	const { maskId, x, y, width, height, skipAnimation } = props;
	const { ref, d } = useAnimateBarClipPath({
		layout: props.layout ?? "vertical",
		hasNegative: props.hasNegative,
		hasPositive: props.hasPositive,
		xOrigin: props.xOrigin,
		yOrigin: props.yOrigin,
		x,
		y,
		width,
		height,
		borderRadius: props.borderRadius ?? 0,
		skipAnimation
	});
	if (!props.borderRadius || props.borderRadius <= 0) return null;
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)("clipPath", {
		id: maskId,
		children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)("path", {
			ref,
			d
		})
	});
}
function generateClipPath(hasNegative, hasPositive, layout, x, y, width, height, xOrigin, yOrigin, borderRadius) {
	if (layout === "vertical") {
		if (hasPositive && hasNegative) {
			const bR = Math.min(borderRadius, width / 2, height / 2);
			return `M${x},${y + height / 2} v${-(height / 2 - bR)} a${bR},${bR} 0 0 1 ${bR},${-bR} h${width - bR * 2} a${bR},${bR} 0 0 1 ${bR},${bR} v${height - 2 * bR} a${bR},${bR} 0 0 1 ${-bR},${bR} h${-(width - bR * 2)} a${bR},${bR} 0 0 1 ${-bR},${-bR} v${-(height / 2 - bR)}`;
		}
		const bR = Math.min(borderRadius, width / 2);
		if (hasPositive) return `M${x},${Math.max(yOrigin, y + bR)} v${Math.min(0, -(yOrigin - y - bR))} a${bR},${bR} 0 0 1 ${bR},${-bR} h${width - bR * 2} a${bR},${bR} 0 0 1 ${bR},${bR} v${Math.max(0, yOrigin - y - bR)} Z`;
		if (hasNegative) return `M${x},${Math.min(yOrigin, y + height - bR)} v${Math.max(0, height - bR)} a${bR},${bR} 0 0 0 ${bR},${bR} h${width - bR * 2} a${bR},${bR} 0 0 0 ${bR},${-bR} v${-Math.max(0, height - bR)} Z`;
	}
	if (layout === "horizontal") {
		if (hasPositive && hasNegative) {
			const bR = Math.min(borderRadius, width / 2, height / 2);
			return `M${x + width / 2},${y} h${width / 2 - bR} a${bR},${bR} 0 0 1 ${bR},${bR} v${height - bR * 2} a${bR},${bR} 0 0 1 ${-bR},${bR} h${-(width - 2 * bR)} a${bR},${bR} 0 0 1 ${-bR},${-bR} v${-(height - bR * 2)} a${bR},${bR} 0 0 1 ${bR},${-bR} h${width / 2 - bR}`;
		}
		const bR = Math.min(borderRadius, height / 2);
		if (hasPositive) return `M${Math.min(xOrigin, x - bR)},${y} h${width} a${bR},${bR} 0 0 1 ${bR},${bR} v${height - bR * 2} a${bR},${bR} 0 0 1 ${-bR},${bR} h${-width} Z`;
		if (hasNegative) return `M${Math.max(xOrigin, x + width + bR)},${y} h${-width} a${bR},${bR} 0 0 0 ${-bR},${bR} v${height - bR * 2} a${bR},${bR} 0 0 0 ${bR},${bR} h${width} Z`;
	}
}
//#endregion
//#region node_modules/@mui/x-charts/BarChart/useRegisterItemClickHandlers.mjs
/**
* Hook that registers pointer event handlers for chart item clicking.
* @param onItemClick Callback for item click events.
*/
function useRegisterItemClickHandlers(onItemClick) {
	const { instance } = useChartsContext();
	const chartsLayerContainerRef = useChartsLayerContainerRef();
	const store = useStore();
	import_react.useEffect(() => {
		const element = chartsLayerContainerRef.current;
		if (!element || !onItemClick) return;
		let lastPointerUp = null;
		const onClick = function onClick(event) {
			let point = event;
			if (lastPointerUp) {
				if (Math.abs(event.clientX - lastPointerUp.clientX) <= 1 && Math.abs(event.clientY - lastPointerUp.clientY) <= 1) point = {
					clientX: lastPointerUp.clientX,
					clientY: lastPointerUp.clientY
				};
			}
			lastPointerUp = null;
			const svgPoint = getChartPoint(element, point);
			if (!instance.isPointInside(svgPoint.x, svgPoint.y)) return;
			const item = selectorBarItemAtPosition(store.state, svgPoint);
			if (item) onItemClick(event, {
				type: "bar",
				seriesId: item.seriesId,
				dataIndex: item.dataIndex
			});
		};
		const onPointerUp = function onPointerUp(event) {
			lastPointerUp = event;
		};
		element.addEventListener("click", onClick);
		element.addEventListener("pointerup", onPointerUp);
		return () => {
			element.removeEventListener("click", onClick);
			element.removeEventListener("pointerup", onPointerUp);
		};
	}, [
		instance,
		onItemClick,
		store,
		chartsLayerContainerRef
	]);
}
//#endregion
//#region node_modules/@mui/x-charts/BarChart/IndividualBarPlot.mjs
var _excluded$29 = [
	"completedData",
	"masksData",
	"borderRadius",
	"onItemClick",
	"skipAnimation"
];
function IndividualBarPlot(_ref) {
	let { completedData, masksData, borderRadius, onItemClick, skipAnimation } = _ref, other = _objectWithoutPropertiesLoose(_ref, _excluded$29);
	useRegisterItemClickHandlers(onItemClick);
	const classes = useUtilityClasses$8();
	const withoutBorderRadius = !borderRadius || borderRadius <= 0;
	return /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(import_react.Fragment, { children: [!withoutBorderRadius && masksData.map(({ id, x, y, xOrigin, yOrigin, width, height, hasPositive, hasNegative, layout }) => {
		return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(BarClipPath, {
			maskId: id,
			borderRadius,
			hasNegative,
			hasPositive,
			layout,
			x,
			y,
			xOrigin,
			yOrigin,
			width,
			height,
			skipAnimation: skipAnimation ?? false
		}, id);
	}), completedData.map(({ seriesId, layout, xOrigin, yOrigin, data }) => {
		return /*#__PURE__*/ (0, import_jsx_runtime.jsx)("g", {
			"data-series": seriesId,
			className: classes.series,
			children: data.map(({ dataIndex, color, maskId, x, y, width, height }) => {
				const barElement = /*#__PURE__*/ (0, import_jsx_runtime.jsx)(BarElement, _extends({
					seriesId,
					dataIndex,
					color,
					skipAnimation: skipAnimation ?? false,
					layout: layout ?? "vertical",
					x,
					xOrigin,
					y,
					yOrigin,
					width,
					height,
					cursor: onItemClick ? "pointer" : void 0
				}, other), dataIndex);
				if (withoutBorderRadius) return barElement;
				return /*#__PURE__*/ (0, import_jsx_runtime.jsx)("g", {
					clipPath: `url(#${maskId})`,
					children: barElement
				}, dataIndex);
			})
		}, seriesId);
	})] });
}
//#endregion
//#region node_modules/@mui/x-charts/internals/appendAtKey.mjs
/**
* Given a map of arrays, appends a value to the array at the given key.
* If no array exists at that key, one is created and the value appended.
* @param map Map of arrays
* @param key Key to append the value at
* @param value Value to append
*/
function appendAtKey(map, key, value) {
	let bucket = map.get(key);
	if (!bucket) {
		bucket = [value];
		map.set(key, bucket);
	} else bucket.push(value);
	return bucket;
}
//#endregion
//#region node_modules/@mui/x-charts/BarChart/BatchBarPlot/useCreateBarPaths.mjs
var MAX_POINTS_PER_PATH$1 = 1e3;
function generateBarPath(x, y, width, height, topLeftBorderRadius, topRightBorderRadius, bottomRightBorderRadius, bottomLeftBorderRadius) {
	const tLBR = Math.min(topLeftBorderRadius, width / 2, height / 2);
	const tRBR = Math.min(topRightBorderRadius, width / 2, height / 2);
	const bRBR = Math.min(bottomRightBorderRadius, width / 2, height / 2);
	const bLBR = Math.min(bottomLeftBorderRadius, width / 2, height / 2);
	return `M${x + tLBR},${y}
   h${width - tLBR - tRBR}
   a${tRBR},${tRBR} 0 0 1 ${tRBR},${tRBR}
   v${height - tRBR - bRBR}
   a${bRBR},${bRBR} 0 0 1 -${bRBR},${bRBR}
   h-${width - bRBR - bLBR}
   a${bLBR},${bLBR} 0 0 1 -${bLBR},-${bLBR}
   v-${height - bLBR - tLBR}
   a${tLBR},${tLBR} 0 0 1 ${tLBR},-${tLBR}
   Z`;
}
function createPath$1(barData, borderRadius) {
	return generateBarPath(barData.x, barData.y, barData.width, barData.height, barData.borderRadiusSide === "left" || barData.borderRadiusSide === "top" ? borderRadius : 0, barData.borderRadiusSide === "right" || barData.borderRadiusSide === "top" ? borderRadius : 0, barData.borderRadiusSide === "right" || barData.borderRadiusSide === "bottom" ? borderRadius : 0, barData.borderRadiusSide === "left" || barData.borderRadiusSide === "bottom" ? borderRadius : 0);
}
/**
* Hook that creates bar paths for a given series data. Used by the batch bar renderer.
* @param seriesData
* @param borderRadius
*/
function useCreateBarPaths(seriesData, borderRadius) {
	const paths = /* @__PURE__ */ new Map();
	const temporaryPaths = /* @__PURE__ */ new Map();
	for (let j = 0; j < seriesData.data.length; j += 1) {
		const barData = seriesData.data[j];
		const pathString = createPath$1(barData, borderRadius);
		const tempPath = appendAtKey(temporaryPaths, barData.color, pathString);
		if (tempPath.length >= MAX_POINTS_PER_PATH$1) {
			appendAtKey(paths, barData.color, tempPath.join(""));
			temporaryPaths.delete(barData.color);
		}
	}
	for (const [fill, tempPath] of temporaryPaths.entries()) if (tempPath.length > 0) appendAtKey(paths, fill, tempPath.join(""));
	return paths;
}
//#endregion
//#region node_modules/@mui/x-charts/BarChart/BatchBarPlot/BarGroup.mjs
var _excluded$28 = [
	"skipAnimation",
	"layout",
	"xOrigin",
	"yOrigin"
], _excluded2$2 = [
	"children",
	"layout",
	"xOrigin",
	"yOrigin"
];
var PathGroup = styled("g")({
	"&[data-faded=\"true\"]": { opacity: .3 },
	"& path": { pointerEvents: "none" }
});
function BarGroup(_ref) {
	let { skipAnimation, layout, xOrigin, yOrigin } = _ref, props = _objectWithoutPropertiesLoose(_ref, _excluded$28);
	if (skipAnimation) return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(PathGroup, _extends({}, props));
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(AnimatedGroup, _extends({}, props, {
		layout,
		xOrigin,
		yOrigin
	}));
}
var AnimatedRect = styled("rect")({
	"@keyframes scaleInX": {
		from: { transform: "scaleX(0)" },
		to: { transform: "scaleX(1)" }
	},
	"@keyframes scaleInY": {
		from: { transform: "scaleY(0)" },
		to: { transform: "scaleY(1)" }
	},
	animationDuration: `300ms`,
	animationFillMode: "forwards",
	"&[data-orientation=\"horizontal\"]": { animationName: "scaleInX" },
	"&[data-orientation=\"vertical\"]": { animationName: "scaleInY" }
});
function AnimatedGroup(_ref2) {
	let { children, layout, xOrigin, yOrigin } = _ref2, props = _objectWithoutPropertiesLoose(_ref2, _excluded2$2);
	const drawingArea = useStore().use(selectorChartDrawingArea);
	const clipPathId = useId();
	const animateChildren = [];
	if (layout === "horizontal") {
		animateChildren.push(/*#__PURE__*/ (0, import_jsx_runtime.jsx)(AnimatedRect, {
			"data-orientation": "horizontal",
			x: drawingArea.left,
			width: xOrigin - drawingArea.left,
			y: drawingArea.top,
			height: drawingArea.height,
			style: { transformOrigin: `${xOrigin}px ${drawingArea.top + drawingArea.height / 2}px` }
		}, "left"));
		animateChildren.push(/*#__PURE__*/ (0, import_jsx_runtime.jsx)(AnimatedRect, {
			"data-orientation": "horizontal",
			x: xOrigin,
			width: drawingArea.left + drawingArea.width - xOrigin,
			y: drawingArea.top,
			height: drawingArea.height,
			style: { transformOrigin: `${xOrigin}px ${drawingArea.top + drawingArea.height / 2}px` }
		}, "right"));
	} else {
		animateChildren.push(/*#__PURE__*/ (0, import_jsx_runtime.jsx)(AnimatedRect, {
			"data-orientation": "vertical",
			x: drawingArea.left,
			width: drawingArea.width,
			y: drawingArea.top,
			height: yOrigin - drawingArea.top,
			style: { transformOrigin: `${drawingArea.left + drawingArea.width / 2}px ${yOrigin}px` }
		}, "top"));
		animateChildren.push(/*#__PURE__*/ (0, import_jsx_runtime.jsx)(AnimatedRect, {
			"data-orientation": "vertical",
			x: drawingArea.left,
			width: drawingArea.width,
			y: yOrigin,
			height: drawingArea.top + drawingArea.height - yOrigin,
			style: { transformOrigin: `${drawingArea.left + drawingArea.width / 2}px ${yOrigin}px` }
		}, "bottom"));
	}
	return /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(import_react.Fragment, { children: [/*#__PURE__*/ (0, import_jsx_runtime.jsx)("clipPath", {
		id: clipPathId,
		children: animateChildren
	}), /*#__PURE__*/ (0, import_jsx_runtime.jsx)(PathGroup, _extends({ clipPath: `url(#${clipPathId})` }, props, { children }))] });
}
//#endregion
//#region node_modules/@mui/x-charts/BarChart/BatchBarPlot/BatchBarPlot.mjs
function BatchBarPlot({ completedData, borderRadius = 0, onItemClick, skipAnimation = false }) {
	useRegisterItemClickHandlers(onItemClick);
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(import_react.Fragment, { children: completedData.map((series) => /*#__PURE__*/ (0, import_jsx_runtime.jsx)(SeriesBatchPlot, {
		series,
		borderRadius,
		skipAnimation
	}, series.seriesId)) });
}
var MemoFadedHighlightedBars = /*#__PURE__*/ import_react.memo(FadedHighlightedBars);
MemoFadedHighlightedBars.displayName = "MemoFadedHighlightedBars";
function SeriesBatchPlot({ series, borderRadius, skipAnimation }) {
	const classes = useUtilityClasses$8();
	const { store } = useChartsContext();
	const isSeriesHighlighted = store.use(selectorChartIsSeriesHighlighted, series.seriesId);
	const isSeriesFaded = store.use(selectorChartIsSeriesFaded, series.seriesId);
	return /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(import_react.Fragment, { children: [/*#__PURE__*/ (0, import_jsx_runtime.jsx)(BarGroup, {
		className: classes.series,
		"data-series": series.seriesId,
		layout: series.layout,
		xOrigin: series.xOrigin,
		yOrigin: series.yOrigin,
		skipAnimation,
		"data-faded": isSeriesFaded || void 0,
		"data-highlighted": isSeriesHighlighted || void 0,
		children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(BatchBarSeriesPlot, {
			processedSeries: series,
			borderRadius
		})
	}), /*#__PURE__*/ (0, import_jsx_runtime.jsx)(MemoFadedHighlightedBars, {
		processedSeries: series,
		borderRadius
	})] });
}
function BatchBarSeriesPlot({ processedSeries, borderRadius }) {
	const paths = useCreateBarPaths(processedSeries, borderRadius);
	const children = [];
	let i = 0;
	for (const [fill, dArray] of paths.entries()) for (const d of dArray) {
		children.push(/*#__PURE__*/ (0, import_jsx_runtime.jsx)("path", {
			fill,
			d
		}, i));
		i += 1;
	}
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(import_react.Fragment, { children });
}
function FadedHighlightedBars({ processedSeries, borderRadius }) {
	const { store } = useChartsContext();
	const seriesHighlightedDataIndex = store.use(selectorChartSeriesHighlightedItem, processedSeries.seriesId);
	const seriesUnfadedDataIndex = store.use(selectorChartSeriesUnfadedItem, processedSeries.seriesId);
	const seriesHighlightedItem = seriesHighlightedDataIndex != null ? processedSeries.data.find((v) => v.dataIndex === seriesHighlightedDataIndex) || null : null;
	const seriesUnfadedItem = seriesUnfadedDataIndex != null ? processedSeries.data.find((v) => v.dataIndex === seriesUnfadedDataIndex) || null : null;
	const siblings = [];
	if (seriesHighlightedItem != null) siblings.push(/*#__PURE__*/ (0, import_jsx_runtime.jsx)("path", {
		fill: seriesHighlightedItem.color,
		filter: "brightness(120%)",
		"data-highlighted": true,
		d: createPath$1(seriesHighlightedItem, borderRadius)
	}, `highlighted-${processedSeries.seriesId}`));
	if (seriesUnfadedItem != null) siblings.push(/*#__PURE__*/ (0, import_jsx_runtime.jsx)("path", {
		fill: seriesUnfadedItem.color,
		d: createPath$1(seriesUnfadedItem, borderRadius)
	}, `unfaded-${seriesUnfadedItem.seriesId}`));
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(import_react.Fragment, { children: siblings });
}
//#endregion
//#region node_modules/@mui/x-charts/BarChart/BarPlot.mjs
var _excluded$27 = [
	"className",
	"skipAnimation",
	"onItemClick",
	"borderRadius",
	"renderer"
];
var BarPlotRoot = styled("g", {
	name: "MuiBarPlot",
	slot: "Root"
})({ [`& .${barClasses.element}`]: {
	transitionProperty: "opacity, fill",
	transitionDuration: `300ms`,
	transitionTimingFunction: ANIMATION_TIMING_FUNCTION
} });
/**
* Demos:
*
* - [Bars](https://mui.com/x/react-charts/bars/)
* - [Bar demonstration](https://mui.com/x/react-charts/bar-demo/)
* - [Stacking](https://mui.com/x/react-charts/stacking/)
*
* API:
*
* - [BarPlot API](https://mui.com/x/api/charts/bar-plot/)
*/
function BarPlot(props) {
	const { className, skipAnimation: inSkipAnimation, onItemClick, borderRadius, renderer } = props, other = _objectWithoutPropertiesLoose(props, _excluded$27);
	const skipAnimation = useSkipAnimation(useInternalIsZoomInteracting() || inSkipAnimation);
	const batchSkipAnimation = useSkipAnimation(inSkipAnimation);
	const { xAxis: xAxes } = useXAxes();
	const { yAxis: yAxes } = useYAxes();
	const { completedData, masksData } = useBarPlotData(useDrawingArea(), xAxes, yAxes);
	const classes = useUtilityClasses$8();
	const BarElementPlot = renderer === "svg-batch" ? BatchBarPlot : IndividualBarPlot;
	return /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(BarPlotRoot, {
		className: clsx(classes.root, className),
		children: [/*#__PURE__*/ (0, import_jsx_runtime.jsx)(BarElementPlot, _extends({
			completedData,
			masksData,
			skipAnimation: renderer === "svg-batch" ? batchSkipAnimation : skipAnimation,
			onItemClick,
			borderRadius
		}, other)), completedData.map((processedSeries) => /*#__PURE__*/ (0, import_jsx_runtime.jsx)(BarLabelPlot, _extends({
			className: classes.seriesLabels,
			processedSeries,
			skipAnimation
		}, other), processedSeries.seriesId))]
	});
}
BarPlot.propTypes = {
	/**
	* Defines the border radius of the bar element.
	*/
	borderRadius: import_prop_types.default.number,
	/**
	* A CSS class name applied to the root element.
	*/
	className: import_prop_types.default.string,
	/**
	* Callback fired when a bar item is clicked.
	* @param {MouseEvent | React.MouseEvent<SVGElement, MouseEvent>} event The event source of the callback.
	*        It is a native MouseEvent for `svg-batch` renderer and a React MouseEvent for `svg-single` renderer.
	* @param {BarItemIdentifier} barItemIdentifier The bar item identifier.
	*/
	onItemClick: import_prop_types.default.func,
	/**
	* The type of renderer to use for the bar plot.
	* - `svg-single`: Renders every bar in a `<rect />` element.
	* - `svg-batch`: Batch renders bars in `<path />` elements for better performance with large datasets, at the cost of some limitations.
	*                Read more: https://mui.com/x/react-charts/bars/#performance
	*
	* @default 'svg-single'
	*/
	renderer: import_prop_types.default.oneOf(["svg-batch", "svg-single"]),
	/**
	* If `true`, animations are skipped.
	* @default undefined
	*/
	skipAnimation: import_prop_types.default.bool,
	/**
	* The props used for each component slot.
	* @default {}
	*/
	slotProps: import_prop_types.default.object,
	/**
	* Overridable component slots.
	* @default {}
	*/
	slots: import_prop_types.default.object
};
//#endregion
//#region node_modules/@mui/x-charts/BarChart/BarChart.plugins.mjs
var BAR_CHART_PLUGINS = [
	useChartZAxis,
	useChartBrush,
	useChartTooltip,
	useChartInteraction,
	useChartCartesianAxis,
	useChartHighlight,
	useChartVisibilityManager,
	useChartKeyboardNavigation
];
//#endregion
//#region node_modules/@mui/x-charts/BarChart/useBarChartProps.mjs
var _excluded$26 = [
	"xAxis",
	"yAxis",
	"series",
	"width",
	"height",
	"margin",
	"colors",
	"dataset",
	"sx",
	"axisHighlight",
	"grid",
	"children",
	"slots",
	"slotProps",
	"skipAnimation",
	"loading",
	"layout",
	"onItemClick",
	"highlightedItem",
	"onHighlightChange",
	"borderRadius",
	"className",
	"hideLegend",
	"showToolbar",
	"brushConfig",
	"renderer"
];
/**
* A helper function that extracts BarChartProps from the input props
* and returns an object with props for the children components of BarChart.
*
* @param props The input props for BarChart
* @returns An object with props for the children components of BarChart
*/
var useBarChartProps = (props) => {
	const { xAxis, yAxis, series, width, height, margin, colors, dataset, sx, axisHighlight, grid, children, slots, slotProps, skipAnimation, loading, layout, onItemClick, highlightedItem, onHighlightChange, borderRadius, className, brushConfig, renderer } = props, other = _objectWithoutPropertiesLoose(props, _excluded$26);
	const clipPathId = `${useId()}-clip-path`;
	const hasHorizontalSeries = layout === "horizontal" || layout === void 0 && series.some((item) => item.layout === "horizontal");
	const defaultBandXAxis = import_react.useMemo(() => [{
		id: DEFAULT_X_AXIS_KEY,
		scaleType: "band",
		data: Array.from({ length: Math.max(...series.map((s) => (s.data ?? dataset ?? []).length)) }, (_, index) => index)
	}], [dataset, series]);
	const defaultBandYAxis = import_react.useMemo(() => [{
		id: DEFAULT_Y_AXIS_KEY,
		scaleType: "band",
		data: Array.from({ length: Math.max(...series.map((s) => (s.data ?? dataset ?? []).length)) }, (_, index) => index)
	}], [dataset, series]);
	const seriesWithDefault = import_react.useMemo(() => series.map((s) => _extends({ type: "bar" }, s, { layout: hasHorizontalSeries ? "horizontal" : "vertical" })), [hasHorizontalSeries, series]);
	const defaultXAxis = hasHorizontalSeries ? void 0 : defaultBandXAxis;
	const processedXAxis = import_react.useMemo(() => {
		if (!xAxis) return defaultXAxis;
		return hasHorizontalSeries ? xAxis : xAxis.map((axis) => _extends({ scaleType: "band" }, axis));
	}, [
		defaultXAxis,
		hasHorizontalSeries,
		xAxis
	]);
	const defaultYAxis = hasHorizontalSeries ? defaultBandYAxis : void 0;
	const chartsContainerProps = _extends({}, other, {
		series: seriesWithDefault,
		width,
		height,
		margin,
		colors,
		dataset,
		xAxis: processedXAxis,
		yAxis: import_react.useMemo(() => {
			if (!yAxis) return defaultYAxis;
			return hasHorizontalSeries ? yAxis.map((axis) => _extends({ scaleType: "band" }, axis)) : yAxis;
		}, [
			defaultYAxis,
			hasHorizontalSeries,
			yAxis
		]),
		highlightedItem,
		onHighlightChange,
		disableAxisListener: slotProps?.tooltip?.trigger !== "axis" && axisHighlight?.x === "none" && axisHighlight?.y === "none",
		skipAnimation,
		brushConfig,
		plugins: BAR_CHART_PLUGINS
	});
	const barPlotProps = {
		onItemClick,
		slots,
		slotProps,
		borderRadius,
		renderer
	};
	const gridProps = {
		vertical: grid?.vertical,
		horizontal: grid?.horizontal
	};
	const clipPathGroupProps = { clipPath: `url(#${clipPathId})` };
	const clipPathProps = { id: clipPathId };
	const overlayProps = {
		slots,
		slotProps,
		loading
	};
	const chartsAxisProps = {
		slots,
		slotProps
	};
	const axisHighlightProps = _extends({}, hasHorizontalSeries ? { y: "band" } : { x: "band" }, axisHighlight);
	const legendProps = {
		slots,
		slotProps
	};
	return {
		chartsWrapperProps: {
			sx,
			legendPosition: props.slotProps?.legend?.position,
			legendDirection: props.slotProps?.legend?.direction,
			hideLegend: props.hideLegend ?? false,
			className
		},
		chartsContainerProps,
		barPlotProps,
		gridProps,
		clipPathProps,
		clipPathGroupProps,
		overlayProps,
		chartsAxisProps,
		axisHighlightProps,
		legendProps,
		children
	};
};
//#endregion
//#region node_modules/@mui/x-internals/ToolbarContext/ToolbarContext.mjs
var ToolbarContext = /*#__PURE__*/ import_react.createContext(void 0);
ToolbarContext.displayName = "ToolbarContext";
function useToolbarContext() {
	const context = import_react.useContext(ToolbarContext);
	if (context === void 0) throw new Error("MUI X: Missing context. Toolbar subcomponents must be placed within a <Toolbar /> component.");
	return context;
}
function ToolbarContextProvider({ children }) {
	const [focusableItemId, setFocusableItemId] = import_react.useState(null);
	const focusableItemIdRef = import_react.useRef(focusableItemId);
	const [items, setItems] = import_react.useState([]);
	const getSortedItems = import_react.useCallback(() => items.sort(sortByDocumentPosition), [items]);
	const findEnabledItem = import_react.useCallback((startIndex, step, wrap = true) => {
		let index = startIndex;
		const sortedItems = getSortedItems();
		const itemCount = sortedItems.length;
		for (let i = 0; i < itemCount; i += 1) {
			index += step;
			if (index >= itemCount) {
				if (!wrap) return -1;
				index = 0;
			} else if (index < 0) {
				if (!wrap) return -1;
				index = itemCount - 1;
			}
			if (!sortedItems[index].ref.current?.disabled && sortedItems[index].ref.current?.ariaDisabled !== "true") return index;
		}
		return -1;
	}, [getSortedItems]);
	const registerItem = import_react.useCallback((id, itemRef) => {
		setItems((prevItems) => [...prevItems, {
			id,
			ref: itemRef
		}]);
	}, []);
	const unregisterItem = import_react.useCallback((id) => {
		setItems((prevItems) => prevItems.filter((i) => i.id !== id));
	}, []);
	const onItemKeyDown = import_react.useCallback((event) => {
		if (!focusableItemId) return;
		const sortedItems = getSortedItems();
		const focusableItemIndex = sortedItems.findIndex((item) => item.id === focusableItemId);
		let newIndex = -1;
		if (event.key === "ArrowRight") {
			event.preventDefault();
			newIndex = findEnabledItem(focusableItemIndex, 1);
		} else if (event.key === "ArrowLeft") {
			event.preventDefault();
			newIndex = findEnabledItem(focusableItemIndex, -1);
		} else if (event.key === "Home") {
			event.preventDefault();
			newIndex = findEnabledItem(-1, 1, false);
		} else if (event.key === "End") {
			event.preventDefault();
			newIndex = findEnabledItem(sortedItems.length, -1, false);
		}
		if (newIndex >= 0 && newIndex < sortedItems.length) {
			const item = sortedItems[newIndex];
			setFocusableItemId(item.id);
			item.ref.current?.focus();
		}
	}, [
		getSortedItems,
		focusableItemId,
		findEnabledItem
	]);
	const onItemFocus = import_react.useCallback((id) => {
		if (focusableItemId !== id) setFocusableItemId(id);
	}, [focusableItemId, setFocusableItemId]);
	const onItemDisabled = import_react.useCallback((id) => {
		const sortedItems = getSortedItems();
		const newIndex = findEnabledItem(sortedItems.findIndex((item) => item.id === id), 1);
		if (newIndex >= 0 && newIndex < sortedItems.length) {
			const item = sortedItems[newIndex];
			setFocusableItemId(item.id);
			item.ref.current?.focus();
		}
	}, [getSortedItems, findEnabledItem]);
	import_react.useEffect(() => {
		focusableItemIdRef.current = focusableItemId;
	}, [focusableItemId]);
	import_react.useEffect(() => {
		const sortedItems = getSortedItems();
		if (sortedItems.length > 0) {
			if (!focusableItemIdRef.current) {
				setFocusableItemId(sortedItems[0].id);
				return;
			}
			const focusableItemIndex = sortedItems.findIndex((item) => item.id === focusableItemIdRef.current);
			if (!sortedItems[focusableItemIndex]) {
				const item = sortedItems[sortedItems.length - 1];
				if (item) {
					setFocusableItemId(item.id);
					item.ref.current?.focus();
				}
			} else if (focusableItemIndex === -1) {
				const item = sortedItems[focusableItemIndex];
				if (item) {
					setFocusableItemId(item.id);
					item.ref.current?.focus();
				}
			}
		}
	}, [getSortedItems, findEnabledItem]);
	const contextValue = import_react.useMemo(() => ({
		focusableItemId,
		registerItem,
		unregisterItem,
		onItemKeyDown,
		onItemFocus,
		onItemDisabled
	}), [
		focusableItemId,
		registerItem,
		unregisterItem,
		onItemKeyDown,
		onItemFocus,
		onItemDisabled
	]);
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ToolbarContext.Provider, {
		value: contextValue,
		children
	});
}
function sortByDocumentPosition(a, b) {
	if (!a.ref.current || !b.ref.current) return 0;
	const position = a.ref.current.compareDocumentPosition(b.ref.current);
	if (!position) return 0;
	if (position & Node.DOCUMENT_POSITION_FOLLOWING || position & Node.DOCUMENT_POSITION_CONTAINED_BY) return -1;
	if (position & Node.DOCUMENT_POSITION_PRECEDING || position & Node.DOCUMENT_POSITION_CONTAINS) return 1;
	return 0;
}
//#endregion
//#region node_modules/@mui/x-internals/ToolbarContext/useRegisterToolbarButton.mjs
function useRegisterToolbarButton(props, ref) {
	const { onKeyDown, onFocus, disabled, "aria-disabled": ariaDisabled } = props;
	const id = useId();
	const { focusableItemId, registerItem, unregisterItem, onItemKeyDown, onItemFocus, onItemDisabled } = useToolbarContext();
	const handleKeyDown = (event) => {
		onItemKeyDown(event);
		onKeyDown?.(event);
	};
	const handleFocus = (event) => {
		onItemFocus(id);
		onFocus?.(event);
	};
	import_react.useEffect(() => {
		registerItem(id, ref);
		return () => unregisterItem(id);
	}, [
		id,
		ref,
		registerItem,
		unregisterItem
	]);
	const previousDisabled = import_react.useRef(disabled);
	import_react.useEffect(() => {
		if (previousDisabled.current !== disabled && disabled === true) onItemDisabled(id, disabled);
		previousDisabled.current = disabled;
	}, [
		disabled,
		id,
		onItemDisabled
	]);
	const previousAriaDisabled = import_react.useRef(ariaDisabled);
	import_react.useEffect(() => {
		if (previousAriaDisabled.current !== ariaDisabled && ariaDisabled === true) onItemDisabled(id, true);
		previousAriaDisabled.current = ariaDisabled;
	}, [
		ariaDisabled,
		id,
		onItemDisabled
	]);
	return {
		tabIndex: focusableItemId === id ? 0 : -1,
		disabled,
		"aria-disabled": ariaDisabled,
		onKeyDown: handleKeyDown,
		onFocus: handleFocus
	};
}
//#endregion
//#region node_modules/@mui/x-charts/Toolbar/Toolbar.mjs
var _excluded$25 = ["className", "render"];
var ToolbarRoot = styled("div", {
	name: "MuiChartsToolbar",
	slot: "Root"
})(({ theme }) => ({
	flex: 0,
	display: "flex",
	alignItems: "center",
	justifyContent: "end",
	gap: theme.spacing(.25),
	padding: theme.spacing(.5),
	marginBottom: theme.spacing(1.5),
	minHeight: 44,
	boxSizing: "border-box",
	border: `1px solid ${(theme.vars || theme).palette.divider}`,
	borderRadius: 4
}));
var Toolbar = /*#__PURE__*/ import_react.forwardRef(function Toolbar(_ref, ref) {
	let { className, render } = _ref, other = _objectWithoutPropertiesLoose(_ref, _excluded$25);
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ToolbarContextProvider, { children: useComponentRenderer(ToolbarRoot, render, _extends({
		role: "toolbar",
		"aria-orientation": "horizontal",
		className: clsx(chartsToolbarClasses.root, className)
	}, other, { ref })) });
});
Toolbar.displayName = "Toolbar";
Toolbar.propTypes = {
	className: import_prop_types.default.string,
	/**
	* A function to customize rendering of the component.
	*/
	render: import_prop_types.default.oneOfType([import_prop_types.default.element, import_prop_types.default.func])
};
//#endregion
//#region node_modules/@mui/x-charts/Toolbar/ToolbarButton.mjs
var _excluded$24 = [
	"render",
	"onKeyDown",
	"onFocus",
	"disabled",
	"aria-disabled"
], _excluded2$1 = ["tabIndex"];
var ToolbarButton = /*#__PURE__*/ import_react.forwardRef(function ToolbarButton(props, ref) {
	const { render } = props, other = _objectWithoutPropertiesLoose(props, _excluded$24);
	const { slots, slotProps } = useChartsSlots();
	const buttonRef = import_react.useRef(null);
	const handleRef = useForkRef(buttonRef, ref);
	const _useRegisterToolbarBu = useRegisterToolbarButton(props, buttonRef), { tabIndex } = _useRegisterToolbarBu, toolbarButtonProps = _objectWithoutPropertiesLoose(_useRegisterToolbarBu, _excluded2$1);
	const element = useComponentRenderer(slots.baseIconButton, render, _extends({}, slotProps?.baseIconButton, { tabIndex }, other, toolbarButtonProps, { ref: handleRef }));
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(import_react.Fragment, { children: element });
});
ToolbarButton.displayName = "ToolbarButton";
ToolbarButton.propTypes = {
	className: import_prop_types.default.string,
	disabled: import_prop_types.default.bool,
	id: import_prop_types.default.string,
	/**
	* A function to customize the rendering of the component.
	*/
	render: import_prop_types.default.oneOfType([import_prop_types.default.element, import_prop_types.default.func]),
	size: import_prop_types.default.oneOf([
		"large",
		"medium",
		"small"
	]),
	style: import_prop_types.default.object,
	tabIndex: import_prop_types.default.number
};
//#endregion
//#region node_modules/@mui/x-charts/BarChart/FocusedBar.mjs
function FocusedBar(props) {
	const theme = useTheme();
	const focusedItem = useFocusedItem();
	const barSeries = useBarSeriesContext();
	const { xAxis, xAxisIds } = useXAxes();
	const { yAxis, yAxisIds } = useYAxes();
	if (focusedItem === null || focusedItem.type !== "bar" || !barSeries) return null;
	const series = barSeries.series[focusedItem.seriesId];
	if (!series || series.hidden) return null;
	if (series.data[focusedItem.dataIndex] == null) return null;
	const xAxisId = series.xAxisId ?? xAxisIds[0];
	const yAxisId = series.yAxisId ?? yAxisIds[0];
	const xAxisConfig = xAxis[xAxisId];
	const yAxisConfig = yAxis[yAxisId];
	const verticalLayout = barSeries.series[focusedItem.seriesId].layout === "vertical";
	const groupIndex = barSeries.stackingGroups.findIndex((group) => group.ids.includes(focusedItem.seriesId));
	const barDimensions = createGetBarDimensions({
		verticalLayout,
		xAxisConfig,
		yAxisConfig,
		series,
		numberOfGroups: barSeries.stackingGroups.length
	})(focusedItem.dataIndex, groupIndex);
	if (barDimensions === null) return null;
	const { x, y, height, width } = barDimensions;
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)("rect", _extends({
		fill: "none",
		stroke: (theme.vars ?? theme).palette.text.primary,
		strokeWidth: 2,
		x: x - 3,
		y: y - 3,
		width: width + 6,
		height: height + 6,
		rx: 3,
		ry: 3
	}, props));
}
//#endregion
//#region node_modules/@mui/x-charts/BarChart/BarChart.mjs
/**
* Demos:
*
* - [Bars](https://mui.com/x/react-charts/bars/)
* - [Bar demonstration](https://mui.com/x/react-charts/bar-demo/)
* - [Stacking](https://mui.com/x/react-charts/stacking/)
*
* API:
*
* - [BarChart API](https://mui.com/x/api/charts/bar-chart/)
*/
var BarChart = /*#__PURE__*/ import_react.forwardRef(function BarChart(inProps, ref) {
	const props = useThemeProps({
		props: inProps,
		name: "MuiBarChart"
	});
	const { chartsWrapperProps, chartsContainerProps, barPlotProps, gridProps, clipPathProps, clipPathGroupProps, overlayProps, chartsAxisProps, axisHighlightProps, legendProps, children } = useBarChartProps(props);
	const { chartsDataProviderProps, chartsSurfaceProps } = useChartsContainerProps(chartsContainerProps);
	const Tooltip = props.slots?.tooltip ?? ChartsTooltip;
	const Toolbar = props.slots?.toolbar;
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ChartsDataProvider, _extends({}, chartsDataProviderProps, { children: /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(ChartsWrapper, _extends({}, chartsWrapperProps, {
		ref,
		children: [
			props.showToolbar && Toolbar ? /*#__PURE__*/ (0, import_jsx_runtime.jsx)(Toolbar, _extends({}, props.slotProps?.toolbar)) : null,
			!props.hideLegend && /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ChartsLegend, _extends({}, legendProps)),
			/*#__PURE__*/ (0, import_jsx_runtime.jsxs)(ChartsSurface, _extends({}, chartsSurfaceProps, { children: [
				/*#__PURE__*/ (0, import_jsx_runtime.jsx)(ChartsGrid, _extends({}, gridProps)),
				/*#__PURE__*/ (0, import_jsx_runtime.jsxs)("g", _extends({}, clipPathGroupProps, { children: [
					/*#__PURE__*/ (0, import_jsx_runtime.jsx)(BarPlot, _extends({}, barPlotProps)),
					/*#__PURE__*/ (0, import_jsx_runtime.jsx)(ChartsOverlay, _extends({}, overlayProps)),
					/*#__PURE__*/ (0, import_jsx_runtime.jsx)(ChartsAxisHighlight, _extends({}, axisHighlightProps)),
					/*#__PURE__*/ (0, import_jsx_runtime.jsx)(FocusedBar, {})
				] })),
				/*#__PURE__*/ (0, import_jsx_runtime.jsx)(ChartsAxis, _extends({}, chartsAxisProps)),
				/*#__PURE__*/ (0, import_jsx_runtime.jsx)(ChartsClipPath, _extends({}, clipPathProps)),
				children
			] })),
			!props.loading && /*#__PURE__*/ (0, import_jsx_runtime.jsx)(Tooltip, _extends({}, props.slotProps?.tooltip))
		]
	})) }));
});
BarChart.displayName = "BarChart";
BarChart.propTypes = {
	apiRef: import_prop_types.default.shape({ current: import_prop_types.default.object }),
	/**
	* A gap added between axes when multiple axes are rendered on the same side of the chart.
	* @default 0
	*/
	axesGap: import_prop_types.default.number,
	/**
	* The configuration of axes highlight.
	* Default is set to 'band' in the bar direction.
	* Depends on `layout` prop.
	* @see See {@link https://mui.com/x/react-charts/highlighting/ highlighting docs} for more details.
	*/
	axisHighlight: import_prop_types.default.shape({
		x: import_prop_types.default.oneOf([
			"band",
			"line",
			"none"
		]),
		y: import_prop_types.default.oneOf([
			"band",
			"line",
			"none"
		])
	}),
	/**
	* Defines the border radius of the bar element.
	*/
	borderRadius: import_prop_types.default.number,
	/**
	* Configuration for the brush interaction.
	*/
	brushConfig: import_prop_types.default.shape({
		enabled: import_prop_types.default.bool,
		preventHighlight: import_prop_types.default.bool,
		preventTooltip: import_prop_types.default.bool
	}),
	children: import_prop_types.default.node,
	className: import_prop_types.default.string,
	/**
	* Color palette used to colorize multiple series.
	* @default rainbowSurgePalette
	*/
	colors: import_prop_types.default.oneOfType([import_prop_types.default.arrayOf(import_prop_types.default.string), import_prop_types.default.func]),
	/**
	* An array of objects that can be used to populate series and axes data using their `dataKey` property.
	*/
	dataset: import_prop_types.default.arrayOf(import_prop_types.default.object),
	/**
	* The description of the chart.
	* Used to provide an accessible description for the chart.
	*/
	desc: import_prop_types.default.string,
	/**
	* If `true`, the charts will not listen to the mouse move event.
	* It might break interactive features, but will improve performance.
	* @default false
	*/
	disableAxisListener: import_prop_types.default.bool,
	/**
	* If `true`, disables keyboard navigation for the chart.
	*/
	disableKeyboardNavigation: import_prop_types.default.bool,
	/**
	* Options to enable features planned for the next major.
	*/
	experimentalFeatures: import_prop_types.default.object,
	/**
	* Option to display a cartesian grid in the background.
	*/
	grid: import_prop_types.default.shape({
		horizontal: import_prop_types.default.bool,
		vertical: import_prop_types.default.bool
	}),
	/**
	* The height of the chart in px. If not defined, it takes the height of the parent element.
	*/
	height: import_prop_types.default.number,
	/**
	* List of hidden series and/or items.
	*
	* Different chart types use different keys.
	*
	* @example
	* ```ts
	* [
	*   {
	*     type: 'pie',
	*     seriesId: 'series-1',
	*     dataIndex: 3,
	*   },
	*   {
	*     type: 'line',
	*     seriesId: 'series-2',
	*   }
	* ]
	* ```
	*/
	hiddenItems: import_prop_types.default.arrayOf(import_prop_types.default.oneOfType([import_prop_types.default.shape({
		dataIndex: import_prop_types.default.number,
		seriesId: import_prop_types.default.string.isRequired,
		type: import_prop_types.default.oneOf(["bar"]).isRequired
	}), import_prop_types.default.shape({
		dataIndex: import_prop_types.default.number,
		seriesId: import_prop_types.default.string.isRequired,
		type: import_prop_types.default.oneOf(["bar"])
	})]).isRequired),
	/**
	* If `true`, the legend is not rendered.
	*/
	hideLegend: import_prop_types.default.bool,
	/**
	* The controlled axis highlight.
	* Identified by the axis id, and data index.
	*/
	highlightedAxis: import_prop_types.default.arrayOf(import_prop_types.default.shape({
		axisId: import_prop_types.default.oneOfType([import_prop_types.default.number, import_prop_types.default.string]).isRequired,
		dataIndex: import_prop_types.default.number.isRequired
	})),
	/**
	* The highlighted item.
	* Used when the highlight is controlled.
	*/
	highlightedItem: import_prop_types.default.oneOfType([import_prop_types.default.shape({
		dataIndex: import_prop_types.default.number,
		seriesId: import_prop_types.default.string.isRequired
	}), import_prop_types.default.shape({
		dataIndex: import_prop_types.default.number,
		seriesId: import_prop_types.default.string.isRequired,
		type: import_prop_types.default.oneOf(["bar"]).isRequired
	})]),
	/**
	* This prop is used to help implement the accessibility logic.
	* If you don't provide this prop. It falls back to a randomly generated id.
	*/
	id: import_prop_types.default.string,
	/**
	* List of initially hidden series and/or items.
	* Used for uncontrolled state.
	*
	* Different chart types use different keys.
	*
	* @example
	* ```ts
	* [
	*   {
	*     type: 'pie',
	*     seriesId: 'series-1',
	*     dataIndex: 3,
	*   },
	*   {
	*     type: 'line',
	*     seriesId: 'series-2',
	*   }
	* ]
	* ```
	*/
	initialHiddenItems: import_prop_types.default.arrayOf(import_prop_types.default.oneOfType([import_prop_types.default.shape({
		dataIndex: import_prop_types.default.number,
		seriesId: import_prop_types.default.string.isRequired,
		type: import_prop_types.default.oneOf(["bar"]).isRequired
	}), import_prop_types.default.shape({
		dataIndex: import_prop_types.default.number,
		seriesId: import_prop_types.default.string.isRequired,
		type: import_prop_types.default.oneOf(["bar"])
	})]).isRequired),
	/**
	* The direction of the bar elements.
	* @default 'vertical'
	*/
	layout: import_prop_types.default.oneOf(["horizontal", "vertical"]),
	/**
	* If `true`, a loading overlay is displayed.
	* @default false
	*/
	loading: import_prop_types.default.bool,
	/**
	* Localized text for chart components.
	*/
	localeText: import_prop_types.default.object,
	/**
	* The margin between the SVG and the drawing area.
	* It's used for leaving some space for extra information such as the x- and y-axis or legend.
	*
	* Accepts a `number` to be used on all sides or an object with the optional properties: `top`, `bottom`, `left`, and `right`.
	*/
	margin: import_prop_types.default.oneOfType([import_prop_types.default.number, import_prop_types.default.shape({
		bottom: import_prop_types.default.number,
		left: import_prop_types.default.number,
		right: import_prop_types.default.number,
		top: import_prop_types.default.number
	})]),
	/**
	* The function called for onClick events.
	* The second argument contains information about all line/bar elements at the current mouse position.
	* @param {MouseEvent} event The mouse event recorded on the `<svg/>` element.
	* @param {null | ChartsAxisData} data The data about the clicked axis and items associated with it.
	*/
	onAxisClick: import_prop_types.default.func,
	/**
	* Callback fired when any hidden identifiers change.
	* @param {VisibilityIdentifierWithType[]} hiddenItems The new list of hidden identifiers.
	*/
	onHiddenItemsChange: import_prop_types.default.func,
	/**
	* The callback fired when the highlighted item changes.
	*
	* @param {HighlightItemIdentifierWithType<SeriesType> | null} highlightedItem  The newly highlighted item.
	*/
	onHighlightChange: import_prop_types.default.func,
	/**
	* The function called when the pointer position corresponds to a new axis data item.
	* This update can either be caused by a pointer movement, or an axis update.
	* In case of multiple axes, the function is called if at least one axis is updated.
	* The argument contains the identifier for all axes with a `data` property.
	* @param {AxisItemIdentifier[]} axisItems The array of axes item identifiers.
	*/
	onHighlightedAxisChange: import_prop_types.default.func,
	/**
	* Callback fired when a bar item is clicked.
	* @param {MouseEvent | React.MouseEvent<SVGElement, MouseEvent>} event The event source of the callback.
	*        It is a native MouseEvent for `svg-batch` renderer and a React MouseEvent for `svg-single` renderer.
	* @param {BarItemIdentifier} barItemIdentifier The bar item identifier.
	*/
	onItemClick: import_prop_types.default.func,
	/**
	* The function called when the pointer position corresponds to a new axis data item.
	* This update can either be caused by a pointer movement, or an axis update.
	* In case of multiple axes, the function is called if at least one axis is updated.
	* The argument contains the identifier for all axes with a `data` property.
	* @param {AxisItemIdentifier[]} axisItems The array of axes item identifiers.
	*/
	onTooltipAxisChange: import_prop_types.default.func,
	/**
	* The callback fired when the tooltip item changes.
	*
	* @param {SeriesItemIdentifier<SeriesType> | null} tooltipItem  The newly highlighted item.
	*/
	onTooltipItemChange: import_prop_types.default.func,
	/**
	* The type of renderer to use for the bar plot.
	* - `svg-single`: Renders every bar in a `<rect />` element.
	* - `svg-batch`: Batch renders bars in `<path />` elements for better performance with large datasets, at the cost of some limitations.
	*                Read more: https://mui.com/x/react-charts/bars/#performance
	*
	* @default 'svg-single'
	*/
	renderer: import_prop_types.default.oneOf(["svg-batch", "svg-single"]),
	/**
	* The series to display in the bar chart.
	* An array of [[BarSeries]] objects.
	*/
	series: import_prop_types.default.arrayOf(import_prop_types.default.object).isRequired,
	/**
	* If true, shows the default chart toolbar.
	* @default false
	*/
	showToolbar: import_prop_types.default.bool,
	/**
	* If `true`, animations are skipped.
	* If unset or `false`, the animations respects the user's `prefers-reduced-motion` setting.
	*/
	skipAnimation: import_prop_types.default.bool,
	/**
	* The props used for each component slot.
	* @default {}
	*/
	slotProps: import_prop_types.default.object,
	/**
	* Overridable component slots.
	* @default {}
	*/
	slots: import_prop_types.default.object,
	sx: import_prop_types.default.oneOfType([
		import_prop_types.default.arrayOf(import_prop_types.default.oneOfType([
			import_prop_types.default.func,
			import_prop_types.default.object,
			import_prop_types.default.bool
		])),
		import_prop_types.default.func,
		import_prop_types.default.object
	]),
	theme: import_prop_types.default.oneOf(["dark", "light"]),
	/**
	* The title of the chart.
	* Used to provide an accessible label for the chart.
	*/
	title: import_prop_types.default.string,
	/**
	* The controlled axis tooltip.
	* Identified by the axis id, and data index.
	*/
	tooltipAxis: import_prop_types.default.arrayOf(import_prop_types.default.shape({
		axisId: import_prop_types.default.oneOfType([import_prop_types.default.number, import_prop_types.default.string]).isRequired,
		dataIndex: import_prop_types.default.number.isRequired
	})),
	/**
	* The tooltip item.
	* Used when the tooltip is controlled.
	*/
	tooltipItem: import_prop_types.default.oneOfType([import_prop_types.default.shape({
		dataIndex: import_prop_types.default.number.isRequired,
		seriesId: import_prop_types.default.string.isRequired
	}), import_prop_types.default.shape({
		dataIndex: import_prop_types.default.number.isRequired,
		seriesId: import_prop_types.default.string.isRequired,
		type: import_prop_types.default.oneOf(["bar"]).isRequired
	})]),
	/**
	* The width of the chart in px. If not defined, it takes the width of the parent element.
	*/
	width: import_prop_types.default.number,
	/**
	* The configuration of the x-axes.
	* If not provided, a default axis config is used.
	* An array of [[AxisConfig]] objects.
	*/
	xAxis: import_prop_types.default.arrayOf(import_prop_types.default.object),
	/**
	* The configuration of the y-axes.
	* If not provided, a default axis config is used.
	* An array of [[AxisConfig]] objects.
	*/
	yAxis: import_prop_types.default.arrayOf(import_prop_types.default.object)
};
//#endregion
//#region node_modules/@mui/x-charts/PieChart/pieClasses.mjs
function getPieUtilityClass(slot) {
	return generateUtilityClass("MuiPieChart", slot);
}
var pieClasses = generateUtilityClasses("MuiPieChart", [
	"root",
	"series",
	"seriesLabels",
	"arc",
	"arcLabel",
	"animate",
	"focusIndicator"
]);
var useUtilityClasses$7 = (options) => {
	const { classes, skipAnimation } = options ?? {};
	return composeClasses({
		root: ["root"],
		series: ["series"],
		seriesLabels: ["seriesLabels"],
		arc: ["arc"],
		arcLabel: ["arcLabel", !skipAnimation && "animate"],
		focusIndicator: ["focusIndicator"]
	}, getPieUtilityClass, classes);
};
//#endregion
//#region node_modules/@mui/x-charts/PieChart/PieArc.mjs
var _excluded$23 = [
	"className",
	"classes",
	"color",
	"dataIndex",
	"seriesId",
	"isFaded",
	"isHighlighted",
	"isFocused",
	"onClick",
	"cornerRadius",
	"startAngle",
	"endAngle",
	"innerRadius",
	"outerRadius",
	"paddingAngle",
	"skipAnimation",
	"stroke",
	"skipInteraction"
];
var PieArcRoot = styled("path", {
	name: "MuiPieArc",
	slot: "Root"
})({
	transitionProperty: "opacity, fill, filter",
	transitionDuration: `300ms`,
	transitionTimingFunction: ANIMATION_TIMING_FUNCTION
});
var PieArc = /*#__PURE__*/ import_react.forwardRef(function PieArc(props, ref) {
	const { className, classes: innerClasses, color, dataIndex, seriesId, isFaded, isHighlighted, isFocused, onClick, cornerRadius, startAngle, endAngle, innerRadius, outerRadius, paddingAngle, skipAnimation, stroke: strokeProp } = props, other = _objectWithoutPropertiesLoose(props, _excluded$23);
	const theme = useTheme();
	const stroke = strokeProp ?? (theme.vars || theme).palette.background.paper;
	const ownerState = {
		seriesId,
		dataIndex,
		classes: innerClasses,
		color,
		isFaded,
		isHighlighted,
		isFocused
	};
	const classes = useUtilityClasses$7(ownerState);
	const interactionProps = useInteractionItemProps({
		type: "pie",
		seriesId,
		dataIndex
	});
	const animatedProps = useAnimatePieArc({
		cornerRadius,
		startAngle,
		endAngle,
		innerRadius,
		outerRadius,
		paddingAngle,
		skipAnimation,
		ref
	});
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(PieArcRoot, _extends({
		onClick,
		cursor: onClick ? "pointer" : "unset",
		ownerState,
		className: clsx(classes.arc, className),
		fill: color,
		opacity: isFaded ? .3 : 1,
		filter: isHighlighted ? "brightness(120%)" : "none",
		stroke,
		strokeWidth: 1,
		strokeLinejoin: "round",
		"data-highlighted": isHighlighted || void 0,
		"data-faded": isFaded || void 0,
		"data-index": dataIndex
	}, other, interactionProps, animatedProps));
});
PieArc.displayName = "PieArc";
PieArc.propTypes = {
	classes: import_prop_types.default.object,
	cornerRadius: import_prop_types.default.number.isRequired,
	dataIndex: import_prop_types.default.number.isRequired,
	endAngle: import_prop_types.default.number.isRequired,
	innerRadius: import_prop_types.default.number.isRequired,
	isFaded: import_prop_types.default.bool.isRequired,
	isFocused: import_prop_types.default.bool.isRequired,
	isHighlighted: import_prop_types.default.bool.isRequired,
	outerRadius: import_prop_types.default.number.isRequired,
	paddingAngle: import_prop_types.default.number.isRequired,
	seriesId: import_prop_types.default.string.isRequired,
	/**
	* If `true`, the animation is disabled.
	*/
	skipAnimation: import_prop_types.default.bool,
	/**
	* If `true`, the default event handlers are disabled.
	* Those are used, for example, to display a tooltip or highlight the arc on hover.
	*/
	skipInteraction: import_prop_types.default.bool,
	startAngle: import_prop_types.default.number.isRequired
};
//#endregion
//#region node_modules/@mui/x-charts/hooks/useIsItemFocusedGetter.mjs
/**
* A hook to check focus state of multiple items.
* If you're interested by a single one, consider using `useIsItemFocused`.
*
* @returns {(item: FocusedItemData) => boolean} callback to get the state of the item.
*/
function useIsItemFocusedGetter() {
	const focusedItem = useFocusedItem();
	return (item) => focusedItem !== null && fastObjectShallowCompare(focusedItem, item);
}
//#endregion
//#region node_modules/@mui/x-charts/PieChart/dataTransform/getModifiedArcProperties.mjs
/**
* Function that returns arc properties after applying transformation from highlight/fade states.
*/
function getModifiedArcProperties(seriesDef, seriesLayout, isHighlighted, isFaded) {
	const { faded, highlighted, paddingAngle: basePaddingAngle = 0, cornerRadius: baseCornerRadius = 0 } = seriesDef;
	const { radius: { inner: baseInnerRadius = 0, label: baseArcLabelRadius, outer: baseOuterRadius } } = seriesLayout;
	const attributesOverride = _extends({ additionalRadius: 0 }, isFaded && faded || isHighlighted && highlighted || {});
	const paddingAngle = Math.max(0, deg2rad(attributesOverride.paddingAngle ?? basePaddingAngle));
	const innerRadius = Math.max(0, attributesOverride.innerRadius ?? baseInnerRadius);
	const outerRadius = Math.max(0, attributesOverride.outerRadius ?? baseOuterRadius + attributesOverride.additionalRadius);
	return {
		paddingAngle,
		innerRadius,
		outerRadius,
		cornerRadius: attributesOverride.cornerRadius ?? baseCornerRadius,
		arcLabelRadius: attributesOverride.arcLabelRadius ?? baseArcLabelRadius ?? (innerRadius + outerRadius) / 2
	};
}
//#endregion
//#region node_modules/@mui/x-charts/PieChart/dataTransform/useTransformData.mjs
function useTransformData(series) {
	const { id: seriesId, data, faded, highlighted } = series;
	const getHighlightState = useItemHighlightStateGetter();
	const isItemFocused = useIsItemFocusedGetter();
	return import_react.useMemo(() => data.map((item, itemIndex) => {
		const identifier = {
			type: "pie",
			seriesId,
			dataIndex: itemIndex
		};
		const highlightState = getHighlightState(identifier);
		const isHighlighted = highlightState === "highlighted";
		const isFaded = highlightState === "faded";
		const isFocused = isItemFocused(identifier);
		const arcSizes = getModifiedArcProperties(series, { radius: {
			inner: series.innerRadius ?? 0,
			outer: series.outerRadius,
			label: series.arcLabelRadius ?? 0,
			available: 0
		} }, isHighlighted, isFaded);
		return _extends({}, item, _extends({ additionalRadius: 0 }, isFaded && faded || isHighlighted && highlighted || {}), {
			dataIndex: itemIndex,
			isFaded,
			isHighlighted,
			isFocused
		}, arcSizes);
	}), [
		data,
		seriesId,
		getHighlightState,
		isItemFocused,
		series,
		faded,
		highlighted
	]);
}
//#endregion
//#region node_modules/@mui/x-charts/PieChart/PieArcPlot.mjs
var _excluded$22 = [
	"slots",
	"slotProps",
	"innerRadius",
	"outerRadius",
	"cornerRadius",
	"paddingAngle",
	"seriesId",
	"highlighted",
	"faded",
	"data",
	"onItemClick",
	"skipAnimation"
];
var PieArcPlotRoot = styled("g", {
	name: "MuiPieArcPlot",
	slot: "Root"
})();
function PieArcPlot(props) {
	const { slots, slotProps, innerRadius = 0, outerRadius, cornerRadius = 0, paddingAngle = 0, seriesId, highlighted, faded = { additionalRadius: -5 }, data, onItemClick, skipAnimation } = props, other = _objectWithoutPropertiesLoose(props, _excluded$22);
	const transformedData = useTransformData({
		innerRadius,
		outerRadius,
		cornerRadius,
		paddingAngle,
		id: seriesId,
		highlighted,
		faded,
		data
	});
	if (data.length === 0) return null;
	const Arc = slots?.pieArc ?? PieArc;
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(PieArcPlotRoot, _extends({}, other, { children: transformedData.map((item, index) => /*#__PURE__*/ (0, import_jsx_runtime.jsx)(Arc, _extends({
		startAngle: item.startAngle,
		endAngle: item.endAngle,
		paddingAngle: item.paddingAngle,
		innerRadius: item.innerRadius,
		outerRadius: item.outerRadius,
		cornerRadius: item.cornerRadius,
		skipAnimation: skipAnimation ?? false,
		seriesId,
		color: item.color,
		dataIndex: index,
		isFaded: item.isFaded,
		isHighlighted: item.isHighlighted,
		isFocused: item.isFocused,
		onClick: onItemClick && ((event) => {
			onItemClick(event, {
				type: "pie",
				seriesId,
				dataIndex: index
			}, item);
		})
	}, slotProps?.pieArc), item.dataIndex)) }));
}
PieArcPlot.propTypes = {
	/**
	* The radius between circle center and the arc label in px.
	* @default (innerRadius - outerRadius) / 2
	*/
	arcLabelRadius: import_prop_types.default.number,
	/**
	* The radius applied to arc corners (similar to border radius).
	* @default 0
	*/
	cornerRadius: import_prop_types.default.number,
	data: import_prop_types.default.arrayOf(import_prop_types.default.shape({
		color: import_prop_types.default.string.isRequired,
		endAngle: import_prop_types.default.number.isRequired,
		formattedValue: import_prop_types.default.string.isRequired,
		hidden: import_prop_types.default.bool.isRequired,
		id: import_prop_types.default.oneOfType([import_prop_types.default.number, import_prop_types.default.string]),
		index: import_prop_types.default.number.isRequired,
		label: import_prop_types.default.oneOfType([import_prop_types.default.func, import_prop_types.default.string]),
		labelMarkType: import_prop_types.default.oneOfType([import_prop_types.default.oneOf([
			"circle",
			"line",
			"line+mark",
			"square"
		]), import_prop_types.default.func]),
		padAngle: import_prop_types.default.number.isRequired,
		startAngle: import_prop_types.default.number.isRequired,
		value: import_prop_types.default.number.isRequired
	})).isRequired,
	/**
	* Override the arc attributes when it is faded.
	* @default { additionalRadius: -5 }
	*/
	faded: import_prop_types.default.shape({
		additionalRadius: import_prop_types.default.number,
		arcLabelRadius: import_prop_types.default.number,
		color: import_prop_types.default.string,
		cornerRadius: import_prop_types.default.number,
		innerRadius: import_prop_types.default.number,
		outerRadius: import_prop_types.default.number,
		paddingAngle: import_prop_types.default.number
	}),
	/**
	* Override the arc attributes when it is highlighted.
	*/
	highlighted: import_prop_types.default.shape({
		additionalRadius: import_prop_types.default.number,
		arcLabelRadius: import_prop_types.default.number,
		color: import_prop_types.default.string,
		cornerRadius: import_prop_types.default.number,
		innerRadius: import_prop_types.default.number,
		outerRadius: import_prop_types.default.number,
		paddingAngle: import_prop_types.default.number
	}),
	/**
	* The radius between circle center and the beginning of the arc.
	* @default 0
	*/
	innerRadius: import_prop_types.default.number,
	/**
	* Callback fired when a pie item is clicked.
	* @param {React.MouseEvent<SVGPathElement, MouseEvent>} event The event source of the callback.
	* @param {PieItemIdentifier} pieItemIdentifier The pie item identifier.
	* @param {DefaultizedPieValueType} item The pie item.
	*/
	onItemClick: import_prop_types.default.func,
	/**
	* The radius between circle center and the end of the arc.
	*/
	outerRadius: import_prop_types.default.number.isRequired,
	/**
	* The padding angle (deg) between two arcs.
	* @default 0
	*/
	paddingAngle: import_prop_types.default.number,
	/**
	* The id of this series.
	*/
	seriesId: import_prop_types.default.string.isRequired,
	/**
	* If `true`, animations are skipped.
	* @default false
	*/
	skipAnimation: import_prop_types.default.bool,
	/**
	* The props used for each component slot.
	* @default {}
	*/
	slotProps: import_prop_types.default.object,
	/**
	* Overridable component slots.
	* @default {}
	*/
	slots: import_prop_types.default.object
};
//#endregion
//#region node_modules/@mui/x-charts/PieChart/PieArcLabel.mjs
var _excluded$21 = [
	"seriesId",
	"classes",
	"color",
	"startAngle",
	"endAngle",
	"paddingAngle",
	"arcLabelRadius",
	"cornerRadius",
	"formattedArcLabel",
	"isHighlighted",
	"isFaded",
	"skipAnimation",
	"hidden",
	"className"
];
var PieArcLabelRoot = styled("text", {
	name: "MuiPieArcLabel",
	slot: "Root"
})(({ theme }) => ({
	fill: (theme.vars || theme).palette.text.primary,
	textAnchor: "middle",
	dominantBaseline: "middle",
	pointerEvents: "none",
	animationName: "animate-opacity",
	animationDuration: "0s",
	animationTimingFunction: ANIMATION_TIMING_FUNCTION,
	transitionDuration: `300ms`,
	transitionProperty: "opacity",
	transitionTimingFunction: ANIMATION_TIMING_FUNCTION,
	[`&.${pieClasses.animate}`]: { animationDuration: `300ms` },
	"@keyframes animate-opacity": { from: { opacity: 0 } }
}));
var PieArcLabel = /*#__PURE__*/ import_react.forwardRef(function PieArcLabel(props, ref) {
	const { seriesId, classes: innerClasses, color, startAngle, endAngle, paddingAngle, arcLabelRadius, cornerRadius, formattedArcLabel, isHighlighted, isFaded, skipAnimation, hidden, className } = props, other = _objectWithoutPropertiesLoose(props, _excluded$21);
	const classes = useUtilityClasses$7({
		seriesId,
		classes: innerClasses,
		color,
		isFaded,
		isHighlighted,
		skipAnimation
	});
	const animatedProps = useAnimatePieArcLabel({
		cornerRadius,
		startAngle,
		endAngle,
		arcLabelRadius,
		paddingAngle,
		skipAnimation,
		ref
	});
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(PieArcLabelRoot, _extends({
		className: clsx(classes.arcLabel, className),
		"data-highlighted": isHighlighted || void 0,
		"data-faded": isFaded || void 0
	}, other, animatedProps, {
		opacity: hidden ? 0 : 1,
		children: formattedArcLabel
	}));
});
PieArcLabel.displayName = "PieArcLabel";
PieArcLabel.propTypes = {
	arcLabelRadius: import_prop_types.default.number.isRequired,
	classes: import_prop_types.default.object,
	color: import_prop_types.default.string.isRequired,
	cornerRadius: import_prop_types.default.number.isRequired,
	endAngle: import_prop_types.default.number.isRequired,
	formattedArcLabel: import_prop_types.default.string,
	hidden: import_prop_types.default.bool,
	isFaded: import_prop_types.default.bool.isRequired,
	isHighlighted: import_prop_types.default.bool.isRequired,
	paddingAngle: import_prop_types.default.number.isRequired,
	seriesId: import_prop_types.default.string.isRequired,
	skipAnimation: import_prop_types.default.bool.isRequired,
	startAngle: import_prop_types.default.number.isRequired
};
//#endregion
//#region node_modules/@mui/x-charts/PieChart/PieArcLabelPlot.mjs
var _excluded$20 = [
	"arcLabel",
	"arcLabelMinAngle",
	"arcLabelRadius",
	"cornerRadius",
	"data",
	"faded",
	"highlighted",
	"seriesId",
	"innerRadius",
	"outerRadius",
	"paddingAngle",
	"skipAnimation",
	"slotProps",
	"slots"
];
var RATIO = 180 / Math.PI;
function getItemLabel(arcLabel, arcLabelMinAngle, item) {
	if (!arcLabel) return null;
	if ((item.endAngle - item.startAngle) * RATIO < arcLabelMinAngle) return null;
	switch (arcLabel) {
		case "label": return getLabel(item.label, "arc");
		case "value": return item.value?.toString();
		case "formattedValue": return item.formattedValue;
		default: return arcLabel(_extends({}, item, { label: getLabel(item.label, "arc") }));
	}
}
var PieArcLabelPlotRoot = styled("g", {
	name: "MuiPieArcLabelPlot",
	slot: "Root"
})();
function PieArcLabelPlot(props) {
	const { arcLabel, arcLabelMinAngle = 0, arcLabelRadius, cornerRadius = 0, data, faded = { additionalRadius: -5 }, highlighted, seriesId, innerRadius, outerRadius, paddingAngle = 0, skipAnimation, slotProps, slots } = props, other = _objectWithoutPropertiesLoose(props, _excluded$20);
	const transformedData = useTransformData({
		innerRadius,
		outerRadius,
		arcLabelRadius,
		cornerRadius,
		paddingAngle,
		id: seriesId,
		highlighted,
		faded,
		data
	});
	if (data.length === 0) return null;
	const ArcLabel = slots?.pieArcLabel ?? PieArcLabel;
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(PieArcLabelPlotRoot, _extends({}, other, { children: transformedData.map((item) => /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ArcLabel, _extends({
		startAngle: item.startAngle,
		endAngle: item.endAngle,
		paddingAngle: item.paddingAngle,
		arcLabelRadius: item.arcLabelRadius,
		cornerRadius: item.cornerRadius,
		hidden: item.hidden,
		seriesId,
		color: item.color,
		isFaded: item.isFaded,
		isHighlighted: item.isHighlighted,
		formattedArcLabel: getItemLabel(arcLabel, arcLabelMinAngle, item),
		skipAnimation: skipAnimation ?? false
	}, slotProps?.pieArcLabel), item.id ?? item.dataIndex)) }));
}
PieArcLabelPlot.propTypes = {
	/**
	* The label displayed into the arc.
	*/
	arcLabel: import_prop_types.default.oneOfType([import_prop_types.default.oneOf([
		"formattedValue",
		"label",
		"value"
	]), import_prop_types.default.func]),
	/**
	* The minimal angle required to display the arc label.
	* @default 0
	*/
	arcLabelMinAngle: import_prop_types.default.number,
	/**
	* The radius between circle center and the arc label in px.
	* @default (innerRadius - outerRadius) / 2
	*/
	arcLabelRadius: import_prop_types.default.number,
	/**
	* The radius applied to arc corners (similar to border radius).
	* @default 0
	*/
	cornerRadius: import_prop_types.default.number,
	data: import_prop_types.default.arrayOf(import_prop_types.default.shape({
		color: import_prop_types.default.string.isRequired,
		endAngle: import_prop_types.default.number.isRequired,
		formattedValue: import_prop_types.default.string.isRequired,
		hidden: import_prop_types.default.bool.isRequired,
		id: import_prop_types.default.oneOfType([import_prop_types.default.number, import_prop_types.default.string]),
		index: import_prop_types.default.number.isRequired,
		label: import_prop_types.default.oneOfType([import_prop_types.default.func, import_prop_types.default.string]),
		labelMarkType: import_prop_types.default.oneOfType([import_prop_types.default.oneOf([
			"circle",
			"line",
			"line+mark",
			"square"
		]), import_prop_types.default.func]),
		padAngle: import_prop_types.default.number.isRequired,
		startAngle: import_prop_types.default.number.isRequired,
		value: import_prop_types.default.number.isRequired
	})).isRequired,
	/**
	* Override the arc attributes when it is faded.
	* @default { additionalRadius: -5 }
	*/
	faded: import_prop_types.default.shape({
		additionalRadius: import_prop_types.default.number,
		arcLabelRadius: import_prop_types.default.number,
		color: import_prop_types.default.string,
		cornerRadius: import_prop_types.default.number,
		innerRadius: import_prop_types.default.number,
		outerRadius: import_prop_types.default.number,
		paddingAngle: import_prop_types.default.number
	}),
	/**
	* Override the arc attributes when it is highlighted.
	*/
	highlighted: import_prop_types.default.shape({
		additionalRadius: import_prop_types.default.number,
		arcLabelRadius: import_prop_types.default.number,
		color: import_prop_types.default.string,
		cornerRadius: import_prop_types.default.number,
		innerRadius: import_prop_types.default.number,
		outerRadius: import_prop_types.default.number,
		paddingAngle: import_prop_types.default.number
	}),
	/**
	* The radius between circle center and the beginning of the arc.
	* @default 0
	*/
	innerRadius: import_prop_types.default.number,
	/**
	* The radius between circle center and the end of the arc.
	*/
	outerRadius: import_prop_types.default.number.isRequired,
	/**
	* The padding angle (deg) between two arcs.
	* @default 0
	*/
	paddingAngle: import_prop_types.default.number,
	/**
	* The id of this series.
	*/
	seriesId: import_prop_types.default.string.isRequired,
	/**
	* If `true`, animations are skipped.
	* @default false
	*/
	skipAnimation: import_prop_types.default.bool,
	/**
	* The props used for each component slot.
	* @default {}
	*/
	slotProps: import_prop_types.default.object,
	/**
	* Overridable component slots.
	* @default {}
	*/
	slots: import_prop_types.default.object
};
//#endregion
//#region node_modules/@mui/x-charts/PieChart/PiePlot.mjs
/**
* Demos:
*
* - [Pie](https://mui.com/x/react-charts/pie/)
* - [Pie demonstration](https://mui.com/x/react-charts/pie-demo/)
*
* API:
*
* - [PiePlot API](https://mui.com/x/api/charts/pie-plot/)
*/
function PiePlot(props) {
	const { skipAnimation: inSkipAnimation, slots, slotProps, onItemClick, className } = props;
	const seriesData = usePieSeriesContext();
	const seriesLayout = usePieSeriesLayout();
	const skipAnimation = useSkipAnimation(inSkipAnimation);
	const classes = useUtilityClasses$7();
	if (seriesData === void 0) return null;
	const { series, seriesOrder } = seriesData;
	return /*#__PURE__*/ (0, import_jsx_runtime.jsxs)("g", {
		className: clsx(classes.root, className),
		children: [seriesOrder.map((seriesId) => {
			const { cornerRadius, paddingAngle, data, highlighted, faded } = series[seriesId];
			return /*#__PURE__*/ (0, import_jsx_runtime.jsx)("g", {
				className: classes.series,
				transform: `translate(${seriesLayout[seriesId].center.x}, ${seriesLayout[seriesId].center.y})`,
				"data-series": seriesId,
				children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(PieArcPlot, {
					innerRadius: seriesLayout[seriesId].radius.inner,
					outerRadius: seriesLayout[seriesId].radius.outer,
					cornerRadius,
					paddingAngle,
					seriesId,
					data,
					skipAnimation,
					highlighted,
					faded,
					onItemClick,
					slots,
					slotProps
				})
			}, seriesId);
		}), seriesOrder.map((seriesId) => {
			const { cornerRadius, paddingAngle, arcLabel, arcLabelMinAngle, data } = series[seriesId];
			return /*#__PURE__*/ (0, import_jsx_runtime.jsx)("g", {
				className: classes.seriesLabels,
				transform: `translate(${seriesLayout[seriesId].center.x}, ${seriesLayout[seriesId].center.y})`,
				"data-series": seriesId,
				children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(PieArcLabelPlot, {
					innerRadius: seriesLayout[seriesId].radius.inner,
					outerRadius: seriesLayout[seriesId].radius.outer,
					arcLabelRadius: seriesLayout[seriesId].radius.label,
					cornerRadius,
					paddingAngle,
					seriesId,
					data,
					skipAnimation,
					arcLabel,
					arcLabelMinAngle,
					slots,
					slotProps
				})
			}, seriesId);
		})]
	});
}
PiePlot.propTypes = {
	className: import_prop_types.default.string,
	/**
	* Callback fired when a pie item is clicked.
	* @param {React.MouseEvent<SVGPathElement, MouseEvent>} event The event source of the callback.
	* @param {PieItemIdentifier} pieItemIdentifier The pie item identifier.
	* @param {DefaultizedPieValueType} item The pie item.
	*/
	onItemClick: import_prop_types.default.func,
	/**
	* If `true`, animations are skipped.
	* @default false
	*/
	skipAnimation: import_prop_types.default.bool,
	/**
	* The props used for each component slot.
	* @default {}
	*/
	slotProps: import_prop_types.default.object,
	/**
	* Overridable component slots.
	* @default {}
	*/
	slots: import_prop_types.default.object
};
//#endregion
//#region node_modules/@mui/x-charts/PieChart/PieChart.plugins.mjs
var PIE_CHART_PLUGINS = [
	useChartTooltip,
	useChartInteraction,
	useChartHighlight,
	useChartVisibilityManager,
	useChartKeyboardNavigation
];
//#endregion
//#region node_modules/@mui/x-charts/PieChart/FocusedPieArc.mjs
var _excluded$19 = ["arcLabelRadius"];
function FocusedPieArc(props) {
	const theme = useTheme();
	const focusedItem = useFocusedItem();
	const pieSeriesLayout = usePieSeriesLayout();
	const highlightState = useItemHighlightState(focusedItem);
	const isHighlighted = highlightState === "highlighted";
	const isFaded = highlightState === "faded";
	const pieSeries = usePieSeriesContext();
	const classes = useUtilityClasses$7();
	if (focusedItem === null || focusedItem.type !== "pie" || !pieSeries) return null;
	const series = pieSeries?.series[focusedItem.seriesId];
	const { center, radius } = pieSeriesLayout[focusedItem.seriesId];
	if (!series || !center || !radius) return null;
	const item = series.data[focusedItem.dataIndex];
	if (!item || item.hidden) return null;
	const arcSizes = _objectWithoutPropertiesLoose(getModifiedArcProperties(series, pieSeriesLayout[focusedItem.seriesId], isHighlighted, isFaded), _excluded$19);
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(PieArc, _extends({
		transform: `translate(${pieSeriesLayout[series.id].center.x}, ${pieSeriesLayout[series.id].center.y})`,
		startAngle: item.startAngle,
		endAngle: item.endAngle,
		color: "transparent",
		pointerEvents: "none",
		skipInteraction: true,
		skipAnimation: true,
		stroke: (theme.vars ?? theme).palette.text.primary,
		seriesId: series.id,
		className: classes.focusIndicator,
		dataIndex: focusedItem.dataIndex,
		isFaded: false,
		isHighlighted: false,
		isFocused: false,
		strokeWidth: 3
	}, arcSizes, props));
}
FocusedPieArc.propTypes = {
	classes: import_prop_types.default.object,
	cornerRadius: import_prop_types.default.number,
	innerRadius: import_prop_types.default.number,
	outerRadius: import_prop_types.default.number,
	paddingAngle: import_prop_types.default.number,
	/**
	* If `true`, the animation is disabled.
	*/
	skipAnimation: import_prop_types.default.bool,
	/**
	* If `true`, the default event handlers are disabled.
	* Those are used, for example, to display a tooltip or highlight the arc on hover.
	*/
	skipInteraction: import_prop_types.default.bool
};
//#endregion
//#region node_modules/@mui/x-charts/PieChart/PieChart.mjs
var _excluded$18 = [
	"series",
	"width",
	"height",
	"margin",
	"colors",
	"sx",
	"skipAnimation",
	"hideLegend",
	"children",
	"slots",
	"slotProps",
	"onItemClick",
	"loading",
	"highlightedItem",
	"onHighlightChange",
	"className",
	"showToolbar"
];
/**
* Demos:
*
* - [Pie](https://mui.com/x/react-charts/pie/)
* - [Pie demonstration](https://mui.com/x/react-charts/pie-demo/)
*
* API:
*
* - [PieChart API](https://mui.com/x/api/charts/pie-chart/)
*/
var PieChart = /*#__PURE__*/ import_react.forwardRef(function PieChart(inProps, ref) {
	const props = useThemeProps({
		props: inProps,
		name: "MuiPieChart"
	});
	const { series, width, height, margin: marginProps, colors, sx, skipAnimation, hideLegend, children, slots, slotProps, onItemClick, loading, highlightedItem, onHighlightChange, className, showToolbar } = props, other = _objectWithoutPropertiesLoose(props, _excluded$18);
	const margin = defaultizeMargin(marginProps, DEFAULT_PIE_CHART_MARGIN);
	const { chartsDataProviderProps, chartsSurfaceProps } = useChartsContainerProps(_extends({}, other, {
		series: series.map((s) => _extends({ type: "pie" }, s)),
		width,
		height,
		margin,
		colors,
		highlightedItem,
		onHighlightChange,
		skipAnimation,
		plugins: PIE_CHART_PLUGINS
	}));
	const Tooltip = slots?.tooltip ?? ChartsTooltip;
	const Toolbar = slots?.toolbar;
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ChartsDataProvider, _extends({}, chartsDataProviderProps, { children: /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(ChartsWrapper, {
		legendPosition: slotProps?.legend?.position,
		legendDirection: slotProps?.legend?.direction ?? "vertical",
		sx,
		hideLegend: hideLegend ?? false,
		className,
		ref,
		children: [
			showToolbar && Toolbar ? /*#__PURE__*/ (0, import_jsx_runtime.jsx)(Toolbar, _extends({}, slotProps?.toolbar)) : null,
			!hideLegend && /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ChartsLegend, {
				direction: slotProps?.legend?.direction ?? "vertical",
				slots,
				slotProps
			}),
			/*#__PURE__*/ (0, import_jsx_runtime.jsxs)(ChartsSurface, _extends({}, chartsSurfaceProps, { children: [
				/*#__PURE__*/ (0, import_jsx_runtime.jsx)(PiePlot, {
					slots,
					slotProps,
					onItemClick
				}),
				/*#__PURE__*/ (0, import_jsx_runtime.jsx)(FocusedPieArc, {}),
				/*#__PURE__*/ (0, import_jsx_runtime.jsx)(ChartsOverlay, {
					loading,
					slots,
					slotProps
				}),
				children
			] })),
			!loading && /*#__PURE__*/ (0, import_jsx_runtime.jsx)(Tooltip, _extends({ trigger: "item" }, slotProps?.tooltip))
		]
	}) }));
});
PieChart.displayName = "PieChart";
PieChart.propTypes = {
	apiRef: import_prop_types.default.shape({ current: import_prop_types.default.object }),
	children: import_prop_types.default.node,
	className: import_prop_types.default.string,
	/**
	* Color palette used to colorize multiple series.
	* @default rainbowSurgePalette
	*/
	colors: import_prop_types.default.oneOfType([import_prop_types.default.arrayOf(import_prop_types.default.string), import_prop_types.default.func]),
	/**
	* An array of objects that can be used to populate series and axes data using their `dataKey` property.
	*/
	dataset: import_prop_types.default.arrayOf(import_prop_types.default.object),
	/**
	* The description of the chart.
	* Used to provide an accessible description for the chart.
	*/
	desc: import_prop_types.default.string,
	/**
	* If `true`, disables keyboard navigation for the chart.
	*/
	disableKeyboardNavigation: import_prop_types.default.bool,
	/**
	* Options to enable features planned for the next major.
	*/
	experimentalFeatures: import_prop_types.default.object,
	/**
	* The height of the chart in px. If not defined, it takes the height of the parent element.
	*/
	height: import_prop_types.default.number,
	/**
	* List of hidden series and/or items.
	*
	* Different chart types use different keys.
	*
	* @example
	* ```ts
	* [
	*   {
	*     type: 'pie',
	*     seriesId: 'series-1',
	*     dataIndex: 3,
	*   },
	*   {
	*     type: 'line',
	*     seriesId: 'series-2',
	*   }
	* ]
	* ```
	*/
	hiddenItems: import_prop_types.default.arrayOf(import_prop_types.default.oneOfType([import_prop_types.default.shape({
		dataIndex: import_prop_types.default.number,
		seriesId: import_prop_types.default.string.isRequired,
		type: import_prop_types.default.oneOf(["pie"]).isRequired
	}), import_prop_types.default.shape({
		dataIndex: import_prop_types.default.number,
		seriesId: import_prop_types.default.string.isRequired,
		type: import_prop_types.default.oneOf(["pie"])
	})]).isRequired),
	/**
	* If `true`, the legend is not rendered.
	*/
	hideLegend: import_prop_types.default.bool,
	/**
	* The highlighted item.
	* Used when the highlight is controlled.
	*/
	highlightedItem: import_prop_types.default.oneOfType([import_prop_types.default.shape({
		dataIndex: import_prop_types.default.number,
		seriesId: import_prop_types.default.string.isRequired
	}), import_prop_types.default.shape({
		dataIndex: import_prop_types.default.number,
		seriesId: import_prop_types.default.string.isRequired,
		type: import_prop_types.default.oneOf(["pie"]).isRequired
	})]),
	/**
	* This prop is used to help implement the accessibility logic.
	* If you don't provide this prop. It falls back to a randomly generated id.
	*/
	id: import_prop_types.default.string,
	/**
	* List of initially hidden series and/or items.
	* Used for uncontrolled state.
	*
	* Different chart types use different keys.
	*
	* @example
	* ```ts
	* [
	*   {
	*     type: 'pie',
	*     seriesId: 'series-1',
	*     dataIndex: 3,
	*   },
	*   {
	*     type: 'line',
	*     seriesId: 'series-2',
	*   }
	* ]
	* ```
	*/
	initialHiddenItems: import_prop_types.default.arrayOf(import_prop_types.default.oneOfType([import_prop_types.default.shape({
		dataIndex: import_prop_types.default.number,
		seriesId: import_prop_types.default.string.isRequired,
		type: import_prop_types.default.oneOf(["pie"]).isRequired
	}), import_prop_types.default.shape({
		dataIndex: import_prop_types.default.number,
		seriesId: import_prop_types.default.string.isRequired,
		type: import_prop_types.default.oneOf(["pie"])
	})]).isRequired),
	/**
	* If `true`, a loading overlay is displayed.
	* @default false
	*/
	loading: import_prop_types.default.bool,
	/**
	* Localized text for chart components.
	*/
	localeText: import_prop_types.default.object,
	/**
	* The margin between the SVG and the drawing area.
	* It's used for leaving some space for extra information such as the x- and y-axis or legend.
	*
	* Accepts a `number` to be used on all sides or an object with the optional properties: `top`, `bottom`, `left`, and `right`.
	*/
	margin: import_prop_types.default.oneOfType([import_prop_types.default.number, import_prop_types.default.shape({
		bottom: import_prop_types.default.number,
		left: import_prop_types.default.number,
		right: import_prop_types.default.number,
		top: import_prop_types.default.number
	})]),
	/**
	* Callback fired when any hidden identifiers change.
	* @param {VisibilityIdentifierWithType[]} hiddenItems The new list of hidden identifiers.
	*/
	onHiddenItemsChange: import_prop_types.default.func,
	/**
	* The callback fired when the highlighted item changes.
	*
	* @param {HighlightItemIdentifierWithType<SeriesType> | null} highlightedItem  The newly highlighted item.
	*/
	onHighlightChange: import_prop_types.default.func,
	/**
	* Callback fired when a pie arc is clicked.
	*/
	onItemClick: import_prop_types.default.func,
	/**
	* The callback fired when the tooltip item changes.
	*
	* @param {SeriesItemIdentifier<SeriesType> | null} tooltipItem  The newly highlighted item.
	*/
	onTooltipItemChange: import_prop_types.default.func,
	/**
	* The series to display in the pie chart.
	* An array of [[PieSeries]] objects.
	*/
	series: import_prop_types.default.arrayOf(import_prop_types.default.object).isRequired,
	/**
	* If true, shows the default chart toolbar.
	* @default false
	*/
	showToolbar: import_prop_types.default.bool,
	/**
	* If `true`, animations are skipped.
	* If unset or `false`, the animations respects the user's `prefers-reduced-motion` setting.
	*/
	skipAnimation: import_prop_types.default.bool,
	/**
	* The props used for each component slot.
	* @default {}
	*/
	slotProps: import_prop_types.default.object,
	/**
	* Overridable component slots.
	* @default {}
	*/
	slots: import_prop_types.default.object,
	sx: import_prop_types.default.oneOfType([
		import_prop_types.default.arrayOf(import_prop_types.default.oneOfType([
			import_prop_types.default.func,
			import_prop_types.default.object,
			import_prop_types.default.bool
		])),
		import_prop_types.default.func,
		import_prop_types.default.object
	]),
	theme: import_prop_types.default.oneOf(["dark", "light"]),
	/**
	* The title of the chart.
	* Used to provide an accessible label for the chart.
	*/
	title: import_prop_types.default.string,
	/**
	* The tooltip item.
	* Used when the tooltip is controlled.
	*/
	tooltipItem: import_prop_types.default.oneOfType([import_prop_types.default.shape({
		dataIndex: import_prop_types.default.number.isRequired,
		seriesId: import_prop_types.default.string.isRequired
	}), import_prop_types.default.shape({
		dataIndex: import_prop_types.default.number.isRequired,
		seriesId: import_prop_types.default.string.isRequired,
		type: import_prop_types.default.oneOf(["pie"]).isRequired
	})]),
	/**
	* The width of the chart in px. If not defined, it takes the width of the parent element.
	*/
	width: import_prop_types.default.number
};
//#endregion
//#region node_modules/@mui/x-charts/ScatterChart/ScatterMarker.mjs
var _excluded$17 = [
	"seriesId",
	"isFaded",
	"isHighlighted",
	"x",
	"y",
	"color",
	"size",
	"dataIndex"
];
function ScatterMarker(props) {
	const { isFaded, isHighlighted, x, y, color, size } = props, other = _objectWithoutPropertiesLoose(props, _excluded$17);
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)("circle", _extends({
		cx: 0,
		cy: 0,
		r: (isHighlighted ? 1.2 : 1) * size,
		transform: `translate(${x}, ${y})`,
		fill: color,
		opacity: isFaded ? .3 : 1,
		cursor: other.onClick ? "pointer" : "unset"
	}, other));
}
ScatterMarker.propTypes = {
	/**
	* The fill color of the marker.
	*/
	color: import_prop_types.default.string.isRequired,
	/**
	* The index of the data point.
	*/
	dataIndex: import_prop_types.default.number.isRequired,
	/**
	* If `true`, the marker is faded.
	*/
	isFaded: import_prop_types.default.bool.isRequired,
	/**
	* If `true`, the marker is highlighted.
	*/
	isHighlighted: import_prop_types.default.bool.isRequired,
	/**
	* Callback fired when clicking on a scatter item.
	* @param {MouseEvent} event Mouse event recorded on the `<svg/>` element.
	*/
	onClick: import_prop_types.default.func,
	/**
	* The series ID.
	*/
	seriesId: import_prop_types.default.string.isRequired,
	/**
	* The size of the marker.
	*/
	size: import_prop_types.default.number.isRequired,
	/**
	* The x coordinate of the data point.
	*/
	x: import_prop_types.default.number.isRequired,
	/**
	* The y coordinate of the data point.
	*/
	y: import_prop_types.default.number.isRequired
};
//#endregion
//#region node_modules/@mui/x-charts/ScatterChart/scatterClasses.mjs
function getScatterUtilityClass(slot) {
	return generateUtilityClass("MuiScatterChart", slot);
}
var scatterClasses = generateUtilityClasses("MuiScatterChart", [
	"root",
	"series",
	"marker",
	"focusedMark",
	"highlightedMark"
]);
var useUtilityClasses$6 = (options) => {
	const { classes } = options ?? {};
	return composeClasses({
		root: ["root"],
		series: ["series"],
		marker: ["marker"],
		focusedMark: ["focusedMark"],
		highlightedMark: ["highlightedMark"]
	}, getScatterUtilityClass, classes);
};
//#endregion
//#region node_modules/@mui/x-charts/ScatterChart/useScatterPlotData.mjs
function useScatterPlotData(series, xScale, yScale, isPointInside) {
	return import_react.useMemo(() => {
		const getXPosition = getValueToPositionMapper(xScale);
		const getYPosition = getValueToPositionMapper(yScale);
		const temp = [];
		for (let i = 0; i < series.data.length; i += 1) {
			const scatterPoint = series.data[i];
			const x = getXPosition(scatterPoint.x);
			const y = getYPosition(scatterPoint.y);
			if (isPointInside(x, y)) temp.push({
				x,
				y,
				id: scatterPoint.id,
				seriesId: series.id,
				type: "scatter",
				dataIndex: i
			});
		}
		return temp;
	}, [
		xScale,
		yScale,
		series.data,
		series.id,
		isPointInside
	]);
}
//#endregion
//#region node_modules/@mui/x-charts/ScatterChart/Scatter.mjs
/**
* @deprecated The `Scatter` component is an internal implementation detail of `ScatterPlot` and will be removed from the public API in v10. Use `ScatterPlot` instead.
*/
/**
* @deprecated The `Scatter` component is an internal implementation detail of `ScatterPlot` and will be removed from the public API in v10. Use `ScatterPlot` instead.
*/
/**
* @deprecated The `Scatter` component is an internal implementation detail of `ScatterPlot` and will be removed from the public API in v10. Use `ScatterPlot` instead.
*/
var _excluded$16 = ["ownerState"];
/**
* @deprecated The `Scatter` component is an internal implementation detail of `ScatterPlot` and will be removed from the public API in v10. Use `ScatterPlot` instead.
*
* Demos:
*
* - [Scatter](https://mui.com/x/react-charts/scatter/)
* - [Scatter demonstration](https://mui.com/x/react-charts/scatter-demo/)
*
* API:
*
* - [Scatter API](https://mui.com/x/api/charts/scatter/)
*/
function Scatter(props) {
	const { series, xScale, yScale, colorGetter, sizeGetter, onItemClick, classes: inClasses, slots, slotProps } = props;
	const { instance } = useChartsContext();
	const skipInteractionHandlers = useStore().use(selectorChartsIsVoronoiEnabled);
	const getHighlightState = useItemHighlightStateGetter();
	const scatterPlotData = useScatterPlotData(series, xScale, yScale, instance.isPointInside);
	const Marker = slots?.marker ?? ScatterMarker;
	const markerProps = _objectWithoutPropertiesLoose(useSlotProps({
		elementType: Marker,
		externalSlotProps: slotProps?.marker,
		additionalProps: { seriesId: series.id },
		ownerState: {}
	}), _excluded$16);
	const classes = useUtilityClasses$6({ classes: inClasses });
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)("g", {
		"data-series": series.id,
		className: classes.series,
		children: scatterPlotData.map((dataPoint) => {
			const highlightState = getHighlightState(dataPoint);
			const isItemHighlighted = highlightState === "highlighted";
			const isItemFaded = highlightState === "faded";
			return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(Marker, _extends({
				className: clsx(classes.marker, markerProps.className),
				dataIndex: dataPoint.dataIndex,
				color: colorGetter(dataPoint.dataIndex),
				size: sizeGetter(dataPoint.dataIndex),
				isHighlighted: isItemHighlighted,
				isFaded: isItemFaded,
				x: dataPoint.x,
				y: dataPoint.y,
				onClick: onItemClick && ((event) => onItemClick(event, {
					type: "scatter",
					seriesId: series.id,
					dataIndex: dataPoint.dataIndex
				})),
				"data-highlighted": isItemHighlighted || void 0,
				"data-faded": isItemFaded || void 0
			}, skipInteractionHandlers ? void 0 : getInteractionItemProps(instance, dataPoint), markerProps), dataPoint.id ?? dataPoint.dataIndex);
		})
	});
}
Scatter.propTypes = {
	classes: import_prop_types.default.object,
	color: import_prop_types.default.string.isRequired,
	/**
	* Function to get the color of a scatter item given its data index.
	* The data index argument is optional. If not provided, the color for the entire series is returned.
	* If provided, the color for the specific scatter item is returned.
	*/
	colorGetter: import_prop_types.default.func.isRequired,
	/**
	* Callback fired when clicking on a scatter item.
	* @param {MouseEvent} event Mouse event recorded on the `<svg/>` element.
	* @param {ScatterItemIdentifier} scatterItemIdentifier The scatter item identifier.
	*/
	onItemClick: import_prop_types.default.func,
	series: import_prop_types.default.object.isRequired,
	/**
	* Function to get the marker size of a scatter item given its data index.
	*/
	sizeGetter: import_prop_types.default.func.isRequired,
	slotProps: import_prop_types.default.object,
	slots: import_prop_types.default.object,
	xScale: import_prop_types.default.func.isRequired,
	yScale: import_prop_types.default.func.isRequired
};
//#endregion
//#region node_modules/@mui/x-charts/ScatterChart/BatchScatter.mjs
var MAX_POINTS_PER_PATH = 1e3;
var ALMOST_ZERO = .01;
function createPath(x, y, markerSize) {
	return `M${x - markerSize} ${y} a${markerSize} ${markerSize} 0 1 1 0 ${ALMOST_ZERO}`;
}
function useCreatePaths(seriesData, markerSize, xScale, yScale, color, colorGetter, sizeGetter) {
	const { instance } = useChartsContext();
	const getXPosition = getValueToPositionMapper(xScale);
	const getYPosition = getValueToPositionMapper(yScale);
	const paths = /* @__PURE__ */ new Map();
	const temporaryPaths = /* @__PURE__ */ new Map();
	for (let i = 0; i < seriesData.length; i += 1) {
		const scatterPoint = seriesData[i];
		const x = getXPosition(scatterPoint.x);
		const y = getYPosition(scatterPoint.y);
		if (!instance.isPointInside(x, y)) continue;
		const path = createPath(x, y, sizeGetter ? sizeGetter(i) : markerSize);
		const fill = colorGetter ? colorGetter(i) : color;
		const tempPath = appendAtKey(temporaryPaths, fill, path);
		if (tempPath.length >= MAX_POINTS_PER_PATH) {
			appendAtKey(paths, fill, tempPath.join(""));
			temporaryPaths.delete(fill);
		}
	}
	for (const [fill, tempPath] of temporaryPaths.entries()) if (tempPath.length > 0) appendAtKey(paths, fill, tempPath.join(""));
	return paths;
}
function BatchScatterPaths(props) {
	const { series, xScale, yScale, color, colorGetter, markerSize, sizeGetter } = props;
	const paths = useCreatePaths(series.data, markerSize, xScale, yScale, color, colorGetter, sizeGetter);
	const children = [];
	let i = 0;
	for (const [fill, dArray] of paths.entries()) for (const d of dArray) {
		children.push(/*#__PURE__*/ (0, import_jsx_runtime.jsx)("path", {
			fill,
			d
		}, i));
		i += 1;
	}
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(import_react.Fragment, { children });
}
var MemoBatchScatterPaths = /*#__PURE__*/ import_react.memo(BatchScatterPaths);
MemoBatchScatterPaths.displayName = "MemoBatchScatterPaths";
var Group = styled("g", {
	slot: "internal",
	shouldForwardProp: void 0
})({
	"&[data-faded=\"true\"]": { opacity: .3 },
	"& path": { pointerEvents: "none" }
});
/**
* @internal
* A batch version of the Scatter component that uses SVG paths to render points.
* This component is optimized for performance and is suitable for rendering large datasets, but has limitations. Some of the limitations include:
* - Limited CSS styling;
* - Overriding the `marker` slot is not supported;
* - Highlight style must not contain opacity.
*
* You can read about all the limitations [here](https://mui.com/x/react-charts/scatter/#performance).
*/
function BatchScatter(props) {
	const { series, xScale, yScale, color, colorGetter, sizeGetter, className } = props;
	const { store } = useChartsContext();
	const isSeriesHighlighted = store.use(selectorChartIsSeriesHighlighted, series.id);
	const isSeriesFaded = store.use(selectorChartIsSeriesFaded, series.id);
	const seriesHighlightedItemDataIndex = store.use(selectorChartSeriesHighlightedItem, series.id);
	const seriesUnfadedItem = store.use(selectorChartSeriesUnfadedItem, series.id);
	const highlightedModifier = 1.2;
	const highlightMultiplier = isSeriesHighlighted ? highlightedModifier : 1;
	const getMarkerRadius = (index) => (sizeGetter ? sizeGetter(index) : series.markerSize) * highlightMultiplier;
	const classes = useUtilityClasses$6(props);
	const siblings = [];
	if (seriesHighlightedItemDataIndex != null) {
		const datum = series.data[seriesHighlightedItemDataIndex];
		const getXPosition = getValueToPositionMapper(xScale);
		const getYPosition = getValueToPositionMapper(yScale);
		siblings.push(/*#__PURE__*/ (0, import_jsx_runtime.jsx)("path", {
			fill: colorGetter ? colorGetter(seriesHighlightedItemDataIndex) : color,
			"data-highlighted": true,
			d: createPath(getXPosition(datum.x), getYPosition(datum.y), getMarkerRadius(seriesHighlightedItemDataIndex) * highlightedModifier)
		}, `highlighted-${series.id}`));
	}
	if (seriesUnfadedItem != null) {
		const datum = series.data[seriesUnfadedItem];
		const getXPosition = getValueToPositionMapper(xScale);
		const getYPosition = getValueToPositionMapper(yScale);
		siblings.push(/*#__PURE__*/ (0, import_jsx_runtime.jsx)("path", {
			fill: colorGetter ? colorGetter(seriesUnfadedItem) : color,
			d: createPath(getXPosition(datum.x), getYPosition(datum.y), getMarkerRadius(seriesUnfadedItem))
		}, `unfaded-${series.id}`));
	}
	return /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(import_react.Fragment, { children: [/*#__PURE__*/ (0, import_jsx_runtime.jsx)(Group, {
		className: clsx(classes.series, className),
		"data-series": series.id,
		"data-faded": isSeriesFaded || void 0,
		"data-highlighted": isSeriesHighlighted || void 0,
		children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(MemoBatchScatterPaths, {
			series,
			xScale,
			yScale,
			color,
			colorGetter,
			sizeGetter: getMarkerRadius,
			markerSize: series.markerSize
		})
	}), siblings] });
}
//#endregion
//#region node_modules/@mui/x-charts/ScatterChart/async/scatterRenderData.selectors.mjs
/**
* Render data for one scatter series. Packed `Float64Array` indexed by
* `dataIndex` (stride 3: `[x, y, visible]`). Off-screen points keep their slot
* so a point's batch stays fixed across zoom/pan (no popping).
*/
var EMPTY_RENDER_DATA = /* @__PURE__ */ new Map();
/** Packed projected coordinates for every scatter series, indexed by `dataIndex`. */
var selectorScatterRenderData = createSelectorMemoized(selectorChartSeriesProcessed, selectorChartXAxis, selectorChartYAxis, selectorChartDrawingArea, function selectorScatterRenderData(processedSeries, xAxes, yAxes, drawingArea) {
	const scatter = processedSeries.scatter;
	if (scatter === void 0) return EMPTY_RENDER_DATA;
	const defaultXAxisId = xAxes.axisIds[0];
	const defaultYAxisId = yAxes.axisIds[0];
	const result = /* @__PURE__ */ new Map();
	const xMin = drawingArea.left - 1;
	const xMax = drawingArea.left + drawingArea.width;
	const yMin = drawingArea.top - 1;
	const yMax = drawingArea.top + drawingArea.height;
	for (const seriesId of scatter.seriesOrder) {
		const series = scatter.series[seriesId];
		const xAxis = xAxes.axis[series.xAxisId ?? defaultXAxisId];
		const yAxis = yAxes.axis[series.yAxisId ?? defaultYAxisId];
		if (xAxis === void 0 || yAxis === void 0) continue;
		const getXPosition = getValueToPositionMapper(xAxis.scale);
		const getYPosition = getValueToPositionMapper(yAxis.scale);
		result.set(seriesId, packScatterSeriesCoords(series.data, getXPosition, getYPosition, {
			xMin,
			xMax,
			yMin,
			yMax
		}));
	}
	return result;
});
/** Pixel bounds (inclusive) a point must fall within to be flagged visible. */
/**
* Projects `data` into a `dataIndex`-indexed packed array (stride 3:
* `[x, y, visible]`). Every point keeps its slot; `visible` is `1` inside
* `bounds`, `0` otherwise.
*/
function packScatterSeriesCoords(data, getXPosition, getYPosition, bounds) {
	const n = data.length;
	const packed = new Float64Array(n * 3);
	for (let i = 0; i < n; i += 1) {
		const x = getXPosition(data[i].x);
		const y = getYPosition(data[i].y);
		const visible = x >= bounds.xMin && x <= bounds.xMax && y >= bounds.yMin && y <= bounds.yMax;
		packed[i * 3] = x;
		packed[i * 3 + 1] = y;
		packed[i * 3 + 2] = visible ? 1 : 0;
	}
	return {
		coords: packed,
		count: n
	};
}
/** Render data for one series, or `undefined` while processors/axes are pending. */
var selectorScatterSeriesRenderData = createSelector(selectorScatterRenderData, (renderData, seriesId) => renderData.get(seriesId));
//#endregion
//#region node_modules/@mui/x-charts/ScatterChart/async/ScatterAsyncBatch.mjs
var _excluded$15 = ["ownerState"];
/**
* @ignore - internal component.
*/
function ScatterAsyncBatchComponent(props) {
	const { series, colorGetter, onItemClick, slots, slotProps, start, step, revealed, isInteracting, classes: inClasses } = props;
	const classes = useUtilityClasses$6({ classes: inClasses });
	const { instance } = useChartsContext();
	const store = useStore();
	const skipInteractionHandlers = store.use(selectorChartsIsVoronoiEnabled);
	const getHighlightState = useItemHighlightStateGetter();
	const renderData = store.use(selectorScatterSeriesRenderData, series.id);
	const Marker = slots?.marker ?? ScatterMarker;
	const markerProps = _objectWithoutPropertiesLoose(useSlotProps({
		elementType: Marker,
		externalSlotProps: slotProps?.marker,
		additionalProps: {
			seriesId: series.id,
			size: series.markerSize
		},
		ownerState: {}
	}), _excluded$15);
	if (renderData === void 0 || !revealed) return /*#__PURE__*/ (0, import_jsx_runtime.jsx)("g", {
		"data-series": series.id,
		className: classes.series
	});
	const { coords, count } = renderData;
	const markers = [];
	const safeStep = Math.max(1, step);
	for (let dataIndex = start; dataIndex < count; dataIndex += safeStep) {
		if (coords[dataIndex * 3 + 2] === 0) continue;
		const x = coords[dataIndex * 3];
		const y = coords[dataIndex * 3 + 1];
		const dataPoint = {
			x,
			y,
			dataIndex,
			seriesId: series.id,
			type: "scatter"
		};
		const highlightState = isInteracting ? "none" : getHighlightState(dataPoint);
		const isItemHighlighted = highlightState === "highlighted";
		const isItemFaded = highlightState === "faded";
		markers.push(/*#__PURE__*/ (0, import_jsx_runtime.jsx)(Marker, _extends({
			className: clsx(classes.marker, markerProps.className),
			dataIndex,
			color: colorGetter(dataIndex),
			isHighlighted: isItemHighlighted,
			isFaded: isItemFaded,
			x,
			y,
			onClick: onItemClick && ((event) => onItemClick(event, {
				type: "scatter",
				seriesId: series.id,
				dataIndex
			})),
			"data-highlighted": isItemHighlighted || void 0,
			"data-faded": isItemFaded || void 0
		}, skipInteractionHandlers || isInteracting ? void 0 : getInteractionItemProps(instance, dataPoint), markerProps), dataIndex));
	}
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)("g", {
		"data-series": series.id,
		className: classes.series,
		children: markers
	});
}
var ScatterAsyncBatch = /*#__PURE__*/ import_react.memo(ScatterAsyncBatchComponent);
ScatterAsyncBatch.displayName = "ScatterAsyncBatch";
//#endregion
//#region node_modules/@mui/x-charts/ScatterChart/async/ScatterAsync.mjs
/** Per-series points sampled while interacting; the rest fills in on settle. */
var INTERACTION_POINT_BUDGET = 2e3;
/**
* Interacting sample stride. Multiple of `nBatches` (batch 0's stride) so the
* sample is a subset of batch 0 — settling only adds points, no jump. Coarsened
* to stay within `budget`.
*/
function getInteractionStep(count, nBatches, budget) {
	if (nBatches <= 0) return 1;
	return nBatches * Math.max(1, Math.ceil(count / (budget * nBatches)));
}
/**
* @ignore - internal component.
*/
function ScatterAsync(props) {
	const { series, colorGetter, onItemClick, slots, slotProps, classes } = props;
	const store = useStore();
	const batchSize = store.use(selectorProgressiveBatchSize);
	const revealedBatches = store.use(selectorProgressiveSeriesRevealedBatches, series.id);
	const isZoomInteracting = store.use(selectorChartZoomIsInteracting);
	const count = store.use(selectorScatterSeriesRenderData, series.id)?.count ?? 0;
	const nBatches = count === 0 ? 0 : Math.ceil(count / Math.max(1, batchSize));
	const mountedBatches = isZoomInteracting ? Math.min(1, nBatches) : nBatches;
	const interactionStep = getInteractionStep(count, nBatches, INTERACTION_POINT_BUDGET);
	const batches = [];
	for (let b = 0; b < mountedBatches; b += 1) batches.push(/*#__PURE__*/ (0, import_jsx_runtime.jsx)(ScatterAsyncBatch, {
		series,
		colorGetter,
		onItemClick,
		slots,
		slotProps,
		start: isZoomInteracting ? 0 : b,
		step: isZoomInteracting ? interactionStep : nBatches,
		classes,
		revealed: isZoomInteracting || b < revealedBatches,
		isInteracting: isZoomInteracting
	}, b));
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(import_react.Fragment, { children: batches });
}
ScatterAsync.propTypes = {
	classes: import_prop_types.default.object,
	color: import_prop_types.default.string.isRequired,
	colorGetter: import_prop_types.default.func.isRequired,
	onItemClick: import_prop_types.default.func,
	series: import_prop_types.default.object.isRequired,
	slotProps: import_prop_types.default.object,
	slots: import_prop_types.default.object,
	xScale: import_prop_types.default.func.isRequired,
	yScale: import_prop_types.default.func.isRequired
};
//#endregion
//#region node_modules/@mui/x-charts/ScatterChart/ScatterPlot.mjs
var EMPTY_SERIES_IDS = [];
var ScatterPlotRoot = styled("g", {
	name: "MuiScatterPlot",
	slot: "Root"
})();
/**
* Demos:
*
* - [Scatter](https://mui.com/x/react-charts/scatter/)
* - [Scatter demonstration](https://mui.com/x/react-charts/scatter-demo/)
*
* API:
*
* - [ScatterPlot API](https://mui.com/x/api/charts/scatter-plot/)
*/
function ScatterPlot(props) {
	const { slots, slotProps, onItemClick, renderer, className, classes: inClasses } = props;
	const seriesData = useScatterSeriesContext();
	const { xAxis, xAxisIds } = useXAxes();
	const { yAxis, yAxisIds } = useYAxes();
	const { zAxis, zAxisIds } = useZAxes();
	const classes = useUtilityClasses$6({ classes: inClasses });
	const { instance } = useChartsContext();
	const store = useStore();
	const plotId = import_react.useId();
	const seriesIds = seriesData?.seriesOrder ?? EMPTY_SERIES_IDS;
	const isProgressive = store.use(selectorShouldUseProgressiveRenderer, seriesIds, renderer);
	import_react.useEffect(() => instance.registerProgressivePlan(plotId, seriesIds, renderer), [
		instance,
		plotId,
		seriesIds,
		renderer
	]);
	if (seriesData === void 0) return null;
	const { series, seriesOrder } = seriesData;
	const defaultXAxisId = xAxisIds[0];
	const defaultYAxisId = yAxisIds[0];
	const defaultZAxisId = zAxisIds[0];
	let DefaultScatterItems;
	if (renderer === "svg-batch") DefaultScatterItems = BatchScatter;
	else if (isProgressive) DefaultScatterItems = ScatterAsync;
	else DefaultScatterItems = Scatter;
	const ScatterItems = slots?.scatter ?? DefaultScatterItems;
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ScatterPlotRoot, {
		className: clsx(classes.root, className),
		children: seriesOrder.map((seriesId) => {
			const { id, xAxisId, yAxisId, colorAxisId, zAxisId, sizeAxisId, color, hidden } = series[seriesId];
			if (hidden) return null;
			const colorGetter = scatterSeriesConfig.colorProcessor(series[seriesId], xAxis[xAxisId ?? defaultXAxisId], yAxis[yAxisId ?? defaultYAxisId], zAxis[colorAxisId ?? zAxisId ?? defaultZAxisId]);
			const sizeGetter = getMarkerSize(series[seriesId], zAxis[sizeAxisId ?? defaultZAxisId]);
			const xScale = xAxis[xAxisId ?? defaultXAxisId].scale;
			const yScale = yAxis[yAxisId ?? defaultYAxisId].scale;
			return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ScatterItems, _extends({
				xScale,
				yScale,
				color,
				colorGetter,
				sizeGetter,
				series: series[seriesId],
				onItemClick,
				slots,
				slotProps
			}, slotProps?.scatter), id);
		})
	});
}
ScatterPlot.propTypes = {
	classes: import_prop_types.default.object,
	className: import_prop_types.default.string,
	/**
	* Callback fired when a marker is clicked directly.
	* For closest-point clicks (the `ScatterChart` default), pass `onItemClick` to the chart container instead.
	* @param {MouseEvent} event Mouse event recorded on the `<svg/>` element.
	* @param {ScatterItemIdentifier} scatterItemIdentifier The scatter item identifier.
	*/
	onItemClick: import_prop_types.default.func,
	/**
	* The type of renderer to use for the scatter plot.
	* - `svg-single`: Renders every scatter item in a `<circle />` element, synchronously.
	* - `svg-progressive`: Renders every scatter item in a `<circle />` element, in progressive batches that paint over several animation frames to keep the main thread responsive.
	* - `svg-batch`: Batch renders scatter items in `<path />` elements for better performance with large datasets, at the cost of some limitations.
	*                Read more: https://mui.com/x/react-charts/scatter/#performance
	*
	* When not set, defaults to `svg-single`. Enable the `progressiveRendering` experimental feature to auto-select `svg-progressive` above an internal point-count threshold; this will become the default in the next major version.
	* @default 'svg-single'
	*/
	renderer: import_prop_types.default.oneOf([
		"svg-batch",
		"svg-progressive",
		"svg-single"
	]),
	/**
	* The props used for each component slot.
	* @default {}
	*/
	slotProps: import_prop_types.default.object,
	/**
	* Overridable component slots.
	* @default {}
	*/
	slots: import_prop_types.default.object
};
//#endregion
//#region node_modules/@mui/x-charts/ScatterChart/ScatterChart.plugins.mjs
var SCATTER_CHART_PLUGINS = [
	useChartZAxis,
	useChartBrush,
	useChartTooltip,
	useChartInteraction,
	useChartCartesianAxis,
	useChartHighlight,
	useChartVisibilityManager,
	useChartClosestPoint,
	useChartKeyboardNavigation,
	useProgressiveRendering
];
//#endregion
//#region node_modules/@mui/x-charts/ScatterChart/useScatterChartProps.mjs
var _excluded$14 = [
	"xAxis",
	"yAxis",
	"zAxis",
	"series",
	"axisHighlight",
	"hitAreaRadius",
	"disableHitArea",
	"hideLegend",
	"width",
	"height",
	"margin",
	"colors",
	"sx",
	"grid",
	"onItemClick",
	"children",
	"slots",
	"slotProps",
	"loading",
	"highlightedItem",
	"onHighlightChange",
	"className",
	"showToolbar",
	"renderer",
	"brushConfig"
];
/**
* A helper function that extracts ScatterChartProps from the input props
* and returns an object with props for the children components of ScatterChart.
*
* @param props The input props for ScatterChart
* @returns An object with props for the children components of ScatterChart
*/
var useScatterChartProps = (props) => {
	const { xAxis, yAxis, zAxis, series, axisHighlight, hitAreaRadius, disableHitArea, width, height, margin, colors, sx, grid, onItemClick, children, slots, slotProps, loading, highlightedItem, onHighlightChange, className, renderer, brushConfig } = props, other = _objectWithoutPropertiesLoose(props, _excluded$14);
	const seriesWithDefault = import_react.useMemo(() => series.map((s) => _extends({ type: "scatter" }, s)), [series]);
	const useVoronoiOnItemClick = disableHitArea !== true || renderer !== void 0 && renderer !== "svg-single";
	const chartsContainerProps = _extends({}, other, {
		series: seriesWithDefault,
		width,
		height,
		margin,
		colors,
		xAxis,
		yAxis,
		zAxis,
		highlightedItem,
		onHighlightChange,
		disableHitArea,
		hitAreaRadius,
		onItemClick: useVoronoiOnItemClick ? onItemClick : void 0,
		plugins: SCATTER_CHART_PLUGINS,
		slots,
		slotProps,
		brushConfig
	});
	const chartsAxisProps = {
		slots,
		slotProps
	};
	const gridProps = {
		vertical: grid?.vertical,
		horizontal: grid?.horizontal
	};
	const scatterPlotProps = {
		onItemClick: useVoronoiOnItemClick ? void 0 : onItemClick,
		slots,
		slotProps,
		renderer
	};
	const overlayProps = {
		loading,
		slots,
		slotProps
	};
	const legendProps = {
		slots,
		slotProps
	};
	const axisHighlightProps = _extends({
		y: "none",
		x: "none"
	}, axisHighlight);
	return {
		chartsWrapperProps: {
			sx,
			legendPosition: props.slotProps?.legend?.position,
			legendDirection: props.slotProps?.legend?.direction,
			hideLegend: props.hideLegend ?? false,
			className
		},
		chartsContainerProps,
		chartsAxisProps,
		gridProps,
		scatterPlotProps,
		overlayProps,
		legendProps,
		axisHighlightProps,
		children
	};
};
//#endregion
//#region node_modules/@mui/x-charts/ScatterChart/useScatterItemPosition.mjs
/**
* Resolves a scatter item identifier to its on-screen position and the owning
* series. Shared by `FocusedScatterMark`, `HighlightedScatterMark`, and other
* overlay components that need to draw an SVG element at a specific scatter point.
*
* Returns `null` if the identifier is missing, the identifier doesn't point to
* a scatter series, or the referenced point can't be resolved.
*/
function useScatterItemPosition(item) {
	const scatterSeries = useScatterSeriesContext();
	const { xAxis, xAxisIds } = useXAxes();
	const { yAxis, yAxisIds } = useYAxes();
	const { zAxis, zAxisIds } = useZAxes();
	if (!item || !scatterSeries) return null;
	const series = scatterSeries.series[item.seriesId];
	if (!series || series.hidden) return null;
	const scatterPoint = series.data[item.dataIndex];
	if (!scatterPoint) return null;
	const xAxisId = series.xAxisId ?? xAxisIds[0];
	const yAxisId = series.yAxisId ?? yAxisIds[0];
	return {
		cx: getValueToPositionMapper(xAxis[xAxisId].scale)(scatterPoint.x),
		cy: getValueToPositionMapper(yAxis[yAxisId].scale)(scatterPoint.y),
		markerSize: getMarkerSize(series, zAxis[series.sizeAxisId ?? zAxisIds[0]])(item.dataIndex),
		series,
		scatterPoint
	};
}
//#endregion
//#region node_modules/@mui/x-charts/ScatterChart/FocusedScatterMark.mjs
var _excluded$13 = ["className"];
function FocusedScatterMark(_ref) {
	let { className } = _ref, props = _objectWithoutPropertiesLoose(_ref, _excluded$13);
	const theme = useTheme();
	const focusedItem = useFocusedItem();
	const classes = useUtilityClasses$6();
	const resolved = useScatterItemPosition(focusedItem?.type === "scatter" ? focusedItem : null);
	if (!resolved) return null;
	const { cx, cy, markerSize } = resolved;
	const size = markerSize + 3;
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)("rect", _extends({
		className: clsx(classes.focusedMark, className),
		fill: "none",
		stroke: (theme.vars ?? theme).palette.text.primary,
		strokeWidth: 2,
		x: cx - size,
		y: cy - size,
		width: 2 * size,
		height: 2 * size,
		rx: 3,
		ry: 3
	}, props));
}
//#endregion
//#region node_modules/@mui/x-charts/ScatterChart/ScatterChart.mjs
/**
* Demos:
*
* - [Scatter](https://mui.com/x/react-charts/scatter/)
* - [Scatter demonstration](https://mui.com/x/react-charts/scatter-demo/)
*
* API:
*
* - [ScatterChart API](https://mui.com/x/api/charts/scatter-chart/)
*/
var ScatterChart = /*#__PURE__*/ import_react.forwardRef(function ScatterChart(inProps, ref) {
	const props = useThemeProps({
		props: inProps,
		name: "MuiScatterChart"
	});
	const { chartsWrapperProps, chartsContainerProps, chartsAxisProps, gridProps, scatterPlotProps, overlayProps, legendProps, axisHighlightProps, children } = useScatterChartProps(props);
	const { chartsDataProviderProps, chartsSurfaceProps } = useChartsContainerProps(chartsContainerProps);
	const Tooltip = props.slots?.tooltip ?? ChartsTooltip;
	const Toolbar = props.slots?.toolbar;
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ChartsDataProvider, _extends({}, chartsDataProviderProps, { children: /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(ChartsWrapper, _extends({}, chartsWrapperProps, {
		ref,
		children: [
			props.showToolbar && Toolbar ? /*#__PURE__*/ (0, import_jsx_runtime.jsx)(Toolbar, _extends({}, props.slotProps?.toolbar)) : null,
			!props.hideLegend && /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ChartsLegend, _extends({}, legendProps)),
			/*#__PURE__*/ (0, import_jsx_runtime.jsxs)(ChartsSurface, _extends({}, chartsSurfaceProps, { children: [
				/*#__PURE__*/ (0, import_jsx_runtime.jsx)(ChartsAxis, _extends({}, chartsAxisProps)),
				/*#__PURE__*/ (0, import_jsx_runtime.jsx)(ChartsGrid, _extends({}, gridProps)),
				/*#__PURE__*/ (0, import_jsx_runtime.jsx)("g", {
					"data-drawing-container": true,
					children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ScatterPlot, _extends({}, scatterPlotProps))
				}),
				/*#__PURE__*/ (0, import_jsx_runtime.jsx)(ChartsOverlay, _extends({}, overlayProps)),
				/*#__PURE__*/ (0, import_jsx_runtime.jsx)(ChartsAxisHighlight, _extends({}, axisHighlightProps)),
				/*#__PURE__*/ (0, import_jsx_runtime.jsx)(FocusedScatterMark, {}),
				children
			] })),
			!props.loading && /*#__PURE__*/ (0, import_jsx_runtime.jsx)(Tooltip, _extends({ trigger: "item" }, props.slotProps?.tooltip))
		]
	})) }));
});
ScatterChart.displayName = "ScatterChart";
ScatterChart.propTypes = {
	apiRef: import_prop_types.default.shape({ current: import_prop_types.default.object }),
	/**
	* A gap added between axes when multiple axes are rendered on the same side of the chart.
	* @default 0
	*/
	axesGap: import_prop_types.default.number,
	/**
	* The configuration of axes highlight.
	* @see See {@link https://mui.com/x/react-charts/highlighting/ highlighting docs} for more details.
	* @default { x: 'none', y: 'none' }
	*/
	axisHighlight: import_prop_types.default.shape({
		x: import_prop_types.default.oneOf([
			"band",
			"line",
			"none"
		]),
		y: import_prop_types.default.oneOf([
			"band",
			"line",
			"none"
		])
	}),
	/**
	* Configuration for the brush interaction.
	*/
	brushConfig: import_prop_types.default.shape({
		enabled: import_prop_types.default.bool,
		preventHighlight: import_prop_types.default.bool,
		preventTooltip: import_prop_types.default.bool
	}),
	children: import_prop_types.default.node,
	className: import_prop_types.default.string,
	/**
	* Color palette used to colorize multiple series.
	* @default rainbowSurgePalette
	*/
	colors: import_prop_types.default.oneOfType([import_prop_types.default.arrayOf(import_prop_types.default.string), import_prop_types.default.func]),
	/**
	* An array of objects that can be used to populate series and axes data using their `dataKey` property.
	*/
	dataset: import_prop_types.default.arrayOf(import_prop_types.default.object),
	/**
	* The description of the chart.
	* Used to provide an accessible description for the chart.
	*/
	desc: import_prop_types.default.string,
	/**
	* If `true`, the charts will not listen to the mouse move event.
	* It might break interactive features, but will improve performance.
	* @default false
	*/
	disableAxisListener: import_prop_types.default.bool,
	/**
	* If true, the closest point interaction is disabled and falls back to hover events.
	* @default false
	* @deprecated Use `disableHitArea` instead.
	*/
	disableClosestPoint: import_prop_types.default.bool,
	/**
	* If true, the hit area interaction is disabled and falls back to hover events.
	* @default false
	*/
	disableHitArea: import_prop_types.default.bool,
	/**
	* If `true`, disables keyboard navigation for the chart.
	*/
	disableKeyboardNavigation: import_prop_types.default.bool,
	/**
	* If true, the interaction will not use the Voronoi cell and fall back to hover events.
	* @default false
	* @deprecated Use `disableHitArea` instead.
	*/
	disableVoronoi: import_prop_types.default.bool,
	/**
	* Options to enable features planned for the next major.
	*/
	experimentalFeatures: import_prop_types.default.shape({ progressiveRendering: import_prop_types.default.bool }),
	/**
	* Option to display a cartesian grid in the background.
	*/
	grid: import_prop_types.default.shape({
		horizontal: import_prop_types.default.bool,
		vertical: import_prop_types.default.bool
	}),
	/**
	* The height of the chart in px. If not defined, it takes the height of the parent element.
	*/
	height: import_prop_types.default.number,
	/**
	* List of hidden series and/or items.
	*
	* Different chart types use different keys.
	*
	* @example
	* ```ts
	* [
	*   {
	*     type: 'pie',
	*     seriesId: 'series-1',
	*     dataIndex: 3,
	*   },
	*   {
	*     type: 'line',
	*     seriesId: 'series-2',
	*   }
	* ]
	* ```
	*/
	hiddenItems: import_prop_types.default.arrayOf(import_prop_types.default.oneOfType([import_prop_types.default.shape({
		dataIndex: import_prop_types.default.number,
		seriesId: import_prop_types.default.string.isRequired,
		type: import_prop_types.default.oneOf(["scatter"]).isRequired
	}), import_prop_types.default.shape({
		dataIndex: import_prop_types.default.number,
		seriesId: import_prop_types.default.string.isRequired,
		type: import_prop_types.default.oneOf(["scatter"])
	})]).isRequired),
	/**
	* If `true`, the legend is not rendered.
	*/
	hideLegend: import_prop_types.default.bool,
	/**
	* The highlighted item.
	* Used when the highlight is controlled.
	*/
	highlightedItem: import_prop_types.default.oneOfType([import_prop_types.default.shape({
		dataIndex: import_prop_types.default.number,
		seriesId: import_prop_types.default.string.isRequired
	}), import_prop_types.default.shape({
		dataIndex: import_prop_types.default.number,
		seriesId: import_prop_types.default.string.isRequired,
		type: import_prop_types.default.oneOf(["scatter"]).isRequired
	})]),
	/**
	* Defines the maximum distance between a scatter point and the pointer that triggers the interaction.
	* If set to `'item'`, the radius is the `markerSize`.
	* If `undefined`, the radius is assumed to be infinite.
	*/
	hitAreaRadius: import_prop_types.default.oneOfType([import_prop_types.default.oneOf(["item"]), import_prop_types.default.number]),
	/**
	* This prop is used to help implement the accessibility logic.
	* If you don't provide this prop. It falls back to a randomly generated id.
	*/
	id: import_prop_types.default.string,
	/**
	* List of initially hidden series and/or items.
	* Used for uncontrolled state.
	*
	* Different chart types use different keys.
	*
	* @example
	* ```ts
	* [
	*   {
	*     type: 'pie',
	*     seriesId: 'series-1',
	*     dataIndex: 3,
	*   },
	*   {
	*     type: 'line',
	*     seriesId: 'series-2',
	*   }
	* ]
	* ```
	*/
	initialHiddenItems: import_prop_types.default.arrayOf(import_prop_types.default.oneOfType([import_prop_types.default.shape({
		dataIndex: import_prop_types.default.number,
		seriesId: import_prop_types.default.string.isRequired,
		type: import_prop_types.default.oneOf(["scatter"]).isRequired
	}), import_prop_types.default.shape({
		dataIndex: import_prop_types.default.number,
		seriesId: import_prop_types.default.string.isRequired,
		type: import_prop_types.default.oneOf(["scatter"])
	})]).isRequired),
	/**
	* If `true`, a loading overlay is displayed.
	* @default false
	*/
	loading: import_prop_types.default.bool,
	/**
	* Localized text for chart components.
	*/
	localeText: import_prop_types.default.object,
	/**
	* The margin between the SVG and the drawing area.
	* It's used for leaving some space for extra information such as the x- and y-axis or legend.
	*
	* Accepts a `number` to be used on all sides or an object with the optional properties: `top`, `bottom`, `left`, and `right`.
	*/
	margin: import_prop_types.default.oneOfType([import_prop_types.default.number, import_prop_types.default.shape({
		bottom: import_prop_types.default.number,
		left: import_prop_types.default.number,
		right: import_prop_types.default.number,
		top: import_prop_types.default.number
	})]),
	/**
	* The function called for onClick events.
	* The second argument contains information about all line/bar elements at the current mouse position.
	* @param {MouseEvent} event The mouse event recorded on the `<svg/>` element.
	* @param {null | ChartsAxisData} data The data about the clicked axis and items associated with it.
	*/
	onAxisClick: import_prop_types.default.func,
	/**
	* Callback fired when any hidden identifiers change.
	* @param {VisibilityIdentifierWithType[]} hiddenItems The new list of hidden identifiers.
	*/
	onHiddenItemsChange: import_prop_types.default.func,
	/**
	* The callback fired when the highlighted item changes.
	*
	* @param {HighlightItemIdentifierWithType<SeriesType> | null} highlightedItem  The newly highlighted item.
	*/
	onHighlightChange: import_prop_types.default.func,
	/**
	* Callback fired when clicking on a scatter item.
	* @param {MouseEvent} event The mouse event recorded on the `<svg/>` element if using hit area interaction. Or the Mouse event from the scatter element, when `disableHitArea=true`.
	* @param {ScatterItemIdentifier} scatterItemIdentifier The scatter item identifier.
	*/
	onItemClick: import_prop_types.default.func,
	/**
	* The function called when the pointer position corresponds to a new axis data item.
	* This update can either be caused by a pointer movement, or an axis update.
	* In case of multiple axes, the function is called if at least one axis is updated.
	* The argument contains the identifier for all axes with a `data` property.
	* @param {AxisItemIdentifier[]} axisItems The array of axes item identifiers.
	*/
	onTooltipAxisChange: import_prop_types.default.func,
	/**
	* The callback fired when the tooltip item changes.
	*
	* @param {SeriesItemIdentifier<SeriesType> | null} tooltipItem  The newly highlighted item.
	*/
	onTooltipItemChange: import_prop_types.default.func,
	/**
	* The type of renderer to use for the scatter plot.
	* - `svg-single`: Renders every scatter item in a `<circle />` element, synchronously.
	* - `svg-progressive`: Renders every scatter item in a `<circle />` element, in progressive batches that paint over several animation frames to keep the main thread responsive.
	* - `svg-batch`: Batch renders scatter items in `<path />` elements for better performance with large datasets, at the cost of some limitations.
	*                Read more: https://mui.com/x/react-charts/scatter/#performance
	*
	* When not set, defaults to `svg-single`. Enable the `progressiveRendering` experimental feature to auto-select `svg-progressive` above an internal point-count threshold; this will become the default in the next major version.
	* @default 'svg-single'
	*/
	renderer: import_prop_types.default.oneOf([
		"svg-batch",
		"svg-progressive",
		"svg-single"
	]),
	/**
	* The series to display in the scatter chart.
	* An array of [[ScatterSeries]] objects.
	*/
	series: import_prop_types.default.arrayOf(import_prop_types.default.object).isRequired,
	/**
	* If true, shows the default chart toolbar.
	* @default false
	*/
	showToolbar: import_prop_types.default.bool,
	/**
	* If `true`, animations are skipped.
	* If unset or `false`, the animations respects the user's `prefers-reduced-motion` setting.
	*/
	skipAnimation: import_prop_types.default.bool,
	/**
	* The props used for each component slot.
	* @default {}
	*/
	slotProps: import_prop_types.default.object,
	/**
	* Overridable component slots.
	* @default {}
	*/
	slots: import_prop_types.default.object,
	sx: import_prop_types.default.oneOfType([
		import_prop_types.default.arrayOf(import_prop_types.default.oneOfType([
			import_prop_types.default.func,
			import_prop_types.default.object,
			import_prop_types.default.bool
		])),
		import_prop_types.default.func,
		import_prop_types.default.object
	]),
	theme: import_prop_types.default.oneOf(["dark", "light"]),
	/**
	* The title of the chart.
	* Used to provide an accessible label for the chart.
	*/
	title: import_prop_types.default.string,
	/**
	* The controlled axis tooltip.
	* Identified by the axis id, and data index.
	*/
	tooltipAxis: import_prop_types.default.arrayOf(import_prop_types.default.shape({
		axisId: import_prop_types.default.oneOfType([import_prop_types.default.number, import_prop_types.default.string]).isRequired,
		dataIndex: import_prop_types.default.number.isRequired
	})),
	/**
	* The tooltip item.
	* Used when the tooltip is controlled.
	*/
	tooltipItem: import_prop_types.default.oneOfType([import_prop_types.default.shape({
		dataIndex: import_prop_types.default.number.isRequired,
		seriesId: import_prop_types.default.string.isRequired
	}), import_prop_types.default.shape({
		dataIndex: import_prop_types.default.number.isRequired,
		seriesId: import_prop_types.default.string.isRequired,
		type: import_prop_types.default.oneOf(["scatter"]).isRequired
	})]),
	/**
	* The width of the chart in px. If not defined, it takes the width of the parent element.
	*/
	width: import_prop_types.default.number,
	/**
	* The configuration of the x-axes.
	* If not provided, a default axis config is used.
	* An array of [[AxisConfig]] objects.
	*/
	xAxis: import_prop_types.default.arrayOf(import_prop_types.default.object),
	/**
	* The configuration of the y-axes.
	* If not provided, a default axis config is used.
	* An array of [[AxisConfig]] objects.
	*/
	yAxis: import_prop_types.default.arrayOf(import_prop_types.default.object),
	/**
	* The configuration of the z-axes.
	*/
	zAxis: import_prop_types.default.arrayOf(import_prop_types.default.shape({
		colorMap: import_prop_types.default.oneOfType([
			import_prop_types.default.shape({
				color: import_prop_types.default.oneOfType([import_prop_types.default.arrayOf(import_prop_types.default.string.isRequired), import_prop_types.default.func]).isRequired,
				max: import_prop_types.default.oneOfType([import_prop_types.default.instanceOf(Date), import_prop_types.default.number]),
				min: import_prop_types.default.oneOfType([import_prop_types.default.instanceOf(Date), import_prop_types.default.number]),
				type: import_prop_types.default.oneOf(["continuous"]).isRequired,
				unknownColor: import_prop_types.default.string
			}),
			import_prop_types.default.shape({
				colors: import_prop_types.default.arrayOf(import_prop_types.default.string).isRequired,
				thresholds: import_prop_types.default.arrayOf(import_prop_types.default.oneOfType([import_prop_types.default.instanceOf(Date), import_prop_types.default.number]).isRequired).isRequired,
				type: import_prop_types.default.oneOf(["piecewise"]).isRequired,
				unknownColor: import_prop_types.default.string
			}),
			import_prop_types.default.shape({
				colors: import_prop_types.default.arrayOf(import_prop_types.default.string).isRequired,
				type: import_prop_types.default.oneOf(["ordinal"]).isRequired,
				unknownColor: import_prop_types.default.string,
				values: import_prop_types.default.arrayOf(import_prop_types.default.oneOfType([
					import_prop_types.default.instanceOf(Date),
					import_prop_types.default.number,
					import_prop_types.default.string
				]).isRequired)
			})
		]),
		data: import_prop_types.default.array,
		dataKey: import_prop_types.default.string,
		id: import_prop_types.default.string,
		max: import_prop_types.default.number,
		min: import_prop_types.default.number,
		sizeMap: import_prop_types.default.oneOfType([
			import_prop_types.default.shape({
				interpolator: import_prop_types.default.oneOf([
					"linear",
					"log",
					"sqrt"
				]),
				max: import_prop_types.default.oneOfType([import_prop_types.default.instanceOf(Date), import_prop_types.default.number]),
				min: import_prop_types.default.oneOfType([import_prop_types.default.instanceOf(Date), import_prop_types.default.number]),
				size: import_prop_types.default.arrayOf(import_prop_types.default.number.isRequired).isRequired,
				type: import_prop_types.default.oneOf(["continuous"]).isRequired
			}),
			import_prop_types.default.shape({
				max: import_prop_types.default.oneOfType([import_prop_types.default.instanceOf(Date), import_prop_types.default.number]),
				min: import_prop_types.default.oneOfType([import_prop_types.default.instanceOf(Date), import_prop_types.default.number]),
				size: import_prop_types.default.func.isRequired,
				type: import_prop_types.default.oneOf(["continuous"]).isRequired
			}),
			import_prop_types.default.shape({
				sizes: import_prop_types.default.arrayOf(import_prop_types.default.number).isRequired,
				thresholds: import_prop_types.default.arrayOf(import_prop_types.default.oneOfType([import_prop_types.default.instanceOf(Date), import_prop_types.default.number]).isRequired).isRequired,
				type: import_prop_types.default.oneOf(["piecewise"]).isRequired
			}),
			import_prop_types.default.shape({
				sizes: import_prop_types.default.arrayOf(import_prop_types.default.number).isRequired,
				type: import_prop_types.default.oneOf(["ordinal"]).isRequired,
				unknownSize: import_prop_types.default.number,
				values: import_prop_types.default.arrayOf(import_prop_types.default.oneOfType([
					import_prop_types.default.instanceOf(Date),
					import_prop_types.default.number,
					import_prop_types.default.string
				]).isRequired)
			})
		]),
		valueGetter: import_prop_types.default.func
	}))
};
//#endregion
//#region node_modules/@mui/x-charts/ScatterChart/HighlightedScatterMark.mjs
/**
* Draws an SVG ring around the currently highlighted scatter point.
* Used by renderers where the point itself is rasterized off the SVG tree
* (for example WebGL or `svg-batch`), so the highlight has to be drawn in SVG
* and positioned via the same axis scales the underlying renderer uses.
*/
var _excluded$12 = ["className"];
function HighlightedScatterMark(_ref) {
	let { className } = _ref, props = _objectWithoutPropertiesLoose(_ref, _excluded$12);
	const theme = useTheme();
	const store = useStore();
	const highlightedItem = store.use(selectorChartsHighlightedItem);
	const drawingArea = store.use(selectorChartDrawingArea);
	const classes = useUtilityClasses$6();
	const resolved = useScatterItemPosition(highlightedItem?.type === "scatter" && highlightedItem.dataIndex !== void 0 ? {
		seriesId: highlightedItem.seriesId,
		dataIndex: highlightedItem.dataIndex
	} : null);
	if (!resolved) return null;
	const { cx, cy, markerSize } = resolved;
	const margin = markerSize;
	if (cx < drawingArea.left - margin || cx > drawingArea.left + drawingArea.width + margin || cy < drawingArea.top - margin || cy > drawingArea.top + drawingArea.height + margin) return null;
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)("circle", _extends({
		className: clsx(classes.highlightedMark, className),
		fill: "none",
		stroke: (theme.vars ?? theme).palette.text.primary,
		strokeWidth: 1,
		cx,
		cy,
		r: markerSize,
		pointerEvents: "none"
	}, props));
}
//#endregion
//#region node_modules/@mui/x-charts/SparkLineChart/SparkLineChart.mjs
var _excluded$11 = [
	"xAxis",
	"yAxis",
	"width",
	"height",
	"margin",
	"color",
	"baseline",
	"sx",
	"showTooltip",
	"showHighlight",
	"axisHighlight",
	"children",
	"slots",
	"slotProps",
	"data",
	"plotType",
	"valueFormatter",
	"area",
	"curve",
	"className",
	"disableClipping",
	"clipAreaOffset",
	"onHighlightChange",
	"onHighlightedAxisChange",
	"highlightedAxis",
	"highlightedItem"
];
var SPARK_LINE_DEFAULT_MARGIN = 5;
/**
* Demos:
*
* - [SparkLine](https://mui.com/x/react-charts/sparkline/)
*
* API:
*
* - [SparkLineChart API](https://mui.com/x/api/charts/spark-line-chart/)
*/
var SparkLineChart = /*#__PURE__*/ import_react.forwardRef(function SparkLineChart(props, ref) {
	const { xAxis: xAxisProps, yAxis: yAxisProps, width, height, margin = SPARK_LINE_DEFAULT_MARGIN, color, baseline, sx, showTooltip, showHighlight, axisHighlight: inAxisHighlight, children, slots, slotProps, data, plotType = "line", valueFormatter = (value) => value === null ? "" : value.toString(), area, curve = "linear", className, disableClipping, clipAreaOffset, onHighlightChange, onHighlightedAxisChange, highlightedAxis, highlightedItem } = props, other = _objectWithoutPropertiesLoose(props, _excluded$11);
	const clipPathId = `${useId()}-clip-path`;
	const clipPathOffset = import_react.useMemo(() => ({
		top: clipAreaOffset?.top ?? 1,
		right: clipAreaOffset?.right ?? 1,
		bottom: clipAreaOffset?.bottom ?? 1,
		left: clipAreaOffset?.left ?? 1
	}), [
		clipAreaOffset?.bottom,
		clipAreaOffset?.left,
		clipAreaOffset?.right,
		clipAreaOffset?.top
	]);
	const defaultXHighlight = import_react.useMemo(() => showHighlight && plotType === "bar" ? { x: "band" } : { x: "none" }, [plotType, showHighlight]);
	const axisHighlight = import_react.useMemo(() => _extends({}, defaultXHighlight, inAxisHighlight), [defaultXHighlight, inAxisHighlight]);
	const Tooltip = props.slots?.tooltip ?? ChartsTooltip;
	const colors = import_react.useMemo(() => {
		if (color == null) return;
		return typeof color === "function" ? (mode) => [color(mode)] : [color];
	}, [color]);
	return /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(ChartsDataProvider, {
		series: import_react.useMemo(() => [_extends({
			type: plotType,
			data,
			valueFormatter
		}, plotType === "bar" ? {} : {
			area,
			curve,
			baseline,
			disableHighlight: !showHighlight
		})], [
			area,
			baseline,
			curve,
			data,
			plotType,
			showHighlight,
			valueFormatter
		]),
		width,
		height,
		margin,
		xAxis: import_react.useMemo(() => [_extends({
			id: DEFAULT_X_AXIS_KEY,
			scaleType: plotType === "bar" ? "band" : "point",
			hideTooltip: xAxisProps === void 0
		}, xAxisProps, {
			data: xAxisProps?.data ?? Array.from({ length: data.length }, (_, index) => index),
			position: "none"
		})], [
			data.length,
			plotType,
			xAxisProps
		]),
		yAxis: import_react.useMemo(() => [_extends({ id: DEFAULT_Y_AXIS_KEY }, yAxisProps, { position: "none" })], [yAxisProps]),
		colors,
		disableAxisListener: onHighlightedAxisChange === void 0 && (!showTooltip || slotProps?.tooltip?.trigger !== "axis") && axisHighlight?.x === "none" && axisHighlight?.y === "none",
		onHighlightChange,
		onHighlightedAxisChange,
		highlightedAxis,
		highlightedItem,
		children: [/*#__PURE__*/ (0, import_jsx_runtime.jsxs)(ChartsSurface, _extends({
			className,
			ref,
			sx
		}, other, { children: [
			/*#__PURE__*/ (0, import_jsx_runtime.jsxs)("g", {
				clipPath: `url(#${clipPathId})`,
				children: [plotType === "bar" && /*#__PURE__*/ (0, import_jsx_runtime.jsx)(BarPlot, {
					skipAnimation: true,
					slots,
					slotProps
				}), plotType === "line" && /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(import_react.Fragment, { children: [/*#__PURE__*/ (0, import_jsx_runtime.jsx)(AreaPlot, {
					skipAnimation: true,
					slots,
					slotProps
				}), /*#__PURE__*/ (0, import_jsx_runtime.jsx)(LinePlot, {
					skipAnimation: true,
					slots,
					slotProps
				})] })]
			}),
			plotType === "line" && /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(import_react.Fragment, { children: [/*#__PURE__*/ (0, import_jsx_runtime.jsx)(LineHighlightPlot, {
				slots,
				slotProps
			}), /*#__PURE__*/ (0, import_jsx_runtime.jsx)(FocusedLineMark, {})] }),
			disableClipping ? null : /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ChartsClipPath, {
				id: clipPathId,
				offset: clipPathOffset
			}),
			/*#__PURE__*/ (0, import_jsx_runtime.jsx)(ChartsAxisHighlight, _extends({}, axisHighlight)),
			children
		] })), showTooltip && /*#__PURE__*/ (0, import_jsx_runtime.jsx)(Tooltip, _extends({}, props.slotProps?.tooltip))]
	});
});
SparkLineChart.displayName = "SparkLineChart";
SparkLineChart.propTypes = {
	apiRef: import_prop_types.default.shape({ current: import_prop_types.default.object }),
	/**
	* Set to `true` to fill spark line area.
	* Has no effect if plotType='bar'.
	* @default false
	*/
	area: import_prop_types.default.bool,
	axisHighlight: import_prop_types.default.shape({
		x: import_prop_types.default.oneOf([
			"band",
			"line",
			"none"
		]),
		y: import_prop_types.default.oneOf([
			"band",
			"line",
			"none"
		])
	}),
	/**
	* The value of the line at the base of the series area.
	*
	* - `'min'` the area will fill the space **under** the line.
	* - `'max'` the area will fill the space **above** the line.
	* - `number` the area will fill the space between this value and the line
	*
	* @default 0
	*/
	baseline: import_prop_types.default.oneOfType([import_prop_types.default.oneOf(["max", "min"]), import_prop_types.default.number]),
	/**
	* Configuration for the brush interaction.
	*/
	brushConfig: import_prop_types.default.shape({
		enabled: import_prop_types.default.bool,
		preventHighlight: import_prop_types.default.bool,
		preventTooltip: import_prop_types.default.bool
	}),
	children: import_prop_types.default.node,
	className: import_prop_types.default.string,
	/**
	* The clipped area offset in pixels.
	*
	* This prevents partial clipping of lines when they are drawn on the edge of the drawing area.
	*
	* @default { top: 1, right: 1, bottom: 1, left: 1 }
	*/
	clipAreaOffset: import_prop_types.default.shape({
		bottom: import_prop_types.default.number,
		left: import_prop_types.default.number,
		right: import_prop_types.default.number,
		top: import_prop_types.default.number
	}),
	/**
	* Color used to colorize the sparkline.
	* @default rainbowSurgePalette[0]
	*/
	color: import_prop_types.default.oneOfType([import_prop_types.default.func, import_prop_types.default.string]),
	/**
	* @default 'linear'
	*/
	curve: import_prop_types.default.oneOf([
		"bumpX",
		"bumpY",
		"catmullRom",
		"linear",
		"monotoneX",
		"monotoneY",
		"natural",
		"step",
		"stepAfter",
		"stepBefore"
	]),
	/**
	* Data to plot.
	*/
	data: import_prop_types.default.arrayOf(import_prop_types.default.number).isRequired,
	/**
	* An array of objects that can be used to populate series and axes data using their `dataKey` property.
	*/
	dataset: import_prop_types.default.arrayOf(import_prop_types.default.object),
	/**
	* The description of the chart.
	* Used to provide an accessible description for the chart.
	*/
	desc: import_prop_types.default.string,
	/**
	* If `true`, the charts will not listen to the mouse move event.
	* It might break interactive features, but will improve performance.
	* @default false
	*/
	disableAxisListener: import_prop_types.default.bool,
	/**
	* When `true`, the chart's drawing area will not be clipped and elements within can visually overflow the chart.
	*
	* @default false
	*/
	disableClipping: import_prop_types.default.bool,
	/**
	* If true, the hit area interaction is disabled and falls back to hover events.
	*/
	disableHitArea: import_prop_types.default.bool,
	/**
	* If `true`, disables keyboard navigation for the chart.
	*/
	disableKeyboardNavigation: import_prop_types.default.bool,
	/**
	* Options to enable features planned for the next major.
	*/
	experimentalFeatures: import_prop_types.default.shape({
		enablePositionBasedPointerInteraction: import_prop_types.default.bool,
		progressiveRendering: import_prop_types.default.bool
	}),
	/**
	* The height of the chart in px. If not defined, it takes the height of the parent element.
	*/
	height: import_prop_types.default.number,
	/**
	* List of hidden series and/or items.
	*
	* Different chart types use different keys.
	*
	* @example
	* ```ts
	* [
	*   {
	*     type: 'pie',
	*     seriesId: 'series-1',
	*     dataIndex: 3,
	*   },
	*   {
	*     type: 'line',
	*     seriesId: 'series-2',
	*   }
	* ]
	* ```
	*/
	hiddenItems: import_prop_types.default.arrayOf(import_prop_types.default.oneOfType([
		import_prop_types.default.shape({
			dataIndex: import_prop_types.default.number,
			seriesId: import_prop_types.default.string.isRequired,
			type: import_prop_types.default.oneOf(["bar"]).isRequired
		}),
		import_prop_types.default.shape({
			dataIndex: import_prop_types.default.number,
			seriesId: import_prop_types.default.string.isRequired,
			type: import_prop_types.default.oneOf(["line"]).isRequired
		}),
		import_prop_types.default.shape({
			dataIndex: import_prop_types.default.number,
			seriesId: import_prop_types.default.string.isRequired,
			type: import_prop_types.default.oneOf(["pie"]).isRequired
		}),
		import_prop_types.default.shape({
			dataIndex: import_prop_types.default.number,
			seriesId: import_prop_types.default.string.isRequired,
			type: import_prop_types.default.oneOf(["radar"]).isRequired
		}),
		import_prop_types.default.shape({
			dataIndex: import_prop_types.default.number,
			seriesId: import_prop_types.default.string.isRequired,
			type: import_prop_types.default.oneOf(["scatter"]).isRequired
		}),
		import_prop_types.default.shape({
			dataIndex: import_prop_types.default.number,
			seriesId: import_prop_types.default.string.isRequired,
			type: import_prop_types.default.oneOf(["bar"])
		}),
		import_prop_types.default.shape({
			dataIndex: import_prop_types.default.number,
			seriesId: import_prop_types.default.string.isRequired,
			type: import_prop_types.default.oneOf(["line"])
		}),
		import_prop_types.default.shape({
			dataIndex: import_prop_types.default.number,
			seriesId: import_prop_types.default.string.isRequired,
			type: import_prop_types.default.oneOf(["pie"])
		}),
		import_prop_types.default.shape({
			dataIndex: import_prop_types.default.number,
			seriesId: import_prop_types.default.string.isRequired,
			type: import_prop_types.default.oneOf(["radar"])
		}),
		import_prop_types.default.shape({
			dataIndex: import_prop_types.default.number,
			seriesId: import_prop_types.default.string.isRequired,
			type: import_prop_types.default.oneOf(["scatter"])
		})
	]).isRequired),
	/**
	* The controlled axis highlight.
	* Identified by the axis id, and data index.
	*/
	highlightedAxis: import_prop_types.default.arrayOf(import_prop_types.default.shape({
		axisId: import_prop_types.default.oneOfType([import_prop_types.default.number, import_prop_types.default.string]).isRequired,
		dataIndex: import_prop_types.default.number.isRequired
	})),
	/**
	* The highlighted item.
	* Used when the highlight is controlled.
	*/
	highlightedItem: import_prop_types.default.oneOfType([
		import_prop_types.default.shape({
			dataIndex: import_prop_types.default.number,
			seriesId: import_prop_types.default.string.isRequired
		}),
		import_prop_types.default.shape({
			dataIndex: import_prop_types.default.number,
			seriesId: import_prop_types.default.string.isRequired,
			type: import_prop_types.default.oneOf(["bar"]).isRequired
		}),
		import_prop_types.default.shape({
			dataIndex: import_prop_types.default.number,
			seriesId: import_prop_types.default.string.isRequired,
			type: import_prop_types.default.oneOf(["line"]).isRequired
		}),
		import_prop_types.default.shape({
			dataIndex: import_prop_types.default.number,
			seriesId: import_prop_types.default.string.isRequired,
			type: import_prop_types.default.oneOf(["pie"]).isRequired
		}),
		import_prop_types.default.shape({
			dataIndex: import_prop_types.default.number,
			seriesId: import_prop_types.default.string.isRequired,
			type: import_prop_types.default.oneOf(["radar"]).isRequired
		}),
		import_prop_types.default.shape({
			dataIndex: import_prop_types.default.number,
			seriesId: import_prop_types.default.string.isRequired,
			type: import_prop_types.default.oneOf(["scatter"]).isRequired
		})
	]),
	/**
	* Defines the maximum distance between a scatter point and the pointer that triggers the interaction.
	* If set to `'item'`, the radius is the `markerSize`.
	* If `undefined`, the radius is assumed to be infinite.
	*/
	hitAreaRadius: import_prop_types.default.oneOfType([import_prop_types.default.oneOf(["item"]), import_prop_types.default.number]),
	/**
	* This prop is used to help implement the accessibility logic.
	* If you don't provide this prop. It falls back to a randomly generated id.
	*/
	id: import_prop_types.default.string,
	/**
	* List of initially hidden series and/or items.
	* Used for uncontrolled state.
	*
	* Different chart types use different keys.
	*
	* @example
	* ```ts
	* [
	*   {
	*     type: 'pie',
	*     seriesId: 'series-1',
	*     dataIndex: 3,
	*   },
	*   {
	*     type: 'line',
	*     seriesId: 'series-2',
	*   }
	* ]
	* ```
	*/
	initialHiddenItems: import_prop_types.default.arrayOf(import_prop_types.default.oneOfType([
		import_prop_types.default.shape({
			dataIndex: import_prop_types.default.number,
			seriesId: import_prop_types.default.string.isRequired,
			type: import_prop_types.default.oneOf(["bar"]).isRequired
		}),
		import_prop_types.default.shape({
			dataIndex: import_prop_types.default.number,
			seriesId: import_prop_types.default.string.isRequired,
			type: import_prop_types.default.oneOf(["line"]).isRequired
		}),
		import_prop_types.default.shape({
			dataIndex: import_prop_types.default.number,
			seriesId: import_prop_types.default.string.isRequired,
			type: import_prop_types.default.oneOf(["pie"]).isRequired
		}),
		import_prop_types.default.shape({
			dataIndex: import_prop_types.default.number,
			seriesId: import_prop_types.default.string.isRequired,
			type: import_prop_types.default.oneOf(["radar"]).isRequired
		}),
		import_prop_types.default.shape({
			dataIndex: import_prop_types.default.number,
			seriesId: import_prop_types.default.string.isRequired,
			type: import_prop_types.default.oneOf(["scatter"]).isRequired
		}),
		import_prop_types.default.shape({
			dataIndex: import_prop_types.default.number,
			seriesId: import_prop_types.default.string.isRequired,
			type: import_prop_types.default.oneOf(["bar"])
		}),
		import_prop_types.default.shape({
			dataIndex: import_prop_types.default.number,
			seriesId: import_prop_types.default.string.isRequired,
			type: import_prop_types.default.oneOf(["line"])
		}),
		import_prop_types.default.shape({
			dataIndex: import_prop_types.default.number,
			seriesId: import_prop_types.default.string.isRequired,
			type: import_prop_types.default.oneOf(["pie"])
		}),
		import_prop_types.default.shape({
			dataIndex: import_prop_types.default.number,
			seriesId: import_prop_types.default.string.isRequired,
			type: import_prop_types.default.oneOf(["radar"])
		}),
		import_prop_types.default.shape({
			dataIndex: import_prop_types.default.number,
			seriesId: import_prop_types.default.string.isRequired,
			type: import_prop_types.default.oneOf(["scatter"])
		})
	]).isRequired),
	/**
	* Localized text for chart components.
	*/
	localeText: import_prop_types.default.object,
	/**
	* The margin between the SVG and the drawing area.
	* It's used for leaving some space for extra information such as the x- and y-axis or legend.
	*
	* Accepts a `number` to be used on all sides or an object with the optional properties: `top`, `bottom`, `left`, and `right`.
	* @default 5
	*/
	margin: import_prop_types.default.oneOfType([import_prop_types.default.number, import_prop_types.default.shape({
		bottom: import_prop_types.default.number,
		left: import_prop_types.default.number,
		right: import_prop_types.default.number,
		top: import_prop_types.default.number
	})]),
	/**
	* The function called for onClick events.
	* The second argument contains information about all line/bar elements at the current mouse position.
	* @param {MouseEvent} event The mouse event recorded on the `<svg/>` element.
	* @param {null | ChartsAxisData} data The data about the clicked axis and items associated with it.
	*/
	onAxisClick: import_prop_types.default.func,
	/**
	* Callback fired when any hidden identifiers change.
	* @param {VisibilityIdentifierWithType[]} hiddenItems The new list of hidden identifiers.
	*/
	onHiddenItemsChange: import_prop_types.default.func,
	/**
	* The callback fired when the highlighted item changes.
	*
	* @param {HighlightItemIdentifierWithType<SeriesType> | null} highlightedItem  The newly highlighted item.
	*/
	onHighlightChange: import_prop_types.default.func,
	/**
	* The function called when the pointer position corresponds to a new axis data item.
	* This update can either be caused by a pointer movement, or an axis update.
	* In case of multiple axes, the function is called if at least one axis is updated.
	* The argument contains the identifier for all axes with a `data` property.
	* @param {AxisItemIdentifier[]} axisItems The array of axes item identifiers.
	*/
	onHighlightedAxisChange: import_prop_types.default.func,
	/**
	* Callback fired when clicking close to an item.
	* This is only available for scatter plot for now.
	* @param {MouseEvent} event Mouse event caught at the svg level
	* @param {ScatterItemIdentifier} scatterItemIdentifier Identify which item got clicked
	*/
	onItemClick: import_prop_types.default.func,
	/**
	* The function called when the pointer position corresponds to a new axis data item.
	* This update can either be caused by a pointer movement, or an axis update.
	* In case of multiple axes, the function is called if at least one axis is updated.
	* The argument contains the identifier for all axes with a `data` property.
	* @param {AxisItemIdentifier[]} axisItems The array of axes item identifiers.
	*/
	onTooltipAxisChange: import_prop_types.default.func,
	/**
	* The callback fired when the tooltip item changes.
	*
	* @param {SeriesItemIdentifier<SeriesType> | null} tooltipItem  The newly highlighted item.
	*/
	onTooltipItemChange: import_prop_types.default.func,
	/**
	* Type of plot used.
	* @default 'line'
	*/
	plotType: import_prop_types.default.oneOf(["bar", "line"]),
	/**
	* Set to `true` to highlight the value.
	* With line, it shows a point.
	* With bar, it shows a highlight band.
	* @default false
	*/
	showHighlight: import_prop_types.default.bool,
	/**
	* Set to `true` to enable the tooltip in the sparkline.
	* @default false
	*/
	showTooltip: import_prop_types.default.bool,
	/**
	* If `true`, animations are skipped.
	* If unset or `false`, the animations respects the user's `prefers-reduced-motion` setting.
	*/
	skipAnimation: import_prop_types.default.bool,
	/**
	* The props used for each component slot.
	* @default {}
	*/
	slotProps: import_prop_types.default.object,
	/**
	* Overridable component slots.
	* @default {}
	*/
	slots: import_prop_types.default.object,
	sx: import_prop_types.default.oneOfType([
		import_prop_types.default.arrayOf(import_prop_types.default.oneOfType([
			import_prop_types.default.func,
			import_prop_types.default.object,
			import_prop_types.default.bool
		])),
		import_prop_types.default.func,
		import_prop_types.default.object
	]),
	theme: import_prop_types.default.oneOf(["dark", "light"]),
	/**
	* The title of the chart.
	* Used to provide an accessible label for the chart.
	*/
	title: import_prop_types.default.string,
	/**
	* The controlled axis tooltip.
	* Identified by the axis id, and data index.
	*/
	tooltipAxis: import_prop_types.default.arrayOf(import_prop_types.default.shape({
		axisId: import_prop_types.default.oneOfType([import_prop_types.default.number, import_prop_types.default.string]).isRequired,
		dataIndex: import_prop_types.default.number.isRequired
	})),
	/**
	* The tooltip item.
	* Used when the tooltip is controlled.
	*/
	tooltipItem: import_prop_types.default.oneOfType([
		import_prop_types.default.shape({
			dataIndex: import_prop_types.default.number.isRequired,
			seriesId: import_prop_types.default.string.isRequired
		}),
		import_prop_types.default.shape({
			dataIndex: import_prop_types.default.number.isRequired,
			seriesId: import_prop_types.default.string.isRequired,
			type: import_prop_types.default.oneOf(["bar"]).isRequired
		}),
		import_prop_types.default.shape({
			dataIndex: import_prop_types.default.number.isRequired,
			seriesId: import_prop_types.default.string.isRequired,
			type: import_prop_types.default.oneOf(["pie"]).isRequired
		}),
		import_prop_types.default.shape({
			dataIndex: import_prop_types.default.number.isRequired,
			seriesId: import_prop_types.default.string.isRequired,
			type: import_prop_types.default.oneOf(["scatter"]).isRequired
		}),
		import_prop_types.default.shape({
			dataIndex: import_prop_types.default.number,
			seriesId: import_prop_types.default.string.isRequired
		}),
		import_prop_types.default.shape({
			dataIndex: import_prop_types.default.number,
			seriesId: import_prop_types.default.string.isRequired,
			type: import_prop_types.default.oneOf(["line"]).isRequired
		}),
		import_prop_types.default.shape({
			dataIndex: import_prop_types.default.number,
			seriesId: import_prop_types.default.string.isRequired,
			type: import_prop_types.default.oneOf(["radar"]).isRequired
		})
	]),
	/**
	* Formatter used by the tooltip.
	* @param {number} value The value to format.
	* @returns {string} the formatted value.
	* @default (value: number | null) => (value === null ? '' : value.toString())
	*/
	valueFormatter: import_prop_types.default.func,
	/**
	* The width of the chart in px. If not defined, it takes the width of the parent element.
	*/
	width: import_prop_types.default.number,
	/**
	* The xAxis configuration.
	* Notice it is a single [[AxisConfig]] object, not an array of configuration.
	*/
	xAxis: import_prop_types.default.object,
	/**
	* The yAxis configuration.
	* Notice it is a single [[AxisConfig]] object, not an array of configuration.
	*/
	yAxis: import_prop_types.default.object
};
//#endregion
//#region node_modules/@mui/x-charts/Gauge/utils.mjs
function getPoint(angle) {
	const radAngle = deg2rad(angle);
	return [Math.sin(radAngle), -Math.cos(radAngle)];
}
/**
* Returns the ratio of the arc bounding box and its center.
* @param startAngle The start angle (in deg)
* @param endAngle The end angle (in deg)
*/
function getArcRatios(startAngle, endAngle) {
	const points = [
		[0, 0],
		getPoint(startAngle),
		getPoint(endAngle)
	];
	const minAngle = Math.min(startAngle, endAngle);
	const maxAngle = Math.max(startAngle, endAngle);
	const initialAngle = Math.floor(minAngle / 90) * 90;
	for (let step = 1; step <= 4; step += 1) {
		const cardinalAngle = initialAngle + step * 90;
		if (cardinalAngle < maxAngle) points.push(getPoint(cardinalAngle));
	}
	const minX = Math.min(...points.map(([x]) => x));
	const maxX = Math.max(...points.map(([x]) => x));
	const minY = Math.min(...points.map(([, y]) => y));
	const maxY = Math.max(...points.map(([, y]) => y));
	return {
		cx: -minX / (maxX - minX),
		cy: -minY / (maxY - minY),
		minX,
		maxX,
		minY,
		maxY
	};
}
function getAvailableRadius(cx, cy, width, height, { minX, maxX, minY, maxY }) {
	return Math.min(...[
		{
			ratio: Math.abs(minX),
			space: cx
		},
		{
			ratio: Math.abs(maxX),
			space: width - cx
		},
		{
			ratio: Math.abs(minY),
			space: cy
		},
		{
			ratio: Math.abs(maxY),
			space: height - cy
		}
	].map(({ ratio, space }) => {
		if (ratio < 1e-5) return Infinity;
		return space / ratio;
	}));
}
//#endregion
//#region node_modules/@mui/x-charts/Gauge/GaugeProvider.mjs
var GaugeContext = /*#__PURE__*/ import_react.createContext({
	value: null,
	valueMin: 0,
	valueMax: 0,
	startAngle: 0,
	endAngle: 0,
	innerRadius: 0,
	outerRadius: 0,
	cornerRadius: 0,
	cx: 0,
	cy: 0,
	maxRadius: 0,
	valueAngle: null
});
GaugeContext.displayName = "GaugeContext";
function GaugeProvider(props) {
	const { value = null, valueMin = 0, valueMax = 100, startAngle = 0, endAngle = 360, outerRadius: outerRadiusParam, innerRadius: innerRadiusParam, cornerRadius: cornerRadiusParam, cx: cxParam, cy: cyParam, children } = props;
	const { left, top, width, height } = useDrawingArea();
	const ratios = getArcRatios(startAngle, endAngle);
	const innerCx = cxParam ? getPercentageValue(cxParam, width) : ratios.cx * width;
	const innerCy = cyParam ? getPercentageValue(cyParam, height) : ratios.cy * height;
	let cx = left + innerCx;
	let cy = top + innerCy;
	const maxRadius = getAvailableRadius(innerCx, innerCy, width, height, ratios);
	if (cxParam === void 0) {
		const usedWidth = maxRadius * (ratios.maxX - ratios.minX);
		cx = left + (width - usedWidth) / 2 + ratios.cx * usedWidth;
	}
	if (cyParam === void 0) {
		const usedHeight = maxRadius * (ratios.maxY - ratios.minY);
		cy = top + (height - usedHeight) / 2 + ratios.cy * usedHeight;
	}
	const outerRadius = getPercentageValue(outerRadiusParam ?? maxRadius, maxRadius);
	const innerRadius = getPercentageValue(innerRadiusParam ?? "80%", maxRadius);
	const cornerRadius = getPercentageValue(cornerRadiusParam ?? 0, outerRadius - innerRadius);
	const contextValue = import_react.useMemo(() => {
		const startAngleRad = deg2rad(startAngle);
		const endAngleRad = deg2rad(endAngle);
		return {
			value,
			valueMin,
			valueMax,
			startAngle: startAngleRad,
			endAngle: endAngleRad,
			outerRadius,
			innerRadius,
			cornerRadius,
			cx,
			cy,
			maxRadius,
			valueAngle: value === null ? null : startAngleRad + (endAngleRad - startAngleRad) * (value - valueMin) / (valueMax - valueMin)
		};
	}, [
		value,
		valueMin,
		valueMax,
		startAngle,
		endAngle,
		outerRadius,
		innerRadius,
		cornerRadius,
		cx,
		cy,
		maxRadius
	]);
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(GaugeContext.Provider, {
		value: contextValue,
		children
	});
}
function useGaugeState() {
	return import_react.useContext(GaugeContext);
}
//#endregion
//#region node_modules/@mui/x-charts/Gauge/GaugeContainer.mjs
var _excluded$10 = [
	"width",
	"height",
	"margin",
	"title",
	"desc",
	"value",
	"valueMin",
	"valueMax",
	"startAngle",
	"endAngle",
	"outerRadius",
	"innerRadius",
	"cornerRadius",
	"cx",
	"cy",
	"children"
];
var GStyled = styled("g", {
	slot: "internal",
	shouldForwardProp: void 0
})(({ theme }) => ({ "& text": { fill: (theme.vars || theme).palette.text.primary } }));
var GaugeContainer = /*#__PURE__*/ import_react.forwardRef(function GaugeContainer(props, ref) {
	const { width: inWidth, height: inHeight, margin, title, desc, value, valueMin = 0, valueMax = 100, startAngle, endAngle, outerRadius, innerRadius, cornerRadius, cx, cy, children } = props, other = _objectWithoutPropertiesLoose(props, _excluded$10);
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ChartsProvider, {
		pluginParams: {
			width: inWidth,
			height: inHeight,
			margin: defaultizeMargin(margin, {
				left: 10,
				right: 10,
				top: 10,
				bottom: 10
			})
		},
		plugins: [],
		children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(GaugeProvider, {
			value,
			valueMin,
			valueMax,
			startAngle,
			endAngle,
			outerRadius,
			innerRadius,
			cornerRadius,
			cx,
			cy,
			children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ChartsSurface, _extends({
				title,
				desc,
				role: "meter",
				"aria-valuenow": value === null ? void 0 : value,
				"aria-valuemin": valueMin,
				"aria-valuemax": valueMax
			}, other, {
				ref,
				children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(GStyled, {
					"aria-hidden": "true",
					children
				})
			}))
		})
	});
});
GaugeContainer.displayName = "GaugeContainer";
GaugeContainer.propTypes = {
	children: import_prop_types.default.node,
	className: import_prop_types.default.string,
	/**
	* The radius applied to arc corners (similar to border radius).
	* Set it to '50%' to get rounded arc.
	* @default 0
	*/
	cornerRadius: import_prop_types.default.oneOfType([import_prop_types.default.number, import_prop_types.default.string]),
	/**
	* The x coordinate of the arc center.
	* Can be a number (in px) or a string with a percentage such as '50%'.
	* The '100%' is the width the drawing area.
	*/
	cx: import_prop_types.default.oneOfType([import_prop_types.default.number, import_prop_types.default.string]),
	/**
	* The y coordinate of the arc center.
	* Can be a number (in px) or a string with a percentage such as '50%'.
	* The '100%' is the height the drawing area.
	*/
	cy: import_prop_types.default.oneOfType([import_prop_types.default.number, import_prop_types.default.string]),
	/**
	* The description of the chart.
	* Used to provide an accessible description for the chart.
	*/
	desc: import_prop_types.default.string,
	/**
	* The end angle (deg).
	* @default 360
	*/
	endAngle: import_prop_types.default.number,
	/**
	* The height of the chart in px. If not defined, it takes the height of the parent element.
	*/
	height: import_prop_types.default.number,
	/**
	* This prop is used to help implement the accessibility logic.
	* If you don't provide this prop. It falls back to a randomly generated id.
	*/
	id: import_prop_types.default.string,
	/**
	* The radius between circle center and the beginning of the arc.
	* Can be a number (in px) or a string with a percentage such as '50%'.
	* The '100%' is the maximal radius that fit into the drawing area.
	* @default '80%'
	*/
	innerRadius: import_prop_types.default.oneOfType([import_prop_types.default.number, import_prop_types.default.string]),
	/**
	* The margin between the SVG and the drawing area.
	* It's used for leaving some space for extra information such as the x- and y-axis or legend.
	*
	* Accepts a `number` to be used on all sides or an object with the optional properties: `top`, `bottom`, `left`, and `right`.
	*/
	margin: import_prop_types.default.oneOfType([import_prop_types.default.number, import_prop_types.default.shape({
		bottom: import_prop_types.default.number,
		left: import_prop_types.default.number,
		right: import_prop_types.default.number,
		top: import_prop_types.default.number
	})]),
	/**
	* The radius between circle center and the end of the arc.
	* Can be a number (in px) or a string with a percentage such as '50%'.
	* The '100%' is the maximal radius that fit into the drawing area.
	* @default '100%'
	*/
	outerRadius: import_prop_types.default.oneOfType([import_prop_types.default.number, import_prop_types.default.string]),
	/**
	* If `true`, animations are skipped.
	* If unset or `false`, the animations respects the user's `prefers-reduced-motion` setting.
	*/
	skipAnimation: import_prop_types.default.bool,
	/**
	* The start angle (deg).
	* @default 0
	*/
	startAngle: import_prop_types.default.number,
	sx: import_prop_types.default.oneOfType([
		import_prop_types.default.arrayOf(import_prop_types.default.oneOfType([
			import_prop_types.default.func,
			import_prop_types.default.object,
			import_prop_types.default.bool
		])),
		import_prop_types.default.func,
		import_prop_types.default.object
	]),
	/**
	* The title of the chart.
	* Used to provide an accessible label for the chart.
	*/
	title: import_prop_types.default.string,
	/**
	* The value of the gauge.
	* Set to `null` to not display a value.
	*/
	value: import_prop_types.default.number,
	/**
	* The maximal value of the gauge.
	* @default 100
	*/
	valueMax: import_prop_types.default.number,
	/**
	* The minimal value of the gauge.
	* @default 0
	*/
	valueMin: import_prop_types.default.number,
	/**
	* The width of the chart in px. If not defined, it takes the width of the parent element.
	*/
	width: import_prop_types.default.number
};
//#endregion
//#region node_modules/@mui/x-charts/hooks/animation/useAnimateGaugeValueArc.mjs
function gaugeValueArcPropsInterpolator(from, to) {
	const interpolateStartAngle = number_default(from.startAngle, to.startAngle);
	const interpolateEndAngle = number_default(from.endAngle, to.endAngle);
	const interpolateInnerRadius = number_default(from.innerRadius, to.innerRadius);
	const interpolateOuterRadius = number_default(from.outerRadius, to.outerRadius);
	const interpolateCornerRadius = number_default(from.cornerRadius, to.cornerRadius);
	return (t) => {
		return {
			startAngle: interpolateStartAngle(t),
			endAngle: interpolateEndAngle(t),
			innerRadius: interpolateInnerRadius(t),
			outerRadius: interpolateOuterRadius(t),
			cornerRadius: interpolateCornerRadius(t)
		};
	};
}
/** Animates a arc of a gauge chart by increasing the `endAngle` from the start angle to the end angle.
* The props object also accepts a `ref` which will be merged with the ref returned from this hook. This means you can
* pass the ref returned by this hook to the `path` element and the `ref` provided as argument will also be called. */
function useAnimateGaugeValueArc(props) {
	return useAnimate({
		startAngle: props.startAngle,
		endAngle: props.endAngle,
		innerRadius: props.innerRadius,
		outerRadius: props.outerRadius,
		cornerRadius: props.cornerRadius
	}, {
		createInterpolator: gaugeValueArcPropsInterpolator,
		transformProps: (p) => ({ d: arc_default().cornerRadius(p.cornerRadius)({
			innerRadius: p.innerRadius,
			outerRadius: p.outerRadius,
			startAngle: p.startAngle,
			endAngle: p.endAngle
		}) }),
		applyProps(element, p) {
			element.setAttribute("d", p.d);
		},
		initialProps: {
			startAngle: props.startAngle,
			endAngle: props.startAngle,
			innerRadius: props.innerRadius,
			outerRadius: props.outerRadius,
			cornerRadius: props.cornerRadius
		},
		skip: props.skipAnimation,
		ref: props.ref
	});
}
//#endregion
//#region node_modules/@mui/x-charts/Gauge/gaugeClasses.mjs
function getGaugeUtilityClass(slot) {
	return generateUtilityClass("MuiGauge", slot);
}
var gaugeClasses = generateUtilityClasses("MuiGauge", [
	"root",
	"valueArc",
	"referenceArc",
	"valueText"
]);
//#endregion
//#region node_modules/@mui/x-charts/Gauge/GaugeValueArc.mjs
var _excluded$9 = ["className"], _excluded2 = [
	"cx",
	"cy",
	"startAngle",
	"endAngle",
	"cornerRadius",
	"innerRadius",
	"outerRadius",
	"skipAnimation"
];
var StyledPath$1 = styled("path", {
	name: "MuiGauge",
	slot: "ValueArc"
})(({ theme }) => ({ fill: (theme.vars || theme).palette.primary.main }));
function GaugeValueArc(_ref) {
	let { className } = _ref, other = _objectWithoutPropertiesLoose(_ref, _excluded$9);
	const { value, valueMin, valueMax, startAngle, endAngle, outerRadius, innerRadius, cornerRadius, cx, cy } = useGaugeState();
	if (value === null) return null;
	const valueAngle = startAngle + (value - valueMin) / (valueMax - valueMin) * (endAngle - startAngle);
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(AnimatedGaugeValueArc, _extends({}, other, {
		className: clsx(gaugeClasses.valueArc, className),
		cx,
		cy,
		startAngle,
		endAngle: valueAngle,
		cornerRadius,
		innerRadius,
		outerRadius
	}));
}
GaugeValueArc.propTypes = { skipAnimation: import_prop_types.default.bool };
function AnimatedGaugeValueArc(_ref2) {
	let { cx, cy, startAngle, endAngle, cornerRadius, innerRadius, outerRadius, skipAnimation: inSkipAnimation } = _ref2, other = _objectWithoutPropertiesLoose(_ref2, _excluded2);
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(StyledPath$1, _extends({}, useAnimateGaugeValueArc({
		startAngle,
		endAngle,
		cornerRadius,
		innerRadius,
		outerRadius,
		skipAnimation: useSkipAnimation(inSkipAnimation)
	}), { transform: `translate(${cx}, ${cy})` }, other));
}
AnimatedGaugeValueArc.propTypes = {
	cornerRadius: import_prop_types.default.number.isRequired,
	cx: import_prop_types.default.number.isRequired,
	cy: import_prop_types.default.number.isRequired,
	endAngle: import_prop_types.default.number.isRequired,
	innerRadius: import_prop_types.default.number.isRequired,
	outerRadius: import_prop_types.default.number.isRequired,
	skipAnimation: import_prop_types.default.bool,
	startAngle: import_prop_types.default.number.isRequired
};
//#endregion
//#region node_modules/@mui/x-charts/Gauge/GaugeReferenceArc.mjs
var _excluded$8 = ["className"];
var StyledPath = styled("path", {
	name: "MuiGauge",
	slot: "ReferenceArc"
})(({ theme }) => ({ fill: (theme.vars || theme).palette.divider }));
function GaugeReferenceArc(_ref) {
	let { className } = _ref, other = _objectWithoutPropertiesLoose(_ref, _excluded$8);
	const { startAngle, endAngle, outerRadius, innerRadius, cornerRadius, cx, cy } = useGaugeState();
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(StyledPath, _extends({
		className: clsx(gaugeClasses.referenceArc, className),
		transform: `translate(${cx}, ${cy})`,
		d: arc_default().cornerRadius(cornerRadius)({
			startAngle,
			endAngle,
			innerRadius,
			outerRadius
		})
	}, other));
}
//#endregion
//#region node_modules/@mui/x-charts/Gauge/GaugeValueText.mjs
var _excluded$7 = ["text", "className"];
function defaultFormatter({ value }) {
	return value === null ? null : value.toLocaleString();
}
function GaugeValueText(props) {
	const { text = defaultFormatter, className } = props, other = _objectWithoutPropertiesLoose(props, _excluded$7);
	const { value, valueMin, valueMax, cx, cy } = useGaugeState();
	const formattedText = typeof text === "function" ? text({
		value,
		valueMin,
		valueMax
	}) : text;
	if (formattedText === null) return null;
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)("g", {
		className: clsx(gaugeClasses.valueText, className),
		children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ChartsText, _extends({
			x: cx,
			y: cy,
			text: formattedText,
			style: {
				textAnchor: "middle",
				dominantBaseline: "central"
			}
		}, other))
	});
}
GaugeValueText.propTypes = {
	/**
	* Height of a text line (in `em`).
	*/
	lineHeight: import_prop_types.default.number,
	/**
	* If `true`, the line width is computed.
	* @default false
	*/
	needsComputation: import_prop_types.default.bool,
	ownerState: import_prop_types.default.any,
	/**
	* Style applied to text elements.
	*/
	style: import_prop_types.default.object,
	text: import_prop_types.default.oneOfType([import_prop_types.default.func, import_prop_types.default.string])
};
//#endregion
//#region node_modules/@mui/x-charts/Gauge/Gauge.mjs
var _excluded$6 = [
	"text",
	"children",
	"classes",
	"className",
	"skipAnimation"
];
var useUtilityClasses$5 = (props) => {
	const { classes } = props;
	return composeClasses({ root: ["root"] }, getGaugeUtilityClass, classes);
};
var Gauge = /*#__PURE__*/ import_react.forwardRef(function Gauge(props, ref) {
	const { text, children, className, skipAnimation } = props;
	return /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(GaugeContainer, _extends({}, _objectWithoutPropertiesLoose(props, _excluded$6), {
		className: clsx(useUtilityClasses$5(props).root, className),
		ref,
		children: [
			/*#__PURE__*/ (0, import_jsx_runtime.jsx)(GaugeReferenceArc, {}),
			/*#__PURE__*/ (0, import_jsx_runtime.jsx)(GaugeValueArc, { skipAnimation }),
			/*#__PURE__*/ (0, import_jsx_runtime.jsx)(GaugeValueText, { text }),
			children
		]
	}));
});
Gauge.displayName = "Gauge";
Gauge.propTypes = {
	children: import_prop_types.default.node,
	classes: import_prop_types.default.object,
	className: import_prop_types.default.string,
	/**
	* The radius applied to arc corners (similar to border radius).
	* Set it to '50%' to get rounded arc.
	* @default 0
	*/
	cornerRadius: import_prop_types.default.oneOfType([import_prop_types.default.number, import_prop_types.default.string]),
	/**
	* The x coordinate of the arc center.
	* Can be a number (in px) or a string with a percentage such as '50%'.
	* The '100%' is the width the drawing area.
	*/
	cx: import_prop_types.default.oneOfType([import_prop_types.default.number, import_prop_types.default.string]),
	/**
	* The y coordinate of the arc center.
	* Can be a number (in px) or a string with a percentage such as '50%'.
	* The '100%' is the height the drawing area.
	*/
	cy: import_prop_types.default.oneOfType([import_prop_types.default.number, import_prop_types.default.string]),
	/**
	* The description of the chart.
	* Used to provide an accessible description for the chart.
	*/
	desc: import_prop_types.default.string,
	/**
	* The end angle (deg).
	* @default 360
	*/
	endAngle: import_prop_types.default.number,
	/**
	* The height of the chart in px. If not defined, it takes the height of the parent element.
	*/
	height: import_prop_types.default.number,
	/**
	* This prop is used to help implement the accessibility logic.
	* If you don't provide this prop. It falls back to a randomly generated id.
	*/
	id: import_prop_types.default.string,
	/**
	* The radius between circle center and the beginning of the arc.
	* Can be a number (in px) or a string with a percentage such as '50%'.
	* The '100%' is the maximal radius that fit into the drawing area.
	* @default '80%'
	*/
	innerRadius: import_prop_types.default.oneOfType([import_prop_types.default.number, import_prop_types.default.string]),
	/**
	* The margin between the SVG and the drawing area.
	* It's used for leaving some space for extra information such as the x- and y-axis or legend.
	*
	* Accepts a `number` to be used on all sides or an object with the optional properties: `top`, `bottom`, `left`, and `right`.
	*/
	margin: import_prop_types.default.oneOfType([import_prop_types.default.number, import_prop_types.default.shape({
		bottom: import_prop_types.default.number,
		left: import_prop_types.default.number,
		right: import_prop_types.default.number,
		top: import_prop_types.default.number
	})]),
	/**
	* The radius between circle center and the end of the arc.
	* Can be a number (in px) or a string with a percentage such as '50%'.
	* The '100%' is the maximal radius that fit into the drawing area.
	* @default '100%'
	*/
	outerRadius: import_prop_types.default.oneOfType([import_prop_types.default.number, import_prop_types.default.string]),
	/**
	* If `true`, animations are skipped.
	* If unset or `false`, the animations respects the user's `prefers-reduced-motion` setting.
	*/
	skipAnimation: import_prop_types.default.bool,
	/**
	* The start angle (deg).
	* @default 0
	*/
	startAngle: import_prop_types.default.number,
	sx: import_prop_types.default.oneOfType([
		import_prop_types.default.arrayOf(import_prop_types.default.oneOfType([
			import_prop_types.default.func,
			import_prop_types.default.object,
			import_prop_types.default.bool
		])),
		import_prop_types.default.func,
		import_prop_types.default.object
	]),
	text: import_prop_types.default.oneOfType([import_prop_types.default.func, import_prop_types.default.string]),
	/**
	* The title of the chart.
	* Used to provide an accessible label for the chart.
	*/
	title: import_prop_types.default.string,
	/**
	* The value of the gauge.
	* Set to `null` to not display a value.
	*/
	value: import_prop_types.default.number,
	/**
	* The maximal value of the gauge.
	* @default 100
	*/
	valueMax: import_prop_types.default.number,
	/**
	* The minimal value of the gauge.
	* @default 0
	*/
	valueMin: import_prop_types.default.number,
	/**
	* The width of the chart in px. If not defined, it takes the width of the parent element.
	*/
	width: import_prop_types.default.number
};
//#endregion
//#region node_modules/@mui/x-charts/RadarChart/RadarChart.plugins.mjs
var RADAR_PLUGINS = [
	useChartTooltip,
	useChartInteraction,
	useChartPolarAxis,
	useChartHighlight,
	useChartKeyboardNavigation,
	useChartVisibilityManager
];
//#endregion
//#region node_modules/@mui/x-charts/RadarChart/useRadarChartProps.mjs
var _excluded$5 = [
	"apiRef",
	"series",
	"radar",
	"width",
	"height",
	"margin",
	"colors",
	"sx",
	"children",
	"slots",
	"slotProps",
	"skipAnimation",
	"loading",
	"highlightedItem",
	"onHighlightChange",
	"hideLegend",
	"divisions",
	"shape",
	"stripeColor",
	"highlight",
	"showToolbar",
	"onAxisClick",
	"onAreaClick",
	"onMarkClick",
	"disableKeyboardNavigation",
	"className"
];
/**
* A helper function that extracts RadarChartProps from the input props
* and returns an object with props for the children components of RadarChart.
*
* @param props The input props for RadarChart
* @returns An object with props for the children components of RadarChart
*/
var useRadarChartProps = (props) => {
	const { apiRef, series, radar, width, height, margin, colors, sx, children, slots, slotProps, skipAnimation, loading, highlightedItem, onHighlightChange, divisions, shape, stripeColor, highlight = "axis", onAxisClick, onAreaClick, onMarkClick, disableKeyboardNavigation, className } = props, other = _objectWithoutPropertiesLoose(props, _excluded$5);
	const radarDataProviderProps = {
		apiRef,
		series,
		radar,
		highlight,
		width,
		height,
		margin,
		colors,
		highlightedItem,
		onHighlightChange,
		skipAnimation,
		onAxisClick,
		disableKeyboardNavigation,
		plugins: RADAR_PLUGINS
	};
	const overlayProps = {
		slots,
		slotProps,
		loading
	};
	const legendProps = {
		slots,
		slotProps
	};
	return {
		highlight,
		chartsWrapperProps: {
			sx,
			hideLegend: props.hideLegend ?? false,
			className
		},
		chartsSurfaceProps: other,
		radarDataProviderProps,
		radarGrid: {
			divisions,
			shape,
			stripeColor
		},
		radarSeriesAreaProps: { onItemClick: onAreaClick },
		radarSeriesMarksProps: { onItemClick: onMarkClick },
		overlayProps,
		legendProps,
		children
	};
};
//#endregion
//#region node_modules/@mui/x-charts/RadarChart/RadarGrid/useRadarGridData.mjs
function useRadarGridData() {
	const { instance, store } = useChartsContext();
	const rotationScale = useRotationScale();
	const { radiusAxis } = useRadiusAxes();
	const { cx, cy } = store.use(selectorChartPolarCenter);
	if (!rotationScale || rotationScale.domain().length === 0) return null;
	const metrics = rotationScale.domain();
	const angles = metrics.map((key) => rotationScale(key));
	return {
		center: {
			x: cx,
			y: cy
		},
		corners: metrics.map((metric, dataIndex) => {
			const r = radiusAxis[metric].scale.range()[1];
			const angle = angles[dataIndex];
			const [x, y] = instance.polar2svg(r, angle);
			return {
				x,
				y
			};
		}),
		radius: radiusAxis[metrics[0]].scale.range()[1]
	};
}
//#endregion
//#region node_modules/@mui/x-charts/RadarChart/RadarGrid/SharpRadarGrid.mjs
/**
* @ignore - internal component.
*/
function SharpRadarGrid(props) {
	const { center, corners, divisions, strokeColor, classes } = props;
	const divisionRatio = Array.from({ length: divisions }, (_, index) => (index + 1) / divisions);
	return /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(import_react.Fragment, { children: [corners.map(({ x, y }, i) => /*#__PURE__*/ (0, import_jsx_runtime.jsx)("path", {
		d: `M ${center.x} ${center.y} L ${x} ${y}`,
		stroke: strokeColor,
		strokeWidth: 1,
		strokeOpacity: .3,
		fill: "none",
		className: classes?.gridRadial
	}, i)), divisionRatio.map((ratio) => /*#__PURE__*/ (0, import_jsx_runtime.jsx)("path", {
		d: `M ${corners.map(({ x, y }) => `${center.x * (1 - ratio) + ratio * x} ${center.y * (1 - ratio) + ratio * y}`).join(" L ")} Z`,
		stroke: strokeColor,
		strokeWidth: 1,
		strokeOpacity: .3,
		fill: "none",
		className: classes?.gridDivider
	}, ratio))] });
}
//#endregion
//#region node_modules/@mui/x-charts/RadarChart/RadarGrid/CircularRadarGrid.mjs
/**
* @ignore - internal component.
*/
function CircularRadarGrid(props) {
	const { center, corners, divisions, radius, strokeColor, classes } = props;
	const divisionRadius = Array.from({ length: divisions }, (_, index) => radius * (index + 1) / divisions);
	return /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(import_react.Fragment, { children: [corners.map(({ x, y }, i) => /*#__PURE__*/ (0, import_jsx_runtime.jsx)("path", {
		d: `M ${center.x} ${center.y} L ${x} ${y}`,
		stroke: strokeColor,
		strokeWidth: 1,
		strokeOpacity: .3,
		fill: "none",
		className: classes?.gridRadial
	}, i)), divisionRadius.map((r) => /*#__PURE__*/ (0, import_jsx_runtime.jsx)("circle", {
		cx: center.x,
		cy: center.y,
		r,
		stroke: strokeColor,
		strokeWidth: 1,
		strokeOpacity: .3,
		fill: "none",
		className: classes?.gridDivider
	}, r))] });
}
//#endregion
//#region node_modules/@mui/x-charts/RadarChart/RadarGrid/SharpRadarStripes.mjs
var getPath = (corners, center, outerRatio, innerRatio) => [
	"M",
	[...corners, corners[0]].map(({ x, y }) => `${center.x * (1 - outerRatio) + outerRatio * x} ${center.y * (1 - outerRatio) + outerRatio * y}`).join(" L "),
	"L",
	[...corners, corners[0]].reverse().map(({ x, y }) => `${center.x * (1 - innerRatio) + innerRatio * x} ${center.y * (1 - innerRatio) + innerRatio * y}`).join(" L "),
	"Z"
].join(" ");
/**
* @ignore - internal component.
*/
function SharpRadarStripes(props) {
	const { center, corners, divisions, stripeColor, classes } = props;
	const divisionRatio = Array.from({ length: divisions }, (_, index) => (index + 1) / divisions);
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(import_react.Fragment, { children: divisionRatio.map((ratio, index) => {
		return /*#__PURE__*/ (0, import_jsx_runtime.jsx)("path", {
			d: getPath(corners, center, ratio, divisionRatio[index - 1] ?? 0),
			stroke: "none",
			fill: stripeColor?.(index) ?? "none",
			fillOpacity: .1,
			className: classes?.gridStripe
		}, ratio);
	}) });
}
//#endregion
//#region node_modules/@mui/x-charts/internals/getRingPath.mjs
/**
* Get the SVG path to display a ring (a donut shape) defined by an outer radius and an inner radius.
*
* @param center - The center of the ring.
* @param outerRadius - The outer radius of the ring.
* @param innerRadius - The inner radius of the ring.
* @param angles - The start and end angles of the ring in radians. If not provided, it defaults to a full circle.
* @returns The SVG path string to display the ring.
*/
function getRingPath(center, outerRadius, innerRadius, angles) {
	if (!angles) return [
		`M ${center.x - outerRadius} ${center.y}`,
		`A ${outerRadius} ${outerRadius} 0 1 0 ${center.x + outerRadius} ${center.y}`,
		`A ${outerRadius} ${outerRadius} 0 1 0 ${center.x - outerRadius} ${center.y} Z`,
		`M ${center.x - innerRadius} ${center.y}`,
		`A ${innerRadius} ${innerRadius} 0 1 0 ${center.x + innerRadius} ${center.y}`,
		`A ${innerRadius} ${innerRadius} 0 1 0 ${center.x - innerRadius} ${center.y} Z`
	].join("");
	const isLargeArc = Math.abs(angles.end - angles.start) > Math.PI;
	const isDirectArc = angles.end > angles.start;
	const startOuterPoint = {
		x: center.x + outerRadius * Math.sin(angles.start),
		y: center.y - outerRadius * Math.cos(angles.start)
	};
	const endOuterPoint = {
		x: center.x + outerRadius * Math.sin(angles.end),
		y: center.y - outerRadius * Math.cos(angles.end)
	};
	const startInnerPoint = {
		x: center.x + innerRadius * Math.sin(angles.start),
		y: center.y - innerRadius * Math.cos(angles.start)
	};
	const endInnerPoint = {
		x: center.x + innerRadius * Math.sin(angles.end),
		y: center.y - innerRadius * Math.cos(angles.end)
	};
	return [
		`M ${startOuterPoint.x} ${startOuterPoint.y}`,
		`A ${outerRadius} ${outerRadius} 0 ${isLargeArc ? 1 : 0} ${isDirectArc ? 1 : 0} ${endOuterPoint.x} ${endOuterPoint.y}`,
		`L ${endInnerPoint.x} ${endInnerPoint.y}`,
		`A ${innerRadius} ${innerRadius} 0 ${isLargeArc ? 1 : 0} ${!isDirectArc ? 1 : 0} ${startInnerPoint.x} ${startInnerPoint.y} Z`
	].join("");
}
//#endregion
//#region node_modules/@mui/x-charts/RadarChart/RadarGrid/CircularRadarStripes.mjs
/**
* @ignore - internal component.
*/
function CircularRadarStripes(props) {
	const { center, divisions, radius, stripeColor, classes } = props;
	const divisionRadius = Array.from({ length: divisions }, (_, index) => radius * (index + 1) / divisions);
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(import_react.Fragment, { children: divisionRadius.map((r, index) => {
		return /*#__PURE__*/ (0, import_jsx_runtime.jsx)("path", {
			d: getRingPath(center, r, divisionRadius[index - 1] ?? 0),
			fillRule: "evenodd",
			fill: stripeColor?.(index) ?? "none",
			fillOpacity: .1,
			className: classes?.gridStripe
		}, r);
	}) });
}
//#endregion
//#region node_modules/@mui/x-charts/RadarChart/radarClasses.mjs
function getRadarUtilityClass(slot) {
	return generateUtilityClass("MuiRadarChart", slot);
}
var radarClasses = generateUtilityClasses("MuiRadarChart", [
	"axisRoot",
	"axisLine",
	"axisLabel",
	"gridRadial",
	"gridDivider",
	"gridStripe",
	"seriesRoot",
	"seriesArea",
	"seriesMark",
	"axisHighlightRoot",
	"axisHighlightLine",
	"axisHighlightDot"
]);
var useUtilityClasses$4 = (classes) => {
	return composeClasses({
		axisRoot: ["axisRoot"],
		axisLine: ["axisLine"],
		axisLabel: ["axisLabel"],
		gridRadial: ["gridRadial"],
		gridDivider: ["gridDivider"],
		gridStripe: ["gridStripe"],
		seriesRoot: ["seriesRoot"],
		seriesArea: ["seriesArea"],
		seriesMark: ["seriesMark"],
		axisHighlightRoot: ["axisHighlightRoot"],
		axisHighlightLine: ["axisHighlightLine"],
		axisHighlightDot: ["axisHighlightDot"]
	}, getRadarUtilityClass, classes);
};
//#endregion
//#region node_modules/@mui/x-charts/RadarChart/RadarGrid/RadarGrid.mjs
function RadarGrid(props) {
	const theme = useTheme();
	const { divisions = 5, shape = "sharp", stripeColor = (index) => index % 2 === 1 ? (theme.vars || theme).palette.text.secondary : "none" } = props;
	const gridData = useRadarGridData();
	const classes = useUtilityClasses$4();
	if (gridData === null) return null;
	const { center, corners, radius } = gridData;
	return shape === "sharp" ? /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(import_react.Fragment, { children: [stripeColor && /*#__PURE__*/ (0, import_jsx_runtime.jsx)(SharpRadarStripes, {
		divisions,
		corners,
		center,
		radius,
		stripeColor,
		classes
	}), /*#__PURE__*/ (0, import_jsx_runtime.jsx)(SharpRadarGrid, {
		divisions,
		corners,
		center,
		radius,
		strokeColor: (theme.vars || theme).palette.text.primary,
		classes
	})] }) : /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(import_react.Fragment, { children: [stripeColor && /*#__PURE__*/ (0, import_jsx_runtime.jsx)(CircularRadarStripes, {
		divisions,
		corners,
		center,
		radius,
		stripeColor,
		classes
	}), /*#__PURE__*/ (0, import_jsx_runtime.jsx)(CircularRadarGrid, {
		divisions,
		corners,
		center,
		radius,
		strokeColor: (theme.vars || theme).palette.text.primary,
		classes
	})] });
}
RadarGrid.propTypes = {
	/**
	* The number of divisions in the radar grid.
	* @default 5
	*/
	divisions: import_prop_types.default.number,
	/**
	* The grid shape.
	* @default 'sharp'
	*/
	shape: import_prop_types.default.oneOf(["circular", "sharp"]),
	/**
	* Get stripe fill color. Set it to `null` to remove stripes
	* @param {number} index The index of the stripe band.
	* @returns {string} The color to fill the stripe.
	* @default (index) => index % 2 === 1 ? (theme.vars || theme).palette.text.secondary : 'none'
	*/
	stripeColor: import_prop_types.default.func
};
//#endregion
//#region node_modules/@mui/x-charts/RadarChart/seriesConfig/seriesProcessor.mjs
var defaultRadarValueFormatter = (v) => v == null ? "" : v.toLocaleString();
var seriesProcessor = (params, _, isItemVisible) => {
	const { seriesOrder, series: seriesMap } = params;
	const completedSeries = {};
	seriesOrder.forEach((seriesId) => {
		const series = seriesMap[seriesId];
		const hidden = !isItemVisible?.({
			type: "radar",
			seriesId
		});
		completedSeries[seriesId] = _extends({ labelMarkType: "square" }, series, {
			valueFormatter: series.valueFormatter ?? defaultRadarValueFormatter,
			hidden
		});
	});
	return {
		seriesOrder,
		series: completedSeries
	};
};
//#endregion
//#region node_modules/@mui/x-charts/RadarChart/seriesConfig/getColor.mjs
var getColor = (series) => {
	const getSeriesColor = getSeriesColorFn(series);
	return (dataIndex) => {
		if (dataIndex === void 0) return series.color;
		const value = series.data[dataIndex];
		return getSeriesColor({
			value,
			dataIndex
		});
	};
};
//#endregion
//#region node_modules/@mui/x-charts/RadarChart/seriesConfig/extremums.mjs
var radiusExtremumGetter = ({ series, axisIndex }) => {
	return Object.keys(series).filter((seriesId) => series[seriesId].type === "radar").reduce((acc, seriesId) => {
		const { data } = series[seriesId];
		return [Math.min(data[axisIndex], acc[0]), Math.max(data[axisIndex], acc[1])];
	}, [Infinity, -Infinity]);
};
var rotationExtremumGetter = ({ axis }) => {
	return [Math.min(...axis.data ?? []), Math.max(...axis.data ?? [])];
};
//#endregion
//#region node_modules/@mui/x-charts/RadarChart/seriesConfig/legend.mjs
var legendGetter = (series) => getSeriesLegendItems("radar", series, "square");
//#endregion
//#region node_modules/@mui/x-charts/RadarChart/seriesConfig/tooltip.mjs
var tooltipGetter = (params) => {
	const { series, axesConfig, getColor, identifier } = params;
	const rotationAxis = axesConfig.rotation;
	if (!identifier || !rotationAxis) return null;
	const label = getLabel(series.label, "tooltip");
	const formatter = (v) => rotationAxis.valueFormatter?.(v, {
		location: "tooltip",
		scale: rotationAxis.scale
	}) ?? (v == null ? "" : v.toLocaleString());
	return {
		identifier,
		color: getColor(),
		label,
		markType: series.labelMarkType,
		values: series.data.map((value, dataIndex) => {
			if (identifier.dataIndex != null && identifier.dataIndex !== dataIndex) return null;
			return {
				value,
				formattedValue: series.valueFormatter(value, { dataIndex }),
				markType: series.labelMarkType,
				label: formatter(rotationAxis?.data?.[dataIndex])
			};
		}).filter((v) => v !== null)
	};
};
var axisTooltipGetter = (series) => {
	return Object.values(series).map(() => ({
		direction: "rotation",
		axisId: void 0
	}));
};
//#endregion
//#region node_modules/@mui/x-charts/RadarChart/seriesConfig/getSeriesWithDefaultValues.mjs
var getSeriesWithDefaultValues = (seriesData, seriesIndex, colors) => {
	return _extends({}, seriesData, {
		id: seriesData.id ?? `auto-generated-id-${seriesIndex}`,
		color: seriesData.color ?? colors[seriesIndex % colors.length]
	});
};
//#endregion
//#region node_modules/@mui/x-charts/RadarChart/seriesConfig/tooltipPosition.mjs
var selectorTooltipItemPosition = createSelectorMemoized(selectorChartsTooltipItem, selectorChartSeriesProcessed, selectorChartRotationAxis, selectorChartRadiusAxis, selectorChartDrawingArea, function selectorTooltipItemPosition(identifier, series, rotationAxes, radiusAxes, drawingArea, placement) {
	if (!identifier || identifier.type !== "radar") return null;
	const itemSeries = series.radar?.series[identifier.seriesId];
	if (itemSeries == null) return null;
	if (radiusAxes === void 0 || rotationAxes === void 0) return null;
	const rotationAxis = rotationAxes.axis[rotationAxes.axisIds[0]];
	const angles = (rotationAxis.scale.domain() ?? []).map((key) => rotationAxis.scale(key));
	const { cx, cy } = getDrawingAreaCenter(drawingArea);
	const polar2svg = generatePolar2svg({
		cx,
		cy
	});
	const points = itemSeries.data.map((value, dataIndex) => {
		const rId = radiusAxes.axisIds[dataIndex];
		const r = radiusAxes.axis[rId].scale(value);
		const angle = angles[dataIndex];
		return polar2svg(r, angle);
	});
	if (points.length === 0) return null;
	if (identifier.dataIndex != null) {
		const point = points[identifier.dataIndex];
		switch (placement) {
			case "right": return {
				x: point[0] + 4,
				y: point[1]
			};
			case "bottom": return {
				x: point[0],
				y: point[1] + 4
			};
			case "left": return {
				x: point[0] - 4,
				y: point[1]
			};
			default: return {
				x: point[0],
				y: point[1] - 4
			};
		}
	}
	const [top, right, bottom, left] = points.reduce((acc, [x, y]) => {
		return [
			Math.min(y, acc[0]),
			Math.max(x, acc[1]),
			Math.max(y, acc[2]),
			Math.min(x, acc[3])
		];
	}, [
		Infinity,
		-Infinity,
		-Infinity,
		Infinity
	]);
	switch (placement) {
		case "right": return {
			x: right,
			y: (top + bottom) / 2
		};
		case "bottom": return {
			x: (left + right) / 2,
			y: bottom
		};
		case "left": return {
			x: left,
			y: (top + bottom) / 2
		};
		default: return {
			x: (left + right) / 2,
			y: top
		};
	}
});
//#endregion
//#region node_modules/@mui/x-charts/RadarChart/seriesConfig/keyboardFocusHandler.mjs
var keyboardFocusHandler = createCommonKeyboardFocusHandler(new Set(["radar"]), true);
//#endregion
//#region node_modules/@mui/x-charts/RadarChart/seriesConfig/descriptionGetter.mjs
var descriptionGetter = (params) => {
	const { identifier, series, rotationAxis, localeText } = params;
	const label = getLabel(series.label, "tooltip");
	const dataIndex = identifier.dataIndex;
	if (dataIndex === void 0) return "";
	const value = series.data[dataIndex] ?? null;
	const categoryValue = rotationAxis.data?.[dataIndex] ?? null;
	const formattedValue = series.valueFormatter(value, { dataIndex });
	const formattedCategory = rotationAxis.valueFormatter?.(categoryValue, {
		location: "tooltip",
		scale: rotationAxis.scale
	});
	return localeText.radarDescription({
		value,
		formattedValue: formattedValue ?? "",
		categoryValue,
		formattedCategoryValue: formattedCategory ?? "",
		seriesLabel: label
	});
};
//#endregion
//#region node_modules/@mui/x-charts/RadarChart/seriesConfig/index.mjs
var radarSeriesConfig = {
	colorProcessor: getColor,
	seriesProcessor,
	legendGetter,
	tooltipGetter,
	selectorTooltipItemPosition,
	axisTooltipGetter,
	getSeriesWithDefaultValues,
	radiusExtremumGetter,
	rotationExtremumGetter,
	keyboardFocusHandler,
	identifierSerializer: identifierSerializerSeriesIdDataIndex,
	identifierCleaner: identifierCleanerSeriesIdDataIndex,
	descriptionGetter,
	isHighlightedCreator: createIsHighlighted,
	isFadedCreator: createIsFaded
};
//#endregion
//#region node_modules/@mui/x-charts/RadarChart/RadarDataProvider/RadarDataProvider.mjs
var _excluded$4 = [
	"series",
	"children",
	"width",
	"height",
	"colors",
	"skipAnimation",
	"margin",
	"radar",
	"highlight",
	"plugins"
];
var RADAR_SERIES_CONFIG = { radar: radarSeriesConfig };
var DEFAULT_RADAR_MARGIN = {
	top: 30,
	bottom: 30,
	left: 50,
	right: 50
};
function RadarDataProvider(props) {
	const { series, children, width, height, colors, skipAnimation, margin, radar, highlight, plugins } = props, other = _objectWithoutPropertiesLoose(props, _excluded$4);
	const rotationAxes = import_react.useMemo(() => [{
		id: "radar-rotation-axis",
		scaleType: "point",
		data: radar.metrics.map((metric) => typeof metric === "string" ? metric : metric.name),
		startAngle: radar.startAngle,
		endAngle: radar.startAngle !== void 0 ? radar.startAngle + 360 : void 0,
		labelGap: radar.labelGap,
		valueFormatter: (name, { location }) => radar.labelFormatter?.(name, { location }) ?? name
	}], [radar]);
	const radiusAxis = import_react.useMemo(() => radar.metrics.map((m) => {
		const { name, min = 0, max = radar.max } = typeof m === "string" ? { name: m } : m;
		return {
			id: name,
			label: name,
			scaleType: "linear",
			min,
			max
		};
	}), [radar]);
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ChartsDataProvider, _extends({}, other, {
		series: import_react.useMemo(() => series.map((s) => _extends({
			type: "radar",
			highlightScope: s.highlightScope ?? (highlight === "series" ? {
				highlight: "series",
				fade: "global"
			} : void 0)
		}, s)), [series, highlight]),
		width,
		height,
		margin: import_react.useMemo(() => defaultizeMargin(margin, DEFAULT_RADAR_MARGIN), [margin]),
		colors,
		skipAnimation,
		plugins: plugins ?? RADAR_PLUGINS,
		rotationAxis: rotationAxes,
		radiusAxis,
		seriesConfig: RADAR_SERIES_CONFIG,
		children
	}));
}
//#endregion
//#region node_modules/@mui/x-charts/RadarChart/RadarSeriesPlot/useRadarSeriesData.mjs
/**
* This hook provides all the data needed to display radar series.
* @param querySeriesId The id of the series to display
* @returns
*/
function useRadarSeriesData(querySeriesId) {
	const { instance } = useChartsContext();
	const rotationScale = useRotationScale();
	const { radiusAxis } = useRadiusAxes();
	const radarSeries = useRadarSeries(querySeriesId === void 0 ? void 0 : [querySeriesId]);
	const getHighlightState = useItemHighlightStateGetter();
	const metrics = rotationScale?.domain() ?? [];
	const angles = metrics.map((key) => rotationScale(key));
	return radarSeries.map((series) => {
		const seriesId = series.id;
		const seriesHighlightState = getHighlightState({
			type: "radar",
			seriesId
		});
		const isSeriesHighlighted = seriesHighlightState === "highlighted";
		const isSeriesFaded = seriesHighlightState === "faded";
		const getColor = getSeriesColorFn(series);
		return _extends({}, series, {
			seriesId: series.id,
			isSeriesHighlighted,
			isSeriesFaded,
			points: series.data.map((value, dataIndex) => {
				const pointHighlightState = getHighlightState({
					type: "radar",
					seriesId,
					dataIndex
				});
				const highlighted = pointHighlightState === "highlighted";
				const faded = pointHighlightState === "faded";
				const r = radiusAxis[metrics[dataIndex]].scale(value);
				const angle = angles[dataIndex];
				const [x, y] = instance.polar2svg(r, angle);
				return {
					x,
					y,
					isItemHighlighted: highlighted,
					isItemFaded: faded,
					dataIndex,
					value,
					color: getColor({
						value,
						dataIndex
					})
				};
			})
		});
	});
}
//#endregion
//#region node_modules/@mui/x-charts/RadarChart/RadarSeriesPlot/useInteractionAllItemProps.mjs
var useInteractionAllItemProps = (data, skip) => {
	const { instance } = useChartsContext();
	return import_react.useMemo(() => {
		return data.map((item) => {
			return skip ? {} : getInteractionItemProps(instance, {
				type: "radar",
				seriesId: item.seriesId,
				dataIndex: item.dataIndex
			});
		});
	}, [
		data,
		instance,
		skip
	]);
};
//#endregion
//#region node_modules/@mui/x-charts/RadarChart/RadarSeriesPlot/getAreaPath.mjs
function getAreaPath(points) {
	return `M ${points.map((p) => `${p.x} ${p.y}`).join("L")} Z`;
}
//#endregion
//#region node_modules/@mui/x-charts/RadarChart/RadarSeriesPlot/useRadarRotationIndex.mjs
/**
* This hook provides a function that from pointer event returns the rotation index.
* @return {(event: { clientX: number; clientY: number }) => number | null} rotationIndexGetter Returns the rotation data index.
*/
function useRadarRotationIndex() {
	const chartsLayerContainerRef = useChartsLayerContainerRef();
	const store = useStore();
	const rotationAxis = useRotationAxis();
	const center = store.use(selectorChartPolarCenter);
	return import_react.useCallback(function rotationIndexGetter(event) {
		const element = chartsLayerContainerRef.current;
		if (!element || !rotationAxis) throw new Error(`MUI X Charts: The ${!element ? "SVG element" : "rotation axis"} was not found. This is required to compute the radar chart dataIndex. Ensure the radar chart is properly initialized with all required axes.`);
		const svgPoint = getChartPoint(element, event);
		return getRotationAxisIndex(rotationAxis, generateSvg2rotation(center)(svgPoint.x, svgPoint.y));
	}, [
		center,
		rotationAxis,
		chartsLayerContainerRef
	]);
}
//#endregion
//#region node_modules/@mui/x-charts/RadarChart/RadarSeriesPlot/RadarSeriesArea.mjs
var _excluded$3 = [
	"seriesId",
	"onItemClick",
	"classes"
];
function getPathProps(params) {
	const { getHighlightState, seriesId, classes, points, fillArea, color } = params;
	const highlightState = getHighlightState({
		type: "radar",
		seriesId
	});
	const isItemHighlighted = highlightState === "highlighted";
	const isItemFaded = highlightState === "faded";
	return {
		d: getAreaPath(points),
		fill: fillArea ? color : "transparent",
		stroke: color,
		className: classes.seriesArea,
		strokeOpacity: isItemFaded ? .5 : 1,
		fillOpacity: isItemHighlighted && .4 || isItemFaded && .1 || .2,
		strokeWidth: !fillArea && isItemHighlighted ? 2 : 1,
		"data-highlighted": isItemHighlighted || void 0,
		"data-faded": isItemFaded || void 0
	};
}
function RadarSeriesArea(props) {
	const { seriesId, onItemClick, classes: inClasses } = props, other = _objectWithoutPropertiesLoose(props, _excluded$3);
	const seriesCoordinates = useRadarSeriesData(seriesId);
	const getRotationIndex = useRadarRotationIndex();
	const interactionProps = useInteractionAllItemProps(seriesCoordinates);
	const getHighlightState = useItemHighlightStateGetter();
	const classes = useUtilityClasses$4(inClasses);
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(import_react.Fragment, { children: seriesCoordinates?.map(({ seriesId: id, points, color, fillArea, hidden }, seriesIndex) => {
		if (hidden) return null;
		return /*#__PURE__*/ (0, import_jsx_runtime.jsx)("path", _extends({ "data-series": id }, getPathProps({
			seriesId: id,
			points,
			color,
			fillArea,
			getHighlightState,
			classes
		}), {
			onClick: (event) => onItemClick?.(event, {
				type: "radar",
				seriesId: id,
				dataIndex: getRotationIndex(event)
			}),
			cursor: onItemClick ? "pointer" : "unset"
		}, interactionProps[seriesIndex], other), id);
	}) });
}
RadarSeriesArea.propTypes = {
	/**
	* Override or extend the styles applied to the component.
	*/
	classes: import_prop_types.default.object,
	/**
	* A CSS class name applied to the root element.
	*/
	className: import_prop_types.default.string,
	/**
	* Callback fired when an area is clicked.
	* @param {React.MouseEvent<SVGPathElement, MouseEvent>} event The event source of the callback.
	* @param {RadarItemIdentifier} radarItemIdentifier The radar item identifier.
	*/
	onItemClick: import_prop_types.default.func,
	/**
	* The id of the series to display.
	* If undefined all series are displayed.
	*/
	seriesId: import_prop_types.default.string
};
//#endregion
//#region node_modules/@mui/x-charts/RadarChart/RadarSeriesPlot/RadarSeriesMarks.mjs
var _excluded$2 = [
	"seriesId",
	"onItemClick",
	"classes"
];
function getCircleProps(params) {
	const { getHighlightState, seriesId, classes, point, fillArea, color } = params;
	const highlightState = getHighlightState({
		type: "radar",
		seriesId
	});
	const isItemHighlighted = highlightState === "highlighted";
	const isItemFaded = highlightState === "faded";
	return {
		cx: point.x,
		cy: point.y,
		r: 3,
		fill: color,
		stroke: color,
		opacity: fillArea && isItemFaded ? .5 : 1,
		className: classes.seriesMark,
		"data-highlighted": isItemHighlighted || void 0,
		"data-faded": isItemFaded || void 0
	};
}
function RadarSeriesMarks(props) {
	const { onItemClick, classes: inClasses } = props, other = _objectWithoutPropertiesLoose(props, _excluded$2);
	const seriesCoordinates = useRadarSeriesData(props.seriesId);
	const classes = useUtilityClasses$4(inClasses);
	const getHighlightState = useItemHighlightStateGetter();
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(import_react.Fragment, { children: seriesCoordinates?.map(({ seriesId: id, points, hideMark, fillArea, hidden }) => {
		if (hideMark || hidden) return null;
		return /*#__PURE__*/ (0, import_jsx_runtime.jsx)("g", {
			"data-series": id,
			children: points.map((point, index) => /*#__PURE__*/ (0, import_jsx_runtime.jsx)("circle", _extends({}, getCircleProps({
				seriesId: id,
				point,
				color: point.color,
				fillArea,
				getHighlightState,
				classes
			}), {
				pointerEvents: onItemClick ? void 0 : "none",
				onClick: (event) => onItemClick?.(event, {
					type: "radar",
					seriesId: id,
					dataIndex: index
				}),
				cursor: onItemClick ? "pointer" : "unset"
			}, other), index))
		}, id);
	}) });
}
RadarSeriesMarks.propTypes = {
	/**
	* Override or extend the styles applied to the component.
	*/
	classes: import_prop_types.default.object,
	/**
	* A CSS class name applied to the root element.
	*/
	className: import_prop_types.default.string,
	/**
	* Callback fired when a mark is clicked.
	* @param {React.MouseEvent<SVGPathElement, MouseEvent>} event The event source of the callback.
	* @param {RadarItemIdentifier} radarItemIdentifier The radar item identifier.
	*/
	onItemClick: import_prop_types.default.func,
	/**
	* The id of the series to display.
	* If undefined all series are displayed.
	*/
	seriesId: import_prop_types.default.string
};
//#endregion
//#region node_modules/@mui/x-charts/RadarChart/RadarSeriesPlot/RadarSeriesPlot.mjs
function RadarSeriesPlot(props) {
	const { seriesId: inSeriesId, className, classes: inClasses, onAreaClick, onMarkClick } = props;
	const seriesCoordinates = useRadarSeriesData(inSeriesId);
	const getRotationIndex = useRadarRotationIndex();
	const interactionProps = useInteractionAllItemProps(seriesCoordinates);
	const getHighlightState = useItemHighlightStateGetter();
	const classes = useUtilityClasses$4(inClasses);
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)("g", {
		className: clsx(classes.seriesRoot, className),
		children: seriesCoordinates?.map(({ seriesId, points, color, hideMark, fillArea, hidden }, seriesIndex) => {
			if (hidden) return null;
			return /*#__PURE__*/ (0, import_jsx_runtime.jsxs)("g", {
				"data-series": seriesId,
				children: [/*#__PURE__*/ (0, import_jsx_runtime.jsx)("path", _extends({}, getPathProps({
					seriesId,
					points,
					color,
					fillArea,
					getHighlightState,
					classes
				}), {
					onClick: (event) => onAreaClick?.(event, {
						type: "radar",
						seriesId,
						dataIndex: getRotationIndex(event)
					}),
					cursor: onAreaClick ? "pointer" : "unset"
				}, interactionProps[seriesIndex]), seriesId), !hideMark && points.map((point, index) => /*#__PURE__*/ (0, import_jsx_runtime.jsx)("circle", _extends({}, getCircleProps({
					seriesId,
					point,
					color: point.color,
					fillArea,
					getHighlightState,
					classes
				}), {
					onClick: (event) => onMarkClick?.(event, {
						type: "radar",
						seriesId,
						dataIndex: index
					}),
					cursor: onMarkClick ? "pointer" : "unset"
				}), index))]
			}, seriesId);
		})
	});
}
RadarSeriesPlot.propTypes = {
	/**
	* Override or extend the styles applied to the component.
	*/
	classes: import_prop_types.default.object,
	/**
	* A CSS class name applied to the root element.
	*/
	className: import_prop_types.default.string,
	/**
	* Callback fired when an area is clicked.
	* @param {React.MouseEvent<SVGPathElement, MouseEvent>} event The event source of the callback.
	* @param {RadarItemIdentifier} radarItemIdentifier The radar item identifier.
	*/
	onAreaClick: import_prop_types.default.func,
	/**
	* Callback fired when a mark is clicked.
	* @param {React.MouseEvent<SVGPathElement, MouseEvent>} event The event source of the callback.
	* @param {RadarItemIdentifier} radarItemIdentifier The radar item identifier.
	*/
	onMarkClick: import_prop_types.default.func,
	/**
	* The id of the series to display.
	* If undefined all series are displayed.
	*/
	seriesId: import_prop_types.default.string
};
//#endregion
//#region node_modules/@mui/x-charts/RadarChart/RadarAxisHighlight/useRadarAxisHighlight.mjs
function useRadarAxisHighlight() {
	const radarSeries = useRadarSeries();
	const rotationScale = useRotationScale();
	const { radiusAxis, radiusAxisIds } = useRadiusAxes();
	const { instance } = useChartsContext();
	const store = useStore();
	const rotationAxisIndex = store.use(selectorChartsInteractionRotationAxisIndex);
	const rotationAxisValue = store.use(selectorChartsInteractionRotationAxisValue);
	const center = store.use(selectorChartPolarCenter);
	const highlightedIndex = rotationAxisIndex;
	if (!rotationScale) return null;
	if (highlightedIndex === null || highlightedIndex === -1) return null;
	if (radarSeries === void 0 || radarSeries.length === 0) return null;
	const metric = radiusAxisIds[highlightedIndex];
	const radiusScale = radiusAxis[metric].scale;
	const angle = rotationScale(rotationAxisValue);
	return {
		center,
		radius: radiusScale.range()[1],
		instance,
		highlightedIndex,
		highlightedMetric: metric,
		highlightedAngle: angle,
		series: radarSeries,
		points: radarSeries.map((series) => {
			const value = series.data[highlightedIndex];
			const r = radiusScale(value);
			const [x, y] = instance.polar2svg(r, angle);
			return {
				x,
				y,
				r,
				angle,
				value
			};
		})
	};
}
//#endregion
//#region node_modules/@mui/x-charts/RadarChart/RadarAxisHighlight/RadarAxisHighlight.mjs
/**
* Attributes to display a shadow around a mark.
*/
var highlightMarkShadow = {
	r: 7,
	opacity: .3
};
/**
* Attributes to display a mark.
*/
var highlightMark = {
	r: 3,
	opacity: 1
};
function RadarAxisHighlight(props) {
	const { className } = props;
	const classes = useUtilityClasses$4(props.classes);
	const theme = useTheme();
	const data = useRadarAxisHighlight();
	if (data === null) return null;
	const { center, series, points, radius, highlightedAngle, highlightedIndex, instance } = data;
	const [x, y] = instance.polar2svg(radius, highlightedAngle);
	return /*#__PURE__*/ (0, import_jsx_runtime.jsxs)("g", {
		className: clsx(classes.axisHighlightRoot, className),
		children: [/*#__PURE__*/ (0, import_jsx_runtime.jsx)("path", {
			d: `M ${center.cx} ${center.cy} L ${x} ${y}`,
			stroke: (theme.vars || theme).palette.text.primary,
			strokeWidth: 1,
			className: classes.axisHighlightLine,
			pointerEvents: "none",
			strokeDasharray: "4 4"
		}), points.map((point, seriesIndex) => {
			return /*#__PURE__*/ (0, import_jsx_runtime.jsx)("circle", _extends({
				fill: getSeriesColorFn(series[seriesIndex])({
					value: point.value,
					dataIndex: highlightedIndex
				}),
				cx: point.x,
				cy: point.y,
				className: classes.axisHighlightDot,
				pointerEvents: "none"
			}, series[seriesIndex].hideMark ? highlightMark : highlightMarkShadow), series[seriesIndex].id);
		})]
	});
}
RadarAxisHighlight.propTypes = {
	/**
	* Override or extend the styles applied to the component.
	*/
	classes: import_prop_types.default.object,
	/**
	* A CSS class name applied to the root element.
	*/
	className: import_prop_types.default.string
};
//#endregion
//#region node_modules/@mui/x-charts/RadarChart/RadarMetricLabels/useRadarMetricData.mjs
function useRadarMetricData() {
	const { scale: rotationScale, valueFormatter, labelGap = 10 } = useRotationAxis();
	const { radiusAxis } = useRadiusAxes();
	const drawingArea = useDrawingArea();
	const cx = drawingArea.left + drawingArea.width / 2;
	const cy = drawingArea.top + drawingArea.height / 2;
	const metrics = rotationScale.domain();
	const angles = metrics.map((key) => rotationScale(key));
	return { corners: metrics.map((metric, dataIndex) => {
		const r = radiusAxis[metric].scale.range()[1] + labelGap;
		const angle = angles[dataIndex];
		const defaultTickLabel = metric;
		return {
			x: cx + r * Math.sin(angle),
			y: cy - r * Math.cos(angle),
			angle: rad2deg(angle),
			label: valueFormatter?.(metric, {
				location: "tick",
				scale: rotationScale,
				defaultTickLabel
			}) ?? defaultTickLabel
		};
	}) };
}
//#endregion
//#region node_modules/@mui/x-charts/RadarChart/RadarMetricLabels/RadarMetricLabels.mjs
function RadarMetricLabels() {
	const { corners } = useRadarMetricData();
	const theme = useTheme();
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(import_react.Fragment, { children: corners.map(({ x, y, angle, label }, i) => /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ChartsText, {
		x,
		y,
		fontSize: 14,
		fill: (theme.vars || theme).palette.text.primary,
		stroke: "none",
		text: label,
		style: _extends({}, theme.typography.caption, {
			fontSize: 12,
			lineHeight: 1.25,
			textAnchor: getDefaultTextAnchor(180 + angle),
			dominantBaseline: getDefaultBaseline(180 + angle)
		})
	}, i)) });
}
//#endregion
//#region node_modules/@mui/x-charts/RadarChart/FocusedRadarMark.mjs
function FocusedRadarMark(props) {
	const theme = useTheme();
	const focusedItem = useFocusedItem();
	const seriesCoordinates = useRadarSeriesData(focusedItem?.seriesId);
	if (!focusedItem || focusedItem.type !== "radar" || seriesCoordinates.length === 0) return null;
	if (seriesCoordinates[0].hidden) return null;
	const point = seriesCoordinates[0].points[focusedItem.dataIndex];
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)("rect", _extends({
		fill: "none",
		stroke: (theme.vars ?? theme).palette.text.primary,
		strokeWidth: 2,
		x: point.x - 6,
		y: point.y - 6,
		width: 12,
		height: 12,
		rx: 3,
		ry: 3
	}, props));
}
//#endregion
//#region node_modules/@mui/x-charts/RadarChart/RadarChart.mjs
/**
* Demos:
*
* - [Radar Chart](https://mui.com/x/react-charts/radar/)
*
* API:
*
* - [RadarChart API](https://mui.com/x/api/charts/radar-chart/)
*/
var RadarChart = /*#__PURE__*/ import_react.forwardRef(function RadarChart(inProps, ref) {
	const props = useThemeProps({
		props: inProps,
		name: "MuiRadarChart"
	});
	const { chartsWrapperProps, chartsSurfaceProps, radarDataProviderProps, radarGrid, radarSeriesAreaProps, radarSeriesMarksProps, overlayProps, legendProps, highlight, children } = useRadarChartProps(props);
	const Tooltip = props.slots?.tooltip ?? ChartsTooltip;
	const Toolbar = props.slots?.toolbar;
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(RadarDataProvider, _extends({}, radarDataProviderProps, { children: /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(ChartsWrapper, _extends({}, chartsWrapperProps, {
		ref,
		children: [
			props.showToolbar && Toolbar ? /*#__PURE__*/ (0, import_jsx_runtime.jsx)(Toolbar, _extends({}, props.slotProps?.toolbar)) : null,
			!props.hideLegend && /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ChartsLegend, _extends({}, legendProps)),
			/*#__PURE__*/ (0, import_jsx_runtime.jsxs)(ChartsSurface, _extends({}, chartsSurfaceProps, { children: [
				/*#__PURE__*/ (0, import_jsx_runtime.jsx)(RadarGrid, _extends({}, radarGrid)),
				/*#__PURE__*/ (0, import_jsx_runtime.jsx)(RadarMetricLabels, {}),
				/*#__PURE__*/ (0, import_jsx_runtime.jsx)(RadarSeriesArea, _extends({}, radarSeriesAreaProps)),
				highlight === "axis" && /*#__PURE__*/ (0, import_jsx_runtime.jsx)(RadarAxisHighlight, {}),
				/*#__PURE__*/ (0, import_jsx_runtime.jsx)(RadarSeriesMarks, _extends({}, radarSeriesMarksProps)),
				/*#__PURE__*/ (0, import_jsx_runtime.jsx)(FocusedRadarMark, {}),
				/*#__PURE__*/ (0, import_jsx_runtime.jsx)(ChartsOverlay, _extends({}, overlayProps)),
				children
			] })),
			!props.loading && /*#__PURE__*/ (0, import_jsx_runtime.jsx)(Tooltip, _extends({}, props.slotProps?.tooltip))
		]
	})) }));
});
RadarChart.displayName = "RadarChart";
RadarChart.propTypes = {
	apiRef: import_prop_types.default.shape({ current: import_prop_types.default.object }),
	/**
	* A CSS class name applied to the root element.
	*/
	className: import_prop_types.default.string,
	/**
	* Color palette used to colorize multiple series.
	* @default rainbowSurgePalette
	*/
	colors: import_prop_types.default.oneOfType([import_prop_types.default.arrayOf(import_prop_types.default.string), import_prop_types.default.func]),
	/**
	* The description of the chart.
	* Used to provide an accessible description for the chart.
	*/
	desc: import_prop_types.default.string,
	/**
	* If `true`, the charts will not listen to the mouse move event.
	* It might break interactive features, but will improve performance.
	* @default false
	*/
	disableAxisListener: import_prop_types.default.bool,
	/**
	* If `true`, disables keyboard navigation for the chart.
	*/
	disableKeyboardNavigation: import_prop_types.default.bool,
	/**
	* The number of divisions in the radar grid.
	* @default 5
	*/
	divisions: import_prop_types.default.number,
	/**
	* Options to enable features planned for the next major.
	*/
	experimentalFeatures: import_prop_types.default.object,
	/**
	* The height of the chart in px. If not defined, it takes the height of the parent element.
	*/
	height: import_prop_types.default.number,
	/**
	* List of hidden series and/or items.
	*
	* Different chart types use different keys.
	*
	* @example
	* ```ts
	* [
	*   {
	*     type: 'pie',
	*     seriesId: 'series-1',
	*     dataIndex: 3,
	*   },
	*   {
	*     type: 'line',
	*     seriesId: 'series-2',
	*   }
	* ]
	* ```
	*/
	hiddenItems: import_prop_types.default.arrayOf(import_prop_types.default.oneOfType([import_prop_types.default.shape({
		dataIndex: import_prop_types.default.number,
		seriesId: import_prop_types.default.string.isRequired,
		type: import_prop_types.default.oneOf(["radar"]).isRequired
	}), import_prop_types.default.shape({
		dataIndex: import_prop_types.default.number,
		seriesId: import_prop_types.default.string.isRequired,
		type: import_prop_types.default.oneOf(["radar"])
	})]).isRequired),
	/**
	* If `true`, the legend is not rendered.
	*/
	hideLegend: import_prop_types.default.bool,
	/**
	* Indicates if the chart should highlight items per axis or per series.
	* @default 'axis'
	*/
	highlight: import_prop_types.default.oneOf([
		"axis",
		"none",
		"series"
	]),
	/**
	* The highlighted item.
	* Used when the highlight is controlled.
	*/
	highlightedItem: import_prop_types.default.oneOfType([import_prop_types.default.shape({
		dataIndex: import_prop_types.default.number,
		seriesId: import_prop_types.default.string.isRequired
	}), import_prop_types.default.shape({
		dataIndex: import_prop_types.default.number,
		seriesId: import_prop_types.default.string.isRequired,
		type: import_prop_types.default.oneOf(["radar"]).isRequired
	})]),
	/**
	* This prop is used to help implement the accessibility logic.
	* If you don't provide this prop. It falls back to a randomly generated id.
	*/
	id: import_prop_types.default.string,
	/**
	* List of initially hidden series and/or items.
	* Used for uncontrolled state.
	*
	* Different chart types use different keys.
	*
	* @example
	* ```ts
	* [
	*   {
	*     type: 'pie',
	*     seriesId: 'series-1',
	*     dataIndex: 3,
	*   },
	*   {
	*     type: 'line',
	*     seriesId: 'series-2',
	*   }
	* ]
	* ```
	*/
	initialHiddenItems: import_prop_types.default.arrayOf(import_prop_types.default.oneOfType([import_prop_types.default.shape({
		dataIndex: import_prop_types.default.number,
		seriesId: import_prop_types.default.string.isRequired,
		type: import_prop_types.default.oneOf(["radar"]).isRequired
	}), import_prop_types.default.shape({
		dataIndex: import_prop_types.default.number,
		seriesId: import_prop_types.default.string.isRequired,
		type: import_prop_types.default.oneOf(["radar"])
	})]).isRequired),
	/**
	* If `true`, a loading overlay is displayed.
	* @default false
	*/
	loading: import_prop_types.default.bool,
	/**
	* Localized text for chart components.
	*/
	localeText: import_prop_types.default.object,
	/**
	* The margin between the SVG and the drawing area.
	* It's used for leaving some space for extra information such as the x- and y-axis or legend.
	*
	* Accepts a `number` to be used on all sides or an object with the optional properties: `top`, `bottom`, `left`, and `right`.
	*/
	margin: import_prop_types.default.oneOfType([import_prop_types.default.number, import_prop_types.default.shape({
		bottom: import_prop_types.default.number,
		left: import_prop_types.default.number,
		right: import_prop_types.default.number,
		top: import_prop_types.default.number
	})]),
	/**
	* Callback fired when an area is clicked.
	* @param {React.MouseEvent<SVGPathElement, MouseEvent>} event The event source of the callback.
	* @param {RadarItemIdentifier} radarItemIdentifier The radar item identifier.
	*/
	onAreaClick: import_prop_types.default.func,
	/**
	* The function called for onClick events.
	* The second argument contains information about all line/bar elements at the current mouse position.
	* @param {MouseEvent} event The mouse event recorded on the `<svg/>` element.
	* @param {null | ChartsAxisData} data The data about the clicked axis and items associated with it.
	*/
	onAxisClick: import_prop_types.default.func,
	/**
	* Callback fired when any hidden identifiers change.
	* @param {VisibilityIdentifierWithType[]} hiddenItems The new list of hidden identifiers.
	*/
	onHiddenItemsChange: import_prop_types.default.func,
	/**
	* The callback fired when the highlighted item changes.
	*
	* @param {HighlightItemIdentifierWithType<SeriesType> | null} highlightedItem  The newly highlighted item.
	*/
	onHighlightChange: import_prop_types.default.func,
	/**
	* Callback fired when a mark is clicked.
	* @param {React.MouseEvent<SVGPathElement, MouseEvent>} event The event source of the callback.
	* @param {RadarItemIdentifier} radarItemIdentifier The radar item identifier.
	*/
	onMarkClick: import_prop_types.default.func,
	/**
	* The callback fired when the tooltip item changes.
	*
	* @param {SeriesItemIdentifier<SeriesType> | null} tooltipItem  The newly highlighted item.
	*/
	onTooltipItemChange: import_prop_types.default.func,
	/**
	* The configuration of the radar scales.
	*/
	radar: import_prop_types.default.shape({
		labelFormatter: import_prop_types.default.func,
		labelGap: import_prop_types.default.number,
		max: import_prop_types.default.number,
		metrics: import_prop_types.default.oneOfType([import_prop_types.default.arrayOf(import_prop_types.default.shape({
			max: import_prop_types.default.number,
			min: import_prop_types.default.number,
			name: import_prop_types.default.string.isRequired
		})), import_prop_types.default.arrayOf(import_prop_types.default.string)]).isRequired,
		startAngle: import_prop_types.default.number
	}).isRequired,
	/**
	* The series to display in the bar chart.
	* An array of [[RadarSeries]] objects.
	*/
	series: import_prop_types.default.arrayOf(import_prop_types.default.object).isRequired,
	/**
	* The grid shape.
	* @default 'sharp'
	*/
	shape: import_prop_types.default.oneOf(["circular", "sharp"]),
	/**
	* If true, shows the default chart toolbar.
	* @default false
	*/
	showToolbar: import_prop_types.default.bool,
	/**
	* If `true`, animations are skipped.
	* If unset or `false`, the animations respects the user's `prefers-reduced-motion` setting.
	*/
	skipAnimation: import_prop_types.default.bool,
	/**
	* The props used for each component slot.
	* @default {}
	*/
	slotProps: import_prop_types.default.object,
	/**
	* Overridable component slots.
	* @default {}
	*/
	slots: import_prop_types.default.object,
	/**
	* Get stripe fill color. Set it to `null` to remove stripes
	* @param {number} index The index of the stripe band.
	* @returns {string} The color to fill the stripe.
	* @default (index) => index % 2 === 1 ? (theme.vars || theme).palette.text.secondary : 'none'
	*/
	stripeColor: import_prop_types.default.func,
	sx: import_prop_types.default.oneOfType([
		import_prop_types.default.arrayOf(import_prop_types.default.oneOfType([
			import_prop_types.default.func,
			import_prop_types.default.object,
			import_prop_types.default.bool
		])),
		import_prop_types.default.func,
		import_prop_types.default.object
	]),
	theme: import_prop_types.default.oneOf(["dark", "light"]),
	/**
	* The title of the chart.
	* Used to provide an accessible label for the chart.
	*/
	title: import_prop_types.default.string,
	/**
	* The tooltip item.
	* Used when the tooltip is controlled.
	*/
	tooltipItem: import_prop_types.default.oneOfType([import_prop_types.default.shape({
		dataIndex: import_prop_types.default.number,
		seriesId: import_prop_types.default.string.isRequired
	}), import_prop_types.default.shape({
		dataIndex: import_prop_types.default.number,
		seriesId: import_prop_types.default.string.isRequired,
		type: import_prop_types.default.oneOf(["radar"]).isRequired
	})]),
	/**
	* The width of the chart in px. If not defined, it takes the width of the parent element.
	*/
	width: import_prop_types.default.number
};
//#endregion
//#region node_modules/@mui/x-charts/RadarChart/RadarAxis/useRadarAxis.mjs
/**
* Returns an array with one item per metric with the different points to label.
*/
function useRadarAxis(params) {
	const { metric, angle, divisions = 1 } = params;
	const { instance, store } = useChartsContext();
	const rotationScale = useRotationScale();
	const { radiusAxis } = useRadiusAxes();
	const { cx, cy } = store.use(selectorChartPolarCenter);
	if (metric === void 0 || !rotationScale || rotationScale.domain().length === 0) return null;
	const existingMetrics = rotationScale.domain();
	if (!existingMetrics.includes(metric)) warnOnce([`MUI X Charts: Your radar axis tries to display values for the metric "${metric}" which does not exist.`, `Either add this metric to your radar, or pick one from the existing metrics: ${existingMetrics.join(", ")}`]);
	const anglesWithDefault = angle !== void 0 ? degToRad(angle) : rotationScale(metric) ?? 0;
	const radiusRatio = Array.from({ length: divisions }, (_, index) => (index + 1) / divisions);
	const radiusScale = radiusAxis[metric].scale;
	const R = radiusScale.range()[1];
	if (isOrdinalScale(radiusScale)) {
		console.error("MUI X Charts: Radar chart does not support ordinal axes");
		return null;
	}
	return {
		metric,
		angle: clampAngle(rad2deg(anglesWithDefault)),
		center: {
			x: cx,
			y: cy
		},
		labels: radiusRatio.map((ratio) => {
			const radius = ratio * R;
			const [x, y] = instance.polar2svg(radius, anglesWithDefault);
			const value = radiusScale.invert(radius);
			const defaultTickLabel = value.toString();
			return {
				x,
				y,
				value,
				formattedValue: radiusAxis[metric].valueFormatter?.(radiusScale.invert(radius), {
					location: "tick",
					scale: radiusScale,
					defaultTickLabel,
					tickNumber: divisions
				}) ?? defaultTickLabel
			};
		})
	};
}
//#endregion
//#region node_modules/@mui/x-charts/RadarChart/RadarAxis/RadarAxis.utils.mjs
/**
* Return text anchor of labels.
* @param angle The axis angle (in rad) with clock direction and 0 at the top
*/
function getTextAnchor(angle) {
	if (angle < 20) return "start";
	if (angle < 80) return "end";
	if (angle < 260) return "start";
	if (angle < 340) return "end";
	return "start";
}
function getDominantBaseline(angle) {
	if (angle < 160) return "auto";
	if (angle < 340) return "hanging";
	return "auto";
}
var LABEL_MARGIN = 2;
function getLabelAttributes(params) {
	const { x, y, angle } = params;
	if (params.labelOrientation === "horizontal") {
		const textAnchor = typeof params.textAnchor === "function" ? params.textAnchor(angle) : params.textAnchor ?? getTextAnchor(angle);
		const dominantBaseline = typeof params.dominantBaseline === "function" ? params.dominantBaseline(angle) : params.dominantBaseline ?? getDominantBaseline(angle);
		const marginX = textAnchor === "start" ? LABEL_MARGIN : -2;
		const marginY = dominantBaseline === "auto" ? -2 : LABEL_MARGIN;
		return {
			x: x + marginX,
			y: y + marginY,
			textAnchor,
			dominantBaseline
		};
	}
	return {
		x,
		y,
		textAnchor: typeof params.textAnchor === "function" ? params.textAnchor(angle) : params.textAnchor ?? "start",
		dominantBaseline: typeof params.dominantBaseline === "function" ? params.dominantBaseline(angle) : params.dominantBaseline ?? "auto",
		transform: `rotate(${angle}, ${x}, ${y})`
	};
}
//#endregion
//#region node_modules/@mui/x-charts/RadarChart/RadarAxis/RadarAxis.mjs
function RadarAxis(props) {
	const { className, labelOrientation = "horizontal", textAnchor, dominantBaseline } = props;
	const classes = useUtilityClasses$4(props.classes);
	const theme = useTheme();
	const data = useRadarAxis(props);
	if (data === null) return null;
	const { center, angle, labels } = data;
	return /*#__PURE__*/ (0, import_jsx_runtime.jsxs)("g", {
		className: clsx(classes.axisRoot, className),
		children: [/*#__PURE__*/ (0, import_jsx_runtime.jsx)("path", {
			d: `M ${center.x} ${center.y} L ${labels[labels.length - 1].x} ${labels[labels.length - 1].y}`,
			stroke: (theme.vars ?? theme).palette.text.primary,
			strokeOpacity: .3,
			className: classes.axisLine
		}), labels.map(({ x, y, formattedValue }) => /*#__PURE__*/ (0, import_jsx_runtime.jsx)("text", _extends({
			fontSize: 12,
			fill: (theme.vars ?? theme).palette.text.primary,
			stroke: "none",
			className: classes.axisLabel
		}, getLabelAttributes({
			labelOrientation,
			x,
			y,
			angle,
			textAnchor,
			dominantBaseline
		}), { children: formattedValue }), formattedValue))]
	});
}
RadarAxis.propTypes = {
	/**
	* The absolute rotation angle of the metrics (in degree)
	* If not defined the metric angle will be used.
	*/
	angle: import_prop_types.default.number,
	/**
	* Override or extend the styles applied to the component.
	*/
	classes: import_prop_types.default.object,
	/**
	* A CSS class name applied to the root element.
	*/
	className: import_prop_types.default.string,
	/**
	* The number of divisions with label.
	* @default 1
	*/
	divisions: import_prop_types.default.number,
	/**
	* The labels dominant baseline or a function returning the dominant baseline for a given axis angle (in degree).
	*/
	dominantBaseline: import_prop_types.default.oneOfType([import_prop_types.default.oneOf([
		"alphabetic",
		"auto",
		"central",
		"hanging",
		"ideographic",
		"inherit",
		"mathematical",
		"middle",
		"no-change",
		"reset-size",
		"text-after-edge",
		"text-before-edge",
		"use-script"
	]), import_prop_types.default.func]),
	/**
	* Defines how label align with the axis.
	* - 'horizontal': labels stay horizontal and their placement change with the axis angle.
	* - 'rotated': labels are rotated 90deg relatively to their axis.
	* @default 'horizontal'
	*/
	labelOrientation: import_prop_types.default.oneOf(["horizontal", "rotated"]),
	/**
	* The metric to get.
	* If `undefined`, the hook returns `null`
	*/
	metric: import_prop_types.default.string,
	/**
	* The labels text anchor or a function returning the text anchor for a given axis angle (in degree).
	*/
	textAnchor: import_prop_types.default.oneOfType([import_prop_types.default.oneOf([
		"end",
		"inherit",
		"middle",
		"start"
	]), import_prop_types.default.func])
};
//#endregion
//#region node_modules/@mui/x-charts/RadarChart/index.mjs
/**
* @deprecated radar chart is now stable, import `RadarChart` instead
*/
var Unstable_RadarChart = RadarChart;
/**
* @deprecated radar data provider is now stable, import `RadarDataProvider` instead
*/
var Unstable_RadarDataProvider = RadarDataProvider;
//#endregion
//#region node_modules/@mui/x-charts/ChartsContainer/ChartsContainer.mjs
/**
* It sets up the data providers as well as the `<svg>` for the chart.
*
* This is a combination of both the `ChartsDataProvider` and `ChartsSurface` components.
*
* Demos:
*
* - [Composition](https://mui.com/x/api/charts/composition/)
*
* API:
*
* - [ChartsContainer API](https://mui.com/x/api/charts/charts-container/)
*
* @example
* ```jsx
* <ChartsContainer
*   series={[{ label: "Label", type: "bar", data: [10, 20] }]}
*   xAxis={[{ data: ["A", "B"], scaleType: "band", id: "x-axis" }]}
* >
*    <BarPlot />
*    <ChartsXAxis axisId="x-axis" />
* </ChartsContainer>
* ```
*/
var ChartsContainer = /*#__PURE__*/ import_react.forwardRef(function ChartsContainer(props, ref) {
	const { chartsDataProviderProps, children, chartsSurfaceProps } = useChartsContainerProps(props);
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ChartsDataProvider, _extends({}, chartsDataProviderProps, { children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ChartsSurface, _extends({}, chartsSurfaceProps, {
		ref,
		children
	})) }));
});
ChartsContainer.displayName = "ChartsContainer";
ChartsContainer.propTypes = {
	apiRef: import_prop_types.default.shape({ current: import_prop_types.default.object }),
	/**
	* A gap added between axes when multiple axes are rendered on the same side of the chart.
	* @default 0
	*/
	axesGap: import_prop_types.default.number,
	/**
	* Configuration for the brush interaction.
	*/
	brushConfig: import_prop_types.default.shape({
		enabled: import_prop_types.default.bool,
		preventHighlight: import_prop_types.default.bool,
		preventTooltip: import_prop_types.default.bool
	}),
	children: import_prop_types.default.node,
	className: import_prop_types.default.string,
	/**
	* Color palette used to colorize multiple series.
	* @default rainbowSurgePalette
	*/
	colors: import_prop_types.default.oneOfType([import_prop_types.default.arrayOf(import_prop_types.default.string), import_prop_types.default.func]),
	/**
	* An array of objects that can be used to populate series and axes data using their `dataKey` property.
	*/
	dataset: import_prop_types.default.arrayOf(import_prop_types.default.object),
	/**
	* The description of the chart.
	* Used to provide an accessible description for the chart.
	*/
	desc: import_prop_types.default.string,
	/**
	* If `true`, the charts will not listen to the mouse move event.
	* It might break interactive features, but will improve performance.
	* @default false
	*/
	disableAxisListener: import_prop_types.default.bool,
	/**
	* If true, the hit area interaction is disabled and falls back to hover events.
	*/
	disableHitArea: import_prop_types.default.bool,
	/**
	* If `true`, disables keyboard navigation for the chart.
	*/
	disableKeyboardNavigation: import_prop_types.default.bool,
	/**
	* Options to enable features planned for the next major.
	*/
	experimentalFeatures: import_prop_types.default.any,
	/**
	* The height of the chart in px. If not defined, it takes the height of the parent element.
	*/
	height: import_prop_types.default.number,
	/**
	* List of hidden series and/or items.
	*
	* Different chart types use different keys.
	*
	* @example
	* ```ts
	* [
	*   {
	*     type: 'pie',
	*     seriesId: 'series-1',
	*     dataIndex: 3,
	*   },
	*   {
	*     type: 'line',
	*     seriesId: 'series-2',
	*   }
	* ]
	* ```
	*/
	hiddenItems: import_prop_types.default.arrayOf(import_prop_types.default.oneOfType([import_prop_types.default.shape({
		dataIndex: import_prop_types.default.number,
		seriesId: import_prop_types.default.string.isRequired,
		type: import_prop_types.default.oneOf([
			"bar",
			"line",
			"pie",
			"radar",
			"scatter"
		]).isRequired
	}), import_prop_types.default.shape({
		dataIndex: import_prop_types.default.number,
		seriesId: import_prop_types.default.string.isRequired,
		type: import_prop_types.default.oneOf([
			"bar",
			"line",
			"pie",
			"radar",
			"scatter"
		])
	})]).isRequired),
	/**
	* The controlled axis highlight.
	* Identified by the axis id, and data index.
	*/
	highlightedAxis: import_prop_types.default.arrayOf(import_prop_types.default.shape({
		axisId: import_prop_types.default.oneOfType([import_prop_types.default.number, import_prop_types.default.string]).isRequired,
		dataIndex: import_prop_types.default.number.isRequired
	})),
	/**
	* The highlighted item.
	* Used when the highlight is controlled.
	*/
	highlightedItem: import_prop_types.default.oneOfType([import_prop_types.default.shape({
		dataIndex: import_prop_types.default.number,
		seriesId: import_prop_types.default.string.isRequired
	}), import_prop_types.default.shape({
		dataIndex: import_prop_types.default.number,
		seriesId: import_prop_types.default.string.isRequired,
		type: import_prop_types.default.oneOf([
			"bar",
			"line",
			"pie",
			"radar",
			"scatter"
		]).isRequired
	})]),
	/**
	* Defines the maximum distance between a scatter point and the pointer that triggers the interaction.
	* If set to `'item'`, the radius is the `markerSize`.
	* If `undefined`, the radius is assumed to be infinite.
	*/
	hitAreaRadius: import_prop_types.default.oneOfType([import_prop_types.default.oneOf(["item"]), import_prop_types.default.number]),
	/**
	* This prop is used to help implement the accessibility logic.
	* If you don't provide this prop. It falls back to a randomly generated id.
	*/
	id: import_prop_types.default.string,
	/**
	* List of initially hidden series and/or items.
	* Used for uncontrolled state.
	*
	* Different chart types use different keys.
	*
	* @example
	* ```ts
	* [
	*   {
	*     type: 'pie',
	*     seriesId: 'series-1',
	*     dataIndex: 3,
	*   },
	*   {
	*     type: 'line',
	*     seriesId: 'series-2',
	*   }
	* ]
	* ```
	*/
	initialHiddenItems: import_prop_types.default.arrayOf(import_prop_types.default.oneOfType([import_prop_types.default.shape({
		dataIndex: import_prop_types.default.number,
		seriesId: import_prop_types.default.string.isRequired,
		type: import_prop_types.default.oneOf([
			"bar",
			"line",
			"pie",
			"radar",
			"scatter"
		]).isRequired
	}), import_prop_types.default.shape({
		dataIndex: import_prop_types.default.number,
		seriesId: import_prop_types.default.string.isRequired,
		type: import_prop_types.default.oneOf([
			"bar",
			"line",
			"pie",
			"radar",
			"scatter"
		])
	})]).isRequired),
	/**
	* Localized text for chart components.
	*/
	localeText: import_prop_types.default.object,
	/**
	* The margin between the SVG and the drawing area.
	* It's used for leaving some space for extra information such as the x- and y-axis or legend.
	*
	* Accepts a `number` to be used on all sides or an object with the optional properties: `top`, `bottom`, `left`, and `right`.
	*/
	margin: import_prop_types.default.oneOfType([import_prop_types.default.number, import_prop_types.default.shape({
		bottom: import_prop_types.default.number,
		left: import_prop_types.default.number,
		right: import_prop_types.default.number,
		top: import_prop_types.default.number
	})]),
	/**
	* The function called for onClick events.
	* The second argument contains information about all line/bar elements at the current mouse position.
	* @param {MouseEvent} event The mouse event recorded on the `<svg/>` element.
	* @param {null | ChartsAxisData} data The data about the clicked axis and items associated with it.
	*/
	onAxisClick: import_prop_types.default.func,
	/**
	* Callback fired when any hidden identifiers change.
	* @param {VisibilityIdentifierWithType[]} hiddenItems The new list of hidden identifiers.
	*/
	onHiddenItemsChange: import_prop_types.default.func,
	/**
	* The callback fired when the highlighted item changes.
	*
	* @param {HighlightItemIdentifierWithType<SeriesType> | null} highlightedItem  The newly highlighted item.
	*/
	onHighlightChange: import_prop_types.default.func,
	/**
	* The function called when the pointer position corresponds to a new axis data item.
	* This update can either be caused by a pointer movement, or an axis update.
	* In case of multiple axes, the function is called if at least one axis is updated.
	* The argument contains the identifier for all axes with a `data` property.
	* @param {AxisItemIdentifier[]} axisItems The array of axes item identifiers.
	*/
	onHighlightedAxisChange: import_prop_types.default.func,
	/**
	* Callback fired when clicking close to an item.
	* This is only available for scatter plot for now.
	* @param {MouseEvent} event Mouse event caught at the svg level
	* @param {ScatterItemIdentifier} scatterItemIdentifier Identify which item got clicked
	*/
	onItemClick: import_prop_types.default.func,
	/**
	* The function called when the pointer position corresponds to a new axis data item.
	* This update can either be caused by a pointer movement, or an axis update.
	* In case of multiple axes, the function is called if at least one axis is updated.
	* The argument contains the identifier for all axes with a `data` property.
	* @param {AxisItemIdentifier[]} axisItems The array of axes item identifiers.
	*/
	onTooltipAxisChange: import_prop_types.default.func,
	/**
	* The callback fired when the tooltip item changes.
	*
	* @param {SeriesItemIdentifier<SeriesType> | null} tooltipItem  The newly highlighted item.
	*/
	onTooltipItemChange: import_prop_types.default.func,
	/**
	* The configuration of the radial-axes.
	* If not provided, a default axis config is used.
	* An array of [[AxisConfig]] objects.
	*/
	radiusAxis: import_prop_types.default.arrayOf(import_prop_types.default.object),
	/**
	* The configuration of the rotation-axes.
	* If not provided, a default axis config is used.
	* An array of [[AxisConfig]] objects.
	*/
	rotationAxis: import_prop_types.default.arrayOf(import_prop_types.default.object),
	/**
	* The array of series to display.
	* Each type of series has its own specificity.
	* Please refer to the appropriate docs page to learn more about it.
	*/
	series: import_prop_types.default.arrayOf(import_prop_types.default.object),
	/**
	* If `true`, animations are skipped.
	* If unset or `false`, the animations respects the user's `prefers-reduced-motion` setting.
	*/
	skipAnimation: import_prop_types.default.bool,
	/**
	* The props for the slots.
	*/
	slotProps: import_prop_types.default.object,
	/**
	* Slots to customize charts' components.
	*/
	slots: import_prop_types.default.object,
	sx: import_prop_types.default.oneOfType([
		import_prop_types.default.arrayOf(import_prop_types.default.oneOfType([
			import_prop_types.default.func,
			import_prop_types.default.object,
			import_prop_types.default.bool
		])),
		import_prop_types.default.func,
		import_prop_types.default.object
	]),
	theme: import_prop_types.default.oneOf(["dark", "light"]),
	/**
	* The title of the chart.
	* Used to provide an accessible label for the chart.
	*/
	title: import_prop_types.default.string,
	/**
	* The controlled axis tooltip.
	* Identified by the axis id, and data index.
	*/
	tooltipAxis: import_prop_types.default.arrayOf(import_prop_types.default.shape({
		axisId: import_prop_types.default.oneOfType([import_prop_types.default.number, import_prop_types.default.string]).isRequired,
		dataIndex: import_prop_types.default.number.isRequired
	})),
	/**
	* The tooltip item.
	* Used when the tooltip is controlled.
	*/
	tooltipItem: import_prop_types.default.oneOfType([import_prop_types.default.shape({
		dataIndex: import_prop_types.default.number,
		seriesId: import_prop_types.default.string.isRequired
	}), import_prop_types.default.shape({
		dataIndex: import_prop_types.default.number,
		seriesId: import_prop_types.default.string.isRequired,
		type: import_prop_types.default.oneOf([
			"bar",
			"line",
			"pie",
			"radar",
			"scatter"
		]).isRequired
	})]),
	/**
	* The width of the chart in px. If not defined, it takes the width of the parent element.
	*/
	width: import_prop_types.default.number,
	/**
	* The configuration of the x-axes.
	* If not provided, a default axis config is used.
	* An array of [[AxisConfig]] objects.
	*/
	xAxis: import_prop_types.default.arrayOf(import_prop_types.default.object),
	/**
	* The configuration of the y-axes.
	* If not provided, a default axis config is used.
	* An array of [[AxisConfig]] objects.
	*/
	yAxis: import_prop_types.default.arrayOf(import_prop_types.default.object),
	/**
	* The configuration of the z-axes.
	*/
	zAxis: import_prop_types.default.arrayOf(import_prop_types.default.shape({
		colorMap: import_prop_types.default.oneOfType([
			import_prop_types.default.shape({
				color: import_prop_types.default.oneOfType([import_prop_types.default.arrayOf(import_prop_types.default.string.isRequired), import_prop_types.default.func]).isRequired,
				max: import_prop_types.default.oneOfType([import_prop_types.default.instanceOf(Date), import_prop_types.default.number]),
				min: import_prop_types.default.oneOfType([import_prop_types.default.instanceOf(Date), import_prop_types.default.number]),
				type: import_prop_types.default.oneOf(["continuous"]).isRequired,
				unknownColor: import_prop_types.default.string
			}),
			import_prop_types.default.shape({
				colors: import_prop_types.default.arrayOf(import_prop_types.default.string).isRequired,
				thresholds: import_prop_types.default.arrayOf(import_prop_types.default.oneOfType([import_prop_types.default.instanceOf(Date), import_prop_types.default.number]).isRequired).isRequired,
				type: import_prop_types.default.oneOf(["piecewise"]).isRequired,
				unknownColor: import_prop_types.default.string
			}),
			import_prop_types.default.shape({
				colors: import_prop_types.default.arrayOf(import_prop_types.default.string).isRequired,
				type: import_prop_types.default.oneOf(["ordinal"]).isRequired,
				unknownColor: import_prop_types.default.string,
				values: import_prop_types.default.arrayOf(import_prop_types.default.oneOfType([
					import_prop_types.default.instanceOf(Date),
					import_prop_types.default.number,
					import_prop_types.default.string
				]).isRequired)
			})
		]),
		data: import_prop_types.default.array,
		dataKey: import_prop_types.default.string,
		id: import_prop_types.default.string,
		max: import_prop_types.default.number,
		min: import_prop_types.default.number,
		sizeMap: import_prop_types.default.oneOfType([
			import_prop_types.default.shape({
				interpolator: import_prop_types.default.oneOf([
					"linear",
					"log",
					"sqrt"
				]),
				max: import_prop_types.default.oneOfType([import_prop_types.default.instanceOf(Date), import_prop_types.default.number]),
				min: import_prop_types.default.oneOfType([import_prop_types.default.instanceOf(Date), import_prop_types.default.number]),
				size: import_prop_types.default.arrayOf(import_prop_types.default.number.isRequired).isRequired,
				type: import_prop_types.default.oneOf(["continuous"]).isRequired
			}),
			import_prop_types.default.shape({
				max: import_prop_types.default.oneOfType([import_prop_types.default.instanceOf(Date), import_prop_types.default.number]),
				min: import_prop_types.default.oneOfType([import_prop_types.default.instanceOf(Date), import_prop_types.default.number]),
				size: import_prop_types.default.func.isRequired,
				type: import_prop_types.default.oneOf(["continuous"]).isRequired
			}),
			import_prop_types.default.shape({
				sizes: import_prop_types.default.arrayOf(import_prop_types.default.number).isRequired,
				thresholds: import_prop_types.default.arrayOf(import_prop_types.default.oneOfType([import_prop_types.default.instanceOf(Date), import_prop_types.default.number]).isRequired).isRequired,
				type: import_prop_types.default.oneOf(["piecewise"]).isRequired
			}),
			import_prop_types.default.shape({
				sizes: import_prop_types.default.arrayOf(import_prop_types.default.number).isRequired,
				type: import_prop_types.default.oneOf(["ordinal"]).isRequired,
				unknownSize: import_prop_types.default.number,
				values: import_prop_types.default.arrayOf(import_prop_types.default.oneOfType([
					import_prop_types.default.instanceOf(Date),
					import_prop_types.default.number,
					import_prop_types.default.string
				]).isRequired)
			})
		]),
		valueGetter: import_prop_types.default.func
	}))
};
//#endregion
//#region node_modules/@mui/x-charts/ChartsRadialDataProvider/ChartsRadialDataProvider.plugins.mjs
var RADIAL_PLUGINS = [
	useChartTooltip,
	useChartInteraction,
	useChartPolarAxis,
	useChartHighlight,
	useChartKeyboardNavigation,
	useChartVisibilityManager
];
//#endregion
//#region node_modules/@mui/x-charts/ChartsRadialDataProvider/useChartsRadialDataProviderProps.mjs
var _excluded$1 = [
	"children",
	"localeText",
	"plugins",
	"slots",
	"slotProps",
	"seriesConfig"
];
var RADIAL_SERIES_CONFIG = {};
var useChartsRadialDataProviderProps = (inProps) => {
	const props = useThemeProps({
		props: inProps,
		name: "MuiChartsRadialDataProvider"
	});
	const { children, localeText, plugins = RADIAL_PLUGINS, slots, slotProps, seriesConfig = RADIAL_SERIES_CONFIG } = props, other = _objectWithoutPropertiesLoose(props, _excluded$1);
	return {
		children,
		localeText,
		chartProviderProps: {
			plugins,
			pluginParams: _extends({
				theme: useTheme().palette.mode,
				seriesConfig
			}, other)
		},
		slots,
		slotProps
	};
};
//#endregion
//#region node_modules/@mui/x-charts/ChartsRadialDataProvider/ChartsRadialDataProvider.mjs
/**
* Orchestrates the data providers for radial chart components and hooks.
*
* Similar to `ChartsDataProvider`, but uses the radial axis plugin instead of the cartesian one,
* and only supports the line series config.
*
* Demos:
*
* - [Composition](https://mui.com/x/react-charts/composition/)
*
* API:
*
* - [ChartsRadialDataProvider API](https://mui.com/x/api/charts/charts-radial-data-provider/)
*/
function ChartsRadialDataProvider(props) {
	const { children, localeText, chartProviderProps, slots, slotProps } = useChartsRadialDataProviderProps(props);
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ChartsProvider, _extends({}, chartProviderProps, { children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ChartsLocalizationProvider, {
		localeText,
		children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ChartsSlotsProvider, {
			slots,
			slotProps,
			defaultSlots: defaultSlotsMaterial,
			children
		})
	}) }));
}
ChartsRadialDataProvider.propTypes = {
	apiRef: import_prop_types.default.shape({ current: import_prop_types.default.any }),
	/**
	* Color palette used to colorize multiple series.
	* @default rainbowSurgePalette
	*/
	colors: import_prop_types.default.oneOfType([import_prop_types.default.arrayOf(import_prop_types.default.string), import_prop_types.default.func]),
	/**
	* An array of objects that can be used to populate series and axes data using their `dataKey` property.
	*/
	dataset: import_prop_types.default.arrayOf(import_prop_types.default.object),
	/**
	* Options to enable features planned for the next major.
	*/
	experimentalFeatures: import_prop_types.default.any,
	/**
	* The height of the chart in px. If not defined, it takes the height of the parent element.
	*/
	height: import_prop_types.default.number,
	/**
	* This prop is used to help implement the accessibility logic.
	* If you don't provide this prop. It falls back to a randomly generated id.
	*/
	id: import_prop_types.default.string,
	/**
	* Localized text for chart components.
	*/
	localeText: import_prop_types.default.object,
	/**
	* The margin between the SVG and the drawing area.
	* It's used for leaving some space for extra information such as the x- and y-axis or legend.
	*
	* Accepts a `number` to be used on all sides or an object with the optional properties: `top`, `bottom`, `left`, and `right`.
	*/
	margin: import_prop_types.default.oneOfType([import_prop_types.default.number, import_prop_types.default.shape({
		bottom: import_prop_types.default.number,
		left: import_prop_types.default.number,
		right: import_prop_types.default.number,
		top: import_prop_types.default.number
	})]),
	/**
	* The array of series to display.
	* Each type of series has its own specificity.
	* Please refer to the appropriate docs page to learn more about it.
	*/
	series: import_prop_types.default.arrayOf(import_prop_types.default.object),
	/**
	* If `true`, animations are skipped.
	* If unset or `false`, the animations respects the user's `prefers-reduced-motion` setting.
	*/
	skipAnimation: import_prop_types.default.bool,
	/**
	* The props for the slots.
	*/
	slotProps: import_prop_types.default.object,
	/**
	* Slots to customize charts' components.
	*/
	slots: import_prop_types.default.object,
	theme: import_prop_types.default.oneOf(["dark", "light"]),
	/**
	* The width of the chart in px. If not defined, it takes the width of the parent element.
	*/
	width: import_prop_types.default.number
};
//#endregion
//#region node_modules/@mui/x-charts/ChartsRadialDataProvider/index.mjs
/**
* @deprecated The radial data provider is now stable, import `ChartsRadialDataProvider` instead
*/
var Unstable_ChartsRadialDataProvider = ChartsRadialDataProvider;
//#endregion
//#region node_modules/@mui/x-charts/ChartsRadialGrid/chartsRadialGridClasses.mjs
function getChartsRadialGridUtilityClass(slot) {
	return generateUtilityClass("MuiChartsRadialGrid", slot);
}
var chartsRadialGridClasses = generateUtilityClasses("MuiChartsRadialGrid", [
	"root",
	"line",
	"rotationLine",
	"radiusLine"
]);
//#endregion
//#region node_modules/@mui/x-charts/ChartsRadialGrid/styledComponents.mjs
var GridRoot = styled("g", {
	name: "MuiChartsRadialGrid",
	slot: "Root",
	overridesResolver: (props, styles) => [
		{ [`& .${chartsRadialGridClasses.rotationLine}`]: styles.rotationLine },
		{ [`& .${chartsRadialGridClasses.radiusLine}`]: styles.radiusLine },
		styles.root
	]
})({});
var GridLine = styled("line", {
	slot: "internal",
	shouldForwardProp: void 0
})(({ theme }) => ({
	stroke: (theme.vars || theme).palette.divider,
	shapeRendering: "crispEdges",
	strokeWidth: 1,
	fill: "none"
}));
var GridPath = styled("path", {
	slot: "internal",
	shouldForwardProp: void 0
})(({ theme }) => ({
	stroke: (theme.vars || theme).palette.divider,
	shapeRendering: "crispEdges",
	strokeWidth: 1,
	fill: "none"
}));
var GridCircle = styled("circle", {
	slot: "internal",
	shouldForwardProp: void 0
})(({ theme }) => ({
	stroke: (theme.vars || theme).palette.divider,
	shapeRendering: "crispEdges",
	strokeWidth: 1,
	fill: "none"
}));
//#endregion
//#region node_modules/@mui/x-charts/ChartsRadialGrid/ChartsRotationGrid.mjs
/**
* @ignore - internal component.
*/
function ChartsRotationGrid(props) {
	const { store } = useChartsContext();
	const { axis, innerRadius, outerRadius, classes } = props;
	const { cx, cy } = store.use(selectorChartPolarCenter);
	const { scale, tickNumber, tickInterval, tickSpacing } = axis;
	const ticks = useTicks({
		scale,
		tickNumber,
		tickInterval,
		tickSpacing,
		direction: "rotation"
	});
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(import_react.Fragment, { children: ticks.map(({ value, offset }) => {
		const angle = offset - Math.PI / 2;
		const dx = Math.cos(angle);
		const dy = Math.sin(angle);
		return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(GridLine, {
			x1: cx + innerRadius * dx,
			y1: cy + innerRadius * dy,
			x2: cx + outerRadius * dx,
			y2: cy + outerRadius * dy,
			className: classes.rotationLine
		}, `rotation-${value?.getTime?.() ?? value}`);
	}) });
}
//#endregion
//#region node_modules/@mui/x-charts/ChartsRadialGrid/ChartsRadiusGrid.mjs
/**
* @ignore - internal component.
*/
function ChartsRadiusGrid(props) {
	const { store } = useChartsContext();
	const { axis, startAngle, endAngle, isFullCircle, classes } = props;
	const { cx, cy } = store.use(selectorChartPolarCenter);
	const { scale, tickNumber, tickInterval, tickSpacing } = axis;
	const ticks = useTicks({
		scale,
		tickNumber,
		tickInterval,
		tickSpacing,
		direction: "radius"
	});
	if (isFullCircle) return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(import_react.Fragment, { children: ticks.map(({ offset: radius }) => /*#__PURE__*/ (0, import_jsx_runtime.jsx)(GridCircle, {
		cx,
		cy,
		r: radius,
		className: classes.radiusLine
	}, `radius-${radius}`)) });
	const startDx = Math.cos(startAngle - Math.PI / 2);
	const startDy = Math.sin(startAngle - Math.PI / 2);
	const endDx = Math.cos(endAngle - Math.PI / 2);
	const endDy = Math.sin(endAngle - Math.PI / 2);
	const isLargeArc = Math.abs(endAngle - startAngle) >= Math.PI;
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(import_react.Fragment, { children: ticks.map(({ offset: radius }) => {
		return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(GridPath, {
			d: `M${cx + startDx * radius},${cy + startDy * radius} A ${radius} ${radius} 0 ${isLargeArc ? 1 : 0} 1 ${cx + endDx * radius},${cy + endDy * radius}`,
			className: classes.radiusLine
		}, `radius-${radius}`);
	}) });
}
//#endregion
//#region node_modules/@mui/x-charts/ChartsRadialGrid/ChartsRadialGrid.mjs
var _excluded = [
	"className",
	"rotation",
	"radius"
];
var useUtilityClasses$3 = ({ classes }) => {
	return composeClasses({
		root: ["root"],
		rotationLine: ["line", "rotationLine"],
		radiusLine: ["line", "radiusLine"]
	}, getChartsRadialGridUtilityClass, classes);
};
/**
* Demos:
*
* - [Radar](https://mui.com/x/react-charts/radar/)
*
* API:
*
* - [ChartsRadialGrid API](https://mui.com/x/api/charts/charts-radial-grid/)
*/
function ChartsRadialGrid(inProps) {
	const props = useThemeProps({
		props: inProps,
		name: "MuiChartsRadialGrid"
	});
	const { className, rotation, radius } = props, other = _objectWithoutPropertiesLoose(props, _excluded);
	const { rotationAxis, rotationAxisIds } = useRotationAxes();
	const { radiusAxis, radiusAxisIds } = useRadiusAxes();
	const classes = useUtilityClasses$3(props);
	const rotationAxisConfig = rotationAxis[rotationAxisIds[0]];
	const radiusAxisConfig = radiusAxis[radiusAxisIds[0]];
	const innerRadius = radiusAxisConfig?.scale.range()[0] ?? 0;
	const outerRadius = radiusAxisConfig?.scale.range()[1] ?? 0;
	const startAngle = rotationAxisConfig?.scale.range()[0] ?? 0;
	const endAngle = rotationAxisConfig?.scale.range()[1] ?? 0;
	const isFullCircle = rotationAxisConfig?.isFullCircle ?? false;
	return /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(GridRoot, _extends({}, other, {
		className: clsx(classes.root, className),
		children: [rotation && rotationAxisConfig && /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ChartsRotationGrid, {
			axis: rotationAxisConfig,
			outerRadius,
			innerRadius,
			classes
		}), radius && radiusAxisConfig && /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ChartsRadiusGrid, {
			axis: radiusAxisConfig,
			startAngle,
			endAngle,
			isFullCircle,
			classes
		})]
	}));
}
ChartsRadialGrid.propTypes = {
	/**
	* Override or extend the styles applied to the component.
	*/
	classes: import_prop_types.default.object,
	/**
	* A CSS class name applied to the root element.
	*/
	className: import_prop_types.default.string,
	/**
	* Displays radius (concentric) grid.
	*/
	radius: import_prop_types.default.bool,
	/**
	* Displays rotation (spoke) grid.
	*/
	rotation: import_prop_types.default.bool
};
//#endregion
//#region node_modules/@mui/x-charts/ChartsRadialGrid/index.mjs
/**
* @deprecated radial grid is now stable, import `ChartsRadialGrid` instead
*/
var Unstable_ChartsRadialGrid = ChartsRadialGrid;
//#endregion
//#region node_modules/@mui/x-charts/ChartsRadialAxisHighlight/chartsRadialAxisHighlightClasses.mjs
function getRadialAxisHighlightUtilityClass(slot) {
	return generateUtilityClass("MuiChartsRadialAxisHighlight", slot);
}
var chartsRadialAxisHighlightClasses = generateUtilityClasses("MuiChartsRadialAxisHighlight", ["root"]);
//#endregion
//#region node_modules/@mui/x-charts/ChartsRadialAxisHighlight/ChartsRadialAxisHighlightPath.mjs
var ChartsRadialAxisHighlightPath = styled("path", {
	name: "MuiChartsRadialAxisHighlight",
	slot: "Root"
})(({ theme }) => ({
	pointerEvents: "none",
	variants: [{
		props: { axisHighlight: "band" },
		style: _extends({
			fill: "white",
			fillOpacity: .1
		}, theme.applyStyles("light", { fill: "gray" }))
	}, {
		props: { axisHighlight: "line" },
		style: _extends({
			fill: "none",
			strokeDasharray: "5 2",
			stroke: "#ffffff"
		}, theme.applyStyles("light", { stroke: "#000000" }))
	}]
}));
var ChartsRadialAxisHighlightCircle = styled("circle", {
	name: "MuiChartsRadialAxisHighlight",
	slot: "Root"
})(({ theme }) => ({
	pointerEvents: "none",
	variants: [{
		props: { axisHighlight: "band" },
		style: _extends({
			fill: "white",
			fillOpacity: .1
		}, theme.applyStyles("light", { fill: "gray" }))
	}, {
		props: { axisHighlight: "line" },
		style: _extends({
			fill: "none",
			strokeDasharray: "5 2",
			stroke: "#ffffff"
		}, theme.applyStyles("light", { stroke: "#000000" }))
	}]
}));
//#endregion
//#region node_modules/@mui/x-charts/ChartsRadialAxisHighlight/ChartsRotationAxisHighlight.mjs
function polarToSvg$1(cx, cy, radius, angle) {
	return [cx + radius * Math.sin(angle), cy - radius * Math.cos(angle)];
}
/**
* @ignore - internal component.
*/
function ChartsRotationAxisHighlight(props) {
	const { type, classes } = props;
	const store = useStore();
	const { cx, cy } = store.use(selectorChartPolarCenter);
	const { axis: rotationAxes, axisIds: rotationAxisIds } = store.use(selectorChartRotationAxis);
	const { axis: radiusAxes, axisIds: radiusAxisIds } = store.use(selectorChartRadiusAxis);
	const rotationIndex = store.use(selectorChartsInteractionRotationAxisIndex);
	const rotationValue = store.use(selectorChartsInteractionRotationAxisValue);
	if (rotationIndex === null || rotationIndex === -1) return null;
	const rotationAxisId = rotationAxisIds[0];
	const radiusAxisId = radiusAxisIds[0];
	if (rotationAxisId === void 0 || radiusAxisId === void 0) return null;
	const rotationAxis = rotationAxes[rotationAxisId];
	const radiusAxis = radiusAxes[radiusAxisId];
	const rotationScale = rotationAxis.scale;
	const radiusScale = radiusAxis.scale;
	const innerRadius = radiusScale.range()[0];
	const outerRadius = radiusScale.range()[1];
	const angle = rotationScale(rotationValue);
	if (angle === void 0) return null;
	if (type === "band") {
		if (!isOrdinalScale(rotationScale)) return null;
		const step = rotationScale.step();
		const startAngle = angle - (step - rotationScale.bandwidth()) / 2;
		const endAngle = startAngle + step;
		const [x1Outer, y1Outer] = polarToSvg$1(cx, cy, outerRadius, startAngle);
		const [x2Outer, y2Outer] = polarToSvg$1(cx, cy, outerRadius, endAngle);
		const [x1Inner, y1Inner] = polarToSvg$1(cx, cy, innerRadius, startAngle);
		const [x2Inner, y2Inner] = polarToSvg$1(cx, cy, innerRadius, endAngle);
		const isLargeArc = Math.abs(endAngle - startAngle) >= Math.PI;
		const innerArc = innerRadius > 0 ? `L ${x2Inner} ${y2Inner} A ${innerRadius} ${innerRadius} 0 ${isLargeArc ? 1 : 0} 0 ${x1Inner} ${y1Inner}` : `L ${cx} ${cy}`;
		return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ChartsRadialAxisHighlightPath, {
			d: `M ${x1Outer} ${y1Outer} A ${outerRadius} ${outerRadius} 0 ${isLargeArc ? 1 : 0} 1 ${x2Outer} ${y2Outer} ${innerArc} Z`,
			className: classes.root,
			ownerState: { axisHighlight: "band" }
		});
	}
	if (type === "line") {
		const lineAngle = isOrdinalScale(rotationScale) ? angle + rotationScale.bandwidth() / 2 : angle;
		const [x1, y1] = polarToSvg$1(cx, cy, innerRadius, lineAngle);
		const [x2, y2] = polarToSvg$1(cx, cy, outerRadius, lineAngle);
		return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ChartsRadialAxisHighlightPath, {
			d: `M ${x1} ${y1} L ${x2} ${y2}`,
			className: classes.root,
			ownerState: { axisHighlight: "line" }
		});
	}
	return null;
}
//#endregion
//#region node_modules/@mui/x-charts/ChartsRadialAxisHighlight/ChartsRadiusAxisHighlight.mjs
function polarToSvg(cx, cy, radius, angle) {
	return [cx + radius * Math.sin(angle), cy - radius * Math.cos(angle)];
}
/**
* @ignore - internal component.
*/
function ChartsRadiusAxisHighlight(props) {
	const { type, classes } = props;
	const store = useStore();
	const { cx, cy } = store.use(selectorChartPolarCenter);
	const { axis: radiusAxes, axisIds: radiusAxisIds } = store.use(selectorChartRadiusAxis);
	const { axis: rotationAxes, axisIds: rotationAxisIds } = store.use(selectorChartRotationAxis);
	const pointerRadius = store.use(selectorChartsInteractionRadius);
	const radiusValue = store.use(selectorChartsInteractionRadiusAxisValue);
	const radiusAxisId = radiusAxisIds[0];
	if (type === "none" || radiusAxisId === void 0 || pointerRadius === null) return null;
	const radiusAxis = radiusAxes[radiusAxisId];
	const radiusScale = radiusAxis.scale;
	const innerRadius = radiusScale.range()[0];
	const outerRadius = radiusScale.range()[1];
	if (pointerRadius < innerRadius || pointerRadius > outerRadius) return null;
	const rotationAxisId = rotationAxisIds[0];
	const rotationAxis = rotationAxisId !== void 0 ? rotationAxes[rotationAxisId] : void 0;
	const startAngle = rotationAxis?.scale.range()[0] ?? 0;
	const endAngle = rotationAxis?.scale.range()[1] ?? 2 * Math.PI;
	const isFullCircle = rotationAxis?.isFullCircle ?? Math.abs(endAngle - startAngle) >= 2 * Math.PI;
	if (type === "line") {
		if (isFullCircle) return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ChartsRadialAxisHighlightCircle, {
			cx,
			cy,
			r: pointerRadius,
			className: classes.root,
			ownerState: { axisHighlight: "line" }
		});
		const [startX, startY] = polarToSvg(cx, cy, pointerRadius, startAngle);
		const [endX, endY] = polarToSvg(cx, cy, pointerRadius, endAngle);
		return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ChartsRadialAxisHighlightPath, {
			d: `M ${startX} ${startY} A ${pointerRadius} ${pointerRadius} 0 ${Math.abs(endAngle - startAngle) >= Math.PI ? 1 : 0} ${endAngle > startAngle ? 1 : 0} ${endX} ${endY}`,
			className: classes.root,
			ownerState: { axisHighlight: "line" }
		});
	}
	if (!isOrdinalScale(radiusAxis.scale)) return null;
	const radius = radiusAxis.scale(radiusValue);
	const step = radiusAxis.scale.step();
	const bandInnerRadius = radius - (step - radiusAxis.scale.bandwidth()) / 2;
	const bandOuterRadius = bandInnerRadius + step;
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ChartsRadialAxisHighlightPath, {
		d: getRingPath({
			x: cx,
			y: cy
		}, bandInnerRadius, bandOuterRadius, isFullCircle ? void 0 : {
			start: startAngle,
			end: endAngle
		}),
		className: classes.root,
		ownerState: { axisHighlight: "band" }
	});
}
//#endregion
//#region node_modules/@mui/x-charts/ChartsRadialAxisHighlight/ChartsRadialAxisHighlight.mjs
var useUtilityClasses$2 = () => {
	return composeClasses({ root: ["root"] }, getRadialAxisHighlightUtilityClass);
};
/**
* Demos:
*
* - [Radial Lines](https://mui.com/x/react-charts/radial-lines/)
*
* API:
*
* - [ChartsRadialAxisHighlight API](https://mui.com/x/api/charts/charts-radial-axis-highlight/)
*/
function ChartsRadialAxisHighlight(props) {
	const { rotation: rotationHighlight, radius: radiusHighlight } = props;
	const classes = useUtilityClasses$2();
	return /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(import_react.Fragment, { children: [rotationHighlight && rotationHighlight !== "none" && /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ChartsRotationAxisHighlight, {
		type: rotationHighlight,
		classes
	}), radiusHighlight && radiusHighlight !== "none" && /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ChartsRadiusAxisHighlight, {
		type: radiusHighlight,
		classes
	})] });
}
ChartsRadialAxisHighlight.propTypes = {
	radius: import_prop_types.default.oneOf([
		"band",
		"line",
		"none"
	]),
	rotation: import_prop_types.default.oneOf([
		"band",
		"line",
		"none"
	])
};
//#endregion
//#region node_modules/@mui/x-charts/ChartsRadialAxisHighlight/index.mjs
/**
* @deprecated radial axis highlight is now stable, import `ChartsRadialAxisHighlight` instead
*/
var Unstable_ChartsRadialAxisHighlight = ChartsRadialAxisHighlight;
//#endregion
//#region node_modules/@mui/x-charts/ChartsRadiusAxis/sharedRadialAxisClasses.mjs
function getRadialAxisUtilityClass(slot) {
	return generateUtilityClass("MuiChartsRadialAxis", slot);
}
var chartsRadialAxisClasses = generateUtilityClasses("MuiChartsRadialAxis", [
	"root",
	"radius",
	"rotation",
	"line",
	"tickContainer",
	"tick",
	"tickLabel",
	"centered"
]);
//#endregion
//#region node_modules/@mui/x-charts/ChartsRadiusAxis/chartsRadiusAxisClasses.mjs
var useUtilityClasses$1 = (props) => {
	const { classes, isCentered } = props;
	return composeClasses({
		root: ["root", "radius"],
		line: ["line"],
		tickContainer: ["tickContainer"],
		tick: ["tick"],
		tickLabel: ["tickLabel", isCentered && "centered"]
	}, getRadialAxisUtilityClass, classes);
};
//#endregion
//#region node_modules/@mui/x-charts/ChartsRadiusAxis/createGetLabelTextAnchors.mjs
/**
* Higher order function that transform a `getLabelTransform` to the <text /> anchor props.
*/
var createGetLabelTextAnchors = (getLabelTransform) => (px, py, tickLabelPosition) => {
	const { verticalAlign, horizontalAlign } = getLabelTransform(px, py, tickLabelPosition);
	let textAnchor;
	let dominantBaseline;
	switch (verticalAlign) {
		case "start":
			dominantBaseline = "hanging";
			break;
		case "middle":
			dominantBaseline = "middle";
			break;
		default:
			dominantBaseline = "auto";
			break;
	}
	switch (horizontalAlign) {
		case "start":
			textAnchor = "start";
			break;
		case "middle":
			textAnchor = "middle";
			break;
		default:
			textAnchor = "end";
			break;
	}
	return {
		textAnchor,
		dominantBaseline
	};
};
//#endregion
//#region node_modules/@mui/x-charts/ChartsRadiusAxis/getLabelTransform.mjs
/**
* Return the vertical/horizontal alignment for a tick label at a given position.
* @param px The normalized x position to the axis line (between -1 and 1).
* @param py The normalized y position to the axis line (between -1 and 1).
* @param tickLabelPosition The position of the tick label relative to the axis line.
* @returns The vertical and horizontal alignment for the tick label.
*/
function getLabelTransform$1(px, py, tickLabelPosition) {
	if (tickLabelPosition === "center") return {
		verticalAlign: "middle",
		horizontalAlign: "middle"
	};
	let verticalAlign = "middle";
	let horizontalAlign = "middle";
	if (px > .3) verticalAlign = tickLabelPosition === "after" ? "start" : "end";
	else if (px < -.3) verticalAlign = tickLabelPosition === "after" ? "end" : "start";
	if (py > .3) horizontalAlign = tickLabelPosition === "after" ? "end" : "start";
	else if (py < -.3) horizontalAlign = tickLabelPosition === "after" ? "start" : "end";
	return {
		verticalAlign,
		horizontalAlign
	};
}
//#endregion
//#region node_modules/@mui/x-charts/ChartsRadiusAxis/ChartsRadiusAxis.mjs
var TICK_LABEL_GAP$1 = 3;
var getLabelTextAnchors$1 = createGetLabelTextAnchors(getLabelTransform$1);
/**
* Get the angle to use to display the radius axis.
* @param position The position props
* @param rotationAxis The default rotation axis
* @returns the angle in radians to use.
*/
function getAxisAngleInRadians(position, rotationAxis) {
	if (position === "start") return rotationAxis?.scale.range()[0] ?? 0;
	if (position === "end") return rotationAxis?.scale.range()[1] ?? 0;
	if (typeof position === "number") return position * Math.PI / 180;
	return 0;
}
function ChartsRadiusAxis(props) {
	const radiusAxis = useRadiusAxis(props.axisId);
	const settings = _extends({}, radiusAxis, props);
	const { position = "start", disableLine, disableTicks, disableTickLabel, tickLabelPosition: tickLabelPositionProp = "auto", tickPosition = position === "start" ? "before" : "after", tickSize = 6, className, classes: classesProp } = settings;
	let tickLabelPosition = tickLabelPositionProp;
	if (tickLabelPosition === "auto") tickLabelPosition = position === "start" ? "before" : "after";
	const isCentered = tickLabelPosition === "center";
	const classes = useUtilityClasses$1({
		classes: classesProp,
		isCentered
	});
	const theme = useTheme();
	const { store } = useChartsContext();
	const { cx, cy } = store.use(selectorChartPolarCenter);
	const rotationAxis = useRotationAxis();
	const ticks = useTicks({
		scale: radiusAxis.scale,
		tickNumber: radiusAxis?.tickNumber ?? 5,
		tickInterval: radiusAxis?.tickInterval,
		tickSpacing: radiusAxis?.tickSpacing,
		valueFormatter: radiusAxis?.valueFormatter,
		direction: "radius"
	});
	if (!radiusAxis || settings.position === "none") return null;
	const angle = getAxisAngleInRadians(position, rotationAxis);
	const dx = Math.sin(angle);
	const dy = -Math.cos(angle);
	const px = -dy;
	const py = dx;
	const [innerRadius, outerRadius] = radiusAxis.scale.range();
	const stroke = (theme.vars ?? theme).palette.text.primary;
	const tickDx = (tickPosition === "after" ? 1 : -1) * px * tickSize;
	const tickDy = (tickPosition === "after" ? 1 : -1) * py * tickSize;
	const tickLabelGap = isCentered ? 0 : TICK_LABEL_GAP$1;
	const tickLabelGapDx = (tickLabelPosition === "after" ? 1 : -1) * px * tickLabelGap;
	const tickLabelGapDy = (tickLabelPosition === "after" ? 1 : -1) * py * tickLabelGap;
	return /*#__PURE__*/ (0, import_jsx_runtime.jsxs)("g", {
		className: clsx(classes.root, className),
		children: [!disableLine && /*#__PURE__*/ (0, import_jsx_runtime.jsx)("line", {
			x1: cx + dx * innerRadius,
			y1: cy + dy * innerRadius,
			x2: cx + dx * outerRadius,
			y2: cy + dy * outerRadius,
			stroke,
			className: classes.line
		}), ticks.map(({ offset: radius, labelOffset, formattedValue }, index) => {
			if (!formattedValue) return null;
			const tickX = cx + dx * radius;
			const tickY = cy + dy * radius;
			let labelX = cx + dx * (radius + labelOffset);
			let labelY = cy + dy * (radius + labelOffset);
			if (tickLabelGap !== 0) {
				labelX += tickLabelGapDx;
				labelY += tickLabelGapDy;
			}
			if (!isCentered && tickLabelPosition === tickPosition && !disableTicks) {
				labelX += tickDx;
				labelY += tickDy;
			}
			return /*#__PURE__*/ (0, import_jsx_runtime.jsxs)("g", {
				className: classes.tickContainer,
				children: [!disableTicks && /*#__PURE__*/ (0, import_jsx_runtime.jsx)("line", {
					x1: tickX,
					y1: tickY,
					x2: tickX + tickDx,
					y2: tickY + tickDy,
					stroke,
					className: classes.tick
				}), !disableTickLabel && /*#__PURE__*/ (0, import_jsx_runtime.jsx)("text", _extends({
					x: labelX,
					y: labelY,
					fill: stroke,
					fontSize: 12,
					className: classes.tickLabel,
					pointerEvents: "none"
				}, getLabelTextAnchors$1(dx, dy, tickLabelPosition), { children: formattedValue }))]
			}, index);
		})]
	});
}
ChartsRadiusAxis.propTypes = {
	axis: import_prop_types.default.oneOf(["radius"]),
	/**
	* The id of the axis to render.
	* If undefined, it will be the first defined axis.
	*/
	axisId: import_prop_types.default.oneOfType([import_prop_types.default.number, import_prop_types.default.string]),
	/**
	* Override or extend the styles applied to the component.
	*/
	classes: import_prop_types.default.object,
	/**
	* A CSS class name applied to the root element.
	*/
	className: import_prop_types.default.string,
	/**
	* If true, the axis line is disabled.
	* @default false
	*/
	disableLine: import_prop_types.default.bool,
	/**
	* If true, the tick labels are not rendered.
	* @default false
	*/
	disableTickLabel: import_prop_types.default.bool,
	/**
	* If true, the ticks are disabled.
	* @default false
	*/
	disableTicks: import_prop_types.default.bool,
	/**
	* The label of the axis.
	*/
	label: import_prop_types.default.string,
	/**
	* The style applied to the axis label.
	*/
	labelStyle: import_prop_types.default.object,
	/**
	* The maximal radius.
	* Can be a number (in pixels), a pixel string (for example `'80px'`), or a percentage string
	* (for example `'80%'`) relative to the available radius (half the smallest side of the drawing area).
	* @default '100%'
	*/
	maxRadius: import_prop_types.default.oneOfType([import_prop_types.default.number, import_prop_types.default.string]),
	/**
	* The minimal radius.
	* Can be a number (in pixels), a pixel string (for example `'20px'`), or a percentage string
	* (for example `'20%'`) relative to the available radius (half the smallest side of the drawing area).
	* @default 0
	*/
	minRadius: import_prop_types.default.oneOfType([import_prop_types.default.number, import_prop_types.default.string]),
	/**
	* The position of the axis in polar coordinates.
	* It can be 'start', 'end', or a specific angle in degrees.
	* @default 'start'
	*/
	position: import_prop_types.default.oneOfType([import_prop_types.default.oneOf([
		"end",
		"none",
		"start"
	]), import_prop_types.default.number]),
	sx: import_prop_types.default.oneOfType([
		import_prop_types.default.arrayOf(import_prop_types.default.oneOfType([
			import_prop_types.default.func,
			import_prop_types.default.object,
			import_prop_types.default.bool
		])),
		import_prop_types.default.func,
		import_prop_types.default.object
	]),
	/**
	* Defines which ticks are displayed.
	* Its value can be:
	* - 'auto' In such case the ticks are computed based on axis scale and other parameters.
	* - a filtering function of the form `(value, index) => boolean` which is available only if the axis has "point" scale.
	* - an array containing the values where ticks should be displayed.
	* @see See {@link https://mui.com/x/react-charts/axis/#fixed-tick-positions}
	* @default 'auto'
	*/
	tickInterval: import_prop_types.default.oneOfType([
		import_prop_types.default.oneOf(["auto"]),
		import_prop_types.default.array,
		import_prop_types.default.func
	]),
	/**
	* Defines which ticks get its label displayed. Its value can be:
	* - 'auto' In such case, labels are displayed if they do not overlap with the previous one.
	* - a filtering function of the form (value, index) => boolean. Warning: the index is tick index, not data ones.
	* @default 'auto'
	*/
	tickLabelInterval: import_prop_types.default.oneOfType([import_prop_types.default.oneOf(["auto"]), import_prop_types.default.func]),
	/**
	* The placement of ticks label. Can be the middle of the band, or the tick position.
	* Only used if scale is 'band'.
	* @default 'middle'
	*/
	tickLabelPlacement: import_prop_types.default.oneOf(["middle", "tick"]),
	/**
	* Set the position of the tick labels relative to the axis line.
	* The before/after is defined based on clockwise direction.
	* Using `'auto'` sets it to `'before'` if position is `'start'` and `'after'` otherwise.
	* @default 'auto'
	*/
	tickLabelPosition: import_prop_types.default.oneOf([
		"after",
		"auto",
		"before",
		"center"
	]),
	/**
	* The style applied to ticks text.
	*/
	tickLabelStyle: import_prop_types.default.object,
	/**
	* Maximal step between two ticks.
	* When using time data, the value is assumed to be in ms.
	* Not supported by categorical axis (band, points).
	*/
	tickMaxStep: import_prop_types.default.number,
	/**
	* Minimal step between two ticks.
	* When using time data, the value is assumed to be in ms.
	* Not supported by categorical axis (band, points).
	*/
	tickMinStep: import_prop_types.default.number,
	/**
	* The number of ticks. This number is not guaranteed.
	* Not supported by categorical axis (band, points).
	*/
	tickNumber: import_prop_types.default.number,
	/**
	* The placement of ticks in regard to the band interval.
	* Only used if scale is 'band'.
	* @default 'extremities'
	*/
	tickPlacement: import_prop_types.default.oneOf([
		"end",
		"extremities",
		"middle",
		"start"
	]),
	/**
	* Set the position of the tick relative to the axis line.
	* The before/after is defined based on clockwise direction.
	* @default position === 'start' ? 'before' : 'after'
	*/
	tickPosition: import_prop_types.default.oneOf(["after", "before"]),
	/**
	* The size of the ticks.
	* @default 6
	*/
	tickSize: import_prop_types.default.number,
	/**
	* The minimum space between ticks when using an ordinal scale. It defines the minimum distance in pixels between two ticks.
	* @default 0
	*/
	tickSpacing: import_prop_types.default.number
};
//#endregion
//#region node_modules/@mui/x-charts/ChartsRadiusAxis/index.mjs
/**
* @deprecated radius axis is now stable, import `ChartsRadiusAxis` instead
*/
var Unstable_ChartsRadiusAxis = ChartsRadiusAxis;
//#endregion
//#region node_modules/@mui/x-charts/ChartsRotationAxis/chartsRotationAxisClasses.mjs
var useUtilityClasses = (props) => {
	const { classes } = props;
	return composeClasses({
		root: ["root", "rotation"],
		line: ["line"],
		tickContainer: ["tickContainer"],
		tick: ["tick"],
		tickLabel: ["tickLabel"]
	}, getRadialAxisUtilityClass, classes);
};
//#endregion
//#region node_modules/@mui/x-charts/ChartsRotationAxis/getLabelTransform.mjs
/**
* Return the vertical/horizontal alignment for a tick label at a given position.
* @param px The normalized x position to the axis line (between -1 and 1).
* @param py The normalized y position to the axis line (between -1 and 1).
* @param tickLabelPosition The position of the tick label relative to the axis line.
* @returns The vertical and horizontal alignment for the tick label.
*/
function getLabelTransform(px, py, tickLabelPosition) {
	if (tickLabelPosition === "center") return {
		verticalAlign: "middle",
		horizontalAlign: "middle"
	};
	let verticalAlign = "middle";
	let horizontalAlign = "middle";
	if (px > .3) horizontalAlign = tickLabelPosition === "after" ? "start" : "end";
	else if (px < -.3) horizontalAlign = tickLabelPosition === "after" ? "end" : "start";
	if (py > .3) verticalAlign = tickLabelPosition === "after" ? "start" : "end";
	else if (py < -.3) verticalAlign = tickLabelPosition === "after" ? "end" : "start";
	return {
		verticalAlign,
		horizontalAlign
	};
}
//#endregion
//#region node_modules/@mui/x-charts/ChartsRotationAxis/ChartsRotationAxis.mjs
var TICK_LABEL_GAP = 3;
var getLabelTextAnchors = createGetLabelTextAnchors(getLabelTransform);
function ChartsRotationAxis(props) {
	const rotationAxis = useRotationAxis(props.axisId);
	const settings = _extends({}, rotationAxis, props);
	const { disableLine, disableTicks, disableTickLabel, position = "outside", tickLabelPosition = position === "outside" ? "after" : "before", tickPosition = position === "outside" ? "after" : "before", tickSize = 6, className, classes: classesProp } = settings;
	const classes = useUtilityClasses({ classes: classesProp });
	const theme = useTheme();
	const { store } = useChartsContext();
	const { cx, cy } = store.use(selectorChartPolarCenter);
	const radiusAxis = useRadiusAxis();
	const ticks = useTicks({
		scale: rotationAxis.scale,
		tickNumber: rotationAxis?.tickNumber ?? 5,
		tickInterval: rotationAxis?.tickInterval,
		tickSpacing: rotationAxis?.tickSpacing,
		valueFormatter: rotationAxis?.valueFormatter,
		direction: "rotation"
	});
	if (!rotationAxis || !radiusAxis || settings.position === "none") return null;
	const radius = radiusAxis.scale.range()[position === "inside" ? 0 : 1];
	const [startAngle, endAngle] = rotationAxis.scale.range();
	const stroke = (theme.vars ?? theme).palette.text.primary;
	const angleToPoint = (angle, r) => ({
		x: cx + Math.sin(angle) * r,
		y: cy - Math.cos(angle) * r
	});
	const sweepFlag = endAngle - startAngle >= 0 ? 1 : 0;
	const start = angleToPoint(startAngle, radius);
	const end = angleToPoint(endAngle, radius);
	const largeArcFlag = Math.abs(endAngle - startAngle) > Math.PI ? 1 : 0;
	const arcPath = `M ${start.x} ${start.y} A ${radius} ${radius} 0 ${largeArcFlag} ${sweepFlag} ${end.x} ${end.y}`;
	return /*#__PURE__*/ (0, import_jsx_runtime.jsxs)("g", {
		className: clsx(classes.root, className),
		children: [!disableLine && (rotationAxis.isFullCircle ? /*#__PURE__*/ (0, import_jsx_runtime.jsx)("circle", {
			cx,
			cy,
			r: radius,
			stroke,
			fill: "none",
			className: classes.line
		}) : /*#__PURE__*/ (0, import_jsx_runtime.jsx)("path", {
			d: arcPath,
			stroke,
			fill: "none",
			className: classes.line
		})), ticks.map(({ offset: angle, labelOffset, formattedValue }, index) => {
			if (!formattedValue) return null;
			const dx = Math.sin(angle);
			const dy = -Math.cos(angle);
			const labelDx = labelOffset === 0 ? dx : Math.sin(angle + labelOffset);
			const labelDy = labelOffset === 0 ? dy : -Math.cos(angle + labelOffset);
			const tx = cx + dx * radius;
			const ty = cy + dy * radius;
			const tickDx = (tickPosition === "after" ? 1 : -1) * dx * tickSize;
			const tickDy = (tickPosition === "after" ? 1 : -1) * dy * tickSize;
			let tickLabelRadius = radius + (tickLabelPosition === "after" ? 1 : -1) * TICK_LABEL_GAP;
			if (tickLabelPosition === tickPosition && !disableTicks) tickLabelRadius += tickSize;
			const labelX = cx + labelDx * tickLabelRadius;
			const labelY = cy + labelDy * tickLabelRadius;
			return /*#__PURE__*/ (0, import_jsx_runtime.jsxs)("g", {
				className: classes.tickContainer,
				children: [!disableTicks && /*#__PURE__*/ (0, import_jsx_runtime.jsx)("line", {
					x1: tx,
					y1: ty,
					x2: tx + tickDx,
					y2: ty + tickDy,
					stroke,
					className: classes.tick
				}), !disableTickLabel && /*#__PURE__*/ (0, import_jsx_runtime.jsx)("text", _extends({
					x: labelX,
					y: labelY,
					fill: stroke,
					fontSize: 12,
					className: classes.tickLabel,
					pointerEvents: "none"
				}, getLabelTextAnchors(labelDx, labelDy, tickLabelPosition), { children: formattedValue }))]
			}, index);
		})]
	});
}
ChartsRotationAxis.propTypes = {
	axis: import_prop_types.default.oneOf(["rotation"]),
	/**
	* The id of the axis to render.
	* If undefined, it will be the first defined axis.
	*/
	axisId: import_prop_types.default.oneOfType([import_prop_types.default.number, import_prop_types.default.string]),
	/**
	* Override or extend the styles applied to the component.
	*/
	classes: import_prop_types.default.object,
	/**
	* A CSS class name applied to the root element.
	*/
	className: import_prop_types.default.string,
	/**
	* If true, the axis line is disabled.
	* @default false
	*/
	disableLine: import_prop_types.default.bool,
	/**
	* If true, the tick labels are not rendered.
	* @default false
	*/
	disableTickLabel: import_prop_types.default.bool,
	/**
	* If true, the ticks are disabled.
	* @default false
	*/
	disableTicks: import_prop_types.default.bool,
	/**
	* The end angle (in deg).
	*/
	endAngle: import_prop_types.default.number,
	/**
	* The label of the axis.
	*/
	label: import_prop_types.default.string,
	/**
	* The gap between the axis and the label.
	*/
	labelGap: import_prop_types.default.number,
	/**
	* The style applied to the axis label.
	*/
	labelStyle: import_prop_types.default.object,
	/**
	* The position of the rotation axis.
	* It can be 'inside' or 'outside'.
	* @default 'outside'
	*/
	position: import_prop_types.default.oneOf([
		"inside",
		"none",
		"outside"
	]),
	/**
	* The start angle (in deg).
	*/
	startAngle: import_prop_types.default.number,
	sx: import_prop_types.default.oneOfType([
		import_prop_types.default.arrayOf(import_prop_types.default.oneOfType([
			import_prop_types.default.func,
			import_prop_types.default.object,
			import_prop_types.default.bool
		])),
		import_prop_types.default.func,
		import_prop_types.default.object
	]),
	/**
	* Defines which ticks are displayed.
	* Its value can be:
	* - 'auto' In such case the ticks are computed based on axis scale and other parameters.
	* - a filtering function of the form `(value, index) => boolean` which is available only if the axis has "point" scale.
	* - an array containing the values where ticks should be displayed.
	* @see See {@link https://mui.com/x/react-charts/axis/#fixed-tick-positions}
	* @default 'auto'
	*/
	tickInterval: import_prop_types.default.oneOfType([
		import_prop_types.default.oneOf(["auto"]),
		import_prop_types.default.array,
		import_prop_types.default.func
	]),
	/**
	* Defines which ticks get its label displayed. Its value can be:
	* - 'auto' In such case, labels are displayed if they do not overlap with the previous one.
	* - a filtering function of the form (value, index) => boolean. Warning: the index is tick index, not data ones.
	* @default 'auto'
	*/
	tickLabelInterval: import_prop_types.default.oneOfType([import_prop_types.default.oneOf(["auto"]), import_prop_types.default.func]),
	/**
	* The placement of ticks label. Can be the middle of the band, or the tick position.
	* Only used if scale is 'band'.
	* @default 'middle'
	*/
	tickLabelPlacement: import_prop_types.default.oneOf(["middle", "tick"]),
	/**
	* Set the position of the tick labels relative to the axis line.
	* `'after'` places them outside the arc, `'before'` inside.
	* @default position === 'outside' ? 'after' : 'before'
	*/
	tickLabelPosition: import_prop_types.default.oneOf(["after", "before"]),
	/**
	* The style applied to ticks text.
	*/
	tickLabelStyle: import_prop_types.default.object,
	/**
	* Maximal step between two ticks.
	* When using time data, the value is assumed to be in ms.
	* Not supported by categorical axis (band, points).
	*/
	tickMaxStep: import_prop_types.default.number,
	/**
	* Minimal step between two ticks.
	* When using time data, the value is assumed to be in ms.
	* Not supported by categorical axis (band, points).
	*/
	tickMinStep: import_prop_types.default.number,
	/**
	* The number of ticks. This number is not guaranteed.
	* Not supported by categorical axis (band, points).
	*/
	tickNumber: import_prop_types.default.number,
	/**
	* The placement of ticks in regard to the band interval.
	* Only used if scale is 'band'.
	* @default 'extremities'
	*/
	tickPlacement: import_prop_types.default.oneOf([
		"end",
		"extremities",
		"middle",
		"start"
	]),
	/**
	* Set the position of the tick relative to the axis line.
	* `'after'` places them outside the arc, `'before'` inside.
	* @default position === 'outside' ? 'after' : 'before'
	*/
	tickPosition: import_prop_types.default.oneOf(["after", "before"]),
	/**
	* The size of the ticks.
	* @default 6
	*/
	tickSize: import_prop_types.default.number,
	/**
	* The minimum space between ticks when using an ordinal scale. It defines the minimum distance in pixels between two ticks.
	* @default 0
	*/
	tickSpacing: import_prop_types.default.number
};
//#endregion
//#region node_modules/@mui/x-charts/ChartsRotationAxis/index.mjs
/**
* @deprecated rotation axis is now stable, import `ChartsRotationAxis` instead
*/
var Unstable_ChartsRotationAxis = ChartsRotationAxis;
//#endregion
//#region node_modules/@mui/x-charts/ChartsBrushOverlay/ChartsBrushOverlay.classes.mjs
var brushOverlayClasses = generateUtilityClasses("MuiChartsBrushOverlay", [
	"root",
	"rect",
	"x",
	"y"
]);
//#endregion
//#region node_modules/@mui/x-charts/ChartsBrushOverlay/ChartsBrushOverlay.mjs
function BrushRect(props) {
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)("rect", _extends({
		className: brushOverlayClasses.rect,
		strokeWidth: 1,
		fillOpacity: .2,
		pointerEvents: "none"
	}, props));
}
/**
* Component that renders visual feedback during brush interaction
*/
function ChartsBrushOverlay(props) {
	const { className } = props;
	const store = useStore();
	const drawingArea = store.use(selectorChartDrawingArea);
	const theme = useTheme();
	const brushStartX = store.use(selectorBrushStartX);
	const brushStartY = store.use(selectorBrushStartY);
	const brushCurrentX = store.use(selectorBrushCurrentX);
	const brushCurrentY = store.use(selectorBrushCurrentY);
	const brushConfig = store.use(selectorBrushConfig);
	if (brushStartX === null || brushStartY === null || brushCurrentX === null || brushCurrentY === null) return null;
	const { left, top, width, height } = drawingArea;
	const clampX = (x) => Math.max(left, Math.min(left + width, x));
	const clampY = (y) => Math.max(top, Math.min(top + height, y));
	const startX = clampX(brushStartX);
	const startY = clampY(brushStartY);
	const currentX = clampX(brushCurrentX);
	const currentY = clampY(brushCurrentY);
	const rectColor = theme.palette.mode === "light" ? theme.palette.common.black : theme.palette.common.white;
	if (brushConfig === "xy") {
		const rectWidth = currentX - startX;
		const rectHeight = currentY - startY;
		return /*#__PURE__*/ (0, import_jsx_runtime.jsx)("g", {
			className: clsx(brushOverlayClasses.root, brushOverlayClasses.x, brushOverlayClasses.y, className),
			children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(BrushRect, {
				fill: rectColor,
				x: rectWidth >= 0 ? startX : currentX,
				y: rectHeight >= 0 ? startY : currentY,
				width: Math.abs(rectWidth),
				height: Math.abs(rectHeight)
			})
		});
	}
	if (brushConfig === "y") {
		const minY = Math.min(startY, currentY);
		const rectHeight = Math.max(startY, currentY) - minY;
		return /*#__PURE__*/ (0, import_jsx_runtime.jsx)("g", {
			className: clsx(brushOverlayClasses.root, brushOverlayClasses.y, className),
			children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(BrushRect, {
				fill: rectColor,
				x: left,
				y: minY,
				width,
				height: rectHeight
			})
		});
	}
	const minX = Math.min(startX, currentX);
	const rectWidth = Math.max(startX, currentX) - minX;
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)("g", {
		className: clsx(brushOverlayClasses.root, brushOverlayClasses.x, className),
		children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(BrushRect, {
			fill: rectColor,
			x: minX,
			y: top,
			width: rectWidth,
			height
		})
	});
}
ChartsBrushOverlay.propTypes = { 
/**
* A CSS class name applied to the root element.
*/
className: import_prop_types.default.string };
//#endregion
//#region node_modules/@mui/x-charts/utils/niceDomain.mjs
/**
* Returns a nice domain for the given scale type and domain.
* Does not modify the original domain.
*
* Providing a count improves the nice domain calculation by trying to align tick values to round
* numbers or dates.
*
* For example, if you have a domain of [29, 72] and there are 5 ticks, the nice domain will be
* [20, 80] so that the ticks can be at [20, 35, 50, 65, 80].
* However, if there are 11 ticks, the nice domain will be [25, 75] so that the ticks can be at
* [25, 30, 35, 40, 45, 50, 55, 60, 65, 70, 75].
*
* @param scaleType The type of the scale (e.g., 'linear', 'log', 'time', etc.).
* @param domain The domain to be made nicer.
* @param count An optional number of ticks to improve the nice domain calculation. Defaults to 5.
*/
function niceDomain(scaleType, domain, count = 5) {
	const scale = getScale(scaleType, domain, [0, 1]);
	scale.nice(count);
	return scale.domain();
}
//#endregion
//#region node_modules/@mui/x-charts/index.mjs
/**
* @mui/x-charts v9.9.0
*
* @license MIT
* This source code is licensed under the MIT license found in the
* LICENSE file in the root directory of this source tree.
*/
//#endregion
export { AXIS_LABEL_DEFAULT_HEIGHT, AnimatedArea, AnimatedLine, AreaElement, AreaPlot, BAR_CHART_PLUGINS, BarChart, BarElement, BarLabel, BarPlot, ChartsAxis, ChartsAxisHighlight, ChartsAxisHighlightPath, ChartsAxisHighlightValue, ChartsAxisTooltipContent, ChartsBrushOverlay, ChartsClipPath, ChartsContainer, ChartsDataProvider, ChartsGrid, ChartsItemTooltipContent, ChartsLabel, ChartsLabelMark, ChartsLayerContainer, ChartsLegend, ChartsLocalizationProvider, ChartsRadialAxisHighlight, ChartsRadialAxisHighlightCircle, ChartsRadialAxisHighlightPath, ChartsRadialDataProvider, ChartsRadialGrid, ChartsRadiusAxis, ChartsReferenceLine, ChartsSurface, ChartsSvgLayer, ChartsText, ChartsTooltip, ChartsTooltipCell, ChartsTooltipContainer, ChartsTooltipPaper, ChartsTooltipRow, ChartsTooltipTable, ChartsWrapper, ChartsXAxis, ChartsYAxis, ContinuousColorLegend, DEFAULT_AXIS_SIZE_HEIGHT, DEFAULT_AXIS_SIZE_WIDTH, DEFAULT_MARGINS, DEFAULT_RADIUS_AXIS_KEY, DEFAULT_ROTATION_AXIS_KEY, DEFAULT_X_AXIS_KEY, DEFAULT_Y_AXIS_KEY, FocusedBar, FocusedLineMark, FocusedPieArc, FocusedRadarMark, FocusedScatterMark, Gauge, GaugeContainer, GaugeReferenceArc, GaugeValueArc, GaugeValueText, HighlightedScatterMark, LINE_CHART_PLUGINS, LineChart, LineElement, LineHighlightElement, LineHighlightPlot, LinePlot, MarkElement, MarkPlot, PIE_CHART_PLUGINS, PieArc, PieArcLabel, PieArcLabelPlot, PieArcPlot, PieChart, PiePlot, PiecewiseColorLegend, RADAR_PLUGINS, RADIAL_PLUGINS, RadarAxis, RadarAxisHighlight, RadarChart, RadarDataProvider, RadarGrid, RadarMetricLabels, RadarSeriesArea, RadarSeriesMarks, RadarSeriesPlot, SCATTER_CHART_PLUGINS, Scatter, ScatterChart, ScatterMarker, ScatterPlot, SparkLineChart, Toolbar, ToolbarButton, Unstable_ChartsRadialAxisHighlight, Unstable_ChartsRadialDataProvider, Unstable_ChartsRadialGrid, Unstable_ChartsRadiusAxis, Unstable_ChartsRotationAxis, Unstable_RadarChart, Unstable_RadarDataProvider, axisClasses, barClasses, bluePalette, bluePaletteDark, bluePaletteLight, blueberryTwilightPalette, blueberryTwilightPaletteDark, blueberryTwilightPaletteLight, brushOverlayClasses, chartsAxisHighlightClasses, chartsGridClasses, chartsRadialAxisClasses, chartsRadialAxisHighlightClasses, chartsRadialGridClasses, chartsSurfaceClasses, chartsSvgLayerClasses, chartsToolbarClasses, chartsTooltipClasses, cheerfulFiestaPalette, cheerfulFiestaPaletteDark, cheerfulFiestaPaletteLight, continuousColorLegendClasses, cyanPalette, cyanPaletteDark, cyanPaletteLight, gaugeClasses, getPieCoordinates, getValueToPositionMapper, greenPalette, greenPaletteDark, greenPaletteLight, labelClasses, labelGradientClasses, labelMarkClasses, legendClasses, lineClasses, mangoFusionPalette, mangoFusionPaletteDark, mangoFusionPaletteLight, niceDomain, orangePalette, orangePaletteDark, orangePaletteLight, pieClasses, piecewiseColorDefaultLabelFormatter, piecewiseColorLegendClasses, pinkPalette, pinkPaletteDark, pinkPaletteLight, purplePalette, purplePaletteDark, purplePaletteLight, radarClasses, rainbowSurgePalette, rainbowSurgePaletteDark, rainbowSurgePaletteLight, redPalette, redPaletteDark, redPaletteLight, referenceLineClasses, scatterClasses, strawberrySkyPalette, strawberrySkyPaletteDark, strawberrySkyPaletteLight, tickFrequencies, useAnimate, useAnimateArea, useAnimateBar, useAnimateBarLabel, useAnimateLine, useAnimatePieArc, useAnimatePieArcLabel, useAxesTooltip, useBarSeries, useBarSeriesContext, useBrush, useChartApiContext, useChartCartesianAxis, useChartClosestPoint, useChartGradientId, useChartGradientIdObjectBound, useChartHighlight, useChartId, useChartInteraction, useChartPolarAxis, useChartRootRef, useChartTooltip, useChartVisibilityManager, useChartZAxis, useChartsLayerContainerRef, useChartsLocalization, useDataset, useDrawingArea, useFocusedItem, useGaugeState, useItemHighlightState, useItemHighlightStateGetter, useItemTooltip, useLegend, useLineSeries, useLineSeriesContext, usePieSeries, usePieSeriesContext, usePolarGeometry, useRadarAxis, useRadarItemTooltip, useRadarSeries, useRadarSeriesContext, useRadiusAxes, useRadiusAxis, useRadiusScale, useRotationAxes, useRotationAxis, useRotationScale, useScatterSeries, useScatterSeriesContext, useSeries, useXAxes, useXAxis, useXAxisCoordinates, useXAxisTicks, useXColorScale, useXScale, useYAxes, useYAxis, useYAxisCoordinates, useYAxisTicks, useYColorScale, useYScale, useZAxes, useZAxis, useZColorScale, yellowPalette, yellowPaletteDark, yellowPaletteLight };

//# sourceMappingURL=@mui_x-charts.js.map