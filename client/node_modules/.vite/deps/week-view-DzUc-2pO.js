import { r as __toESM, t as __commonJSMin } from "./chunk-B-1-B7_t.js";
import { t as require_react } from "./react.js";
import { t as require_jsx_runtime } from "./react_jsx-runtime.js";
import { $ as generateUtilityClass, C as grey, Ct as composeClasses, S as purple, _ as green, q as useEnhancedEffect, t as styled, wt as require_prop_types, x as red, y as blue } from "./styled-CNP9jUFH.js";
import { t as clsx } from "./clsx-DQoRUQAf.js";
import { t as _extends } from "./extends-DrH2PCIy.js";
import { T as formControlLabelClasses, f as useThemeProps } from "./styles-BeNPniuI.js";
import { t as generateUtilityClasses } from "./generateUtilityClasses-BLzP2xdp.js";
import { t as createSvgIcon } from "./createSvgIcon-CeHHFvOi.js";
import { _ as teal, d as dialogClasses, g as lime, h as amber, l as Dialog, m as deepOrange, o as DialogActions, t as DialogContent$1, v as indigo, y as pink } from "./DialogContent-BfDaayh8.js";
import { i as Select, t as TextField } from "./TextField-DHFrZoZR.js";
import { o as FormHelperText, s as FormControl, t as InputLabel } from "./InputLabel-DDHtePo5.js";
import { m as inputBaseClasses } from "./outlinedInputClasses-CKeynb4E.js";
import { t as Paper } from "./Paper-aYW8Dxq9.js";
import { t as IconButton } from "./IconButton-A5Pl-d0P.js";
import { t as Typography } from "./Typography-DYKLC8_d.js";
import { f as Switch, h as Skeleton, l as Tab, t as Tabs, v as FormControlLabel, x as ListSubheader, y as Divider } from "./Tabs-OtxUtgYr.js";
import { n as backdropClasses } from "./Backdrop-h-PH_3EJ.js";
import { t as Button } from "./Button-CH_Ul6xi.js";
import { t as MenuItem } from "./MenuItem-DH6O5v0p.js";
import { t as InputAdornment } from "./InputAdornment-ZQLOCY0t.js";
import { t as ListItemIcon } from "./ListItemIcon-DbXSSp_K.js";
import { t as ListItemText } from "./ListItemText-DrL2R2f5.js";
import { i as lruMemoize, n as require_shim, r as createSelectorCreator, t as require_with_selector } from "./with-selector-BYG_d6Q0.js";
import { t as _objectWithoutPropertiesLoose } from "./objectWithoutPropertiesLoose-BwcsosU7.js";
import { t as warnOnce } from "./warning-ci_nFDcC.js";
import { n as toPropertyKey, t as EventManager } from "./EventManager-B_87hSpQ.js";
import { i as buildFormatLongFn, n as buildMatchFn, r as buildLocalizeFn, t as buildMatchPatternFn } from "./buildMatchPatternFn-DdpNnqbc.js";
//#region node_modules/@base-ui/utils/store/createSelector.mjs
/**
* The NoOptionalParams type is a utility type that checks if a function has optional or default parameters.
* If the function has optional or default parameters, it returns a string literal type with an error message.
* Otherwise, it returns the original function type.
*
* This is used to enforce that the combiner function passed to createSelector does not have optional or default parameters,
* as memoization relies on the Function.length property, which does not account for optional or default parameters.
*/
/**
* Creates a selector function that can be used to derive values from the store's state.
*
* The combiner function can have up to three additional parameters, but it **cannot have optional or default parameters**.
*
* This function accepts up to six functions and combines them into a single selector function.
* The resulting selector will take the state from the combined selectors and any additional parameters required by the combiner.
*
* The return type of the resulting selector is determined by the return type of the combiner function.
*
* @example
* const selector = createSelector(
*  (state) => state.disabled
* );
*
* @example
* const selector = createSelector(
*   (state) => state.disabled,
*   (state) => state.open,
*   (disabled, open) => ({ disabled, open })
* );
*/
var createSelector = (a, b, c, d, e, f, ...other) => {
	if (other.length > 0) throw new Error("Unsupported number of selectors");
	let selector;
	if (a && b && c && d && e && f) selector = (state, a1, a2, a3) => {
		return f(a(state, a1, a2, a3), b(state, a1, a2, a3), c(state, a1, a2, a3), d(state, a1, a2, a3), e(state, a1, a2, a3), a1, a2, a3);
	};
	else if (a && b && c && d && e) selector = (state, a1, a2, a3) => {
		return e(a(state, a1, a2, a3), b(state, a1, a2, a3), c(state, a1, a2, a3), d(state, a1, a2, a3), a1, a2, a3);
	};
	else if (a && b && c && d) selector = (state, a1, a2, a3) => {
		return d(a(state, a1, a2, a3), b(state, a1, a2, a3), c(state, a1, a2, a3), a1, a2, a3);
	};
	else if (a && b && c) selector = (state, a1, a2, a3) => {
		return c(a(state, a1, a2, a3), b(state, a1, a2, a3), a1, a2, a3);
	};
	else if (a && b) selector = (state, a1, a2, a3) => {
		return b(a(state, a1, a2, a3), a1, a2, a3);
	};
	else if (a) selector = a;
	else throw new Error("Missing arguments");
	return selector;
};
//#endregion
//#region node_modules/@base-ui/utils/store/createSelectorMemoized.mjs
var reselectCreateSelector = createSelectorCreator({
	memoize: lruMemoize,
	memoizeOptions: {
		maxSize: 1,
		equalityCheck: Object.is
	}
});
var createSelectorMemoized = (...selectors) => {
	const cache = /* @__PURE__ */ new WeakMap();
	let nextCacheId = 1;
	const combiner = selectors[selectors.length - 1];
	const nSelectors = selectors.length - 1 || 1;
	const argsLength = combiner.length - nSelectors;
	if (argsLength > 3) throw new Error("Unsupported number of arguments");
	const selector = (state, a1, a2, a3) => {
		let cacheKey = state.__cacheKey__;
		if (!cacheKey) {
			cacheKey = { id: nextCacheId };
			state.__cacheKey__ = cacheKey;
			nextCacheId += 1;
		}
		let fn = cache.get(cacheKey);
		if (!fn) {
			let reselectArgs = selectors;
			const selectorArgs = [
				void 0,
				void 0,
				void 0
			];
			switch (argsLength) {
				case 0: break;
				case 1:
					reselectArgs = [
						...selectors.slice(0, -1),
						() => selectorArgs[0],
						combiner
					];
					break;
				case 2:
					reselectArgs = [
						...selectors.slice(0, -1),
						() => selectorArgs[0],
						() => selectorArgs[1],
						combiner
					];
					break;
				case 3:
					reselectArgs = [
						...selectors.slice(0, -1),
						() => selectorArgs[0],
						() => selectorArgs[1],
						() => selectorArgs[2],
						combiner
					];
					break;
				default: throw new Error("Unsupported number of arguments");
			}
			fn = reselectCreateSelector(...reselectArgs);
			fn.selectorArgs = selectorArgs;
			cache.set(cacheKey, fn);
		}
		fn.selectorArgs[0] = a1;
		fn.selectorArgs[1] = a2;
		fn.selectorArgs[2] = a3;
		switch (argsLength) {
			case 0: return fn(state);
			case 1: return fn(state, a1);
			case 2: return fn(state, a1, a2);
			case 3: return fn(state, a1, a2, a3);
			default: throw new Error("unreachable");
		}
	};
	return selector;
};
//#endregion
//#region node_modules/@base-ui/utils/reactVersion.mjs
var import_shim = require_shim();
var import_with_selector = require_with_selector();
var import_react = /* @__PURE__ */ __toESM(require_react(), 1);
var majorVersion = parseInt("19.2.7", 10);
function isReactVersionAtLeast(reactVersionToCheck) {
	return majorVersion >= reactVersionToCheck;
}
//#endregion
//#region node_modules/@base-ui/utils/useRefWithInit.mjs
var UNINITIALIZED$1 = {};
/**
* A React.useRef() that is initialized with a function. Note that it accepts an optional
* initialization argument, so the initialization function doesn't need to be an inline closure.
*
* @usage
*   const ref = useRefWithInit(sortColumns, columns)
*/
function useRefWithInit(init, initArg) {
	const ref = import_react.useRef(UNINITIALIZED$1);
	if (ref.current === UNINITIALIZED$1) ref.current = init(initArg);
	return ref;
}
//#endregion
//#region node_modules/@base-ui/utils/fastHooks.mjs
var hooks = [];
var currentInstance = void 0;
function getInstance() {
	return currentInstance;
}
function register$1(hook) {
	hooks.push(hook);
}
//#endregion
//#region node_modules/@base-ui/utils/store/useStore.mjs
var useStoreImplementation = isReactVersionAtLeast(19) ? useStoreFast : useStoreLegacy;
function useStore(store, selector, a1, a2, a3) {
	return useStoreImplementation(store, selector, a1, a2, a3);
}
function useStoreR19(store, selector, a1, a2, a3) {
	const getSelection = import_react.useCallback(() => selector(store.getSnapshot(), a1, a2, a3), [
		store,
		selector,
		a1,
		a2,
		a3
	]);
	return (0, import_shim.useSyncExternalStore)(store.subscribe, getSelection, getSelection);
}
register$1({
	before(instance) {
		instance.syncIndex = 0;
		if (!instance.didInitialize) {
			instance.syncTick = 1;
			instance.syncHooks = [];
			instance.didChangeStore = true;
			instance.getSnapshot = () => {
				let didChange = false;
				for (let i = 0; i < instance.syncHooks.length; i += 1) {
					const hook = instance.syncHooks[i];
					const value = hook.selector(hook.store.state, hook.a1, hook.a2, hook.a3);
					if (!Object.is(hook.value, value)) {
						didChange = true;
						hook.value = value;
					}
				}
				if (didChange) instance.syncTick += 1;
				return instance.syncTick;
			};
		}
	},
	after(instance) {
		if (instance.syncHooks.length > 0) {
			if (instance.didChangeStore) {
				instance.didChangeStore = false;
				instance.subscribe = (onStoreChange) => {
					const stores = /* @__PURE__ */ new Set();
					for (const hook of instance.syncHooks) stores.add(hook.store);
					const unsubscribes = [];
					for (const store of stores) unsubscribes.push(store.subscribe(onStoreChange));
					return () => {
						for (const unsubscribe of unsubscribes) unsubscribe();
					};
				};
			}
			(0, import_shim.useSyncExternalStore)(instance.subscribe, instance.getSnapshot, instance.getSnapshot);
		}
	}
});
function useStoreFast(store, selector, a1, a2, a3) {
	const instance = getInstance();
	if (!instance) return useStoreR19(store, selector, a1, a2, a3);
	const index = instance.syncIndex;
	instance.syncIndex += 1;
	let hook;
	if (!instance.didInitialize) {
		hook = {
			store,
			selector,
			a1,
			a2,
			a3,
			value: selector(store.getSnapshot(), a1, a2, a3)
		};
		instance.syncHooks.push(hook);
	} else {
		hook = instance.syncHooks[index];
		if (hook.store !== store || hook.selector !== selector || !Object.is(hook.a1, a1) || !Object.is(hook.a2, a2) || !Object.is(hook.a3, a3)) {
			if (hook.store !== store) instance.didChangeStore = true;
			hook.store = store;
			hook.selector = selector;
			hook.a1 = a1;
			hook.a2 = a2;
			hook.a3 = a3;
			hook.value = selector(store.getSnapshot(), a1, a2, a3);
		}
	}
	return hook.value;
}
function useStoreLegacy(store, selector, a1, a2, a3) {
	return (0, import_with_selector.useSyncExternalStoreWithSelector)(store.subscribe, store.getSnapshot, store.getSnapshot, (state) => selector(state, a1, a2, a3));
}
//#endregion
//#region node_modules/@base-ui/utils/store/Store.mjs
/**
* A data store implementation that allows subscribing to state changes and updating the state.
* It uses an observer pattern to notify subscribers when the state changes.
*/
var Store = class {
	/**
	* The current state of the store.
	* This property is updated immediately when the state changes as a result of calling {@link setState}, {@link update}, or {@link set}.
	* To subscribe to state changes, use the {@link useState} method. The value returned by {@link useState} is updated after the component renders (similarly to React's useState).
	* The values can be used directly (to avoid subscribing to the store) in effects or event handlers.
	*
	* Do not modify properties in state directly. Instead, use the provided methods to ensure proper state management and listener notification.
	*/
	constructor(state) {
		this.state = state;
		this.listeners = /* @__PURE__ */ new Set();
		this.updateTick = 0;
	}
	/**
	* Registers a listener that will be called whenever the store's state changes.
	*
	* @param fn The listener function to be called on state changes.
	* @returns A function to unsubscribe the listener.
	*/
	subscribe = (fn) => {
		this.listeners.add(fn);
		return () => {
			this.listeners.delete(fn);
		};
	};
	/**
	* Returns the current state of the store.
	*/
	getSnapshot = () => {
		return this.state;
	};
	/**
	* Updates the entire store's state and notifies all registered listeners.
	*
	* @param newState The new state to set for the store.
	*/
	setState(newState) {
		if (this.state === newState) return;
		this.state = newState;
		this.updateTick += 1;
		const currentTick = this.updateTick;
		for (const listener of this.listeners) {
			if (currentTick !== this.updateTick) return;
			listener(newState);
		}
	}
	/**
	* Merges the provided changes into the current state and notifies listeners if there are changes.
	*
	* @param changes An object containing the changes to apply to the current state.
	*/
	update(changes) {
		for (const key in changes) if (!Object.is(this.state[key], changes[key])) {
			this.setState({
				...this.state,
				...changes
			});
			return;
		}
	}
	/**
	* Sets a specific key in the store's state to a new value and notifies listeners if the value has changed.
	*
	* @param key The key in the store's state to update.
	* @param value The new value to set for the specified key.
	*/
	set(key, value) {
		if (!Object.is(this.state[key], value)) this.setState({
			...this.state,
			[key]: value
		});
	}
	/**
	* Gives the state a new reference and updates all registered listeners.
	*/
	notifyAll() {
		const newState = { ...this.state };
		this.setState(newState);
	}
	use(selector, a1, a2, a3) {
		return useStore(this, selector, a1, a2, a3);
	}
};
//#endregion
//#region node_modules/@base-ui/utils/safeReact.mjs
var import_jsx_runtime = require_jsx_runtime();
/**
* A clone of the React namespace for reading APIs that may be missing in older
* supported React versions. Bundlers can rewrite direct `React.someNewApi`
* reads into named imports, which breaks React 17. Reading from this cloned
* object keeps those lookups optional.
*
* @see https://github.com/mui/material-ui/issues/41190#issuecomment-2040873379
*/
var SafeReact = { ...import_react };
//#endregion
//#region node_modules/@base-ui/utils/useStableCallback.mjs
var useInsertionEffect = SafeReact.useInsertionEffect;
var useSafeInsertionEffect = useInsertionEffect && useInsertionEffect !== SafeReact.useLayoutEffect ? useInsertionEffect : (fn) => fn();
/**
* Stabilizes the function passed so it's always the same between renders.
*
* The function becomes non-reactive to any values it captures.
* It can safely be passed as a dependency of `React.useMemo` and `React.useEffect` without re-triggering them if its captured values change.
*
* The function must only be called inside effects and event handlers, never during render (which throws an error).
*
* This hook is a more permissive version of React 19.2's `React.useEffectEvent` in that it can be passed through contexts and called in event handler props, not just effects.
*/
function useStableCallback(callback) {
	const stable = useRefWithInit(createStableCallback).current;
	stable.next = callback;
	useSafeInsertionEffect(stable.effect);
	return stable.trampoline;
}
function createStableCallback() {
	const stable = {
		next: void 0,
		callback: assertNotCalled,
		trampoline: (...args) => stable.callback?.(...args),
		effect: () => {
			stable.callback = stable.next;
		}
	};
	return stable;
}
function assertNotCalled() {
	throw new Error("Base UI: Cannot call an event handler while rendering.");
}
//#endregion
//#region node_modules/@base-ui/utils/useIsoLayoutEffect.mjs
var noop$1 = () => {};
var useIsoLayoutEffect = typeof document !== "undefined" ? import_react.useLayoutEffect : noop$1;
//#endregion
//#region node_modules/@base-ui/utils/empty.mjs
var EMPTY_ARRAY$1 = Object.freeze([]);
var EMPTY_OBJECT = Object.freeze({});
//#endregion
//#region node_modules/@floating-ui/utils/dist/floating-ui.utils.dom.mjs
function hasWindow() {
	return typeof window !== "undefined";
}
function getWindow(node) {
	var _node$ownerDocument;
	return (node == null || (_node$ownerDocument = node.ownerDocument) == null ? void 0 : _node$ownerDocument.defaultView) || window;
}
function isHTMLElement(value) {
	if (!hasWindow()) return false;
	return value instanceof HTMLElement || value instanceof getWindow(value).HTMLElement;
}
function getComputedStyle$1(element) {
	return getWindow(element).getComputedStyle(element);
}
//#endregion
//#region node_modules/@base-ui/utils/useOnMount.mjs
var EMPTY = [];
/**
* A React.useEffect equivalent that runs once, when the component is mounted.
*/
function useOnMount(fn) {
	import_react.useEffect(fn, EMPTY);
}
//#endregion
//#region node_modules/@base-ui/utils/useMergedRefs.mjs
/**
* Merges refs into a single memoized callback ref or `null`.
* This makes sure multiple refs are updated together and have the same value.
*
* This function accepts up to four refs. If you need to merge more, or have an unspecified number of refs to merge,
* use `useMergedRefsN` instead.
*/
function useMergedRefs(a, b, c, d) {
	const forkRef = useRefWithInit(createForkRef).current;
	if (didChange(forkRef, a, b, c, d)) update(forkRef, [
		a,
		b,
		c,
		d
	]);
	return forkRef.callback;
}
/**
* Merges an array of refs into a single memoized callback ref or `null`.
*
* If you need to merge a fixed number (up to four) of refs, use `useMergedRefs` instead for better performance.
*/
function useMergedRefsN(refs) {
	const forkRef = useRefWithInit(createForkRef).current;
	if (didChangeN(forkRef, refs)) update(forkRef, refs);
	return forkRef.callback;
}
function createForkRef() {
	return {
		callback: null,
		cleanup: null,
		refs: []
	};
}
function didChange(forkRef, a, b, c, d) {
	return forkRef.refs[0] !== a || forkRef.refs[1] !== b || forkRef.refs[2] !== c || forkRef.refs[3] !== d;
}
function didChangeN(forkRef, newRefs) {
	return forkRef.refs.length !== newRefs.length || forkRef.refs.some((ref, index) => ref !== newRefs[index]);
}
function update(forkRef, refs) {
	forkRef.refs = refs;
	if (refs.every((ref) => ref == null)) {
		forkRef.callback = null;
		return;
	}
	forkRef.callback = (instance) => {
		if (forkRef.cleanup) {
			forkRef.cleanup();
			forkRef.cleanup = null;
		}
		if (instance != null) {
			const cleanupCallbacks = Array(refs.length).fill(null);
			for (let i = 0; i < refs.length; i += 1) {
				const ref = refs[i];
				if (ref == null) continue;
				switch (typeof ref) {
					case "function": {
						const refCleanup = ref(instance);
						if (typeof refCleanup === "function") cleanupCallbacks[i] = refCleanup;
						break;
					}
					case "object":
						ref.current = instance;
						break;
					default:
				}
			}
			forkRef.cleanup = () => {
				for (let i = 0; i < refs.length; i += 1) {
					const ref = refs[i];
					if (ref == null) continue;
					switch (typeof ref) {
						case "function": {
							const cleanupCallback = cleanupCallbacks[i];
							if (typeof cleanupCallback === "function") cleanupCallback();
							else ref(null);
							break;
						}
						case "object":
							ref.current = null;
							break;
						default:
					}
				}
			};
		}
	};
}
//#endregion
//#region node_modules/date-fns/constants.js
/**
* @constant
* @name daysInYear
* @summary Days in 1 year.
*
* @description
* How many days in a year.
*
* One years equals 365.2425 days according to the formula:
*
* > Leap year occurs every 4 years, except for years that are divisible by 100 and not divisible by 400.
* > 1 mean year = (365+1/4-1/100+1/400) days = 365.2425 days
*/
var daysInYear = 365.2425;
-(Math.pow(10, 8) * 24 * 60 * 60 * 1e3);
/**
* @constant
* @name millisecondsInWeek
* @summary Milliseconds in 1 week.
*/
var millisecondsInWeek = 6048e5;
/**
* @constant
* @name millisecondsInDay
* @summary Milliseconds in 1 day.
*/
var millisecondsInDay = 864e5;
/**
* @constant
* @name millisecondsInMinute
* @summary Milliseconds in 1 minute
*/
var millisecondsInMinute = 6e4;
/**
* @constant
* @name millisecondsInHour
* @summary Milliseconds in 1 hour
*/
var millisecondsInHour = 36e5;
/**
* @constant
* @name millisecondsInSecond
* @summary Milliseconds in 1 second
*/
var millisecondsInSecond = 1e3;
/**
* @constant
* @name secondsInDay
* @summary Seconds in 1 day.
*/
var secondsInDay = 3600 * 24;
secondsInDay * 7;
secondsInDay * daysInYear / 12 * 3;
/**
* @constant
* @name constructFromSymbol
* @summary Symbol enabling Date extensions to inherit properties from the reference date.
*
* The symbol is used to enable the `constructFrom` function to construct a date
* using a reference date and a value. It allows to transfer extra properties
* from the reference date to the new date. It's useful for extensions like
* [`TZDate`](https://github.com/date-fns/tz) that accept a time zone as
* a constructor argument.
*/
var constructFromSymbol = Symbol.for("constructDateFrom");
//#endregion
//#region node_modules/date-fns/constructFrom.js
/**
* @name constructFrom
* @category Generic Helpers
* @summary Constructs a date using the reference date and the value
*
* @description
* The function constructs a new date using the constructor from the reference
* date and the given value. It helps to build generic functions that accept
* date extensions.
*
* It defaults to `Date` if the passed reference date is a number or a string.
*
* Starting from v3.7.0, it allows to construct a date using `[Symbol.for("constructDateFrom")]`
* enabling to transfer extra properties from the reference date to the new date.
* It's useful for extensions like [`TZDate`](https://github.com/date-fns/tz)
* that accept a time zone as a constructor argument.
*
* @typeParam DateType - The `Date` type, the function operates on. Gets inferred from passed arguments. Allows to use extensions like [`UTCDate`](https://github.com/date-fns/utc).
*
* @param date - The reference date to take constructor from
* @param value - The value to create the date
*
* @returns Date initialized using the given date and value
*
* @example
* import { constructFrom } from "./constructFrom/date-fns";
*
* // A function that clones a date preserving the original type
* function cloneDate<DateType extends Date>(date: DateType): DateType {
*   return constructFrom(
*     date, // Use constructor from the given date
*     date.getTime() // Use the date value to create a new date
*   );
* }
*/
function constructFrom(date, value) {
	if (typeof date === "function") return date(value);
	if (date && typeof date === "object" && constructFromSymbol in date) return date[constructFromSymbol](value);
	if (date instanceof Date) return new date.constructor(value);
	return new Date(value);
}
//#endregion
//#region node_modules/date-fns/toDate.js
/**
* @name toDate
* @category Common Helpers
* @summary Convert the given argument to an instance of Date.
*
* @description
* Convert the given argument to an instance of Date.
*
* If the argument is an instance of Date, the function returns its clone.
*
* If the argument is a number, it is treated as a timestamp.
*
* If the argument is none of the above, the function returns Invalid Date.
*
* Starting from v3.7.0, it clones a date using `[Symbol.for("constructDateFrom")]`
* enabling to transfer extra properties from the reference date to the new date.
* It's useful for extensions like [`TZDate`](https://github.com/date-fns/tz)
* that accept a time zone as a constructor argument.
*
* **Note**: *all* Date arguments passed to any *date-fns* function is processed by `toDate`.
*
* @typeParam DateType - The `Date` type, the function operates on. Gets inferred from passed arguments. Allows to use extensions like [`UTCDate`](https://github.com/date-fns/utc).
* @typeParam ResultDate - The result `Date` type, it is the type returned from the context function if it is passed, or inferred from the arguments.
*
* @param argument - The value to convert
*
* @returns The parsed date in the local time zone
*
* @example
* // Clone the date:
* const result = toDate(new Date(2014, 1, 11, 11, 30, 30))
* //=> Tue Feb 11 2014 11:30:30
*
* @example
* // Convert the timestamp to date:
* const result = toDate(1392098430000)
* //=> Tue Feb 11 2014 11:30:30
*/
function toDate(argument, context) {
	return constructFrom(context || argument, argument);
}
//#endregion
//#region node_modules/date-fns/addDays.js
/**
* The {@link addDays} function options.
*/
/**
* @name addDays
* @category Day Helpers
* @summary Add the specified number of days to the given date.
*
* @description
* Add the specified number of days to the given date.
*
* **You don't need date-fns\***:
*
* Temporal has a built-in `add` method on all its classes:
*
* - [`Temporal.Instant.prototype.add()`](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Temporal/Instant/add)
* - [`Temporal.PlainDate.prototype.add()`](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Temporal/PlainDate/add)
* - [`Temporal.PlainDateTime.prototype.add()`](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Temporal/PlainDateTime/add)
* - [`Temporal.PlainTime.prototype.add()`](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Temporal/PlainTime/add)
* - [`Temporal.PlainYearMonth.prototype.add()`](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Temporal/PlainYearMonth/add)
* - [`Temporal.ZonedDateTime.prototype.add()`](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Temporal/ZonedDateTime/add)
*
* \* **Not really**, see: https://date-fns.org/you-dont-need-date-fns
*
* @typeParam DateType - The `Date` type, the function operates on. Gets inferred from passed arguments. Allows to use extensions like [`UTCDate`](https://github.com/date-fns/utc).
* @typeParam ResultDate - The result `Date` type, it is the type returned from the context function if it is passed, or inferred from the arguments.
*
* @param date - The date to be changed
* @param amount - The amount of days to be added.
* @param options - An object with options
*
* @returns The new date with the days added
*
* @example
* // Add 10 days to 1 September 2014:
* const result = addDays(new Date(2014, 8, 1), 10)
* //=> Thu Sep 11 2014 00:00:00
*
* @example
* // Using Temporal:
* // Add 10 days to 1 September 2014:
* Temporal.PlainDate.from("2014-09-01").add({ days: 10 }).toString();
* //=> "2014-09-11"
*/
function addDays(date, amount, options) {
	const _date = toDate(date, options?.in);
	if (isNaN(amount)) return constructFrom(options?.in || date, NaN);
	if (!amount) return _date;
	_date.setDate(_date.getDate() + amount);
	return _date;
}
//#endregion
//#region node_modules/date-fns/addMilliseconds.js
/**
* The {@link addMilliseconds} function options.
*/
/**
* @name addMilliseconds
* @category Millisecond Helpers
* @summary Add the specified number of milliseconds to the given date.
*
* @description
* Add the specified number of milliseconds to the given date.
*
* @typeParam DateType - The `Date` type, the function operates on. Gets inferred from passed arguments. Allows to use extensions like [`UTCDate`](https://github.com/date-fns/utc).
* @typeParam ResultDate - The result `Date` type, it is the type returned from the context function if it is passed, or inferred from the arguments.
*
* @param date - The date to be changed
* @param amount - The amount of milliseconds to be added.
* @param options - The options object
*
* @returns The new date with the milliseconds added
*
* @example
* // Add 750 milliseconds to 10 July 2014 12:45:30.000:
* const result = addMilliseconds(new Date(2014, 6, 10, 12, 45, 30, 0), 750)
* //=> Thu Jul 10 2014 12:45:30.750
*/
function addMilliseconds(date, amount, options) {
	return constructFrom(options?.in || date, +toDate(date) + amount);
}
//#endregion
//#region node_modules/date-fns/addHours.js
/**
* The {@link addHours} function options.
*/
/**
* @name addHours
* @category Hour Helpers
* @summary Add the specified number of hours to the given date.
*
* @description
* Add the specified number of hours to the given date.
*
* @typeParam DateType - The `Date` type, the function operates on. Gets inferred from passed arguments. Allows to use extensions like [`UTCDate`](https://github.com/date-fns/utc).
* @typeParam ResultDate - The result `Date` type, it is the type returned from the context function if it is passed, or inferred from the arguments.
*
* @param date - The date to be changed
* @param amount - The amount of hours to be added
* @param options - An object with options
*
* @returns The new date with the hours added
*
* @example
* // Add 2 hours to 10 July 2014 23:00:00:
* const result = addHours(new Date(2014, 6, 10, 23, 0), 2)
* //=> Fri Jul 11 2014 01:00:00
*/
function addHours(date, amount, options) {
	return addMilliseconds(date, amount * millisecondsInHour, options);
}
//#endregion
//#region node_modules/date-fns/addMinutes.js
/**
* The {@link addMinutes} function options.
*/
/**
* @name addMinutes
* @category Minute Helpers
* @summary Add the specified number of minutes to the given date.
*
* @description
* Add the specified number of minutes to the given date.
*
* @typeParam DateType - The `Date` type, the function operates on. Gets inferred from passed arguments. Allows to use extensions like [`UTCDate`](https://github.com/date-fns/utc).
* @typeParam ResultDate - The result `Date` type, it is the type returned from the context function if it is passed, or inferred from the arguments.
*
* @param date - The date to be changed
* @param amount - The amount of minutes to be added.
* @param options - An object with options
*
* @returns The new date with the minutes added
*
* @example
* // Add 30 minutes to 10 July 2014 12:00:00:
* const result = addMinutes(new Date(2014, 6, 10, 12, 0), 30)
* //=> Thu Jul 10 2014 12:30:00
*/
function addMinutes(date, amount, options) {
	const _date = toDate(date, options?.in);
	_date.setTime(_date.getTime() + amount * millisecondsInMinute);
	return _date;
}
//#endregion
//#region node_modules/date-fns/addMonths.js
/**
* The {@link addMonths} function options.
*/
/**
* @name addMonths
* @category Month Helpers
* @summary Add the specified number of months to the given date.
*
* @description
* Add the specified number of months to the given date.
*
* @typeParam DateType - The `Date` type, the function operates on. Gets inferred from passed arguments. Allows to use extensions like [`UTCDate`](https://github.com/date-fns/utc).
* @typeParam ResultDate - The result `Date` type, it is the type returned from the context function if it is passed, or inferred from the arguments.
*
* @param date - The date to be changed
* @param amount - The amount of months to be added.
* @param options - The options object
*
* @returns The new date with the months added
*
* @example
* // Add 5 months to 1 September 2014:
* const result = addMonths(new Date(2014, 8, 1), 5)
* //=> Sun Feb 01 2015 00:00:00
*
* // Add one month to 30 January 2023:
* const result = addMonths(new Date(2023, 0, 30), 1)
* //=> Tue Feb 28 2023 00:00:00
*/
function addMonths(date, amount, options) {
	const _date = toDate(date, options?.in);
	if (isNaN(amount)) return constructFrom(options?.in || date, NaN);
	if (!amount) return _date;
	const dayOfMonth = _date.getDate();
	const endOfDesiredMonth = constructFrom(options?.in || date, _date.getTime());
	endOfDesiredMonth.setMonth(_date.getMonth() + amount + 1, 0);
	if (dayOfMonth >= endOfDesiredMonth.getDate()) return endOfDesiredMonth;
	else {
		_date.setFullYear(endOfDesiredMonth.getFullYear(), endOfDesiredMonth.getMonth(), dayOfMonth);
		return _date;
	}
}
//#endregion
//#region node_modules/date-fns/addSeconds.js
/**
* The {@link addSeconds} function options.
*/
/**
* @name addSeconds
* @category Second Helpers
* @summary Add the specified number of seconds to the given date.
*
* @description
* Add the specified number of seconds to the given date.
*
* @typeParam DateType - The `Date` type, the function operates on. Gets inferred from passed arguments. Allows to use extensions like [`UTCDate`](https://github.com/date-fns/utc).
* @typeParam ResultDate - The result `Date` type, it is the type returned from the context function if it is passed, or inferred from the arguments.
*
* @param date - The date to be changed
* @param amount - The amount of seconds to be added.
* @param options - An object with options
*
* @returns The new date with the seconds added
*
* @example
* // Add 30 seconds to 10 July 2014 12:45:00:
* const result = addSeconds(new Date(2014, 6, 10, 12, 45, 0), 30)
* //=> Thu Jul 10 2014 12:45:30
*/
function addSeconds(date, amount, options) {
	return addMilliseconds(date, amount * 1e3, options);
}
//#endregion
//#region node_modules/date-fns/addWeeks.js
/**
* The {@link addWeeks} function options.
*/
/**
* @name addWeeks
* @category Week Helpers
* @summary Add the specified number of weeks to the given date.
*
* @description
* Add the specified number of weeks to the given date.
*
* @typeParam DateType - The `Date` type, the function operates on. Gets inferred from passed arguments. Allows to use extensions like [`UTCDate`](https://github.com/date-fns/utc).
* @typeParam ResultDate - The result `Date` type, it is the type returned from the context function if it is passed, or inferred from the arguments.
*
* @param date - The date to be changed
* @param amount - The amount of weeks to be added.
* @param options - An object with options
*
* @returns The new date with the weeks added
*
* @example
* // Add 4 weeks to 1 September 2014:
* const result = addWeeks(new Date(2014, 8, 1), 4)
* //=> Mon Sep 29 2014 00:00:00
*/
function addWeeks(date, amount, options) {
	return addDays(date, amount * 7, options);
}
//#endregion
//#region node_modules/date-fns/addYears.js
/**
* The {@link addYears} function options.
*/
/**
* @name addYears
* @category Year Helpers
* @summary Add the specified number of years to the given date.
*
* @description
* Add the specified number of years to the given date.
*
* @typeParam DateType - The `Date` type, the function operates on. Gets inferred from passed arguments. Allows to use extensions like [`UTCDate`](https://github.com/date-fns/utc).
* @typeParam ResultDate - The result `Date` type.
*
* @param date - The date to be changed
* @param amount - The amount of years to be added.
* @param options - The options
*
* @returns The new date with the years added
*
* @example
* // Add 5 years to 1 September 2014:
* const result = addYears(new Date(2014, 8, 1), 5)
* //=> Sun Sep 01 2019 00:00:00
*/
function addYears(date, amount, options) {
	return addMonths(date, amount * 12, options);
}
//#endregion
//#region node_modules/date-fns/_lib/normalizeDates.js
function normalizeDates(context, ...dates) {
	const normalize = constructFrom.bind(null, context || dates.find((date) => typeof date === "object"));
	return dates.map(normalize);
}
//#endregion
//#region node_modules/date-fns/_lib/getTimezoneOffsetInMilliseconds.js
/**
* Google Chrome as of 67.0.3396.87 introduced timezones with offset that includes seconds.
* They usually appear for dates that denote time before the timezones were introduced
* (e.g. for 'Europe/Prague' timezone the offset is GMT+00:57:44 before 1 October 1891
* and GMT+01:00:00 after that date)
*
* Date#getTimezoneOffset returns the offset in minutes and would return 57 for the example above,
* which would lead to incorrect calculations.
*
* This function returns the timezone offset in milliseconds that takes seconds in account.
*/
function getTimezoneOffsetInMilliseconds(date) {
	const _date = toDate(date);
	const utcDate = new Date(Date.UTC(_date.getFullYear(), _date.getMonth(), _date.getDate(), _date.getHours(), _date.getMinutes(), _date.getSeconds(), _date.getMilliseconds()));
	utcDate.setUTCFullYear(_date.getFullYear());
	return +date - +utcDate;
}
//#endregion
//#region node_modules/date-fns/startOfDay.js
/**
* The {@link startOfDay} function options.
*/
/**
* @name startOfDay
* @category Day Helpers
* @summary Return the start of a day for the given date.
*
* @description
* Return the start of a day for the given date.
* The result will be in the local timezone.
*
* @typeParam DateType - The `Date` type, the function operates on. Gets inferred from passed arguments. Allows to use extensions like [`UTCDate`](https://github.com/date-fns/utc).
* @typeParam ResultDate - The result `Date` type, it is the type returned from the context function if it is passed, or inferred from the arguments.
*
* @param date - The original date
* @param options - The options
*
* @returns The start of a day
*
* @example
* // The start of a day for 2 September 2014 11:55:00:
* const result = startOfDay(new Date(2014, 8, 2, 11, 55, 0))
* //=> Tue Sep 02 2014 00:00:00
*/
function startOfDay(date, options) {
	const _date = toDate(date, options?.in);
	_date.setHours(0, 0, 0, 0);
	return _date;
}
//#endregion
//#region node_modules/date-fns/differenceInCalendarDays.js
/**
* The {@link differenceInCalendarDays} function options.
*/
/**
* @name differenceInCalendarDays
* @category Day Helpers
* @summary Get the number of calendar days between the given dates.
*
* @description
* Get the number of calendar days between the given dates. This means that the times are removed
* from the dates and then the difference in days is calculated.
*
* @param laterDate - The later date
* @param earlierDate - The earlier date
* @param options - The options object
*
* @returns The number of calendar days
*
* @example
* // How many calendar days are between
* // 2 July 2011 23:00:00 and 2 July 2012 00:00:00?
* const result = differenceInCalendarDays(
*   new Date(2012, 6, 2, 0, 0),
*   new Date(2011, 6, 2, 23, 0)
* )
* //=> 366
* // How many calendar days are between
* // 2 July 2011 23:59:00 and 3 July 2011 00:01:00?
* const result = differenceInCalendarDays(
*   new Date(2011, 6, 3, 0, 1),
*   new Date(2011, 6, 2, 23, 59)
* )
* //=> 1
*/
function differenceInCalendarDays(laterDate, earlierDate, options) {
	const [laterDate_, earlierDate_] = normalizeDates(options?.in, laterDate, earlierDate);
	const laterStartOfDay = startOfDay(laterDate_);
	const earlierStartOfDay = startOfDay(earlierDate_);
	const laterTimestamp = +laterStartOfDay - getTimezoneOffsetInMilliseconds(laterStartOfDay);
	const earlierTimestamp = +earlierStartOfDay - getTimezoneOffsetInMilliseconds(earlierStartOfDay);
	return Math.round((laterTimestamp - earlierTimestamp) / millisecondsInDay);
}
//#endregion
//#region node_modules/date-fns/differenceInDays.js
/**
* The {@link differenceInDays} function options.
*/
/**
* @name differenceInDays
* @category Day Helpers
* @summary Get the number of full days between the given dates.
*
* @description
* Get the number of full day periods between two dates. Fractional days are
* truncated towards zero.
*
* One "full day" is the distance between a local time in one day to the same
* local time on the next or previous day. A full day can sometimes be less than
* or more than 24 hours if a daylight savings change happens between two dates.
*
* To ignore DST and only measure exact 24-hour periods, use this instead:
* `Math.trunc(differenceInHours(dateLeft, dateRight)/24)|0`.
*
* @param laterDate - The later date
* @param earlierDate - The earlier date
* @param options - An object with options
*
* @returns The number of full days according to the local timezone
*
* @example
* // How many full days are between
* // 2 July 2011 23:00:00 and 2 July 2012 00:00:00?
* const result = differenceInDays(
*   new Date(2012, 6, 2, 0, 0),
*   new Date(2011, 6, 2, 23, 0)
* )
* //=> 365
*
* @example
* // How many full days are between
* // 2 July 2011 23:59:00 and 3 July 2011 00:01:00?
* const result = differenceInDays(
*   new Date(2011, 6, 3, 0, 1),
*   new Date(2011, 6, 2, 23, 59)
* )
* //=> 0
*
* @example
* // How many full days are between
* // 1 March 2020 0:00 and 1 June 2020 0:00 ?
* // Note: because local time is used, the
* // result will always be 92 days, even in
* // time zones where DST starts and the
* // period has only 92*24-1 hours.
* const result = differenceInDays(
*   new Date(2020, 5, 1),
*   new Date(2020, 2, 1)
* )
* //=> 92
*/
function differenceInDays(laterDate, earlierDate, options) {
	const [laterDate_, earlierDate_] = normalizeDates(options?.in, laterDate, earlierDate);
	const sign = compareLocalAsc(laterDate_, earlierDate_);
	const difference = Math.abs(differenceInCalendarDays(laterDate_, earlierDate_));
	laterDate_.setDate(laterDate_.getDate() - sign * difference);
	const result = sign * (difference - Number(compareLocalAsc(laterDate_, earlierDate_) === -sign));
	return result === 0 ? 0 : result;
}
function compareLocalAsc(laterDate, earlierDate) {
	const diff = laterDate.getFullYear() - earlierDate.getFullYear() || laterDate.getMonth() - earlierDate.getMonth() || laterDate.getDate() - earlierDate.getDate() || laterDate.getHours() - earlierDate.getHours() || laterDate.getMinutes() - earlierDate.getMinutes() || laterDate.getSeconds() - earlierDate.getSeconds() || laterDate.getMilliseconds() - earlierDate.getMilliseconds();
	if (diff < 0) return -1;
	if (diff > 0) return 1;
	return diff;
}
//#endregion
//#region node_modules/date-fns/_lib/getRoundingMethod.js
function getRoundingMethod(method) {
	return (number) => {
		const result = (method ? Math[method] : Math.trunc)(number);
		return result === 0 ? 0 : result;
	};
}
//#endregion
//#region node_modules/date-fns/differenceInHours.js
/**
* The {@link differenceInHours} function options.
*/
/**
* @name differenceInHours
* @category Hour Helpers
* @summary Get the number of hours between the given dates.
*
* @description
* Get the number of hours between the given dates.
*
* @param laterDate - The later date
* @param earlierDate - The earlier date
* @param options - An object with options.
*
* @returns The number of hours
*
* @example
* // How many hours are between 2 July 2014 06:50:00 and 2 July 2014 19:00:00?
* const result = differenceInHours(
*   new Date(2014, 6, 2, 19, 0),
*   new Date(2014, 6, 2, 6, 50)
* )
* //=> 12
*/
function differenceInHours(laterDate, earlierDate, options) {
	const [laterDate_, earlierDate_] = normalizeDates(options?.in, laterDate, earlierDate);
	const diff = (+laterDate_ - +earlierDate_) / millisecondsInHour;
	return getRoundingMethod(options?.roundingMethod)(diff);
}
//#endregion
//#region node_modules/date-fns/differenceInMilliseconds.js
/**
* @name differenceInMilliseconds
* @category Millisecond Helpers
* @summary Get the number of milliseconds between the given dates.
*
* @description
* Get the number of milliseconds between the given dates.
*
* @param laterDate - The later date
* @param earlierDate - The earlier date
*
* @returns The number of milliseconds
*
* @example
* // How many milliseconds are between
* // 2 July 2014 12:30:20.600 and 2 July 2014 12:30:21.700?
* const result = differenceInMilliseconds(
*   new Date(2014, 6, 2, 12, 30, 21, 700),
*   new Date(2014, 6, 2, 12, 30, 20, 600)
* )
* //=> 1100
*/
function differenceInMilliseconds(laterDate, earlierDate) {
	return +toDate(laterDate) - +toDate(earlierDate);
}
//#endregion
//#region node_modules/date-fns/differenceInMinutes.js
/**
* The {@link differenceInMinutes} function options.
*/
/**
* @name differenceInMinutes
* @category Minute Helpers
* @summary Get the number of minutes between the given dates.
*
* @description
* Get the signed number of full (rounded towards 0) minutes between the given dates.
*
* @param dateLeft - The later date
* @param dateRight - The earlier date
* @param options - An object with options.
*
* @returns The number of minutes
*
* @example
* // How many minutes are between 2 July 2014 12:07:59 and 2 July 2014 12:20:00?
* const result = differenceInMinutes(
*   new Date(2014, 6, 2, 12, 20, 0),
*   new Date(2014, 6, 2, 12, 7, 59)
* )
* //=> 12
*
* @example
* // How many minutes are between 10:01:59 and 10:00:00
* const result = differenceInMinutes(
*   new Date(2000, 0, 1, 10, 0, 0),
*   new Date(2000, 0, 1, 10, 1, 59)
* )
* //=> -1
*/
function differenceInMinutes(dateLeft, dateRight, options) {
	const diff = differenceInMilliseconds(dateLeft, dateRight) / millisecondsInMinute;
	return getRoundingMethod(options?.roundingMethod)(diff);
}
//#endregion
//#region node_modules/date-fns/compareAsc.js
/**
* @name compareAsc
* @category Common Helpers
* @summary Compare the two dates and return -1, 0 or 1.
*
* @description
* Compare the two dates and return 1 if the first date is after the second,
* -1 if the first date is before the second or 0 if dates are equal.
*
* @param dateLeft - The first date to compare
* @param dateRight - The second date to compare
*
* @returns The result of the comparison
*
* @example
* // Compare 11 February 1987 and 10 July 1989:
* const result = compareAsc(new Date(1987, 1, 11), new Date(1989, 6, 10))
* //=> -1
*
* @example
* // Sort the array of dates:
* const result = [
*   new Date(1995, 6, 2),
*   new Date(1987, 1, 11),
*   new Date(1989, 6, 10)
* ].sort(compareAsc)
* //=> [
* //   Wed Feb 11 1987 00:00:00,
* //   Mon Jul 10 1989 00:00:00,
* //   Sun Jul 02 1995 00:00:00
* // ]
*/
function compareAsc(dateLeft, dateRight) {
	const diff = +toDate(dateLeft) - +toDate(dateRight);
	if (diff < 0) return -1;
	else if (diff > 0) return 1;
	return diff;
}
//#endregion
//#region node_modules/date-fns/differenceInCalendarMonths.js
/**
* The {@link differenceInCalendarMonths} function options.
*/
/**
* @name differenceInCalendarMonths
* @category Month Helpers
* @summary Get the number of calendar months between the given dates.
*
* @description
* Get the number of calendar months between the given dates.
*
* @param laterDate - The later date
* @param earlierDate - The earlier date
* @param options - An object with options
*
* @returns The number of calendar months
*
* @example
* // How many calendar months are between 31 January 2014 and 1 September 2014?
* const result = differenceInCalendarMonths(
*   new Date(2014, 8, 1),
*   new Date(2014, 0, 31)
* )
* //=> 8
*/
function differenceInCalendarMonths(laterDate, earlierDate, options) {
	const [laterDate_, earlierDate_] = normalizeDates(options?.in, laterDate, earlierDate);
	const yearsDiff = laterDate_.getFullYear() - earlierDate_.getFullYear();
	const monthsDiff = laterDate_.getMonth() - earlierDate_.getMonth();
	return yearsDiff * 12 + monthsDiff;
}
//#endregion
//#region node_modules/date-fns/endOfDay.js
/**
* The {@link endOfDay} function options.
*/
/**
* @name endOfDay
* @category Day Helpers
* @summary Return the end of a day for the given date.
*
* @description
* Return the end of a day for the given date.
* The result will be in the local timezone.
*
* @typeParam DateType - The `Date` type, the function operates on. Gets inferred from passed arguments. Allows to use extensions like [`UTCDate`](https://github.com/date-fns/utc).
* @typeParam ResultDate - The result `Date` type, it is the type returned from the context function if it is passed, or inferred from the arguments.
*
* @param date - The original date
* @param options - An object with options
*
* @returns The end of a day
*
* @example
* // The end of a day for 2 September 2014 11:55:00:
* const result = endOfDay(new Date(2014, 8, 2, 11, 55, 0))
* //=> Tue Sep 02 2014 23:59:59.999
*/
function endOfDay(date, options) {
	const _date = toDate(date, options?.in);
	_date.setHours(23, 59, 59, 999);
	return _date;
}
//#endregion
//#region node_modules/date-fns/endOfMonth.js
/**
* The {@link endOfMonth} function options.
*/
/**
* @name endOfMonth
* @category Month Helpers
* @summary Return the end of a month for the given date.
*
* @description
* Return the end of a month for the given date.
* The result will be in the local timezone.
*
* @typeParam DateType - The `Date` type, the function operates on. Gets inferred from passed arguments. Allows to use extensions like [`UTCDate`](https://github.com/date-fns/utc).
* @typeParam ResultDate - The result `Date` type, it is the type returned from the context function if it is passed, or inferred from the arguments.
*
* @param date - The original date
* @param options - An object with options
*
* @returns The end of a month
*
* @example
* // The end of a month for 2 September 2014 11:55:00:
* const result = endOfMonth(new Date(2014, 8, 2, 11, 55, 0))
* //=> Tue Sep 30 2014 23:59:59.999
*/
function endOfMonth(date, options) {
	const _date = toDate(date, options?.in);
	const month = _date.getMonth();
	_date.setFullYear(_date.getFullYear(), month + 1, 0);
	_date.setHours(23, 59, 59, 999);
	return _date;
}
//#endregion
//#region node_modules/date-fns/isLastDayOfMonth.js
/**
* @name isLastDayOfMonth
* @category Month Helpers
* @summary Is the given date the last day of a month?
*
* @description
* Is the given date the last day of a month?
*
* @param date - The date to check
* @param options - An object with options
*
* @returns The date is the last day of a month
*
* @example
* // Is 28 February 2014 the last day of a month?
* const result = isLastDayOfMonth(new Date(2014, 1, 28))
* //=> true
*/
function isLastDayOfMonth(date, options) {
	const _date = toDate(date, options?.in);
	return +endOfDay(_date, options) === +endOfMonth(_date, options);
}
//#endregion
//#region node_modules/date-fns/differenceInMonths.js
/**
* The {@link differenceInMonths} function options.
*/
/**
* @name differenceInMonths
* @category Month Helpers
* @summary Get the number of full months between the given dates.
*
* @param laterDate - The later date
* @param earlierDate - The earlier date
* @param options - An object with options
*
* @returns The number of full months
*
* @example
* // How many full months are between 31 January 2014 and 1 September 2014?
* const result = differenceInMonths(new Date(2014, 8, 1), new Date(2014, 0, 31))
* //=> 7
*/
function differenceInMonths(laterDate, earlierDate, options) {
	const [laterDate_, workingLaterDate, earlierDate_] = normalizeDates(options?.in, laterDate, laterDate, earlierDate);
	const sign = compareAsc(workingLaterDate, earlierDate_);
	const difference = Math.abs(differenceInCalendarMonths(workingLaterDate, earlierDate_));
	if (difference < 1) return 0;
	if (workingLaterDate.getMonth() === 1 && workingLaterDate.getDate() > 27) workingLaterDate.setDate(30);
	workingLaterDate.setMonth(workingLaterDate.getMonth() - sign * difference);
	let isLastMonthNotFull = compareAsc(workingLaterDate, earlierDate_) === -sign;
	if (isLastDayOfMonth(laterDate_) && difference === 1 && compareAsc(laterDate_, earlierDate_) === 1) isLastMonthNotFull = false;
	const result = sign * (difference - +isLastMonthNotFull);
	return result === 0 ? 0 : result;
}
//#endregion
//#region node_modules/date-fns/differenceInWeeks.js
/**
* The {@link differenceInWeeks} function options.
*/
/**
* @name differenceInWeeks
* @category Week Helpers
* @summary Get the number of full weeks between the given dates.
*
* @description
* Get the number of full weeks between two dates. Fractional weeks are
* truncated towards zero by default.
*
* One "full week" is the distance between a local time in one day to the same
* local time 7 days earlier or later. A full week can sometimes be less than
* or more than 7*24 hours if a daylight savings change happens between two dates.
*
* To ignore DST and only measure exact 7*24-hour periods, use this instead:
* `Math.trunc(differenceInHours(dateLeft, dateRight)/(7*24))|0`.
*
* @param laterDate - The later date
* @param earlierDate - The earlier date
* @param options - An object with options
*
* @returns The number of full weeks
*
* @example
* // How many full weeks are between 5 July 2014 and 20 July 2014?
* const result = differenceInWeeks(new Date(2014, 6, 20), new Date(2014, 6, 5))
* //=> 2
*
* @example
* // How many full weeks are between
* // 1 March 2020 0:00 and 6 June 2020 0:00 ?
* // Note: because local time is used, the
* // result will always be 8 weeks (54 days),
* // even if DST starts and the period has
* // only 54*24-1 hours.
* const result = differenceInWeeks(
*   new Date(2020, 5, 1),
*   new Date(2020, 2, 6)
* )
* //=> 8
*/
function differenceInWeeks(laterDate, earlierDate, options) {
	const diff = differenceInDays(laterDate, earlierDate, options) / 7;
	return getRoundingMethod(options?.roundingMethod)(diff);
}
//#endregion
//#region node_modules/date-fns/differenceInCalendarYears.js
/**
* The {@link differenceInCalendarYears} function options.
*/
/**
* @name differenceInCalendarYears
* @category Year Helpers
* @summary Get the number of calendar years between the given dates.
*
* @description
* Get the number of calendar years between the given dates.
*
* @param laterDate - The later date
* @param earlierDate - The earlier date
* @param options - An object with options

* @returns The number of calendar years
*
* @example
* // How many calendar years are between 31 December 2013 and 11 February 2015?
* const result = differenceInCalendarYears(
*   new Date(2015, 1, 11),
*   new Date(2013, 11, 31)
* );
* //=> 2
*/
function differenceInCalendarYears(laterDate, earlierDate, options) {
	const [laterDate_, earlierDate_] = normalizeDates(options?.in, laterDate, earlierDate);
	return laterDate_.getFullYear() - earlierDate_.getFullYear();
}
//#endregion
//#region node_modules/date-fns/differenceInYears.js
/**
* The {@link differenceInYears} function options.
*/
/**
* @name differenceInYears
* @category Year Helpers
* @summary Get the number of full years between the given dates.
*
* @description
* Get the number of full years between the given dates.
*
* @param laterDate - The later date
* @param earlierDate - The earlier date
* @param options - An object with options
*
* @returns The number of full years
*
* @example
* // How many full years are between 31 December 2013 and 11 February 2015?
* const result = differenceInYears(new Date(2015, 1, 11), new Date(2013, 11, 31))
* //=> 1
*/
function differenceInYears(laterDate, earlierDate, options) {
	const [laterDate_, earlierDate_] = normalizeDates(options?.in, laterDate, earlierDate);
	const sign = compareAsc(laterDate_, earlierDate_);
	const diff = Math.abs(differenceInCalendarYears(laterDate_, earlierDate_));
	laterDate_.setFullYear(1584);
	earlierDate_.setFullYear(1584);
	const result = sign * (diff - +(compareAsc(laterDate_, earlierDate_) === -sign));
	return result === 0 ? 0 : result;
}
//#endregion
//#region node_modules/date-fns/endOfHour.js
/**
* The {@link endOfHour} function options.
*/
/**
* @name endOfHour
* @category Hour Helpers
* @summary Return the end of an hour for the given date.
*
* @description
* Return the end of an hour for the given date.
* The result will be in the local timezone.
*
* @typeParam DateType - The `Date` type, the function operates on. Gets inferred from passed arguments. Allows to use extensions like [`UTCDate`](https://github.com/date-fns/utc).
* @typeParam ResultDate - The result `Date` type, it is the type returned from the context function if it is passed, or inferred from the arguments.
*
* @param date - The original date
* @param options - An object with options
*
* @returns The end of an hour
*
* @example
* // The end of an hour for 2 September 2014 11:55:00:
* const result = endOfHour(new Date(2014, 8, 2, 11, 55))
* //=> Tue Sep 02 2014 11:59:59.999
*/
function endOfHour(date, options) {
	const _date = toDate(date, options?.in);
	_date.setMinutes(59, 59, 999);
	return _date;
}
//#endregion
//#region node_modules/date-fns/endOfMinute.js
/**
* The {@link endOfMinute} function options.
*/
/**
* @name endOfMinute
* @category Minute Helpers
* @summary Return the end of a minute for the given date.
*
* @description
* Return the end of a minute for the given date.
* The result will be in the local timezone or the provided context.
*
* @typeParam DateType - The `Date` type, the function operates on. Gets inferred from passed arguments. Allows to use extensions like [`UTCDate`](https://github.com/date-fns/utc).
* @typeParam ResultDate - The result `Date` type, it is the type returned from the context function if it is passed, or inferred from the arguments.
*
* @param date - The original date
* @param options - An object with options
*
* @returns The end of a minute
*
* @example
* // The end of a minute for 1 December 2014 22:15:45.400:
* const result = endOfMinute(new Date(2014, 11, 1, 22, 15, 45, 400))
* //=> Mon Dec 01 2014 22:15:59.999
*/
function endOfMinute(date, options) {
	const _date = toDate(date, options?.in);
	_date.setSeconds(59, 999);
	return _date;
}
//#endregion
//#region node_modules/date-fns/endOfSecond.js
/**
* The {@link endOfSecond} function options.
*/
/**
* @name endOfSecond
* @category Second Helpers
* @summary Return the end of a second for the given date.
*
* @description
* Return the end of a second for the given date.
* The result will be in the local timezone if no `in` option is specified.
*
* @typeParam DateType - The `Date` type, the function operates on. Gets inferred from passed arguments. Allows to use extensions like [`UTCDate`](https://github.com/date-fns/utc).
* @typeParam ResultDate - The result `Date` type, it is the type returned from the context function if it is passed, or inferred from the arguments.
*
* @param date - The original date
* @param options - An object with options
*
* @returns The end of a second
*
* @example
* // The end of a second for 1 December 2014 22:15:45.400:
* const result = endOfSecond(new Date(2014, 11, 1, 22, 15, 45, 400))
* //=> Mon Dec 01 2014 22:15:45.999
*/
function endOfSecond(date, options) {
	const _date = toDate(date, options?.in);
	_date.setMilliseconds(999);
	return _date;
}
//#endregion
//#region node_modules/date-fns/_lib/defaultOptions.js
var defaultOptions = {};
function getDefaultOptions$1() {
	return defaultOptions;
}
//#endregion
//#region node_modules/date-fns/endOfWeek.js
/**
* The {@link endOfWeek} function options.
*/
/**
* @name endOfWeek
* @category Week Helpers
* @summary Return the end of a week for the given date.
*
* @description
* Return the end of a week for the given date.
* The result will be in the local timezone.
*
* @typeParam DateType - The `Date` type, the function operates on. Gets inferred from passed arguments. Allows to use extensions like [`UTCDate`](https://github.com/date-fns/utc).
* @typeParam ResultDate - The result `Date` type, it is the type returned from the context function if it is passed, or inferred from the arguments.
*
* @param date - The original date
* @param options - An object with options
*
* @returns The end of a week
*
* @example
* // The end of a week for 2 September 2014 11:55:00:
* const result = endOfWeek(new Date(2014, 8, 2, 11, 55, 0))
* //=> Sat Sep 06 2014 23:59:59.999
*
* @example
* // If the week starts on Monday, the end of the week for 2 September 2014 11:55:00:
* const result = endOfWeek(new Date(2014, 8, 2, 11, 55, 0), { weekStartsOn: 1 })
* //=> Sun Sep 07 2014 23:59:59.999
*/
function endOfWeek(date, options) {
	const defaultOptions = getDefaultOptions$1();
	const weekStartsOn = options?.weekStartsOn ?? options?.locale?.options?.weekStartsOn ?? defaultOptions.weekStartsOn ?? defaultOptions.locale?.options?.weekStartsOn ?? 0;
	const _date = toDate(date, options?.in);
	const day = _date.getDay();
	const diff = (day < weekStartsOn ? -7 : 0) + 6 - (day - weekStartsOn);
	_date.setDate(_date.getDate() + diff);
	_date.setHours(23, 59, 59, 999);
	return _date;
}
//#endregion
//#region node_modules/date-fns/endOfYear.js
/**
* The {@link endOfYear} function options.
*/
/**
* @name endOfYear
* @category Year Helpers
* @summary Return the end of a year for the given date.
*
* @description
* Return the end of a year for the given date.
* The result will be in the local timezone.
*
* @typeParam DateType - The `Date` type, the function operates on. Gets inferred from passed arguments. Allows to use extensions like [`UTCDate`](https://github.com/date-fns/utc).
* @typeParam ResultDate - The result `Date` type, it is the type returned from the context function if it is passed, or inferred from the arguments.
*
* @param date - The original date
* @param options - The options
*
* @returns The end of a year
*
* @example
* // The end of a year for 2 September 2014 11:55:00:
* const result = endOfYear(new Date(2014, 8, 2, 11, 55, 0))
* //=> Wed Dec 31 2014 23:59:59.999
*/
function endOfYear(date, options) {
	const _date = toDate(date, options?.in);
	const year = _date.getFullYear();
	_date.setFullYear(year + 1, 0, 0);
	_date.setHours(23, 59, 59, 999);
	return _date;
}
//#endregion
//#region node_modules/date-fns/locale/en-US/_lib/formatDistance.js
var formatDistanceLocale = {
	lessThanXSeconds: {
		one: "less than a second",
		other: "less than {{count}} seconds"
	},
	xSeconds: {
		one: "1 second",
		other: "{{count}} seconds"
	},
	halfAMinute: "half a minute",
	lessThanXMinutes: {
		one: "less than a minute",
		other: "less than {{count}} minutes"
	},
	xMinutes: {
		one: "1 minute",
		other: "{{count}} minutes"
	},
	aboutXHours: {
		one: "about 1 hour",
		other: "about {{count}} hours"
	},
	xHours: {
		one: "1 hour",
		other: "{{count}} hours"
	},
	xDays: {
		one: "1 day",
		other: "{{count}} days"
	},
	aboutXWeeks: {
		one: "about 1 week",
		other: "about {{count}} weeks"
	},
	xWeeks: {
		one: "1 week",
		other: "{{count}} weeks"
	},
	aboutXMonths: {
		one: "about 1 month",
		other: "about {{count}} months"
	},
	xMonths: {
		one: "1 month",
		other: "{{count}} months"
	},
	aboutXYears: {
		one: "about 1 year",
		other: "about {{count}} years"
	},
	xYears: {
		one: "1 year",
		other: "{{count}} years"
	},
	overXYears: {
		one: "over 1 year",
		other: "over {{count}} years"
	},
	almostXYears: {
		one: "almost 1 year",
		other: "almost {{count}} years"
	}
};
var formatDistance = (token, count, options) => {
	let result;
	const tokenValue = formatDistanceLocale[token];
	if (typeof tokenValue === "string") result = tokenValue;
	else if (count === 1) result = tokenValue.one;
	else result = tokenValue.other.replace("{{count}}", count.toString());
	if (options?.addSuffix) if (options.comparison && options.comparison > 0) return "in " + result;
	else return result + " ago";
	return result;
};
var formatLong = {
	date: buildFormatLongFn({
		formats: {
			full: "EEEE, MMMM do, y",
			long: "MMMM do, y",
			medium: "MMM d, y",
			short: "MM/dd/yyyy"
		},
		defaultWidth: "full"
	}),
	time: buildFormatLongFn({
		formats: {
			full: "h:mm:ss a zzzz",
			long: "h:mm:ss a z",
			medium: "h:mm:ss a",
			short: "h:mm a"
		},
		defaultWidth: "full"
	}),
	dateTime: buildFormatLongFn({
		formats: {
			full: "{{date}} 'at' {{time}}",
			long: "{{date}} 'at' {{time}}",
			medium: "{{date}}, {{time}}",
			short: "{{date}}, {{time}}"
		},
		defaultWidth: "full"
	})
};
//#endregion
//#region node_modules/date-fns/locale/en-US/_lib/formatRelative.js
var formatRelativeLocale = {
	lastWeek: "'last' eeee 'at' p",
	yesterday: "'yesterday at' p",
	today: "'today at' p",
	tomorrow: "'tomorrow at' p",
	nextWeek: "eeee 'at' p",
	other: "P"
};
var formatRelative = (token, _date, _baseDate, _options) => formatRelativeLocale[token];
//#endregion
//#region node_modules/date-fns/locale/en-US/_lib/localize.js
var eraValues = {
	narrow: ["B", "A"],
	abbreviated: ["BC", "AD"],
	wide: ["Before Christ", "Anno Domini"]
};
var quarterValues = {
	narrow: [
		"1",
		"2",
		"3",
		"4"
	],
	abbreviated: [
		"Q1",
		"Q2",
		"Q3",
		"Q4"
	],
	wide: [
		"1st quarter",
		"2nd quarter",
		"3rd quarter",
		"4th quarter"
	]
};
var monthValues = {
	narrow: [
		"J",
		"F",
		"M",
		"A",
		"M",
		"J",
		"J",
		"A",
		"S",
		"O",
		"N",
		"D"
	],
	abbreviated: [
		"Jan",
		"Feb",
		"Mar",
		"Apr",
		"May",
		"Jun",
		"Jul",
		"Aug",
		"Sep",
		"Oct",
		"Nov",
		"Dec"
	],
	wide: [
		"January",
		"February",
		"March",
		"April",
		"May",
		"June",
		"July",
		"August",
		"September",
		"October",
		"November",
		"December"
	]
};
var dayValues = {
	narrow: [
		"S",
		"M",
		"T",
		"W",
		"T",
		"F",
		"S"
	],
	short: [
		"Su",
		"Mo",
		"Tu",
		"We",
		"Th",
		"Fr",
		"Sa"
	],
	abbreviated: [
		"Sun",
		"Mon",
		"Tue",
		"Wed",
		"Thu",
		"Fri",
		"Sat"
	],
	wide: [
		"Sunday",
		"Monday",
		"Tuesday",
		"Wednesday",
		"Thursday",
		"Friday",
		"Saturday"
	]
};
var dayPeriodValues = {
	narrow: {
		am: "a",
		pm: "p",
		midnight: "mi",
		noon: "n",
		morning: "morning",
		afternoon: "afternoon",
		evening: "evening",
		night: "night"
	},
	abbreviated: {
		am: "AM",
		pm: "PM",
		midnight: "midnight",
		noon: "noon",
		morning: "morning",
		afternoon: "afternoon",
		evening: "evening",
		night: "night"
	},
	wide: {
		am: "a.m.",
		pm: "p.m.",
		midnight: "midnight",
		noon: "noon",
		morning: "morning",
		afternoon: "afternoon",
		evening: "evening",
		night: "night"
	}
};
var formattingDayPeriodValues = {
	narrow: {
		am: "a",
		pm: "p",
		midnight: "mi",
		noon: "n",
		morning: "in the morning",
		afternoon: "in the afternoon",
		evening: "in the evening",
		night: "at night"
	},
	abbreviated: {
		am: "AM",
		pm: "PM",
		midnight: "midnight",
		noon: "noon",
		morning: "in the morning",
		afternoon: "in the afternoon",
		evening: "in the evening",
		night: "at night"
	},
	wide: {
		am: "a.m.",
		pm: "p.m.",
		midnight: "midnight",
		noon: "noon",
		morning: "in the morning",
		afternoon: "in the afternoon",
		evening: "in the evening",
		night: "at night"
	}
};
var ordinalNumber = (dirtyNumber, _options) => {
	const number = Number(dirtyNumber);
	const rem100 = number % 100;
	if (rem100 > 20 || rem100 < 10) switch (rem100 % 10) {
		case 1: return number + "st";
		case 2: return number + "nd";
		case 3: return number + "rd";
	}
	return number + "th";
};
//#endregion
//#region node_modules/date-fns/locale/en-US.js
/**
* @category Locales
* @summary English locale (United States).
* @language English
* @iso-639-2 eng
* @author Sasha Koss [@kossnocorp](https://github.com/kossnocorp)
* @author Lesha Koss [@leshakoss](https://github.com/leshakoss)
*/
var enUS$1 = {
	code: "en-US",
	formatDistance,
	formatLong,
	formatRelative,
	localize: {
		ordinalNumber,
		era: buildLocalizeFn({
			values: eraValues,
			defaultWidth: "wide"
		}),
		quarter: buildLocalizeFn({
			values: quarterValues,
			defaultWidth: "wide",
			argumentCallback: (quarter) => quarter - 1
		}),
		month: buildLocalizeFn({
			values: monthValues,
			defaultWidth: "wide"
		}),
		day: buildLocalizeFn({
			values: dayValues,
			defaultWidth: "wide"
		}),
		dayPeriod: buildLocalizeFn({
			values: dayPeriodValues,
			defaultWidth: "wide",
			formattingValues: formattingDayPeriodValues,
			defaultFormattingWidth: "wide"
		})
	},
	match: {
		ordinalNumber: buildMatchPatternFn({
			matchPattern: /^(\d+)(th|st|nd|rd)?/i,
			parsePattern: /\d+/i,
			valueCallback: (value) => parseInt(value, 10)
		}),
		era: buildMatchFn({
			matchPatterns: {
				narrow: /^(b|a)/i,
				abbreviated: /^(b\.?\s?c\.?|b\.?\s?c\.?\s?e\.?|a\.?\s?d\.?|c\.?\s?e\.?)/i,
				wide: /^(before christ|before common era|anno domini|common era)/i
			},
			defaultMatchWidth: "wide",
			parsePatterns: { any: [/^b/i, /^(a|c)/i] },
			defaultParseWidth: "any"
		}),
		quarter: buildMatchFn({
			matchPatterns: {
				narrow: /^[1234]/i,
				abbreviated: /^q[1234]/i,
				wide: /^[1234](th|st|nd|rd)? quarter/i
			},
			defaultMatchWidth: "wide",
			parsePatterns: { any: [
				/1/i,
				/2/i,
				/3/i,
				/4/i
			] },
			defaultParseWidth: "any",
			valueCallback: (index) => index + 1
		}),
		month: buildMatchFn({
			matchPatterns: {
				narrow: /^[jfmasond]/i,
				abbreviated: /^(jan|feb|mar|apr|may|jun|jul|aug|sep|oct|nov|dec)/i,
				wide: /^(january|february|march|april|may|june|july|august|september|october|november|december)/i
			},
			defaultMatchWidth: "wide",
			parsePatterns: {
				narrow: [
					/^j/i,
					/^f/i,
					/^m/i,
					/^a/i,
					/^m/i,
					/^j/i,
					/^j/i,
					/^a/i,
					/^s/i,
					/^o/i,
					/^n/i,
					/^d/i
				],
				any: [
					/^ja/i,
					/^f/i,
					/^mar/i,
					/^ap/i,
					/^may/i,
					/^jun/i,
					/^jul/i,
					/^au/i,
					/^s/i,
					/^o/i,
					/^n/i,
					/^d/i
				]
			},
			defaultParseWidth: "any"
		}),
		day: buildMatchFn({
			matchPatterns: {
				narrow: /^[smtwf]/i,
				short: /^(su|mo|tu|we|th|fr|sa)/i,
				abbreviated: /^(sun|mon|tue|wed|thu|fri|sat)/i,
				wide: /^(sunday|monday|tuesday|wednesday|thursday|friday|saturday)/i
			},
			defaultMatchWidth: "wide",
			parsePatterns: {
				narrow: [
					/^s/i,
					/^m/i,
					/^t/i,
					/^w/i,
					/^t/i,
					/^f/i,
					/^s/i
				],
				any: [
					/^su/i,
					/^m/i,
					/^tu/i,
					/^w/i,
					/^th/i,
					/^f/i,
					/^sa/i
				]
			},
			defaultParseWidth: "any"
		}),
		dayPeriod: buildMatchFn({
			matchPatterns: {
				narrow: /^(a|p|mi|n|(in the|at) (morning|afternoon|evening|night))/i,
				any: /^([ap]\.?\s?m\.?|midnight|noon|(in the|at) (morning|afternoon|evening|night))/i
			},
			defaultMatchWidth: "any",
			parsePatterns: { any: {
				am: /^a/i,
				pm: /^p/i,
				midnight: /^mi/i,
				noon: /^no/i,
				morning: /morning/i,
				afternoon: /afternoon/i,
				evening: /evening/i,
				night: /night/i
			} },
			defaultParseWidth: "any"
		})
	},
	options: {
		weekStartsOn: 0,
		firstWeekContainsDate: 1
	}
};
//#endregion
//#region node_modules/date-fns/startOfYear.js
/**
* The {@link startOfYear} function options.
*/
/**
* @name startOfYear
* @category Year Helpers
* @summary Return the start of a year for the given date.
*
* @description
* Return the start of a year for the given date.
* The result will be in the local timezone.
*
* @typeParam DateType - The `Date` type, the function operates on. Gets inferred from passed arguments. Allows to use extensions like [`UTCDate`](https://github.com/date-fns/utc).
* @typeParam ResultDate - The result `Date` type, it is the type returned from the context function if it is passed, or inferred from the arguments.
*
* @param date - The original date
* @param options - The options
*
* @returns The start of a year
*
* @example
* // The start of a year for 2 September 2014 11:55:00:
* const result = startOfYear(new Date(2014, 8, 2, 11, 55, 00))
* //=> Wed Jan 01 2014 00:00:00
*/
function startOfYear(date, options) {
	const date_ = toDate(date, options?.in);
	date_.setFullYear(date_.getFullYear(), 0, 1);
	date_.setHours(0, 0, 0, 0);
	return date_;
}
//#endregion
//#region node_modules/date-fns/getDayOfYear.js
/**
* The {@link getDayOfYear} function options.
*/
/**
* @name getDayOfYear
* @category Day Helpers
* @summary Get the day of the year of the given date.
*
* @description
* Get the day of the year of the given date.
*
* @param date - The given date
* @param options - The options
*
* @returns The day of year
*
* @example
* // Which day of the year is 2 July 2014?
* const result = getDayOfYear(new Date(2014, 6, 2))
* //=> 183
*/
function getDayOfYear(date, options) {
	const _date = toDate(date, options?.in);
	return differenceInCalendarDays(_date, startOfYear(_date)) + 1;
}
//#endregion
//#region node_modules/date-fns/startOfWeek.js
/**
* The {@link startOfWeek} function options.
*/
/**
* @name startOfWeek
* @category Week Helpers
* @summary Return the start of a week for the given date.
*
* @description
* Return the start of a week for the given date.
* The result will be in the local timezone.
*
* @typeParam DateType - The `Date` type, the function operates on. Gets inferred from passed arguments. Allows to use extensions like [`UTCDate`](https://github.com/date-fns/utc).
* @typeParam ResultDate - The result `Date` type, it is the type returned from the context function if it is passed, or inferred from the arguments.
*
* @param date - The original date
* @param options - An object with options
*
* @returns The start of a week
*
* @example
* // The start of a week for 2 September 2014 11:55:00:
* const result = startOfWeek(new Date(2014, 8, 2, 11, 55, 0))
* //=> Sun Aug 31 2014 00:00:00
*
* @example
* // If the week starts on Monday, the start of the week for 2 September 2014 11:55:00:
* const result = startOfWeek(new Date(2014, 8, 2, 11, 55, 0), { weekStartsOn: 1 })
* //=> Mon Sep 01 2014 00:00:00
*/
function startOfWeek(date, options) {
	const defaultOptions = getDefaultOptions$1();
	const weekStartsOn = options?.weekStartsOn ?? options?.locale?.options?.weekStartsOn ?? defaultOptions.weekStartsOn ?? defaultOptions.locale?.options?.weekStartsOn ?? 0;
	const _date = toDate(date, options?.in);
	const day = _date.getDay();
	const diff = (day < weekStartsOn ? 7 : 0) + day - weekStartsOn;
	_date.setDate(_date.getDate() - diff);
	_date.setHours(0, 0, 0, 0);
	return _date;
}
//#endregion
//#region node_modules/date-fns/startOfISOWeek.js
/**
* The {@link startOfISOWeek} function options.
*/
/**
* @name startOfISOWeek
* @category ISO Week Helpers
* @summary Return the start of an ISO week for the given date.
*
* @description
* Return the start of an ISO week for the given date.
* The result will be in the local timezone.
*
* ISO week-numbering year: http://en.wikipedia.org/wiki/ISO_week_date
*
* @typeParam DateType - The `Date` type, the function operates on. Gets inferred from passed arguments. Allows to use extensions like [`UTCDate`](https://github.com/date-fns/utc).
* @typeParam ResultDate - The result `Date` type, it is the type returned from the context function if it is passed, or inferred from the arguments.
*
* @param date - The original date
* @param options - An object with options
*
* @returns The start of an ISO week
*
* @example
* // The start of an ISO week for 2 September 2014 11:55:00:
* const result = startOfISOWeek(new Date(2014, 8, 2, 11, 55, 0))
* //=> Mon Sep 01 2014 00:00:00
*/
function startOfISOWeek(date, options) {
	return startOfWeek(date, {
		...options,
		weekStartsOn: 1
	});
}
//#endregion
//#region node_modules/date-fns/getISOWeekYear.js
/**
* The {@link getISOWeekYear} function options.
*/
/**
* @name getISOWeekYear
* @category ISO Week-Numbering Year Helpers
* @summary Get the ISO week-numbering year of the given date.
*
* @description
* Get the ISO week-numbering year of the given date,
* which always starts 3 days before the year's first Thursday.
*
* ISO week-numbering year: http://en.wikipedia.org/wiki/ISO_week_date
*
* @param date - The given date
*
* @returns The ISO week-numbering year
*
* @example
* // Which ISO-week numbering year is 2 January 2005?
* const result = getISOWeekYear(new Date(2005, 0, 2))
* //=> 2004
*/
function getISOWeekYear(date, options) {
	const _date = toDate(date, options?.in);
	const year = _date.getFullYear();
	const fourthOfJanuaryOfNextYear = constructFrom(_date, 0);
	fourthOfJanuaryOfNextYear.setFullYear(year + 1, 0, 4);
	fourthOfJanuaryOfNextYear.setHours(0, 0, 0, 0);
	const startOfNextYear = startOfISOWeek(fourthOfJanuaryOfNextYear);
	const fourthOfJanuaryOfThisYear = constructFrom(_date, 0);
	fourthOfJanuaryOfThisYear.setFullYear(year, 0, 4);
	fourthOfJanuaryOfThisYear.setHours(0, 0, 0, 0);
	const startOfThisYear = startOfISOWeek(fourthOfJanuaryOfThisYear);
	if (_date.getTime() >= startOfNextYear.getTime()) return year + 1;
	else if (_date.getTime() >= startOfThisYear.getTime()) return year;
	else return year - 1;
}
//#endregion
//#region node_modules/date-fns/startOfISOWeekYear.js
/**
* The {@link startOfISOWeekYear} function options.
*/
/**
* @name startOfISOWeekYear
* @category ISO Week-Numbering Year Helpers
* @summary Return the start of an ISO week-numbering year for the given date.
*
* @description
* Return the start of an ISO week-numbering year,
* which always starts 3 days before the year's first Thursday.
* The result will be in the local timezone.
*
* ISO week-numbering year: http://en.wikipedia.org/wiki/ISO_week_date
*
* @typeParam DateType - The `Date` type, the function operates on. Gets inferred from passed arguments. Allows to use extensions like [`UTCDate`](https://github.com/date-fns/utc).
* @typeParam ResultDate - The result `Date` type, it is the type returned from the context function if it is passed, or inferred from the arguments.
*
* @param date - The original date
* @param options - An object with options
*
* @returns The start of an ISO week-numbering year
*
* @example
* // The start of an ISO week-numbering year for 2 July 2005:
* const result = startOfISOWeekYear(new Date(2005, 6, 2))
* //=> Mon Jan 03 2005 00:00:00
*/
function startOfISOWeekYear(date, options) {
	const year = getISOWeekYear(date, options);
	const fourthOfJanuary = constructFrom(options?.in || date, 0);
	fourthOfJanuary.setFullYear(year, 0, 4);
	fourthOfJanuary.setHours(0, 0, 0, 0);
	return startOfISOWeek(fourthOfJanuary);
}
//#endregion
//#region node_modules/date-fns/getISOWeek.js
/**
* The {@link getISOWeek} function options.
*/
/**
* @name getISOWeek
* @category ISO Week Helpers
* @summary Get the ISO week of the given date.
*
* @description
* Get the ISO week of the given date.
*
* ISO week-numbering year: http://en.wikipedia.org/wiki/ISO_week_date
*
* @param date - The given date
* @param options - The options
*
* @returns The ISO week
*
* @example
* // Which week of the ISO-week numbering year is 2 January 2005?
* const result = getISOWeek(new Date(2005, 0, 2))
* //=> 53
*/
function getISOWeek(date, options) {
	const _date = toDate(date, options?.in);
	const diff = +startOfISOWeek(_date) - +startOfISOWeekYear(_date);
	return Math.round(diff / millisecondsInWeek) + 1;
}
//#endregion
//#region node_modules/date-fns/getWeekYear.js
/**
* The {@link getWeekYear} function options.
*/
/**
* @name getWeekYear
* @category Week-Numbering Year Helpers
* @summary Get the local week-numbering year of the given date.
*
* @description
* Get the local week-numbering year of the given date.
* The exact calculation depends on the values of
* `options.weekStartsOn` (which is the index of the first day of the week)
* and `options.firstWeekContainsDate` (which is the day of January, which is always in
* the first week of the week-numbering year)
*
* Week numbering: https://en.wikipedia.org/wiki/Week#The_ISO_week_date_system
*
* @param date - The given date
* @param options - An object with options.
*
* @returns The local week-numbering year
*
* @example
* // Which week numbering year is 26 December 2004 with the default settings?
* const result = getWeekYear(new Date(2004, 11, 26))
* //=> 2005
*
* @example
* // Which week numbering year is 26 December 2004 if week starts on Saturday?
* const result = getWeekYear(new Date(2004, 11, 26), { weekStartsOn: 6 })
* //=> 2004
*
* @example
* // Which week numbering year is 26 December 2004 if the first week contains 4 January?
* const result = getWeekYear(new Date(2004, 11, 26), { firstWeekContainsDate: 4 })
* //=> 2004
*/
function getWeekYear(date, options) {
	const _date = toDate(date, options?.in);
	const year = _date.getFullYear();
	const defaultOptions = getDefaultOptions$1();
	const firstWeekContainsDate = options?.firstWeekContainsDate ?? options?.locale?.options?.firstWeekContainsDate ?? defaultOptions.firstWeekContainsDate ?? defaultOptions.locale?.options?.firstWeekContainsDate ?? 1;
	const firstWeekOfNextYear = constructFrom(options?.in || date, 0);
	firstWeekOfNextYear.setFullYear(year + 1, 0, firstWeekContainsDate);
	firstWeekOfNextYear.setHours(0, 0, 0, 0);
	const startOfNextYear = startOfWeek(firstWeekOfNextYear, options);
	const firstWeekOfThisYear = constructFrom(options?.in || date, 0);
	firstWeekOfThisYear.setFullYear(year, 0, firstWeekContainsDate);
	firstWeekOfThisYear.setHours(0, 0, 0, 0);
	const startOfThisYear = startOfWeek(firstWeekOfThisYear, options);
	if (+_date >= +startOfNextYear) return year + 1;
	else if (+_date >= +startOfThisYear) return year;
	else return year - 1;
}
//#endregion
//#region node_modules/date-fns/startOfWeekYear.js
/**
* The {@link startOfWeekYear} function options.
*/
/**
* @name startOfWeekYear
* @category Week-Numbering Year Helpers
* @summary Return the start of a local week-numbering year for the given date.
*
* @description
* Return the start of a local week-numbering year.
* The exact calculation depends on the values of
* `options.weekStartsOn` (which is the index of the first day of the week)
* and `options.firstWeekContainsDate` (which is the day of January, which is always in
* the first week of the week-numbering year)
*
* Week numbering: https://en.wikipedia.org/wiki/Week#The_ISO_week_date_system
*
* @typeParam DateType - The `Date` type, the function operates on. Gets inferred from passed arguments. Allows to use extensions like [`UTCDate`](https://github.com/date-fns/utc).
* @typeParam ResultDate - The result `Date` type.
*
* @param date - The original date
* @param options - An object with options
*
* @returns The start of a week-numbering year
*
* @example
* // The start of an a week-numbering year for 2 July 2005 with default settings:
* const result = startOfWeekYear(new Date(2005, 6, 2))
* //=> Sun Dec 26 2004 00:00:00
*
* @example
* // The start of a week-numbering year for 2 July 2005
* // if Monday is the first day of week
* // and 4 January is always in the first week of the year:
* const result = startOfWeekYear(new Date(2005, 6, 2), {
*   weekStartsOn: 1,
*   firstWeekContainsDate: 4
* })
* //=> Mon Jan 03 2005 00:00:00
*/
function startOfWeekYear(date, options) {
	const defaultOptions = getDefaultOptions$1();
	const firstWeekContainsDate = options?.firstWeekContainsDate ?? options?.locale?.options?.firstWeekContainsDate ?? defaultOptions.firstWeekContainsDate ?? defaultOptions.locale?.options?.firstWeekContainsDate ?? 1;
	const year = getWeekYear(date, options);
	const firstWeek = constructFrom(options?.in || date, 0);
	firstWeek.setFullYear(year, 0, firstWeekContainsDate);
	firstWeek.setHours(0, 0, 0, 0);
	return startOfWeek(firstWeek, options);
}
//#endregion
//#region node_modules/date-fns/getWeek.js
/**
* The {@link getWeek} function options.
*/
/**
* @name getWeek
* @category Week Helpers
* @summary Get the local week index of the given date.
*
* @description
* Get the local week index of the given date.
* The exact calculation depends on the values of
* `options.weekStartsOn` (which is the index of the first day of the week)
* and `options.firstWeekContainsDate` (which is the day of January, which is always in
* the first week of the week-numbering year)
*
* Week numbering: https://en.wikipedia.org/wiki/Week#The_ISO_week_date_system
*
* @param date - The given date
* @param options - An object with options
*
* @returns The week
*
* @example
* // Which week of the local week numbering year is 2 January 2005 with default options?
* const result = getWeek(new Date(2005, 0, 2))
* //=> 2
*
* @example
* // Which week of the local week numbering year is 2 January 2005,
* // if Monday is the first day of the week,
* // and the first week of the year always contains 4 January?
* const result = getWeek(new Date(2005, 0, 2), {
*   weekStartsOn: 1,
*   firstWeekContainsDate: 4
* })
* //=> 53
*/
function getWeek(date, options) {
	const _date = toDate(date, options?.in);
	const diff = +startOfWeek(_date, options) - +startOfWeekYear(_date, options);
	return Math.round(diff / millisecondsInWeek) + 1;
}
//#endregion
//#region node_modules/date-fns/_lib/addLeadingZeros.js
function addLeadingZeros(number, targetLength) {
	return (number < 0 ? "-" : "") + Math.abs(number).toString().padStart(targetLength, "0");
}
//#endregion
//#region node_modules/date-fns/_lib/format/lightFormatters.js
var lightFormatters = {
	y(date, token) {
		const signedYear = date.getFullYear();
		const year = signedYear > 0 ? signedYear : 1 - signedYear;
		return addLeadingZeros(token === "yy" ? year % 100 : year, token.length);
	},
	M(date, token) {
		const month = date.getMonth();
		return token === "M" ? String(month + 1) : addLeadingZeros(month + 1, 2);
	},
	d(date, token) {
		return addLeadingZeros(date.getDate(), token.length);
	},
	a(date, token) {
		const dayPeriodEnumValue = date.getHours() / 12 >= 1 ? "pm" : "am";
		switch (token) {
			case "a":
			case "aa": return dayPeriodEnumValue.toUpperCase();
			case "aaa": return dayPeriodEnumValue;
			case "aaaaa": return dayPeriodEnumValue[0];
			default: return dayPeriodEnumValue === "am" ? "a.m." : "p.m.";
		}
	},
	h(date, token) {
		return addLeadingZeros(date.getHours() % 12 || 12, token.length);
	},
	H(date, token) {
		return addLeadingZeros(date.getHours(), token.length);
	},
	m(date, token) {
		return addLeadingZeros(date.getMinutes(), token.length);
	},
	s(date, token) {
		return addLeadingZeros(date.getSeconds(), token.length);
	},
	S(date, token) {
		const numberOfDigits = token.length;
		const milliseconds = date.getMilliseconds();
		return addLeadingZeros(Math.trunc(milliseconds * Math.pow(10, numberOfDigits - 3)), token.length);
	}
};
//#endregion
//#region node_modules/date-fns/_lib/format/formatters.js
var dayPeriodEnum = {
	am: "am",
	pm: "pm",
	midnight: "midnight",
	noon: "noon",
	morning: "morning",
	afternoon: "afternoon",
	evening: "evening",
	night: "night"
};
var formatters = {
	G: function(date, token, localize) {
		const era = date.getFullYear() > 0 ? 1 : 0;
		switch (token) {
			case "G":
			case "GG":
			case "GGG": return localize.era(era, { width: "abbreviated" });
			case "GGGGG": return localize.era(era, { width: "narrow" });
			default: return localize.era(era, { width: "wide" });
		}
	},
	y: function(date, token, localize) {
		if (token === "yo") {
			const signedYear = date.getFullYear();
			const year = signedYear > 0 ? signedYear : 1 - signedYear;
			return localize.ordinalNumber(year, { unit: "year" });
		}
		return lightFormatters.y(date, token);
	},
	Y: function(date, token, localize, options) {
		const signedWeekYear = getWeekYear(date, options);
		const weekYear = signedWeekYear > 0 ? signedWeekYear : 1 - signedWeekYear;
		if (token === "YY") return addLeadingZeros(weekYear % 100, 2);
		if (token === "Yo") return localize.ordinalNumber(weekYear, { unit: "year" });
		return addLeadingZeros(weekYear, token.length);
	},
	R: function(date, token) {
		return addLeadingZeros(getISOWeekYear(date), token.length);
	},
	u: function(date, token) {
		return addLeadingZeros(date.getFullYear(), token.length);
	},
	Q: function(date, token, localize) {
		const quarter = Math.ceil((date.getMonth() + 1) / 3);
		switch (token) {
			case "Q": return String(quarter);
			case "QQ": return addLeadingZeros(quarter, 2);
			case "Qo": return localize.ordinalNumber(quarter, { unit: "quarter" });
			case "QQQ": return localize.quarter(quarter, {
				width: "abbreviated",
				context: "formatting"
			});
			case "QQQQQ": return localize.quarter(quarter, {
				width: "narrow",
				context: "formatting"
			});
			default: return localize.quarter(quarter, {
				width: "wide",
				context: "formatting"
			});
		}
	},
	q: function(date, token, localize) {
		const quarter = Math.ceil((date.getMonth() + 1) / 3);
		switch (token) {
			case "q": return String(quarter);
			case "qq": return addLeadingZeros(quarter, 2);
			case "qo": return localize.ordinalNumber(quarter, { unit: "quarter" });
			case "qqq": return localize.quarter(quarter, {
				width: "abbreviated",
				context: "standalone"
			});
			case "qqqqq": return localize.quarter(quarter, {
				width: "narrow",
				context: "standalone"
			});
			default: return localize.quarter(quarter, {
				width: "wide",
				context: "standalone"
			});
		}
	},
	M: function(date, token, localize) {
		const month = date.getMonth();
		switch (token) {
			case "M":
			case "MM": return lightFormatters.M(date, token);
			case "Mo": return localize.ordinalNumber(month + 1, { unit: "month" });
			case "MMM": return localize.month(month, {
				width: "abbreviated",
				context: "formatting"
			});
			case "MMMMM": return localize.month(month, {
				width: "narrow",
				context: "formatting"
			});
			default: return localize.month(month, {
				width: "wide",
				context: "formatting"
			});
		}
	},
	L: function(date, token, localize) {
		const month = date.getMonth();
		switch (token) {
			case "L": return String(month + 1);
			case "LL": return addLeadingZeros(month + 1, 2);
			case "Lo": return localize.ordinalNumber(month + 1, { unit: "month" });
			case "LLL": return localize.month(month, {
				width: "abbreviated",
				context: "standalone"
			});
			case "LLLLL": return localize.month(month, {
				width: "narrow",
				context: "standalone"
			});
			default: return localize.month(month, {
				width: "wide",
				context: "standalone"
			});
		}
	},
	w: function(date, token, localize, options) {
		const week = getWeek(date, options);
		if (token === "wo") return localize.ordinalNumber(week, { unit: "week" });
		return addLeadingZeros(week, token.length);
	},
	I: function(date, token, localize) {
		const isoWeek = getISOWeek(date);
		if (token === "Io") return localize.ordinalNumber(isoWeek, { unit: "week" });
		return addLeadingZeros(isoWeek, token.length);
	},
	d: function(date, token, localize) {
		if (token === "do") return localize.ordinalNumber(date.getDate(), { unit: "date" });
		return lightFormatters.d(date, token);
	},
	D: function(date, token, localize) {
		const dayOfYear = getDayOfYear(date);
		if (token === "Do") return localize.ordinalNumber(dayOfYear, { unit: "dayOfYear" });
		return addLeadingZeros(dayOfYear, token.length);
	},
	E: function(date, token, localize) {
		const dayOfWeek = date.getDay();
		switch (token) {
			case "E":
			case "EE":
			case "EEE": return localize.day(dayOfWeek, {
				width: "abbreviated",
				context: "formatting"
			});
			case "EEEEE": return localize.day(dayOfWeek, {
				width: "narrow",
				context: "formatting"
			});
			case "EEEEEE": return localize.day(dayOfWeek, {
				width: "short",
				context: "formatting"
			});
			default: return localize.day(dayOfWeek, {
				width: "wide",
				context: "formatting"
			});
		}
	},
	e: function(date, token, localize, options) {
		const dayOfWeek = date.getDay();
		const localDayOfWeek = (dayOfWeek - options.weekStartsOn + 8) % 7 || 7;
		switch (token) {
			case "e": return String(localDayOfWeek);
			case "ee": return addLeadingZeros(localDayOfWeek, 2);
			case "eo": return localize.ordinalNumber(localDayOfWeek, { unit: "day" });
			case "eee": return localize.day(dayOfWeek, {
				width: "abbreviated",
				context: "formatting"
			});
			case "eeeee": return localize.day(dayOfWeek, {
				width: "narrow",
				context: "formatting"
			});
			case "eeeeee": return localize.day(dayOfWeek, {
				width: "short",
				context: "formatting"
			});
			default: return localize.day(dayOfWeek, {
				width: "wide",
				context: "formatting"
			});
		}
	},
	c: function(date, token, localize, options) {
		const dayOfWeek = date.getDay();
		const localDayOfWeek = (dayOfWeek - options.weekStartsOn + 8) % 7 || 7;
		switch (token) {
			case "c": return String(localDayOfWeek);
			case "cc": return addLeadingZeros(localDayOfWeek, token.length);
			case "co": return localize.ordinalNumber(localDayOfWeek, { unit: "day" });
			case "ccc": return localize.day(dayOfWeek, {
				width: "abbreviated",
				context: "standalone"
			});
			case "ccccc": return localize.day(dayOfWeek, {
				width: "narrow",
				context: "standalone"
			});
			case "cccccc": return localize.day(dayOfWeek, {
				width: "short",
				context: "standalone"
			});
			default: return localize.day(dayOfWeek, {
				width: "wide",
				context: "standalone"
			});
		}
	},
	i: function(date, token, localize) {
		const dayOfWeek = date.getDay();
		const isoDayOfWeek = dayOfWeek === 0 ? 7 : dayOfWeek;
		switch (token) {
			case "i": return String(isoDayOfWeek);
			case "ii": return addLeadingZeros(isoDayOfWeek, token.length);
			case "io": return localize.ordinalNumber(isoDayOfWeek, { unit: "day" });
			case "iii": return localize.day(dayOfWeek, {
				width: "abbreviated",
				context: "formatting"
			});
			case "iiiii": return localize.day(dayOfWeek, {
				width: "narrow",
				context: "formatting"
			});
			case "iiiiii": return localize.day(dayOfWeek, {
				width: "short",
				context: "formatting"
			});
			default: return localize.day(dayOfWeek, {
				width: "wide",
				context: "formatting"
			});
		}
	},
	a: function(date, token, localize) {
		const dayPeriodEnumValue = date.getHours() / 12 >= 1 ? "pm" : "am";
		switch (token) {
			case "a":
			case "aa": return localize.dayPeriod(dayPeriodEnumValue, {
				width: "abbreviated",
				context: "formatting"
			});
			case "aaa": return localize.dayPeriod(dayPeriodEnumValue, {
				width: "abbreviated",
				context: "formatting"
			}).toLowerCase();
			case "aaaaa": return localize.dayPeriod(dayPeriodEnumValue, {
				width: "narrow",
				context: "formatting"
			});
			default: return localize.dayPeriod(dayPeriodEnumValue, {
				width: "wide",
				context: "formatting"
			});
		}
	},
	b: function(date, token, localize) {
		const hours = date.getHours();
		let dayPeriodEnumValue;
		if (hours === 12) dayPeriodEnumValue = dayPeriodEnum.noon;
		else if (hours === 0) dayPeriodEnumValue = dayPeriodEnum.midnight;
		else dayPeriodEnumValue = hours / 12 >= 1 ? "pm" : "am";
		switch (token) {
			case "b":
			case "bb": return localize.dayPeriod(dayPeriodEnumValue, {
				width: "abbreviated",
				context: "formatting"
			});
			case "bbb": return localize.dayPeriod(dayPeriodEnumValue, {
				width: "abbreviated",
				context: "formatting"
			}).toLowerCase();
			case "bbbbb": return localize.dayPeriod(dayPeriodEnumValue, {
				width: "narrow",
				context: "formatting"
			});
			default: return localize.dayPeriod(dayPeriodEnumValue, {
				width: "wide",
				context: "formatting"
			});
		}
	},
	B: function(date, token, localize) {
		const hours = date.getHours();
		let dayPeriodEnumValue;
		if (hours >= 17) dayPeriodEnumValue = dayPeriodEnum.evening;
		else if (hours >= 12) dayPeriodEnumValue = dayPeriodEnum.afternoon;
		else if (hours >= 4) dayPeriodEnumValue = dayPeriodEnum.morning;
		else dayPeriodEnumValue = dayPeriodEnum.night;
		switch (token) {
			case "B":
			case "BB":
			case "BBB": return localize.dayPeriod(dayPeriodEnumValue, {
				width: "abbreviated",
				context: "formatting"
			});
			case "BBBBB": return localize.dayPeriod(dayPeriodEnumValue, {
				width: "narrow",
				context: "formatting"
			});
			default: return localize.dayPeriod(dayPeriodEnumValue, {
				width: "wide",
				context: "formatting"
			});
		}
	},
	h: function(date, token, localize) {
		if (token === "ho") {
			let hours = date.getHours() % 12;
			if (hours === 0) hours = 12;
			return localize.ordinalNumber(hours, { unit: "hour" });
		}
		return lightFormatters.h(date, token);
	},
	H: function(date, token, localize) {
		if (token === "Ho") return localize.ordinalNumber(date.getHours(), { unit: "hour" });
		return lightFormatters.H(date, token);
	},
	K: function(date, token, localize) {
		const hours = date.getHours() % 12;
		if (token === "Ko") return localize.ordinalNumber(hours, { unit: "hour" });
		return addLeadingZeros(hours, token.length);
	},
	k: function(date, token, localize) {
		let hours = date.getHours();
		if (hours === 0) hours = 24;
		if (token === "ko") return localize.ordinalNumber(hours, { unit: "hour" });
		return addLeadingZeros(hours, token.length);
	},
	m: function(date, token, localize) {
		if (token === "mo") return localize.ordinalNumber(date.getMinutes(), { unit: "minute" });
		return lightFormatters.m(date, token);
	},
	s: function(date, token, localize) {
		if (token === "so") return localize.ordinalNumber(date.getSeconds(), { unit: "second" });
		return lightFormatters.s(date, token);
	},
	S: function(date, token) {
		return lightFormatters.S(date, token);
	},
	X: function(date, token, _localize) {
		const timezoneOffset = date.getTimezoneOffset();
		if (timezoneOffset === 0) return "Z";
		switch (token) {
			case "X": return formatTimezoneWithOptionalMinutes(timezoneOffset);
			case "XXXX":
			case "XX": return formatTimezone(timezoneOffset);
			default: return formatTimezone(timezoneOffset, ":");
		}
	},
	x: function(date, token, _localize) {
		const timezoneOffset = date.getTimezoneOffset();
		switch (token) {
			case "x": return formatTimezoneWithOptionalMinutes(timezoneOffset);
			case "xxxx":
			case "xx": return formatTimezone(timezoneOffset);
			default: return formatTimezone(timezoneOffset, ":");
		}
	},
	O: function(date, token, _localize) {
		const timezoneOffset = date.getTimezoneOffset();
		switch (token) {
			case "O":
			case "OO":
			case "OOO": return "GMT" + formatTimezoneShort(timezoneOffset, ":");
			default: return "GMT" + formatTimezone(timezoneOffset, ":");
		}
	},
	z: function(date, token, _localize) {
		const timezoneOffset = date.getTimezoneOffset();
		switch (token) {
			case "z":
			case "zz":
			case "zzz": return "GMT" + formatTimezoneShort(timezoneOffset, ":");
			default: return "GMT" + formatTimezone(timezoneOffset, ":");
		}
	},
	t: function(date, token, _localize) {
		return addLeadingZeros(Math.trunc(+date / 1e3), token.length);
	},
	T: function(date, token, _localize) {
		return addLeadingZeros(+date, token.length);
	}
};
function formatTimezoneShort(offset, delimiter = "") {
	const sign = offset > 0 ? "-" : "+";
	const absOffset = Math.abs(offset);
	const hours = Math.trunc(absOffset / 60);
	const minutes = absOffset % 60;
	if (minutes === 0) return sign + String(hours);
	return sign + String(hours) + delimiter + addLeadingZeros(minutes, 2);
}
function formatTimezoneWithOptionalMinutes(offset, delimiter) {
	if (offset % 60 === 0) return (offset > 0 ? "-" : "+") + addLeadingZeros(Math.abs(offset) / 60, 2);
	return formatTimezone(offset, delimiter);
}
function formatTimezone(offset, delimiter = "") {
	const sign = offset > 0 ? "-" : "+";
	const absOffset = Math.abs(offset);
	const hours = addLeadingZeros(Math.trunc(absOffset / 60), 2);
	const minutes = addLeadingZeros(absOffset % 60, 2);
	return sign + hours + delimiter + minutes;
}
//#endregion
//#region node_modules/date-fns/_lib/format/longFormatters.js
var dateLongFormatter = (pattern, formatLong) => {
	switch (pattern) {
		case "P": return formatLong.date({ width: "short" });
		case "PP": return formatLong.date({ width: "medium" });
		case "PPP": return formatLong.date({ width: "long" });
		default: return formatLong.date({ width: "full" });
	}
};
var timeLongFormatter = (pattern, formatLong) => {
	switch (pattern) {
		case "p": return formatLong.time({ width: "short" });
		case "pp": return formatLong.time({ width: "medium" });
		case "ppp": return formatLong.time({ width: "long" });
		default: return formatLong.time({ width: "full" });
	}
};
var dateTimeLongFormatter = (pattern, formatLong) => {
	const matchResult = pattern.match(/(P+)(p+)?/) || [];
	const datePattern = matchResult[1];
	const timePattern = matchResult[2];
	if (!timePattern) return dateLongFormatter(pattern, formatLong);
	let dateTimeFormat;
	switch (datePattern) {
		case "P":
			dateTimeFormat = formatLong.dateTime({ width: "short" });
			break;
		case "PP":
			dateTimeFormat = formatLong.dateTime({ width: "medium" });
			break;
		case "PPP":
			dateTimeFormat = formatLong.dateTime({ width: "long" });
			break;
		default:
			dateTimeFormat = formatLong.dateTime({ width: "full" });
			break;
	}
	return dateTimeFormat.replace("{{date}}", dateLongFormatter(datePattern, formatLong)).replace("{{time}}", timeLongFormatter(timePattern, formatLong));
};
var longFormatters = {
	p: timeLongFormatter,
	P: dateTimeLongFormatter
};
//#endregion
//#region node_modules/date-fns/_lib/protectedTokens.js
var dayOfYearTokenRE = /^D+$/;
var weekYearTokenRE = /^Y+$/;
var throwTokens = [
	"D",
	"DD",
	"YY",
	"YYYY"
];
function isProtectedDayOfYearToken(token) {
	return dayOfYearTokenRE.test(token);
}
function isProtectedWeekYearToken(token) {
	return weekYearTokenRE.test(token);
}
function warnOrThrowProtectedError(token, format, input) {
	const _message = message(token, format, input);
	console.warn(_message);
	if (throwTokens.includes(token)) throw new RangeError(_message);
}
function message(token, format, input) {
	const subject = token[0] === "Y" ? "years" : "days of the month";
	return `Use \`${token.toLowerCase()}\` instead of \`${token}\` (in \`${format}\`) for formatting ${subject} to the input \`${input}\`; see: https://github.com/date-fns/date-fns/blob/master/docs/unicodeTokens.md`;
}
//#endregion
//#region node_modules/date-fns/isDate.js
/**
* @name isDate
* @category Common Helpers
* @summary Is the given value a date?
*
* @description
* Returns true if the given value is an instance of Date. The function works for dates transferred across iframes.
*
* @param value - The value to check
*
* @returns True if the given value is a date
*
* @example
* // For a valid date:
* const result = isDate(new Date())
* //=> true
*
* @example
* // For an invalid date:
* const result = isDate(new Date(NaN))
* //=> true
*
* @example
* // For some value:
* const result = isDate('2014-02-31')
* //=> false
*
* @example
* // For an object:
* const result = isDate({})
* //=> false
*/
function isDate(value) {
	return value instanceof Date || typeof value === "object" && Object.prototype.toString.call(value) === "[object Date]";
}
//#endregion
//#region node_modules/date-fns/isValid.js
/**
* @name isValid
* @category Common Helpers
* @summary Is the given date valid?
*
* @description
* Returns false if argument is Invalid Date and true otherwise.
* Argument is converted to Date using `toDate`. See [toDate](https://date-fns.org/docs/toDate)
* Invalid Date is a Date, whose time value is NaN.
*
* Time value of Date: http://es5.github.io/#x15.9.1.1
*
* @param date - The date to check
*
* @returns The date is valid
*
* @example
* // For the valid date:
* const result = isValid(new Date(2014, 1, 31))
* //=> true
*
* @example
* // For the value, convertible into a date:
* const result = isValid(1393804800000)
* //=> true
*
* @example
* // For the invalid date:
* const result = isValid(new Date(''))
* //=> false
*/
function isValid(date) {
	return !(!isDate(date) && typeof date !== "number" || isNaN(+toDate(date)));
}
//#endregion
//#region node_modules/date-fns/format.js
var formattingTokensRegExp$1 = /[yYQqMLwIdDecihHKkms]o|(\w)\1*|''|'(''|[^'])+('|$)|./g;
var longFormattingTokensRegExp$1 = /P+p+|P+|p+|''|'(''|[^'])+('|$)|./g;
var escapedStringRegExp$1 = /^'([^]*?)'?$/;
var doubleQuoteRegExp$1 = /''/g;
var unescapedLatinCharacterRegExp$1 = /[a-zA-Z]/;
/**
* The {@link format} function options.
*/
/**
* @name format
* @alias formatDate
* @category Common Helpers
* @summary Format the date.
*
* @description
* Return the formatted date string in the given format. The result may vary by locale.
*
* > ⚠️ Please note that the `format` tokens differ from Moment.js and other libraries.
* > See: https://github.com/date-fns/date-fns/blob/master/docs/unicodeTokens.md
*
* The characters wrapped between two single quotes characters (') are escaped.
* Two single quotes in a row, whether inside or outside a quoted sequence, represent a 'real' single quote.
* (see the last example)
*
* Format of the string is based on Unicode Technical Standard #35:
* https://www.unicode.org/reports/tr35/tr35-dates.html#Date_Field_Symbol_Table
* with a few additions (see note 7 below the table).
*
* Accepted patterns:
* | Unit                            | Pattern | Result examples                   | Notes |
* |---------------------------------|---------|-----------------------------------|-------|
* | Era                             | G..GGG  | AD, BC                            |       |
* |                                 | GGGG    | Anno Domini, Before Christ        | 2     |
* |                                 | GGGGG   | A, B                              |       |
* | Calendar year                   | y       | 44, 1, 1900, 2017                 | 5     |
* |                                 | yo      | 44th, 1st, 0th, 17th              | 5,7   |
* |                                 | yy      | 44, 01, 00, 17                    | 5     |
* |                                 | yyy     | 044, 001, 1900, 2017              | 5     |
* |                                 | yyyy    | 0044, 0001, 1900, 2017            | 5     |
* |                                 | yyyyy   | ...                               | 3,5   |
* | Local week-numbering year       | Y       | 44, 1, 1900, 2017                 | 5     |
* |                                 | Yo      | 44th, 1st, 1900th, 2017th         | 5,7   |
* |                                 | YY      | 44, 01, 00, 17                    | 5,8   |
* |                                 | YYY     | 044, 001, 1900, 2017              | 5     |
* |                                 | YYYY    | 0044, 0001, 1900, 2017            | 5,8   |
* |                                 | YYYYY   | ...                               | 3,5   |
* | ISO week-numbering year         | R       | -43, 0, 1, 1900, 2017             | 5,7   |
* |                                 | RR      | -43, 00, 01, 1900, 2017           | 5,7   |
* |                                 | RRR     | -043, 000, 001, 1900, 2017        | 5,7   |
* |                                 | RRRR    | -0043, 0000, 0001, 1900, 2017     | 5,7   |
* |                                 | RRRRR   | ...                               | 3,5,7 |
* | Extended year                   | u       | -43, 0, 1, 1900, 2017             | 5     |
* |                                 | uu      | -43, 01, 1900, 2017               | 5     |
* |                                 | uuu     | -043, 001, 1900, 2017             | 5     |
* |                                 | uuuu    | -0043, 0001, 1900, 2017           | 5     |
* |                                 | uuuuu   | ...                               | 3,5   |
* | Quarter (formatting)            | Q       | 1, 2, 3, 4                        |       |
* |                                 | Qo      | 1st, 2nd, 3rd, 4th                | 7     |
* |                                 | QQ      | 01, 02, 03, 04                    |       |
* |                                 | QQQ     | Q1, Q2, Q3, Q4                    |       |
* |                                 | QQQQ    | 1st quarter, 2nd quarter, ...     | 2     |
* |                                 | QQQQQ   | 1, 2, 3, 4                        | 4     |
* | Quarter (stand-alone)           | q       | 1, 2, 3, 4                        |       |
* |                                 | qo      | 1st, 2nd, 3rd, 4th                | 7     |
* |                                 | qq      | 01, 02, 03, 04                    |       |
* |                                 | qqq     | Q1, Q2, Q3, Q4                    |       |
* |                                 | qqqq    | 1st quarter, 2nd quarter, ...     | 2     |
* |                                 | qqqqq   | 1, 2, 3, 4                        | 4     |
* | Month (formatting)              | M       | 1, 2, ..., 12                     |       |
* |                                 | Mo      | 1st, 2nd, ..., 12th               | 7     |
* |                                 | MM      | 01, 02, ..., 12                   |       |
* |                                 | MMM     | Jan, Feb, ..., Dec                |       |
* |                                 | MMMM    | January, February, ..., December  | 2     |
* |                                 | MMMMM   | J, F, ..., D                      |       |
* | Month (stand-alone)             | L       | 1, 2, ..., 12                     |       |
* |                                 | Lo      | 1st, 2nd, ..., 12th               | 7     |
* |                                 | LL      | 01, 02, ..., 12                   |       |
* |                                 | LLL     | Jan, Feb, ..., Dec                |       |
* |                                 | LLLL    | January, February, ..., December  | 2     |
* |                                 | LLLLL   | J, F, ..., D                      |       |
* | Local week of year              | w       | 1, 2, ..., 53                     |       |
* |                                 | wo      | 1st, 2nd, ..., 53th               | 7     |
* |                                 | ww      | 01, 02, ..., 53                   |       |
* | ISO week of year                | I       | 1, 2, ..., 53                     | 7     |
* |                                 | Io      | 1st, 2nd, ..., 53th               | 7     |
* |                                 | II      | 01, 02, ..., 53                   | 7     |
* | Day of month                    | d       | 1, 2, ..., 31                     |       |
* |                                 | do      | 1st, 2nd, ..., 31st               | 7     |
* |                                 | dd      | 01, 02, ..., 31                   |       |
* | Day of year                     | D       | 1, 2, ..., 365, 366               | 9     |
* |                                 | Do      | 1st, 2nd, ..., 365th, 366th       | 7     |
* |                                 | DD      | 01, 02, ..., 365, 366             | 9     |
* |                                 | DDD     | 001, 002, ..., 365, 366           |       |
* |                                 | DDDD    | ...                               | 3     |
* | Day of week (formatting)        | E..EEE  | Mon, Tue, Wed, ..., Sun           |       |
* |                                 | EEEE    | Monday, Tuesday, ..., Sunday      | 2     |
* |                                 | EEEEE   | M, T, W, T, F, S, S               |       |
* |                                 | EEEEEE  | Mo, Tu, We, Th, Fr, Sa, Su        |       |
* | ISO day of week (formatting)    | i       | 1, 2, 3, ..., 7                   | 7     |
* |                                 | io      | 1st, 2nd, ..., 7th                | 7     |
* |                                 | ii      | 01, 02, ..., 07                   | 7     |
* |                                 | iii     | Mon, Tue, Wed, ..., Sun           | 7     |
* |                                 | iiii    | Monday, Tuesday, ..., Sunday      | 2,7   |
* |                                 | iiiii   | M, T, W, T, F, S, S               | 7     |
* |                                 | iiiiii  | Mo, Tu, We, Th, Fr, Sa, Su        | 7     |
* | Local day of week (formatting)  | e       | 2, 3, 4, ..., 1                   |       |
* |                                 | eo      | 2nd, 3rd, ..., 1st                | 7     |
* |                                 | ee      | 02, 03, ..., 01                   |       |
* |                                 | eee     | Mon, Tue, Wed, ..., Sun           |       |
* |                                 | eeee    | Monday, Tuesday, ..., Sunday      | 2     |
* |                                 | eeeee   | M, T, W, T, F, S, S               |       |
* |                                 | eeeeee  | Mo, Tu, We, Th, Fr, Sa, Su        |       |
* | Local day of week (stand-alone) | c       | 2, 3, 4, ..., 1                   |       |
* |                                 | co      | 2nd, 3rd, ..., 1st                | 7     |
* |                                 | cc      | 02, 03, ..., 01                   |       |
* |                                 | ccc     | Mon, Tue, Wed, ..., Sun           |       |
* |                                 | cccc    | Monday, Tuesday, ..., Sunday      | 2     |
* |                                 | ccccc   | M, T, W, T, F, S, S               |       |
* |                                 | cccccc  | Mo, Tu, We, Th, Fr, Sa, Su        |       |
* | AM, PM                          | a..aa   | AM, PM                            |       |
* |                                 | aaa     | am, pm                            |       |
* |                                 | aaaa    | a.m., p.m.                        | 2     |
* |                                 | aaaaa   | a, p                              |       |
* | AM, PM, noon, midnight          | b..bb   | AM, PM, noon, midnight            |       |
* |                                 | bbb     | am, pm, noon, midnight            |       |
* |                                 | bbbb    | a.m., p.m., noon, midnight        | 2     |
* |                                 | bbbbb   | a, p, n, mi                       |       |
* | Flexible day period             | B..BBB  | at night, in the morning, ...     |       |
* |                                 | BBBB    | at night, in the morning, ...     | 2     |
* |                                 | BBBBB   | at night, in the morning, ...     |       |
* | Hour [1-12]                     | h       | 1, 2, ..., 11, 12                 |       |
* |                                 | ho      | 1st, 2nd, ..., 11th, 12th         | 7     |
* |                                 | hh      | 01, 02, ..., 11, 12               |       |
* | Hour [0-23]                     | H       | 0, 1, 2, ..., 23                  |       |
* |                                 | Ho      | 0th, 1st, 2nd, ..., 23rd          | 7     |
* |                                 | HH      | 00, 01, 02, ..., 23               |       |
* | Hour [0-11]                     | K       | 1, 2, ..., 11, 0                  |       |
* |                                 | Ko      | 1st, 2nd, ..., 11th, 0th          | 7     |
* |                                 | KK      | 01, 02, ..., 11, 00               |       |
* | Hour [1-24]                     | k       | 24, 1, 2, ..., 23                 |       |
* |                                 | ko      | 24th, 1st, 2nd, ..., 23rd         | 7     |
* |                                 | kk      | 24, 01, 02, ..., 23               |       |
* | Minute                          | m       | 0, 1, ..., 59                     |       |
* |                                 | mo      | 0th, 1st, ..., 59th               | 7     |
* |                                 | mm      | 00, 01, ..., 59                   |       |
* | Second                          | s       | 0, 1, ..., 59                     |       |
* |                                 | so      | 0th, 1st, ..., 59th               | 7     |
* |                                 | ss      | 00, 01, ..., 59                   |       |
* | Fraction of second              | S       | 0, 1, ..., 9                      |       |
* |                                 | SS      | 00, 01, ..., 99                   |       |
* |                                 | SSS     | 000, 001, ..., 999                |       |
* |                                 | SSSS    | ...                               | 3     |
* | Timezone (ISO-8601 w/ Z)        | X       | -08, +0530, Z                     |       |
* |                                 | XX      | -0800, +0530, Z                   |       |
* |                                 | XXX     | -08:00, +05:30, Z                 |       |
* |                                 | XXXX    | -0800, +0530, Z, +123456          | 2     |
* |                                 | XXXXX   | -08:00, +05:30, Z, +12:34:56      |       |
* | Timezone (ISO-8601 w/o Z)       | x       | -08, +0530, +00                   |       |
* |                                 | xx      | -0800, +0530, +0000               |       |
* |                                 | xxx     | -08:00, +05:30, +00:00            | 2     |
* |                                 | xxxx    | -0800, +0530, +0000, +123456      |       |
* |                                 | xxxxx   | -08:00, +05:30, +00:00, +12:34:56 |       |
* | Timezone (GMT)                  | O...OOO | GMT-8, GMT+5:30, GMT+0            |       |
* |                                 | OOOO    | GMT-08:00, GMT+05:30, GMT+00:00   | 2     |
* | Timezone (specific non-locat.)  | z...zzz | GMT-8, GMT+5:30, GMT+0            | 6     |
* |                                 | zzzz    | GMT-08:00, GMT+05:30, GMT+00:00   | 2,6   |
* | Seconds timestamp               | t       | 512969520                         | 7     |
* |                                 | tt      | ...                               | 3,7   |
* | Milliseconds timestamp          | T       | 512969520900                      | 7     |
* |                                 | TT      | ...                               | 3,7   |
* | Long localized date             | P       | 04/29/1453                        | 7     |
* |                                 | PP      | Apr 29, 1453                      | 7     |
* |                                 | PPP     | April 29th, 1453                  | 7     |
* |                                 | PPPP    | Friday, April 29th, 1453          | 2,7   |
* | Long localized time             | p       | 12:00 AM                          | 7     |
* |                                 | pp      | 12:00:00 AM                       | 7     |
* |                                 | ppp     | 12:00:00 AM GMT+2                 | 7     |
* |                                 | pppp    | 12:00:00 AM GMT+02:00             | 2,7   |
* | Combination of date and time    | Pp      | 04/29/1453, 12:00 AM              | 7     |
* |                                 | PPpp    | Apr 29, 1453, 12:00:00 AM         | 7     |
* |                                 | PPPppp  | April 29th, 1453 at ...           | 7     |
* |                                 | PPPPpppp| Friday, April 29th, 1453 at ...   | 2,7   |
* Notes:
* 1. "Formatting" units (e.g. formatting quarter) in the default en-US locale
*    are the same as "stand-alone" units, but are different in some languages.
*    "Formatting" units are declined according to the rules of the language
*    in the context of a date. "Stand-alone" units are always nominative singular:
*
*    `format(new Date(2017, 10, 6), 'do LLLL', {locale: cs}) //=> '6. listopad'`
*
*    `format(new Date(2017, 10, 6), 'do MMMM', {locale: cs}) //=> '6. listopadu'`
*
* 2. Any sequence of the identical letters is a pattern, unless it is escaped by
*    the single quote characters (see below).
*    If the sequence is longer than listed in table (e.g. `EEEEEEEEEEE`)
*    the output will be the same as default pattern for this unit, usually
*    the longest one (in case of ISO weekdays, `EEEE`). Default patterns for units
*    are marked with "2" in the last column of the table.
*
*    `format(new Date(2017, 10, 6), 'MMM') //=> 'Nov'`
*
*    `format(new Date(2017, 10, 6), 'MMMM') //=> 'November'`
*
*    `format(new Date(2017, 10, 6), 'MMMMM') //=> 'N'`
*
*    `format(new Date(2017, 10, 6), 'MMMMMM') //=> 'November'`
*
*    `format(new Date(2017, 10, 6), 'MMMMMMM') //=> 'November'`
*
* 3. Some patterns could be unlimited length (such as `yyyyyyyy`).
*    The output will be padded with zeros to match the length of the pattern.
*
*    `format(new Date(2017, 10, 6), 'yyyyyyyy') //=> '00002017'`
*
* 4. `QQQQQ` and `qqqqq` could be not strictly numerical in some locales.
*    These tokens represent the shortest form of the quarter.
*
* 5. The main difference between `y` and `u` patterns are B.C. years:
*
*    | Year | `y` | `u` |
*    |------|-----|-----|
*    | AC 1 |   1 |   1 |
*    | BC 1 |   1 |   0 |
*    | BC 2 |   2 |  -1 |
*
*    Also `yy` always returns the last two digits of a year,
*    while `uu` pads single digit years to 2 characters and returns other years unchanged:
*
*    | Year | `yy` | `uu` |
*    |------|------|------|
*    | 1    |   01 |   01 |
*    | 14   |   14 |   14 |
*    | 376  |   76 |  376 |
*    | 1453 |   53 | 1453 |
*
*    The same difference is true for local and ISO week-numbering years (`Y` and `R`),
*    except local week-numbering years are dependent on `options.weekStartsOn`
*    and `options.firstWeekContainsDate` (compare [getISOWeekYear](https://date-fns.org/docs/getISOWeekYear)
*    and [getWeekYear](https://date-fns.org/docs/getWeekYear)).
*
* 6. Specific non-location timezones are currently unavailable in `date-fns`,
*    so right now these tokens fall back to GMT timezones.
*
* 7. These patterns are not in the Unicode Technical Standard #35:
*    - `i`: ISO day of week
*    - `I`: ISO week of year
*    - `R`: ISO week-numbering year
*    - `t`: seconds timestamp
*    - `T`: milliseconds timestamp
*    - `o`: ordinal number modifier
*    - `P`: long localized date
*    - `p`: long localized time
*
* 8. `YY` and `YYYY` tokens represent week-numbering years but they are often confused with years.
*    You should enable `options.useAdditionalWeekYearTokens` to use them. See: https://github.com/date-fns/date-fns/blob/master/docs/unicodeTokens.md
*
* 9. `D` and `DD` tokens represent days of the year but they are often confused with days of the month.
*    You should enable `options.useAdditionalDayOfYearTokens` to use them. See: https://github.com/date-fns/date-fns/blob/master/docs/unicodeTokens.md
*
* @param date - The original date
* @param format - The string of tokens
* @param options - An object with options
*
* @returns The formatted date string
*
* @throws `date` must not be Invalid Date
* @throws `options.locale` must contain `localize` property
* @throws `options.locale` must contain `formatLong` property
* @throws use `yyyy` instead of `YYYY` for formatting years using [format provided] to the input [input provided]; see: https://github.com/date-fns/date-fns/blob/master/docs/unicodeTokens.md
* @throws use `yy` instead of `YY` for formatting years using [format provided] to the input [input provided]; see: https://github.com/date-fns/date-fns/blob/master/docs/unicodeTokens.md
* @throws use `d` instead of `D` for formatting days of the month using [format provided] to the input [input provided]; see: https://github.com/date-fns/date-fns/blob/master/docs/unicodeTokens.md
* @throws use `dd` instead of `DD` for formatting days of the month using [format provided] to the input [input provided]; see: https://github.com/date-fns/date-fns/blob/master/docs/unicodeTokens.md
* @throws format string contains an unescaped latin alphabet character
*
* @example
* // Represent 11 February 2014 in middle-endian format:
* const result = format(new Date(2014, 1, 11), 'MM/dd/yyyy')
* //=> '02/11/2014'
*
* @example
* // Represent 2 July 2014 in Esperanto:
* import { eoLocale } from 'date-fns/locale/eo'
* const result = format(new Date(2014, 6, 2), "do 'de' MMMM yyyy", {
*   locale: eoLocale
* })
* //=> '2-a de julio 2014'
*
* @example
* // Escape string by single quote characters:
* const result = format(new Date(2014, 6, 2, 15), "h 'o''clock'")
* //=> "3 o'clock"
*/
function format(date, formatStr, options) {
	const defaultOptions = getDefaultOptions$1();
	const locale = options?.locale ?? defaultOptions.locale ?? enUS$1;
	const firstWeekContainsDate = options?.firstWeekContainsDate ?? options?.locale?.options?.firstWeekContainsDate ?? defaultOptions.firstWeekContainsDate ?? defaultOptions.locale?.options?.firstWeekContainsDate ?? 1;
	const weekStartsOn = options?.weekStartsOn ?? options?.locale?.options?.weekStartsOn ?? defaultOptions.weekStartsOn ?? defaultOptions.locale?.options?.weekStartsOn ?? 0;
	const originalDate = toDate(date, options?.in);
	if (!isValid(originalDate)) throw new RangeError("Invalid time value");
	let parts = formatStr.match(longFormattingTokensRegExp$1).map((substring) => {
		const firstCharacter = substring[0];
		if (firstCharacter === "p" || firstCharacter === "P") {
			const longFormatter = longFormatters[firstCharacter];
			return longFormatter(substring, locale.formatLong);
		}
		return substring;
	}).join("").match(formattingTokensRegExp$1).map((substring) => {
		if (substring === "''") return {
			isToken: false,
			value: "'"
		};
		const firstCharacter = substring[0];
		if (firstCharacter === "'") return {
			isToken: false,
			value: cleanEscapedString$1(substring)
		};
		if (formatters[firstCharacter]) return {
			isToken: true,
			value: substring
		};
		if (firstCharacter.match(unescapedLatinCharacterRegExp$1)) throw new RangeError("Format string contains an unescaped latin alphabet character `" + firstCharacter + "`");
		return {
			isToken: false,
			value: substring
		};
	});
	if (locale.localize.preprocessor) parts = locale.localize.preprocessor(originalDate, parts);
	const formatterOptions = {
		firstWeekContainsDate,
		weekStartsOn,
		locale
	};
	return parts.map((part) => {
		if (!part.isToken) return part.value;
		const token = part.value;
		if (!options?.useAdditionalWeekYearTokens && isProtectedWeekYearToken(token) || !options?.useAdditionalDayOfYearTokens && isProtectedDayOfYearToken(token)) warnOrThrowProtectedError(token, formatStr, String(date));
		const formatter = formatters[token[0]];
		return formatter(originalDate, token, locale.localize, formatterOptions);
	}).join("");
}
function cleanEscapedString$1(input) {
	const matched = input.match(escapedStringRegExp$1);
	if (!matched) return input;
	return matched[1].replace(doubleQuoteRegExp$1, "'");
}
//#endregion
//#region node_modules/date-fns/getDate.js
/**
* The {@link getDate} function options.
*/
/**
* @name getDate
* @category Day Helpers
* @summary Get the day of the month of the given date.
*
* @description
* Get the day of the month of the given date.
*
* @param date - The given date
* @param options - An object with options.
*
* @returns The day of month
*
* @example
* // Which day of the month is 29 February 2012?
* const result = getDate(new Date(2012, 1, 29))
* //=> 29
*/
function getDate(date, options) {
	return toDate(date, options?.in).getDate();
}
//#endregion
//#region node_modules/date-fns/getDay.js
/**
* The {@link getDay} function options.
*/
/**
* @name getDay
* @category Weekday Helpers
* @summary Get the day of the week of the given date.
*
* @description
* Get the day of the week of the given date.
*
* @param date - The given date
* @param options - The options
*
* @returns The day of week, 0 represents Sunday
*
* @example
* // Which day of the week is 29 February 2012?
* const result = getDay(new Date(2012, 1, 29))
* //=> 3
*/
function getDay(date, options) {
	return toDate(date, options?.in).getDay();
}
//#endregion
//#region node_modules/date-fns/getDaysInMonth.js
/**
* The {@link getDaysInMonth} function options.
*/
/**
* @name getDaysInMonth
* @category Month Helpers
* @summary Get the number of days in a month of the given date.
*
* @description
* Get the number of days in a month of the given date, considering the context if provided.
*
* @param date - The given date
* @param options - An object with options
*
* @returns The number of days in a month
*
* @example
* // How many days are in February 2000?
* const result = getDaysInMonth(new Date(2000, 1))
* //=> 29
*/
function getDaysInMonth(date, options) {
	const _date = toDate(date, options?.in);
	const year = _date.getFullYear();
	const monthIndex = _date.getMonth();
	const lastDayOfMonth = constructFrom(_date, 0);
	lastDayOfMonth.setFullYear(year, monthIndex + 1, 0);
	lastDayOfMonth.setHours(0, 0, 0, 0);
	return lastDayOfMonth.getDate();
}
//#endregion
//#region node_modules/date-fns/getHours.js
/**
* The {@link getHours} function options.
*/
/**
* @name getHours
* @category Hour Helpers
* @summary Get the hours of the given date.
*
* @description
* Get the hours of the given date.
*
* @param date - The given date
* @param options - An object with options
*
* @returns The hours
*
* @example
* // Get the hours of 29 February 2012 11:45:00:
* const result = getHours(new Date(2012, 1, 29, 11, 45))
* //=> 11
*/
function getHours(date, options) {
	return toDate(date, options?.in).getHours();
}
//#endregion
//#region node_modules/date-fns/getMilliseconds.js
/**
* @name getMilliseconds
* @category Millisecond Helpers
* @summary Get the milliseconds of the given date.
*
* @description
* Get the milliseconds of the given date.
*
* @param date - The given date
*
* @returns The milliseconds
*
* @example
* // Get the milliseconds of 29 February 2012 11:45:05.123:
* const result = getMilliseconds(new Date(2012, 1, 29, 11, 45, 5, 123))
* //=> 123
*/
function getMilliseconds(date) {
	return toDate(date).getMilliseconds();
}
//#endregion
//#region node_modules/date-fns/getMinutes.js
/**
* The {@link getMinutes} function options.
*/
/**
* @name getMinutes
* @category Minute Helpers
* @summary Get the minutes of the given date.
*
* @description
* Get the minutes of the given date.
*
* @param date - The given date
* @param options - The options
*
* @returns The minutes
*
* @example
* // Get the minutes of 29 February 2012 11:45:05:
* const result = getMinutes(new Date(2012, 1, 29, 11, 45, 5))
* //=> 45
*/
function getMinutes(date, options) {
	return toDate(date, options?.in).getMinutes();
}
//#endregion
//#region node_modules/date-fns/getMonth.js
/**
* The {@link getMonth} function options.
*/
/**
* @name getMonth
* @category Month Helpers
* @summary Get the month of the given date.
*
* @description
* Get the month of the given date.
*
* @param date - The given date
* @param options - An object with options
*
* @returns The month index (0-11)
*
* @example
* // Which month is 29 February 2012?
* const result = getMonth(new Date(2012, 1, 29))
* //=> 1
*/
function getMonth(date, options) {
	return toDate(date, options?.in).getMonth();
}
//#endregion
//#region node_modules/date-fns/getSeconds.js
/**
* @name getSeconds
* @category Second Helpers
* @summary Get the seconds of the given date.
*
* @description
* Get the seconds of the given date.
*
* @param date - The given date
*
* @returns The seconds
*
* @example
* // Get the seconds of 29 February 2012 11:45:05.123:
* const result = getSeconds(new Date(2012, 1, 29, 11, 45, 5, 123))
* //=> 5
*/
function getSeconds(date) {
	return toDate(date).getSeconds();
}
//#endregion
//#region node_modules/date-fns/getYear.js
/**
* The {@link getYear} function options.
*/
/**
* @name getYear
* @category Year Helpers
* @summary Get the year of the given date.
*
* @description
* Get the year of the given date.
*
* @param date - The given date
* @param options - An object with options
*
* @returns The year
*
* @example
* // Which year is 2 July 2014?
* const result = getYear(new Date(2014, 6, 2))
* //=> 2014
*/
function getYear(date, options) {
	return toDate(date, options?.in).getFullYear();
}
//#endregion
//#region node_modules/date-fns/isAfter.js
/**
* @name isAfter
* @category Common Helpers
* @summary Is the first date after the second one?
*
* @description
* Is the first date after the second one?
*
* @param date - The date that should be after the other one to return true
* @param dateToCompare - The date to compare with
*
* @returns The first date is after the second date
*
* @example
* // Is 10 July 1989 after 11 February 1987?
* const result = isAfter(new Date(1989, 6, 10), new Date(1987, 1, 11))
* //=> true
*/
function isAfter(date, dateToCompare) {
	return +toDate(date) > +toDate(dateToCompare);
}
//#endregion
//#region node_modules/date-fns/isBefore.js
/**
* @name isBefore
* @category Common Helpers
* @summary Is the first date before the second one?
*
* @description
* Is the first date before the second one?
*
* @param date - The date that should be before the other one to return true
* @param dateToCompare - The date to compare with
*
* @returns The first date is before the second date
*
* @example
* // Is 10 July 1989 before 11 February 1987?
* const result = isBefore(new Date(1989, 6, 10), new Date(1987, 1, 11))
* //=> false
*/
function isBefore(date, dateToCompare) {
	return +toDate(date) < +toDate(dateToCompare);
}
//#endregion
//#region node_modules/date-fns/isEqual.js
/**
* @name isEqual
* @category Common Helpers
* @summary Are the given dates equal?
*
* @description
* Are the given dates equal?
*
* @param dateLeft - The first date to compare
* @param dateRight - The second date to compare
*
* @returns The dates are equal
*
* @example
* // Are 2 July 2014 06:30:45.000 and 2 July 2014 06:30:45.500 equal?
* const result = isEqual(
*   new Date(2014, 6, 2, 6, 30, 45, 0),
*   new Date(2014, 6, 2, 6, 30, 45, 500)
* )
* //=> false
*/
function isEqual(leftDate, rightDate) {
	return +toDate(leftDate) === +toDate(rightDate);
}
//#endregion
//#region node_modules/date-fns/isSameDay.js
/**
* The {@link isSameDay} function options.
*/
/**
* @name isSameDay
* @category Day Helpers
* @summary Are the given dates in the same day (and year and month)?
*
* @description
* Are the given dates in the same day (and year and month)?
*
* @param laterDate - The first date to check
* @param earlierDate - The second date to check
* @param options - An object with options
*
* @returns The dates are in the same day (and year and month)
*
* @example
* // Are 4 September 06:00:00 and 4 September 18:00:00 in the same day?
* const result = isSameDay(new Date(2014, 8, 4, 6, 0), new Date(2014, 8, 4, 18, 0))
* //=> true
*
* @example
* // Are 4 September and 4 October in the same day?
* const result = isSameDay(new Date(2014, 8, 4), new Date(2014, 9, 4))
* //=> false
*
* @example
* // Are 4 September, 2014 and 4 September, 2015 in the same day?
* const result = isSameDay(new Date(2014, 8, 4), new Date(2015, 8, 4))
* //=> false
*/
function isSameDay(laterDate, earlierDate, options) {
	const [dateLeft_, dateRight_] = normalizeDates(options?.in, laterDate, earlierDate);
	return +startOfDay(dateLeft_) === +startOfDay(dateRight_);
}
//#endregion
//#region node_modules/date-fns/startOfHour.js
/**
* The {@link startOfHour} function options.
*/
/**
* @name startOfHour
* @category Hour Helpers
* @summary Return the start of an hour for the given date.
*
* @description
* Return the start of an hour for the given date.
* The result will be in the local timezone.
*
* @typeParam DateType - The `Date` type, the function operates on. Gets inferred from passed arguments. Allows to use extensions like [`UTCDate`](https://github.com/date-fns/utc).
* @typeParam ResultDate - The result `Date` type, it is the type returned from the context function if it is passed, or inferred from the arguments.
*
* @param date - The original date
* @param options - An object with options
*
* @returns The start of an hour
*
* @example
* // The start of an hour for 2 September 2014 11:55:00:
* const result = startOfHour(new Date(2014, 8, 2, 11, 55))
* //=> Tue Sep 02 2014 11:00:00
*/
function startOfHour(date, options) {
	const _date = toDate(date, options?.in);
	_date.setMinutes(0, 0, 0);
	return _date;
}
//#endregion
//#region node_modules/date-fns/isSameHour.js
/**
* The {@link isSameHour} function options.
*/
/**
* @name isSameHour
* @category Hour Helpers
* @summary Are the given dates in the same hour (and same day)?
*
* @description
* Are the given dates in the same hour (and same day)?
*
* @param dateLeft - The first date to check
* @param dateRight - The second date to check
* @param options - An object with options
*
* @returns The dates are in the same hour (and same day)
*
* @example
* // Are 4 September 2014 06:00:00 and 4 September 06:30:00 in the same hour?
* const result = isSameHour(new Date(2014, 8, 4, 6, 0), new Date(2014, 8, 4, 6, 30))
* //=> true
*
* @example
* // Are 4 September 2014 06:00:00 and 5 September 06:00:00 in the same hour?
* const result = isSameHour(new Date(2014, 8, 4, 6, 0), new Date(2014, 8, 5, 6, 0))
* //=> false
*/
function isSameHour(dateLeft, dateRight, options) {
	const [dateLeft_, dateRight_] = normalizeDates(options?.in, dateLeft, dateRight);
	return +startOfHour(dateLeft_) === +startOfHour(dateRight_);
}
//#endregion
//#region node_modules/date-fns/isSameYear.js
/**
* The {@link isSameYear} function options.
*/
/**
* @name isSameYear
* @category Year Helpers
* @summary Are the given dates in the same year?
*
* @description
* Are the given dates in the same year?
*
* @param laterDate - The first date to check
* @param earlierDate - The second date to check
* @param options - An object with options
*
* @returns The dates are in the same year
*
* @example
* // Are 2 September 2014 and 25 September 2014 in the same year?
* const result = isSameYear(new Date(2014, 8, 2), new Date(2014, 8, 25))
* //=> true
*/
function isSameYear(laterDate, earlierDate, options) {
	const [laterDate_, earlierDate_] = normalizeDates(options?.in, laterDate, earlierDate);
	return laterDate_.getFullYear() === earlierDate_.getFullYear();
}
//#endregion
//#region node_modules/date-fns/isSameMonth.js
/**
* The {@link isSameMonth} function options.
*/
/**
* @name isSameMonth
* @category Month Helpers
* @summary Are the given dates in the same month (and year)?
*
* @description
* Are the given dates in the same month (and year)?
*
* @param laterDate - The first date to check
* @param earlierDate - The second date to check
* @param options - An object with options
*
* @returns The dates are in the same month (and year)
*
* @example
* // Are 2 September 2014 and 25 September 2014 in the same month?
* const result = isSameMonth(new Date(2014, 8, 2), new Date(2014, 8, 25))
* //=> true
*
* @example
* // Are 2 September 2014 and 25 September 2015 in the same month?
* const result = isSameMonth(new Date(2014, 8, 2), new Date(2015, 8, 25))
* //=> false
*/
function isSameMonth(laterDate, earlierDate, options) {
	const [laterDate_, earlierDate_] = normalizeDates(options?.in, laterDate, earlierDate);
	return laterDate_.getFullYear() === earlierDate_.getFullYear() && laterDate_.getMonth() === earlierDate_.getMonth();
}
//#endregion
//#region node_modules/date-fns/isWithinInterval.js
/**
* The {@link isWithinInterval} function options.
*/
/**
* @name isWithinInterval
* @category Interval Helpers
* @summary Is the given date within the interval?
*
* @description
* Is the given date within the interval? (Including start and end.)
*
* @param date - The date to check
* @param interval - The interval to check
* @param options - An object with options
*
* @returns The date is within the interval
*
* @example
* // For the date within the interval:
* isWithinInterval(new Date(2014, 0, 3), {
*   start: new Date(2014, 0, 1),
*   end: new Date(2014, 0, 7)
* })
* // => true
*
* @example
* // For the date outside of the interval:
* isWithinInterval(new Date(2014, 0, 10), {
*   start: new Date(2014, 0, 1),
*   end: new Date(2014, 0, 7)
* })
* // => false
*
* @example
* // For date equal to the interval start:
* isWithinInterval(date, { start, end: date })
* // => true
*
* @example
* // For date equal to the interval end:
* isWithinInterval(date, { start: date, end })
* // => true
*/
function isWithinInterval(date, interval, options) {
	const time = +toDate(date, options?.in);
	const [startTime, endTime] = [+toDate(interval.start, options?.in), +toDate(interval.end, options?.in)].sort((a, b) => a - b);
	return time >= startTime && time <= endTime;
}
//#endregion
//#region node_modules/date-fns/getDefaultOptions.js
/**
* @name getDefaultOptions
* @category Common Helpers
* @summary Get default options.
* @pure false
*
* @description
* Returns an object that contains defaults for
* `options.locale`, `options.weekStartsOn` and `options.firstWeekContainsDate`
* arguments for all functions.
*
* You can change these with [setDefaultOptions](https://date-fns.org/docs/setDefaultOptions).
*
* @returns The default options
*
* @example
* const result = getDefaultOptions()
* //=> {}
*
* @example
* setDefaultOptions({ weekStarsOn: 1, firstWeekContainsDate: 4 })
* const result = getDefaultOptions()
* //=> { weekStarsOn: 1, firstWeekContainsDate: 4 }
*/
function getDefaultOptions() {
	return Object.assign({}, getDefaultOptions$1());
}
//#endregion
//#region node_modules/date-fns/transpose.js
/**
* @name transpose
* @category Generic Helpers
* @summary Transpose the date to the given constructor.
*
* @description
* The function transposes the date to the given constructor. It helps you
* to transpose the date in the system time zone to say `UTCDate` or any other
* date extension.
*
* @typeParam InputDate - The input `Date` type derived from the passed argument.
* @typeParam ResultDate - The result `Date` type derived from the passed constructor.
*
* @param date - The date to use values from
* @param constructor - The date constructor to use
*
* @returns Date transposed to the given constructor
*
* @example
* // Create July 10, 2022 00:00 in locale time zone
* const date = new Date(2022, 6, 10)
* //=> 'Sun Jul 10 2022 00:00:00 GMT+0800 (Singapore Standard Time)'
*
* @example
* // Transpose the date to July 10, 2022 00:00 in UTC
* transpose(date, UTCDate)
* //=> 'Sun Jul 10 2022 00:00:00 GMT+0000 (Coordinated Universal Time)'
*/
function transpose(date, constructor) {
	const date_ = isConstructor(constructor) ? new constructor(0) : constructFrom(constructor, 0);
	date_.setFullYear(date.getFullYear(), date.getMonth(), date.getDate());
	date_.setHours(date.getHours(), date.getMinutes(), date.getSeconds(), date.getMilliseconds());
	return date_;
}
function isConstructor(constructor) {
	return typeof constructor === "function" && constructor.prototype?.constructor === constructor;
}
//#endregion
//#region node_modules/date-fns/parse/_lib/Setter.js
var TIMEZONE_UNIT_PRIORITY = 10;
var Setter = class {
	subPriority = 0;
	validate(_utcDate, _options) {
		return true;
	}
};
var ValueSetter = class extends Setter {
	constructor(value, validateValue, setValue, priority, subPriority) {
		super();
		this.value = value;
		this.validateValue = validateValue;
		this.setValue = setValue;
		this.priority = priority;
		if (subPriority) this.subPriority = subPriority;
	}
	validate(date, options) {
		return this.validateValue(date, this.value, options);
	}
	set(date, flags, options) {
		return this.setValue(date, flags, this.value, options);
	}
};
var DateTimezoneSetter = class extends Setter {
	priority = TIMEZONE_UNIT_PRIORITY;
	subPriority = -1;
	constructor(context, reference) {
		super();
		this.context = context || ((date) => constructFrom(reference, date));
	}
	set(date, flags) {
		if (flags.timestampIsSet) return date;
		return constructFrom(date, transpose(date, this.context));
	}
};
//#endregion
//#region node_modules/date-fns/parse/_lib/Parser.js
var Parser = class {
	run(dateString, token, match, options) {
		const result = this.parse(dateString, token, match, options);
		if (!result) return null;
		return {
			setter: new ValueSetter(result.value, this.validate, this.set, this.priority, this.subPriority),
			rest: result.rest
		};
	}
	validate(_utcDate, _value, _options) {
		return true;
	}
};
//#endregion
//#region node_modules/date-fns/parse/_lib/parsers/EraParser.js
var EraParser = class extends Parser {
	priority = 140;
	parse(dateString, token, match) {
		switch (token) {
			case "G":
			case "GG":
			case "GGG": return match.era(dateString, { width: "abbreviated" }) || match.era(dateString, { width: "narrow" });
			case "GGGGG": return match.era(dateString, { width: "narrow" });
			default: return match.era(dateString, { width: "wide" }) || match.era(dateString, { width: "abbreviated" }) || match.era(dateString, { width: "narrow" });
		}
	}
	set(date, flags, value) {
		flags.era = value;
		date.setFullYear(value, 0, 1);
		date.setHours(0, 0, 0, 0);
		return date;
	}
	incompatibleTokens = [
		"R",
		"u",
		"t",
		"T"
	];
};
//#endregion
//#region node_modules/date-fns/parse/_lib/constants.js
var numericPatterns = {
	month: /^(1[0-2]|0?\d)/,
	date: /^(3[0-1]|[0-2]?\d)/,
	dayOfYear: /^(36[0-6]|3[0-5]\d|[0-2]?\d?\d)/,
	week: /^(5[0-3]|[0-4]?\d)/,
	hour23h: /^(2[0-3]|[0-1]?\d)/,
	hour24h: /^(2[0-4]|[0-1]?\d)/,
	hour11h: /^(1[0-1]|0?\d)/,
	hour12h: /^(1[0-2]|0?\d)/,
	minute: /^[0-5]?\d/,
	second: /^[0-5]?\d/,
	singleDigit: /^\d/,
	twoDigits: /^\d{1,2}/,
	threeDigits: /^\d{1,3}/,
	fourDigits: /^\d{1,4}/,
	anyDigitsSigned: /^-?\d+/,
	singleDigitSigned: /^-?\d/,
	twoDigitsSigned: /^-?\d{1,2}/,
	threeDigitsSigned: /^-?\d{1,3}/,
	fourDigitsSigned: /^-?\d{1,4}/
};
var timezonePatterns = {
	basicOptionalMinutes: /^([+-])(\d{2})(\d{2})?|Z/,
	basic: /^([+-])(\d{2})(\d{2})|Z/,
	basicOptionalSeconds: /^([+-])(\d{2})(\d{2})((\d{2}))?|Z/,
	extended: /^([+-])(\d{2}):(\d{2})|Z/,
	extendedOptionalSeconds: /^([+-])(\d{2}):(\d{2})(:(\d{2}))?|Z/
};
//#endregion
//#region node_modules/date-fns/parse/_lib/utils.js
function mapValue(parseFnResult, mapFn) {
	if (!parseFnResult) return parseFnResult;
	return {
		value: mapFn(parseFnResult.value),
		rest: parseFnResult.rest
	};
}
function parseNumericPattern(pattern, dateString) {
	const matchResult = dateString.match(pattern);
	if (!matchResult) return null;
	return {
		value: parseInt(matchResult[0], 10),
		rest: dateString.slice(matchResult[0].length)
	};
}
function parseTimezonePattern(pattern, dateString) {
	const matchResult = dateString.match(pattern);
	if (!matchResult) return null;
	if (matchResult[0] === "Z") return {
		value: 0,
		rest: dateString.slice(1)
	};
	const sign = matchResult[1] === "+" ? 1 : -1;
	const hours = matchResult[2] ? parseInt(matchResult[2], 10) : 0;
	const minutes = matchResult[3] ? parseInt(matchResult[3], 10) : 0;
	const seconds = matchResult[5] ? parseInt(matchResult[5], 10) : 0;
	return {
		value: sign * (hours * millisecondsInHour + minutes * millisecondsInMinute + seconds * millisecondsInSecond),
		rest: dateString.slice(matchResult[0].length)
	};
}
function parseAnyDigitsSigned(dateString) {
	return parseNumericPattern(numericPatterns.anyDigitsSigned, dateString);
}
function parseNDigits(n, dateString) {
	switch (n) {
		case 1: return parseNumericPattern(numericPatterns.singleDigit, dateString);
		case 2: return parseNumericPattern(numericPatterns.twoDigits, dateString);
		case 3: return parseNumericPattern(numericPatterns.threeDigits, dateString);
		case 4: return parseNumericPattern(numericPatterns.fourDigits, dateString);
		default: return parseNumericPattern(new RegExp("^\\d{1," + n + "}"), dateString);
	}
}
function parseNDigitsSigned(n, dateString) {
	switch (n) {
		case 1: return parseNumericPattern(numericPatterns.singleDigitSigned, dateString);
		case 2: return parseNumericPattern(numericPatterns.twoDigitsSigned, dateString);
		case 3: return parseNumericPattern(numericPatterns.threeDigitsSigned, dateString);
		case 4: return parseNumericPattern(numericPatterns.fourDigitsSigned, dateString);
		default: return parseNumericPattern(new RegExp("^-?\\d{1," + n + "}"), dateString);
	}
}
function dayPeriodEnumToHours(dayPeriod) {
	switch (dayPeriod) {
		case "morning": return 4;
		case "evening": return 17;
		case "pm":
		case "noon":
		case "afternoon": return 12;
		default: return 0;
	}
}
function normalizeTwoDigitYear(twoDigitYear, currentYear) {
	const isCommonEra = currentYear > 0;
	const absCurrentYear = isCommonEra ? currentYear : 1 - currentYear;
	let result;
	if (absCurrentYear <= 50) result = twoDigitYear || 100;
	else {
		const rangeEnd = absCurrentYear + 50;
		const rangeEndCentury = Math.trunc(rangeEnd / 100) * 100;
		const isPreviousCentury = twoDigitYear >= rangeEnd % 100;
		result = twoDigitYear + rangeEndCentury - (isPreviousCentury ? 100 : 0);
	}
	return isCommonEra ? result : 1 - result;
}
function isLeapYearIndex(year) {
	return year % 400 === 0 || year % 4 === 0 && year % 100 !== 0;
}
//#endregion
//#region node_modules/date-fns/parse/_lib/parsers/YearParser.js
var YearParser = class extends Parser {
	priority = 130;
	incompatibleTokens = [
		"Y",
		"R",
		"u",
		"w",
		"I",
		"i",
		"e",
		"c",
		"t",
		"T"
	];
	parse(dateString, token, match) {
		const valueCallback = (year) => ({
			year,
			isTwoDigitYear: token === "yy"
		});
		switch (token) {
			case "y": return mapValue(parseNDigits(4, dateString), valueCallback);
			case "yo": return mapValue(match.ordinalNumber(dateString, { unit: "year" }), valueCallback);
			default: return mapValue(parseNDigits(token.length, dateString), valueCallback);
		}
	}
	validate(_date, value) {
		return value.isTwoDigitYear || value.year > 0;
	}
	set(date, flags, value) {
		const currentYear = date.getFullYear();
		if (value.isTwoDigitYear) {
			const normalizedTwoDigitYear = normalizeTwoDigitYear(value.year, currentYear);
			date.setFullYear(normalizedTwoDigitYear, 0, 1);
			date.setHours(0, 0, 0, 0);
			return date;
		}
		const year = !("era" in flags) || flags.era === 1 ? value.year : 1 - value.year;
		date.setFullYear(year, 0, 1);
		date.setHours(0, 0, 0, 0);
		return date;
	}
};
//#endregion
//#region node_modules/date-fns/parse/_lib/parsers/LocalWeekYearParser.js
var LocalWeekYearParser = class extends Parser {
	priority = 130;
	parse(dateString, token, match) {
		const valueCallback = (year) => ({
			year,
			isTwoDigitYear: token === "YY"
		});
		switch (token) {
			case "Y": return mapValue(parseNDigits(4, dateString), valueCallback);
			case "Yo": return mapValue(match.ordinalNumber(dateString, { unit: "year" }), valueCallback);
			default: return mapValue(parseNDigits(token.length, dateString), valueCallback);
		}
	}
	validate(_date, value) {
		return value.isTwoDigitYear || value.year > 0;
	}
	set(date, flags, value, options) {
		const currentYear = getWeekYear(date, options);
		if (value.isTwoDigitYear) {
			const normalizedTwoDigitYear = normalizeTwoDigitYear(value.year, currentYear);
			date.setFullYear(normalizedTwoDigitYear, 0, options.firstWeekContainsDate);
			date.setHours(0, 0, 0, 0);
			return startOfWeek(date, options);
		}
		const year = !("era" in flags) || flags.era === 1 ? value.year : 1 - value.year;
		date.setFullYear(year, 0, options.firstWeekContainsDate);
		date.setHours(0, 0, 0, 0);
		return startOfWeek(date, options);
	}
	incompatibleTokens = [
		"y",
		"R",
		"u",
		"Q",
		"q",
		"M",
		"L",
		"I",
		"d",
		"D",
		"i",
		"t",
		"T"
	];
};
//#endregion
//#region node_modules/date-fns/parse/_lib/parsers/ISOWeekYearParser.js
var ISOWeekYearParser = class extends Parser {
	priority = 130;
	parse(dateString, token) {
		if (token === "R") return parseNDigitsSigned(4, dateString);
		return parseNDigitsSigned(token.length, dateString);
	}
	set(date, _flags, value) {
		const firstWeekOfYear = constructFrom(date, 0);
		firstWeekOfYear.setFullYear(value, 0, 4);
		firstWeekOfYear.setHours(0, 0, 0, 0);
		return startOfISOWeek(firstWeekOfYear);
	}
	incompatibleTokens = [
		"G",
		"y",
		"Y",
		"u",
		"Q",
		"q",
		"M",
		"L",
		"w",
		"d",
		"D",
		"e",
		"c",
		"t",
		"T"
	];
};
//#endregion
//#region node_modules/date-fns/parse/_lib/parsers/ExtendedYearParser.js
var ExtendedYearParser = class extends Parser {
	priority = 130;
	parse(dateString, token) {
		if (token === "u") return parseNDigitsSigned(4, dateString);
		return parseNDigitsSigned(token.length, dateString);
	}
	set(date, _flags, value) {
		date.setFullYear(value, 0, 1);
		date.setHours(0, 0, 0, 0);
		return date;
	}
	incompatibleTokens = [
		"G",
		"y",
		"Y",
		"R",
		"w",
		"I",
		"i",
		"e",
		"c",
		"t",
		"T"
	];
};
//#endregion
//#region node_modules/date-fns/parse/_lib/parsers/QuarterParser.js
var QuarterParser = class extends Parser {
	priority = 120;
	parse(dateString, token, match) {
		switch (token) {
			case "Q":
			case "QQ": return parseNDigits(token.length, dateString);
			case "Qo": return match.ordinalNumber(dateString, { unit: "quarter" });
			case "QQQ": return match.quarter(dateString, {
				width: "abbreviated",
				context: "formatting"
			}) || match.quarter(dateString, {
				width: "narrow",
				context: "formatting"
			});
			case "QQQQQ": return match.quarter(dateString, {
				width: "narrow",
				context: "formatting"
			});
			default: return match.quarter(dateString, {
				width: "wide",
				context: "formatting"
			}) || match.quarter(dateString, {
				width: "abbreviated",
				context: "formatting"
			}) || match.quarter(dateString, {
				width: "narrow",
				context: "formatting"
			});
		}
	}
	validate(_date, value) {
		return value >= 1 && value <= 4;
	}
	set(date, _flags, value) {
		date.setMonth((value - 1) * 3, 1);
		date.setHours(0, 0, 0, 0);
		return date;
	}
	incompatibleTokens = [
		"Y",
		"R",
		"q",
		"M",
		"L",
		"w",
		"I",
		"d",
		"D",
		"i",
		"e",
		"c",
		"t",
		"T"
	];
};
//#endregion
//#region node_modules/date-fns/parse/_lib/parsers/StandAloneQuarterParser.js
var StandAloneQuarterParser = class extends Parser {
	priority = 120;
	parse(dateString, token, match) {
		switch (token) {
			case "q":
			case "qq": return parseNDigits(token.length, dateString);
			case "qo": return match.ordinalNumber(dateString, { unit: "quarter" });
			case "qqq": return match.quarter(dateString, {
				width: "abbreviated",
				context: "standalone"
			}) || match.quarter(dateString, {
				width: "narrow",
				context: "standalone"
			});
			case "qqqqq": return match.quarter(dateString, {
				width: "narrow",
				context: "standalone"
			});
			default: return match.quarter(dateString, {
				width: "wide",
				context: "standalone"
			}) || match.quarter(dateString, {
				width: "abbreviated",
				context: "standalone"
			}) || match.quarter(dateString, {
				width: "narrow",
				context: "standalone"
			});
		}
	}
	validate(_date, value) {
		return value >= 1 && value <= 4;
	}
	set(date, _flags, value) {
		date.setMonth((value - 1) * 3, 1);
		date.setHours(0, 0, 0, 0);
		return date;
	}
	incompatibleTokens = [
		"Y",
		"R",
		"Q",
		"M",
		"L",
		"w",
		"I",
		"d",
		"D",
		"i",
		"e",
		"c",
		"t",
		"T"
	];
};
//#endregion
//#region node_modules/date-fns/parse/_lib/parsers/MonthParser.js
var MonthParser = class extends Parser {
	incompatibleTokens = [
		"Y",
		"R",
		"q",
		"Q",
		"L",
		"w",
		"I",
		"D",
		"i",
		"e",
		"c",
		"t",
		"T"
	];
	priority = 110;
	parse(dateString, token, match) {
		const valueCallback = (value) => value - 1;
		switch (token) {
			case "M": return mapValue(parseNumericPattern(numericPatterns.month, dateString), valueCallback);
			case "MM": return mapValue(parseNDigits(2, dateString), valueCallback);
			case "Mo": return mapValue(match.ordinalNumber(dateString, { unit: "month" }), valueCallback);
			case "MMM": return match.month(dateString, {
				width: "abbreviated",
				context: "formatting"
			}) || match.month(dateString, {
				width: "narrow",
				context: "formatting"
			});
			case "MMMMM": return match.month(dateString, {
				width: "narrow",
				context: "formatting"
			});
			default: return match.month(dateString, {
				width: "wide",
				context: "formatting"
			}) || match.month(dateString, {
				width: "abbreviated",
				context: "formatting"
			}) || match.month(dateString, {
				width: "narrow",
				context: "formatting"
			});
		}
	}
	validate(_date, value) {
		return value >= 0 && value <= 11;
	}
	set(date, _flags, value) {
		date.setMonth(value, 1);
		date.setHours(0, 0, 0, 0);
		return date;
	}
};
//#endregion
//#region node_modules/date-fns/parse/_lib/parsers/StandAloneMonthParser.js
var StandAloneMonthParser = class extends Parser {
	priority = 110;
	parse(dateString, token, match) {
		const valueCallback = (value) => value - 1;
		switch (token) {
			case "L": return mapValue(parseNumericPattern(numericPatterns.month, dateString), valueCallback);
			case "LL": return mapValue(parseNDigits(2, dateString), valueCallback);
			case "Lo": return mapValue(match.ordinalNumber(dateString, { unit: "month" }), valueCallback);
			case "LLL": return match.month(dateString, {
				width: "abbreviated",
				context: "standalone"
			}) || match.month(dateString, {
				width: "narrow",
				context: "standalone"
			});
			case "LLLLL": return match.month(dateString, {
				width: "narrow",
				context: "standalone"
			});
			default: return match.month(dateString, {
				width: "wide",
				context: "standalone"
			}) || match.month(dateString, {
				width: "abbreviated",
				context: "standalone"
			}) || match.month(dateString, {
				width: "narrow",
				context: "standalone"
			});
		}
	}
	validate(_date, value) {
		return value >= 0 && value <= 11;
	}
	set(date, _flags, value) {
		date.setMonth(value, 1);
		date.setHours(0, 0, 0, 0);
		return date;
	}
	incompatibleTokens = [
		"Y",
		"R",
		"q",
		"Q",
		"M",
		"w",
		"I",
		"D",
		"i",
		"e",
		"c",
		"t",
		"T"
	];
};
//#endregion
//#region node_modules/date-fns/setWeek.js
/**
* The {@link setWeek} function options.
*/
/**
* @name setWeek
* @category Week Helpers
* @summary Set the local week to the given date.
*
* @description
* Set the local week to the given date, saving the weekday number.
* The exact calculation depends on the values of
* `options.weekStartsOn` (which is the index of the first day of the week)
* and `options.firstWeekContainsDate` (which is the day of January, which is always in
* the first week of the week-numbering year)
*
* Week numbering: https://en.wikipedia.org/wiki/Week#The_ISO_week_date_system
*
* @typeParam DateType - The `Date` type, the function operates on. Gets inferred from passed arguments. Allows to use extensions like [`UTCDate`](https://github.com/date-fns/utc).
* @typeParam ResultDate - The result `Date` type, it is the type returned from the context function if it is passed, or inferred from the arguments.
*
* @param date - The date to be changed
* @param week - The week of the new date
* @param options - An object with options
*
* @returns The new date with the local week set
*
* @example
* // Set the 1st week to 2 January 2005 with default options:
* const result = setWeek(new Date(2005, 0, 2), 1)
* //=> Sun Dec 26 2004 00:00:00
*
* @example
* // Set the 1st week to 2 January 2005,
* // if Monday is the first day of the week,
* // and the first week of the year always contains 4 January:
* const result = setWeek(new Date(2005, 0, 2), 1, {
*   weekStartsOn: 1,
*   firstWeekContainsDate: 4
* })
* //=> Sun Jan 4 2004 00:00:00
*/
function setWeek(date, week, options) {
	const date_ = toDate(date, options?.in);
	const diff = getWeek(date_, options) - week;
	date_.setDate(date_.getDate() - diff * 7);
	return toDate(date_, options?.in);
}
//#endregion
//#region node_modules/date-fns/parse/_lib/parsers/LocalWeekParser.js
var LocalWeekParser = class extends Parser {
	priority = 100;
	parse(dateString, token, match) {
		switch (token) {
			case "w": return parseNumericPattern(numericPatterns.week, dateString);
			case "wo": return match.ordinalNumber(dateString, { unit: "week" });
			default: return parseNDigits(token.length, dateString);
		}
	}
	validate(_date, value) {
		return value >= 1 && value <= 53;
	}
	set(date, _flags, value, options) {
		return startOfWeek(setWeek(date, value, options), options);
	}
	incompatibleTokens = [
		"y",
		"R",
		"u",
		"q",
		"Q",
		"M",
		"L",
		"I",
		"d",
		"D",
		"i",
		"t",
		"T"
	];
};
//#endregion
//#region node_modules/date-fns/setISOWeek.js
/**
* The {@link setISOWeek} function options.
*/
/**
* @name setISOWeek
* @category ISO Week Helpers
* @summary Set the ISO week to the given date.
*
* @description
* Set the ISO week to the given date, saving the weekday number.
*
* ISO week-numbering year: http://en.wikipedia.org/wiki/ISO_week_date
*
* @typeParam DateType - The `Date` type, the function operates on. Gets inferred from passed arguments. Allows to use extensions like [`UTCDate`](https://github.com/date-fns/utc).
* @typeParam ResultDate - The `Date` type of the context function.
*
* @param date - The date to be changed
* @param week - The ISO week of the new date
* @param options - An object with options
*
* @returns The new date with the ISO week set
*
* @example
* // Set the 53rd ISO week to 7 August 2004:
* const result = setISOWeek(new Date(2004, 7, 7), 53)
* //=> Sat Jan 01 2005 00:00:00
*/
function setISOWeek(date, week, options) {
	const _date = toDate(date, options?.in);
	const diff = getISOWeek(_date, options) - week;
	_date.setDate(_date.getDate() - diff * 7);
	return _date;
}
//#endregion
//#region node_modules/date-fns/parse/_lib/parsers/ISOWeekParser.js
var ISOWeekParser = class extends Parser {
	priority = 100;
	parse(dateString, token, match) {
		switch (token) {
			case "I": return parseNumericPattern(numericPatterns.week, dateString);
			case "Io": return match.ordinalNumber(dateString, { unit: "week" });
			default: return parseNDigits(token.length, dateString);
		}
	}
	validate(_date, value) {
		return value >= 1 && value <= 53;
	}
	set(date, _flags, value) {
		return startOfISOWeek(setISOWeek(date, value));
	}
	incompatibleTokens = [
		"y",
		"Y",
		"u",
		"q",
		"Q",
		"M",
		"L",
		"w",
		"d",
		"D",
		"e",
		"c",
		"t",
		"T"
	];
};
//#endregion
//#region node_modules/date-fns/parse/_lib/parsers/DateParser.js
var DAYS_IN_MONTH = [
	31,
	28,
	31,
	30,
	31,
	30,
	31,
	31,
	30,
	31,
	30,
	31
];
var DAYS_IN_MONTH_LEAP_YEAR = [
	31,
	29,
	31,
	30,
	31,
	30,
	31,
	31,
	30,
	31,
	30,
	31
];
var DateParser = class extends Parser {
	priority = 90;
	subPriority = 1;
	parse(dateString, token, match) {
		switch (token) {
			case "d": return parseNumericPattern(numericPatterns.date, dateString);
			case "do": return match.ordinalNumber(dateString, { unit: "date" });
			default: return parseNDigits(token.length, dateString);
		}
	}
	validate(date, value) {
		const isLeapYear = isLeapYearIndex(date.getFullYear());
		const month = date.getMonth();
		if (isLeapYear) return value >= 1 && value <= DAYS_IN_MONTH_LEAP_YEAR[month];
		else return value >= 1 && value <= DAYS_IN_MONTH[month];
	}
	set(date, _flags, value) {
		date.setDate(value);
		date.setHours(0, 0, 0, 0);
		return date;
	}
	incompatibleTokens = [
		"Y",
		"R",
		"q",
		"Q",
		"w",
		"I",
		"D",
		"i",
		"e",
		"c",
		"t",
		"T"
	];
};
//#endregion
//#region node_modules/date-fns/parse/_lib/parsers/DayOfYearParser.js
var DayOfYearParser = class extends Parser {
	priority = 90;
	subpriority = 1;
	parse(dateString, token, match) {
		switch (token) {
			case "D":
			case "DD": return parseNumericPattern(numericPatterns.dayOfYear, dateString);
			case "Do": return match.ordinalNumber(dateString, { unit: "date" });
			default: return parseNDigits(token.length, dateString);
		}
	}
	validate(date, value) {
		if (isLeapYearIndex(date.getFullYear())) return value >= 1 && value <= 366;
		else return value >= 1 && value <= 365;
	}
	set(date, _flags, value) {
		date.setMonth(0, value);
		date.setHours(0, 0, 0, 0);
		return date;
	}
	incompatibleTokens = [
		"Y",
		"R",
		"q",
		"Q",
		"M",
		"L",
		"w",
		"I",
		"d",
		"E",
		"i",
		"e",
		"c",
		"t",
		"T"
	];
};
//#endregion
//#region node_modules/date-fns/setDay.js
/**
* The {@link setDay} function options.
*/
/**
* @name setDay
* @category Weekday Helpers
* @summary Set the day of the week to the given date.
*
* @description
* Set the day of the week to the given date.
*
* @typeParam DateType - The `Date` type, the function operates on. Gets inferred from passed arguments. Allows to use extensions like [`UTCDate`](https://github.com/date-fns/utc).
* @typeParam ResultDate - The result `Date` type, it is the type returned from the context function if it is passed, or inferred from the arguments.
*
* @param date - The date to be changed
* @param day - The day of the week of the new date
* @param options - An object with options.
*
* @returns The new date with the day of the week set
*
* @example
* // Set week day to Sunday, with the default weekStartsOn of Sunday:
* const result = setDay(new Date(2014, 8, 1), 0)
* //=> Sun Aug 31 2014 00:00:00
*
* @example
* // Set week day to Sunday, with a weekStartsOn of Monday:
* const result = setDay(new Date(2014, 8, 1), 0, { weekStartsOn: 1 })
* //=> Sun Sep 07 2014 00:00:00
*/
function setDay(date, day, options) {
	const defaultOptions = getDefaultOptions$1();
	const weekStartsOn = options?.weekStartsOn ?? options?.locale?.options?.weekStartsOn ?? defaultOptions.weekStartsOn ?? defaultOptions.locale?.options?.weekStartsOn ?? 0;
	const date_ = toDate(date, options?.in);
	const currentDay = date_.getDay();
	const dayIndex = (day % 7 + 7) % 7;
	const delta = 7 - weekStartsOn;
	return addDays(date_, day < 0 || day > 6 ? day - (currentDay + delta) % 7 : (dayIndex + delta) % 7 - (currentDay + delta) % 7, options);
}
//#endregion
//#region node_modules/date-fns/parse/_lib/parsers/DayParser.js
var DayParser = class extends Parser {
	priority = 90;
	parse(dateString, token, match) {
		switch (token) {
			case "E":
			case "EE":
			case "EEE": return match.day(dateString, {
				width: "abbreviated",
				context: "formatting"
			}) || match.day(dateString, {
				width: "short",
				context: "formatting"
			}) || match.day(dateString, {
				width: "narrow",
				context: "formatting"
			});
			case "EEEEE": return match.day(dateString, {
				width: "narrow",
				context: "formatting"
			});
			case "EEEEEE": return match.day(dateString, {
				width: "short",
				context: "formatting"
			}) || match.day(dateString, {
				width: "narrow",
				context: "formatting"
			});
			default: return match.day(dateString, {
				width: "wide",
				context: "formatting"
			}) || match.day(dateString, {
				width: "abbreviated",
				context: "formatting"
			}) || match.day(dateString, {
				width: "short",
				context: "formatting"
			}) || match.day(dateString, {
				width: "narrow",
				context: "formatting"
			});
		}
	}
	validate(_date, value) {
		return value >= 0 && value <= 6;
	}
	set(date, _flags, value, options) {
		date = setDay(date, value, options);
		date.setHours(0, 0, 0, 0);
		return date;
	}
	incompatibleTokens = [
		"D",
		"i",
		"e",
		"c",
		"t",
		"T"
	];
};
//#endregion
//#region node_modules/date-fns/parse/_lib/parsers/LocalDayParser.js
var LocalDayParser = class extends Parser {
	priority = 90;
	parse(dateString, token, match, options) {
		const valueCallback = (value) => {
			const wholeWeekDays = Math.floor((value - 1) / 7) * 7;
			return (value + options.weekStartsOn + 6) % 7 + wholeWeekDays;
		};
		switch (token) {
			case "e":
			case "ee": return mapValue(parseNDigits(token.length, dateString), valueCallback);
			case "eo": return mapValue(match.ordinalNumber(dateString, { unit: "day" }), valueCallback);
			case "eee": return match.day(dateString, {
				width: "abbreviated",
				context: "formatting"
			}) || match.day(dateString, {
				width: "short",
				context: "formatting"
			}) || match.day(dateString, {
				width: "narrow",
				context: "formatting"
			});
			case "eeeee": return match.day(dateString, {
				width: "narrow",
				context: "formatting"
			});
			case "eeeeee": return match.day(dateString, {
				width: "short",
				context: "formatting"
			}) || match.day(dateString, {
				width: "narrow",
				context: "formatting"
			});
			default: return match.day(dateString, {
				width: "wide",
				context: "formatting"
			}) || match.day(dateString, {
				width: "abbreviated",
				context: "formatting"
			}) || match.day(dateString, {
				width: "short",
				context: "formatting"
			}) || match.day(dateString, {
				width: "narrow",
				context: "formatting"
			});
		}
	}
	validate(_date, value) {
		return value >= 0 && value <= 6;
	}
	set(date, _flags, value, options) {
		date = setDay(date, value, options);
		date.setHours(0, 0, 0, 0);
		return date;
	}
	incompatibleTokens = [
		"y",
		"R",
		"u",
		"q",
		"Q",
		"M",
		"L",
		"I",
		"d",
		"D",
		"E",
		"i",
		"c",
		"t",
		"T"
	];
};
//#endregion
//#region node_modules/date-fns/parse/_lib/parsers/StandAloneLocalDayParser.js
var StandAloneLocalDayParser = class extends Parser {
	priority = 90;
	parse(dateString, token, match, options) {
		const valueCallback = (value) => {
			const wholeWeekDays = Math.floor((value - 1) / 7) * 7;
			return (value + options.weekStartsOn + 6) % 7 + wholeWeekDays;
		};
		switch (token) {
			case "c":
			case "cc": return mapValue(parseNDigits(token.length, dateString), valueCallback);
			case "co": return mapValue(match.ordinalNumber(dateString, { unit: "day" }), valueCallback);
			case "ccc": return match.day(dateString, {
				width: "abbreviated",
				context: "standalone"
			}) || match.day(dateString, {
				width: "short",
				context: "standalone"
			}) || match.day(dateString, {
				width: "narrow",
				context: "standalone"
			});
			case "ccccc": return match.day(dateString, {
				width: "narrow",
				context: "standalone"
			});
			case "cccccc": return match.day(dateString, {
				width: "short",
				context: "standalone"
			}) || match.day(dateString, {
				width: "narrow",
				context: "standalone"
			});
			default: return match.day(dateString, {
				width: "wide",
				context: "standalone"
			}) || match.day(dateString, {
				width: "abbreviated",
				context: "standalone"
			}) || match.day(dateString, {
				width: "short",
				context: "standalone"
			}) || match.day(dateString, {
				width: "narrow",
				context: "standalone"
			});
		}
	}
	validate(_date, value) {
		return value >= 0 && value <= 6;
	}
	set(date, _flags, value, options) {
		date = setDay(date, value, options);
		date.setHours(0, 0, 0, 0);
		return date;
	}
	incompatibleTokens = [
		"y",
		"R",
		"u",
		"q",
		"Q",
		"M",
		"L",
		"I",
		"d",
		"D",
		"E",
		"i",
		"e",
		"t",
		"T"
	];
};
//#endregion
//#region node_modules/date-fns/getISODay.js
/**
* The {@link getISODay} function options.
*/
/**
* @name getISODay
* @category Weekday Helpers
* @summary Get the day of the ISO week of the given date.
*
* @description
* Get the day of the ISO week of the given date,
* which is 7 for Sunday, 1 for Monday etc.
*
* ISO week-numbering year: http://en.wikipedia.org/wiki/ISO_week_date
*
* @param date - The given date
* @param options - An object with options
*
* @returns The day of ISO week
*
* @example
* // Which day of the ISO week is 26 February 2012?
* const result = getISODay(new Date(2012, 1, 26))
* //=> 7
*/
function getISODay(date, options) {
	const day = toDate(date, options?.in).getDay();
	return day === 0 ? 7 : day;
}
//#endregion
//#region node_modules/date-fns/setISODay.js
/**
* The {@link setISODay} function options.
*/
/**
* @name setISODay
* @category Weekday Helpers
* @summary Set the day of the ISO week to the given date.
*
* @description
* Set the day of the ISO week to the given date.
* ISO week starts with Monday.
* 7 is the index of Sunday, 1 is the index of Monday, etc.
*
* @typeParam DateType - The `Date` type, the function operates on. Gets inferred from passed arguments. Allows to use extensions like [`UTCDate`](https://github.com/date-fns/utc).
* @typeParam ResultDate - The result `Date` type, it is the type returned from the context function if it is passed, or inferred from the arguments.
*
* @param date - The date to be changed
* @param day - The day of the ISO week of the new date
* @param options - An object with options
*
* @returns The new date with the day of the ISO week set
*
* @example
* // Set Sunday to 1 September 2014:
* const result = setISODay(new Date(2014, 8, 1), 7)
* //=> Sun Sep 07 2014 00:00:00
*/
function setISODay(date, day, options) {
	const date_ = toDate(date, options?.in);
	return addDays(date_, day - getISODay(date_, options), options);
}
//#endregion
//#region node_modules/date-fns/parse/_lib/parsers/ISODayParser.js
var ISODayParser = class extends Parser {
	priority = 90;
	parse(dateString, token, match) {
		const valueCallback = (value) => {
			if (value === 0) return 7;
			return value;
		};
		switch (token) {
			case "i":
			case "ii": return parseNDigits(token.length, dateString);
			case "io": return match.ordinalNumber(dateString, { unit: "day" });
			case "iii": return mapValue(match.day(dateString, {
				width: "abbreviated",
				context: "formatting"
			}) || match.day(dateString, {
				width: "short",
				context: "formatting"
			}) || match.day(dateString, {
				width: "narrow",
				context: "formatting"
			}), valueCallback);
			case "iiiii": return mapValue(match.day(dateString, {
				width: "narrow",
				context: "formatting"
			}), valueCallback);
			case "iiiiii": return mapValue(match.day(dateString, {
				width: "short",
				context: "formatting"
			}) || match.day(dateString, {
				width: "narrow",
				context: "formatting"
			}), valueCallback);
			default: return mapValue(match.day(dateString, {
				width: "wide",
				context: "formatting"
			}) || match.day(dateString, {
				width: "abbreviated",
				context: "formatting"
			}) || match.day(dateString, {
				width: "short",
				context: "formatting"
			}) || match.day(dateString, {
				width: "narrow",
				context: "formatting"
			}), valueCallback);
		}
	}
	validate(_date, value) {
		return value >= 1 && value <= 7;
	}
	set(date, _flags, value) {
		date = setISODay(date, value);
		date.setHours(0, 0, 0, 0);
		return date;
	}
	incompatibleTokens = [
		"y",
		"Y",
		"u",
		"q",
		"Q",
		"M",
		"L",
		"w",
		"d",
		"D",
		"E",
		"e",
		"c",
		"t",
		"T"
	];
};
//#endregion
//#region node_modules/date-fns/parse/_lib/parsers/AMPMParser.js
var AMPMParser = class extends Parser {
	priority = 80;
	parse(dateString, token, match) {
		switch (token) {
			case "a":
			case "aa":
			case "aaa": return match.dayPeriod(dateString, {
				width: "abbreviated",
				context: "formatting"
			}) || match.dayPeriod(dateString, {
				width: "narrow",
				context: "formatting"
			});
			case "aaaaa": return match.dayPeriod(dateString, {
				width: "narrow",
				context: "formatting"
			});
			default: return match.dayPeriod(dateString, {
				width: "wide",
				context: "formatting"
			}) || match.dayPeriod(dateString, {
				width: "abbreviated",
				context: "formatting"
			}) || match.dayPeriod(dateString, {
				width: "narrow",
				context: "formatting"
			});
		}
	}
	set(date, _flags, value) {
		date.setHours(dayPeriodEnumToHours(value), 0, 0, 0);
		return date;
	}
	incompatibleTokens = [
		"b",
		"B",
		"H",
		"k",
		"t",
		"T"
	];
};
//#endregion
//#region node_modules/date-fns/parse/_lib/parsers/AMPMMidnightParser.js
var AMPMMidnightParser = class extends Parser {
	priority = 80;
	parse(dateString, token, match) {
		switch (token) {
			case "b":
			case "bb":
			case "bbb": return match.dayPeriod(dateString, {
				width: "abbreviated",
				context: "formatting"
			}) || match.dayPeriod(dateString, {
				width: "narrow",
				context: "formatting"
			});
			case "bbbbb": return match.dayPeriod(dateString, {
				width: "narrow",
				context: "formatting"
			});
			default: return match.dayPeriod(dateString, {
				width: "wide",
				context: "formatting"
			}) || match.dayPeriod(dateString, {
				width: "abbreviated",
				context: "formatting"
			}) || match.dayPeriod(dateString, {
				width: "narrow",
				context: "formatting"
			});
		}
	}
	set(date, _flags, value) {
		date.setHours(dayPeriodEnumToHours(value), 0, 0, 0);
		return date;
	}
	incompatibleTokens = [
		"a",
		"B",
		"H",
		"k",
		"t",
		"T"
	];
};
//#endregion
//#region node_modules/date-fns/parse/_lib/parsers/DayPeriodParser.js
var DayPeriodParser = class extends Parser {
	priority = 80;
	parse(dateString, token, match) {
		switch (token) {
			case "B":
			case "BB":
			case "BBB": return match.dayPeriod(dateString, {
				width: "abbreviated",
				context: "formatting"
			}) || match.dayPeriod(dateString, {
				width: "narrow",
				context: "formatting"
			});
			case "BBBBB": return match.dayPeriod(dateString, {
				width: "narrow",
				context: "formatting"
			});
			default: return match.dayPeriod(dateString, {
				width: "wide",
				context: "formatting"
			}) || match.dayPeriod(dateString, {
				width: "abbreviated",
				context: "formatting"
			}) || match.dayPeriod(dateString, {
				width: "narrow",
				context: "formatting"
			});
		}
	}
	set(date, _flags, value) {
		date.setHours(dayPeriodEnumToHours(value), 0, 0, 0);
		return date;
	}
	incompatibleTokens = [
		"a",
		"b",
		"t",
		"T"
	];
};
//#endregion
//#region node_modules/date-fns/parse/_lib/parsers/Hour1to12Parser.js
var Hour1to12Parser = class extends Parser {
	priority = 70;
	parse(dateString, token, match) {
		switch (token) {
			case "h": return parseNumericPattern(numericPatterns.hour12h, dateString);
			case "ho": return match.ordinalNumber(dateString, { unit: "hour" });
			default: return parseNDigits(token.length, dateString);
		}
	}
	validate(_date, value) {
		return value >= 1 && value <= 12;
	}
	set(date, _flags, value) {
		const isPM = date.getHours() >= 12;
		if (isPM && value < 12) date.setHours(value + 12, 0, 0, 0);
		else if (!isPM && value === 12) date.setHours(0, 0, 0, 0);
		else date.setHours(value, 0, 0, 0);
		return date;
	}
	incompatibleTokens = [
		"H",
		"K",
		"k",
		"t",
		"T"
	];
};
//#endregion
//#region node_modules/date-fns/parse/_lib/parsers/Hour0to23Parser.js
var Hour0to23Parser = class extends Parser {
	priority = 70;
	parse(dateString, token, match) {
		switch (token) {
			case "H": return parseNumericPattern(numericPatterns.hour23h, dateString);
			case "Ho": return match.ordinalNumber(dateString, { unit: "hour" });
			default: return parseNDigits(token.length, dateString);
		}
	}
	validate(_date, value) {
		return value >= 0 && value <= 23;
	}
	set(date, _flags, value) {
		date.setHours(value, 0, 0, 0);
		return date;
	}
	incompatibleTokens = [
		"a",
		"b",
		"h",
		"K",
		"k",
		"t",
		"T"
	];
};
//#endregion
//#region node_modules/date-fns/parse/_lib/parsers/Hour0To11Parser.js
var Hour0To11Parser = class extends Parser {
	priority = 70;
	parse(dateString, token, match) {
		switch (token) {
			case "K": return parseNumericPattern(numericPatterns.hour11h, dateString);
			case "Ko": return match.ordinalNumber(dateString, { unit: "hour" });
			default: return parseNDigits(token.length, dateString);
		}
	}
	validate(_date, value) {
		return value >= 0 && value <= 11;
	}
	set(date, _flags, value) {
		if (date.getHours() >= 12 && value < 12) date.setHours(value + 12, 0, 0, 0);
		else date.setHours(value, 0, 0, 0);
		return date;
	}
	incompatibleTokens = [
		"h",
		"H",
		"k",
		"t",
		"T"
	];
};
//#endregion
//#region node_modules/date-fns/parse/_lib/parsers/Hour1To24Parser.js
var Hour1To24Parser = class extends Parser {
	priority = 70;
	parse(dateString, token, match) {
		switch (token) {
			case "k": return parseNumericPattern(numericPatterns.hour24h, dateString);
			case "ko": return match.ordinalNumber(dateString, { unit: "hour" });
			default: return parseNDigits(token.length, dateString);
		}
	}
	validate(_date, value) {
		return value >= 1 && value <= 24;
	}
	set(date, _flags, value) {
		const hours = value <= 24 ? value % 24 : value;
		date.setHours(hours, 0, 0, 0);
		return date;
	}
	incompatibleTokens = [
		"a",
		"b",
		"h",
		"H",
		"K",
		"t",
		"T"
	];
};
//#endregion
//#region node_modules/date-fns/parse/_lib/parsers/MinuteParser.js
var MinuteParser = class extends Parser {
	priority = 60;
	parse(dateString, token, match) {
		switch (token) {
			case "m": return parseNumericPattern(numericPatterns.minute, dateString);
			case "mo": return match.ordinalNumber(dateString, { unit: "minute" });
			default: return parseNDigits(token.length, dateString);
		}
	}
	validate(_date, value) {
		return value >= 0 && value <= 59;
	}
	set(date, _flags, value) {
		date.setMinutes(value, 0, 0);
		return date;
	}
	incompatibleTokens = ["t", "T"];
};
//#endregion
//#region node_modules/date-fns/parse/_lib/parsers/SecondParser.js
var SecondParser = class extends Parser {
	priority = 50;
	parse(dateString, token, match) {
		switch (token) {
			case "s": return parseNumericPattern(numericPatterns.second, dateString);
			case "so": return match.ordinalNumber(dateString, { unit: "second" });
			default: return parseNDigits(token.length, dateString);
		}
	}
	validate(_date, value) {
		return value >= 0 && value <= 59;
	}
	set(date, _flags, value) {
		date.setSeconds(value, 0);
		return date;
	}
	incompatibleTokens = ["t", "T"];
};
//#endregion
//#region node_modules/date-fns/parse/_lib/parsers/FractionOfSecondParser.js
var FractionOfSecondParser = class extends Parser {
	priority = 30;
	parse(dateString, token) {
		const valueCallback = (value) => Math.trunc(value * Math.pow(10, -token.length + 3));
		return mapValue(parseNDigits(token.length, dateString), valueCallback);
	}
	set(date, _flags, value) {
		date.setMilliseconds(value);
		return date;
	}
	incompatibleTokens = ["t", "T"];
};
//#endregion
//#region node_modules/date-fns/parse/_lib/parsers/ISOTimezoneWithZParser.js
var ISOTimezoneWithZParser = class extends Parser {
	priority = 10;
	parse(dateString, token) {
		switch (token) {
			case "X": return parseTimezonePattern(timezonePatterns.basicOptionalMinutes, dateString);
			case "XX": return parseTimezonePattern(timezonePatterns.basic, dateString);
			case "XXXX": return parseTimezonePattern(timezonePatterns.basicOptionalSeconds, dateString);
			case "XXXXX": return parseTimezonePattern(timezonePatterns.extendedOptionalSeconds, dateString);
			default: return parseTimezonePattern(timezonePatterns.extended, dateString);
		}
	}
	set(date, flags, value) {
		if (flags.timestampIsSet) return date;
		return constructFrom(date, date.getTime() - getTimezoneOffsetInMilliseconds(date) - value);
	}
	incompatibleTokens = [
		"t",
		"T",
		"x"
	];
};
//#endregion
//#region node_modules/date-fns/parse/_lib/parsers/ISOTimezoneParser.js
var ISOTimezoneParser = class extends Parser {
	priority = 10;
	parse(dateString, token) {
		switch (token) {
			case "x": return parseTimezonePattern(timezonePatterns.basicOptionalMinutes, dateString);
			case "xx": return parseTimezonePattern(timezonePatterns.basic, dateString);
			case "xxxx": return parseTimezonePattern(timezonePatterns.basicOptionalSeconds, dateString);
			case "xxxxx": return parseTimezonePattern(timezonePatterns.extendedOptionalSeconds, dateString);
			default: return parseTimezonePattern(timezonePatterns.extended, dateString);
		}
	}
	set(date, flags, value) {
		if (flags.timestampIsSet) return date;
		return constructFrom(date, date.getTime() - getTimezoneOffsetInMilliseconds(date) - value);
	}
	incompatibleTokens = [
		"t",
		"T",
		"X"
	];
};
//#endregion
//#region node_modules/date-fns/parse/_lib/parsers/TimestampSecondsParser.js
var TimestampSecondsParser = class extends Parser {
	priority = 40;
	parse(dateString) {
		return parseAnyDigitsSigned(dateString);
	}
	set(date, _flags, value) {
		return [constructFrom(date, value * 1e3), { timestampIsSet: true }];
	}
	incompatibleTokens = "*";
};
//#endregion
//#region node_modules/date-fns/parse/_lib/parsers/TimestampMillisecondsParser.js
var TimestampMillisecondsParser = class extends Parser {
	priority = 20;
	parse(dateString) {
		return parseAnyDigitsSigned(dateString);
	}
	set(date, _flags, value) {
		return [constructFrom(date, value), { timestampIsSet: true }];
	}
	incompatibleTokens = "*";
};
//#endregion
//#region node_modules/date-fns/parse/_lib/parsers.js
var parsers = {
	G: new EraParser(),
	y: new YearParser(),
	Y: new LocalWeekYearParser(),
	R: new ISOWeekYearParser(),
	u: new ExtendedYearParser(),
	Q: new QuarterParser(),
	q: new StandAloneQuarterParser(),
	M: new MonthParser(),
	L: new StandAloneMonthParser(),
	w: new LocalWeekParser(),
	I: new ISOWeekParser(),
	d: new DateParser(),
	D: new DayOfYearParser(),
	E: new DayParser(),
	e: new LocalDayParser(),
	c: new StandAloneLocalDayParser(),
	i: new ISODayParser(),
	a: new AMPMParser(),
	b: new AMPMMidnightParser(),
	B: new DayPeriodParser(),
	h: new Hour1to12Parser(),
	H: new Hour0to23Parser(),
	K: new Hour0To11Parser(),
	k: new Hour1To24Parser(),
	m: new MinuteParser(),
	s: new SecondParser(),
	S: new FractionOfSecondParser(),
	X: new ISOTimezoneWithZParser(),
	x: new ISOTimezoneParser(),
	t: new TimestampSecondsParser(),
	T: new TimestampMillisecondsParser()
};
//#endregion
//#region node_modules/date-fns/parse.js
/**
* The {@link parse} function options.
*/
var formattingTokensRegExp = /[yYQqMLwIdDecihHKkms]o|(\w)\1*|''|'(''|[^'])+('|$)|./g;
var longFormattingTokensRegExp = /P+p+|P+|p+|''|'(''|[^'])+('|$)|./g;
var escapedStringRegExp = /^'([^]*?)'?$/;
var doubleQuoteRegExp = /''/g;
var notWhitespaceRegExp = /\S/;
var unescapedLatinCharacterRegExp = /[a-zA-Z]/;
/**
* @name parse
* @category Common Helpers
* @summary Parse the date.
*
* @description
* Return the date parsed from string using the given format string.
*
* > ⚠️ Please note that the `format` tokens differ from Moment.js and other libraries.
* > See: https://github.com/date-fns/date-fns/blob/master/docs/unicodeTokens.md
*
* The characters in the format string wrapped between two single quotes characters (') are escaped.
* Two single quotes in a row, whether inside or outside a quoted sequence, represent a 'real' single quote.
*
* Format of the format string is based on Unicode Technical Standard #35:
* https://www.unicode.org/reports/tr35/tr35-dates.html#Date_Field_Symbol_Table
* with a few additions (see note 5 below the table).
*
* Not all tokens are compatible. Combinations that don't make sense or could lead to bugs are prohibited
* and will throw `RangeError`. For example usage of 24-hour format token with AM/PM token will throw an exception:
*
* ```javascript
* parse('23 AM', 'HH a', new Date())
* //=> RangeError: The format string mustn't contain `HH` and `a` at the same time
* ```
*
* See the compatibility table: https://docs.google.com/spreadsheets/d/e/2PACX-1vQOPU3xUhplll6dyoMmVUXHKl_8CRDs6_ueLmex3SoqwhuolkuN3O05l4rqx5h1dKX8eb46Ul-CCSrq/pubhtml?gid=0&single=true
*
* Accepted format string patterns:
* | Unit                            |Prior| Pattern | Result examples                   | Notes |
* |---------------------------------|-----|---------|-----------------------------------|-------|
* | Era                             | 140 | G..GGG  | AD, BC                            |       |
* |                                 |     | GGGG    | Anno Domini, Before Christ        | 2     |
* |                                 |     | GGGGG   | A, B                              |       |
* | Calendar year                   | 130 | y       | 44, 1, 1900, 2017, 9999           | 4     |
* |                                 |     | yo      | 44th, 1st, 1900th, 9999999th      | 4,5   |
* |                                 |     | yy      | 44, 01, 00, 17                    | 4     |
* |                                 |     | yyy     | 044, 001, 123, 999                | 4     |
* |                                 |     | yyyy    | 0044, 0001, 1900, 2017            | 4     |
* |                                 |     | yyyyy   | ...                               | 2,4   |
* | Local week-numbering year       | 130 | Y       | 44, 1, 1900, 2017, 9000           | 4     |
* |                                 |     | Yo      | 44th, 1st, 1900th, 9999999th      | 4,5   |
* |                                 |     | YY      | 44, 01, 00, 17                    | 4,6   |
* |                                 |     | YYY     | 044, 001, 123, 999                | 4     |
* |                                 |     | YYYY    | 0044, 0001, 1900, 2017            | 4,6   |
* |                                 |     | YYYYY   | ...                               | 2,4   |
* | ISO week-numbering year         | 130 | R       | -43, 1, 1900, 2017, 9999, -9999   | 4,5   |
* |                                 |     | RR      | -43, 01, 00, 17                   | 4,5   |
* |                                 |     | RRR     | -043, 001, 123, 999, -999         | 4,5   |
* |                                 |     | RRRR    | -0043, 0001, 2017, 9999, -9999    | 4,5   |
* |                                 |     | RRRRR   | ...                               | 2,4,5 |
* | Extended year                   | 130 | u       | -43, 1, 1900, 2017, 9999, -999    | 4     |
* |                                 |     | uu      | -43, 01, 99, -99                  | 4     |
* |                                 |     | uuu     | -043, 001, 123, 999, -999         | 4     |
* |                                 |     | uuuu    | -0043, 0001, 2017, 9999, -9999    | 4     |
* |                                 |     | uuuuu   | ...                               | 2,4   |
* | Quarter (formatting)            | 120 | Q       | 1, 2, 3, 4                        |       |
* |                                 |     | Qo      | 1st, 2nd, 3rd, 4th                | 5     |
* |                                 |     | QQ      | 01, 02, 03, 04                    |       |
* |                                 |     | QQQ     | Q1, Q2, Q3, Q4                    |       |
* |                                 |     | QQQQ    | 1st quarter, 2nd quarter, ...     | 2     |
* |                                 |     | QQQQQ   | 1, 2, 3, 4                        | 4     |
* | Quarter (stand-alone)           | 120 | q       | 1, 2, 3, 4                        |       |
* |                                 |     | qo      | 1st, 2nd, 3rd, 4th                | 5     |
* |                                 |     | qq      | 01, 02, 03, 04                    |       |
* |                                 |     | qqq     | Q1, Q2, Q3, Q4                    |       |
* |                                 |     | qqqq    | 1st quarter, 2nd quarter, ...     | 2     |
* |                                 |     | qqqqq   | 1, 2, 3, 4                        | 3     |
* | Month (formatting)              | 110 | M       | 1, 2, ..., 12                     |       |
* |                                 |     | Mo      | 1st, 2nd, ..., 12th               | 5     |
* |                                 |     | MM      | 01, 02, ..., 12                   |       |
* |                                 |     | MMM     | Jan, Feb, ..., Dec                |       |
* |                                 |     | MMMM    | January, February, ..., December  | 2     |
* |                                 |     | MMMMM   | J, F, ..., D                      |       |
* | Month (stand-alone)             | 110 | L       | 1, 2, ..., 12                     |       |
* |                                 |     | Lo      | 1st, 2nd, ..., 12th               | 5     |
* |                                 |     | LL      | 01, 02, ..., 12                   |       |
* |                                 |     | LLL     | Jan, Feb, ..., Dec                |       |
* |                                 |     | LLLL    | January, February, ..., December  | 2     |
* |                                 |     | LLLLL   | J, F, ..., D                      |       |
* | Local week of year              | 100 | w       | 1, 2, ..., 53                     |       |
* |                                 |     | wo      | 1st, 2nd, ..., 53th               | 5     |
* |                                 |     | ww      | 01, 02, ..., 53                   |       |
* | ISO week of year                | 100 | I       | 1, 2, ..., 53                     | 5     |
* |                                 |     | Io      | 1st, 2nd, ..., 53th               | 5     |
* |                                 |     | II      | 01, 02, ..., 53                   | 5     |
* | Day of month                    |  90 | d       | 1, 2, ..., 31                     |       |
* |                                 |     | do      | 1st, 2nd, ..., 31st               | 5     |
* |                                 |     | dd      | 01, 02, ..., 31                   |       |
* | Day of year                     |  90 | D       | 1, 2, ..., 365, 366               | 7     |
* |                                 |     | Do      | 1st, 2nd, ..., 365th, 366th       | 5     |
* |                                 |     | DD      | 01, 02, ..., 365, 366             | 7     |
* |                                 |     | DDD     | 001, 002, ..., 365, 366           |       |
* |                                 |     | DDDD    | ...                               | 2     |
* | Day of week (formatting)        |  90 | E..EEE  | Mon, Tue, Wed, ..., Sun           |       |
* |                                 |     | EEEE    | Monday, Tuesday, ..., Sunday      | 2     |
* |                                 |     | EEEEE   | M, T, W, T, F, S, S               |       |
* |                                 |     | EEEEEE  | Mo, Tu, We, Th, Fr, Sa, Su        |       |
* | ISO day of week (formatting)    |  90 | i       | 1, 2, 3, ..., 7                   | 5     |
* |                                 |     | io      | 1st, 2nd, ..., 7th                | 5     |
* |                                 |     | ii      | 01, 02, ..., 07                   | 5     |
* |                                 |     | iii     | Mon, Tue, Wed, ..., Sun           | 5     |
* |                                 |     | iiii    | Monday, Tuesday, ..., Sunday      | 2,5   |
* |                                 |     | iiiii   | M, T, W, T, F, S, S               | 5     |
* |                                 |     | iiiiii  | Mo, Tu, We, Th, Fr, Sa, Su        | 5     |
* | Local day of week (formatting)  |  90 | e       | 2, 3, 4, ..., 1                   |       |
* |                                 |     | eo      | 2nd, 3rd, ..., 1st                | 5     |
* |                                 |     | ee      | 02, 03, ..., 01                   |       |
* |                                 |     | eee     | Mon, Tue, Wed, ..., Sun           |       |
* |                                 |     | eeee    | Monday, Tuesday, ..., Sunday      | 2     |
* |                                 |     | eeeee   | M, T, W, T, F, S, S               |       |
* |                                 |     | eeeeee  | Mo, Tu, We, Th, Fr, Sa, Su        |       |
* | Local day of week (stand-alone) |  90 | c       | 2, 3, 4, ..., 1                   |       |
* |                                 |     | co      | 2nd, 3rd, ..., 1st                | 5     |
* |                                 |     | cc      | 02, 03, ..., 01                   |       |
* |                                 |     | ccc     | Mon, Tue, Wed, ..., Sun           |       |
* |                                 |     | cccc    | Monday, Tuesday, ..., Sunday      | 2     |
* |                                 |     | ccccc   | M, T, W, T, F, S, S               |       |
* |                                 |     | cccccc  | Mo, Tu, We, Th, Fr, Sa, Su        |       |
* | AM, PM                          |  80 | a..aaa  | AM, PM                            |       |
* |                                 |     | aaaa    | a.m., p.m.                        | 2     |
* |                                 |     | aaaaa   | a, p                              |       |
* | AM, PM, noon, midnight          |  80 | b..bbb  | AM, PM, noon, midnight            |       |
* |                                 |     | bbbb    | a.m., p.m., noon, midnight        | 2     |
* |                                 |     | bbbbb   | a, p, n, mi                       |       |
* | Flexible day period             |  80 | B..BBB  | at night, in the morning, ...     |       |
* |                                 |     | BBBB    | at night, in the morning, ...     | 2     |
* |                                 |     | BBBBB   | at night, in the morning, ...     |       |
* | Hour [1-12]                     |  70 | h       | 1, 2, ..., 11, 12                 |       |
* |                                 |     | ho      | 1st, 2nd, ..., 11th, 12th         | 5     |
* |                                 |     | hh      | 01, 02, ..., 11, 12               |       |
* | Hour [0-23]                     |  70 | H       | 0, 1, 2, ..., 23                  |       |
* |                                 |     | Ho      | 0th, 1st, 2nd, ..., 23rd          | 5     |
* |                                 |     | HH      | 00, 01, 02, ..., 23               |       |
* | Hour [0-11]                     |  70 | K       | 1, 2, ..., 11, 0                  |       |
* |                                 |     | Ko      | 1st, 2nd, ..., 11th, 0th          | 5     |
* |                                 |     | KK      | 01, 02, ..., 11, 00               |       |
* | Hour [1-24]                     |  70 | k       | 24, 1, 2, ..., 23                 |       |
* |                                 |     | ko      | 24th, 1st, 2nd, ..., 23rd         | 5     |
* |                                 |     | kk      | 24, 01, 02, ..., 23               |       |
* | Minute                          |  60 | m       | 0, 1, ..., 59                     |       |
* |                                 |     | mo      | 0th, 1st, ..., 59th               | 5     |
* |                                 |     | mm      | 00, 01, ..., 59                   |       |
* | Second                          |  50 | s       | 0, 1, ..., 59                     |       |
* |                                 |     | so      | 0th, 1st, ..., 59th               | 5     |
* |                                 |     | ss      | 00, 01, ..., 59                   |       |
* | Seconds timestamp               |  40 | t       | 512969520                         |       |
* |                                 |     | tt      | ...                               | 2     |
* | Fraction of second              |  30 | S       | 0, 1, ..., 9                      |       |
* |                                 |     | SS      | 00, 01, ..., 99                   |       |
* |                                 |     | SSS     | 000, 001, ..., 999                |       |
* |                                 |     | SSSS    | ...                               | 2     |
* | Milliseconds timestamp          |  20 | T       | 512969520900                      |       |
* |                                 |     | TT      | ...                               | 2     |
* | Timezone (ISO-8601 w/ Z)        |  10 | X       | -08, +0530, Z                     |       |
* |                                 |     | XX      | -0800, +0530, Z                   |       |
* |                                 |     | XXX     | -08:00, +05:30, Z                 |       |
* |                                 |     | XXXX    | -0800, +0530, Z, +123456          | 2     |
* |                                 |     | XXXXX   | -08:00, +05:30, Z, +12:34:56      |       |
* | Timezone (ISO-8601 w/o Z)       |  10 | x       | -08, +0530, +00                   |       |
* |                                 |     | xx      | -0800, +0530, +0000               |       |
* |                                 |     | xxx     | -08:00, +05:30, +00:00            | 2     |
* |                                 |     | xxxx    | -0800, +0530, +0000, +123456      |       |
* |                                 |     | xxxxx   | -08:00, +05:30, +00:00, +12:34:56 |       |
* | Long localized date             |  NA | P       | 05/29/1453                        | 5,8   |
* |                                 |     | PP      | May 29, 1453                      |       |
* |                                 |     | PPP     | May 29th, 1453                    |       |
* |                                 |     | PPPP    | Sunday, May 29th, 1453            | 2,5,8 |
* | Long localized time             |  NA | p       | 12:00 AM                          | 5,8   |
* |                                 |     | pp      | 12:00:00 AM                       |       |
* | Combination of date and time    |  NA | Pp      | 05/29/1453, 12:00 AM              |       |
* |                                 |     | PPpp    | May 29, 1453, 12:00:00 AM         |       |
* |                                 |     | PPPpp   | May 29th, 1453 at ...             |       |
* |                                 |     | PPPPpp  | Sunday, May 29th, 1453 at ...     | 2,5,8 |
* Notes:
* 1. "Formatting" units (e.g. formatting quarter) in the default en-US locale
*    are the same as "stand-alone" units, but are different in some languages.
*    "Formatting" units are declined according to the rules of the language
*    in the context of a date. "Stand-alone" units are always nominative singular.
*    In `format` function, they will produce different result:
*
*    `format(new Date(2017, 10, 6), 'do LLLL', {locale: cs}) //=> '6. listopad'`
*
*    `format(new Date(2017, 10, 6), 'do MMMM', {locale: cs}) //=> '6. listopadu'`
*
*    `parse` will try to match both formatting and stand-alone units interchangeably.
*
* 2. Any sequence of the identical letters is a pattern, unless it is escaped by
*    the single quote characters (see below).
*    If the sequence is longer than listed in table:
*    - for numerical units (`yyyyyyyy`) `parse` will try to match a number
*      as wide as the sequence
*    - for text units (`MMMMMMMM`) `parse` will try to match the widest variation of the unit.
*      These variations are marked with "2" in the last column of the table.
*
* 3. `QQQQQ` and `qqqqq` could be not strictly numerical in some locales.
*    These tokens represent the shortest form of the quarter.
*
* 4. The main difference between `y` and `u` patterns are B.C. years:
*
*    | Year | `y` | `u` |
*    |------|-----|-----|
*    | AC 1 |   1 |   1 |
*    | BC 1 |   1 |   0 |
*    | BC 2 |   2 |  -1 |
*
*    Also `yy` will try to guess the century of two digit year by proximity with `referenceDate`:
*
*    `parse('50', 'yy', new Date(2018, 0, 1)) //=> Sat Jan 01 2050 00:00:00`
*
*    `parse('75', 'yy', new Date(2018, 0, 1)) //=> Wed Jan 01 1975 00:00:00`
*
*    while `uu` will just assign the year as is:
*
*    `parse('50', 'uu', new Date(2018, 0, 1)) //=> Sat Jan 01 0050 00:00:00`
*
*    `parse('75', 'uu', new Date(2018, 0, 1)) //=> Tue Jan 01 0075 00:00:00`
*
*    The same difference is true for local and ISO week-numbering years (`Y` and `R`),
*    except local week-numbering years are dependent on `options.weekStartsOn`
*    and `options.firstWeekContainsDate` (compare [setISOWeekYear](https://date-fns.org/docs/setISOWeekYear)
*    and [setWeekYear](https://date-fns.org/docs/setWeekYear)).
*
* 5. These patterns are not in the Unicode Technical Standard #35:
*    - `i`: ISO day of week
*    - `I`: ISO week of year
*    - `R`: ISO week-numbering year
*    - `o`: ordinal number modifier
*    - `P`: long localized date
*    - `p`: long localized time
*
* 6. `YY` and `YYYY` tokens represent week-numbering years but they are often confused with years.
*    You should enable `options.useAdditionalWeekYearTokens` to use them. See: https://github.com/date-fns/date-fns/blob/master/docs/unicodeTokens.md
*
* 7. `D` and `DD` tokens represent days of the year but they are often confused with days of the month.
*    You should enable `options.useAdditionalDayOfYearTokens` to use them. See: https://github.com/date-fns/date-fns/blob/master/docs/unicodeTokens.md
*
* 8. `P+` tokens do not have a defined priority since they are merely aliases to other tokens based
*    on the given locale.
*
*    using `en-US` locale: `P` => `MM/dd/yyyy`
*    using `en-US` locale: `p` => `hh:mm a`
*    using `pt-BR` locale: `P` => `dd/MM/yyyy`
*    using `pt-BR` locale: `p` => `HH:mm`
*
* Values will be assigned to the date in the descending order of its unit's priority.
* Units of an equal priority overwrite each other in the order of appearance.
*
* If no values of higher priority are parsed (e.g. when parsing string 'January 1st' without a year),
* the values will be taken from 3rd argument `referenceDate` which works as a context of parsing.
*
* `referenceDate` must be passed for correct work of the function.
* If you're not sure which `referenceDate` to supply, create a new instance of Date:
* `parse('02/11/2014', 'MM/dd/yyyy', new Date())`
* In this case parsing will be done in the context of the current date.
* If `referenceDate` is `Invalid Date` or a value not convertible to valid `Date`,
* then `Invalid Date` will be returned.
*
* The result may vary by locale.
*
* If `formatString` matches with `dateString` but does not provides tokens, `referenceDate` will be returned.
*
* If parsing failed, `Invalid Date` will be returned.
* Invalid Date is a Date, whose time value is NaN.
* Time value of Date: http://es5.github.io/#x15.9.1.1
*
* @typeParam DateType - The `Date` type, the function operates on. Gets inferred from passed arguments. Allows to use extensions like [`UTCDate`](https://github.com/date-fns/utc).
* @typeParam ResultDate - The result `Date` type, it is the type returned from the context function if it is passed, or inferred from the arguments.
*
* @param dateStr - The string to parse
* @param formatStr - The string of tokens
* @param referenceDate - defines values missing from the parsed dateString
* @param options - An object with options.
*   see: https://github.com/date-fns/date-fns/blob/master/docs/unicodeTokens.md
*   see: https://github.com/date-fns/date-fns/blob/master/docs/unicodeTokens.md
*
* @returns The parsed date
*
* @throws `options.locale` must contain `match` property
* @throws use `yyyy` instead of `YYYY` for formatting years using [format provided] to the input [input provided]; see: https://github.com/date-fns/date-fns/blob/master/docs/unicodeTokens.md
* @throws use `yy` instead of `YY` for formatting years using [format provided] to the input [input provided]; see: https://github.com/date-fns/date-fns/blob/master/docs/unicodeTokens.md
* @throws use `d` instead of `D` for formatting days of the month using [format provided] to the input [input provided]; see: https://github.com/date-fns/date-fns/blob/master/docs/unicodeTokens.md
* @throws use `dd` instead of `DD` for formatting days of the month using [format provided] to the input [input provided]; see: https://github.com/date-fns/date-fns/blob/master/docs/unicodeTokens.md
* @throws format string contains an unescaped latin alphabet character
*
* @example
* // Parse 11 February 2014 from middle-endian format:
* var result = parse('02/11/2014', 'MM/dd/yyyy', new Date())
* //=> Tue Feb 11 2014 00:00:00
*
* @example
* // Parse 28th of February in Esperanto locale in the context of 2010 year:
* import eo from 'date-fns/locale/eo'
* var result = parse('28-a de februaro', "do 'de' MMMM", new Date(2010, 0, 1), {
*   locale: eo
* })
* //=> Sun Feb 28 2010 00:00:00
*/
function parse(dateStr, formatStr, referenceDate, options) {
	const invalidDate = () => constructFrom(options?.in || referenceDate, NaN);
	const defaultOptions = getDefaultOptions();
	const locale = options?.locale ?? defaultOptions.locale ?? enUS$1;
	const firstWeekContainsDate = options?.firstWeekContainsDate ?? options?.locale?.options?.firstWeekContainsDate ?? defaultOptions.firstWeekContainsDate ?? defaultOptions.locale?.options?.firstWeekContainsDate ?? 1;
	const weekStartsOn = options?.weekStartsOn ?? options?.locale?.options?.weekStartsOn ?? defaultOptions.weekStartsOn ?? defaultOptions.locale?.options?.weekStartsOn ?? 0;
	if (!formatStr) return dateStr ? invalidDate() : toDate(referenceDate, options?.in);
	const subFnOptions = {
		firstWeekContainsDate,
		weekStartsOn,
		locale
	};
	const setters = [new DateTimezoneSetter(options?.in, referenceDate)];
	const tokens = formatStr.match(longFormattingTokensRegExp).map((substring) => {
		const firstCharacter = substring[0];
		if (firstCharacter in longFormatters) {
			const longFormatter = longFormatters[firstCharacter];
			return longFormatter(substring, locale.formatLong);
		}
		return substring;
	}).join("").match(formattingTokensRegExp);
	const usedTokens = [];
	for (let token of tokens) {
		if (!options?.useAdditionalWeekYearTokens && isProtectedWeekYearToken(token)) warnOrThrowProtectedError(token, formatStr, dateStr);
		if (!options?.useAdditionalDayOfYearTokens && isProtectedDayOfYearToken(token)) warnOrThrowProtectedError(token, formatStr, dateStr);
		const firstCharacter = token[0];
		const parser = parsers[firstCharacter];
		if (parser) {
			const { incompatibleTokens } = parser;
			if (Array.isArray(incompatibleTokens)) {
				const incompatibleToken = usedTokens.find((usedToken) => incompatibleTokens.includes(usedToken.token) || usedToken.token === firstCharacter);
				if (incompatibleToken) throw new RangeError(`The format string mustn't contain \`${incompatibleToken.fullToken}\` and \`${token}\` at the same time`);
			} else if (parser.incompatibleTokens === "*" && usedTokens.length > 0) throw new RangeError(`The format string mustn't contain \`${token}\` and any other token at the same time`);
			usedTokens.push({
				token: firstCharacter,
				fullToken: token
			});
			const parseResult = parser.run(dateStr, token, locale.match, subFnOptions);
			if (!parseResult) return invalidDate();
			setters.push(parseResult.setter);
			dateStr = parseResult.rest;
		} else {
			if (firstCharacter.match(unescapedLatinCharacterRegExp)) throw new RangeError("Format string contains an unescaped latin alphabet character `" + firstCharacter + "`");
			if (token === "''") token = "'";
			else if (firstCharacter === "'") token = cleanEscapedString(token);
			if (dateStr.indexOf(token) === 0) dateStr = dateStr.slice(token.length);
			else return invalidDate();
		}
	}
	if (dateStr.length > 0 && notWhitespaceRegExp.test(dateStr)) return invalidDate();
	const uniquePrioritySetters = setters.map((setter) => setter.priority).sort((a, b) => b - a).filter((priority, index, array) => array.indexOf(priority) === index).map((priority) => setters.filter((setter) => setter.priority === priority).sort((a, b) => b.subPriority - a.subPriority)).map((setterArray) => setterArray[0]);
	let date = toDate(referenceDate, options?.in);
	if (isNaN(+date)) return invalidDate();
	const flags = {};
	for (const setter of uniquePrioritySetters) {
		if (!setter.validate(date, subFnOptions)) return invalidDate();
		const result = setter.set(date, flags, subFnOptions);
		if (Array.isArray(result)) {
			date = result[0];
			Object.assign(flags, result[1]);
		} else date = result;
	}
	return date;
}
function cleanEscapedString(input) {
	return input.match(escapedStringRegExp)[1].replace(doubleQuoteRegExp, "'");
}
//#endregion
//#region node_modules/date-fns/setDate.js
/**
* The {@link setDate} function options.
*/
/**
* @name setDate
* @category Day Helpers
* @summary Set the day of the month to the given date.
*
* @description
* Set the day of the month to the given date.
*
* @typeParam DateType - The `Date` type, the function operates on. Gets inferred from passed arguments. Allows using extensions like [`UTCDate`](https://github.com/date-fns/utc).
* @typeParam ResultDate - The result `Date` type, it is the type returned from the context function if it is passed, or inferred from the arguments.
*
* @param date - The date to be changed
* @param dayOfMonth - The day of the month of the new date
* @param options - The options
*
* @returns The new date with the day of the month set
*
* @example
* // Set the 30th day of the month to 1 September 2014:
* const result = setDate(new Date(2014, 8, 1), 30)
* //=> Tue Sep 30 2014 00:00:00
*/
function setDate(date, dayOfMonth, options) {
	const _date = toDate(date, options?.in);
	_date.setDate(dayOfMonth);
	return _date;
}
//#endregion
//#region node_modules/date-fns/setHours.js
/**
* The {@link setHours} function options.
*/
/**
* @name setHours
* @category Hour Helpers
* @summary Set the hours to the given date.
*
* @description
* Set the hours to the given date.
*
* @typeParam DateType - The `Date` type, the function operates on. Gets inferred from passed arguments. Allows to use extensions like [`UTCDate`](https://github.com/date-fns/utc).
* @typeParam ResultDate - The result `Date` type, it is the type returned from the context function if it is passed, or inferred from the arguments.
*
* @param date - The date to be changed
* @param hours - The hours of the new date
* @param options - An object with options
*
* @returns The new date with the hours set
*
* @example
* // Set 4 hours to 1 September 2014 11:30:00:
* const result = setHours(new Date(2014, 8, 1, 11, 30), 4)
* //=> Mon Sep 01 2014 04:30:00
*/
function setHours(date, hours, options) {
	const _date = toDate(date, options?.in);
	_date.setHours(hours);
	return _date;
}
//#endregion
//#region node_modules/date-fns/setMilliseconds.js
/**
* The {@link setMilliseconds} function options.
*/
/**
* @name setMilliseconds
* @category Millisecond Helpers
* @summary Set the milliseconds to the given date.
*
* @description
* Set the milliseconds to the given date.
*
* @typeParam DateType - The `Date` type, the function operates on. Gets inferred from passed arguments. Allows to use extensions like [`UTCDate`](https://github.com/date-fns/utc).
* @typeParam ResultDate - The result `Date` type, it is the type returned from the context function if it is passed, or inferred from the arguments.
*
* @param date - The date to be changed
* @param milliseconds - The milliseconds of the new date
* @param options - The options
*
* @returns The new date with the milliseconds set
*
* @example
* // Set 300 milliseconds to 1 September 2014 11:30:40.500:
* const result = setMilliseconds(new Date(2014, 8, 1, 11, 30, 40, 500), 300)
* //=> Mon Sep 01 2014 11:30:40.300
*/
function setMilliseconds(date, milliseconds, options) {
	const _date = toDate(date, options?.in);
	_date.setMilliseconds(milliseconds);
	return _date;
}
//#endregion
//#region node_modules/date-fns/setMinutes.js
/**
* The {@link setMinutes} function options.
*/
/**
* @name setMinutes
* @category Minute Helpers
* @summary Set the minutes to the given date.
*
* @description
* Set the minutes to the given date.
*
* @typeParam DateType - The `Date` type, the function operates on. Gets inferred from passed arguments. Allows using extensions like [`UTCDate`](https://github.com/date-fns/utc).
* @typeParam ResultDate - The result `Date` type, returned from the context function, or inferred from the arguments.
*
* @param date - The date to be changed
* @param minutes - The minutes of the new date
* @param options - An object with options
*
* @returns The new date with the minutes set
*
* @example
* // Set 45 minutes to 1 September 2014 11:30:40:
* const result = setMinutes(new Date(2014, 8, 1, 11, 30, 40), 45)
* //=> Mon Sep 01 2014 11:45:40
*/
function setMinutes(date, minutes, options) {
	const date_ = toDate(date, options?.in);
	date_.setMinutes(minutes);
	return date_;
}
//#endregion
//#region node_modules/date-fns/setMonth.js
/**
* The {@link setMonth} function options.
*/
/**
* @name setMonth
* @category Month Helpers
* @summary Set the month to the given date.
*
* @description
* Set the month to the given date.
*
* @typeParam DateType - The `Date` type, the function operates on. Gets inferred from passed arguments. Allows to use extensions like [`UTCDate`](https://github.com/date-fns/utc).
* @typeParam ResultDate - The result `Date` type, it is the type returned from the context function if it is passed, or inferred from the arguments.
*
* @param date - The date to be changed
* @param month - The month index to set (0-11)
* @param options - The options
*
* @returns The new date with the month set
*
* @example
* // Set February to 1 September 2014:
* const result = setMonth(new Date(2014, 8, 1), 1)
* //=> Sat Feb 01 2014 00:00:00
*/
function setMonth(date, month, options) {
	const _date = toDate(date, options?.in);
	const year = _date.getFullYear();
	const day = _date.getDate();
	const midMonth = constructFrom(options?.in || date, 0);
	midMonth.setFullYear(year, month, 15);
	midMonth.setHours(0, 0, 0, 0);
	const daysInMonth = getDaysInMonth(midMonth);
	_date.setMonth(month, Math.min(day, daysInMonth));
	return _date;
}
//#endregion
//#region node_modules/date-fns/setSeconds.js
/**
* The {@link setSeconds} function options.
*/
/**
* @name setSeconds
* @category Second Helpers
* @summary Set the seconds to the given date, with context support.
*
* @description
* Set the seconds to the given date, with an optional context for time zone specification.
*
* @typeParam DateType - The `Date` type, the function operates on. Gets inferred from passed arguments. Allows to use extensions like [`UTCDate`](https://github.com/date-fns/utc).
* @typeParam ResultDate - The result `Date` type, it is the type returned from the context function if it is passed, or inferred from the arguments.
*
* @param date - The date to be changed
* @param seconds - The seconds of the new date
* @param options - An object with options
*
* @returns The new date with the seconds set
*
* @example
* // Set 45 seconds to 1 September 2014 11:30:40:
* const result = setSeconds(new Date(2014, 8, 1, 11, 30, 40), 45)
* //=> Mon Sep 01 2014 11:30:45
*/
function setSeconds(date, seconds, options) {
	const _date = toDate(date, options?.in);
	_date.setSeconds(seconds);
	return _date;
}
//#endregion
//#region node_modules/date-fns/setYear.js
/**
* The {@link setYear} function options.
*/
/**
* @name setYear
* @category Year Helpers
* @summary Set the year to the given date.
*
* @description
* Set the year to the given date.
*
* @typeParam DateType - The `Date` type, the function operates on. Gets inferred from passed arguments. Allows to use extensions like [`UTCDate`](https://github.com/date-fns/utc).
* @typeParam ResultDate - The result `Date` type, it is the type returned from the context function if it is passed, or inferred from the arguments.
*
* @param date - The date to be changed
* @param year - The year of the new date
* @param options - An object with options.
*
* @returns The new date with the year set
*
* @example
* // Set year 2013 to 1 September 2014:
* const result = setYear(new Date(2014, 8, 1), 2013)
* //=> Sun Sep 01 2013 00:00:00
*/
function setYear(date, year, options) {
	const date_ = toDate(date, options?.in);
	if (isNaN(+date_)) return constructFrom(options?.in || date, NaN);
	date_.setFullYear(year);
	return date_;
}
//#endregion
//#region node_modules/date-fns/startOfMinute.js
/**
* The {@link startOfMinute} function options.
*/
/**
* @name startOfMinute
* @category Minute Helpers
* @summary Return the start of a minute for the given date.
*
* @description
* Return the start of a minute for the given date.
* The result will be in the local timezone.
*
* @typeParam DateType - The `Date` type, the function operates on. Gets inferred from passed arguments. Allows to use extensions like [`UTCDate`](https://github.com/date-fns/utc).
* @typeParam ResultDate - The result `Date` type, it is the type returned from the context function if it is passed, or inferred from the arguments.
*
* @param date - The original date
* @param options - An object with options
*
* @returns The start of a minute
*
* @example
* // The start of a minute for 1 December 2014 22:15:45.400:
* const result = startOfMinute(new Date(2014, 11, 1, 22, 15, 45, 400))
* //=> Mon Dec 01 2014 22:15:00
*/
function startOfMinute(date, options) {
	const date_ = toDate(date, options?.in);
	date_.setSeconds(0, 0);
	return date_;
}
//#endregion
//#region node_modules/date-fns/startOfMonth.js
/**
* The {@link startOfMonth} function options.
*/
/**
* @name startOfMonth
* @category Month Helpers
* @summary Return the start of a month for the given date.
*
* @description
* Return the start of a month for the given date. The result will be in the local timezone.
*
* @typeParam DateType - The `Date` type, the function operates on. Gets inferred from passed arguments.
* Allows to use extensions like [`UTCDate`](https://github.com/date-fns/utc).
* @typeParam ResultDate - The result `Date` type, it is the type returned from the context function if it is passed,
* or inferred from the arguments.
*
* @param date - The original date
* @param options - An object with options
*
* @returns The start of a month
*
* @example
* // The start of a month for 2 September 2014 11:55:00:
* const result = startOfMonth(new Date(2014, 8, 2, 11, 55, 0))
* //=> Mon Sep 01 2014 00:00:00
*/
function startOfMonth(date, options) {
	const _date = toDate(date, options?.in);
	_date.setDate(1);
	_date.setHours(0, 0, 0, 0);
	return _date;
}
//#endregion
//#region node_modules/date-fns/startOfSecond.js
/**
* The {@link startOfSecond} function options.
*/
/**
* @name startOfSecond
* @category Second Helpers
* @summary Return the start of a second for the given date.
*
* @description
* Return the start of a second for the given date.
* The result will be in the local timezone.
*
* @typeParam DateType - The `Date` type, the function operates on. Gets inferred from passed arguments. Allows to use extensions like [`UTCDate`](https://github.com/date-fns/utc).
* @typeParam ResultDate - The result `Date` type, it is the type returned from the context function if it is passed, or inferred from the arguments.
*
* @param date - The original date
* @param options - The options
*
* @returns The start of a second
*
* @example
* // The start of a second for 1 December 2014 22:15:45.400:
* const result = startOfSecond(new Date(2014, 11, 1, 22, 15, 45, 400))
* //=> Mon Dec 01 2014 22:15:45.000
*/
function startOfSecond(date, options) {
	const date_ = toDate(date, options?.in);
	date_.setMilliseconds(0);
	return date_;
}
//#endregion
//#region node_modules/@date-fns/tz/tzName/index.js
/**
* Time zone name format.
*/
/**
* The function returns the time zone name for the given date in the specified
* time zone.
*
* It uses the `Intl.DateTimeFormat` API and by default outputs the time zone
* name in a long format, e.g. "Pacific Standard Time" or
* "Singapore Standard Time".
*
* It is possible to specify the format as the third argument using one of the following options
*
* - "short": e.g. "EDT" or "GMT+8".
* - "long": e.g. "Eastern Daylight Time".
* - "shortGeneric": e.g. "ET" or "Singapore Time".
* - "longGeneric": e.g. "Eastern Time" or "Singapore Standard Time".
*
* These options correspond to TR35 tokens `z..zzz`, `zzzz`, `v`, and `vvvv` respectively: https://www.unicode.org/reports/tr35/tr35-dates.html#dfst-zone
*
* @param timeZone - Time zone name (IANA or UTC offset)
* @param date - Date object to get the time zone name for
* @param format - Optional format of the time zone name. Defaults to "long". Can be "short", "long", "shortGeneric", or "longGeneric".
*
* @returns Time zone name (e.g. "Singapore Standard Time")
*/
function tzName(timeZone, date, format = "long") {
	return new Intl.DateTimeFormat("en-US", {
		hour: "numeric",
		timeZone,
		timeZoneName: format
	}).format(date).split(/\s/g).slice(2).join(" ");
}
//#endregion
//#region node_modules/@date-fns/tz/tzOffset/index.js
var offsetFormatCache = {};
var offsetCache = {};
/**
* The function returns UTC offset in minutes for the given date in specified
* time zone.
*
* Unlike `Date.prototype.getTimezoneOffset`, this function returns the value
* mirrored to the sign of the offset in the time zone. For Asia/Singapore
* (UTC+8), `tzOffset` returns `480`, while `getTimezoneOffset` returns `-480`.
*
* It uses `Intl.DateTimeFormat` internally to access otherwise unavailable
* runtime's time zone data, and falls back to basic manual parsing if Intl API
* is not supported (e.g., older Node.js versions, React Native's Hermes, etc.).
*
* @param timeZone - Time zone name (IANA or UTC offset).
* @param date - Date to check the offset for.
*
* @returns UTC offset in minutes (e.g., `480` for date in UTC+8).
*/
function tzOffset(timeZone, date) {
	try {
		const offsetStr = (offsetFormatCache[timeZone] ||= new Intl.DateTimeFormat("en-US", {
			timeZone,
			timeZoneName: "longOffset"
		}).format)(date).split("GMT")[1];
		if (offsetStr in offsetCache) return offsetCache[offsetStr];
		return calcOffset(offsetStr, offsetStr.split(":"));
	} catch {
		if (timeZone in offsetCache) return offsetCache[timeZone];
		const captures = timeZone?.match(offsetRe);
		if (captures) return calcOffset(timeZone, captures.slice(1));
		return NaN;
	}
}
var offsetRe = /([+-]\d\d):?(\d\d)?/;
function calcOffset(cacheStr, values) {
	const hours = +(values[0] || 0);
	const minutes = +(values[1] || 0);
	const seconds = +(values[2] || 0) / 60;
	return offsetCache[cacheStr] = hours * 60 + minutes > 0 ? hours * 60 + minutes + seconds : hours * 60 - minutes - seconds;
}
//#endregion
//#region node_modules/@date-fns/tz/date/mini.js
var TZDateMini = class TZDateMini extends Date {
	constructor(...args) {
		super();
		if (args.length > 1 && typeof args[args.length - 1] === "string") this.timeZone = args.pop();
		this.internal = /* @__PURE__ */ new Date();
		if (isNaN(tzOffset(this.timeZone, this))) this.setTime(NaN);
		else if (!args.length) this.setTime(Date.now());
		else if (typeof args[0] === "number" && (args.length === 1 || args.length === 2 && typeof args[1] !== "number")) this.setTime(args[0]);
		else if (typeof args[0] === "string") this.setTime(+new Date(args[0]));
		else if (args[0] instanceof Date) this.setTime(+args[0]);
		else {
			this.setTime(+new Date(...args));
			adjustToSystemTZ(this, args);
		}
	}
	static tz(tz, ...args) {
		return args.length ? new TZDateMini(...args, tz) : new TZDateMini(Date.now(), tz);
	}
	withTimeZone(timeZone) {
		return new TZDateMini(+this, timeZone);
	}
	getTimezoneOffset() {
		const offset = -tzOffset(this.timeZone, this);
		return offset > 0 ? Math.floor(offset) : Math.ceil(offset);
	}
	setTime(_time) {
		Date.prototype.setTime.apply(this, arguments);
		syncToInternal(this);
		return +this;
	}
	[Symbol.for("constructDateFrom")](date) {
		return new TZDateMini(+new Date(date), this.timeZone);
	}
};
var re = /^(get|set)(?!UTC)/;
Object.getOwnPropertyNames(Date.prototype).forEach((method) => {
	if (!re.test(method)) return;
	const utcMethod = method.replace(re, "$1UTC");
	if (!TZDateMini.prototype[utcMethod]) return;
	if (method.startsWith("get")) TZDateMini.prototype[method] = function() {
		return this.internal[utcMethod]();
	};
	else {
		TZDateMini.prototype[method] = function() {
			Date.prototype[utcMethod].apply(this.internal, arguments);
			syncFromInternal(this);
			return +this;
		};
		TZDateMini.prototype[utcMethod] = function() {
			Date.prototype[utcMethod].apply(this, arguments);
			syncToInternal(this);
			return +this;
		};
	}
});
/**
* @internal
* Function syncs time to internal date, applying the current time zone offset.
*
* @param {Date} date - `Date` to sync
*/
function syncToInternal(date) {
	date.internal.setTime(+date);
	date.internal.setUTCSeconds(date.internal.getUTCSeconds() - Math.round(-tzOffset(date.timeZone, date) * 60));
}
/**
* @internal
* Function syncs internal wall-clock fields into the external date.
*
* @param {Date} date - The date to sync
*/
function syncFromInternal(date) {
	Date.prototype.setFullYear.call(date, date.internal.getUTCFullYear(), date.internal.getUTCMonth(), date.internal.getUTCDate());
	Date.prototype.setHours.call(date, date.internal.getUTCHours(), date.internal.getUTCMinutes(), date.internal.getUTCSeconds(), date.internal.getUTCMilliseconds());
	adjustToSystemTZ(date);
}
/**
* @internal
* Function adjusts the date to the system time zone. It uses the time zone
* differences to calculate the offset and adjust the date.
*
* We use it when constructing `TZDate` and syncing the external date from
* the internal one.
*
* @param {TZDate} date - `TZDate` to adjust.
*/
function adjustToSystemTZ(date, constructorArgs) {
	const expectedInternalTime = Array.isArray(constructorArgs) ? constructorArgsToInternalTime(constructorArgs) : +date.internal;
	const offsetWithSeconds = tzOffset(date.timeZone, date);
	const offset = offsetWithSeconds > 0 ? Math.floor(offsetWithSeconds) : Math.ceil(offsetWithSeconds);
	const prevHour = /* @__PURE__ */ new Date(+date);
	prevHour.setUTCHours(prevHour.getUTCHours() - 1);
	const systemOffset = -(/* @__PURE__ */ new Date(+date)).getTimezoneOffset();
	const prevHourSystemOffset = -(/* @__PURE__ */ new Date(+prevHour)).getTimezoneOffset();
	const systemDSTChange = systemOffset - prevHourSystemOffset;
	let systemOffsetForDiff = systemOffset;
	if (systemDSTChange && systemOffset !== offset) {
		if (Date.prototype.getHours.apply(date) !== (Array.isArray(constructorArgs) ? constructorArgs[3] || 0 : date.internal.getUTCHours())) {
			const testDate = /* @__PURE__ */ new Date(+date);
			const testOffsetDiff = systemOffset - offset;
			if (testOffsetDiff) testDate.setUTCMinutes(testDate.getUTCMinutes() + testOffsetDiff);
			const testOffsetWithSeconds = tzOffset(date.timeZone, testDate);
			if ((testOffsetWithSeconds > 0 ? Math.floor(testOffsetWithSeconds) : Math.ceil(testOffsetWithSeconds)) === offset) systemOffsetForDiff = prevHourSystemOffset;
		}
	}
	const offsetDiff = systemOffsetForDiff - offset;
	if (offsetDiff) Date.prototype.setUTCMinutes.call(date, Date.prototype.getUTCMinutes.call(date) + offsetDiff);
	const systemDate = /* @__PURE__ */ new Date(+date);
	systemDate.setUTCSeconds(0);
	const systemSecondsOffset = systemOffset > 0 ? systemDate.getSeconds() : (systemDate.getSeconds() - 60) % 60;
	const secondsOffset = Math.round(-(tzOffset(date.timeZone, date) * 60)) % 60;
	if (secondsOffset || systemSecondsOffset) Date.prototype.setUTCSeconds.call(date, Date.prototype.getUTCSeconds.call(date) + secondsOffset + systemSecondsOffset);
	const postOffsetWithSeconds = tzOffset(date.timeZone, date);
	const postOffset = postOffsetWithSeconds > 0 ? Math.floor(postOffsetWithSeconds) : Math.ceil(postOffsetWithSeconds);
	const postOffsetDiff = -(/* @__PURE__ */ new Date(+date)).getTimezoneOffset() - postOffset;
	const offsetChanged = postOffset !== offset;
	const postDiff = postOffsetDiff - offsetDiff;
	const targetDSTShift = postOffset - offset;
	const postOffsetCandidate = expectedInternalTime - postOffset * 60 * 1e3;
	const normalizedTargetDSTGap = targetDSTShift > 0 && targetInternalTime(date) - expectedInternalTime === targetDSTShift * 60 * 1e3 && targetInternalTime(date, postOffsetCandidate) !== expectedInternalTime;
	if (offsetChanged && postDiff && !normalizedTargetDSTGap) {
		Date.prototype.setUTCMinutes.call(date, Date.prototype.getUTCMinutes.call(date) + postDiff);
		const newOffsetWithSeconds = tzOffset(date.timeZone, date);
		const offsetChange = postOffset - (newOffsetWithSeconds > 0 ? Math.floor(newOffsetWithSeconds) : Math.ceil(newOffsetWithSeconds));
		if (offsetChange && postDiff < 0) Date.prototype.setUTCMinutes.call(date, Date.prototype.getUTCMinutes.call(date) + offsetChange);
	}
	syncToInternal(date);
	const drift = (constructorArgs ? expectedInternalTime : expectedInternalTime + secondsOffset * 1e3) - +date.internal;
	if (drift && Math.abs(drift) < 1800 * 1e3) {
		Date.prototype.setTime.call(date, +date + drift);
		syncToInternal(date);
	}
}
function constructorArgsToInternalTime(args) {
	return Date.UTC(args[0], args.length > 1 ? args[1] : 0, args.length > 2 ? args[2] : 1, ...args.slice(3));
}
function targetInternalTime(date, time) {
	const internal = new Date(time ?? +date);
	internal.setUTCSeconds(internal.getUTCSeconds() - Math.round(-tzOffset(date.timeZone, internal) * 60));
	return +internal;
}
//#endregion
//#region node_modules/@date-fns/tz/date/index.js
var TZDate = class TZDate extends TZDateMini {
	static tz(tz, ...args) {
		return args.length ? new TZDate(...args, tz) : new TZDate(Date.now(), tz);
	}
	toISOString() {
		const [sign, hours, minutes] = this.tzComponents();
		const tz = `${sign}${hours}:${minutes}`;
		return this.internal.toISOString().slice(0, -1) + tz;
	}
	toString() {
		return `${this.toDateString()} ${this.toTimeString()}`;
	}
	toDateString() {
		const [day, date, month, year] = this.internal.toUTCString().split(" ");
		return `${day?.slice(0, -1)} ${month} ${date} ${year}`;
	}
	toTimeString() {
		const time = this.internal.toUTCString().split(" ")[4];
		const [sign, hours, minutes] = this.tzComponents();
		return `${time} GMT${sign}${hours}${minutes} (${tzName(this.timeZone, this)})`;
	}
	toLocaleString(locales, options) {
		return Date.prototype.toLocaleString.call(this, locales, {
			...options,
			timeZone: options?.timeZone || this.timeZone
		});
	}
	toLocaleDateString(locales, options) {
		return Date.prototype.toLocaleDateString.call(this, locales, {
			...options,
			timeZone: options?.timeZone || this.timeZone
		});
	}
	toLocaleTimeString(locales, options) {
		return Date.prototype.toLocaleTimeString.call(this, locales, {
			...options,
			timeZone: options?.timeZone || this.timeZone
		});
	}
	tzComponents() {
		const offset = this.getTimezoneOffset();
		return [
			offset > 0 ? "-" : "+",
			String(Math.floor(Math.abs(offset) / 60)).padStart(2, "0"),
			String(Math.abs(offset) % 60).padStart(2, "0")
		];
	}
	withTimeZone(timeZone) {
		return new TZDate(+this, timeZone);
	}
	[Symbol.for("constructDateFrom")](date) {
		return new TZDate(+new Date(date), this.timeZone);
	}
};
//#endregion
//#region node_modules/@mui/x-scheduler-internals/base-ui-copy/temporal-adapter-date-fns/TemporalAdapterDateFns.mjs
var FORMATS = {
	yearPadded: "yyyy",
	monthPadded: "MM",
	dayOfMonthPadded: "dd",
	hours24hPadded: "HH",
	hours12hPadded: "hh",
	minutesPadded: "mm",
	secondsPadded: "ss",
	dayOfMonth: "d",
	hours24h: "H",
	hours12h: "h",
	month3Letters: "MMM",
	monthFullLetter: "MMMM",
	weekday: "EEEE",
	weekday3Letters: "EEE",
	weekday1Letter: "EEEEE",
	meridiem: "a",
	localizedDateWithFullMonthAndWeekDay: "PPPP",
	localizedNumericDate: "P"
};
var TemporalAdapterDateFns = class {
	isTimezoneCompatible = true;
	lib = "date-fns";
	formats = FORMATS;
	escapedCharacters = {
		start: "'",
		end: "'"
	};
	constructor({ locale } = {}) {
		this.locale = locale ?? enUS$1;
	}
	now = (timezone) => {
		if (timezone === "system" || timezone === "default") return /* @__PURE__ */ new Date();
		return TZDate.tz(timezone);
	};
	date = (value, timezone) => {
		if (value === null) return null;
		const date = new Date(value);
		const isDateOnly = typeof value === "string" && !value.includes("T");
		if (timezone === "system" || timezone === "default") {
			if (isDateOnly) return new Date(date.getUTCFullYear(), date.getUTCMonth(), date.getUTCDate());
			return date;
		}
		return new TZDate(isDateOnly ? date.getUTCFullYear() : date.getFullYear(), isDateOnly ? date.getUTCMonth() : date.getMonth(), isDateOnly ? date.getUTCDate() : date.getDate(), isDateOnly ? date.getUTCHours() : date.getHours(), isDateOnly ? date.getUTCMinutes() : date.getMinutes(), isDateOnly ? date.getUTCSeconds() : date.getSeconds(), isDateOnly ? date.getUTCMilliseconds() : date.getMilliseconds(), timezone);
	};
	parse = (value, format, timezone) => {
		const date = parse(value, format, /* @__PURE__ */ new Date(), { locale: this.locale });
		if (timezone === "system" || timezone === "default") return date;
		return new TZDate(date.getFullYear(), date.getMonth(), date.getDate(), date.getHours(), date.getMinutes(), date.getSeconds(), date.getMilliseconds(), timezone);
	};
	getTimezone = (value) => {
		if (value instanceof TZDate) return value.timeZone ?? "system";
		return "system";
	};
	setTimezone = (value, timezone) => {
		const isSystemTimezone = timezone === "system" || timezone === "default";
		const canChangeTz = typeof value?.withTimeZone === "function";
		if (isSystemTimezone) return this.toJsDate(value);
		if (canChangeTz) return value.withTimeZone(timezone);
		return new TZDate(value, timezone);
	};
	toJsDate = (value) => {
		if (value instanceof TZDate) return new Date(value.getTime());
		return value;
	};
	getCurrentLocaleCode = () => {
		return this.locale.code;
	};
	isValid = (value) => {
		if (value == null) return false;
		return isValid(value);
	};
	format = (value, formatKey) => {
		return this.formatByString(value, this.formats[formatKey]);
	};
	formatByString = (value, format$1) => {
		return format(value, format$1, { locale: this.locale });
	};
	isEqual = (value, comparing) => {
		if (value === null && comparing === null) return true;
		if (value === null || comparing === null) return false;
		return isEqual(value, comparing);
	};
	isSameYear = (value, comparing) => {
		return isSameYear(value, comparing);
	};
	isSameMonth = (value, comparing) => {
		return isSameMonth(value, comparing);
	};
	isSameDay = (value, comparing) => {
		return isSameDay(value, comparing);
	};
	isSameHour = (value, comparing) => {
		return isSameHour(value, comparing);
	};
	isAfter = (value, comparing) => {
		return isAfter(value, comparing);
	};
	isBefore = (value, comparing) => {
		return isBefore(value, comparing);
	};
	isWithinRange = (value, [start, end]) => {
		return isWithinInterval(value, {
			start,
			end
		});
	};
	startOfYear = (value) => {
		return startOfYear(value);
	};
	startOfMonth = (value) => {
		return startOfMonth(value);
	};
	startOfWeek = (value) => {
		return startOfWeek(value, { locale: this.locale });
	};
	startOfDay = (value) => {
		return startOfDay(value);
	};
	startOfHour = (value) => {
		return startOfHour(value);
	};
	startOfMinute = (value) => {
		return startOfMinute(value);
	};
	startOfSecond = (value) => {
		return startOfSecond(value);
	};
	endOfYear = (value) => {
		return endOfYear(value);
	};
	endOfMonth = (value) => {
		return endOfMonth(value);
	};
	endOfWeek = (value) => {
		return endOfWeek(value, { locale: this.locale });
	};
	endOfDay = (value) => {
		return endOfDay(value);
	};
	endOfHour = (value) => {
		return endOfHour(value);
	};
	endOfMinute = (value) => {
		return endOfMinute(value);
	};
	endOfSecond = (value) => {
		return endOfSecond(value);
	};
	addYears = (value, amount) => {
		return addYears(value, amount);
	};
	addMonths = (value, amount) => {
		return addMonths(value, amount);
	};
	addWeeks = (value, amount) => {
		return addWeeks(value, amount);
	};
	addDays = (value, amount) => {
		return addDays(value, amount);
	};
	addHours = (value, amount) => {
		return addHours(value, amount);
	};
	addMinutes = (value, amount) => {
		return addMinutes(value, amount);
	};
	addSeconds = (value, amount) => {
		return addSeconds(value, amount);
	};
	addMilliseconds = (value, amount) => {
		return addMilliseconds(value, amount);
	};
	getYear = (value) => {
		return getYear(value);
	};
	getMonth = (value) => {
		return getMonth(value);
	};
	getDate = (value) => {
		return getDate(value);
	};
	getHours = (value) => {
		return getHours(value);
	};
	getMinutes = (value) => {
		return getMinutes(value);
	};
	getSeconds = (value) => {
		return getSeconds(value);
	};
	getMilliseconds = (value) => {
		return getMilliseconds(value);
	};
	getTime = (value) => {
		return value.getTime();
	};
	setYear = (value, year) => {
		return setYear(value, year);
	};
	setMonth = (value, month) => {
		return setMonth(value, month);
	};
	setDate = (value, date) => {
		return setDate(value, date);
	};
	setHours = (value, hours) => {
		return setHours(value, hours);
	};
	setMinutes = (value, minutes) => {
		return setMinutes(value, minutes);
	};
	setSeconds = (value, seconds) => {
		return setSeconds(value, seconds);
	};
	setMilliseconds = (value, milliseconds) => {
		return setMilliseconds(value, milliseconds);
	};
	differenceInYears = (value, comparing) => {
		return differenceInYears(value, comparing);
	};
	differenceInMonths = (value, comparing) => {
		return differenceInMonths(value, comparing);
	};
	differenceInWeeks = (value, comparing) => {
		return differenceInWeeks(value, comparing);
	};
	differenceInDays = (value, comparing) => {
		return differenceInDays(value, comparing);
	};
	differenceInHours = (value, comparing) => {
		return differenceInHours(value, comparing);
	};
	differenceInMinutes = (value, comparing) => {
		return differenceInMinutes(value, comparing);
	};
	getDaysInMonth = (value) => {
		return getDaysInMonth(value);
	};
	getWeekNumber = (value) => {
		return getWeek(value, { locale: this.locale });
	};
	getDayOfWeek = (value) => {
		const weekStartsOn = this.locale.options?.weekStartsOn ?? 0;
		return (getDay(value) + 7 - weekStartsOn) % 7 + 1;
	};
};
//#endregion
//#region node_modules/@mui/x-scheduler-internals/use-scheduler-store-context/useSchedulerStoreContext.mjs
var SchedulerStoreContext = /*#__PURE__*/ import_react.createContext(null);
SchedulerStoreContext.displayName = "SchedulerStoreContext";
function useSchedulerStoreContext(optional = false) {
	const context = import_react.useContext(SchedulerStoreContext);
	if (context == null && !optional) throw new Error("MUI X Scheduler: useSchedulerStoreContext must be used within an <EventCalendar /> or a <EventTimelinePremium /> component");
	return context;
}
//#endregion
//#region node_modules/@mui/x-scheduler-internals/use-adapter-context/useAdapterContext.mjs
var DEFAULT_ADAPTER$1 = new TemporalAdapterDateFns();
var getAdapter = (state) => state.adapter;
/**
* Reads the adapter from the nearest scheduler store context.
* Use this in child components instead of `useAdapter()`.
* Falls back to the default adapter when used outside a store context (e.g. in unit tests).
*/
function useAdapterContext() {
	const store = import_react.useContext(SchedulerStoreContext);
	const subscribe = import_react.useCallback((cb) => store ? store.subscribe(cb) : () => {}, [store]);
	const getSnapshot = import_react.useCallback(() => store ? getAdapter(store.state) : DEFAULT_ADAPTER$1, [store]);
	return (0, import_shim.useSyncExternalStore)(subscribe, getSnapshot, getSnapshot);
}
//#endregion
//#region node_modules/@mui/x-scheduler-internals/use-event-calendar-store-context/useEventCalendarStoreContext.mjs
function useEventCalendarStoreContext() {
	const context = useSchedulerStoreContext();
	if (context.instanceName !== "EventCalendarStore" && context.instanceName !== "EventCalendarPremiumStore") throw new Error("MUI X Scheduler: useEventCalendarStoreContext must be used within an <EventCalendar /> component");
	return context;
}
//#endregion
//#region node_modules/@mui/x-scheduler-internals/use-event-calendar-view/useEventCalendarView.mjs
/**
* Initializes the view on the event calendar.
* The initialization is only done during the 1st render, the definition should be defined statically.
* ```
* @param parameters Parameters for the view.
*/
function useEventCalendarView(definition) {
	const store = useEventCalendarStoreContext();
	const days = useStore(store, definition.visibleDaysSelector);
	useOnMount(() => store.setViewDefinition(definition));
	return { days };
}
//#endregion
//#region node_modules/@mui/x-scheduler-internals/sort-event-occurrences/sortEventOccurrences.mjs
/**
* Sorts event occurrences by their start date (earliest first).
* If two occurrences have the same start date, the one with the later end date comes first.
*/
function sortEventOccurrences(occurrences) {
	return occurrences.toSorted((a, b) => a.displayTimezone.start.timestamp - b.displayTimezone.start.timestamp || b.displayTimezone.end.timestamp - a.displayTimezone.end.timestamp);
}
//#endregion
//#region node_modules/@base-ui/utils/warn.mjs
var set$1 = /* @__PURE__ */ new Set();
function warn(...messages) {
	{
		const messageKey = messages.join(" ");
		if (!set$1.has(messageKey)) {
			set$1.add(messageKey);
			console.warn(`Base UI: ${messageKey}`);
		}
	}
}
//#endregion
//#region node_modules/@mui/x-internals/disposable/index.mjs
/**
* Well-known `Symbol.dispose`. Resolves to the native well-known symbol when
* the runtime ships the explicit resource management proposal; otherwise falls
* back to `Symbol.for('Symbol.dispose')`, the same registry symbol used by the
* fallback stacks below, so `DisposableStack#use` finds the method.
*
* Consumers should key dispose methods on this constant —
* `class Foo { [disposeSymbol]() {} }` — rather than `Symbol.dispose`, so the
* class works on runtimes without the proposal.
*/
var disposeSymbol = typeof Symbol.dispose === "symbol" ? Symbol.dispose : Symbol.for("Symbol.dispose");
/** Object disposable via {@link disposeSymbol}. Mirrors `Disposable` from `esnext.disposable`. */
/** Object disposable via {@link asyncDisposeSymbol}. Mirrors `AsyncDisposable` from `esnext.disposable`. */
/**
* Mirrors the `DisposableStack` interface from `esnext.disposable`, declared
* locally so consumers don't need that lib in their TypeScript config.
*/
/**
* Mirrors the `AsyncDisposableStack` interface from `esnext.disposable`, declared
* locally so consumers don't need that lib in their TypeScript config.
*/
var NativeSuppressedError = typeof SuppressedError === "function" ? SuppressedError : void 0;
/**
* Builds a `SuppressedError` (native when available, otherwise a compatible
* `{ error, suppressed }` Error) so {@link unwrapSuppressedErrors} can flatten
* the chain produced when several disposers throw.
*/
function createSuppressedError(error, suppressed) {
	if (NativeSuppressedError) return new NativeSuppressedError(error, suppressed);
	return Object.assign(/* @__PURE__ */ new Error("An error was suppressed during disposal."), {
		name: "SuppressedError",
		error,
		suppressed
	});
}
function assertNotDisposed(disposed) {
	if (disposed) throw new ReferenceError("MUI X: The disposable stack is disposed.");
}
/**
* Minimal fallback for `DisposableStack`, used on runtimes that do not ship the
* explicit resource management proposal. Implements the subset of the proposal
* MUI X uses — `use`, `adopt`, `defer`, `move`, `dispose`, `disposed` and
* `[Symbol.dispose]` — so the `core-js-pure` ponyfill no longer has to be bundled.
*/
var FallbackDisposableStack = class FallbackDisposableStack {
	stack = [];
	isDisposed = false;
	get disposed() {
		return this.isDisposed;
	}
	use(value) {
		assertNotDisposed(this.isDisposed);
		if (value != null) {
			const method = value[disposeSymbol];
			if (typeof method !== "function") throw new TypeError("MUI X: The value is not disposable.");
			this.stack.push(() => method.call(value));
		}
		return value;
	}
	adopt(value, onDispose) {
		assertNotDisposed(this.isDisposed);
		this.stack.push(() => onDispose(value));
		return value;
	}
	defer(onDispose) {
		assertNotDisposed(this.isDisposed);
		this.stack.push(onDispose);
	}
	move() {
		assertNotDisposed(this.isDisposed);
		const next = new FallbackDisposableStack();
		next.stack = this.stack;
		this.stack = [];
		this.isDisposed = true;
		return next;
	}
	dispose() {
		if (this.isDisposed) return;
		this.isDisposed = true;
		let hasError = false;
		let error;
		for (let i = this.stack.length - 1; i >= 0; i -= 1) try {
			this.stack[i]();
		} catch (caught) {
			error = hasError ? createSuppressedError(caught, error) : caught;
			hasError = true;
		}
		this.stack = [];
		if (hasError) throw error;
	}
	[disposeSymbol]() {
		this.dispose();
	}
};
/**
* Spec-compliant `DisposableStack`. Prefers the platform's native class when
* present; falls back to a minimal built-in implementation otherwise.
*/
var DisposableStack = globalThis.DisposableStack ?? FallbackDisposableStack;
globalThis.AsyncDisposableStack;
/**
* Unwraps a `SuppressedError` chain (as produced by `DisposableStack.dispose()`
* when multiple disposers throw) into a flat list, outermost failure first.
* Returns `[error]` unchanged if it isn't a `SuppressedError`.
*/
function unwrapSuppressedErrors(error) {
	const failures = [];
	let current = error;
	while (typeof current === "object" && current !== null && "error" in current && "suppressed" in current) {
		failures.push(current.error);
		current = current.suppressed;
	}
	failures.push(current);
	return failures;
}
//#endregion
//#region node_modules/@mui/x-scheduler-internals/scheduler-selectors/schedulerResourceSelectors.mjs
var resourceParentIdLookupSelector = createSelectorMemoized((state) => state.resourceChildrenIdLookup, (resourceChildrenIdLookup) => {
	const result = /* @__PURE__ */ new Map();
	for (const [resourceId, childrenIds] of resourceChildrenIdLookup) for (const childId of childrenIds) result.set(childId, resourceId);
	return result;
});
/**
* Walks the resource hierarchy (child → parent → …) and returns the first
* defined value found by `getValue`, or `fallback` if none is found.
*/
function resolveResourceProperty(state, resourceId, getValue, fallback) {
	const parentLookup = resourceParentIdLookupSelector(state);
	let currentId = resourceId ?? null;
	while (currentId != null) {
		const resource = state.processedResourceLookup.get(currentId);
		if (resource != null) {
			const value = getValue(resource);
			if (value !== void 0) return value;
		}
		currentId = parentLookup.get(currentId) ?? null;
	}
	return fallback;
}
var resourceDepthLookupSelector = createSelectorMemoized(resourceParentIdLookupSelector, (state) => state.processedResourceLookup, (parentLookup, processedResourceLookup) => {
	const result = /* @__PURE__ */ new Map();
	const cache = /* @__PURE__ */ new Map();
	const getDepth = (resourceId) => {
		const cached = cache.get(resourceId);
		if (cached !== void 0) return cached;
		const parentId = parentLookup.get(resourceId);
		const depth = parentId ? getDepth(parentId) + 1 : 0;
		cache.set(resourceId, depth);
		return depth;
	};
	for (const resourceId of processedResourceLookup.keys()) result.set(resourceId, getDepth(resourceId));
	return result;
});
var resourceHasVisibleChildrenLookupSelector = createSelectorMemoized((state) => state.resourceChildrenIdLookup, (state) => state.visibleResources, (childrenIdLookup, visibleResources) => {
	const result = /* @__PURE__ */ new Map();
	for (const [resourceId, childrenIds] of childrenIdLookup) result.set(resourceId, childrenIds.some((childId) => visibleResources[childId] !== false));
	return result;
});
var schedulerResourceSelectors = {
	processedResource: createSelector((state) => state.processedResourceLookup, (processedResourceLookup, resourceId) => resourceId == null ? null : processedResourceLookup.get(resourceId) ?? null),
	processedResourceList: createSelectorMemoized((state) => state.resourceIdList, (state) => state.processedResourceLookup, (resourceIds, processedResourceLookup) => resourceIds.map((id) => processedResourceLookup.get(id))),
	processedResourceFlatList: createSelectorMemoized((state) => state.resourceIdList, (state) => state.processedResourceLookup, (state) => state.resourceChildrenIdLookup, (resourceIds, processedResourceLookup, resourceChildrenIdLookup) => {
		const flatList = [];
		const addResourceAndChildren = (resourceId) => {
			const resource = processedResourceLookup.get(resourceId);
			if (!resource) return;
			flatList.push(resource);
			const childrenIds = resourceChildrenIdLookup.get(resourceId);
			if (childrenIds?.length) for (const childId of childrenIds) addResourceAndChildren(childId);
		};
		for (const resourceId of resourceIds) addResourceAndChildren(resourceId);
		return flatList;
	}),
	processedResourceChildrenLookup: createSelectorMemoized((state) => state.processedResourceLookup, (state) => state.resourceChildrenIdLookup, (processedResourceLookup, resourceChildrenIdLookup) => {
		const result = /* @__PURE__ */ new Map();
		for (const [resourceId, childrenIds] of resourceChildrenIdLookup) {
			const children = childrenIds.map((id) => processedResourceLookup.get(id));
			result.set(resourceId, children);
		}
		return result;
	}),
	childrenIdLookup: (state) => state.resourceChildrenIdLookup,
	resourceChildrenIds: createSelector((state, resourceId) => state.resourceChildrenIdLookup.get(resourceId) ?? EMPTY_ARRAY$1),
	resourceHasVisibleChildren: createSelector(resourceHasVisibleChildrenLookupSelector, (lookup, resourceId) => lookup.get(resourceId) ?? false),
	hasNestedResources: createSelector(resourceParentIdLookupSelector, (parentLookup) => parentLookup.size > 0),
	isResourceCollapsed: createSelector((state, resourceId) => state.collapsedResources[resourceId] === true),
	collapsedResources: (state) => state.collapsedResources,
	resourceParentIdLookup: resourceParentIdLookupSelector,
	resourceDepthLookup: resourceDepthLookupSelector,
	resourceDepth: createSelector(resourceDepthLookupSelector, (resourceDepthLookup, resourceId) => resourceDepthLookup.get(resourceId) ?? 0),
	idList: createSelector((state) => state.resourceIdList),
	visibleMap: createSelectorMemoized((state) => state.visibleResources, resourceParentIdLookupSelector, (state) => state.processedResourceLookup, (visibleResources, parentLookup, processedResourceLookup) => {
		if (parentLookup.size === 0) return visibleResources;
		const cache = /* @__PURE__ */ new Map();
		const checkVisibility = (resourceId) => {
			const cached = cache.get(resourceId);
			if (cached !== void 0) return cached;
			const isDirectlyVisible = visibleResources[resourceId] !== false;
			let result;
			if (!isDirectlyVisible) result = false;
			else {
				const parentId = parentLookup.get(resourceId);
				result = parentId ? checkVisibility(parentId) : true;
			}
			cache.set(resourceId, result);
			return result;
		};
		const curatedMap = {};
		for (const resourceId of processedResourceLookup.keys()) if (!checkVisibility(resourceId)) curatedMap[resourceId] = false;
		return curatedMap;
	}),
	/**
	* Gets the default event color used when no color is specified on the event.
	* Walks the resource hierarchy (child → parent → …) until a color is found,
	* falling back to the component-level default.
	*/
	defaultEventColor: createSelector((state, resourceId) => {
		if (resourceId == null) return state.eventColor;
		return resolveResourceProperty(state, resourceId, (r) => r.eventColor, state.eventColor);
	})
};
//#endregion
//#region node_modules/@mui/x-scheduler-internals/constants/constants.mjs
var EVENT_COLORS = [
	"red",
	"pink",
	"purple",
	"indigo",
	"blue",
	"teal",
	"green",
	"lime",
	"amber",
	"orange",
	"grey"
];
var EVENT_DRAG_PRECISION_MS = 900 * 1e3;
var DEFAULT_EVENT_CREATION_CONFIG = {
	interaction: "click",
	duration: 30
};
//#endregion
//#region node_modules/@mui/x-scheduler-internals/internals/utils/event-utils.mjs
function generateOccurrenceFromEvent({ event, eventId, occurrenceKey, start, end }) {
	return _extends({}, event, {
		id: eventId,
		key: occurrenceKey,
		displayTimezone: _extends({}, event?.displayTimezone, {
			start,
			end
		}),
		dataTimezone: _extends({}, event?.dataTimezone, {
			start,
			end
		})
	});
}
/**
*  Returns the key of the days an event occurrence should be visible on.
*/
function getDaysTheOccurrenceIsVisibleOn(event, days, adapter) {
	const eventStartStartOfDay = adapter.startOfDay(event.displayTimezone.start.value);
	const eventEndEndOfDay = adapter.endOfDay(event.displayTimezone.end.value);
	const dayKeys = [];
	for (const day of days) {
		if (adapter.isBefore(day.value, eventStartStartOfDay)) continue;
		if (adapter.isAfter(day.value, eventEndEndOfDay)) break;
		dayKeys.push(day.key);
	}
	return dayKeys;
}
/**
* Returns the occurrences to render in the given date range, expanding recurring events.
*/
function getOccurrencesFromEvents(parameters) {
	const { adapter, start, end, events, visibleResources, displayTimezone, recurringEventsPlugin } = parameters;
	const occurrences = [];
	for (const event of events) {
		const eventResourceIds = getEventResourceIds(event.resource);
		if (eventResourceIds.length > 0 && eventResourceIds.every((id) => visibleResources[id] === false)) continue;
		if (event.displayTimezone.rrule) {
			if (recurringEventsPlugin == null) {
				if (adapter.isAfter(event.displayTimezone.start.value, end) || adapter.isBefore(event.displayTimezone.end.value, start)) continue;
				occurrences.push(_extends({}, event, { key: String(event.id) }));
				continue;
			}
			occurrences.push(...recurringEventsPlugin.getOccurrencesForVisibleDays(event, start, end, adapter, displayTimezone));
			continue;
		}
		if (adapter.isAfter(event.displayTimezone.start.value, end) || adapter.isBefore(event.displayTimezone.end.value, start)) continue;
		occurrences.push(_extends({}, event, { key: String(event.id) }));
	}
	return occurrences;
}
/**
* Returns the resource IDs for the given resource, or an empty array if the resource is null or undefined.
*/
function getEventResourceIds(resource) {
	if (resource == null) return [];
	return Array.isArray(resource) ? resource : [resource];
}
/**
* Returns the primary resource ID for the given resource, or null if the resource is null or undefined.
*/
function getPrimaryResourceId(resource) {
	if (resource == null) return null;
	if (Array.isArray(resource)) return resource[0] ?? null;
	return resource;
}
//#endregion
//#region node_modules/@mui/x-scheduler-internals/scheduler-selectors/schedulerEventSelectors.mjs
var processedEventSelector = createSelector((state) => state.processedEventLookup, (processedEventLookup, eventId) => eventId == null ? null : processedEventLookup.get(eventId));
var isEventReadOnlySelector = createSelector((state, eventId) => {
	const processedEvent = processedEventSelector(state, eventId);
	if (!processedEvent) return false;
	return resolveEventProperty({
		state,
		resourceId: getPrimaryResourceId(processedEvent.resource),
		valueInEvent: processedEvent.modelInBuiltInFormat?.readOnly,
		getValueInResource: (r) => r.areEventsReadOnly,
		valueInState: state.readOnly ?? false
	});
});
var schedulerEventSelectors = {
	creationConfig: createSelectorMemoized((state) => state.readOnly, (state) => state.eventCreation, (isSchedulerReadOnly, creationConfig) => {
		if (isSchedulerReadOnly) return false;
		if (creationConfig === false) return false;
		if (creationConfig === true) return DEFAULT_EVENT_CREATION_CONFIG;
		return _extends({}, DEFAULT_EVENT_CREATION_CONFIG, creationConfig);
	}),
	/**
	* Gets the default duration (in minutes) for newly created events.
	* This can be used when you need the value event on read-only calendar.
	*/
	defaultEventDuration: createSelector((state) => state.eventCreation, (eventCreation) => {
		if (typeof eventCreation === "boolean") return DEFAULT_EVENT_CREATION_CONFIG.duration;
		return eventCreation?.duration ?? DEFAULT_EVENT_CREATION_CONFIG.duration;
	}),
	processedEvent: processedEventSelector,
	processedEventRequired: createSelector(processedEventSelector, (event, eventId) => {
		if (!event) throw new Error(`MUI X Scheduler: Event with id="${eventId}" was not found. The requested event does not exist in the scheduler state. Verify the event id is correct and the event has been added.`);
		return event;
	}),
	isReadOnly: isEventReadOnlySelector,
	color: createSelector((state, eventId) => {
		const event = processedEventSelector(state, eventId);
		if (!event) return state.eventColor;
		return resolveEventProperty({
			state,
			resourceId: getPrimaryResourceId(event.resource),
			valueInEvent: event.color,
			getValueInResource: (r) => r.eventColor,
			valueInState: state.eventColor
		});
	}),
	isPropertyReadOnly: createSelectorMemoized(isEventReadOnlySelector, (state) => state.eventModelStructure, (isEventReadOnly, eventModelStructure, _eventId) => {
		if (isEventReadOnly) return () => true;
		return (property) => {
			if (eventModelStructure?.[property] && !eventModelStructure?.[property].setter) return true;
			return false;
		};
	}),
	processedEventList: createSelectorMemoized((state) => state.eventIdList, (state) => state.processedEventLookup, (eventIds, processedEventLookup) => eventIds.map((id) => processedEventLookup.get(id))),
	idList: createSelector((state) => state.eventIdList),
	modelList: createSelector((state) => state.eventModelList),
	modelLookup: createSelector((state) => state.eventModelLookup),
	canDragEventsFromTheOutside: createSelector((state) => state.canDragEventsFromTheOutside && !state.readOnly),
	canDropEventsToTheOutside: createSelector((state) => state.canDropEventsToTheOutside && !state.readOnly),
	isDraggable: createSelector((state, eventId) => {
		if (isEventReadOnlySelector(state, eventId)) return false;
		const eventModelStructure = state.eventModelStructure;
		if (eventModelStructure?.start && !eventModelStructure?.start.setter) return false;
		if (eventModelStructure?.end && !eventModelStructure?.end.setter) return false;
		const processedEvent = processedEventSelector(state, eventId);
		if (!processedEvent) return false;
		return resolveEventProperty({
			state,
			resourceId: getPrimaryResourceId(processedEvent.resource),
			valueInEvent: processedEvent.draggable,
			getValueInResource: (r) => r.areEventsDraggable,
			valueInState: state.areEventsDraggable
		});
	}),
	isResizable: createSelector((state, eventId, side) => {
		if (isEventReadOnlySelector(state, eventId)) return false;
		const eventModelStructure = state.eventModelStructure;
		if (side === "start" && eventModelStructure?.start && !eventModelStructure?.start.setter) return false;
		if (side === "end" && eventModelStructure?.end && !eventModelStructure?.end.setter) return false;
		const processedEvent = processedEventSelector(state, eventId);
		if (!processedEvent) return false;
		return resolveEventProperty({
			state,
			resourceId: getPrimaryResourceId(processedEvent.resource),
			valueInEvent: getIsResizableFromProperty(processedEvent.resizable, side) ?? void 0,
			getValueInResource: (r) => getIsResizableFromProperty(r.areEventsResizable, side) ?? void 0,
			valueInState: getIsResizableFromProperty(state.areEventsResizable, side) ?? false
		});
	}),
	isRecurring: createSelector(processedEventSelector, (state) => state.recurringEventsPlugin, (event, recurringEventsPlugin, _eventId) => recurringEventsPlugin != null && Boolean(event?.dataTimezone.rrule))
};
function getIsResizableFromProperty(propertyValue, side) {
	if (propertyValue === void 0) return null;
	if (propertyValue === true) return true;
	if (propertyValue === false) return false;
	if (propertyValue === side) return true;
	return false;
}
/**
* Resolves an event property by checking (in order of priority):
* 1. The event itself (`valueInEvent`)
* 2. The resource hierarchy, child → parent → … (`getValueInResource`)
* 3. The component-level state (`valueInState`)
*/
function resolveEventProperty(parameters) {
	const { state, resourceId, valueInEvent, getValueInResource, valueInState } = parameters;
	if (valueInEvent !== void 0) return valueInEvent;
	return resolveResourceProperty(state, resourceId, getValueInResource, valueInState);
}
//#endregion
//#region node_modules/@mui/x-scheduler-internals/scheduler-selectors/schedulerPreferenceSelectors.mjs
var allSchedulerPreferencesSelector = createSelectorMemoized((state) => state.preferences, (preferences) => _extends({}, DEFAULT_SCHEDULER_PREFERENCES, preferences));
var schedulerPreferenceSelectors = {
	all: allSchedulerPreferencesSelector,
	ampm: createSelector(allSchedulerPreferencesSelector, (preferences) => preferences.ampm),
	weekStartsOn: createSelector(allSchedulerPreferencesSelector, (preferences) => preferences.weekStartsOn)
};
//#endregion
//#region node_modules/@mui/x-scheduler-internals/scheduler-selectors/schedulerOtherSelectors.mjs
var schedulerOtherSelectors = {
	/**
	* Returns `true` if the occurrence with the given key is the currently active occurrence.
	*/
	isEditedOccurrence: createSelector((state) => state.editedOccurrenceKey, (editedOccurrenceKey, occurrenceKey) => editedOccurrenceKey === occurrenceKey),
	visibleDate: createSelectorMemoized((state) => state.adapter, (state) => state.visibleDate, (state) => state.displayTimezone, (adapter, visibleDate, timezone) => adapter.setTimezone(visibleDate, timezone)),
	isRecurringScopeDialogOpen: createSelector((state) => state.pendingRecurringEventOperation != null),
	/**
	* The default event color used when no color is specified on the event or its resource.
	*/
	defaultEventColor: createSelector((state) => state.eventColor),
	displayTimezone: createSelector((state) => state.displayTimezone),
	/**
	* Whether each event must be assigned to a resource. When true, the resource cannot be cleared in the edit dialog and the form cannot be submitted without one.
	*/
	shouldEventRequireResource: createSelector((state) => state.shouldEventRequireResource),
	recurringEventsPlugin: createSelector((state) => state.recurringEventsPlugin),
	areRecurringEventsAvailable: createSelector((state) => state.recurringEventsPlugin != null),
	isLoading: createSelector((state) => state.isLoading),
	errors: createSelector((state) => state.errors)
};
//#endregion
//#region node_modules/@mui/x-scheduler-internals/scheduler-selectors/schedulerOccurrenceSelectors.mjs
var occurrencesGroupedByResourceListSelector = createSelectorMemoized((state) => state.adapter, schedulerEventSelectors.processedEventList, schedulerResourceSelectors.visibleMap, (state) => state.collapsedResources, schedulerResourceSelectors.processedResourceList, schedulerResourceSelectors.processedResourceChildrenLookup, schedulerOtherSelectors.displayTimezone, (state) => state.recurringEventsPlugin, (adapter, events, visibleResources, collapsedResources, resources, resourcesChildrenMap, displayTimezone, recurringEventsPlugin, start, end) => {
	const occurrencesGroupedByResource = /* @__PURE__ */ new Map();
	const occurrences = getOccurrencesFromEvents({
		adapter,
		start,
		end,
		events,
		visibleResources,
		displayTimezone,
		recurringEventsPlugin
	});
	for (const occurrence of occurrences) getEventResourceIds(occurrence.resource).forEach((id) => {
		if (!occurrencesGroupedByResource.has(id)) occurrencesGroupedByResource.set(id, []);
		occurrencesGroupedByResource.get(id).push(occurrence);
	});
	const processResources = (innerResources) => {
		const sortedResources = innerResources.toSorted((a, b) => a.title.localeCompare(b.title));
		const result = [];
		for (const resource of sortedResources) {
			if (visibleResources[resource.id] === false) continue;
			result.push({
				resource,
				occurrences: occurrencesGroupedByResource.get(resource.id) ?? []
			});
			if (collapsedResources[resource.id] !== true) {
				const children = resourcesChildrenMap.get(resource.id) ?? [];
				if (children.length > 0) result.push(...processResources(children));
			}
		}
		return result;
	};
	return processResources(resources);
});
var occurrencesGroupedByResourceMapSelector = createSelectorMemoized(occurrencesGroupedByResourceListSelector, (groupedByResourceList, _start, _end) => {
	const map = /* @__PURE__ */ new Map();
	for (const { resource, occurrences } of groupedByResourceList) map.set(resource.id, occurrences);
	return map;
});
var schedulerOccurrenceSelectors = {
	isStarted: createSelector((state) => state.adapter, (state) => state.nowUpdatedEveryMinute, (adapter, now, start) => {
		return adapter.isBefore(start.value, now) || adapter.isEqual(start.value, now);
	}),
	isEnded: createSelector((state) => state.adapter, (state) => state.nowUpdatedEveryMinute, (adapter, now, end) => {
		return adapter.isBefore(end.value, now);
	}),
	groupedByResourceList: occurrencesGroupedByResourceListSelector,
	resourceOccurrences: createSelector(occurrencesGroupedByResourceMapSelector, (map, _start, end, resourceId) => map.get(resourceId) ?? EMPTY_ARRAY$1)
};
//#endregion
//#region node_modules/@mui/x-scheduler-internals/internals/utils/drag-utils.mjs
var INTERNAL_DRAG_OR_RESIZE_PLACEHOLDER_TYPES = new Set(["internal-drag", "internal-resize"]);
function isInternalDragOrResizePlaceholder(placeholder) {
	return placeholder !== null && INTERNAL_DRAG_OR_RESIZE_PLACEHOLDER_TYPES.has(placeholder.type);
}
//#endregion
//#region node_modules/@mui/x-scheduler-internals/scheduler-selectors/schedulerOccurrencePlaceholderSelectors.mjs
var schedulerOccurrencePlaceholderSelectors = {
	value: createSelector((state) => state.occurrencePlaceholder),
	isDefined: createSelector((state) => state.occurrencePlaceholder !== null),
	actionForOccurrence: createSelector((state, occurrenceKey) => {
		const placeholder = state.occurrencePlaceholder;
		if (isInternalDragOrResizePlaceholder(placeholder) && placeholder.occurrenceKey === occurrenceKey) return placeholder.type;
		return null;
	})
};
//#endregion
//#region node_modules/@mui/x-scheduler-internals/scheduler-selectors/schedulerNowSelectors.mjs
var schedulerNowSelectors = {
	showCurrentTimeIndicator: createSelector((state) => state.showCurrentTimeIndicator),
	nowUpdatedEveryMinute: createSelector((state) => state.nowUpdatedEveryMinute),
	isCurrentDay: createSelector((state) => state.adapter, (state) => state.nowUpdatedEveryMinute, (adapter, now, date) => adapter.isSameDay(date, now))
};
//#endregion
//#region node_modules/@mui/x-scheduler-internals/scheduler-selectors/schedulerRecurringEventSelectors.mjs
var selectRecurringEventsPlugin = (state) => state.recurringEventsPlugin;
/** Memoized selectors that delegate to the recurring-events plugin. */
var schedulerRecurringEventSelectors = {
	presets: createSelectorMemoized((state) => state.adapter, selectRecurringEventsPlugin, (adapter, recurringEventsPlugin, date) => recurringEventsPlugin?.computePresets(adapter, date) ?? null),
	defaultPresetKey: createSelectorMemoized((state) => state.adapter, selectRecurringEventsPlugin, (adapter, recurringEventsPlugin, rule, occurrenceStart) => recurringEventsPlugin?.getDefaultPresetKey(adapter, rule, occurrenceStart) ?? null),
	isSameRRule: createSelector((state) => state.adapter, selectRecurringEventsPlugin, (adapter, recurringEventsPlugin, rruleA, rruleB) => {
		if (!rruleA && !rruleB) return true;
		if (!rruleA || !rruleB) return false;
		return recurringEventsPlugin?.isSameRRule(adapter, rruleA, rruleB) ?? false;
	})
};
//#endregion
//#region node_modules/@base-ui/utils/generateId.mjs
var counter = 0;
function generateId(prefix) {
	counter += 1;
	return `${prefix}-${Math.random().toString(36).slice(2, 6)}-${counter}`;
}
//#endregion
//#region node_modules/@mui/x-scheduler-internals/internals/utils/date-utils.mjs
/**
* Builds an adapter-agnostic format string that produces an ISO 8601 date-time
* **without** the trailing `Z` (wall-time representation).
* Produces e.g. `yyyy'-'MM'-'dd'T'HH':'mm':'ss` for date-fns.
*/
function getWallTimeIsoFormat(adapter) {
	const f = adapter.formats;
	const esc = adapter.escapedCharacters;
	return [
		f.yearPadded,
		esc.start,
		"-",
		esc.end,
		f.monthPadded,
		esc.start,
		"-",
		esc.end,
		f.dayOfMonthPadded,
		esc.start,
		"T",
		esc.end,
		f.hours24hPadded,
		esc.start,
		":",
		esc.end,
		f.minutesPadded,
		esc.start,
		":",
		esc.end,
		f.secondsPadded
	].join("");
}
/**
* Converts a `TemporalSupportedObject` back to a string, respecting the
* original date format: instant strings (ending with `Z`) stay as UTC ISO
* strings, while wall-time strings (no `Z`) are formatted in the event's
* data timezone without the `Z` suffix.
*/
function dateToEventString(adapter, date, originalString, dataTimezone) {
	if (originalString.endsWith("Z")) return adapter.toJsDate(date).toISOString();
	const dateInDataTz = adapter.setTimezone(date, dataTimezone);
	return adapter.formatByString(dateInDataTz, getWallTimeIsoFormat(adapter));
}
function mergeDateAndTime(adapter, dateParam, timeParam) {
	let mergedDate = dateParam;
	mergedDate = adapter.setHours(mergedDate, adapter.getHours(timeParam));
	mergedDate = adapter.setMinutes(mergedDate, adapter.getMinutes(timeParam));
	mergedDate = adapter.setSeconds(mergedDate, adapter.getSeconds(timeParam));
	mergedDate = adapter.setMilliseconds(mergedDate, adapter.getMilliseconds(timeParam));
	return mergedDate;
}
/**
* Snaps an all-day event's bounds to span the whole day; non-all-day bounds are returned unchanged.
*/
function normalizeAllDayBounds(adapter, start, end, allDay) {
	if (!allDay) return {
		start,
		end
	};
	return {
		start: adapter.startOfDay(start),
		end: adapter.endOfDay(end)
	};
}
/**
* Returns a string representation of the date.
* It can be used as key in Maps or passed to the React `key` property when looping through days.
* It only contains date information, two dates representing the same day but with different time will have the same key.
*/
function getDateKey(day, adapter) {
	return adapter.format(day, "localizedNumericDate");
}
/**
* Maps a known-Sunday reference date through adapter.getDayOfWeek to anchor
* the 1..7 adapter day-number to JS weekdays (0=Sun … 6=Sat). Cached per
* adapter instance.
*/
var sundayDayNumberCache = /* @__PURE__ */ new WeakMap();
function getSundayDayNumber(adapter) {
	let cached = sundayDayNumberCache.get(adapter);
	if (cached === void 0) {
		const knownSunday = adapter.date("2025-01-05", "UTC");
		cached = adapter.getDayOfWeek(knownSunday);
		if (!Number.isInteger(cached) || cached < 1 || cached > 7) throw new Error(`MUI X Scheduler: adapter.getDayOfWeek returned an unexpected value (${cached}). Expected an integer in the range 1–7. Check that your adapter implements getDayOfWeek correctly.`);
		sundayDayNumberCache.set(adapter, cached);
	}
	return cached;
}
/**
* Returns the start of the week for `date`, using `weekStartsOn` as the first
* day of the week (0 = Sunday … 6 = Saturday).
*
* When `weekStartsOn` is `undefined` the locale default (adapter.startOfWeek)
* is used unchanged.
*/
function getStartOfWeek(adapter, date, weekStartsOn) {
	if (weekStartsOn === void 0) return adapter.startOfWeek(date);
	if (!Number.isInteger(weekStartsOn) || weekStartsOn < 0 || weekStartsOn > 6) throw new Error(`MUI X Scheduler: weekStartsOn must be an integer between 0 (Sunday) and 6 (Saturday), but received ${weekStartsOn}.`);
	const targetDayNumber = (getSundayDayNumber(adapter) - 1 + weekStartsOn) % 7 + 1;
	const delta = (adapter.getDayOfWeek(date) - targetDayNumber + 7) % 7;
	return adapter.startOfDay(adapter.addDays(date, -delta));
}
/**
* Returns the end of the week for `date` (inclusive last moment of the 7th day),
* using `weekStartsOn` as the first day of the week.
*
* When `weekStartsOn` is `undefined` the locale default (adapter.endOfWeek) is used.
*/
function getEndOfWeek(adapter, date, weekStartsOn) {
	if (weekStartsOn === void 0) return adapter.endOfWeek(date);
	const weekStart = getStartOfWeek(adapter, date, weekStartsOn);
	return adapter.endOfDay(adapter.addDays(weekStart, 6));
}
/**
* Returns the week number for `date`, using `weekStartsOn` as the first day of
* the week (0 = Sunday … 6 = Saturday).
*
* Applies the ISO 8601 "4-day minimum" rule (the Thursday of each week
* determines its year) with a parametric first day of the week. When
* `weekStartsOn` is 1 the result is a standard ISO 8601 week number; for
* other values it is the same algorithm with a shifted origin.
*/
function getWeekNumber(adapter, date, weekStartsOn) {
	if (weekStartsOn === void 0) return adapter.getWeekNumber(date);
	const weekStart = getStartOfWeek(adapter, date, weekStartsOn);
	const midWeek = adapter.addDays(weekStart, 3);
	const week1Start = getStartOfWeek(adapter, adapter.addDays(adapter.startOfYear(midWeek), 3), weekStartsOn);
	return adapter.differenceInDays(weekStart, week1Start) / 7 + 1;
}
//#endregion
//#region node_modules/@mui/x-scheduler-internals/process-date/processDate.mjs
/**
* Creates a processed date object from a date object.
*/
function processDate(date, adapter) {
	const hours = adapter.getHours(date);
	const minutes = adapter.getMinutes(date);
	return {
		value: date,
		key: getDateKey(date, adapter),
		timestamp: adapter.getTime(date),
		minutesInDay: hours * 60 + minutes
	};
}
//#endregion
//#region node_modules/@mui/x-scheduler-internals/process-event/resolveEventDate.mjs
/**
* Resolves an event date string to a `TemporalSupportedObject`.
*
* - Strings ending with `"Z"` are treated as instants (UTC).
* - Strings without `"Z"` are treated as wall-time and interpreted in `dataTimezone`.
*/
function resolveEventDate(value, dataTimezone, adapter, eventId) {
	const date = value.endsWith("Z") ? adapter.date(value, "default") : adapter.date(value, dataTimezone);
	if (!adapter.isValid(date)) {
		const eventRef = eventId === void 0 ? "" : ` of event "${eventId}"`;
		throw new Error(`MUI X Scheduler: The date "${value}"${eventRef} is not a valid date.\nInvalid dates produce a NaN timestamp that silently breaks sorting, positioning, and overlap calculations.\nProvide a valid ISO 8601 date string (for example "2025-01-01T09:00:00" or "2025-01-01T09:00:00Z").`);
	}
	return date;
}
//#endregion
//#region node_modules/@mui/x-scheduler-internals/process-event/processEvent.mjs
function processEvent(model, displayTimezone, adapter, recurringEventsPlugin = null) {
	const dataTimezone = model.timezone ?? "default";
	const startInstant = resolveEventDate(model.start, dataTimezone, adapter, model.id);
	const endInstant = resolveEventDate(model.end, dataTimezone, adapter, model.id);
	const resolvedExDates = model.exDates ? model.exDates.map((exDate) => resolveEventDate(exDate, dataTimezone, adapter, model.id)) : void 0;
	const displayBounds = normalizeAllDayBounds(adapter, adapter.setTimezone(startInstant, displayTimezone), adapter.setTimezone(endInstant, displayTimezone), model.allDay);
	const exDatesInDisplayTz = resolvedExDates ? resolvedExDates.map((exDate) => adapter.setTimezone(exDate, displayTimezone)) : void 0;
	if (recurringEventsPlugin == null && model.rrule != null) warnOnce(["MUI X Scheduler: Recurring events are a premium feature. The `rrule` property will be ignored.", "Use <EventCalendarPremium /> or <EventTimelinePremium /> to enable recurring events."]);
	const parsedDataRRule = recurringEventsPlugin && model.rrule ? recurringEventsPlugin.parseRRule(adapter, model.rrule, dataTimezone) : void 0;
	const displayTimezoneRRule = recurringEventsPlugin && parsedDataRRule ? recurringEventsPlugin.projectRRuleToTimezone(adapter, parsedDataRRule, displayTimezone, startInstant) : void 0;
	return {
		id: model.id,
		title: model.title,
		description: model.description,
		dataTimezone: {
			start: processDate(startInstant, adapter),
			end: processDate(endInstant, adapter),
			timezone: dataTimezone,
			rrule: parsedDataRRule,
			exDates: resolvedExDates
		},
		displayTimezone: {
			start: processDate(displayBounds.start, adapter),
			end: processDate(displayBounds.end, adapter),
			timezone: displayTimezone,
			rrule: displayTimezoneRRule,
			exDates: exDatesInDisplayTz
		},
		resource: model.resource,
		allDay: model.allDay ?? false,
		readOnly: model.readOnly ?? false,
		extractedFromId: model.extractedFromId,
		modelInBuiltInFormat: model,
		color: model.color,
		draggable: model.draggable,
		resizable: model.resizable,
		className: model.className
	};
}
//#endregion
//#region node_modules/@mui/x-scheduler-internals/internals/utils/SchedulerStore/SchedulerStore.utils.mjs
var _excluded$23 = ["children"];
/**
* Determines if the occurrence placeholder has changed in a meaningful way that requires updating the store.
*/
function shouldUpdateOccurrencePlaceholder(adapter, previous, next) {
	if (next == null || previous == null) return next !== previous;
	const untypedPrevious = previous;
	const untypedNext = next;
	for (const key in untypedNext) if (key === "start" || key === "end") {
		if (!adapter.isEqual(untypedNext[key], untypedPrevious[key])) return true;
	} else if (!Object.is(untypedNext[key], untypedPrevious[key])) return true;
	for (const key in untypedPrevious) if (!(key in untypedNext)) return true;
	return false;
}
var EVENT_PROPERTIES_LOOKUP = {
	id: true,
	title: true,
	description: true,
	start: true,
	end: true,
	resource: true,
	rrule: true,
	allDay: true,
	readOnly: true,
	extractedFromId: true,
	exDates: true,
	color: true,
	draggable: true,
	resizable: true,
	className: true,
	timezone: true
};
var EVENT_PROPERTIES = Object.keys(EVENT_PROPERTIES_LOOKUP);
/**
* Returns the properties of an event model that are not part of the built-in `SchedulerEvent` shape.
*/
function getCustomEventProperties(model) {
	const customProperties = {};
	for (const key in model) if (model.hasOwnProperty(key) && !EVENT_PROPERTIES_LOOKUP.hasOwnProperty(key)) customProperties[key] = model[key];
	return customProperties;
}
var RESOURCE_PROPERTIES = Object.keys({
	id: true,
	title: true,
	eventColor: true,
	children: true,
	areEventsDraggable: true,
	areEventsResizable: true,
	areEventsReadOnly: true
});
/**
* Converts an event model to a processed event using the provided model structure.
*/
function getProcessedEventFromModel(model, adapter, eventModelStructure, displayTimezone, recurringEventsPlugin = null) {
	const modelInDefaultFormat = {};
	for (const key of EVENT_PROPERTIES) {
		const getter = eventModelStructure?.[key]?.getter;
		modelInDefaultFormat[key] = getter ? getter(model) : model[key];
	}
	return processEvent(modelInDefaultFormat, displayTimezone, adapter, recurringEventsPlugin);
}
/**
* Updates an event model based on the provided changes and model structure.
* Converts internal date objects (`TemporalSupportedObject`) to strings
* before applying them to the user's model, because `SchedulerEvent` date
* fields are strings. Respects the original string format (instant vs wall-time).
*/
function getUpdatedEventModelFromChanges(oldModel, changes, eventModelStructure, adapter, originalBuiltInModel) {
	const dataTimezone = originalBuiltInModel.timezone ?? "default";
	const stringified = _extends({}, changes);
	if (changes.start != null) stringified.start = dateToEventString(adapter, changes.start, originalBuiltInModel.start, dataTimezone);
	if (changes.end != null) stringified.end = dateToEventString(adapter, changes.end, originalBuiltInModel.end, dataTimezone);
	if (changes.exDates != null) stringified.exDates = changes.exDates.map((d, i) => {
		const originalExDate = originalBuiltInModel.exDates?.[i];
		if (originalExDate) return dateToEventString(adapter, d, originalExDate, dataTimezone);
		return dateToEventString(adapter, d, originalBuiltInModel.start, dataTimezone);
	});
	if (changes.rrule != null && typeof changes.rrule === "object" && changes.rrule.until != null) {
		const originalRRule = originalBuiltInModel.rrule;
		const referenceString = (typeof originalRRule === "object" ? originalRRule.until : void 0) ?? originalBuiltInModel.start;
		stringified.rrule = _extends({}, changes.rrule, { until: dateToEventString(adapter, changes.rrule.until, referenceString, dataTimezone) });
	}
	return createOrUpdateEventModelFromBuiltInEventModel(oldModel, stringified, eventModelStructure);
}
/**
* Create an event model from a processed event using the provided model structure.
*/
function createEventModel(event, eventModelStructure, adapter) {
	const id = generateId("event");
	const formatNewDate = (value) => {
		if (typeof value === "string") return value;
		return adapter.toJsDate(value).toISOString();
	};
	const rrule = typeof event.rrule === "object" && event.rrule.until != null && typeof event.rrule.until !== "string" ? _extends({}, event.rrule, { until: formatNewDate(event.rrule.until) }) : event.rrule;
	return {
		id,
		model: createOrUpdateEventModelFromBuiltInEventModel(null, _extends({}, event, {
			id,
			start: formatNewDate(event.start),
			end: formatNewDate(event.end),
			exDates: event.exDates?.map(formatNewDate),
			rrule
		}), eventModelStructure)
	};
}
function createOrUpdateEventModelFromBuiltInEventModel(oldModel, changes, eventModelStructure) {
	let eventModel = oldModel == null ? {} : _extends({}, oldModel);
	const propertiesWithSetter = [];
	for (const key in changes) if (changes.hasOwnProperty(key)) {
		const setter = eventModelStructure?.[key]?.setter;
		if (setter) propertiesWithSetter.push([setter, changes[key]]);
		else if (changes[key] === void 0) delete eventModel[key];
		else if (oldModel != null && key === "allDay" && changes[key] === false) delete eventModel[key];
		else eventModel[key] = changes[key];
	}
	for (const [setter, value] of propertiesWithSetter) eventModel = setter(eventModel, value);
	return eventModel;
}
/**
* Converts a resource model to a processed resource using the provided model structure.
*/
function getProcessedResourceFromModel(resource, resourceModelStructure) {
	const processedResource = {};
	for (const key of RESOURCE_PROPERTIES) {
		const getter = resourceModelStructure?.[key]?.getter;
		const resourceProperty = getter ? getter(resource) : resource[key];
		if (key === "children" && Array.isArray(resourceProperty)) {
			processedResource[key] = resourceProperty.map((child) => getProcessedResourceFromModel(child, resourceModelStructure));
			continue;
		}
		processedResource[key] = resourceProperty;
	}
	return processedResource;
}
/**
* Throws if the resolved event id is missing.
*/
function checkSchedulerEventIdIsValid(id, event) {
	if (id == null) throw new Error(`MUI X Scheduler: All events must have a unique \`id\`.
Without an \`id\`, an event cannot be tracked and silently overwrites another event in the calendar state.
Add an \`id\` to every event, or set \`eventModelStructure.id.getter\` to derive one from your event model.
An event was provided without an \`id\`:
${JSON.stringify(event)}`);
}
function buildEventsState(parameters, adapter, displayTimezone, recurringEventsPlugin = null) {
	const { events = EMPTY_ARRAY$1, eventModelStructure } = parameters;
	const eventIdList = [];
	const eventModelLookup = /* @__PURE__ */ new Map();
	const processedEventLookup = /* @__PURE__ */ new Map();
	for (const event of events) {
		const processedEvent = getProcessedEventFromModel(event, adapter, eventModelStructure, displayTimezone, recurringEventsPlugin);
		const { id } = processedEvent;
		checkSchedulerEventIdIsValid(id, event);
		if (eventModelLookup.has(id)) warnOnce([`MUI X Scheduler: Two or more events share the same id "${String(id)}".`, "Event ids must be unique. Only the last event with a given id is kept, the others are ignored."]);
		else eventIdList.push(id);
		eventModelLookup.set(id, event);
		processedEventLookup.set(id, processedEvent);
	}
	return {
		eventIdList,
		eventModelLookup,
		eventModelStructure,
		processedEventLookup,
		eventModelList: events
	};
}
function buildResourcesState(parameters) {
	const { resources = EMPTY_ARRAY$1, resourceModelStructure } = parameters;
	const resourceIdList = [];
	const processedResourceLookup = /* @__PURE__ */ new Map();
	const resourceChildrenIdLookup = /* @__PURE__ */ new Map();
	const addResourceToState = (processedResource) => {
		const { children } = processedResource, resourceWithoutChildren = _objectWithoutPropertiesLoose(processedResource, _excluded$23);
		processedResourceLookup.set(processedResource.id, resourceWithoutChildren);
		if (children) for (const child of children) {
			if (!resourceChildrenIdLookup.get(processedResource.id)) resourceChildrenIdLookup.set(processedResource.id, []);
			resourceChildrenIdLookup.get(processedResource.id)?.push(child.id);
			addResourceToState(child);
		}
	};
	for (const resource of resources) {
		const processedResource = getProcessedResourceFromModel(resource, resourceModelStructure);
		resourceIdList.push(processedResource.id);
		addResourceToState(processedResource);
	}
	return {
		resourceIdList,
		processedResourceLookup,
		resourceModelStructure,
		resourceChildrenIdLookup
	};
}
//#endregion
//#region node_modules/@mui/x-scheduler-internals/internals/utils/extractStandaloneEvent.mjs
/**
* Builds the creation payload for a standalone event derived from `source`, linked via `extractedFromId`.
*/
function extractStandaloneEvent(source, changes) {
	const createdEvent = _extends({}, source.modelInBuiltInFormat, changes, { extractedFromId: source.id });
	delete createdEvent.id;
	delete createdEvent.rrule;
	delete createdEvent.exDates;
	return createdEvent;
}
//#endregion
//#region node_modules/@mui/x-scheduler-internals/internals/utils/TimeoutManager.mjs
var TimeoutManager = class {
	timeoutIds = /* @__PURE__ */ new Map();
	intervalIds = /* @__PURE__ */ new Map();
	startTimeout = (key, delay, fn) => {
		this.clearTimeout(key);
		const id = setTimeout(() => {
			this.timeoutIds.delete(key);
			fn();
		}, delay);
		this.timeoutIds.set(key, id);
	};
	startInterval = (key, delay, fn) => {
		this.clearInterval(key);
		const id = setInterval(fn, delay);
		this.intervalIds.set(key, id);
	};
	clearTimeout = (key) => {
		const id = this.timeoutIds.get(key);
		if (id != null) {
			clearTimeout(id);
			this.timeoutIds.delete(key);
		}
	};
	clearInterval = (key) => {
		const id = this.intervalIds.get(key);
		if (id != null) {
			clearInterval(id);
			this.intervalIds.delete(key);
		}
	};
	clearAll = () => {
		this.timeoutIds.forEach(clearTimeout);
		this.timeoutIds.clear();
		this.intervalIds.forEach(clearInterval);
		this.intervalIds.clear();
	};
	[disposeSymbol]() {
		this.clearAll();
	}
};
//#endregion
//#region node_modules/@mui/x-scheduler-internals/base-ui-copy/utils/createBaseUIEventDetails.mjs
/**
* Maps a change `reason` string to the corresponding native event type.
*/
/**
* Details of custom change events emitted by Base UI components.
*/
/**
* Details of custom generic events emitted by Base UI components.
*/
/**
* Creates a Base UI event details object with the given reason and utilities
* for preventing Base UI's internal event handling.
*/
function createChangeEventDetails$1(reason, event, trigger, customProperties) {
	let canceled = false;
	let allowPropagation = false;
	const custom = customProperties ?? EMPTY_OBJECT;
	return _extends({
		reason,
		event: event ?? new Event("base-ui"),
		cancel() {
			canceled = true;
		},
		allowPropagation() {
			allowPropagation = true;
		},
		get isCanceled() {
			return canceled;
		},
		get isPropagationAllowed() {
			return allowPropagation;
		},
		trigger
	}, custom);
}
//#endregion
//#region node_modules/@mui/x-scheduler-internals/internals/utils/SchedulerStore/SchedulerStore.mjs
var _excluded$22 = ["id"];
var ONE_MINUTE_IN_MS = 60 * 1e3;
var DEFAULT_SCHEDULER_PREFERENCES = { ampm: true };
var MOCK_EVENT_STATE = {
	eventIdList: [],
	eventModelLookup: /* @__PURE__ */ new Map(),
	eventModelStructure: {},
	processedEventLookup: /* @__PURE__ */ new Map(),
	eventModelList: []
};
/**
* Instance shared by the Event Calendar and the Event Timeline Premium components.
*/
var SchedulerStore = class SchedulerStore extends Store {
	initialParameters = null;
	disposables = new DisposableStack();
	timeoutManager = this.disposables.use(new TimeoutManager());
	eventManager = this.disposables.adopt(new EventManager(), (m) => m.removeAllListeners());
	/**
	* Plugin that provides event-scheduling support (dependencies). `null` when not attached.
	*/
	schedulingPlugin = null;
	constructor(parameters, adapter, instanceName, mapper, recurringEventsPlugin = null) {
		const stateFromParameters = SchedulerStore.deriveStateFromParameters(parameters, adapter);
		const schedulerInitialState = _extends({}, SchedulerStore.deriveStateFromParameters(parameters, adapter), parameters.dataSource ? _extends({}, MOCK_EVENT_STATE, { eventModelStructure: parameters.eventModelStructure ?? {} }) : buildEventsState(parameters, adapter, stateFromParameters.displayTimezone, recurringEventsPlugin), buildResourcesState(parameters), {
			preferences: DEFAULT_SCHEDULER_PREFERENCES,
			adapter,
			occurrencePlaceholder: null,
			editedOccurrenceKey: null,
			copiedEvent: null,
			nowUpdatedEveryMinute: adapter.now(stateFromParameters.displayTimezone),
			pendingRecurringEventOperation: null,
			visibleResources: parameters.visibleResources ?? parameters.defaultVisibleResources ?? EMPTY_OBJECT,
			collapsedResources: parameters.collapsedResources ?? parameters.defaultCollapsedResources ?? EMPTY_OBJECT,
			visibleDate: parameters.visibleDate ?? parameters.defaultVisibleDate ?? adapter.startOfDay(adapter.now(stateFromParameters.displayTimezone)),
			errors: [],
			isLoading: !!parameters.dataSource,
			recurringEventsPlugin
		});
		const initialState = mapper.getInitialState(schedulerInitialState, parameters, adapter);
		super(initialState);
		this.parameters = parameters;
		this.instanceName = instanceName;
		this.mapper = mapper;
		const currentDate = /* @__PURE__ */ new Date();
		const timeUntilNextMinuteMs = ONE_MINUTE_IN_MS - (currentDate.getSeconds() * 1e3 + currentDate.getMilliseconds());
		this.timeoutManager.startTimeout("set-now", timeUntilNextMinuteMs, () => {
			this.set("nowUpdatedEveryMinute", this.state.adapter.now(this.state.displayTimezone));
			this.timeoutManager.startInterval("set-now", ONE_MINUTE_IN_MS, () => {
				this.set("nowUpdatedEveryMinute", this.state.adapter.now(this.state.displayTimezone));
			});
		});
		this.initialParameters = parameters;
	}
	/**
	* Returns the properties of the state that are derived from the parameters.
	* This do not contain state properties that don't update whenever the parameters update.
	*/
	static deriveStateFromParameters(parameters, adapter) {
		return {
			adapter,
			areEventsDraggable: parameters.areEventsDraggable ?? true,
			areEventsResizable: parameters.areEventsResizable ?? true,
			canDragEventsFromTheOutside: parameters.canDragEventsFromTheOutside ?? false,
			canDropEventsToTheOutside: parameters.canDropEventsToTheOutside ?? false,
			eventColor: parameters.eventColor ?? "teal",
			showCurrentTimeIndicator: parameters.showCurrentTimeIndicator ?? true,
			readOnly: parameters.readOnly ?? false,
			eventCreation: parameters.eventCreation ?? true,
			displayTimezone: parameters.displayTimezone ?? "default"
		};
	}
	/**
	* Updates the state of the calendar based on the new parameters provided to the root component.
	*/
	updateStateFromParameters = (parameters, adapter) => {
		const updateModel = (mutableNewState, controlledProp, defaultProp) => {
			if (parameters[controlledProp] !== void 0) mutableNewState[controlledProp] = parameters[controlledProp];
			{
				const defaultValue = parameters[defaultProp];
				const isControlled = parameters[controlledProp] !== void 0;
				const initialDefaultValue = this.initialParameters?.[defaultProp];
				const initialIsControlled = this.initialParameters?.[controlledProp] !== void 0;
				if (initialIsControlled !== isControlled) warnOnce([
					`MUI X Scheduler: A component is changing the ${initialIsControlled ? "" : "un"}controlled ${controlledProp} state of ${this.instanceName} to be ${initialIsControlled ? "un" : ""}controlled.`,
					"Elements should not switch from uncontrolled to controlled (or vice versa).",
					`Decide between using a controlled or uncontrolled ${controlledProp} element for the lifetime of the component.`,
					"The nature of the state is determined during the first render. It's considered controlled if the value is not `undefined`.",
					"More info: https://fb.me/react-controlled-components"
				]);
				else if (JSON.stringify(initialDefaultValue) !== JSON.stringify(defaultValue)) warnOnce([`MUI X Scheduler: A component is changing the default ${controlledProp} state of an uncontrolled ${this.instanceName} after being initialized. `, `To suppress this warning opt to use a controlled ${this.instanceName}.`]);
			}
		};
		const newSchedulerState = SchedulerStore.deriveStateFromParameters(parameters, adapter);
		if (!parameters.dataSource && (parameters.events !== this.parameters.events || parameters.eventModelStructure !== this.parameters.eventModelStructure || adapter !== this.state.adapter)) Object.assign(newSchedulerState, buildEventsState(parameters, adapter, newSchedulerState.displayTimezone, this.state.recurringEventsPlugin));
		if (newSchedulerState.displayTimezone !== this.state.displayTimezone) newSchedulerState.nowUpdatedEveryMinute = adapter.now(newSchedulerState.displayTimezone);
		if (parameters.resources !== this.parameters.resources || parameters.resourceModelStructure !== this.parameters.resourceModelStructure) Object.assign(newSchedulerState, buildResourcesState(parameters));
		updateModel(newSchedulerState, "visibleDate", "defaultVisibleDate");
		updateModel(newSchedulerState, "visibleResources", "defaultVisibleResources");
		updateModel(newSchedulerState, "collapsedResources", "defaultCollapsedResources");
		const newState = this.mapper.updateStateFromParameters(newSchedulerState, parameters, updateModel);
		this.update(newState);
		this.parameters = parameters;
	};
	/**
	* Disposes the store synchronously. The React consumer (`useDisposable`)
	* handles the StrictMode double-invocation by suppressing the simulated
	* unmount, so this method does not need to defer the teardown itself.
	*/
	[disposeSymbol]() {
		if (this.disposables.disposed) return;
		try {
			this.disposables.dispose();
		} catch (error) {
			console.error("MUI X Scheduler: error while disposing the store.", ...unwrapSuppressedErrors(error));
		}
	}
	/**
	* Removes the error with the given key from `state.errors`.
	* The key is the one carried by the matching `StoredError` entry.
	*/
	dismissError = (key) => {
		this.set("errors", this.state.errors.filter((entry) => entry.key !== key));
	};
	nextErrorKey = 0;
	/**
	* Appends an error to `state.errors`, wrapping non-Error rejections to preserve
	* the original payload via `cause`. The store owns the key counter so uniqueness
	* is enforced in one place. Does not dedupe — pushing the same `Error` instance
	* twice produces two entries (intentional; e.g. a retried failure that should
	* re-display after the previous one was dismissed).
	* @internal
	*/
	pushError = (error) => {
		const wrapped = error instanceof Error ? error : new Error(String(error), { cause: error });
		this.nextErrorKey += 1;
		this.set("errors", [...this.state.errors, {
			error: wrapped,
			key: String(this.nextErrorKey)
		}]);
	};
	/**
	* Registers an effect to be run when the value returned by the selector changes.
	*/
	registerStoreEffect = (selector, effect) => {
		let previousValue = selector(this.state);
		return this.subscribe((state) => {
			const nextValue = selector(state);
			if (nextValue !== previousValue) {
				effect(previousValue, nextValue);
				previousValue = nextValue;
			}
		});
	};
	/**
	* Publishes an event to all its subscribers.
	*/
	publishEvent = (name, params) => {
		this.eventManager.emit(name, params);
	};
	/**
	* Subscribe to an event emitted by the store. Returns an unsubscribe function.
	*/
	subscribeEvent = (eventName, handler) => {
		this.eventManager.on(eventName, handler);
		return () => this.eventManager.removeListener(eventName, handler);
	};
	setVisibleDate = ({ visibleDate, event }) => {
		const { visibleDate: visibleDateProp, onVisibleDateChange } = this.parameters;
		const { adapter } = this.state;
		if (!adapter.isEqual(this.state.visibleDate, visibleDate)) {
			const eventDetails = createChangeEventDetails$1("none", event?.nativeEvent);
			onVisibleDateChange?.(visibleDate, eventDetails);
			if (!eventDetails.isCanceled && visibleDateProp === void 0) this.set("visibleDate", visibleDate);
		}
	};
	/**
	* Adds, updates and / or deletes events in the calendar.
	*/
	updateEvents(parameters) {
		const eventDetails = createChangeEventDetails$1("none");
		const { deleted: deletedParam, updated: updatedParam = [], created = [] } = parameters;
		const updated = new Map(updatedParam.map((ev) => [ev.id, ev]));
		const deleted = new Set(deletedParam);
		for (const id of deleted) if (updated.has(id)) warnOnce([`MUI X Scheduler: id "${String(id)}" appears in both \`deleted\` and \`updated\`.`, "These two arrays must be disjoint, otherwise the order of operations is undefined."]);
		const originalEventIds = schedulerEventSelectors.idList(this.state);
		const originalEventModelLookup = schedulerEventSelectors.modelLookup(this.state);
		const newEvents = [];
		const updatedEvents = [];
		if (deleted.size > 0 || updated.size > 0) for (const eventId of originalEventIds) {
			if (deleted.has(eventId)) continue;
			if (updated.has(eventId)) {
				const processedEvent = this.state.processedEventLookup.get(eventId);
				const newEvent = getUpdatedEventModelFromChanges(originalEventModelLookup.get(eventId), updated.get(eventId), this.state.eventModelStructure, this.state.adapter, processedEvent.modelInBuiltInFormat);
				newEvents.push(newEvent);
				updatedEvents.push(newEvent);
			} else newEvents.push(originalEventModelLookup.get(eventId));
		}
		else newEvents.push(...schedulerEventSelectors.modelList(this.state));
		const createdIds = [];
		const createdEvents = [];
		for (const createdEvent of created) {
			const source = createdEvent.extractedFromId == null ? void 0 : originalEventModelLookup.get(createdEvent.extractedFromId);
			const response = createEventModel(source ? _extends({}, getCustomEventProperties(source), createdEvent) : createdEvent, this.state.eventModelStructure, this.state.adapter);
			newEvents.push(response.model);
			createdEvents.push(response.model);
			createdIds.push(response.id);
		}
		this.schedulingPlugin?.handleEventsUpdate(parameters);
		if (!this.parameters.onEventsChange && !this.parameters.dataSource) warnOnce([
			"MUI X Scheduler: An event update was ignored because no `onEventsChange` handler nor `dataSource` is provided.",
			"The `events` prop is fully controlled, so without one of them the changes are lost and the UI does not update.",
			"Pass an `onEventsChange` handler that updates the `events` prop, provide a `dataSource`, or set `readOnly` to disable editing."
		]);
		this.parameters.onEventsChange?.(newEvents, eventDetails);
		queueMicrotask(() => this.publishEvent("eventsUpdated", {
			deleted: deletedParam ?? [],
			updated: updatedEvents,
			created: createdEvents,
			newEvents
		}));
		return {
			deleted: deletedParam ?? [],
			updated: Array.from(updated.keys()),
			created: createdIds
		};
	}
	/**
	* Goes to today's date without changing the view.
	*/
	goToToday = (event) => {
		const { adapter } = this.state;
		this.setVisibleDate({
			visibleDate: adapter.startOfDay(adapter.now(this.state.displayTimezone)),
			event
		});
	};
	/**
	* Goes to a specific date without changing the view.
	*/
	goToDate = (visibleDate, event) => {
		this.setVisibleDate({
			visibleDate,
			event
		});
	};
	/**
	* Creates a new event in the calendar.
	*/
	createEvent = (calendarEvent) => {
		if (this.state.recurringEventsPlugin == null && calendarEvent.rrule) {
			warnOnce(["MUI X Scheduler: Recurring events are a premium feature. The `rrule` property will be ignored.", "Use <EventCalendarPremium /> or <EventTimelinePremium /> to enable recurring events."]);
			return this.updateEvents({ created: [_extends({}, calendarEvent, { rrule: void 0 })] }).created[0];
		}
		return this.updateEvents({ created: [calendarEvent] }).created[0];
	};
	/**
	* Updates an event in the calendar.
	*/
	updateEvent = (calendarEvent) => {
		const original = schedulerEventSelectors.processedEventRequired(this.state, calendarEvent.id);
		if (this.state.recurringEventsPlugin != null && original.dataTimezone.rrule) throw new Error("MUI X Scheduler: This event is recurring and cannot be updated with updateEvent(). Recurring events require special handling to manage series and exceptions. Use updateRecurringEvent() instead to update recurring events.");
		if (this.state.recurringEventsPlugin == null && calendarEvent.rrule != null) {
			warnOnce(["MUI X Scheduler: Recurring events are a premium feature. The `rrule` property will be ignored.", "Use <EventCalendarPremium /> or <EventTimelinePremium /> to enable recurring events."]);
			this.updateEvents({ updated: [_extends({}, calendarEvent, { rrule: void 0 })] });
			return;
		}
		this.updateEvents({ updated: [calendarEvent] });
	};
	/**
	* Updates a recurring event in the calendar.
	*/
	updateRecurringEvent = (params) => {
		if (this.state.recurringEventsPlugin == null) {
			warnOnce(["MUI X Scheduler: Recurring event updates are a premium feature.", "Use <EventCalendarPremium /> or <EventTimelinePremium /> to enable recurring events."]);
			return;
		}
		this.set("pendingRecurringEventOperation", _extends({ kind: "update" }, params));
	};
	/**
	* Opens the recurring scope dialog to delete a recurring event.
	*/
	deleteRecurringEvent = (params) => {
		if (this.state.recurringEventsPlugin == null) {
			warnOnce(["MUI X Scheduler: Recurring event deletions are a premium feature.", "Use <EventCalendarPremium /> or <EventTimelinePremium /> to enable recurring events."]);
			return;
		}
		this.set("pendingRecurringEventOperation", _extends({ kind: "delete" }, params));
	};
	/**
	* Applies the pending recurring event operation after the user selects a scope.
	* @param scope The selected scope, or null if canceled.
	*/
	selectRecurringEventScope = (scope) => {
		const { recurringEventsPlugin, pendingRecurringEventOperation, adapter } = this.state;
		if (recurringEventsPlugin == null || pendingRecurringEventOperation == null) return;
		this.set("pendingRecurringEventOperation", null);
		if (scope == null) return;
		const { occurrenceStart, onSubmit } = pendingRecurringEventOperation;
		const eventId = pendingRecurringEventOperation.kind === "update" ? pendingRecurringEventOperation.changes.id : pendingRecurringEventOperation.eventId;
		const original = schedulerEventSelectors.processedEventRequired(this.state, eventId);
		if (!original.dataTimezone.rrule) throw new Error("MUI X Scheduler: The event targeted by the recurring scope dialog is not recurring. Recurring scope changes require an event with a recurrence rule. Use updateEvent() or deleteEvent() for non-recurring events.");
		const occurrenceStartInDataTimezone = adapter.setTimezone(occurrenceStart, original.dataTimezone.timezone);
		let updatedEvents;
		if (pendingRecurringEventOperation.kind === "delete") updatedEvents = recurringEventsPlugin.deleteRecurringEvent(adapter, original, occurrenceStartInDataTimezone, scope);
		else {
			const changesInDataTimezone = recurringEventsPlugin.applyDataTimezoneToEventUpdate({
				adapter,
				originalEvent: original,
				changes: pendingRecurringEventOperation.changes
			});
			updatedEvents = recurringEventsPlugin.updateRecurringEvent(adapter, original, occurrenceStartInDataTimezone, changesInDataTimezone, scope);
		}
		this.updateEvents(updatedEvents);
		if (onSubmit) queueMicrotask(() => onSubmit());
	};
	/**
	* Deletes an event from the calendar.
	*/
	deleteEvent = (eventId) => {
		this.updateEvents({ deleted: [eventId] });
	};
	/**
	* Creates an event from an event occurrence.
	* The new event will have the same properties as the original event except:
	* - the start and end dates will be those provided as parameters.
	* - the recurrence rule will be removed.
	*/
	duplicateEventOccurrence = (eventId, start, end) => {
		const { adapter } = this.state;
		const original = schedulerEventSelectors.processedEventRequired(this.state, eventId);
		const originalModel = original.modelInBuiltInFormat;
		const dataTimezone = originalModel.timezone ?? "default";
		const duplicatedEvent = extractStandaloneEvent(original, {
			start: dateToEventString(adapter, start, originalModel.start, dataTimezone),
			end: dateToEventString(adapter, end, originalModel.end, dataTimezone)
		});
		return this.updateEvents({ created: [duplicatedEvent] }).created[0];
	};
	/**
	* Copies an event to be pasted later.
	*/
	copyEvent = (eventId) => {
		schedulerEventSelectors.processedEventRequired(this.state, eventId);
		this.set("copiedEvent", {
			id: eventId,
			action: "copy"
		});
	};
	/**
	* Cuts an event to be pasted later.
	*/
	cutEvent = (eventId) => {
		schedulerEventSelectors.processedEventRequired(this.state, eventId);
		this.set("copiedEvent", {
			id: eventId,
			action: "cut"
		});
	};
	/**
	* Pastes the copied or cut event with the provided changes.
	*/
	pasteEvent = (changes) => {
		const { adapter, copiedEvent } = this.state;
		if (!copiedEvent) return null;
		const original = schedulerEventSelectors.processedEventRequired(this.state, copiedEvent.id);
		const cleanChanges = _extends({}, changes);
		if (cleanChanges.start != null) cleanChanges.end = adapter.addMilliseconds(cleanChanges.start, original.dataTimezone.end.timestamp - original.dataTimezone.start.timestamp);
		if (copiedEvent.action === "cut") {
			const updatedEvent = _extends({ id: copiedEvent.id }, cleanChanges);
			const result = this.updateEvents({ updated: [updatedEvent] }).updated[0];
			this.set("copiedEvent", null);
			return result;
		}
		const _original$modelInBuil = original.modelInBuiltInFormat, { id } = _original$modelInBuil, copiedEventWithoutId = _objectWithoutPropertiesLoose(_original$modelInBuil, _excluded$22);
		const dataTimezone = original.modelInBuiltInFormat.timezone ?? "default";
		const stringifiedChanges = _extends({}, cleanChanges);
		if (cleanChanges.start != null) stringifiedChanges.start = dateToEventString(adapter, cleanChanges.start, original.modelInBuiltInFormat.start, dataTimezone);
		if (stringifiedChanges.end != null) stringifiedChanges.end = dateToEventString(adapter, stringifiedChanges.end, original.modelInBuiltInFormat.end, dataTimezone);
		const createdEvent = _extends({}, copiedEventWithoutId, stringifiedChanges, { extractedFromId: id });
		return this.updateEvents({ created: [createdEvent] }).created[0];
	};
	/**
	* Updates the visible resources.
	*/
	setVisibleResources = (visibleResources, event) => {
		const { visibleResources: visibleResourcesProp, onVisibleResourcesChange } = this.parameters;
		if (this.state.visibleResources !== visibleResources) {
			const eventDetails = createChangeEventDetails$1("none", event);
			onVisibleResourcesChange?.(visibleResources, eventDetails);
			if (!eventDetails.isCanceled && visibleResourcesProp === void 0) this.set("visibleResources", visibleResources);
		}
	};
	/**
	* Updates the collapsed resources.
	*/
	setCollapsedResources = (collapsedResources, event) => {
		const { collapsedResources: collapsedResourcesProp, onCollapsedResourcesChange } = this.parameters;
		if (this.state.collapsedResources !== collapsedResources) {
			const eventDetails = createChangeEventDetails$1("none", event);
			onCollapsedResourcesChange?.(collapsedResources, eventDetails);
			if (!eventDetails.isCanceled && collapsedResourcesProp === void 0) this.set("collapsedResources", collapsedResources);
		}
	};
	/**
	* Toggles the collapsed state of a single resource.
	*/
	toggleResourceCollapse = (resourceId, event) => {
		const isCollapsed = this.state.collapsedResources[resourceId] === true;
		const nextCollapsedResources = _extends({}, this.state.collapsedResources);
		if (isCollapsed) delete nextCollapsedResources[resourceId];
		else nextCollapsedResources[resourceId] = true;
		this.setCollapsedResources(nextCollapsedResources, event);
	};
	/**
	* Sets the occurrence placeholder to render while creating a new event or dragging an existing event occurrence.
	*/
	setOccurrencePlaceholder = (newPlaceholder) => {
		const { adapter, occurrencePlaceholder: previous } = this.state;
		if (shouldUpdateOccurrencePlaceholder(adapter, previous, newPlaceholder)) this.set("occurrencePlaceholder", newPlaceholder);
	};
	/**
	* Sets the key of the currently active occurrence (e.g. open in the event dialog).
	* Pass `null` to clear the active occurrence.
	*/
	setEditedOccurrenceKey = (occurrenceKey) => {
		this.set("editedOccurrenceKey", occurrenceKey);
	};
	/**
	* Builds an object containing the methods that should be exposed publicly by the scheduler components.
	*/
	buildPublicAPI() {
		return { setVisibleDate: this.setVisibleDate };
	}
};
//#endregion
//#region node_modules/@mui/x-scheduler-internals/use-event-calendar/EventCalendarStore.mjs
var DEFAULT_VIEWS = [
	"day",
	"week",
	"month",
	"agenda"
];
var DEFAULT_EVENT_CALENDAR_PREFERENCES = _extends({}, DEFAULT_SCHEDULER_PREFERENCES, {
	showWeekends: true,
	showWeekNumber: false,
	showEmptyDaysInAgenda: true,
	isSidePanelOpen: true
});
var DEFAULT_PREFERENCES_MENU_CONFIG = {
	toggleWeekendVisibility: true,
	toggleWeekNumberVisibility: true,
	toggleEmptyDaysInAgenda: true,
	toggleAmpm: true,
	toggleWeekStartsOn: false
};
var deriveStateFromParameters = (parameters) => {
	const views = parameters.views ?? DEFAULT_VIEWS;
	if (views.length === 0) throw new Error("MUI X Scheduler: EventCalendar received an empty `views` prop. This leaves the calendar without any view to render. Pass at least one view, or omit the prop to use the default set. See https://mui.com/x/react-scheduler/event-calendar/views/ for more details.");
	return {
		views,
		viewConfig: parameters.viewConfig ?? EMPTY_OBJECT
	};
};
function warnIfShouldEventRequireResourceMisconfigured(shouldEventRequireResource, resources) {
	if (shouldEventRequireResource && (resources == null || resources.length === 0)) warnOnce([
		"MUI X Scheduler: `shouldEventRequireResource` is `true` but no resources are configured.",
		"Users will not be able to select a resource, and events cannot be saved from the event dialog.",
		"Either provide at least one resource, or set `shouldEventRequireResource={false}`."
	]);
}
var mapper = {
	getInitialState: (schedulerInitialState, parameters) => {
		const shouldEventRequireResource = parameters.shouldEventRequireResource ?? false;
		warnIfShouldEventRequireResourceMisconfigured(shouldEventRequireResource, parameters.resources);
		return _extends({}, schedulerInitialState, deriveStateFromParameters(parameters), {
			preferences: parameters.preferences ?? parameters.defaultPreferences ?? EMPTY_OBJECT,
			preferencesMenuConfig: parameters.preferencesMenuConfig === false ? parameters.preferencesMenuConfig : _extends({}, DEFAULT_PREFERENCES_MENU_CONFIG, parameters.preferencesMenuConfig),
			viewDefinition: null,
			view: parameters.view ?? parameters.defaultView ?? "week",
			shouldEventRequireResource
		});
	},
	updateStateFromParameters: (newSchedulerState, parameters, updateModel) => {
		const shouldEventRequireResource = parameters.shouldEventRequireResource ?? false;
		warnIfShouldEventRequireResourceMisconfigured(shouldEventRequireResource, parameters.resources);
		const newState = _extends({}, newSchedulerState, deriveStateFromParameters(parameters), { shouldEventRequireResource });
		updateModel(newState, "view", "defaultView");
		updateModel(newState, "preferences", "defaultPreferences");
		return newState;
	}
};
/**
* Base class that can be extended by premium stores.
* Accepts instanceName as a parameter to allow subclasses to provide their own instance name.
*/
var ExtendableEventCalendarStore = class extends SchedulerStore {
	constructor(parameters, adapter, instanceName, recurringEventsPlugin = null) {
		super(parameters, adapter, instanceName, mapper, recurringEventsPlugin);
		this.assertViewValidity(this.state.view);
		this.disposables.defer(this.subscribe((state) => {
			this.assertViewValidity(state.view);
			return null;
		}));
	}
	assertViewValidity(view) {
		const views = this.state.views;
		if (!views.includes(view)) throw new Error(`MUI X Scheduler: EventCalendar received the view "${view}", which is not part of the \`views\` prop (received: ${views.join(", ")}). This leaves the calendar in an inconsistent state where the current view is not one of the allowed options. Add "${view}" to the \`views\` prop, or pass a view that is already included. See https://mui.com/x/react-scheduler/event-calendar/views/ for more details.`);
	}
	setVisibleDateAndView = (visibleDate, view, event) => {
		const { visibleDate: visibleDateProp, view: viewProp, onVisibleDateChange, onViewChange } = this.parameters;
		const hasVisibleDateChange = visibleDate !== this.state.visibleDate;
		const hasViewChange = view !== this.state.view;
		if (!hasVisibleDateChange && !hasViewChange) return;
		this.assertViewValidity(view);
		const eventDetails = createChangeEventDetails$1("none", event.nativeEvent);
		if (hasVisibleDateChange) onVisibleDateChange?.(visibleDate, eventDetails);
		if (hasViewChange) onViewChange?.(view, eventDetails);
		if (eventDetails.isCanceled) return;
		const canSetVisibleDate = visibleDateProp === void 0 && hasVisibleDateChange;
		const canSetView = viewProp === void 0 && hasViewChange;
		if (canSetVisibleDate || canSetView) this.update(_extends({}, canSetVisibleDate ? { visibleDate } : void 0, canSetView ? { view } : void 0));
	};
	setSiblingVisibleDate = (delta, event) => {
		const siblingVisibleDateGetter = this.state.viewDefinition?.siblingVisibleDateGetter;
		if (!siblingVisibleDateGetter) {
			warn("MUI X Scheduler: No definition found for the current view. Please use useEventCalendarView in your custom view.");
			return;
		}
		this.setVisibleDate({
			visibleDate: siblingVisibleDateGetter({
				delta,
				state: this.state
			}),
			event
		});
	};
	/**
	* Sets the view of the calendar.
	*/
	setView = (view, event) => {
		const { view: viewProp, onViewChange } = this.parameters;
		if (viewProp !== void 0 && !onViewChange) warn("MUI X Scheduler: EventCalendar is controlled (received a `view` prop) but `onViewChange` is not provided. View changes will be silently ignored.");
		if (view !== this.state.view) {
			this.assertViewValidity(view);
			const eventDetails = createChangeEventDetails$1("none", event);
			onViewChange?.(view, eventDetails);
			if (!eventDetails.isCanceled && viewProp === void 0) this.set("view", view);
		}
	};
	/**
	* Goes to the previous visible date span based on the current view.
	*/
	goToPreviousVisibleDate = (event) => this.setSiblingVisibleDate(-1, event);
	/**
	* Goes to the next visible date span based on the current view.
	*/
	goToNextVisibleDate = (event) => this.setSiblingVisibleDate(1, event);
	/**
	* Goes to a specific day and set the view to 'day'.
	*/
	switchToDay = (visibleDate, event) => {
		this.setVisibleDateAndView(visibleDate, "day", event);
	};
	/**
	* Updates some preferences of the calendar.
	*/
	setPreferences = (partialPreferences, event) => {
		const { preferences: preferencesProp, onPreferencesChange } = this.parameters;
		const updated = _extends({}, this.state.preferences, partialPreferences);
		const eventDetails = createChangeEventDetails$1("none", event);
		onPreferencesChange?.(updated, eventDetails);
		if (!eventDetails.isCanceled && preferencesProp === void 0) this.set("preferences", updated);
	};
	/**
	* Sets the method used to determine the previous / next visible date.
	* Returns the cleanup function.
	*/
	setViewDefinition = (definition) => {
		this.set("viewDefinition", definition);
		return () => this.set("viewDefinition", null);
	};
	buildPublicAPI() {
		return _extends({}, super.buildPublicAPI());
	}
};
/**
* Store for the EventCalendar component.
*/
var EventCalendarStore = class extends ExtendableEventCalendarStore {
	constructor(parameters, adapter) {
		super(parameters, adapter, "EventCalendarStore");
	}
};
//#endregion
//#region node_modules/@mui/x-scheduler-internals/event-calendar-selectors/eventCalendarPreferenceSelectors.mjs
var allPreferencesSelector = createSelectorMemoized((state) => state.preferences, (preferences) => _extends({}, DEFAULT_EVENT_CALENDAR_PREFERENCES, preferences));
var eventCalendarPreferenceSelectors = {
	all: allPreferencesSelector,
	menuConfig: createSelector((state) => state.preferencesMenuConfig),
	ampm: createSelector(allPreferencesSelector, (preferences) => preferences.ampm),
	showWeekends: createSelector(allPreferencesSelector, (preferences) => preferences.showWeekends),
	showWeekNumber: createSelector(allPreferencesSelector, (preferences) => preferences.showWeekNumber),
	showEmptyDaysInAgenda: createSelector(allPreferencesSelector, (preferences) => preferences.showEmptyDaysInAgenda),
	isSidePanelOpen: createSelector(allPreferencesSelector, (preferences) => preferences.isSidePanelOpen),
	weekStartsOn: createSelector(allPreferencesSelector, (preferences) => preferences.weekStartsOn)
};
//#endregion
//#region node_modules/@mui/x-scheduler-internals/event-calendar-selectors/eventCalendarViewSelectors.mjs
var eventCalendarViewSelectors = {
	view: createSelector((state) => state.view),
	views: createSelector((state) => state.views),
	hasDayView: createSelector((state) => state.views.includes("day")),
	/**
	* The user configuration for a time-grid based view (`day` or `week`), or `null` when none is set.
	*/
	timeGridConfig: createSelector((state, view) => state.viewConfig?.[view] ?? null)
};
//#endregion
//#region node_modules/@mui/x-scheduler-internals/event-calendar-selectors/eventCalendarOccurrencePlaceholderSelectors.mjs
var eventCalendarOccurrencePlaceholderSelectors = {
	placeholderInDayCell: createSelector((state, day, rowStart) => {
		if (state.occurrencePlaceholder === null || state.occurrencePlaceholder.surfaceType !== "day-grid" || state.occurrencePlaceholder.isHidden) return null;
		if (state.adapter.isSameDay(day, state.occurrencePlaceholder.start)) return state.occurrencePlaceholder;
		if (state.adapter.isSameDay(day, rowStart) && state.adapter.isWithinRange(rowStart, [state.occurrencePlaceholder.start, state.occurrencePlaceholder.end])) return state.occurrencePlaceholder;
		return null;
	}),
	placeholderInTimeRange: createSelector((state, start, end) => {
		if (state.occurrencePlaceholder === null || state.occurrencePlaceholder.surfaceType !== "time-grid" || state.occurrencePlaceholder.isHidden) return null;
		if (state.adapter.isBefore(state.occurrencePlaceholder.end, start) || state.adapter.isAfter(state.occurrencePlaceholder.start, end)) return null;
		return state.occurrencePlaceholder;
	}),
	isCreatingInDayCell: createSelector((state, day) => {
		const placeholder = state.occurrencePlaceholder;
		if (placeholder?.surfaceType !== "day-grid" || placeholder.type !== "creation") return false;
		return state.adapter.isSameDay(day, placeholder.start);
	}),
	isCreatingInTimeRange: createSelector((state, dayStart, dayEnd) => {
		const placeholder = state.occurrencePlaceholder;
		if (placeholder?.surfaceType !== "time-grid" || placeholder.type !== "creation") return false;
		if (!state.adapter.isSameDay(placeholder.start, dayStart)) return false;
		const startsBeforeEnd = state.adapter.isBefore(placeholder.start, dayEnd);
		const endsAfterStart = state.adapter.isAfter(placeholder.end, dayStart);
		return startsBeforeEnd && endsAfterStart;
	})
};
//#endregion
//#region node_modules/@mui/x-scheduler-internals/use-event-occurrences-grouped-by-day/useEventOccurrencesGroupedByDay.mjs
/**
* Gets all the event occurrences for the given days.
* For recurring events, it expands them to get all the occurrences that fall within the given days.
* It should be called once per view to get the occurrences for all the visible days in one go.
* The returned value is a Map where the key is the day key and the value is the list of occurrences for that day.
*/
function useEventOccurrencesGroupedByDay(parameters) {
	const { days } = parameters;
	const adapter = useAdapterContext();
	const store = useEventCalendarStoreContext();
	const events = useStore(store, schedulerEventSelectors.processedEventList);
	const visibleResources = useStore(store, schedulerResourceSelectors.visibleMap);
	const displayTimezone = useStore(store, schedulerOtherSelectors.displayTimezone);
	const recurringEventsPlugin = useStore(store, schedulerOtherSelectors.recurringEventsPlugin);
	return import_react.useMemo(() => innerGetEventOccurrencesGroupedByDay({
		adapter,
		days,
		events,
		visibleResources,
		displayTimezone,
		recurringEventsPlugin
	}), [
		adapter,
		days,
		events,
		visibleResources,
		displayTimezone,
		recurringEventsPlugin
	]);
}
/**
* Do not use directly, use the `useEventOccurrencesGroupedByDay` hook instead.
* This is only exported for testing purposes.
*/
function innerGetEventOccurrencesGroupedByDay(parameters) {
	const { adapter, days, events, visibleResources, displayTimezone, recurringEventsPlugin } = parameters;
	const occurrenceMap = new Map(days.map((day) => [day.key, []]));
	const occurrences = getOccurrencesFromEvents({
		adapter,
		start: adapter.startOfDay(days[0].value),
		end: adapter.endOfDay(days[days.length - 1].value),
		events,
		visibleResources,
		displayTimezone,
		recurringEventsPlugin
	});
	for (const occurrence of occurrences) {
		const eventDays = getDaysTheOccurrenceIsVisibleOn(occurrence, days, adapter);
		for (const dayKey of eventDays) occurrenceMap.get(dayKey).push(occurrence);
	}
	return occurrenceMap;
}
//#endregion
//#region node_modules/@mui/x-scheduler-internals/use-adapter/useAdapter.mjs
var DEFAULT_ADAPTER = new TemporalAdapterDateFns();
function useAdapter(dateLocale) {
	return import_react.useMemo(() => dateLocale ? new TemporalAdapterDateFns({ locale: dateLocale }) : DEFAULT_ADAPTER, [dateLocale]);
}
/**
* Checks if the given date is a weekend (Saturday or Sunday).
* TODO: move to adapter.
*/
function isWeekend(adapter, value) {
	const sunday = adapter.format(adapter.date("2025-08-09", "default"), "weekday");
	const saturday = adapter.format(adapter.date("2025-08-10", "default"), "weekday");
	const formattedValue = adapter.format(value, "weekday");
	return formattedValue === sunday || formattedValue === saturday;
}
//#endregion
//#region node_modules/@mui/x-scheduler-internals/get-day-list/getDayList.mjs
function getDayList(parameters) {
	const { adapter, start: rawStart, end: rawEnd, excludeWeekends } = parameters;
	const start = adapter.startOfDay(rawStart);
	const end = adapter.endOfDay(rawEnd);
	if (adapter.isBefore(adapter.startOfDay(end), adapter.startOfDay(start))) throw new Error(`MUI X Scheduler: getDayList: The 'end' parameter must be a day after the 'start' parameter.`);
	let current = start;
	let currentDayNumber = adapter.getDayOfWeek(current);
	const days = [];
	while (adapter.isBefore(current, end)) {
		if (!excludeWeekends || !isWeekend(adapter, current)) days.push(current);
		const prevDayNumber = currentDayNumber;
		current = adapter.addDays(current, 1);
		currentDayNumber = adapter.getDayOfWeek(current);
		if (prevDayNumber === currentDayNumber) current = adapter.startOfDay(adapter.addHours(current, 12));
	}
	return days.map((day) => processDate(day, adapter));
}
//#endregion
//#region node_modules/@mui/x-scheduler-internals/build-is-valid-drop-target/buildIsValidDropTarget.mjs
function buildIsValidDropTarget(targets) {
	const targetsSet = new Set(targets);
	return (data) => targetsSet.has(data.source);
}
//#endregion
//#region node_modules/@babel/runtime/helpers/esm/arrayWithHoles.js
function _arrayWithHoles(r) {
	if (Array.isArray(r)) return r;
}
//#endregion
//#region node_modules/@babel/runtime/helpers/esm/iterableToArrayLimit.js
function _iterableToArrayLimit(r, l) {
	var t = null == r ? null : "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"];
	if (null != t) {
		var e, n, i, u, a = [], f = !0, o = !1;
		try {
			if (i = (t = t.call(r)).next, 0 === l) {
				if (Object(t) !== t) return;
				f = !1;
			} else for (; !(f = (e = i.call(t)).done) && (a.push(e.value), a.length !== l); f = !0);
		} catch (r) {
			o = !0, n = r;
		} finally {
			try {
				if (!f && null != t["return"] && (u = t["return"](), Object(u) !== u)) return;
			} finally {
				if (o) throw n;
			}
		}
		return a;
	}
}
//#endregion
//#region node_modules/@babel/runtime/helpers/esm/arrayLikeToArray.js
function _arrayLikeToArray$3(r, a) {
	(null == a || a > r.length) && (a = r.length);
	for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e];
	return n;
}
//#endregion
//#region node_modules/@babel/runtime/helpers/esm/unsupportedIterableToArray.js
function _unsupportedIterableToArray$3(r, a) {
	if (r) {
		if ("string" == typeof r) return _arrayLikeToArray$3(r, a);
		var t = {}.toString.call(r).slice(8, -1);
		return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray$3(r, a) : void 0;
	}
}
//#endregion
//#region node_modules/@babel/runtime/helpers/esm/nonIterableRest.js
function _nonIterableRest() {
	throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
}
//#endregion
//#region node_modules/@babel/runtime/helpers/esm/slicedToArray.js
function _slicedToArray(r, e) {
	return _arrayWithHoles(r) || _iterableToArrayLimit(r, e) || _unsupportedIterableToArray$3(r, e) || _nonIterableRest();
}
//#endregion
//#region node_modules/bind-event-listener/dist/bind.js
var require_bind = /* @__PURE__ */ __commonJSMin(((exports) => {
	Object.defineProperty(exports, "__esModule", { value: true });
	exports.bind = void 0;
	function bind(target, _a) {
		var type = _a.type, listener = _a.listener, options = _a.options;
		target.addEventListener(type, listener, options);
		return function unbind() {
			target.removeEventListener(type, listener, options);
		};
	}
	exports.bind = bind;
}));
//#endregion
//#region node_modules/bind-event-listener/dist/bind-all.js
var require_bind_all = /* @__PURE__ */ __commonJSMin(((exports) => {
	var __assign = exports && exports.__assign || function() {
		__assign = Object.assign || function(t) {
			for (var s, i = 1, n = arguments.length; i < n; i++) {
				s = arguments[i];
				for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p)) t[p] = s[p];
			}
			return t;
		};
		return __assign.apply(this, arguments);
	};
	Object.defineProperty(exports, "__esModule", { value: true });
	exports.bindAll = void 0;
	var bind_1 = require_bind();
	function toOptions(value) {
		if (typeof value === "undefined") return;
		if (typeof value === "boolean") return { capture: value };
		return value;
	}
	function getBinding(original, sharedOptions) {
		if (sharedOptions == null) return original;
		return __assign(__assign({}, original), { options: __assign(__assign({}, toOptions(sharedOptions)), toOptions(original.options)) });
	}
	function bindAll(target, bindings, sharedOptions) {
		var unbinds = bindings.map(function(original) {
			var binding = getBinding(original, sharedOptions);
			return (0, bind_1.bind)(target, binding);
		});
		return function unbindAll() {
			unbinds.forEach(function(unbind) {
				return unbind();
			});
		};
	}
	exports.bindAll = bindAll;
}));
//#endregion
//#region node_modules/@atlaskit/pragmatic-drag-and-drop/dist/esm/honey-pot-fix/honey-pot-data-attribute.js
var import_dist = (/* @__PURE__ */ __commonJSMin(((exports) => {
	Object.defineProperty(exports, "__esModule", { value: true });
	exports.bindAll = exports.bind = void 0;
	var bind_1 = require_bind();
	Object.defineProperty(exports, "bind", {
		enumerable: true,
		get: function() {
			return bind_1.bind;
		}
	});
	var bind_all_1 = require_bind_all();
	Object.defineProperty(exports, "bindAll", {
		enumerable: true,
		get: function() {
			return bind_all_1.bindAll;
		}
	});
})))();
var honeyPotDataAttribute = "data-pdnd-honey-pot";
//#endregion
//#region node_modules/@atlaskit/pragmatic-drag-and-drop/dist/esm/honey-pot-fix/is-honey-pot-element.js
function isHoneyPotElement(target) {
	return target instanceof Element && target.hasAttribute("data-pdnd-honey-pot");
}
//#endregion
//#region node_modules/@atlaskit/pragmatic-drag-and-drop/dist/esm/honey-pot-fix/get-element-from-point-without-honey-pot.js
function getElementFromPointWithoutHoneypot(client) {
	var _document$elementsFro2 = _slicedToArray(document.elementsFromPoint(client.x, client.y), 2), top = _document$elementsFro2[0], second = _document$elementsFro2[1];
	if (!top) return null;
	if (isHoneyPotElement(top)) return second !== null && second !== void 0 ? second : null;
	return top;
}
//#endregion
//#region node_modules/@babel/runtime/helpers/esm/defineProperty.js
function _defineProperty(e, r, t) {
	return (r = toPropertyKey(r)) in e ? Object.defineProperty(e, r, {
		value: t,
		enumerable: !0,
		configurable: !0,
		writable: !0
	}) : e[r] = t, e;
}
//#endregion
//#region node_modules/@atlaskit/pragmatic-drag-and-drop/dist/esm/util/max-z-index.js
var maxZIndex = 2147483647;
//#endregion
//#region node_modules/@atlaskit/pragmatic-drag-and-drop/dist/esm/util/popover-reset-styles.js
/**
* Resets user agent styles for `[popover]` elements.
*
* Browsers apply these defaults to popovers (per the WHATWG HTML spec rendering section):
* - `inset: 0` + `margin: auto` → centers the popover in the viewport
* - `border: solid` → visible border
* - `padding: 0.25em` → internal spacing
* - `overflow: auto` → scrollbars when content overflows
* - `color: CanvasText` + `background-color: Canvas` → system theme colors
*
* This object neutralizes those defaults so the popover behaves like a plain
* positioned element. `width` and `height` (UA default: `fit-content`) are not
* reset here because consumers set their own dimensions.
*/
var popoverResetUserAgentStyles = {
	inset: "unset",
	border: "none",
	padding: 0,
	margin: 0,
	overflow: "visible",
	color: "inherit",
	background: "transparent"
};
//#endregion
//#region node_modules/@atlaskit/pragmatic-drag-and-drop/dist/esm/public-utils/once.js
/** Provide a function that you only ever want to be called a single time */
function once(fn) {
	var cache = null;
	return function wrapped() {
		if (!cache) {
			for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) args[_key] = arguments[_key];
			cache = { result: fn.apply(this, args) };
		}
		return cache.result;
	};
}
//#endregion
//#region node_modules/@atlaskit/pragmatic-drag-and-drop/dist/esm/util/supports-popover.js
/**
* Returns `true` if the browser supports the Popover API (top-layer).
*
* When supported, elements with `popover="manual"` can be promoted to the
* browser's top layer via `element.showPopover()`, which is more reliable
* than `z-index` for ensuring an element renders above everything else.
*
* @see https://developer.mozilla.org/en-US/docs/Web/API/Popover_API
*/
var supportsPopover = once(function supportsPopover() {
	return typeof HTMLElement !== "undefined" && typeof HTMLElement.prototype.showPopover === "function";
});
//#endregion
//#region node_modules/@atlaskit/pragmatic-drag-and-drop/dist/esm/honey-pot-fix/make-honey-pot-fix.js
function ownKeys$4(e, r) {
	var t = Object.keys(e);
	if (Object.getOwnPropertySymbols) {
		var o = Object.getOwnPropertySymbols(e);
		r && (o = o.filter(function(r) {
			return Object.getOwnPropertyDescriptor(e, r).enumerable;
		})), t.push.apply(t, o);
	}
	return t;
}
function _objectSpread$4(e) {
	for (var r = 1; r < arguments.length; r++) {
		var t = null != arguments[r] ? arguments[r] : {};
		r % 2 ? ownKeys$4(Object(t), !0).forEach(function(r) {
			_defineProperty(e, r, t[r]);
		}) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys$4(Object(t)).forEach(function(r) {
			Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r));
		});
	}
	return e;
}
var honeyPotSize = 2;
var halfHoneyPotSize = honeyPotSize / 2;
/**
* `clientX` and `clientY` can be in sub pixels (eg `2.332`)
* However, browser hitbox testing is commonly do to the closest pixel.
*
* → https://issues.chromium.org/issues/40940531
*
* To be sure that the honey pot will be over the `client` position,
* we `.floor()` `clientX` and`clientY` and then make it `2px` in size.
**/
function floorToClosestPixel(point) {
	return {
		x: Math.floor(point.x),
		y: Math.floor(point.y)
	};
}
/**
* We want to make sure the honey pot sits around the users position.
* This seemed to be the most resilient while testing.
*/
function pullBackByHalfHoneyPotSize(point) {
	return {
		x: point.x - halfHoneyPotSize,
		y: point.y - halfHoneyPotSize
	};
}
/**
* Prevent the honey pot from changing the window size.
* This is super unlikely to occur, but just being safe.
*/
function preventGoingBackwardsOffScreen(point) {
	return {
		x: Math.max(point.x, 0),
		y: Math.max(point.y, 0)
	};
}
/**
* Prevent the honey pot from changing the window size.
* This is super unlikely to occur, but just being safe.
*/
function preventGoingForwardsOffScreen(point) {
	return {
		x: Math.min(point.x, window.innerWidth - honeyPotSize),
		y: Math.min(point.y, window.innerHeight - honeyPotSize)
	};
}
/**
* Create a `2x2` `DOMRect` around the `client` position
*/
function getHoneyPotRectFor(_ref) {
	var client = _ref.client;
	var point = preventGoingForwardsOffScreen(preventGoingBackwardsOffScreen(pullBackByHalfHoneyPotSize(floorToClosestPixel(client))));
	return DOMRect.fromRect({
		x: point.x,
		y: point.y,
		width: honeyPotSize,
		height: honeyPotSize
	});
}
function getRectStyles(_ref2) {
	var clientRect = _ref2.clientRect;
	return {
		left: "".concat(clientRect.left, "px"),
		top: "".concat(clientRect.top, "px"),
		width: "".concat(clientRect.width, "px"),
		height: "".concat(clientRect.height, "px")
	};
}
function isWithin$1(_ref3) {
	var client = _ref3.client, clientRect = _ref3.clientRect;
	return client.x >= clientRect.x && client.x <= clientRect.x + clientRect.width && client.y >= clientRect.y && client.y <= clientRect.y + clientRect.height;
}
/**
* The honey pot fix is designed to get around a painful bug in all browsers.
*
* [Overview](https://www.youtube.com/watch?v=udE9qbFTeQg)
*
* **Background**
*
* When a drag starts, browsers incorrectly think that the users pointer is
* still depressed where the drag started. Any element that goes under this position
* will be entered into, causing `"mouseenter"` events and `":hover"` styles to be applied.
*
* _This is a violation of the spec_
*
* > "From the moment that the user agent is to initiate the drag-and-drop operation,
* > until the end 	of the drag-and-drop operation, device input events
* > (e.g. mouse and keyboard events) must be suppressed."
* >
* > - https://html.spec.whatwg.org/multipage/dnd.html#drag-and-drop-processing-model
*
* _Some impacts_
*
* - `":hover"` styles being applied where they shouldn't (looks messy)
* - components such as tooltips responding to `"mouseenter"` can show during a drag,
*   and on an element the user isn't even over
*
* Bug: https://issues.chromium.org/issues/41129937
*
* **Honey pot fix**
*
* 1. Create an element where the browser thinks the depressed pointer is
*    to absorb the incorrect pointer events
* 2. Remove that element when it is no longer needed
*/
function mountHoneyPot(_ref4) {
	var initial = _ref4.initial;
	var element = document.createElement("div");
	element.setAttribute(honeyPotDataAttribute, "true");
	if (supportsPopover()) element.setAttribute("popover", "manual");
	var clientRect = getHoneyPotRectFor({ client: initial });
	Object.assign(element.style, _objectSpread$4(_objectSpread$4({ position: "fixed" }, supportsPopover() ? popoverResetUserAgentStyles : { zIndex: maxZIndex }), {}, {
		backgroundColor: "transparent",
		padding: 0,
		margin: 0,
		boxSizing: "border-box",
		pointerEvents: "auto"
	}, getRectStyles({ clientRect })));
	document.body.appendChild(element);
	if (supportsPopover()) element.showPopover();
	/**
	*  🦊 In firefox we can get `"pointermove"` events after the drag
	* has started, which is a spec violation.
	* The final `"pointermove"` will reveal where the "depressed" position
	* is for our honey pot fix.
	*/
	var unbindPointerMove = (0, import_dist.bind)(window, {
		type: "pointermove",
		listener: function listener(event) {
			clientRect = getHoneyPotRectFor({ client: {
				x: event.clientX,
				y: event.clientY
			} });
			Object.assign(element.style, getRectStyles({ clientRect }));
		},
		options: { capture: true }
	});
	return function finish(_ref5) {
		var current = _ref5.current;
		unbindPointerMove();
		if (isWithin$1({
			client: current,
			clientRect
		})) {
			element.remove();
			return;
		}
		function cleanup() {
			unbindPostDragEvents();
			element.remove();
		}
		var unbindPostDragEvents = (0, import_dist.bindAll)(window, [
			{
				type: "pointerdown",
				listener: cleanup
			},
			{
				type: "pointermove",
				listener: cleanup
			},
			{
				type: "focusin",
				listener: cleanup
			},
			{
				type: "focusout",
				listener: cleanup
			},
			{
				type: "dragstart",
				listener: cleanup
			},
			{
				type: "dragenter",
				listener: cleanup
			},
			{
				type: "dragover",
				listener: cleanup
			}
		], { capture: true });
	};
}
function makeHoneyPotFix() {
	var latestPointerMove = null;
	function bindEvents() {
		latestPointerMove = null;
		return (0, import_dist.bind)(window, {
			type: "pointermove",
			listener: function listener(event) {
				latestPointerMove = {
					x: event.clientX,
					y: event.clientY
				};
			},
			options: { capture: true }
		});
	}
	function getOnPostDispatch() {
		var finish = null;
		return function onPostEvent(_ref6) {
			var eventName = _ref6.eventName, payload = _ref6.payload;
			if (eventName === "onDragStart") {
				var input = payload.location.initial.input;
				finish = mountHoneyPot({ initial: latestPointerMove !== null && latestPointerMove !== void 0 ? latestPointerMove : {
					x: input.clientX,
					y: input.clientY
				} });
			}
			if (eventName === "onDrop") {
				var _finish;
				var _input = payload.location.current.input;
				(_finish = finish) === null || _finish === void 0 || _finish({ current: {
					x: _input.clientX,
					y: _input.clientY
				} });
				finish = null;
				latestPointerMove = null;
			}
		};
	}
	return {
		bindEvents,
		getOnPostDispatch
	};
}
//#endregion
//#region node_modules/@babel/runtime/helpers/esm/arrayWithoutHoles.js
function _arrayWithoutHoles(r) {
	if (Array.isArray(r)) return _arrayLikeToArray$3(r);
}
//#endregion
//#region node_modules/@babel/runtime/helpers/esm/iterableToArray.js
function _iterableToArray(r) {
	if ("undefined" != typeof Symbol && null != r[Symbol.iterator] || null != r["@@iterator"]) return Array.from(r);
}
//#endregion
//#region node_modules/@babel/runtime/helpers/esm/nonIterableSpread.js
function _nonIterableSpread() {
	throw new TypeError("Invalid attempt to spread non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
}
//#endregion
//#region node_modules/@babel/runtime/helpers/esm/toConsumableArray.js
function _toConsumableArray(r) {
	return _arrayWithoutHoles(r) || _iterableToArray(r) || _unsupportedIterableToArray$3(r) || _nonIterableSpread();
}
//#endregion
//#region node_modules/@atlaskit/pragmatic-drag-and-drop/dist/esm/util/is-firefox.js
/**
* Returns `true` if a `Firefox` browser
* */
var isFirefox = once(function isFirefox() {
	return navigator.userAgent.includes("Firefox");
});
//#endregion
//#region node_modules/@atlaskit/pragmatic-drag-and-drop/dist/esm/util/is-safari.js
/**
* Returns `true` if a `Safari` browser.
* Returns `true` if the browser is running on iOS (they are all Safari).
*
* Use `isSafariOnIOS` if you want to check if something is Safari + iOS
* */
var isSafari = once(function isSafari() {
	var userAgent = navigator.userAgent;
	return userAgent.includes("AppleWebKit") && !userAgent.includes("Chrome");
});
//#endregion
//#region node_modules/@atlaskit/pragmatic-drag-and-drop/dist/esm/util/changing-window/count-events-for-safari.js
var symbols = {
	isLeavingWindow: Symbol("leaving"),
	isEnteringWindow: Symbol("entering")
};
function isLeavingWindowInSafari(_ref2) {
	var dragLeave = _ref2.dragLeave;
	if (!isSafari()) return false;
	return dragLeave.hasOwnProperty(symbols.isLeavingWindow);
}
(function fixSafari() {
	if (typeof window === "undefined") return;
	if (!isSafari()) return;
	function getInitialState() {
		return {
			enterCount: 0,
			isOverWindow: false
		};
	}
	var state = getInitialState();
	function resetState() {
		state = getInitialState();
	}
	(0, import_dist.bindAll)(window, [
		{
			type: "dragstart",
			listener: function listener() {
				state.enterCount = 0;
				state.isOverWindow = true;
			}
		},
		{
			type: "drop",
			listener: resetState
		},
		{
			type: "dragend",
			listener: resetState
		},
		{
			type: "dragenter",
			listener: function listener(event) {
				if (!state.isOverWindow && state.enterCount === 0) event[symbols.isEnteringWindow] = true;
				state.isOverWindow = true;
				state.enterCount++;
			}
		},
		{
			type: "dragleave",
			listener: function listener(event) {
				state.enterCount--;
				if (state.isOverWindow && state.enterCount === 0) {
					event[symbols.isLeavingWindow] = true;
					state.isOverWindow = false;
				}
			}
		}
	], { capture: true });
})();
//#endregion
//#region node_modules/@atlaskit/pragmatic-drag-and-drop/dist/esm/util/changing-window/is-from-another-window.js
/**
* Does the `EventTarget` look like a `Node` based on "duck typing".
*
* Helpful when the `Node` might be outside of the current document
* so we cannot to an `target instanceof Node` check.
*/
function isNodeLike(target) {
	return "nodeName" in target;
}
/**
* Is an `EventTarget` a `Node` from another `window`?
*/
function isFromAnotherWindow(eventTarget) {
	return isNodeLike(eventTarget) && eventTarget.ownerDocument !== document;
}
//#endregion
//#region node_modules/@atlaskit/pragmatic-drag-and-drop/dist/esm/util/changing-window/is-leaving-window.js
function isLeavingWindow(_ref) {
	var dragLeave = _ref.dragLeave;
	var type = dragLeave.type, relatedTarget = dragLeave.relatedTarget;
	if (type !== "dragleave") return false;
	if (isSafari()) return isLeavingWindowInSafari({ dragLeave });
	if (relatedTarget == null) return true;
	/**
	* 🦊 Exception: `iframe` in Firefox (`125.0`)
	*
	* Case 1: parent `window` → child `iframe`
	* `dragLeave.relatedTarget` is element _inside_ the child `iframe`
	* (foreign element)
	*
	* Case 2: child `iframe` → parent `window`
	* `dragLeave.relatedTarget` is the `iframe` in the parent `window`
	* (foreign element)
	*/
	if (isFirefox()) return isFromAnotherWindow(relatedTarget);
	/**
	* 🌏 Exception: `iframe` in Chrome (`124.0`)
	*
	* Case 1: parent `window` → child `iframe`
	* `dragLeave.relatedTarget` is the `iframe` in the parent `window`
	*
	* Case 2: child `iframe` → parent `window`
	* `dragLeave.relatedTarget` is `null` *(standard check)*
	*/
	return relatedTarget instanceof HTMLIFrameElement;
}
//#endregion
//#region node_modules/@atlaskit/pragmatic-drag-and-drop/dist/esm/util/detect-broken-drag.js
function getBindingsForBrokenDrags(_ref) {
	var onDragEnd = _ref.onDragEnd;
	return [{
		type: "pointermove",
		listener: function() {
			var callCount = 0;
			return function listener() {
				if (callCount < 20) {
					callCount++;
					return;
				}
				onDragEnd();
			};
		}()
	}, {
		type: "pointerdown",
		listener: onDragEnd
	}];
}
//#endregion
//#region node_modules/@atlaskit/pragmatic-drag-and-drop/dist/esm/util/get-input.js
function getInput(event) {
	return {
		altKey: event.altKey,
		button: event.button,
		buttons: event.buttons,
		ctrlKey: event.ctrlKey,
		metaKey: event.metaKey,
		shiftKey: event.shiftKey,
		clientX: event.clientX,
		clientY: event.clientY,
		pageX: event.pageX,
		pageY: event.pageY
	};
}
//#endregion
//#region node_modules/@atlaskit/pragmatic-drag-and-drop/dist/esm/ledger/dispatch-consumer-event.js
var scheduleOnDrag = function rafSchd(fn) {
	var lastArgs = [];
	var frameId = null;
	var wrapperFn = function wrapperFn() {
		for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) args[_key] = arguments[_key];
		lastArgs = args;
		if (frameId) return;
		frameId = requestAnimationFrame(function() {
			frameId = null;
			fn.apply(void 0, lastArgs);
		});
	};
	wrapperFn.cancel = function() {
		if (!frameId) return;
		cancelAnimationFrame(frameId);
		frameId = null;
	};
	return wrapperFn;
}(function(fn) {
	return fn();
});
var dragStart = function() {
	var scheduled = null;
	function schedule(fn) {
		scheduled = {
			frameId: requestAnimationFrame(function() {
				scheduled = null;
				fn();
			}),
			fn
		};
	}
	function flush() {
		if (scheduled) {
			cancelAnimationFrame(scheduled.frameId);
			scheduled.fn();
			scheduled = null;
		}
	}
	return {
		schedule,
		flush
	};
}();
function makeDispatch(_ref) {
	var source = _ref.source, initial = _ref.initial, dispatchEvent = _ref.dispatchEvent;
	var previous = { dropTargets: [] };
	function safeDispatch(args) {
		dispatchEvent(args);
		previous = { dropTargets: args.payload.location.current.dropTargets };
	}
	return {
		start: function start(_ref2) {
			var nativeSetDragImage = _ref2.nativeSetDragImage;
			var location = {
				current: initial,
				previous,
				initial
			};
			safeDispatch({
				eventName: "onGenerateDragPreview",
				payload: {
					source,
					location,
					nativeSetDragImage
				}
			});
			dragStart.schedule(function() {
				safeDispatch({
					eventName: "onDragStart",
					payload: {
						source,
						location
					}
				});
			});
		},
		dragUpdate: function dragUpdate(_ref3) {
			var current = _ref3.current;
			dragStart.flush();
			scheduleOnDrag.cancel();
			safeDispatch({
				eventName: "onDropTargetChange",
				payload: {
					source,
					location: {
						initial,
						previous,
						current
					}
				}
			});
		},
		drag: function drag(_ref4) {
			var current = _ref4.current;
			scheduleOnDrag(function() {
				dragStart.flush();
				safeDispatch({
					eventName: "onDrag",
					payload: {
						source,
						location: {
							initial,
							previous,
							current
						}
					}
				});
			});
		},
		drop: function drop(_ref5) {
			var current = _ref5.current, updatedSourcePayload = _ref5.updatedSourcePayload;
			dragStart.flush();
			scheduleOnDrag.cancel();
			safeDispatch({
				eventName: "onDrop",
				payload: {
					source: updatedSourcePayload !== null && updatedSourcePayload !== void 0 ? updatedSourcePayload : source,
					location: {
						current,
						previous,
						initial
					}
				}
			});
		}
	};
}
//#endregion
//#region node_modules/@atlaskit/pragmatic-drag-and-drop/dist/esm/ledger/lifecycle-manager.js
var globalState = { isActive: false };
function canStart() {
	return !globalState.isActive;
}
function getNativeSetDragImage(event) {
	if (event.dataTransfer) return event.dataTransfer.setDragImage.bind(event.dataTransfer);
	return null;
}
function hasHierarchyChanged(_ref) {
	var current = _ref.current, next = _ref.next;
	if (current.length !== next.length) return true;
	for (var i = 0; i < current.length; i++) if (current[i].element !== next[i].element) return true;
	return false;
}
function start$1(_ref2) {
	var event = _ref2.event, dragType = _ref2.dragType, getDropTargetsOver = _ref2.getDropTargetsOver, dispatchEvent = _ref2.dispatchEvent;
	if (!canStart()) return;
	var initial = getStartLocation({
		event,
		dragType,
		getDropTargetsOver
	});
	globalState.isActive = true;
	var state = { current: initial };
	setDropEffectOnEvent({
		event,
		current: initial.dropTargets
	});
	var dispatch = makeDispatch({
		source: dragType.payload,
		dispatchEvent,
		initial
	});
	function updateState(next) {
		var hasChanged = hasHierarchyChanged({
			current: state.current.dropTargets,
			next: next.dropTargets
		});
		state.current = next;
		if (hasChanged) dispatch.dragUpdate({ current: state.current });
	}
	function onUpdateEvent(event) {
		var input = getInput(event);
		var nextDropTargets = getDropTargetsOver({
			target: isHoneyPotElement(event.target) ? getElementFromPointWithoutHoneypot({
				x: input.clientX,
				y: input.clientY
			}) : event.target,
			input,
			source: dragType.payload,
			current: state.current.dropTargets
		});
		if (nextDropTargets.length) {
			event.preventDefault();
			setDropEffectOnEvent({
				event,
				current: nextDropTargets
			});
		}
		updateState({
			dropTargets: nextDropTargets,
			input
		});
	}
	function cancel() {
		if (state.current.dropTargets.length) updateState({
			dropTargets: [],
			input: state.current.input
		});
		dispatch.drop({
			current: state.current,
			updatedSourcePayload: null
		});
		finish();
	}
	function finish() {
		globalState.isActive = false;
		unbindEvents();
	}
	var unbindEvents = (0, import_dist.bindAll)(window, [
		{
			type: "dragover",
			listener: function listener(event) {
				onUpdateEvent(event);
				dispatch.drag({ current: state.current });
			}
		},
		{
			type: "dragenter",
			listener: onUpdateEvent
		},
		{
			type: "dragleave",
			listener: function listener(event) {
				if (!isLeavingWindow({ dragLeave: event })) return;
				/**
				* At this point we don't know if a drag is being cancelled,
				* or if a drag is leaving the `window`.
				*
				* Both have:
				*   1. "dragleave" (with `relatedTarget: null`)
				*   2. "dragend" (a "dragend" can occur when outside the `window`)
				*
				* **Clearing drop targets**
				*
				* For either case we are clearing the the drop targets
				*
				* - cancelling: we clear drop targets in `"dragend"` anyway
				* - leaving the `window`: we clear the drop targets (to clear stickiness)
				*
				* **Leaving the window and finishing the drag**
				*
				* _internal drags_
				*
				* - The drag continues when the user is outside the `window`
				*   and can resume if the user drags back over the `window`,
				*   or end when the user drops in an external `window`.
				* - We will get a `"dragend"`, or we can listen for other
				*   events to determine the drag is finished when the user re-enters the `window`).
				*
				* _external drags_
				*
				* - We conclude the drag operation.
				* - We have no idea if the user will drag back over the `window`,
				*   or if the drag ends elsewhere.
				* - We will create a new drag if the user re-enters the `window`.
				*
				* **Not updating `input`**
				*
				* 🐛 Bug[Chrome] the final `"dragleave"` has default input values (eg `clientX == 0`)
				* Workaround: intentionally not updating `input` in "dragleave"
				* rather than the users current input values
				* - [Conversation](https://twitter.com/alexandereardon/status/1642697633864241152)
				* - [Bug](https://bugs.chromium.org/p/chromium/issues/detail?id=1429937)
				**/
				updateState({
					input: state.current.input,
					dropTargets: []
				});
				if (dragType.startedFrom === "external") cancel();
			}
		},
		{
			type: "drop",
			listener: function listener(event) {
				state.current = {
					dropTargets: state.current.dropTargets,
					input: getInput(event)
				};
				/** If there are no drop targets, then we will get
				* a "drop" event if:
				* - `preventUnhandled()` is being used
				* - there is an unmanaged drop target (eg another library)
				* In these cases, it's up to the consumer
				* to handle the drop if it's not over one of our drop targets
				* - `preventUnhandled()` will cancel the "drop"
				* - unmanaged drop targets can handle the "drop" how they want to
				* We won't call `event.preventDefault()` in this call path */
				if (!state.current.dropTargets.length) {
					cancel();
					return;
				}
				event.preventDefault();
				setDropEffectOnEvent({
					event,
					current: state.current.dropTargets
				});
				dispatch.drop({
					current: state.current,
					updatedSourcePayload: dragType.type === "external" ? dragType.getDropPayload(event) : null
				});
				finish();
			}
		},
		{
			type: "dragend",
			listener: function listener(event) {
				state.current = {
					dropTargets: state.current.dropTargets,
					input: getInput(event)
				};
				cancel();
			}
		}
	].concat(_toConsumableArray(getBindingsForBrokenDrags({ onDragEnd: cancel }))), { capture: true });
	dispatch.start({ nativeSetDragImage: getNativeSetDragImage(event) });
}
function setDropEffectOnEvent(_ref3) {
	var _current$;
	var event = _ref3.event;
	var innerMost = (_current$ = _ref3.current[0]) === null || _current$ === void 0 ? void 0 : _current$.dropEffect;
	if (innerMost != null && event.dataTransfer) event.dataTransfer.dropEffect = innerMost;
}
function getStartLocation(_ref4) {
	var event = _ref4.event, dragType = _ref4.dragType, getDropTargetsOver = _ref4.getDropTargetsOver;
	var input = getInput(event);
	if (dragType.startedFrom === "external") return {
		input,
		dropTargets: []
	};
	return {
		input,
		dropTargets: getDropTargetsOver({
			input,
			source: dragType.payload,
			target: event.target,
			current: []
		})
	};
}
var lifecycle = {
	canStart,
	start: start$1
};
//#endregion
//#region node_modules/@atlaskit/pragmatic-drag-and-drop/dist/esm/ledger/usage-ledger.js
var ledger$1 = /* @__PURE__ */ new Map();
function registerUsage(_ref) {
	var typeKey = _ref.typeKey, mount = _ref.mount;
	var entry = ledger$1.get(typeKey);
	if (entry) {
		entry.usageCount++;
		return entry;
	}
	var initial = {
		typeKey,
		unmount: mount(),
		usageCount: 1
	};
	ledger$1.set(typeKey, initial);
	return initial;
}
function register(args) {
	var entry = registerUsage(args);
	return function unregister() {
		entry.usageCount--;
		if (entry.usageCount > 0) return;
		entry.unmount();
		ledger$1.delete(args.typeKey);
	};
}
//#endregion
//#region node_modules/@atlaskit/pragmatic-drag-and-drop/dist/esm/public-utils/combine.js
/** Create a new combined function that will call all the provided functions */
function combine() {
	for (var _len = arguments.length, fns = new Array(_len), _key = 0; _key < _len; _key++) fns[_key] = arguments[_key];
	return function cleanup() {
		fns.forEach(function(fn) {
			return fn();
		});
	};
}
//#endregion
//#region node_modules/@atlaskit/pragmatic-drag-and-drop/dist/esm/util/add-attribute.js
function addAttribute(element, _ref) {
	var attribute = _ref.attribute, value = _ref.value;
	element.setAttribute(attribute, value);
	return function() {
		return element.removeAttribute(attribute);
	};
}
//#endregion
//#region node_modules/@atlaskit/pragmatic-drag-and-drop/dist/esm/make-adapter/make-drop-target.js
function ownKeys$3(e, r) {
	var t = Object.keys(e);
	if (Object.getOwnPropertySymbols) {
		var o = Object.getOwnPropertySymbols(e);
		r && (o = o.filter(function(r) {
			return Object.getOwnPropertyDescriptor(e, r).enumerable;
		})), t.push.apply(t, o);
	}
	return t;
}
function _objectSpread$3(e) {
	for (var r = 1; r < arguments.length; r++) {
		var t = null != arguments[r] ? arguments[r] : {};
		r % 2 ? ownKeys$3(Object(t), !0).forEach(function(r) {
			_defineProperty(e, r, t[r]);
		}) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys$3(Object(t)).forEach(function(r) {
			Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r));
		});
	}
	return e;
}
function _createForOfIteratorHelper$2(r, e) {
	var t = "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"];
	if (!t) {
		if (Array.isArray(r) || (t = _unsupportedIterableToArray$2(r)) || e && r && "number" == typeof r.length) {
			t && (r = t);
			var _n = 0, F = function F() {};
			return {
				s: F,
				n: function n() {
					return _n >= r.length ? { done: !0 } : {
						done: !1,
						value: r[_n++]
					};
				},
				e: function e(r) {
					throw r;
				},
				f: F
			};
		}
		throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
	}
	var o, a = !0, u = !1;
	return {
		s: function s() {
			t = t.call(r);
		},
		n: function n() {
			var r = t.next();
			return a = r.done, r;
		},
		e: function e(r) {
			u = !0, o = r;
		},
		f: function f() {
			try {
				a || null == t.return || t.return();
			} finally {
				if (u) throw o;
			}
		}
	};
}
function _unsupportedIterableToArray$2(r, a) {
	if (r) {
		if ("string" == typeof r) return _arrayLikeToArray$2(r, a);
		var t = {}.toString.call(r).slice(8, -1);
		return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray$2(r, a) : void 0;
	}
}
function _arrayLikeToArray$2(r, a) {
	(null == a || a > r.length) && (a = r.length);
	for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e];
	return n;
}
function copyReverse(array) {
	return array.slice(0).reverse();
}
function makeDropTarget(_ref) {
	var typeKey = _ref.typeKey, defaultDropEffect = _ref.defaultDropEffect;
	var registry = /* @__PURE__ */ new WeakMap();
	var dropTargetDataAtt = "data-drop-target-for-".concat(typeKey);
	var dropTargetSelector = "[".concat(dropTargetDataAtt, "]");
	function addToRegistry(args) {
		registry.set(args.element, args);
		return function() {
			return registry.delete(args.element);
		};
	}
	function dropTargetForConsumers(args) {
		var existing = registry.get(args.element);
		if (existing) console.warn("You have already registered a [".concat(typeKey, "] dropTarget on the same element"), {
			existing,
			proposed: args
		});
		if (args.element instanceof HTMLIFrameElement) console.warn("\n            We recommend not registering <iframe> elements as drop targets\n            as it can result in some strange browser event ordering.\n          ".replace(/\s{2,}/g, " ").trim());
		return once(combine(addAttribute(args.element, {
			attribute: dropTargetDataAtt,
			value: "true"
		}), addToRegistry(args)));
	}
	function getActualDropTargets(_ref2) {
		var _args$getData, _args$getData2, _args$getDropEffect, _args$getDropEffect2;
		var source = _ref2.source, target = _ref2.target, input = _ref2.input, _ref2$result = _ref2.result, result = _ref2$result === void 0 ? [] : _ref2$result;
		if (target == null) return result;
		if (!(target instanceof Element)) {
			if (target instanceof Node) return getActualDropTargets({
				source,
				target: target.parentElement,
				input,
				result
			});
			return result;
		}
		var closest = target.closest(dropTargetSelector);
		if (closest == null) return result;
		var args = registry.get(closest);
		if (args == null) return result;
		var feedback = {
			input,
			source,
			element: args.element
		};
		if (args.canDrop && !args.canDrop(feedback)) return getActualDropTargets({
			source,
			target: args.element.parentElement,
			input,
			result
		});
		var data = (_args$getData = (_args$getData2 = args.getData) === null || _args$getData2 === void 0 ? void 0 : _args$getData2.call(args, feedback)) !== null && _args$getData !== void 0 ? _args$getData : {};
		var dropEffect = (_args$getDropEffect = (_args$getDropEffect2 = args.getDropEffect) === null || _args$getDropEffect2 === void 0 ? void 0 : _args$getDropEffect2.call(args, feedback)) !== null && _args$getDropEffect !== void 0 ? _args$getDropEffect : defaultDropEffect;
		var record = {
			data,
			element: args.element,
			dropEffect,
			isActiveDueToStickiness: false
		};
		return getActualDropTargets({
			source,
			target: args.element.parentElement,
			input,
			result: [].concat(_toConsumableArray(result), [record])
		});
	}
	function notifyCurrent(_ref3) {
		var eventName = _ref3.eventName, payload = _ref3.payload;
		var _iterator = _createForOfIteratorHelper$2(payload.location.current.dropTargets), _step;
		try {
			for (_iterator.s(); !(_step = _iterator.n()).done;) {
				var _entry$eventName;
				var record = _step.value;
				var entry = registry.get(record.element);
				var args = _objectSpread$3(_objectSpread$3({}, payload), {}, { self: record });
				entry === null || entry === void 0 || (_entry$eventName = entry[eventName]) === null || _entry$eventName === void 0 || _entry$eventName.call(entry, args);
			}
		} catch (err) {
			_iterator.e(err);
		} finally {
			_iterator.f();
		}
	}
	var actions = {
		onGenerateDragPreview: notifyCurrent,
		onDrag: notifyCurrent,
		onDragStart: notifyCurrent,
		onDrop: notifyCurrent,
		onDropTargetChange: function onDropTargetChange(_ref4) {
			var payload = _ref4.payload;
			var isCurrent = new Set(payload.location.current.dropTargets.map(function(record) {
				return record.element;
			}));
			var visited = /* @__PURE__ */ new Set();
			var _iterator2 = _createForOfIteratorHelper$2(payload.location.previous.dropTargets), _step2;
			try {
				for (_iterator2.s(); !(_step2 = _iterator2.n()).done;) {
					var _entry$onDropTargetCh;
					var record = _step2.value;
					visited.add(record.element);
					var entry = registry.get(record.element);
					var isOver = isCurrent.has(record.element);
					var args = _objectSpread$3(_objectSpread$3({}, payload), {}, { self: record });
					entry === null || entry === void 0 || (_entry$onDropTargetCh = entry.onDropTargetChange) === null || _entry$onDropTargetCh === void 0 || _entry$onDropTargetCh.call(entry, args);
					if (!isOver) {
						var _entry$onDragLeave;
						entry === null || entry === void 0 || (_entry$onDragLeave = entry.onDragLeave) === null || _entry$onDragLeave === void 0 || _entry$onDragLeave.call(entry, args);
					}
				}
			} catch (err) {
				_iterator2.e(err);
			} finally {
				_iterator2.f();
			}
			var _iterator3 = _createForOfIteratorHelper$2(payload.location.current.dropTargets), _step3;
			try {
				for (_iterator3.s(); !(_step3 = _iterator3.n()).done;) {
					var _entry$onDropTargetCh2, _entry$onDragEnter;
					var _record = _step3.value;
					if (visited.has(_record.element)) continue;
					var _args = _objectSpread$3(_objectSpread$3({}, payload), {}, { self: _record });
					var _entry = registry.get(_record.element);
					_entry === null || _entry === void 0 || (_entry$onDropTargetCh2 = _entry.onDropTargetChange) === null || _entry$onDropTargetCh2 === void 0 || _entry$onDropTargetCh2.call(_entry, _args);
					_entry === null || _entry === void 0 || (_entry$onDragEnter = _entry.onDragEnter) === null || _entry$onDragEnter === void 0 || _entry$onDragEnter.call(_entry, _args);
				}
			} catch (err) {
				_iterator3.e(err);
			} finally {
				_iterator3.f();
			}
		}
	};
	function dispatchEvent(args) {
		actions[args.eventName](args);
	}
	function getIsOver(_ref5) {
		var source = _ref5.source, target = _ref5.target, input = _ref5.input, current = _ref5.current;
		var actual = getActualDropTargets({
			source,
			target,
			input
		});
		if (actual.length >= current.length) return actual;
		var lastCaptureOrdered = copyReverse(current);
		var actualCaptureOrdered = copyReverse(actual);
		var resultCaptureOrdered = [];
		for (var index = 0; index < lastCaptureOrdered.length; index++) {
			var _argsForLast$getIsSti;
			var last = lastCaptureOrdered[index];
			var fresh = actualCaptureOrdered[index];
			if (fresh != null) {
				resultCaptureOrdered.push(fresh);
				continue;
			}
			var parent = resultCaptureOrdered[index - 1];
			var lastParent = lastCaptureOrdered[index - 1];
			if ((parent === null || parent === void 0 ? void 0 : parent.element) !== (lastParent === null || lastParent === void 0 ? void 0 : lastParent.element)) break;
			var argsForLast = registry.get(last.element);
			if (!argsForLast) break;
			var feedback = {
				input,
				source,
				element: argsForLast.element
			};
			if (argsForLast.canDrop && !argsForLast.canDrop(feedback)) break;
			if (!((_argsForLast$getIsSti = argsForLast.getIsSticky) !== null && _argsForLast$getIsSti !== void 0 && _argsForLast$getIsSti.call(argsForLast, feedback))) break;
			resultCaptureOrdered.push(_objectSpread$3(_objectSpread$3({}, last), {}, { isActiveDueToStickiness: true }));
		}
		return copyReverse(resultCaptureOrdered);
	}
	return {
		dropTargetForConsumers,
		getIsOver,
		dispatchEvent
	};
}
//#endregion
//#region node_modules/@atlaskit/pragmatic-drag-and-drop/dist/esm/make-adapter/make-monitor.js
function _createForOfIteratorHelper$1(r, e) {
	var t = "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"];
	if (!t) {
		if (Array.isArray(r) || (t = _unsupportedIterableToArray$1(r)) || e && r && "number" == typeof r.length) {
			t && (r = t);
			var _n = 0, F = function F() {};
			return {
				s: F,
				n: function n() {
					return _n >= r.length ? { done: !0 } : {
						done: !1,
						value: r[_n++]
					};
				},
				e: function e(r) {
					throw r;
				},
				f: F
			};
		}
		throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
	}
	var o, a = !0, u = !1;
	return {
		s: function s() {
			t = t.call(r);
		},
		n: function n() {
			var r = t.next();
			return a = r.done, r;
		},
		e: function e(r) {
			u = !0, o = r;
		},
		f: function f() {
			try {
				a || null == t.return || t.return();
			} finally {
				if (u) throw o;
			}
		}
	};
}
function _unsupportedIterableToArray$1(r, a) {
	if (r) {
		if ("string" == typeof r) return _arrayLikeToArray$1(r, a);
		var t = {}.toString.call(r).slice(8, -1);
		return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray$1(r, a) : void 0;
	}
}
function _arrayLikeToArray$1(r, a) {
	(null == a || a > r.length) && (a = r.length);
	for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e];
	return n;
}
function ownKeys$2(e, r) {
	var t = Object.keys(e);
	if (Object.getOwnPropertySymbols) {
		var o = Object.getOwnPropertySymbols(e);
		r && (o = o.filter(function(r) {
			return Object.getOwnPropertyDescriptor(e, r).enumerable;
		})), t.push.apply(t, o);
	}
	return t;
}
function _objectSpread$2(e) {
	for (var r = 1; r < arguments.length; r++) {
		var t = null != arguments[r] ? arguments[r] : {};
		r % 2 ? ownKeys$2(Object(t), !0).forEach(function(r) {
			_defineProperty(e, r, t[r]);
		}) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys$2(Object(t)).forEach(function(r) {
			Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r));
		});
	}
	return e;
}
function makeMonitor() {
	var registry = /* @__PURE__ */ new Set();
	var dragging = null;
	function tryAddToActive(monitor) {
		if (!dragging) return;
		if (!monitor.canMonitor || monitor.canMonitor(dragging.canMonitorArgs)) dragging.active.add(monitor);
	}
	function monitorForConsumers(args) {
		var entry = _objectSpread$2({}, args);
		registry.add(entry);
		tryAddToActive(entry);
		function cleanup() {
			registry.delete(entry);
			if (dragging) dragging.active.delete(entry);
		}
		return once(cleanup);
	}
	function dispatchEvent(_ref) {
		var eventName = _ref.eventName, payload = _ref.payload;
		if (eventName === "onGenerateDragPreview") {
			dragging = {
				canMonitorArgs: {
					initial: payload.location.initial,
					source: payload.source
				},
				active: /* @__PURE__ */ new Set()
			};
			var _iterator = _createForOfIteratorHelper$1(registry), _step;
			try {
				for (_iterator.s(); !(_step = _iterator.n()).done;) {
					var monitor = _step.value;
					tryAddToActive(monitor);
				}
			} catch (err) {
				_iterator.e(err);
			} finally {
				_iterator.f();
			}
		}
		if (!dragging) return;
		var active = Array.from(dragging.active);
		for (var _i = 0, _active = active; _i < _active.length; _i++) {
			var _monitor = _active[_i];
			if (dragging.active.has(_monitor)) {
				var _monitor$eventName;
				(_monitor$eventName = _monitor[eventName]) === null || _monitor$eventName === void 0 || _monitor$eventName.call(_monitor, payload);
			}
		}
		if (eventName === "onDrop") {
			dragging.active.clear();
			dragging = null;
		}
	}
	return {
		dispatchEvent,
		monitorForConsumers
	};
}
//#endregion
//#region node_modules/@atlaskit/pragmatic-drag-and-drop/dist/esm/make-adapter/make-adapter.js
function makeAdapter(_ref) {
	var typeKey = _ref.typeKey, mount = _ref.mount, dispatchEventToSource = _ref.dispatchEventToSource, onPostDispatch = _ref.onPostDispatch, defaultDropEffect = _ref.defaultDropEffect;
	var monitorAPI = makeMonitor();
	var dropTargetAPI = makeDropTarget({
		typeKey,
		defaultDropEffect
	});
	function dispatchEvent(args) {
		dispatchEventToSource === null || dispatchEventToSource === void 0 || dispatchEventToSource(args);
		dropTargetAPI.dispatchEvent(args);
		monitorAPI.dispatchEvent(args);
		onPostDispatch === null || onPostDispatch === void 0 || onPostDispatch(args);
	}
	function start(_ref2) {
		var event = _ref2.event, dragType = _ref2.dragType;
		lifecycle.start({
			event,
			dragType,
			getDropTargetsOver: dropTargetAPI.getIsOver,
			dispatchEvent
		});
	}
	function registerUsage() {
		function mountAdapter() {
			return mount({
				canStart: lifecycle.canStart,
				start
			});
		}
		return register({
			typeKey,
			mount: mountAdapter
		});
	}
	return {
		registerUsage,
		dropTarget: dropTargetAPI.dropTargetForConsumers,
		monitor: monitorAPI.monitorForConsumers
	};
}
//#endregion
//#region node_modules/@atlaskit/pragmatic-drag-and-drop/dist/esm/util/android.js
var isAndroid = once(function isAndroid() {
	return navigator.userAgent.toLocaleLowerCase().includes("android");
});
var androidFallbackText = "pdnd:android-fallback";
//#endregion
//#region node_modules/@atlaskit/pragmatic-drag-and-drop/dist/esm/util/media-types/text-media-type.js
var textMediaType = "text/plain";
//#endregion
//#region node_modules/@atlaskit/pragmatic-drag-and-drop/dist/esm/adapter/element-adapter-native-data-key.js
/**
* This key has been pulled into a separate module
* so that the external adapter does not need to import
* the element adapter
*/
var elementAdapterNativeDataKey = "application/vnd.pdnd";
//#endregion
//#region node_modules/@atlaskit/pragmatic-drag-and-drop/dist/esm/adapter/element-adapter.js
var draggableRegistry = /* @__PURE__ */ new WeakMap();
function addToRegistry(args) {
	draggableRegistry.set(args.element, args);
	return function cleanup() {
		draggableRegistry.delete(args.element);
	};
}
var honeyPotFix = makeHoneyPotFix();
var adapter = makeAdapter({
	typeKey: "element",
	defaultDropEffect: "move",
	mount: function mount(api) {
		/**  Binding event listeners the `document` rather than `window` so that
		* this adapter always gets preference over the text adapter.
		* `document` is the first `EventTarget` under `window`
		* https://twitter.com/alexandereardon/status/1604658588311465985
		*/
		return combine(honeyPotFix.bindEvents(), (0, import_dist.bind)(document, {
			type: "dragstart",
			listener: function listener(event) {
				var _entry$dragHandle, _entry$getInitialData, _entry$getInitialData2, _entry$dragHandle2, _entry$getInitialData3, _entry$getInitialData4;
				if (!api.canStart(event)) return;
				if (event.defaultPrevented) return;
				if (!event.dataTransfer) {
					console.warn("\n              It appears as though you have are not testing DragEvents correctly.\n\n              - If you are unit testing, ensure you have polyfilled DragEvent.\n              - If you are browser testing, ensure you are dispatching drag events correctly.\n\n              Please see our testing guides for more information:\n              https://atlassian.design/components/pragmatic-drag-and-drop/core-package/testing\n            ".replace(/ {2}/g, ""));
					return;
				}
				var target = event.target;
				if (!(target instanceof HTMLElement)) return;
				var entry = draggableRegistry.get(target);
				if (!entry) return;
				/**
				* A text selection drag _can_ have the `draggable` element be
				* the `event.target` if the user is dragging the text selection
				* from the `draggable`.
				*
				* To know if the `draggable` is being dragged, we look at whether any
				* `"text/plain"` data is being dragged. If it is, then a text selection
				* drag is occurring.
				*
				* This behaviour has been validated on:
				*
				* - Chrome@128 on Android@14
				* - Chrome@128 on iOS@17.6.1
				* - Chrome@128 on Windows@11
				* - Chrome@128 on MacOS@14.6.1
				* - Firefox@129 on Windows@11 (not possible for user to select text in a draggable)
				* - Firefox@129 on MacOS@14.6.1 (not possible for user to select text in a draggable)
				*
				* Note: Could usually just use: `event.dataTransfer.types.includes(textMediaType)`
				* but unfortunately ProseMirror is always setting `""` as the dragged text
				*
				* Note: Unfortunately editor is (heavily) leaning on the current functionality today
				* and unwinding it will be a decent amount of effort. So for now, a text selection
				* where the `event.target` is a `draggable` element will still trigger the
				* element adapter.
				*
				* // Future state:
				* if(event.dataTransfer.getData(textMediaType)) {
				* 	return;
				* }
				*
				*/
				var input = getInput(event);
				var feedback = {
					element: entry.element,
					dragHandle: (_entry$dragHandle = entry.dragHandle) !== null && _entry$dragHandle !== void 0 ? _entry$dragHandle : null,
					input
				};
				if (entry.canDrag && !entry.canDrag(feedback)) {
					event.preventDefault();
					return;
				}
				if (entry.dragHandle) {
					var over = getElementFromPointWithoutHoneypot({
						x: input.clientX,
						y: input.clientY
					});
					if (!entry.dragHandle.contains(over)) {
						event.preventDefault();
						return;
					}
				}
				/**
				*  **Goal**
				*  Pass information to other applications
				*
				* **Approach**
				*  Put data into the native data store
				*
				*  **What about the native adapter?**
				*  When the element adapter puts native data into the native data store
				*  the native adapter is not triggered in the current window,
				*  but a native adapter in an external window _can_ be triggered
				*
				*  **Why bake this into core?**
				*  This functionality could be pulled out and exposed inside of
				*  `onGenerateDragPreview`. But decided to make it a part of the
				*  base API as it felt like a common enough use case and ended
				*  up being a similar amount of code to include this function as
				*  it was to expose the hook for it
				*/
				var nativeData = (_entry$getInitialData = (_entry$getInitialData2 = entry.getInitialDataForExternal) === null || _entry$getInitialData2 === void 0 ? void 0 : _entry$getInitialData2.call(entry, feedback)) !== null && _entry$getInitialData !== void 0 ? _entry$getInitialData : null;
				if (nativeData) for (var _i = 0, _Object$entries = Object.entries(nativeData); _i < _Object$entries.length; _i++) {
					var _Object$entries$_i = _slicedToArray(_Object$entries[_i], 2), key = _Object$entries$_i[0], data = _Object$entries$_i[1];
					event.dataTransfer.setData(key, data !== null && data !== void 0 ? data : "");
				}
				/**
				*  📱 For Android devices, a drag operation will not start unless
				* "text/plain" or "text/uri-list" data exists in the native data store
				* https://twitter.com/alexandereardon/status/1732189803754713424
				*
				* Tested on:
				* Device: Google Pixel 5
				* Android version: 14 (November 5, 2023)
				* Chrome version: 120.0
				*/
				if (isAndroid() && !event.dataTransfer.types.includes("text/plain") && !event.dataTransfer.types.includes("text/uri-list")) event.dataTransfer.setData(textMediaType, androidFallbackText);
				/**
				* 1. Must set any media type for `iOS15` to work
				* 2. We are also doing adding data so that the native adapter
				* can know that the element adapter has handled this drag
				*
				* We used to wrap this `setData()` in a `try/catch` for Firefox,
				* but it looks like that was not needed.
				*
				* Tested using: https://codesandbox.io/s/checking-firefox-throw-behaviour-on-dragstart-qt8h4f
				*
				* - ✅ Firefox@70.0 (Oct 2019) on macOS Sonoma
				* - ✅ Firefox@70.0 (Oct 2019) on macOS Big Sur
				* - ✅ Firefox@70.0 (Oct 2019) on Windows 10
				*
				* // just checking a few more combinations to be super safe
				*
				* - ✅ Chrome@78 (Oct 2019) on macOS Big Sur
				* - ✅ Chrome@78 (Oct 2019) on Windows 10
				* - ✅ Safari@14.1 on macOS Big Sur
				*/
				event.dataTransfer.setData(elementAdapterNativeDataKey, "");
				var dragType = {
					type: "element",
					payload: {
						element: entry.element,
						dragHandle: (_entry$dragHandle2 = entry.dragHandle) !== null && _entry$dragHandle2 !== void 0 ? _entry$dragHandle2 : null,
						data: (_entry$getInitialData3 = (_entry$getInitialData4 = entry.getInitialData) === null || _entry$getInitialData4 === void 0 ? void 0 : _entry$getInitialData4.call(entry, feedback)) !== null && _entry$getInitialData3 !== void 0 ? _entry$getInitialData3 : {}
					},
					startedFrom: "internal"
				};
				api.start({
					event,
					dragType
				});
			}
		}));
	},
	dispatchEventToSource: function dispatchEventToSource(_ref) {
		var _draggableRegistry$ge, _draggableRegistry$ge2;
		var eventName = _ref.eventName, payload = _ref.payload;
		(_draggableRegistry$ge = draggableRegistry.get(payload.source.element)) === null || _draggableRegistry$ge === void 0 || (_draggableRegistry$ge2 = _draggableRegistry$ge[eventName]) === null || _draggableRegistry$ge2 === void 0 || _draggableRegistry$ge2.call(_draggableRegistry$ge, payload);
	},
	onPostDispatch: honeyPotFix.getOnPostDispatch()
});
var dropTargetForElements = adapter.dropTarget;
var monitorForElements = adapter.monitor;
function draggable(args) {
	if (args.dragHandle && !args.element.contains(args.dragHandle)) console.warn("Drag handle element must be contained in draggable element", {
		element: args.element,
		dragHandle: args.dragHandle
	});
	var existing = draggableRegistry.get(args.element);
	if (existing) console.warn("You have already registered a `draggable` on the same element", {
		existing,
		proposed: args
	});
	return once(combine(adapter.registerUsage(), addToRegistry(args), addAttribute(args.element, {
		attribute: "draggable",
		value: "true"
	})));
}
/** Common event payload for all events */
/** A map containing payloads for all events */
/** Common event payload for all drop target events */
/** A map containing payloads for all events on drop targets */
/** Arguments given to all feedback functions (eg `canDrag()`) on for a `draggable()` */
/** Arguments given to all feedback functions (eg `canDrop()`) on a `dropTargetForElements()` */
/** Arguments given to all monitor feedback functions (eg `canMonitor()`) for a `monitorForElements` */
//#endregion
//#region node_modules/@mui/x-scheduler-internals/internals/utils/useDropTarget.mjs
function useDropTarget(parameters) {
	const { surfaceType, ref, resourceId = null, getEventDropData, isValidDropTarget, addPropertiesToDroppedEvent } = parameters;
	const adapter = useAdapterContext();
	const store = useSchedulerStoreContext();
	import_react.useEffect(() => {
		if (!ref.current) return;
		const getDataFromInside = (data, newStart, newEnd) => {
			return {
				type: data.source === "CalendarGridDayEventResizeHandler" || data.source === "CalendarGridTimeEventResizeHandler" ? "internal-resize" : "internal-drag",
				surfaceType,
				start: newStart,
				end: newEnd,
				eventId: data.eventId,
				occurrenceKey: data.occurrenceKey,
				originalOccurrence: data.originalOccurrence,
				resourceId: resourceId === void 0 ? getPrimaryResourceId(data.originalOccurrence.resource) ?? null : resourceId
			};
		};
		const getDataFromOutside = (data, start) => {
			const eventCreationConfig = schedulerEventSelectors.creationConfig(store.state);
			if (eventCreationConfig === false) return;
			return {
				type: "external-drag",
				surfaceType,
				start,
				end: adapter.addMinutes(start, data.eventData.duration ?? eventCreationConfig.duration),
				eventData: data.eventData,
				onEventDrop: data.onEventDrop,
				resourceId: resourceId === void 0 ? getPrimaryResourceId(data.eventData.resource) ?? null : resourceId
			};
		};
		return dropTargetForElements({
			element: ref.current,
			getData: () => ({
				isSchedulerDropTarget: true,
				surfaceType
			}),
			canDrop: ({ source }) => {
				if (!isValidDropTarget(source.data)) return false;
				if (source.data.source === "StandaloneEvent" && !schedulerEventSelectors.canDragEventsFromTheOutside(store.state)) return false;
				return true;
			},
			onDrag: ({ source, location }) => {
				const newPlaceholder = getEventDropData({
					data: source.data,
					getDataFromInside,
					getDataFromOutside,
					input: location.current.input
				});
				if (newPlaceholder) store.setOccurrencePlaceholder(newPlaceholder);
			},
			onDrop: ({ source, location }) => {
				const placeholder = getEventDropData({
					data: source.data,
					getDataFromInside,
					getDataFromOutside,
					input: location.current.input
				}) ?? schedulerOccurrencePlaceholderSelectors.value(store.state);
				if (isInternalDragOrResizePlaceholder(placeholder)) applyInternalDragOrResizeOccurrencePlaceholder(store, placeholder, addPropertiesToDroppedEvent);
				else if (placeholder?.type === "external-drag") applyExternalDragOccurrencePlaceholder(store, placeholder, addPropertiesToDroppedEvent);
			},
			onDragLeave: () => {
				const currentPlaceholder = schedulerOccurrencePlaceholderSelectors.value(store.state);
				if (currentPlaceholder?.surfaceType !== surfaceType) return;
				if (currentPlaceholder.type === "external-drag" || isInternalDragOrResizePlaceholder(currentPlaceholder) && schedulerEventSelectors.canDropEventsToTheOutside(store.state)) store.setOccurrencePlaceholder(_extends({}, currentPlaceholder, { isHidden: true }));
			}
		});
	}, [
		ref,
		surfaceType,
		resourceId,
		getEventDropData,
		isValidDropTarget,
		addPropertiesToDroppedEvent,
		adapter,
		store
	]);
}
/**
* Applies the data from the placeholder occurrence to the event it represents.
*/
function applyInternalDragOrResizeOccurrencePlaceholder(store, placeholder, addPropertiesToDroppedEvent) {
	store.setOccurrencePlaceholder(null);
	const { eventId, start, end, originalOccurrence } = placeholder;
	const adapter = store.state.adapter;
	const changes = {
		id: eventId,
		start,
		end
	};
	if (placeholder.resourceId !== null) changes.resource = placeholder.resourceId;
	const additionalChanges = addPropertiesToDroppedEvent?.() ?? {};
	Object.assign(changes, additionalChanges);
	if (!Object.entries(changes).some(([key, value]) => {
		if (key === "id") return false;
		if (key === "start" || key === "end") return !adapter.isEqual(originalOccurrence.displayTimezone[key].value, value);
		return originalOccurrence[key] !== value;
	})) return;
	if (originalOccurrence.displayTimezone.rrule) {
		store.updateRecurringEvent({
			occurrenceStart: originalOccurrence.displayTimezone.start.value,
			changes
		});
		return;
	}
	store.updateEvent(changes);
}
function applyExternalDragOccurrencePlaceholder(store, placeholder, addPropertiesToDroppedEvent) {
	const event = _extends({
		start: placeholder.start,
		end: placeholder.end
	}, placeholder.eventData);
	if (placeholder.resourceId !== null) event.resource = placeholder.resourceId;
	if (addPropertiesToDroppedEvent) Object.assign(event, addPropertiesToDroppedEvent());
	store.setOccurrencePlaceholder(null);
	store.createEvent(event);
	placeholder.onEventDrop?.();
}
//#endregion
//#region node_modules/@atlaskit/pragmatic-drag-and-drop/dist/esm/public-utils/element/disable-native-drag-preview.js
var tinyTransparentImage = function() {
	if (typeof window === "undefined") return null;
	var img = new Image();
	img.src = "data:image/gif;base64,R0lGODlhAQABAIAAAP///wAAACH5BAEAAAAALAAAAAABAAEAAAICRAEAOw==";
	return img;
}();
function disableNativeDragPreview(_ref) {
	var nativeSetDragImage = _ref.nativeSetDragImage;
	if (nativeSetDragImage && tinyTransparentImage) nativeSetDragImage(tinyTransparentImage, 0, 0);
}
//#endregion
//#region node_modules/@mui/x-scheduler-internals/internals/utils/useEventResizeHandler.mjs
function useEventResizeHandler(parameters) {
	const { ref, side, getDragData, contextValue: { doesEventStartBeforeCollectionStart, doesEventEndAfterCollectionEnd } } = parameters;
	const enabled = side === "start" && !doesEventStartBeforeCollectionStart || side === "end" && !doesEventEndAfterCollectionEnd;
	const state = import_react.useMemo(() => ({
		start: side === "start",
		end: side === "end"
	}), [side]);
	import_react.useEffect(() => {
		if (!ref.current || !enabled) return;
		return draggable({
			element: ref.current,
			getInitialData: ({ input }) => getDragData(input),
			onGenerateDragPreview: ({ nativeSetDragImage }) => {
				disableNativeDragPreview({ nativeSetDragImage });
			}
		});
	}, [
		ref,
		enabled,
		side,
		getDragData
	]);
	return {
		state,
		enabled
	};
}
//#endregion
//#region node_modules/@mui/x-scheduler-internals/internals/utils/useElementPositionInCollection.mjs
var FIXED_24H_GRID_MINUTES = 1440;
function useElementPositionInCollection(parameters) {
	const { start, end, collectionStart, collectionEnd, dayStartMinute = 0, dayEndMinute = FIXED_24H_GRID_MINUTES } = parameters;
	const adapter = useAdapterContext();
	return import_react.useMemo(() => computeElementPositionInCollection(adapter, {
		start,
		end,
		collectionStart,
		collectionEnd,
		dayStartMinute,
		dayEndMinute
	}), [
		adapter,
		start,
		end,
		collectionStart,
		collectionEnd,
		dayStartMinute,
		dayEndMinute
	]);
}
/**
* Pure helper behind `useElementPositionInCollection`, callable outside React.
*/
function computeElementPositionInCollection(adapter, parameters) {
	const { start, end, collectionStart, collectionEnd, dayStartMinute = 0, dayEndMinute = FIXED_24H_GRID_MINUTES } = parameters;
	const dayMinutes = Math.max(1, dayEndMinute - dayStartMinute);
	const startDayIndex = adapter.differenceInDays(adapter.startOfDay(start.value), adapter.startOfDay(collectionStart));
	const endDayIndex = adapter.differenceInDays(adapter.startOfDay(end.value), adapter.startOfDay(collectionStart));
	const startIndexMinutes = startDayIndex * dayMinutes + (start.minutesInDay - dayStartMinute);
	let endIndexMinutes = endDayIndex * dayMinutes + (end.minutesInDay - dayStartMinute);
	if (endIndexMinutes < startIndexMinutes) endIndexMinutes += dayMinutes;
	const totalDays = adapter.differenceInDays(adapter.startOfDay(collectionEnd), adapter.startOfDay(collectionStart)) + 1;
	const totalMinutes = Math.max(1, totalDays * dayMinutes);
	const clampToTimeline = (value) => Math.min(Math.max(value, 0), totalMinutes);
	const clampedStartMinutes = clampToTimeline(startIndexMinutes);
	const clampedEndMinutes = clampToTimeline(endIndexMinutes);
	const startingBeforeEdge = startIndexMinutes < 0;
	const endingAfterEdge = endIndexMinutes > totalMinutes;
	return {
		position: clampedStartMinutes / totalMinutes,
		duration: Math.max(0, clampedEndMinutes - clampedStartMinutes) / totalMinutes,
		startingBeforeEdge,
		endingAfterEdge
	};
}
//#endregion
//#region node_modules/@mui/x-scheduler-internals/internals/utils/useEvent.mjs
function useEvent(parameters) {
	const { start, end, occurrenceKey } = parameters;
	const store = useSchedulerStoreContext();
	return { state: {
		started: useStore(store, schedulerOccurrenceSelectors.isStarted, start),
		ended: useStore(store, schedulerOccurrenceSelectors.isEnded, end),
		editing: useStore(store, schedulerOtherSelectors.isEditedOccurrence, occurrenceKey)
	} };
}
//#endregion
//#region node_modules/@mui/x-scheduler-internals/internals/utils/useDragPreview.mjs
/**
* Returns the drag preview to render when the dragged event is not over a valid drop target.
*/
function useDragPreview(parameters) {
	const { renderDragPreview, showPreviewOnDragStart, data, type } = parameters;
	const store = useSchedulerStoreContext(true);
	const [state, setState] = import_react.useState({
		isDragging: false,
		dragPosition: null
	});
	return {
		element: import_react.useMemo(() => {
			if (state.dragPosition == null) return null;
			return /*#__PURE__*/ (0, import_jsx_runtime.jsx)("div", {
				style: {
					position: "fixed",
					top: state.dragPosition.clientY,
					left: state.dragPosition.clientX,
					pointerEvents: "none",
					zIndex: 9999
				},
				children: renderDragPreview({
					data,
					type
				})
			});
		}, [
			state.dragPosition,
			renderDragPreview,
			data,
			type
		]),
		state,
		actions: import_react.useMemo(() => ({
			onDragStart: (location) => {
				setState({
					isDragging: true,
					dragPosition: showPreviewOnDragStart ? location.current.input : null
				});
			},
			onDrag: (location) => {
				let shouldShowPreview = true;
				if (store && type === "internal-event" && !schedulerEventSelectors.canDropEventsToTheOutside(store.state)) shouldShowPreview = false;
				else if (location.current.dropTargets.some((el) => el.data.isSchedulerDropTarget)) shouldShowPreview = false;
				setState({
					isDragging: true,
					dragPosition: shouldShowPreview ? location.current.input : null
				});
			},
			onDrop: () => {
				setState({
					isDragging: false,
					dragPosition: null
				});
			}
		}), [
			store,
			showPreviewOnDragStart,
			type
		])
	};
}
//#endregion
//#region node_modules/@mui/x-scheduler-internals/internals/utils/useDraggableEvent.mjs
function useDraggableEvent(parameters) {
	const { ref, start, end, occurrenceKey, eventId, renderDragPreview, getDragData, collectionStart, collectionEnd, isDraggable = false } = parameters;
	const adapter = useAdapterContext();
	const store = useSchedulerStoreContext();
	const placeholderAction = useStore(store, schedulerOccurrencePlaceholderSelectors.actionForOccurrence, occurrenceKey);
	const event = useStore(store, schedulerEventSelectors.processedEvent, eventId);
	const { state: eventState } = useEvent({
		start,
		end,
		occurrenceKey
	});
	const preview = useDragPreview({
		type: "internal-event",
		data: event,
		renderDragPreview,
		showPreviewOnDragStart: false
	});
	const state = _extends({}, eventState, {
		dragging: placeholderAction === "internal-drag",
		resizing: placeholderAction === "internal-resize"
	});
	import_react.useEffect(() => {
		if (!isDraggable || !ref.current) return;
		return draggable({
			element: ref.current,
			getInitialData: ({ input }) => getDragData(input),
			onGenerateDragPreview: ({ nativeSetDragImage }) => {
				disableNativeDragPreview({ nativeSetDragImage });
			},
			onDragStart: ({ location }) => {
				preview.actions.onDragStart(location);
			},
			onDrag: ({ location }) => {
				preview.actions.onDrag(location);
			},
			onDrop: () => {
				store.setOccurrencePlaceholder(null);
				preview.actions.onDrop();
			}
		});
	}, [
		ref,
		getDragData,
		isDraggable,
		store,
		preview.actions
	]);
	return {
		state,
		preview,
		contextValue: import_react.useMemo(() => ({
			doesEventStartBeforeCollectionStart: adapter.isBefore(start.value, collectionStart),
			doesEventEndAfterCollectionEnd: adapter.isAfter(end.value, collectionEnd)
		}), [
			adapter,
			start,
			end,
			collectionStart,
			collectionEnd
		])
	};
}
//#endregion
//#region node_modules/@atlaskit/pragmatic-drag-and-drop-auto-scroll/dist/esm/shared/engagement-history.js
var ledger = /* @__PURE__ */ new Map();
var requested = /* @__PURE__ */ new Set();
function markAndGetEngagement(element) {
	markEngagement(element);
	var entry = ledger.get(element);
	if (entry) return entry;
	var fresh = { timeOfEngagementStart: Date.now() };
	ledger.set(element, fresh);
	return fresh;
}
function markEngagement(element) {
	requested.add(element);
}
function clearUnusedEngagements(fn) {
	requested.clear();
	fn();
	ledger.forEach(function(_, element) {
		if (!requested.has(element)) ledger.delete(element);
	});
	requested.clear();
}
function clearEngagementHistory() {
	ledger.clear();
}
//#endregion
//#region node_modules/@atlaskit/pragmatic-drag-and-drop-auto-scroll/dist/esm/shared/scheduler.js
var schedulers = /* @__PURE__ */ new Map();
function getScheduler(monitor) {
	var scheduler = schedulers.get(monitor);
	if (scheduler) return scheduler;
	var created = makeScheduler(monitor);
	schedulers.set(monitor, created);
	return created;
}
function makeScheduler(monitor) {
	var state = { type: "idle" };
	var callbacks = [];
	function loop(timeLastFrameFinished) {
		if (state.type !== "running") return;
		var timeSinceLastFrame = timeLastFrameFinished - state.timeLastFrameFinished;
		var latestArgs = state.latestArgs;
		var underUsersPointer = getElementFromPointWithoutHoneypot({
			x: latestArgs.location.current.input.clientX,
			y: latestArgs.location.current.input.clientY
		});
		clearUnusedEngagements(function() {
			callbacks.forEach(function(onFrame) {
				return onFrame({
					underUsersPointer,
					latestArgs,
					timeSinceLastFrame
				});
			});
		});
		state.timeLastFrameFinished = timeLastFrameFinished;
		state.frameId = requestAnimationFrame(loop);
	}
	function reset() {
		if (state.type === "idle") return;
		cancelAnimationFrame(state.frameId);
		clearEngagementHistory();
		state = { type: "idle" };
	}
	function start(args) {
		if (state.type !== "idle") return;
		state = {
			type: "initializing",
			latestArgs: args,
			frameId: requestAnimationFrame(function(timeLastFrameFinished) {
				if (state.type !== "initializing") return;
				state = {
					type: "running",
					timeLastFrameFinished,
					latestArgs: state.latestArgs,
					frameId: requestAnimationFrame(loop)
				};
			})
		};
	}
	function update(args) {
		if (state.type === "idle") {
			start(args);
			return;
		}
		state.latestArgs = args;
	}
	monitor({
		onDragStart: start,
		onDropTargetChange: update,
		onDrag: update,
		onDrop: reset
	});
	return { onFrame: function onFrame(fn) {
		callbacks.push(fn);
	} };
}
//#endregion
//#region node_modules/@atlaskit/pragmatic-drag-and-drop-auto-scroll/dist/esm/over-element/data-attributes.js
var dataAttribute = "data-auto-scrollable";
var selector = "[".concat(dataAttribute, "=\"true\"]");
function addScrollableAttribute(element) {
	element.setAttribute(dataAttribute, "true");
	return function() {
		return element.removeAttribute(dataAttribute);
	};
}
//#endregion
//#region node_modules/@atlaskit/pragmatic-drag-and-drop-auto-scroll/dist/esm/shared/configuration.js
function ownKeys$1(e, r) {
	var t = Object.keys(e);
	if (Object.getOwnPropertySymbols) {
		var o = Object.getOwnPropertySymbols(e);
		r && (o = o.filter(function(r) {
			return Object.getOwnPropertyDescriptor(e, r).enumerable;
		})), t.push.apply(t, o);
	}
	return t;
}
function _objectSpread$1(e) {
	for (var r = 1; r < arguments.length; r++) {
		var t = null != arguments[r] ? arguments[r] : {};
		r % 2 ? ownKeys$1(Object(t), !0).forEach(function(r) {
			_defineProperty(e, r, t[r]);
		}) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys$1(Object(t)).forEach(function(r) {
			Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r));
		});
	}
	return e;
}
var baseConfig = {
	startHitboxAtPercentageRemainingOfElement: {
		top: .25,
		right: .25,
		bottom: .25,
		left: .25
	},
	maxScrollAtPercentageRemainingOfHitbox: {
		top: .5,
		right: .5,
		bottom: .5,
		left: .5
	},
	timeDampeningDurationMs: 400,
	maxMainAxisHitboxSize: 180
};
/** What the max scroll should be per second. Using "per second" rather than "per frame"
* as we want a consistent scroll speed regardless of frame rate.
*
*
* I explored trying to make the max scroll speed dynamic based on particular factors.
* However, it ended up being difficult to find a _perfect_ formula.
*
* Likely the perfect answer would involve:
* - the size of the scrollable element
* - the size of the scrollable element relative to the screen size
* - the size of the drag preview
* - the size of elements being scrolled in scrollable elements (expensive and difficult to compute)
*/
var maxPixelScrollPerSecond = {
	standard: 900,
	fast: 1500
};
function getInternalConfig(provided) {
	var _provided$maxScrollSp;
	return _objectSpread$1(_objectSpread$1({}, baseConfig), {}, { maxPixelScrollPerSecond: maxPixelScrollPerSecond[(_provided$maxScrollSp = provided === null || provided === void 0 ? void 0 : provided.maxScrollSpeed) !== null && _provided$maxScrollSp !== void 0 ? _provided$maxScrollSp : "standard"] });
}
//#endregion
//#region node_modules/@atlaskit/pragmatic-drag-and-drop-auto-scroll/dist/esm/shared/can-scroll-on-edge.js
var canScrollOnEdge = {
	top: function top(element) {
		return element.scrollTop > 0;
	},
	right: function right(element) {
		return Math.ceil(element.scrollLeft) + element.clientWidth < element.scrollWidth;
	},
	bottom: function bottom(element) {
		return Math.ceil(element.scrollTop) + element.clientHeight < element.scrollHeight;
	},
	left: function left(element) {
		return element.scrollLeft > 0;
	}
};
//#endregion
//#region node_modules/@atlaskit/pragmatic-drag-and-drop-auto-scroll/dist/esm/shared/edges.js
var edges = [
	"top",
	"right",
	"bottom",
	"left"
];
var edgeAxisLookup = {
	top: "vertical",
	right: "horizontal",
	bottom: "vertical",
	left: "horizontal"
};
//#endregion
//#region node_modules/@atlaskit/pragmatic-drag-and-drop-auto-scroll/dist/esm/shared/axis.js
var vertical = {
	start: "top",
	end: "bottom",
	point: "y",
	size: "height"
};
var horizontal = {
	start: "left",
	end: "right",
	point: "x",
	size: "width"
};
var axisLookup = {
	vertical: {
		mainAxis: vertical,
		crossAxis: horizontal
	},
	horizontal: {
		mainAxis: horizontal,
		crossAxis: vertical
	}
};
//#endregion
//#region node_modules/@atlaskit/pragmatic-drag-and-drop-auto-scroll/dist/esm/shared/side.js
var mainAxisSideLookup = {
	top: "start",
	right: "end",
	bottom: "end",
	left: "start"
};
//#endregion
//#region node_modules/@atlaskit/pragmatic-drag-and-drop-auto-scroll/dist/esm/shared/get-over-element-hitbox.js
function makeGetHitbox(_ref) {
	var edge = _ref.edge, axis = _ref.axis;
	return function hitbox(_ref2) {
		var clientRect = _ref2.clientRect, config = _ref2.config;
		var _axisLookup$axis = axisLookup[axis], mainAxis = _axisLookup$axis.mainAxis, crossAxis = _axisLookup$axis.crossAxis;
		var side = mainAxisSideLookup[edge];
		var mainAxisHitboxSize = Math.min(config.startHitboxAtPercentageRemainingOfElement[edge] * clientRect[mainAxis.size], config.maxMainAxisHitboxSize);
		return DOMRect.fromRect(_defineProperty(_defineProperty(_defineProperty(_defineProperty({}, mainAxis.point, side === "start" ? clientRect[mainAxis.point] : clientRect[mainAxis.point] + clientRect[mainAxis.size] - mainAxisHitboxSize), crossAxis.point, clientRect[crossAxis.point]), mainAxis.size, mainAxisHitboxSize), crossAxis.size, clientRect[crossAxis.size]));
	};
}
var getOverElementHitbox = {
	top: makeGetHitbox({
		axis: "vertical",
		edge: "top"
	}),
	right: makeGetHitbox({
		axis: "horizontal",
		edge: "right"
	}),
	bottom: makeGetHitbox({
		axis: "vertical",
		edge: "bottom"
	}),
	left: makeGetHitbox({
		axis: "horizontal",
		edge: "left"
	})
};
//#endregion
//#region node_modules/@atlaskit/pragmatic-drag-and-drop-auto-scroll/dist/esm/shared/get-percentage-in-range.js
function getPercentageInRange(_ref) {
	var startOfRange = _ref.startOfRange, endOfRange = _ref.endOfRange, value = _ref.value;
	if (!(startOfRange < endOfRange)) return 0;
	if (value < startOfRange) return 0;
	if (value > endOfRange) return 1;
	var range = endOfRange - startOfRange;
	return (value - startOfRange) / range;
}
//#endregion
//#region node_modules/@atlaskit/pragmatic-drag-and-drop-auto-scroll/dist/esm/shared/get-scroll-change.js
function getMaxScrollChange(_ref) {
	var timeSinceLastFrame = _ref.timeSinceLastFrame, config = _ref.config;
	var targetScrollPerMs = config.maxPixelScrollPerSecond / 1e3;
	var proposed = Math.ceil(targetScrollPerMs * timeSinceLastFrame);
	var maximum = config.maxPixelScrollPerSecond / 60;
	return Math.min(proposed, maximum);
}
function getDistanceDampening(_ref2) {
	var client = _ref2.client, axis = _ref2.axis, edge = _ref2.edge, hitbox = _ref2.hitbox, config = _ref2.config;
	var mainAxis = axisLookup[axis].mainAxis;
	var side = mainAxisSideLookup[edge];
	var maxSpeedBuffer = hitbox[mainAxis.size] * config.maxScrollAtPercentageRemainingOfHitbox[edge];
	if (side === "end") return getPercentageInRange({
		startOfRange: hitbox[mainAxis.start],
		endOfRange: hitbox[mainAxis.end] - maxSpeedBuffer,
		value: client[mainAxis.point]
	});
	return 1 - getPercentageInRange({
		startOfRange: hitbox[mainAxis.start] + maxSpeedBuffer,
		endOfRange: hitbox[mainAxis.end],
		value: client[mainAxis.point]
	});
}
function getScrollChange(_ref3) {
	var client = _ref3.client, timeSinceLastFrame = _ref3.timeSinceLastFrame, engagement = _ref3.engagement, axis = _ref3.axis, hitbox = _ref3.hitbox, edge = _ref3.edge, isDistanceDampeningEnabled = _ref3.isDistanceDampeningEnabled, config = _ref3.config;
	var maxScroll = getMaxScrollChange({
		timeSinceLastFrame,
		config
	});
	var percentageOfMaxScroll = (isDistanceDampeningEnabled ? getDistanceDampening({
		client,
		edge,
		hitbox,
		axis,
		config
	}) : 1) * getPercentageInRange({
		startOfRange: engagement.timeOfEngagementStart,
		endOfRange: engagement.timeOfEngagementStart + config.timeDampeningDurationMs,
		value: Date.now()
	});
	var scroll = Math.max(maxScroll * percentageOfMaxScroll, 1);
	return mainAxisSideLookup[edge] === "end" ? scroll : -1 * scroll;
}
//#endregion
//#region node_modules/@atlaskit/pragmatic-drag-and-drop-auto-scroll/dist/esm/shared/is-axis-allowed.js
function isAxisAllowed(axis, allowedAxis) {
	return allowedAxis === "all" || axis === allowedAxis;
}
//#endregion
//#region node_modules/@atlaskit/pragmatic-drag-and-drop-auto-scroll/dist/esm/shared/is-within.js
function isWithin(_ref) {
	var client = _ref.client, clientRect = _ref.clientRect;
	return client.x >= clientRect.x && client.x <= clientRect.x + clientRect.width && client.y >= clientRect.y && client.y <= clientRect.y + clientRect.height;
}
//#endregion
//#region node_modules/@atlaskit/pragmatic-drag-and-drop-auto-scroll/dist/esm/over-element/get-scroll-by.js
function getRectDefault(element) {
	return element.getBoundingClientRect();
}
function getScrollBy(_ref) {
	var element = _ref.element, input = _ref.input, timeSinceLastFrame = _ref.timeSinceLastFrame, engagement = _ref.engagement, config = _ref.config, allowedAxis = _ref.allowedAxis, _ref$getRect = _ref.getRect, getRect = _ref$getRect === void 0 ? getRectDefault : _ref$getRect;
	var client = {
		x: input.clientX,
		y: input.clientY
	};
	var clientRect = getRect(element);
	var scrollableEdges = edges.reduce(function(map, edge) {
		var hitbox = getOverElementHitbox[edge]({
			clientRect,
			config
		});
		var axis = edgeAxisLookup[edge];
		if (!isAxisAllowed(axis, allowedAxis)) return map;
		if (!isWithin({
			client,
			clientRect: hitbox
		})) return map;
		if (!canScrollOnEdge[edge](element)) return map;
		map.set(edge, {
			edge,
			hitbox
		});
		return map;
	}, /* @__PURE__ */ new Map());
	return {
		left: function() {
			var axis = "horizontal";
			var leftEdge = scrollableEdges.get("left");
			if (leftEdge) return getScrollChange({
				client,
				edge: leftEdge.edge,
				hitbox: leftEdge.hitbox,
				axis,
				timeSinceLastFrame,
				engagement,
				isDistanceDampeningEnabled: true,
				config
			});
			var rightEdge = scrollableEdges.get("right");
			if (rightEdge) return getScrollChange({
				client,
				edge: rightEdge.edge,
				hitbox: rightEdge.hitbox,
				axis,
				timeSinceLastFrame,
				engagement,
				isDistanceDampeningEnabled: true,
				config
			});
			return 0;
		}(),
		top: function() {
			var axis = "vertical";
			var bottomEdge = scrollableEdges.get("bottom");
			if (bottomEdge) return getScrollChange({
				client,
				edge: bottomEdge.edge,
				hitbox: bottomEdge.hitbox,
				axis,
				timeSinceLastFrame,
				engagement,
				isDistanceDampeningEnabled: true,
				config
			});
			var topEdge = scrollableEdges.get("top");
			if (topEdge) return getScrollChange({
				client,
				edge: topEdge.edge,
				hitbox: topEdge.hitbox,
				axis,
				timeSinceLastFrame,
				engagement,
				isDistanceDampeningEnabled: true,
				config
			});
			return 0;
		}()
	};
}
//#endregion
//#region node_modules/@atlaskit/pragmatic-drag-and-drop-auto-scroll/dist/esm/over-element/try-scroll.js
function _createForOfIteratorHelper(r, e) {
	var t = "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"];
	if (!t) {
		if (Array.isArray(r) || (t = _unsupportedIterableToArray(r)) || e && r && "number" == typeof r.length) {
			t && (r = t);
			var _n = 0, F = function F() {};
			return {
				s: F,
				n: function n() {
					return _n >= r.length ? { done: !0 } : {
						done: !1,
						value: r[_n++]
					};
				},
				e: function e(r) {
					throw r;
				},
				f: F
			};
		}
		throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
	}
	var o, a = !0, u = !1;
	return {
		s: function s() {
			t = t.call(r);
		},
		n: function n() {
			var r = t.next();
			return a = r.done, r;
		},
		e: function e(r) {
			u = !0, o = r;
		},
		f: function f() {
			try {
				a || null == t.return || t.return();
			} finally {
				if (u) throw o;
			}
		}
	};
}
function _unsupportedIterableToArray(r, a) {
	if (r) {
		if ("string" == typeof r) return _arrayLikeToArray(r, a);
		var t = {}.toString.call(r).slice(8, -1);
		return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray(r, a) : void 0;
	}
}
function _arrayLikeToArray(r, a) {
	(null == a || a > r.length) && (a = r.length);
	for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e];
	return n;
}
function isScrollingAvailable(value) {
	return Boolean(value.top || value.left);
}
function tryScrollElements(_ref) {
	var _container$getConfigu, _container$getAllowed, _container$getAllowed2;
	var target = _ref.target, input = _ref.input, source = _ref.source, findEntry = _ref.findEntry, timeSinceLastFrame = _ref.timeSinceLastFrame, _ref$available = _ref.available, available = _ref$available === void 0 ? {
		top: true,
		left: true
	} : _ref$available;
	if (!isScrollingAvailable(available)) return available;
	if (!target) return available;
	var element = target.closest(selector);
	if (!element) return available;
	var container = findEntry(element);
	if (!container) return available;
	function continueSearchUp() {
		var _element$parentElemen;
		return tryScrollElements({
			target: (_element$parentElemen = element === null || element === void 0 ? void 0 : element.parentElement) !== null && _element$parentElemen !== void 0 ? _element$parentElemen : null,
			findEntry,
			source,
			timeSinceLastFrame,
			input,
			available
		});
	}
	var feedback = {
		input,
		source,
		element
	};
	if (container.canScroll && !container.canScroll(feedback)) return continueSearchUp();
	var engagement = markAndGetEngagement(element);
	var config = getInternalConfig((_container$getConfigu = container.getConfiguration) === null || _container$getConfigu === void 0 ? void 0 : _container$getConfigu.call(container, feedback));
	var scrollBy = getScrollBy({
		element,
		engagement,
		input,
		timeSinceLastFrame,
		allowedAxis: (_container$getAllowed = (_container$getAllowed2 = container.getAllowedAxis) === null || _container$getAllowed2 === void 0 ? void 0 : _container$getAllowed2.call(container, feedback)) !== null && _container$getAllowed !== void 0 ? _container$getAllowed : "all",
		config
	});
	var scroll = {
		top: 0,
		left: 0
	};
	if (available.top && scrollBy.top !== 0) {
		scroll.top = scrollBy.top;
		available.top = false;
	}
	if (available.left && scrollBy.left !== 0) {
		scroll.left = scrollBy.left;
		available.left = false;
	}
	if (scroll.top !== 0 || scroll.left !== 0) element.scrollBy(scroll);
	return continueSearchUp();
}
function tryScrollWindow(_ref2) {
	var input = _ref2.input, timeSinceLastFrame = _ref2.timeSinceLastFrame, available = _ref2.available, source = _ref2.source, entries = _ref2.entries;
	var element = document.documentElement;
	var feedback = {
		input,
		source,
		element
	};
	var _iterator = _createForOfIteratorHelper(entries), _step;
	try {
		for (_iterator.s(); !(_step = _iterator.n()).done;) {
			var _entry$getConfigurati, _entry$getAllowedAxis, _entry$getAllowedAxis2;
			var entry = _step.value;
			if (entry.canScroll && !entry.canScroll(feedback)) continue;
			var scrollBy = getScrollBy({
				element,
				engagement: markAndGetEngagement(element),
				input,
				config: getInternalConfig((_entry$getConfigurati = entry.getConfiguration) === null || _entry$getConfigurati === void 0 ? void 0 : _entry$getConfigurati.call(entry, feedback)),
				allowedAxis: (_entry$getAllowedAxis = (_entry$getAllowedAxis2 = entry.getAllowedAxis) === null || _entry$getAllowedAxis2 === void 0 ? void 0 : _entry$getAllowedAxis2.call(entry, feedback)) !== null && _entry$getAllowedAxis !== void 0 ? _entry$getAllowedAxis : "all",
				getRect: function getRect(element) {
					return DOMRect.fromRect({
						y: 0,
						x: 0,
						width: element.clientWidth,
						height: element.clientHeight
					});
				},
				timeSinceLastFrame
			});
			var scroll = {
				top: available.top ? scrollBy.top : 0,
				left: available.left ? scrollBy.left : 0
			};
			if (scroll.top !== 0 || scroll.left !== 0) element.scrollBy(scroll);
			break;
		}
	} catch (err) {
		_iterator.e(err);
	} finally {
		_iterator.f();
	}
}
function tryScroll(_ref3) {
	var input = _ref3.input, findEntry = _ref3.findEntry, timeSinceLastFrame = _ref3.timeSinceLastFrame, source = _ref3.source, getWindowScrollEntries = _ref3.getWindowScrollEntries, underUsersPointer = _ref3.underUsersPointer;
	var remainder = tryScrollElements({
		target: underUsersPointer,
		timeSinceLastFrame,
		input,
		source,
		findEntry
	});
	if (!isScrollingAvailable(remainder)) return;
	tryScrollWindow({
		input,
		source,
		entries: getWindowScrollEntries(),
		timeSinceLastFrame,
		available: remainder
	});
}
//#endregion
//#region node_modules/@atlaskit/pragmatic-drag-and-drop-auto-scroll/dist/esm/over-element/make-api.js
function ownKeys(e, r) {
	var t = Object.keys(e);
	if (Object.getOwnPropertySymbols) {
		var o = Object.getOwnPropertySymbols(e);
		r && (o = o.filter(function(r) {
			return Object.getOwnPropertyDescriptor(e, r).enumerable;
		})), t.push.apply(t, o);
	}
	return t;
}
function _objectSpread(e) {
	for (var r = 1; r < arguments.length; r++) {
		var t = null != arguments[r] ? arguments[r] : {};
		r % 2 ? ownKeys(Object(t), !0).forEach(function(r) {
			_defineProperty(e, r, t[r]);
		}) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys(Object(t)).forEach(function(r) {
			Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r));
		});
	}
	return e;
}
function makeApi(_ref) {
	var monitor = _ref.monitor;
	var elementRegistry = /* @__PURE__ */ new Map();
	var windowRegistry = /* @__PURE__ */ new Set();
	function autoScroll(args) {
		var _window$getComputedSt = window.getComputedStyle(args.element), overflowX = _window$getComputedSt.overflowX, overflowY = _window$getComputedSt.overflowY;
		if (!(overflowX === "auto" || overflowX === "scroll" || overflowY === "auto" || overflowY === "scroll")) console.warn("Auto scrolling has been attached to an element that appears not to be scrollable", {
			element: args.element,
			overflowX,
			overflowY
		});
		var existing = elementRegistry.get(args.element);
		if (existing) console.warn("You have already registered autoScrolling on the same element", {
			existing,
			proposed: args
		});
		elementRegistry.set(args.element, args);
		return once(combine(addScrollableAttribute(args.element), function() {
			return elementRegistry.delete(args.element);
		}));
	}
	function autoScrollWindow() {
		var unique = _objectSpread({}, arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {});
		windowRegistry.add(unique);
		function cleanup() {
			windowRegistry.delete(unique);
		}
		return once(cleanup);
	}
	function findEntry(element) {
		var _elementRegistry$get;
		return (_elementRegistry$get = elementRegistry.get(element)) !== null && _elementRegistry$get !== void 0 ? _elementRegistry$get : null;
	}
	function getWindowScrollEntries() {
		return Array.from(windowRegistry);
	}
	function onFrame(_ref2) {
		var latestArgs = _ref2.latestArgs, underUsersPointer = _ref2.underUsersPointer, timeSinceLastFrame = _ref2.timeSinceLastFrame;
		tryScroll({
			input: latestArgs.location.current.input,
			source: latestArgs.source,
			findEntry,
			underUsersPointer,
			timeSinceLastFrame,
			getWindowScrollEntries
		});
	}
	getScheduler(monitor).onFrame(onFrame);
	return {
		autoScroll,
		autoScrollWindow
	};
}
//#endregion
//#region node_modules/@atlaskit/pragmatic-drag-and-drop-auto-scroll/dist/esm/entry-point/element.js
var api = makeApi({ monitor: monitorForElements });
var autoScrollForElements = api.autoScroll;
api.autoScrollWindow;
//#endregion
//#region node_modules/@mui/x-scheduler-internals/internals/utils/useEventCreation.mjs
function useEventCreation(getCreationPlaceholder) {
	const store = useSchedulerStoreContext();
	const creationConfig = useStore(store, schedulerEventSelectors.creationConfig);
	if (creationConfig === false) return {};
	const handler = (event) => {
		const target = event.target;
		if (target !== event.currentTarget && target.closest("button, [role=\"button\"]")) return;
		store.setOccurrencePlaceholder(_extends({ type: "creation" }, getCreationPlaceholder({
			event,
			creationConfig
		})));
	};
	if (creationConfig.interaction === "double-click") return { onDoubleClick: handler };
	return { onClick: handler };
}
//#endregion
//#region node_modules/@mui/x-scheduler-internals/internals/utils/useKeyboardEventCreation.mjs
/**
* Hook that returns a function to trigger event creation via keyboard (Enter key).
* Returns `undefined` when event creation is disabled.
*
* This is the keyboard counterpart of `useEventCreation` (which handles mouse clicks).
* Both call `store.setOccurrencePlaceholder` with the same `type: 'creation'` placeholder.
*/
function useKeyboardEventCreation(getCreationPlaceholder) {
	const store = useSchedulerStoreContext();
	const creationConfig = useStore(store, schedulerEventSelectors.creationConfig);
	if (creationConfig === false) return;
	return () => {
		store.setOccurrencePlaceholder(_extends({ type: "creation" }, getCreationPlaceholder({ creationConfig })));
	};
}
//#endregion
//#region node_modules/@mui/x-scheduler-internals/internals/utils/getNavigationTarget.mjs
var DEFAULT_ROW_TYPES = [
	"header",
	"day-grid",
	"time-grid"
];
function getRowCount(rowsPerType, rowType) {
	return rowsPerType[rowType] ?? 1;
}
/**
* Computes the target cell coordinates for a given arrow key press.
* Returns `null` if navigation is not possible (e.g., ArrowUp on the header row).
*/
function getNavigationTarget(key, rowType, rowIndex, columnIndex, options) {
	const { columnCount, rowTypes, rowsPerType } = options;
	switch (key) {
		case "ArrowLeft": return columnIndex > 0 ? {
			rowType,
			rowIndex,
			columnIndex: columnIndex - 1
		} : null;
		case "ArrowRight": return columnIndex < columnCount - 1 ? {
			rowType,
			rowIndex,
			columnIndex: columnIndex + 1
		} : null;
		case "ArrowDown": {
			if (rowIndex < getRowCount(rowsPerType, rowType) - 1) return {
				rowType,
				rowIndex: rowIndex + 1,
				columnIndex
			};
			const typeIndex = rowTypes.indexOf(rowType);
			if (typeIndex >= 0 && typeIndex < rowTypes.length - 1) return {
				rowType: rowTypes[typeIndex + 1],
				rowIndex: 0,
				columnIndex
			};
			return null;
		}
		case "ArrowUp": {
			if (rowIndex > 0) return {
				rowType,
				rowIndex: rowIndex - 1,
				columnIndex
			};
			const typeIndex = rowTypes.indexOf(rowType);
			if (typeIndex > 0) {
				const prevRowType = rowTypes[typeIndex - 1];
				return {
					rowType: prevRowType,
					rowIndex: getRowCount(rowsPerType, prevRowType) - 1,
					columnIndex
				};
			}
			return null;
		}
		default: return null;
	}
}
//#endregion
//#region node_modules/@base-ui/utils/useId.mjs
var globalId = 0;
function useGlobalId(idOverride, prefix = "mui") {
	const [defaultId, setDefaultId] = import_react.useState(idOverride);
	const id = idOverride || defaultId;
	import_react.useEffect(() => {
		if (defaultId == null) {
			globalId += 1;
			setDefaultId(`${prefix}-${globalId}`);
		}
	}, [defaultId, prefix]);
	return id;
}
var maybeReactUseId = SafeReact.useId;
/**
*
* @example <div id={useId()} />
* @param idOverride
* @returns {string}
*/
function useId(idOverride, prefix) {
	if (maybeReactUseId !== void 0) {
		const reactId = maybeReactUseId();
		return idOverride ?? (prefix ? `${prefix}-${reactId}` : reactId);
	}
	return useGlobalId(idOverride, prefix);
}
//#endregion
//#region node_modules/@mui/icons-material/RepeatRounded.mjs
var RepeatRounded_default = createSvgIcon(/*#__PURE__*/ (0, import_jsx_runtime.jsx)("path", { d: "M7 7h10v1.79c0 .45.54.67.85.35l2.79-2.79c.2-.2.2-.51 0-.71l-2.79-2.79c-.31-.31-.85-.09-.85.36V5H6c-.55 0-1 .45-1 1v4c0 .55.45 1 1 1s1-.45 1-1zm10 10H7v-1.79c0-.45-.54-.67-.85-.35l-2.79 2.79c-.2.2-.2.51 0 .71l2.79 2.79c.31.31.85.09.85-.36V19h11c.55 0 1-.45 1-1v-4c0-.55-.45-1-1-1s-1 .45-1 1z" }), "RepeatRounded");
//#endregion
//#region node_modules/@base-ui/utils/error.mjs
var set = /* @__PURE__ */ new Set();
function error(...messages) {
	{
		const messageKey = messages.join(" ");
		if (!set.has(messageKey)) {
			set.add(messageKey);
			console.error(`Base UI: ${messageKey}`);
		}
	}
}
//#endregion
//#region node_modules/@base-ui/utils/mergeObjects.mjs
function mergeObjects(a, b) {
	if (a && !b) return a;
	if (!a && b) return b;
	if (a || b) return {
		...a,
		...b
	};
}
//#endregion
//#region node_modules/@base-ui/react/merge-props/mergeProps.mjs
var EMPTY_PROPS = {};
/**
* Merges multiple sets of React props. It follows the Object.assign pattern where the rightmost object's fields overwrite
* the conflicting ones from others. This doesn't apply to event handlers, `className` and `style` props.
*
* Event handlers are merged and called in right-to-left order (rightmost handler executes first, leftmost last).
* For React synthetic events, the rightmost handler can prevent prior (left-positioned) handlers from executing
* by calling `event.preventBaseUIHandler()`. For non-synthetic events (custom events with primitive/object values),
* all handlers always execute without prevention capability.
*
* The `className` prop is merged by concatenating classes in right-to-left order (rightmost class appears first in the string).
* The `style` prop is merged with rightmost styles overwriting the prior ones.
*
* Props can either be provided as objects or as functions that take the previous props as an argument.
* The function will receive the merged props up to that point (going from left to right):
* so in the case of `(obj1, obj2, fn, obj3)`, `fn` will receive the merged props of `obj1` and `obj2`.
* The function is responsible for chaining event handlers if needed (that is, we don't run the merge logic).
*
* Event handlers returned by the functions are not automatically prevented when `preventBaseUIHandler` is called.
* They must check `event.baseUIHandlerPrevented` themselves and bail out if it's true.
*
* @important **`ref` is not merged.**
* @param a Props object to merge.
* @param b Props object to merge. The function will overwrite conflicting props from `a`.
* @param c Props object to merge. The function will overwrite conflicting props from previous parameters.
* @param d Props object to merge. The function will overwrite conflicting props from previous parameters.
* @param e Props object to merge. The function will overwrite conflicting props from previous parameters.
* @returns The merged props.
* @public
*/
function mergeProps(a, b, c, d, e) {
	if (!c && !d && !e && !a) return createInitialMergedProps(b);
	let merged = createInitialMergedProps(a);
	if (b) merged = mergeInto(merged, b);
	if (c) merged = mergeInto(merged, c);
	if (d) merged = mergeInto(merged, d);
	if (e) merged = mergeInto(merged, e);
	return merged;
}
/**
* Merges an arbitrary number of React props using the same logic as {@link mergeProps}.
* This function accepts an array of props instead of individual arguments.
*
* This has slightly lower performance than {@link mergeProps} due to accepting an array
* instead of a fixed number of arguments. Prefer {@link mergeProps} when merging 5 or
* fewer prop sets for better performance.
*
* @param props Array of props to merge.
* @returns The merged props.
* @see mergeProps
* @public
*/
function mergePropsN(props) {
	if (props.length === 0) return EMPTY_PROPS;
	if (props.length === 1) return createInitialMergedProps(props[0]);
	let merged = createInitialMergedProps(props[0]);
	for (let i = 1; i < props.length; i += 1) merged = mergeInto(merged, props[i]);
	return merged;
}
function createInitialMergedProps(inputProps) {
	if (isPropsGetter(inputProps)) return { ...resolvePropsGetter(inputProps, EMPTY_PROPS) };
	return copyInitialProps(inputProps);
}
function mergeInto(merged, inputProps) {
	if (isPropsGetter(inputProps)) return resolvePropsGetter(inputProps, merged);
	return mutablyMergeInto(merged, inputProps);
}
function copyInitialProps(inputProps) {
	const copiedProps = { ...inputProps };
	for (const propName in copiedProps) {
		const propValue = copiedProps[propName];
		if (isEventHandler(propName, propValue)) copiedProps[propName] = wrapEventHandler(propValue);
	}
	return copiedProps;
}
/**
* Merges two sets of props. In case of conflicts, the external props take precedence.
*/
function mutablyMergeInto(mergedProps, externalProps) {
	if (!externalProps) return mergedProps;
	for (const propName in externalProps) {
		const externalPropValue = externalProps[propName];
		switch (propName) {
			case "style":
				mergedProps[propName] = mergeObjects(mergedProps.style, externalPropValue);
				break;
			case "className":
				mergedProps[propName] = mergeClassNames(mergedProps.className, externalPropValue);
				break;
			default: if (isEventHandler(propName, externalPropValue)) mergedProps[propName] = mergeEventHandlers(mergedProps[propName], externalPropValue);
			else mergedProps[propName] = externalPropValue;
		}
	}
	return mergedProps;
}
function isEventHandler(key, value) {
	const code0 = key.charCodeAt(0);
	const code1 = key.charCodeAt(1);
	const code2 = key.charCodeAt(2);
	return code0 === 111 && code1 === 110 && code2 >= 65 && code2 <= 90 && (typeof value === "function" || typeof value === "undefined");
}
function isPropsGetter(inputProps) {
	return typeof inputProps === "function";
}
function resolvePropsGetter(inputProps, previousProps) {
	if (isPropsGetter(inputProps)) return inputProps(previousProps);
	return inputProps ?? EMPTY_PROPS;
}
function mergeEventHandlers(ourHandler, theirHandler) {
	if (!theirHandler) return ourHandler;
	if (!ourHandler) return wrapEventHandler(theirHandler);
	return (...args) => {
		const event = args[0];
		if (isSyntheticEvent(event)) {
			const baseUIEvent = event;
			makeEventPreventable(baseUIEvent);
			const result = theirHandler(...args);
			if (!baseUIEvent.baseUIHandlerPrevented) ourHandler?.(...args);
			return result;
		}
		const result = theirHandler(...args);
		ourHandler?.(...args);
		return result;
	};
}
function wrapEventHandler(handler) {
	if (!handler) return handler;
	return (...args) => {
		const event = args[0];
		if (isSyntheticEvent(event)) makeEventPreventable(event);
		return handler(...args);
	};
}
function makeEventPreventable(event) {
	event.preventBaseUIHandler = () => {
		event.baseUIHandlerPrevented = true;
	};
	return event;
}
function mergeClassNames(ourClassName, theirClassName) {
	if (theirClassName) {
		if (ourClassName) return theirClassName + " " + ourClassName;
		return theirClassName;
	}
	return ourClassName;
}
function isSyntheticEvent(event) {
	return event != null && typeof event === "object" && "nativeEvent" in event;
}
//#endregion
//#region node_modules/@base-ui/react/internals/composite/root/CompositeRootContext.mjs
var CompositeRootContext$1 = /*#__PURE__*/ import_react.createContext(void 0);
CompositeRootContext$1.displayName = "CompositeRootContext";
function useCompositeRootContext$1(optional = false) {
	const context = import_react.useContext(CompositeRootContext$1);
	if (context === void 0 && !optional) throw new Error("Base UI: CompositeRootContext is missing. Composite parts must be placed within <Composite.Root>.");
	return context;
}
//#endregion
//#region node_modules/@base-ui/react/utils/useFocusableWhenDisabled.mjs
function useFocusableWhenDisabled$1(parameters) {
	const { focusableWhenDisabled, disabled, composite = false, tabIndex: tabIndexProp = 0, isNativeButton } = parameters;
	const isFocusableComposite = composite && focusableWhenDisabled !== false;
	const isNonFocusableComposite = composite && focusableWhenDisabled === false;
	return { props: import_react.useMemo(() => {
		const additionalProps = { onKeyDown(event) {
			if (disabled && focusableWhenDisabled && event.key !== "Tab") event.preventDefault();
		} };
		if (!composite) {
			additionalProps.tabIndex = tabIndexProp;
			if (!isNativeButton && disabled) additionalProps.tabIndex = focusableWhenDisabled ? tabIndexProp : -1;
		}
		if (isNativeButton && (focusableWhenDisabled || isFocusableComposite) || !isNativeButton && disabled) additionalProps["aria-disabled"] = disabled;
		if (isNativeButton && (!focusableWhenDisabled || isNonFocusableComposite)) additionalProps.disabled = disabled;
		return additionalProps;
	}, [
		composite,
		disabled,
		focusableWhenDisabled,
		isFocusableComposite,
		isNonFocusableComposite,
		isNativeButton,
		tabIndexProp
	]) };
}
//#endregion
//#region node_modules/@base-ui/react/internals/use-button/useButton.mjs
function useButton$1(parameters = {}) {
	const { disabled = false, focusableWhenDisabled, tabIndex = 0, native: isNativeButton = true, composite: compositeProp } = parameters;
	const elementRef = import_react.useRef(null);
	const compositeRootContext = useCompositeRootContext$1(true);
	const isCompositeItem = compositeProp ?? compositeRootContext !== void 0;
	const { props: focusableWhenDisabledProps } = useFocusableWhenDisabled$1({
		focusableWhenDisabled,
		disabled,
		composite: isCompositeItem,
		tabIndex,
		isNativeButton
	});
	import_react.useEffect(() => {
		if (!elementRef.current) return;
		const isButtonTag = isButtonElement$1(elementRef.current);
		if (isNativeButton) {
			if (!isButtonTag) error(`A component that acts as a button expected a native <button> because the \`nativeButton\` prop is true. Rendering a non-<button> removes native button semantics, which can impact forms and accessibility. Use a real <button> in the \`render\` prop, or set \`nativeButton\` to \`false\`.${SafeReact.captureOwnerStack?.() || ""}`);
		} else if (isButtonTag) error(`A component that acts as a button expected a non-<button> because the \`nativeButton\` prop is false. Rendering a <button> keeps native behavior while Base UI applies non-native attributes and handlers, which can add unintended extra attributes (such as \`role\` or \`aria-disabled\`). Use a non-<button> in the \`render\` prop, or set \`nativeButton\` to \`true\`.${SafeReact.captureOwnerStack?.() || ""}`);
	}, [isNativeButton]);
	const updateDisabled = import_react.useCallback(() => {
		const element = elementRef.current;
		if (!isButtonElement$1(element)) return;
		if (isCompositeItem && disabled && focusableWhenDisabledProps.disabled === void 0 && element.disabled) element.disabled = false;
	}, [
		disabled,
		focusableWhenDisabledProps.disabled,
		isCompositeItem
	]);
	useIsoLayoutEffect(updateDisabled, [updateDisabled]);
	return {
		getButtonProps: import_react.useCallback((externalProps = {}) => {
			const { onClick: externalOnClick, onMouseDown: externalOnMouseDown, onKeyUp: externalOnKeyUp, onKeyDown: externalOnKeyDown, onPointerDown: externalOnPointerDown, ...otherExternalProps } = externalProps;
			return mergeProps({
				onClick(event) {
					if (disabled) {
						event.preventDefault();
						return;
					}
					externalOnClick?.(event);
				},
				onMouseDown(event) {
					if (!disabled) externalOnMouseDown?.(event);
				},
				onKeyDown(event) {
					if (disabled) return;
					makeEventPreventable(event);
					externalOnKeyDown?.(event);
					if (event.baseUIHandlerPrevented) return;
					const isCurrentTarget = event.target === event.currentTarget;
					const currentTarget = event.currentTarget;
					const isButton = isButtonElement$1(currentTarget);
					const isLink = !isNativeButton && isValidLinkElement$1(currentTarget);
					const shouldClick = isCurrentTarget && (isNativeButton ? isButton : !isLink);
					const isEnterKey = event.key === "Enter";
					const isSpaceKey = event.key === " ";
					const role = currentTarget.getAttribute("role");
					const isTextNavigationRole = role?.startsWith("menuitem") || role === "option" || role === "gridcell";
					if (isCurrentTarget && isCompositeItem && isSpaceKey) {
						if (event.defaultPrevented && isTextNavigationRole) return;
						event.preventDefault();
						if (isLink || isNativeButton && isButton) {
							currentTarget.click();
							event.preventBaseUIHandler();
						} else if (shouldClick) {
							externalOnClick?.(event);
							event.preventBaseUIHandler();
						}
						return;
					}
					if (shouldClick) {
						if (!isNativeButton && (isSpaceKey || isEnterKey)) event.preventDefault();
						if (!isNativeButton && isEnterKey) externalOnClick?.(event);
					}
				},
				onKeyUp(event) {
					if (disabled) return;
					makeEventPreventable(event);
					externalOnKeyUp?.(event);
					if (event.target === event.currentTarget && isNativeButton && isCompositeItem && isButtonElement$1(event.currentTarget) && event.key === " ") {
						event.preventDefault();
						return;
					}
					if (event.baseUIHandlerPrevented) return;
					if (event.target === event.currentTarget && !isNativeButton && !isCompositeItem && event.key === " ") externalOnClick?.(event);
				},
				onPointerDown(event) {
					if (disabled) {
						event.preventDefault();
						return;
					}
					externalOnPointerDown?.(event);
				}
			}, isNativeButton ? { type: "button" } : { role: "button" }, focusableWhenDisabledProps, otherExternalProps);
		}, [
			disabled,
			focusableWhenDisabledProps,
			isCompositeItem,
			isNativeButton
		]),
		buttonRef: useStableCallback((element) => {
			elementRef.current = element;
			updateDisabled();
		})
	};
}
function isButtonElement$1(elem) {
	return isHTMLElement(elem) && elem.tagName === "BUTTON";
}
function isValidLinkElement$1(elem) {
	return Boolean(elem?.tagName === "A" && elem?.href);
}
//#endregion
//#region node_modules/@base-ui/utils/getReactElementRef.mjs
/**
* Extracts the `ref` from a React element, handling different React versions.
*/
function getReactElementRef(element) {
	if (!/*#__PURE__*/ import_react.isValidElement(element)) return null;
	const reactElement = element;
	const propsWithRef = reactElement.props;
	return (isReactVersionAtLeast(19) ? propsWithRef?.ref : reactElement.ref) ?? null;
}
//#endregion
//#region node_modules/@base-ui/react/internals/getStateAttributesProps.mjs
function getStateAttributesProps$1(state, customMapping) {
	const props = {};
	for (const key in state) {
		const value = state[key];
		if (customMapping?.hasOwnProperty(key)) {
			const customProps = customMapping[key](value);
			if (customProps != null) Object.assign(props, customProps);
			continue;
		}
		if (value === true) props[`data-${key.toLowerCase()}`] = "";
		else if (value) props[`data-${key.toLowerCase()}`] = value.toString();
	}
	return props;
}
//#endregion
//#region node_modules/@base-ui/react/utils/resolveClassName.mjs
/**
* If the provided className is a string, it will be returned as is.
* Otherwise, the function will call the className function with the state as the first argument.
*
* @param className
* @param state
*/
function resolveClassName$1(className, state) {
	return typeof className === "function" ? className(state) : className;
}
//#endregion
//#region node_modules/@base-ui/react/utils/resolveStyle.mjs
/**
* If the provided style is an object, it will be returned as is.
* Otherwise, the function will call the style function with the state as the first argument.
*
* @param style
* @param state
*/
function resolveStyle$1(style, state) {
	return typeof style === "function" ? style(state) : style;
}
//#endregion
//#region node_modules/@base-ui/react/internals/useRenderElement.mjs
/**
* Renders a Base UI element.
*
* @param element The default HTML element to render. Can be overridden by the `render` prop.
* @param componentProps An object containing the `render` and `className` props to be used for element customization. Other props are ignored.
* @param params Additional parameters for rendering the element.
*/
function useRenderElement$1(element, componentProps, params = {}) {
	const renderProp = componentProps.render;
	const outProps = useRenderElementProps$1(componentProps, params);
	if (params.enabled === false) return null;
	return evaluateRenderProp$1(element, renderProp, outProps, params.state ?? EMPTY_OBJECT);
}
/**
* Computes render element final props.
*/
function useRenderElementProps$1(componentProps, params = {}) {
	const { className: classNameProp, style: styleProp, render: renderProp } = componentProps;
	const { state = EMPTY_OBJECT, ref, props, stateAttributesMapping, enabled = true } = params;
	const className = enabled ? resolveClassName$1(classNameProp, state) : void 0;
	const style = enabled ? resolveStyle$1(styleProp, state) : void 0;
	const stateProps = enabled ? getStateAttributesProps$1(state, stateAttributesMapping) : EMPTY_OBJECT;
	const resolvedProps = enabled && props ? resolveRenderFunctionProps$1(props) : void 0;
	const outProps = enabled ? mergeObjects(stateProps, resolvedProps) ?? {} : EMPTY_OBJECT;
	if (typeof document !== "undefined") if (!enabled) useMergedRefs(null, null);
	else if (Array.isArray(ref)) outProps.ref = useMergedRefsN([
		outProps.ref,
		getReactElementRef(renderProp),
		...ref
	]);
	else outProps.ref = useMergedRefs(outProps.ref, getReactElementRef(renderProp), ref);
	if (!enabled) return EMPTY_OBJECT;
	if (className !== void 0) outProps.className = mergeClassNames(outProps.className, className);
	if (style !== void 0) outProps.style = mergeObjects(outProps.style, style);
	return outProps;
}
function resolveRenderFunctionProps$1(props) {
	if (Array.isArray(props)) return mergePropsN(props);
	return mergeProps(void 0, props);
}
var REACT_LAZY_TYPE$1 = Symbol.for("react.lazy");
var COMPONENT_IDENTIFIER_PATTERN$1 = /^[A-Z][A-Za-z0-9$]*$/;
var LOWERCASE_CHARACTER_PATTERN$1 = /[a-z]/;
function evaluateRenderProp$1(element, render, props, state) {
	if (render) {
		if (typeof render === "function") {
			warnIfRenderPropLooksLikeComponent$1(render);
			return render(props, state);
		}
		const mergedProps = mergeProps(props, render.props);
		mergedProps.ref = props.ref;
		let newElement = render;
		if (newElement?.$$typeof === REACT_LAZY_TYPE$1) newElement = import_react.Children.toArray(render)[0];
		if (!/*#__PURE__*/ import_react.isValidElement(newElement)) throw new Error([
			"Base UI: The `render` prop was provided an invalid React element as `React.isValidElement(render)` is `false`.",
			"A valid React element must be provided to the `render` prop because it is cloned with props to replace the default element.",
			"https://base-ui.com/r/invalid-render-prop"
		].join("\n"));
		return /*#__PURE__*/ import_react.cloneElement(newElement, mergedProps);
	}
	if (element) {
		if (typeof element === "string") return renderTag$1(element, props);
	}
	throw new Error("Base UI: Render element or function are not defined.");
}
function warnIfRenderPropLooksLikeComponent$1(renderFn) {
	const functionName = renderFn.name;
	if (functionName.length === 0) return;
	if (!COMPONENT_IDENTIFIER_PATTERN$1.test(functionName)) return;
	if (!LOWERCASE_CHARACTER_PATTERN$1.test(functionName)) return;
	warn(`The \`render\` prop received a function named \`${functionName}\` that starts with an uppercase letter.`, "This usually means a React component was passed directly as `render={Component}`.", "Base UI calls `render` as a plain function, which can break the Rules of Hooks during reconciliation.", "If this is an intentional render callback, rename it to start with a lowercase letter.", "Use `render={<Component />}` or `render={(props) => <Component {...props} />}` instead.", "https://base-ui.com/r/invalid-render-prop");
}
function renderTag$1(Tag, props) {
	if (Tag === "button") return /*#__PURE__*/ (0, import_react.createElement)("button", {
		type: "button",
		...props,
		key: props.key
	});
	if (Tag === "img") return /*#__PURE__*/ (0, import_react.createElement)("img", {
		alt: "",
		...props,
		key: props.key
	});
	return /*#__PURE__*/ import_react.createElement(Tag, props);
}
//#endregion
//#region node_modules/@mui/x-scheduler/internals/utils/date-utils.mjs
/**
* @example "Sun, Jul 13"
*/
function formatWeekDayMonthAndDayOfMonth(date, adapter) {
	const f = adapter.formats;
	const dateFormat = `${f.weekday3Letters}, ${f.month3Letters} ${f.dayOfMonth}`;
	return adapter.formatByString(date, dateFormat);
}
/**
* @example "Jul 13"
*/
function formatMonthAndDayOfMonth(date, adapter) {
	const f = adapter.formats;
	const dateFormat = `${f.month3Letters} ${f.dayOfMonth}`;
	return adapter.formatByString(date, dateFormat);
}
/**
* @example "13 December"
*/
function formatDayOfMonthAndMonthFullLetter(date, adapter) {
	const f = adapter.formats;
	const dateFormat = `${f.dayOfMonth} ${f.monthFullLetter}`;
	return adapter.formatByString(date, dateFormat);
}
function formatMonthFullLetterAndYear(date, adapter) {
	const f = adapter.formats;
	const dateFormat = `${f.monthFullLetter} ${f.yearPadded}`;
	return adapter.formatByString(date, dateFormat);
}
/**
* @example "1:30 PM" or "13:30"
*/
function formatHourAndMinutes(date, adapter, ampm) {
	const f = adapter.formats;
	const timeFormat = ampm ? `${f.hours12h}:${f.minutesPadded} ${f.meridiem}` : `${f.hours24h}:${f.minutesPadded}`;
	return adapter.formatByString(date, timeFormat);
}
//#endregion
//#region node_modules/@mui/x-scheduler/internals/hooks/useFormatTime.mjs
function useFormatTime() {
	const adapter = useAdapterContext();
	const ampm = useStore(useSchedulerStoreContext(), schedulerPreferenceSelectors.ampm);
	return import_react.useCallback((date) => {
		return formatHourAndMinutes(date, adapter, ampm);
	}, [adapter, ampm]);
}
//#endregion
//#region node_modules/@mui/x-scheduler/event-calendar/EventCalendarStyledContext.mjs
var EventCalendarStyledContext = /*#__PURE__*/ import_react.createContext(null);
EventCalendarStyledContext.displayName = "EventCalendarStyledContext";
function useEventCalendarStyledContext() {
	const value = import_react.useContext(EventCalendarStyledContext);
	if (!value) throw new Error("MUI X Scheduler: useEventCalendarStyledContext must be used within an EventCalendarStyledContext.Provider. The component requires access to calendar styling and locale information. Ensure the component is rendered inside an EventCalendar component.");
	return value;
}
//#endregion
//#region node_modules/@mui/x-scheduler/internals/utils/tokens.mjs
/**
* Custom color mappings for scheduler events.
* These are derived from Material UI color palettes but with custom selections
* for optimal visual appearance in both light and dark modes.
*/
var eventColorTokens = {
	red: {
		light: {
			main: red[500],
			"surface-bold": "#ffa3a3",
			"surface-bold-hover": "#ff9494",
			"on-surface-bold": "#770000",
			"surface-selected": red[600],
			"surface-selected-hover": red[700],
			"on-surface-selected": "#FFFFFF",
			"surface-subtle": red[50],
			"surface-subtle-hover": "#FEDCDF",
			"surface-accent": red[600],
			"on-surface-subtle-primary": "#931717",
			"on-surface-subtle-secondary": red[900]
		},
		dark: {
			main: red[600],
			"surface-bold": "#821b1b",
			"surface-bold-hover": "#A02424",
			"on-surface-bold": "#FFFFFF",
			"surface-selected": red[800],
			"surface-selected-hover": red[900],
			"on-surface-selected": "#FFFFFF",
			"surface-subtle": "#330909",
			"surface-subtle-hover": "#450C0C",
			"surface-accent": red[600],
			"on-surface-subtle-primary": red[100],
			"on-surface-subtle-secondary": red[300]
		}
	},
	pink: {
		light: {
			main: pink[400],
			"surface-bold": "#fca4c1",
			"surface-bold-hover": pink[200],
			"on-surface-bold": "#840f4d",
			"surface-selected": pink[400],
			"surface-selected-hover": "#e9437b",
			"on-surface-selected": "#FFFFFF",
			"surface-subtle": pink[50],
			"surface-subtle-hover": "#FACCDC",
			"surface-accent": pink[400],
			"on-surface-subtle-primary": pink[900],
			"on-surface-subtle-secondary": pink[800]
		},
		dark: {
			main: pink[600],
			"surface-bold": "#67103E",
			"surface-bold-hover": "#73234D",
			"on-surface-bold": "#FFFFFF",
			"surface-selected": pink[800],
			"surface-selected-hover": "#c22066",
			"on-surface-selected": "#FFFFFF",
			"surface-subtle": "#43132d",
			"surface-subtle-hover": "#4F1735",
			"surface-accent": pink[600],
			"on-surface-subtle-primary": pink[50],
			"on-surface-subtle-secondary": pink[200]
		}
	},
	purple: {
		light: {
			main: purple[400],
			"surface-bold": "#D6ABDD",
			"surface-bold-hover": purple[200],
			"on-surface-bold": "#380673",
			"surface-selected": purple[400],
			"surface-selected-hover": "#993ea8",
			"on-surface-selected": "#FFFFFF",
			"surface-subtle": purple[50],
			"surface-subtle-hover": "#EDD0F1",
			"surface-accent": purple[400],
			"on-surface-subtle-primary": "#380673",
			"on-surface-subtle-secondary": purple[700]
		},
		dark: {
			main: purple[700],
			"surface-bold": "#5A0C70",
			"surface-bold-hover": "#751690",
			"on-surface-bold": "#FFFFFF",
			"surface-selected": purple[700],
			"surface-selected-hover": purple[800],
			"on-surface-selected": "#FFFFFF",
			"surface-subtle": "#2c153a",
			"surface-subtle-hover": "#361A47",
			"surface-accent": purple[300],
			"on-surface-subtle-primary": purple[50],
			"on-surface-subtle-secondary": purple[200]
		}
	},
	indigo: {
		light: {
			main: indigo[500],
			"surface-bold": "#bab4fc",
			"surface-bold-hover": "#A59DFB",
			"on-surface-bold": indigo[900],
			"surface-selected": indigo[500],
			"surface-selected-hover": indigo[600],
			"on-surface-selected": "#FFFFFF",
			"surface-subtle": indigo[50],
			"surface-subtle-hover": "#D3D6EE",
			"surface-accent": indigo[500],
			"on-surface-subtle-primary": indigo[900],
			"on-surface-subtle-secondary": indigo[700]
		},
		dark: {
			main: indigo[600],
			"surface-bold": "#19205E",
			"surface-bold-hover": "#2B316B",
			"on-surface-bold": "#FFFFFF",
			"surface-selected": indigo[600],
			"surface-selected-hover": "#4957B2",
			"on-surface-selected": "#FFFFFF",
			"surface-subtle": "#13163c",
			"surface-subtle-hover": "#191d49",
			"surface-accent": indigo[300],
			"on-surface-subtle-primary": indigo[50],
			"on-surface-subtle-secondary": indigo[200]
		}
	},
	blue: {
		light: {
			main: blue[600],
			"surface-bold": blue[200],
			"surface-bold-hover": "#5EB5FC",
			"on-surface-bold": "#0B3A84",
			"surface-selected": blue[700],
			"surface-selected-hover": blue[800],
			"on-surface-selected": "#FFFFFF",
			"surface-subtle": blue[50],
			"surface-subtle-hover": "#CAE5FC",
			"surface-accent": blue[700],
			"on-surface-subtle-primary": "#0B3A84",
			"on-surface-subtle-secondary": blue[800]
		},
		dark: {
			main: blue[600],
			"surface-bold": "#00347c",
			"surface-bold-hover": "#043668",
			"on-surface-bold": "#FFFFFF",
			"surface-selected": blue[700],
			"surface-selected-hover": blue[800],
			"on-surface-selected": "#FFFFFF",
			"surface-subtle": "#002040",
			"surface-subtle-hover": "#032547",
			"surface-accent": blue[800],
			"on-surface-subtle-primary": blue[50],
			"on-surface-subtle-secondary": blue[200]
		}
	},
	teal: {
		light: {
			main: teal[600],
			"surface-bold": teal[200],
			"surface-bold-hover": "#3DBEB2",
			"on-surface-bold": "#00473b",
			"surface-selected": teal[600],
			"surface-selected-hover": teal[700],
			"on-surface-selected": "#FFFFFF",
			"surface-subtle": teal[50],
			"surface-subtle-hover": "#C9E9E6",
			"surface-accent": teal[600],
			"on-surface-subtle-primary": teal[900],
			"on-surface-subtle-secondary": teal[800]
		},
		dark: {
			main: teal[700],
			"surface-bold": "#044036",
			"surface-bold-hover": "#034539",
			"on-surface-bold": "#FFFFFF",
			"surface-selected": teal[800],
			"surface-selected-hover": teal[800],
			"on-surface-selected": "#FFFFFF",
			"surface-subtle": "#023028",
			"surface-subtle-hover": "#03342c",
			"surface-accent": teal[700],
			"on-surface-subtle-primary": teal[50],
			"on-surface-subtle-secondary": teal[200]
		}
	},
	green: {
		light: {
			main: green[600],
			"surface-bold": green[200],
			"surface-bold-hover": "#80CF83",
			"on-surface-bold": "#164B1A",
			"surface-selected": "#2E7D32",
			"surface-selected-hover": "#316F35",
			"on-surface-selected": "#FFFFFF",
			"surface-subtle": green[50],
			"surface-subtle-hover": "#D5ECD6",
			"surface-accent": green[800],
			"on-surface-subtle-primary": "#164B1A",
			"on-surface-subtle-secondary": green[800]
		},
		dark: {
			main: green[700],
			"surface-bold": "#0F3D13",
			"surface-bold-hover": "#133515",
			"on-surface-bold": "#FFFFFF",
			"surface-selected": green[800],
			"surface-selected-hover": green[900],
			"on-surface-selected": "#FFFFFF",
			"surface-subtle": "#0e2810",
			"surface-subtle-hover": "#0D3010",
			"surface-accent": green[300],
			"on-surface-subtle-primary": green[50],
			"on-surface-subtle-secondary": green[200]
		}
	},
	lime: {
		light: {
			main: lime[700],
			"surface-bold": "#D0DF86",
			"surface-bold-hover": "#CAE059",
			"on-surface-bold": "#524b07",
			"surface-selected": "#959B1F",
			"surface-selected-hover": "#898F1D",
			"on-surface-selected": "#FFFFFF",
			"surface-subtle": lime[50],
			"surface-subtle-hover": "#F3F6D0",
			"surface-accent": lime[900],
			"on-surface-subtle-primary": "#625A0D",
			"on-surface-subtle-secondary": lime[900]
		},
		dark: {
			main: lime[800],
			"surface-bold": "#50490A",
			"surface-bold-hover": "#5E571D",
			"on-surface-bold": "#FFFFFF",
			"surface-selected": lime[900],
			"surface-selected-hover": "#706510",
			"on-surface-selected": "#FFFFFF",
			"surface-subtle": "#3c380d",
			"surface-subtle-hover": "#433F0F",
			"surface-accent": lime[300],
			"on-surface-subtle-primary": lime[50],
			"on-surface-subtle-secondary": lime[200]
		}
	},
	amber: {
		light: {
			main: "#FFB840",
			"surface-bold": amber[200],
			"surface-bold-hover": "#FFD75C",
			"on-surface-bold": "#6c4600",
			"surface-selected": "#e09312",
			"surface-selected-hover": "#E98300",
			"on-surface-selected": "#FFFFFF",
			"surface-subtle": "#FFF2CC",
			"surface-subtle-hover": "#FFECB2",
			"surface-accent": "#e09312",
			"on-surface-subtle-primary": "#6c4600",
			"on-surface-subtle-secondary": "#AA7F00"
		},
		dark: {
			main: "#FFB840",
			"surface-bold": "#8e5c00",
			"surface-bold-hover": "#9C6501",
			"on-surface-bold": "#FFFFFF",
			"surface-selected": amber[700],
			"surface-selected-hover": "#D58600",
			"on-surface-selected": "#FFFFFF",
			"surface-subtle": "#3d2504",
			"surface-subtle-hover": "#4D2E05",
			"surface-accent": amber[400],
			"on-surface-subtle-primary": amber[50],
			"on-surface-subtle-secondary": amber[100]
		}
	},
	orange: {
		light: {
			main: deepOrange[400],
			"surface-bold": "#ffb77c",
			"surface-bold-hover": "#FFA55C",
			"on-surface-bold": "#A73B00",
			"surface-selected": deepOrange[400],
			"on-surface-selected": "#FFFFFF",
			"surface-selected-hover": deepOrange[500],
			"surface-subtle": "#ffe3cc",
			"surface-subtle-hover": "#FFD5B2",
			"surface-accent": deepOrange[400],
			"on-surface-subtle-primary": "#A73B00",
			"on-surface-subtle-secondary": deepOrange[700]
		},
		dark: {
			main: deepOrange[700],
			"surface-bold": "#6D2510",
			"surface-bold-hover": "#7D351F",
			"on-surface-bold": "#FFFFFF",
			"surface-selected": deepOrange[600],
			"surface-selected-hover": "#E85F14",
			"on-surface-selected": "#FFFFFF",
			"surface-subtle": "#4b1a0b",
			"surface-subtle-hover": "#7D351F",
			"surface-accent": deepOrange[600],
			"on-surface-subtle-primary": deepOrange[50],
			"on-surface-subtle-secondary": deepOrange[100]
		}
	},
	grey: {
		light: {
			main: grey[400],
			"surface-bold": grey[400],
			"surface-bold-hover": grey[500],
			"on-surface-bold": grey[900],
			"surface-selected": grey[600],
			"surface-selected-hover": grey[700],
			"on-surface-selected": "#FFFFFF",
			"surface-subtle": grey[100],
			"surface-subtle-hover": grey[200],
			"surface-accent": grey[800],
			"on-surface-subtle-primary": grey[900],
			"on-surface-subtle-secondary": grey[800]
		},
		dark: {
			main: grey[600],
			"surface-bold": grey[800],
			"surface-bold-hover": "#4f4f4f",
			"on-surface-bold": "#FFFFFF",
			"surface-selected": grey[700],
			"surface-selected-hover": grey[800],
			"on-surface-selected": "#FFFFFF",
			"surface-subtle": "#1d1d1d",
			"surface-subtle-hover": grey[900],
			"surface-accent": grey[400],
			"on-surface-subtle-primary": grey[50],
			"on-surface-subtle-secondary": grey[400]
		}
	}
};
/**
* Generates palette variants for MUI styled components.
* Use with styled components: `variants: getPaletteVariants(theme)`
*/
/**
* Returns the focus-visible background style for grid cells (day cells, time columns, month cells).
*/
var getCellFocusBackground = (theme) => theme.alpha((theme.vars || theme).palette.primary.light, .12);
var getPaletteVariants = (theme) => {
	return Object.entries(eventColorTokens).map(([colorName, colorValues]) => ({
		props: { "data-palette": colorName },
		style: _extends({
			"--event-main": colorValues.light.main,
			"--event-surface-bold": colorValues.light["surface-bold"],
			"--event-on-surface-bold": colorValues.light["on-surface-bold"],
			"--event-surface-bold-hover": colorValues.light["surface-bold-hover"] || "color-mix(in srgb, var(--event-surface-bold), black 4%);",
			"--event-surface-subtle": colorValues.light["surface-subtle"],
			"--event-surface-subtle-hover": colorValues.light["surface-subtle-hover"],
			"--event-surface-accent": colorValues.light["surface-accent"],
			"--event-on-surface-subtle-primary": colorValues.light["on-surface-subtle-primary"],
			"--event-on-surface-subtle-secondary": colorValues.light["on-surface-subtle-secondary"],
			"--event-surface-selected": colorValues.light["surface-selected"],
			"--event-surface-selected-hover": colorValues.light["surface-selected-hover"],
			"--event-on-surface-selected": colorValues.light["on-surface-selected"]
		}, theme.applyStyles("dark", {
			"--event-main": colorValues.dark.main,
			"--event-surface-bold": colorValues.dark["surface-bold"],
			"--event-on-surface-bold": colorValues.dark["on-surface-bold"],
			"--event-surface-bold-hover": colorValues.dark["surface-bold-hover"],
			"--event-surface-subtle": colorValues.dark["surface-subtle"],
			"--event-surface-subtle-hover": colorValues.dark["surface-subtle-hover"],
			"--event-surface-accent": colorValues.dark["surface-accent"],
			"--event-on-surface-subtle-primary": colorValues.dark["on-surface-subtle-primary"],
			"--event-on-surface-subtle-secondary": colorValues.dark["on-surface-subtle-secondary"],
			"--event-surface-selected": colorValues.dark["surface-selected"],
			"--event-surface-selected-hover": colorValues.dark["surface-selected-hover"],
			"--event-on-surface-selected": colorValues.dark["on-surface-selected"]
		}))
	}));
};
var LEFT_ARROW_CLIP = `polygon(8px 0, 100% 0, 100% 100%, 8px 100%, 0 50%)`;
var RIGHT_ARROW_CLIP = `polygon(0 0, calc(100% - 8px) 0, 100% 50%, calc(100% - 8px) 100%, 0 100%)`;
var BOTH_ARROWS_CLIP = `polygon(8px 0, calc(100% - 8px) 0, 100% 50%, calc(100% - 8px) 100%, 8px 100%, 0 50%)`;
//#endregion
//#region node_modules/@mui/x-scheduler/internals/components/SharedComponentsStyledContext.mjs
/**
* Styling injected by the consuming product into the shared internal components
* (e.g. `ErrorContainer`, `EventSkeleton`) so they stay decoupled from any
* product-specific styled context.
*/
var SharedComponentsStyledContext = /*#__PURE__*/ import_react.createContext(null);
SharedComponentsStyledContext.displayName = "SharedComponentsStyledContext";
function useSharedComponentsStyledContext() {
	const value = import_react.useContext(SharedComponentsStyledContext);
	if (!value) throw new Error("MUI X Scheduler: useSharedComponentsStyledContext must be used within a SharedComponentsStyledContext.Provider. The shared internal components require the product to inject their utility classes. Ensure the component is rendered inside an EventCalendar, EventCalendarPremium, or EventTimelinePremium component, a standalone view, or another component that provides SharedComponentsStyledContext.");
	return value;
}
//#endregion
//#region node_modules/@mui/x-scheduler/internals/components/event-skeleton/EventSkeleton.mjs
var EventSkeletonRoot = styled(Skeleton, {
	name: "MuiEventSkeleton",
	slot: "Root"
})(({ theme }) => {
	const variantStyles = {
		"time-column": {
			position: "absolute",
			left: 0,
			right: 12,
			top: 0,
			height: "100%",
			borderRadius: 0
		},
		"day-grid": { height: 18 },
		agenda: { height: 28 },
		"timeline-row": {
			height: `calc(${theme.typography.body2.lineHeight}em + ${theme.spacing(1.125)})`,
			width: "100%"
		}
	};
	return _extends({ opacity: .5 }, Object.fromEntries(Object.entries(variantStyles).map(([variant, style]) => [`&[data-variant="${variant}"]`, style])));
});
function EventSkeleton(props) {
	const { classes } = useSharedComponentsStyledContext();
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(EventSkeletonRoot, _extends({
		variant: "rounded",
		animation: "wave",
		"aria-hidden": true
	}, props, { className: clsx(classes.eventSkeleton, props.className) }));
}
//#endregion
//#region node_modules/@mui/x-scheduler/internals/components/event-skeleton/eventSkeletonClasses.mjs
var eventSkeletonClassKeys = ["eventSkeleton"];
var eventSkeletonSlots = { eventSkeleton: ["eventSkeleton"] };
//#endregion
//#region node_modules/@atlaskit/pragmatic-drag-and-drop/dist/esm/public-utils/prevent-unhandled.js
function acceptDrop(event) {
	if (event.defaultPrevented) return;
	if (event.dataTransfer) event.dataTransfer.dropEffect = "move";
	event.preventDefault();
}
var unbindEvents = null;
/**
* Block drag operations outside of `@atlaskit/pragmatic-drag-and-drop`
*/
function start() {
	cleanup();
	unbindEvents = (0, import_dist.bindAll)(window, [
		{
			type: "dragover",
			listener: acceptDrop
		},
		{
			type: "dragenter",
			listener: acceptDrop
		},
		{
			type: "drop",
			listener: function listener(event) {
				event.preventDefault();
				cleanup();
			}
		},
		{
			type: "dragend",
			listener: cleanup
		}
	].concat(_toConsumableArray(getBindingsForBrokenDrags({ onDragEnd: cleanup }))), { capture: false });
}
function cleanup() {
	var _unbindEvents;
	(_unbindEvents = unbindEvents) === null || _unbindEvents === void 0 || _unbindEvents();
	unbindEvents = null;
}
/**
* TODO: for next major, we could look at do the following:
*
* ```diff
* - preventUnhandled.start();
* - preventUnhandled.stop();
* + const stop = preventUnhandled();
* ```
*/
function stop() {
	var _window$event;
	/**
	* if `stop()` is called in a `"drop"` event, then `event.preventDefault()` won't be called.
	* Our `"drop"` listener calls `event.preventDefault()` for handled drop events
	* ("drop" events caused by dropping over a drop target)
	* `preventUnhandled()` causes every element to become a drop target (according to the browser)
	*
	* To opt out of the default behaviour for a `"drop"` event, we need to make sure
	* that we cancel it.
	*
	* The `"drop"` event listener in core is in the `capture` phase, so people calling
	* `preventUnhandled.stop()` in `onDrop()` will remove the `"drop"` event listener in this
	* file before it has the chance to cancel the event.
	*
	* Being sneaky and using the `window.event` global to sniff out the current event
	*/
	if (((_window$event = window.event) === null || _window$event === void 0 ? void 0 : _window$event.type) === "drop") {
		var _window$event2;
		(_window$event2 = window.event) === null || _window$event2 === void 0 || _window$event2.preventDefault();
	}
	cleanup();
}
var preventUnhandled = {
	start,
	stop
};
//#endregion
//#region node_modules/@mui/x-scheduler-internals/use-draggable-dialog/useDraggableDialog.mjs
var getDeltas = (location) => {
	return {
		deltaX: location.current.input.clientX - location.initial.input.clientX,
		deltaY: location.current.input.clientY - location.initial.input.clientY
	};
};
function useDraggableDialog(elementRef, handleRef, mutateStyle) {
	const offset = import_react.useRef({
		x: 0,
		y: 0
	});
	const resetDrag = import_react.useCallback(() => {
		offset.current = {
			x: 0,
			y: 0
		};
		if (elementRef.current) mutateStyle("none");
	}, [elementRef, mutateStyle]);
	import_react.useEffect(() => {
		const element = elementRef.current;
		if (!element) return;
		return draggable({
			element,
			dragHandle: handleRef.current || void 0,
			canDrag: ({ input }) => {
				return !document.elementFromPoint(input.clientX, input.clientY)?.closest("input, textarea, select, [contenteditable=\"true\"]");
			},
			onGenerateDragPreview: ({ nativeSetDragImage }) => {
				disableNativeDragPreview({ nativeSetDragImage });
			},
			onDragStart: () => {
				preventUnhandled.start();
				element.setAttribute("data-dragging", "true");
			},
			onDrag: ({ location }) => {
				const { deltaX, deltaY } = getDeltas(location);
				const x = offset.current.x + deltaX;
				const y = offset.current.y + deltaY;
				if (elementRef.current) mutateStyle(`translate(${x}px, ${y}px)`);
			},
			onDrop: ({ location }) => {
				preventUnhandled.stop();
				element.removeAttribute("data-dragging");
				const { deltaX, deltaY } = getDeltas(location);
				offset.current.x += deltaX;
				offset.current.y += deltaY;
			}
		});
	}, [
		elementRef,
		mutateStyle,
		handleRef
	]);
	return resetDrag;
}
//#endregion
//#region node_modules/@mui/x-scheduler/internals/components/create-modal/createModal.mjs
function createModal(config) {
	const Context = /*#__PURE__*/ import_react.createContext(void 0);
	Context.displayName = "Context";
	function useModalContext() {
		const context = import_react.useContext(Context);
		if (!context) throw new Error(`MUI X Scheduler: \`${config.contextName}\` is missing. Hook must be placed within its Provider.`);
		return context;
	}
	function Provider(props) {
		const { children, render, onOpen: onOpenProp, onClose: onCloseProp } = props;
		const anchorRef = import_react.useRef(null);
		const eventManager = import_react.useRef(new EventManager());
		const [state, setState] = import_react.useState({
			isOpen: false,
			data: null
		});
		const onOpen = useStableCallback((forwardedAnchorRef, data) => {
			anchorRef.current = forwardedAnchorRef?.current ?? null;
			setState({
				isOpen: true,
				data
			});
			onOpenProp?.(data);
			eventManager.current.emit("open", {
				data,
				anchorRef: anchorRef.current
			});
		});
		const onClose = useStableCallback(() => {
			onCloseProp?.();
			setState({
				isOpen: false,
				data: null
			});
			eventManager.current.emit("close");
		});
		const subscribeCloseHandler = import_react.useCallback((handler) => {
			eventManager.current.on("close", handler);
			return () => {
				eventManager.current.removeListener("close", handler);
			};
		}, []);
		const contextValue = import_react.useMemo(() => ({
			onOpen,
			onClose,
			isOpen: state.isOpen || false,
			data: state.data,
			subscribeCloseHandler
		}), [
			onOpen,
			onClose,
			state.isOpen,
			state.data,
			subscribeCloseHandler
		]);
		return /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(Context.Provider, {
			value: contextValue,
			children: [children, state.data && anchorRef.current && render({
				isOpen: Boolean(state.isOpen),
				anchorRef,
				data: state.data,
				onClose
			})]
		});
	}
	const Trigger = /*#__PURE__*/ import_react.forwardRef(function Trigger(props, ref) {
		const { data, onClick, children } = props;
		const { onOpen } = useModalContext();
		return /*#__PURE__*/ import_react.cloneElement(children, {
			ref,
			onClick: (event) => {
				onClick?.(event);
				onOpen(ref, data);
			}
		});
	});
	Trigger.displayName = "Trigger";
	return {
		Context,
		useContext: useModalContext,
		Provider,
		Trigger
	};
}
//#endregion
//#region node_modules/@mui/x-scheduler/internals/components/event-dialog/EventDialogStyledContext.mjs
var EventDialogStyledContext = /*#__PURE__*/ import_react.createContext(null);
EventDialogStyledContext.displayName = "EventDialogStyledContext";
function useEventDialogStyledContext() {
	const value = import_react.useContext(EventDialogStyledContext);
	if (!value) throw new Error("MUI X Scheduler: useEventDialogStyledContext must be used within an EventDialogStyledContext.Provider. The component requires access to dialog styling and locale information. Ensure the component is rendered inside an EventDialog component.");
	return value;
}
//#endregion
//#region node_modules/@mui/x-scheduler/internals/components/event-dialog/EventDialogOptionalRenderersContext.mjs
/**
* Components optionally rendered inside `EventDialog`. The premium scheduler fills these
* via the `optionalRenderers` prop on `EventDialogProvider`; community leaves them empty.
*/
var EventDialogOptionalRenderersContext = /*#__PURE__*/ import_react.createContext({});
EventDialogOptionalRenderersContext.displayName = "EventDialogOptionalRenderersContext";
function useEventDialogOptionalRenderers() {
	return import_react.useContext(EventDialogOptionalRenderersContext);
}
//#endregion
//#region node_modules/@mui/x-scheduler/internals/components/event-dialog/utils.mjs
var WEEKDAYS = [
	"sunday",
	"monday",
	"tuesday",
	"wednesday",
	"thursday",
	"friday",
	"saturday"
];
var getWeekdayToken = (adapter, value) => {
	return WEEKDAYS[adapter.toJsDate(value).getDay()];
};
function computeRange(adapter, next, displayTimezone) {
	if (next.allDay) return {
		start: next.startDate === "" ? adapter.now(displayTimezone) : adapter.startOfDay(adapter.date(next.startDate, displayTimezone)),
		end: next.endDate === "" ? adapter.now(displayTimezone) : adapter.endOfDay(adapter.date(next.endDate, displayTimezone)),
		surfaceType: "day-grid"
	};
	return {
		start: next.startDate === "" || next.startTime === "" ? adapter.now(displayTimezone) : adapter.date(`${next.startDate}T${next.startTime}`, displayTimezone),
		end: next.endDate === "" || next.endTime === "" ? adapter.now(displayTimezone) : adapter.date(`${next.endDate}T${next.endTime}`, displayTimezone),
		surfaceType: "time-grid"
	};
}
function validateRange(adapter, start, end, allDay) {
	const startDay = adapter.startOfDay(start);
	const endDay = adapter.startOfDay(end);
	if (adapter.isAfter(startDay, endDay)) return { field: "startDate" };
	if (adapter.isEqual(startDay, endDay)) {
		if (!allDay && !adapter.isAfter(end, start)) return { field: "startTime" };
	}
	return null;
}
function getRecurrenceLabel(adapter, start, recurrenceKey, localeText) {
	if (!recurrenceKey) return localeText.recurrenceNoRepeat;
	switch (recurrenceKey) {
		case "DAILY": return localeText.recurrenceDailyPresetLabel;
		case "WEEKLY": {
			const weekday = getWeekdayToken(adapter, start.value);
			const weekdayName = adapter.format(start.value, "weekday");
			return localeText.recurrenceWeeklyPresetLabel({
				weekday,
				weekdayName
			});
		}
		case "MONTHLY": {
			const date = adapter.getDate(start.value);
			return localeText.recurrenceMonthlyPresetLabel(date);
		}
		case "YEARLY": {
			const normalDate = formatDayOfMonthAndMonthFullLetter(start.value, adapter);
			return localeText.recurrenceYearlyPresetLabel(normalDate);
		}
		case "custom": return localeText.recurrenceCustomRepeat;
		default: return localeText.recurrenceNoRepeat;
	}
}
function hasProp(obj, prop) {
	return prop in obj;
}
//#endregion
//#region node_modules/@mui/icons-material/CloseRounded.mjs
var CloseRounded_default = createSvgIcon(/*#__PURE__*/ (0, import_jsx_runtime.jsx)("path", { d: "M18.3 5.71a.996.996 0 0 0-1.41 0L12 10.59 7.11 5.7a.996.996 0 0 0-1.41 0c-.39.39-.39 1.02 0 1.41L10.59 12 5.7 16.89c-.39.39-.39 1.02 0 1.41s1.02.39 1.41 0L12 13.41l4.89 4.89c.39.39 1.02.39 1.41 0s.39-1.02 0-1.41L13.41 12l4.89-4.89c.38-.38.38-1.02 0-1.4" }), "CloseRounded");
//#endregion
//#region node_modules/@mui/x-scheduler/internals/components/event-dialog/EventDialogHeader.mjs
var EventDialogHeaderRoot = styled("header", {
	name: "MuiEventDialog",
	slot: "Header"
})(({ theme }) => ({
	display: "flex",
	alignItems: "center",
	justifyContent: "space-between",
	gap: theme.spacing(1.5),
	padding: theme.spacing(3),
	cursor: "move"
}));
function EventDialogHeader(props) {
	const { children, onClose, dragHandlerRef } = props;
	const { classes, localeText } = useEventDialogStyledContext();
	return /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(EventDialogHeaderRoot, {
		ref: dragHandlerRef,
		className: classes.eventDialogHeader,
		children: [children, /*#__PURE__*/ (0, import_jsx_runtime.jsx)(IconButton, {
			className: classes.eventDialogCloseButton,
			"aria-label": localeText.closeButtonAriaLabel,
			onClick: onClose,
			children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(CloseRounded_default, {})
		})]
	});
}
//#endregion
//#region node_modules/@mui/icons-material/Check.mjs
var Check_default = createSvgIcon(/*#__PURE__*/ (0, import_jsx_runtime.jsx)("path", { d: "M9 16.17 4.83 12l-1.42 1.41L9 19 21 7l-1.41-1.41z" }), "Check");
//#endregion
//#region node_modules/@base-ui/utils/useControlled.mjs
function useControlled({ controlled, default: defaultProp, name, state = "value" }) {
	const { current: isControlled } = import_react.useRef(controlled !== void 0);
	const [valueState, setValue] = import_react.useState(defaultProp);
	const value = isControlled ? controlled : valueState;
	{
		import_react.useEffect(() => {
			if (isControlled !== (controlled !== void 0)) error([
				`A component is changing the ${isControlled ? "" : "un"}controlled ${state} state of ${name} to be ${isControlled ? "un" : ""}controlled.`,
				"Elements should not switch from uncontrolled to controlled (or vice versa).",
				`Decide between using a controlled or uncontrolled ${name} element for the lifetime of the component.`,
				"The nature of the state is determined during the first render. It's considered controlled if the value is not `undefined`.",
				"More info: https://fb.me/react-controlled-components"
			].join("\n"));
		}, [
			state,
			name,
			controlled
		]);
		const { current: defaultValue } = import_react.useRef(defaultProp);
		import_react.useEffect(() => {
			if (!isControlled && serializeToDevModeString(defaultValue) !== serializeToDevModeString(defaultProp)) error([`A component is changing the default ${state} state of an uncontrolled ${name} after being initialized. To suppress this warning opt to use a controlled ${name}.`].join("\n"));
		}, [defaultProp]);
	}
	return [value, import_react.useCallback((newValue) => {
		if (!isControlled) setValue(newValue);
	}, [])];
}
function serializeToDevModeString(input) {
	let nextId = 0;
	const seen = /* @__PURE__ */ new WeakMap();
	try {
		return JSON.stringify(input, function replacer(key, value) {
			if (key === "_owner" && this != null && typeof this === "object" && "$$typeof" in this) return;
			if (typeof value === "bigint") return `__bigint__:${value}`;
			if (value !== null && typeof value === "object") {
				const id = seen.get(value);
				if (id !== void 0) return `__object__:${id}`;
				seen.set(value, nextId);
				nextId += 1;
			}
			return value;
		}) ?? `__top__:${typeof input}`;
	} catch {
		return "__unserializable__";
	}
}
//#endregion
//#region node_modules/@base-ui/react/internals/useBaseUiId.mjs
/**
* Wraps `useId` and prefixes generated `id`s with `base-ui-`
* @param {string | undefined} idOverride overrides the generated id when provided
* @returns {string | undefined}
*/
function useBaseUiId(idOverride) {
	return useId(idOverride, "base-ui");
}
//#endregion
//#region node_modules/@base-ui/react/toggle-group/ToggleGroupContext.mjs
var ToggleGroupContext = /*#__PURE__*/ import_react.createContext(void 0);
ToggleGroupContext.displayName = "ToggleGroupContext";
function useToggleGroupContext(optional = true) {
	const context = import_react.useContext(ToggleGroupContext);
	if (context === void 0 && !optional) throw new Error("Base UI: ToggleGroupContext is missing. ToggleGroup parts must be placed within <ToggleGroup>.");
	return context;
}
//#endregion
//#region node_modules/@base-ui/react/internals/composite/list/CompositeListContext.mjs
var CompositeListContext$1 = /*#__PURE__*/ import_react.createContext({
	register: () => {},
	unregister: () => {},
	subscribeMapChange: () => {
		return () => {};
	},
	elementsRef: { current: [] },
	nextIndexRef: { current: 0 }
});
CompositeListContext$1.displayName = "CompositeListContext";
function useCompositeListContext$1() {
	return import_react.useContext(CompositeListContext$1);
}
//#endregion
//#region node_modules/@base-ui/react/internals/composite/list/useCompositeListItem.mjs
var IndexGuessBehavior$1 = /*#__PURE__*/ function(IndexGuessBehavior) {
	IndexGuessBehavior[IndexGuessBehavior["None"] = 0] = "None";
	IndexGuessBehavior[IndexGuessBehavior["GuessFromOrder"] = 1] = "GuessFromOrder";
	return IndexGuessBehavior;
}({});
/**
* Used to register a list item and its index (DOM position) in the `CompositeList`.
*/
function useCompositeListItem$1(params = {}) {
	const { label, metadata, textRef, indexGuessBehavior, index: externalIndex } = params;
	const { register, unregister, subscribeMapChange, elementsRef, labelsRef, nextIndexRef } = useCompositeListContext$1();
	const indexRef = import_react.useRef(-1);
	const [index, setIndex] = import_react.useState(externalIndex ?? (indexGuessBehavior === IndexGuessBehavior$1.GuessFromOrder ? () => {
		if (indexRef.current === -1) {
			const newIndex = nextIndexRef.current;
			nextIndexRef.current += 1;
			indexRef.current = newIndex;
		}
		return indexRef.current;
	} : -1));
	const componentRef = import_react.useRef(null);
	const ref = import_react.useCallback((node) => {
		componentRef.current = node;
		if (index !== -1 && node !== null) {
			elementsRef.current[index] = node;
			if (labelsRef) {
				const isLabelDefined = label !== void 0;
				labelsRef.current[index] = isLabelDefined ? label : textRef?.current?.textContent ?? node.textContent;
			}
		}
	}, [
		index,
		elementsRef,
		labelsRef,
		label,
		textRef
	]);
	useIsoLayoutEffect(() => {
		if (externalIndex != null) return;
		const node = componentRef.current;
		if (node) {
			register(node, metadata);
			return () => {
				unregister(node);
			};
		}
	}, [
		externalIndex,
		register,
		unregister,
		metadata
	]);
	useIsoLayoutEffect(() => {
		if (externalIndex != null) return;
		return subscribeMapChange((map) => {
			const i = componentRef.current ? map.get(componentRef.current)?.index : null;
			if (i != null) setIndex(i);
		});
	}, [
		externalIndex,
		subscribeMapChange,
		setIndex
	]);
	return {
		ref,
		index
	};
}
//#endregion
//#region node_modules/@base-ui/react/internals/composite/item/useCompositeItem.mjs
function useCompositeItem(params = {}) {
	const { highlightItemOnHover, highlightedIndex, onHighlightedIndexChange } = useCompositeRootContext$1();
	const { ref, index } = useCompositeListItem$1(params);
	const isHighlighted = highlightedIndex === index;
	const itemRef = import_react.useRef(null);
	const mergedRef = useMergedRefs(ref, itemRef);
	return {
		compositeProps: {
			tabIndex: isHighlighted ? 0 : -1,
			onFocus() {
				onHighlightedIndexChange(index);
			},
			onMouseMove() {
				const item = itemRef.current;
				if (!highlightItemOnHover || !item) return;
				const disabled = item.hasAttribute("disabled") || item.ariaDisabled === "true";
				if (!isHighlighted && !disabled) item.focus();
			}
		},
		compositeRef: mergedRef,
		index
	};
}
//#endregion
//#region node_modules/@base-ui/react/internals/composite/item/CompositeItem.mjs
/**
* @internal
*/
function CompositeItem(componentProps) {
	const { render, className, style, state = EMPTY_OBJECT, props = EMPTY_ARRAY$1, refs = EMPTY_ARRAY$1, metadata, stateAttributesMapping, tag = "div", ...elementProps } = componentProps;
	const { compositeProps, compositeRef } = useCompositeItem({ metadata });
	return useRenderElement$1(tag, componentProps, {
		state,
		ref: [...refs, compositeRef],
		props: [
			compositeProps,
			...props,
			elementProps
		],
		stateAttributesMapping
	});
}
//#endregion
//#region node_modules/@base-ui/react/internals/reason-parts.mjs
var none = "none";
//#endregion
//#region node_modules/@base-ui/react/internals/createBaseUIEventDetails.mjs
/**
* Maps a change `reason` string to the corresponding native event type.
*/
/**
* Details of custom change events emitted by Base UI components.
*/
/**
* Details of custom generic events emitted by Base UI components.
*/
/**
* Creates a Base UI event details object with the given reason and utilities
* for preventing Base UI's internal event handling.
*/
function createChangeEventDetails(reason, event, trigger, customProperties) {
	let canceled = false;
	let allowPropagation = false;
	const custom = customProperties ?? EMPTY_OBJECT;
	return {
		reason,
		event: event ?? new Event("base-ui"),
		cancel() {
			canceled = true;
		},
		allowPropagation() {
			allowPropagation = true;
		},
		get isCanceled() {
			return canceled;
		},
		get isPropagationAllowed() {
			return allowPropagation;
		},
		trigger,
		...custom
	};
}
//#endregion
//#region node_modules/@base-ui/react/toggle/Toggle.mjs
/**
* A two-state button that can be on or off.
* Renders a `<button>` element.
*
* Documentation: [Base UI Toggle](https://base-ui.com/react/components/toggle)
*/
var Toggle = /*#__PURE__*/ import_react.forwardRef(function Toggle(componentProps, forwardedRef) {
	const { className, defaultPressed: defaultPressedProp = false, disabled: disabledProp = false, form, onPressedChange, pressed: pressedProp, render, type, value: valueProp, nativeButton = true, style, ...elementProps } = componentProps;
	const value = useBaseUiId(valueProp || void 0);
	const groupContext = useToggleGroupContext();
	const groupValue = groupContext?.value ?? [];
	const defaultPressed = groupContext ? void 0 : defaultPressedProp;
	const disabled = (disabledProp || groupContext?.disabled) ?? false;
	useIsoLayoutEffect(() => {
		if (groupContext && valueProp === void 0 && groupContext.isValueInitialized) error("A `<Toggle>` component rendered in a `<ToggleGroup>` has no explicit `value` prop.", "This will cause issues between the Toggle Group and Toggle values.", "Provide the `<Toggle>` with a `value` prop matching the `<ToggleGroup>` values prop type.");
	}, [
		groupContext,
		valueProp,
		groupContext?.isValueInitialized
	]);
	const [pressed, setPressedState] = useControlled({
		controlled: groupContext ? value !== void 0 && groupValue.indexOf(value) > -1 : pressedProp,
		default: defaultPressed,
		name: "Toggle",
		state: "pressed"
	});
	const { getButtonProps, buttonRef } = useButton$1({
		disabled,
		native: nativeButton
	});
	const state = {
		disabled,
		pressed
	};
	const refs = [buttonRef, forwardedRef];
	const props = [
		{
			"aria-pressed": pressed,
			onClick(event) {
				const nextPressed = !pressed;
				const details = createChangeEventDetails(none, event.nativeEvent);
				onPressedChange?.(nextPressed, details);
				if (details.isCanceled) return;
				if (value) groupContext?.setGroupValue?.(value, nextPressed, details);
				if (details.isCanceled) return;
				setPressedState(nextPressed);
			}
		},
		elementProps,
		getButtonProps
	];
	const element = useRenderElement$1("button", componentProps, {
		enabled: !groupContext,
		state,
		ref: refs,
		props
	});
	const itemMetadata = import_react.useMemo(() => ({
		disabled,
		focusableWhenDisabled: false
	}), [disabled]);
	if (groupContext) return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(CompositeItem, {
		tag: "button",
		render,
		className,
		style,
		metadata: itemMetadata,
		state,
		refs,
		props
	});
	return element;
});
Toggle.displayName = "Toggle";
//#endregion
//#region node_modules/@base-ui/react/internals/composite/list/CompositeList.mjs
/**
* Provides context for a list of items in a composite component.
* @internal
*/
function CompositeList$1(props) {
	const { children, elementsRef, labelsRef, onMapChange: onMapChangeProp } = props;
	const onMapChange = useStableCallback(onMapChangeProp);
	const nextIndexRef = import_react.useRef(0);
	const listeners = useRefWithInit(createListeners$1).current;
	const map = useRefWithInit(createMap$1).current;
	const [mapTick, setMapTick] = import_react.useState(0);
	const lastTickRef = import_react.useRef(mapTick);
	const register = useStableCallback((node, metadata) => {
		map.set(node, metadata ?? null);
		lastTickRef.current += 1;
		setMapTick(lastTickRef.current);
	});
	const unregister = useStableCallback((node) => {
		map.delete(node);
		lastTickRef.current += 1;
		setMapTick(lastTickRef.current);
	});
	const sortedMap = import_react.useMemo(() => {
		const newMap = /* @__PURE__ */ new Map();
		Array.from(map.keys()).filter((node) => node.isConnected).sort(sortByDocumentPosition$1).forEach((node, index) => {
			const metadata = map.get(node) ?? {};
			newMap.set(node, {
				...metadata,
				index
			});
		});
		return newMap;
	}, [map, mapTick]);
	useIsoLayoutEffect(() => {
		if (typeof MutationObserver !== "function" || sortedMap.size === 0) return;
		const mutationObserver = new MutationObserver((entries) => {
			const diff = /* @__PURE__ */ new Set();
			const updateDiff = (node) => diff.has(node) ? diff.delete(node) : diff.add(node);
			entries.forEach((entry) => {
				entry.removedNodes.forEach(updateDiff);
				entry.addedNodes.forEach(updateDiff);
			});
			if (diff.size === 0) {
				lastTickRef.current += 1;
				setMapTick(lastTickRef.current);
			}
		});
		sortedMap.forEach((_, node) => {
			if (node.parentElement) mutationObserver.observe(node.parentElement, { childList: true });
		});
		return () => {
			mutationObserver.disconnect();
		};
	}, [sortedMap]);
	useIsoLayoutEffect(() => {
		if (lastTickRef.current === mapTick) {
			if (elementsRef.current.length !== sortedMap.size) elementsRef.current.length = sortedMap.size;
			if (labelsRef && labelsRef.current.length !== sortedMap.size) labelsRef.current.length = sortedMap.size;
			nextIndexRef.current = sortedMap.size;
		}
		onMapChange(sortedMap);
	}, [
		onMapChange,
		sortedMap,
		elementsRef,
		labelsRef,
		mapTick
	]);
	useIsoLayoutEffect(() => {
		return () => {
			elementsRef.current = [];
		};
	}, [elementsRef]);
	useIsoLayoutEffect(() => {
		return () => {
			if (labelsRef) labelsRef.current = [];
		};
	}, [labelsRef]);
	const subscribeMapChange = useStableCallback((fn) => {
		listeners.add(fn);
		return () => {
			listeners.delete(fn);
		};
	});
	useIsoLayoutEffect(() => {
		listeners.forEach((l) => l(sortedMap));
	}, [listeners, sortedMap]);
	const contextValue = import_react.useMemo(() => ({
		register,
		unregister,
		subscribeMapChange,
		elementsRef,
		labelsRef,
		nextIndexRef
	}), [
		register,
		unregister,
		subscribeMapChange,
		elementsRef,
		labelsRef,
		nextIndexRef
	]);
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(CompositeListContext$1.Provider, {
		value: contextValue,
		children
	});
}
function createMap$1() {
	return /* @__PURE__ */ new Map();
}
function createListeners$1() {
	return /* @__PURE__ */ new Set();
}
function sortByDocumentPosition$1(a, b) {
	const position = a.compareDocumentPosition(b);
	if (position & Node.DOCUMENT_POSITION_FOLLOWING || position & Node.DOCUMENT_POSITION_CONTAINED_BY) return -1;
	if (position & Node.DOCUMENT_POSITION_PRECEDING || position & Node.DOCUMENT_POSITION_CONTAINS) return 1;
	return 0;
}
//#endregion
//#region node_modules/@base-ui/utils/isElementDisabled.mjs
function isElementDisabled(element) {
	return element == null || element.hasAttribute("disabled") || element.getAttribute("aria-disabled") === "true";
}
//#endregion
//#region node_modules/@base-ui/react/internals/shadowDom.mjs
function getTarget(event) {
	if ("composedPath" in event) return event.composedPath()[0];
	return event.target;
}
//#endregion
//#region node_modules/@base-ui/react/floating-ui-react/utils/composite.mjs
function isIndexOutOfListBounds(list, index) {
	return index < 0 || index >= list.length;
}
function getMinListIndex(listRef, disabledIndices) {
	return findNonDisabledListIndex(listRef.current, { disabledIndices });
}
function getMaxListIndex(listRef, disabledIndices) {
	return findNonDisabledListIndex(listRef.current, {
		decrement: true,
		startingIndex: listRef.current.length,
		disabledIndices
	});
}
function findNonDisabledListIndex(list, { startingIndex = -1, decrement = false, disabledIndices, amount = 1 } = {}) {
	let index = startingIndex;
	do
		index += decrement ? -amount : amount;
	while (index >= 0 && index <= list.length - 1 && isListIndexDisabled(list, index, disabledIndices));
	return index;
}
function isListIndexDisabled(list, index, disabledIndices) {
	if (typeof disabledIndices === "function" ? disabledIndices(index) : disabledIndices?.includes(index) ?? false) return true;
	const element = list[index];
	if (!element) return false;
	if (!isElementVisible(element)) return true;
	return !disabledIndices && (element.hasAttribute("disabled") || element.getAttribute("aria-disabled") === "true");
}
function isHiddenByStyles(styles) {
	return styles.visibility === "hidden" || styles.visibility === "collapse";
}
function isElementVisible(element, styles = element ? getComputedStyle$1(element) : null) {
	if (!element || !element.isConnected || !styles || isHiddenByStyles(styles)) return false;
	if (typeof element.checkVisibility === "function") return element.checkVisibility();
	return styles.display !== "none" && styles.display !== "contents";
}
//#endregion
//#region node_modules/@base-ui/react/internals/composite/composite.mjs
var ARROW_UP = "ArrowUp";
var ARROW_DOWN = "ArrowDown";
var ARROW_LEFT = "ArrowLeft";
var ARROW_RIGHT = "ArrowRight";
var HOME = "Home";
var HORIZONTAL_KEYS = new Set([ARROW_LEFT, ARROW_RIGHT]);
var HORIZONTAL_KEYS_WITH_EXTRA_KEYS = new Set([
	ARROW_LEFT,
	ARROW_RIGHT,
	HOME,
	"End"
]);
var VERTICAL_KEYS = new Set([ARROW_UP, ARROW_DOWN]);
var VERTICAL_KEYS_WITH_EXTRA_KEYS = new Set([
	ARROW_UP,
	ARROW_DOWN,
	HOME,
	"End"
]);
var ARROW_KEYS = new Set([...HORIZONTAL_KEYS, ...VERTICAL_KEYS]);
var COMPOSITE_KEYS = new Set([
	...ARROW_KEYS,
	HOME,
	"End"
]);
var MODIFIER_KEYS = new Set([
	"Shift",
	"Control",
	"Alt",
	"Meta"
]);
function isInputElement(element) {
	return isHTMLElement(element) && element.tagName === "INPUT";
}
function isNativeInput(element) {
	if (isInputElement(element) && element.selectionStart != null) return true;
	if (isHTMLElement(element) && element.tagName === "TEXTAREA") return true;
	return false;
}
function scrollIntoViewIfNeeded(scrollContainer, element, direction, orientation) {
	if (!scrollContainer || !element || !element.scrollTo) return;
	let targetX = scrollContainer.scrollLeft;
	let targetY = scrollContainer.scrollTop;
	const isOverflowingX = scrollContainer.clientWidth < scrollContainer.scrollWidth;
	const isOverflowingY = scrollContainer.clientHeight < scrollContainer.scrollHeight;
	if (isOverflowingX && orientation !== "vertical") {
		const elementOffsetLeft = getOffset(scrollContainer, element, "left");
		const containerStyles = getStyles(scrollContainer);
		const elementStyles = getStyles(element);
		if (direction === "ltr") {
			if (elementOffsetLeft + element.offsetWidth + elementStyles.scrollMarginRight > scrollContainer.scrollLeft + scrollContainer.clientWidth - containerStyles.scrollPaddingRight) targetX = elementOffsetLeft + element.offsetWidth + elementStyles.scrollMarginRight - scrollContainer.clientWidth + containerStyles.scrollPaddingRight;
			else if (elementOffsetLeft - elementStyles.scrollMarginLeft < scrollContainer.scrollLeft + containerStyles.scrollPaddingLeft) targetX = elementOffsetLeft - elementStyles.scrollMarginLeft - containerStyles.scrollPaddingLeft;
		}
		if (direction === "rtl") {
			if (elementOffsetLeft - elementStyles.scrollMarginRight < scrollContainer.scrollLeft + containerStyles.scrollPaddingLeft) targetX = elementOffsetLeft - elementStyles.scrollMarginLeft - containerStyles.scrollPaddingLeft;
			else if (elementOffsetLeft + element.offsetWidth + elementStyles.scrollMarginRight > scrollContainer.scrollLeft + scrollContainer.clientWidth - containerStyles.scrollPaddingRight) targetX = elementOffsetLeft + element.offsetWidth + elementStyles.scrollMarginRight - scrollContainer.clientWidth + containerStyles.scrollPaddingRight;
		}
	}
	if (isOverflowingY && orientation !== "horizontal") {
		const elementOffsetTop = getOffset(scrollContainer, element, "top");
		const containerStyles = getStyles(scrollContainer);
		const elementStyles = getStyles(element);
		if (elementOffsetTop - elementStyles.scrollMarginTop < scrollContainer.scrollTop + containerStyles.scrollPaddingTop) targetY = elementOffsetTop - elementStyles.scrollMarginTop - containerStyles.scrollPaddingTop;
		else if (elementOffsetTop + element.offsetHeight + elementStyles.scrollMarginBottom > scrollContainer.scrollTop + scrollContainer.clientHeight - containerStyles.scrollPaddingBottom) targetY = elementOffsetTop + element.offsetHeight + elementStyles.scrollMarginBottom - scrollContainer.clientHeight + containerStyles.scrollPaddingBottom;
	}
	scrollContainer.scrollTo({
		left: targetX,
		top: targetY,
		behavior: "auto"
	});
}
function getOffset(ancestor, element, side) {
	const propName = side === "left" ? "offsetLeft" : "offsetTop";
	let result = 0;
	while (element.offsetParent) {
		result += element[propName];
		if (element.offsetParent === ancestor) break;
		element = element.offsetParent;
	}
	return result;
}
function getStyles(element) {
	const styles = getComputedStyle(element);
	return {
		scrollMarginTop: parseFloat(styles.scrollMarginTop) || 0,
		scrollMarginRight: parseFloat(styles.scrollMarginRight) || 0,
		scrollMarginBottom: parseFloat(styles.scrollMarginBottom) || 0,
		scrollMarginLeft: parseFloat(styles.scrollMarginLeft) || 0,
		scrollPaddingTop: parseFloat(styles.scrollPaddingTop) || 0,
		scrollPaddingRight: parseFloat(styles.scrollPaddingRight) || 0,
		scrollPaddingBottom: parseFloat(styles.scrollPaddingBottom) || 0,
		scrollPaddingLeft: parseFloat(styles.scrollPaddingLeft) || 0
	};
}
//#endregion
//#region node_modules/@base-ui/react/internals/composite/root/useCompositeRoot.mjs
var EMPTY_ARRAY = [];
function useCompositeRoot(params) {
	const { loopFocus = true, orientation = "both", grid, onLoop, direction, highlightedIndex: externalHighlightedIndex, onHighlightedIndexChange: externalSetHighlightedIndex, rootRef: externalRef, enableHomeAndEndKeys = false, stopEventPropagation = false, disabledIndices, modifierKeys = EMPTY_ARRAY } = params;
	const [internalHighlightedIndex, internalSetHighlightedIndex] = import_react.useState(0);
	const isGrid = grid != null;
	const rootRef = import_react.useRef(null);
	const mergedRef = useMergedRefs(rootRef, externalRef);
	const elementsRef = import_react.useRef([]);
	const hasSetDefaultIndexRef = import_react.useRef(false);
	const highlightedIndex = externalHighlightedIndex ?? internalHighlightedIndex;
	const onHighlightedIndexChange = useStableCallback((index, shouldScrollIntoView = false) => {
		(externalSetHighlightedIndex ?? internalSetHighlightedIndex)(index);
		if (shouldScrollIntoView) {
			const newActiveItem = elementsRef.current[index];
			scrollIntoViewIfNeeded(rootRef.current, newActiveItem, direction, orientation);
		}
	});
	const onMapChange = useStableCallback((map) => {
		if (map.size === 0 || hasSetDefaultIndexRef.current) return;
		hasSetDefaultIndexRef.current = true;
		const sortedElements = Array.from(map.keys());
		const activeItem = sortedElements.find((compositeElement) => compositeElement?.hasAttribute("data-composite-item-active")) ?? null;
		const activeIndex = activeItem ? sortedElements.indexOf(activeItem) : -1;
		if (activeIndex !== -1) onHighlightedIndexChange(activeIndex);
		else if (isListIndexDisabled(sortedElements, highlightedIndex, disabledIndices)) {
			const firstEnabledIndex = findNonDisabledListIndex(sortedElements, { disabledIndices });
			if (!isIndexOutOfListBounds(sortedElements, firstEnabledIndex)) onHighlightedIndexChange(firstEnabledIndex);
		}
		scrollIntoViewIfNeeded(rootRef.current, activeItem, direction, orientation);
	});
	useIsoLayoutEffect(() => {
		if (disabledIndices == null || externalHighlightedIndex != null || !hasSetDefaultIndexRef.current) return;
		const elements = elementsRef.current;
		if (isListIndexDisabled(elements, highlightedIndex, disabledIndices)) {
			const firstEnabledIndex = findNonDisabledListIndex(elements, { disabledIndices });
			if (!isIndexOutOfListBounds(elements, firstEnabledIndex)) onHighlightedIndexChange(firstEnabledIndex);
		}
	}, [
		disabledIndices,
		externalHighlightedIndex,
		highlightedIndex,
		elementsRef,
		onHighlightedIndexChange
	]);
	const wrappedOnLoop = useStableCallback((event, prevIndex, nextIndex) => {
		if (!onLoop) return nextIndex;
		return onLoop(event, prevIndex, nextIndex, elementsRef);
	});
	const onKeyDown = useStableCallback((event) => {
		const RELEVANT_KEYS = enableHomeAndEndKeys ? COMPOSITE_KEYS : ARROW_KEYS;
		if (!RELEVANT_KEYS.has(event.key)) return;
		if (isModifierKeySet(event, modifierKeys)) return;
		if (!rootRef.current) return;
		const isRtl = direction === "rtl";
		const horizontalForwardKey = isRtl ? ARROW_LEFT : ARROW_RIGHT;
		const forwardKey = {
			horizontal: horizontalForwardKey,
			vertical: ARROW_DOWN,
			both: horizontalForwardKey
		}[orientation];
		const horizontalBackwardKey = isRtl ? ARROW_RIGHT : ARROW_LEFT;
		const backwardKey = {
			horizontal: horizontalBackwardKey,
			vertical: ARROW_UP,
			both: horizontalBackwardKey
		}[orientation];
		const target = getTarget(event.nativeEvent);
		if (target != null && isNativeInput(target) && !isElementDisabled(target)) {
			const selectionStart = target.selectionStart;
			const selectionEnd = target.selectionEnd;
			const textContent = target.value ?? "";
			if (selectionStart == null || event.shiftKey || selectionStart !== selectionEnd) return;
			if (event.key !== backwardKey && selectionStart < textContent.length) return;
			if (event.key !== forwardKey && selectionStart > 0) return;
		}
		let nextIndex = highlightedIndex;
		const minIndex = getMinListIndex(elementsRef, disabledIndices);
		const maxIndex = getMaxListIndex(elementsRef, disabledIndices);
		if (grid != null) nextIndex = grid({
			disabledIndices,
			elementsRef,
			event,
			highlightedIndex,
			loopFocus,
			maxIndex,
			minIndex,
			onLoop: wrappedOnLoop,
			orientation,
			rtl: isRtl
		});
		const forwardKeys = {
			horizontal: [horizontalForwardKey],
			vertical: [ARROW_DOWN],
			both: [horizontalForwardKey, ARROW_DOWN]
		}[orientation];
		const backwardKeys = {
			horizontal: [horizontalBackwardKey],
			vertical: [ARROW_UP],
			both: [horizontalBackwardKey, ARROW_UP]
		}[orientation];
		const preventedKeys = isGrid ? RELEVANT_KEYS : {
			horizontal: enableHomeAndEndKeys ? HORIZONTAL_KEYS_WITH_EXTRA_KEYS : HORIZONTAL_KEYS,
			vertical: enableHomeAndEndKeys ? VERTICAL_KEYS_WITH_EXTRA_KEYS : VERTICAL_KEYS,
			both: RELEVANT_KEYS
		}[orientation];
		if (enableHomeAndEndKeys) {
			if (event.key === "Home") nextIndex = minIndex;
			else if (event.key === "End") nextIndex = maxIndex;
		}
		if (nextIndex === highlightedIndex && (forwardKeys.includes(event.key) || backwardKeys.includes(event.key))) if (loopFocus && nextIndex === maxIndex && forwardKeys.includes(event.key)) {
			nextIndex = minIndex;
			if (onLoop) nextIndex = onLoop(event, highlightedIndex, nextIndex, elementsRef);
		} else if (loopFocus && nextIndex === minIndex && backwardKeys.includes(event.key)) {
			nextIndex = maxIndex;
			if (onLoop) nextIndex = onLoop(event, highlightedIndex, nextIndex, elementsRef);
		} else nextIndex = findNonDisabledListIndex(elementsRef.current, {
			startingIndex: nextIndex,
			decrement: backwardKeys.includes(event.key),
			disabledIndices
		});
		if (nextIndex !== highlightedIndex && !isIndexOutOfListBounds(elementsRef.current, nextIndex)) {
			if (stopEventPropagation) event.stopPropagation();
			if (preventedKeys.has(event.key)) event.preventDefault();
			onHighlightedIndexChange(nextIndex, true);
			queueMicrotask(() => {
				elementsRef.current[nextIndex]?.focus();
			});
		}
	});
	return {
		props: {
			ref: mergedRef,
			onFocus(event) {
				const element = rootRef.current;
				const target = getTarget(event.nativeEvent);
				if (!element || target == null || !isNativeInput(target)) return;
				target.setSelectionRange(0, target.value.length ?? 0);
			},
			onKeyDown
		},
		highlightedIndex,
		onHighlightedIndexChange,
		elementsRef,
		disabledIndices,
		onMapChange,
		relayKeyboardEvent: onKeyDown
	};
}
function isModifierKeySet(event, ignoredModifierKeys) {
	for (const key of MODIFIER_KEYS.values()) {
		if (ignoredModifierKeys.includes(key)) continue;
		if (event.getModifierState(key)) return true;
	}
	return false;
}
//#endregion
//#region node_modules/@base-ui/react/internals/direction-context/DirectionContext.mjs
/**
* @internal
*/
var DirectionContext = /*#__PURE__*/ import_react.createContext(void 0);
DirectionContext.displayName = "DirectionContext";
function useDirection() {
	return import_react.useContext(DirectionContext)?.direction ?? "ltr";
}
//#endregion
//#region node_modules/@base-ui/react/internals/composite/root/CompositeRoot.mjs
/**
* @internal
*/
function CompositeRoot(componentProps) {
	const { render, className, style, refs = EMPTY_ARRAY$1, props = EMPTY_ARRAY$1, state = EMPTY_OBJECT, stateAttributesMapping, highlightedIndex: highlightedIndexProp, onHighlightedIndexChange: onHighlightedIndexChangeProp, orientation, grid, loopFocus, onLoop, enableHomeAndEndKeys, onMapChange: onMapChangeProp, stopEventPropagation = true, rootRef, disabledIndices, modifierKeys, highlightItemOnHover = false, tag = "div", ...elementProps } = componentProps;
	const { props: defaultProps, highlightedIndex, onHighlightedIndexChange, elementsRef, onMapChange: onMapChangeUnwrapped, relayKeyboardEvent } = useCompositeRoot({
		grid,
		loopFocus,
		onLoop,
		orientation,
		highlightedIndex: highlightedIndexProp,
		onHighlightedIndexChange: onHighlightedIndexChangeProp,
		rootRef,
		stopEventPropagation,
		enableHomeAndEndKeys,
		direction: useDirection(),
		disabledIndices,
		modifierKeys
	});
	const element = useRenderElement$1(tag, componentProps, {
		state,
		ref: refs,
		props: [
			defaultProps,
			...props,
			elementProps
		],
		stateAttributesMapping
	});
	const contextValue = import_react.useMemo(() => ({
		highlightedIndex,
		onHighlightedIndexChange,
		highlightItemOnHover,
		relayKeyboardEvent
	}), [
		highlightedIndex,
		onHighlightedIndexChange,
		highlightItemOnHover,
		relayKeyboardEvent
	]);
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(CompositeRootContext$1.Provider, {
		value: contextValue,
		children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(CompositeList$1, {
			elementsRef,
			onMapChange: (newMap) => {
				onMapChangeProp?.(newMap);
				onMapChangeUnwrapped(newMap);
			},
			children: element
		})
	});
}
//#endregion
//#region node_modules/@base-ui/react/toolbar/root/ToolbarRootContext.mjs
var ToolbarRootContext = /*#__PURE__*/ import_react.createContext(void 0);
ToolbarRootContext.displayName = "ToolbarRootContext";
function useToolbarRootContext(optional) {
	const context = import_react.useContext(ToolbarRootContext);
	if (context === void 0 && !optional) throw new Error("Base UI: ToolbarRootContext is missing. Toolbar parts must be placed within <Toolbar.Root>.");
	return context;
}
//#endregion
//#region node_modules/@base-ui/react/toolbar/group/ToolbarGroupContext.mjs
var ToolbarGroupContext = /*#__PURE__*/ import_react.createContext(void 0);
ToolbarGroupContext.displayName = "ToolbarGroupContext";
function useToolbarGroupContext(optional) {
	const context = import_react.useContext(ToolbarGroupContext);
	if (context === void 0 && !optional) throw new Error("Base UI: ToolbarGroupContext is missing. ToolbarGroup parts must be placed within <Toolbar.Group>.");
	return context;
}
//#endregion
//#region node_modules/@base-ui/react/toggle-group/ToggleGroupDataAttributes.mjs
var ToggleGroupDataAttributes = /*#__PURE__*/ function(ToggleGroupDataAttributes) {
	/**
	* Present when the toggle group is disabled.
	*/
	ToggleGroupDataAttributes["disabled"] = "data-disabled";
	/**
	* Indicates the orientation of the toggle group.
	* @type {'horizontal' | 'vertical'}
	*/
	ToggleGroupDataAttributes["orientation"] = "data-orientation";
	/**
	* Present when the toggle group allows multiple buttons to be in the pressed state at the same time.
	*/
	ToggleGroupDataAttributes["multiple"] = "data-multiple";
	return ToggleGroupDataAttributes;
}({});
//#endregion
//#region node_modules/@base-ui/react/toggle-group/ToggleGroup.mjs
var stateAttributesMapping = { multiple(value) {
	if (value) return { [ToggleGroupDataAttributes.multiple]: "" };
	return null;
} };
/**
* Provides a shared state to a series of toggle buttons.
*
* Documentation: [Base UI Toggle Group](https://base-ui.com/react/components/toggle-group)
*/
var ToggleGroup = /*#__PURE__*/ import_react.forwardRef(function ToggleGroup(componentProps, forwardedRef) {
	const { defaultValue: defaultValueProp, disabled: disabledProp = false, loopFocus = true, onValueChange, orientation = "horizontal", multiple = false, value: valueProp, className, render, style, ...elementProps } = componentProps;
	const toolbarContext = useToolbarRootContext(true);
	const toolbarGroupContext = useToolbarGroupContext(true);
	const isValueInitialized = import_react.useMemo(() => valueProp !== void 0 || defaultValueProp !== void 0, [valueProp, defaultValueProp]);
	const disabled = (toolbarContext?.disabled ?? false) || (toolbarGroupContext?.disabled ?? false) || disabledProp;
	const [groupValue, setValueState] = useControlled({
		controlled: valueProp,
		default: valueProp === void 0 ? defaultValueProp ?? EMPTY_ARRAY$1 : void 0,
		name: "ToggleGroup",
		state: "value"
	});
	const setGroupValue = useStableCallback((newValue, nextPressed, eventDetails) => {
		let newGroupValue;
		if (multiple) {
			newGroupValue = groupValue.slice();
			if (nextPressed) newGroupValue.push(newValue);
			else newGroupValue.splice(groupValue.indexOf(newValue), 1);
		} else newGroupValue = nextPressed ? [newValue] : [];
		onValueChange?.(newGroupValue, eventDetails);
		if (eventDetails.isCanceled) return;
		setValueState(newGroupValue);
	});
	const state = {
		disabled,
		multiple,
		orientation
	};
	const contextValue = import_react.useMemo(() => ({
		disabled,
		orientation,
		setGroupValue,
		value: groupValue,
		isValueInitialized
	}), [
		disabled,
		orientation,
		setGroupValue,
		groupValue,
		isValueInitialized
	]);
	const defaultProps = { role: "group" };
	const element = useRenderElement$1("div", componentProps, {
		enabled: Boolean(toolbarContext),
		state,
		ref: forwardedRef,
		props: [defaultProps, elementProps],
		stateAttributesMapping
	});
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ToggleGroupContext.Provider, {
		value: contextValue,
		children: toolbarContext ? element : /*#__PURE__*/ (0, import_jsx_runtime.jsx)(CompositeRoot, {
			render,
			className,
			style,
			state,
			refs: [forwardedRef],
			props: [defaultProps, elementProps],
			stateAttributesMapping,
			loopFocus,
			enableHomeAndEndKeys: true,
			orientation
		})
	});
});
ToggleGroup.displayName = "ToggleGroup";
//#endregion
//#region node_modules/@mui/x-scheduler/internals/components/event-dialog/ResourceAndColorSection.mjs
var NO_RESOURCE_VALUE = "";
var ResourceMenuItem = styled(MenuItem, {
	name: "MuiEventDialog",
	slot: "ResourceMenuItem"
})(({ theme }) => ({ paddingLeft: `calc(${theme.spacing(2)} + var(--resource-indent) * ${theme.spacing(2)})` }));
var ResourceMenuListSubheader = styled(ListSubheader, {
	name: "MuiEventDialog",
	slot: "ResourceMenuListSubheader"
})({});
var ResourceMenuColorDot = styled("span", {
	name: "MuiEventDialog",
	slot: "ResourceMenuColorDot"
})(({ theme }) => ({
	width: 14,
	height: 14,
	borderRadius: "2px",
	flexShrink: 0,
	backgroundColor: "var(--event-main)",
	variants: getPaletteVariants(theme),
	[`&[data-no-resource="true"]`]: {
		backgroundColor: "var(--event-surface-subtle)",
		border: "1.2px dashed var(--event-main)"
	}
}));
var ResourceMenuColorToggleGroup = styled(ToggleGroup, {
	name: "MuiEventDialog",
	slot: "ResourceMenuColorToggleGroup"
})(({ theme }) => ({
	display: "flex",
	flexWrap: "wrap",
	gap: theme.spacing(1),
	borderRadius: theme.shape.borderRadius
}));
var ResourceMenuColorToggle = styled(Toggle, {
	name: "MuiEventDialog",
	slot: "ResourceMenuColorToggle"
})(({ theme }) => ({
	width: 24,
	height: 24,
	borderRadius: theme.shape.borderRadius,
	border: "none",
	cursor: "pointer",
	display: "flex",
	alignItems: "center",
	justifyContent: "center",
	backgroundColor: "var(--event-main)",
	color: "white",
	"&:disabled": {
		cursor: "not-allowed",
		opacity: .5
	},
	variants: getPaletteVariants(theme)
}));
function ResourceSelectAdornment(props) {
	const { resource } = props;
	const store = useSchedulerStoreContext();
	const { classes } = useEventDialogStyledContext();
	const resourceColor = useStore(store, schedulerResourceSelectors.defaultEventColor, resource?.value);
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ResourceMenuColorDot, {
		className: classes.eventDialogResourceMenuColorDot,
		"data-palette": resourceColor,
		"data-no-resource": Boolean(resource?.value === null)
	});
}
function ResourceAndColorSection(props) {
	const { readOnly, resourceId, onResourceChange, onColorChange, color, error } = props;
	const { schedulerId, classes, localeText } = useEventDialogStyledContext();
	const store = useSchedulerStoreContext();
	const resources = useStore(store, schedulerResourceSelectors.processedResourceFlatList);
	const resourceDepthLookup = useStore(store, schedulerResourceSelectors.resourceDepthLookup);
	const childrenIdLookup = useStore(store, schedulerResourceSelectors.childrenIdLookup);
	const eventDefaultColor = useStore(store, schedulerOtherSelectors.defaultEventColor);
	const shouldEventRequireResource = useStore(store, schedulerOtherSelectors.shouldEventRequireResource);
	const resourcesOptions = import_react.useMemo(() => {
		const hasNesting = resources.some((resource) => (childrenIdLookup.get(resource.id)?.length ?? 0) > 0);
		const firstTopLevelIndex = resources.findIndex((resource) => (resourceDepthLookup.get(resource.id) ?? 0) === 0);
		return [{
			label: localeText.labelNoResource,
			value: null,
			eventColor: eventDefaultColor,
			isGroupRoot: false,
			indentLevel: 0,
			showDivider: false,
			hidden: shouldEventRequireResource
		}, ...resources.map((resource, index) => {
			const depth = resourceDepthLookup.get(resource.id) ?? 0;
			const hasChildren = (childrenIdLookup.get(resource.id)?.length ?? 0) > 0;
			const isTopLevel = depth === 0;
			const showDivider = hasNesting && isTopLevel && (!(index === firstTopLevelIndex) || !shouldEventRequireResource);
			return {
				label: resource.title,
				value: resource.id,
				eventColor: resource.eventColor ?? eventDefaultColor,
				isGroupRoot: isTopLevel && hasChildren,
				indentLevel: Math.max(0, depth - 1),
				showDivider
			};
		})];
	}, [
		resources,
		resourceDepthLookup,
		childrenIdLookup,
		localeText.labelNoResource,
		eventDefaultColor,
		shouldEventRequireResource
	]);
	const resource = import_react.useMemo(() => resourcesOptions.find((option) => resourceId ? option.value === resourceId : option.value === null) || null, [resourcesOptions, resourceId]);
	const handleChange = (event) => {
		const value = event.target.value;
		onResourceChange(value === NO_RESOURCE_VALUE ? null : value);
	};
	const errorId = `${schedulerId}-resource-error`;
	return /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(import_react.Fragment, { children: [/*#__PURE__*/ (0, import_jsx_runtime.jsxs)(FormControl, {
		size: "small",
		fullWidth: true,
		error: !!error,
		children: [
			/*#__PURE__*/ (0, import_jsx_runtime.jsx)(InputLabel, {
				id: `${schedulerId}-resource-select-label`,
				children: localeText.resourceLabel
			}),
			/*#__PURE__*/ (0, import_jsx_runtime.jsx)(Select, {
				labelId: `${schedulerId}-resource-select-label`,
				label: localeText.resourceLabel,
				value: resourceId ?? NO_RESOURCE_VALUE,
				displayEmpty: true,
				onChange: handleChange,
				readOnly,
				"aria-describedby": error ? errorId : void 0,
				startAdornment: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(InputAdornment, {
					position: "start",
					children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ResourceSelectAdornment, { resource })
				}),
				renderValue: () => {
					if (resource) return resource.label;
					if (resourceId == null) return localeText.labelNoResource;
					return localeText.labelInvalidResource;
				},
				children: resourcesOptions.flatMap((resourceOption) => {
					const items = [];
					if (resourceOption.showDivider) items.push(/*#__PURE__*/ (0, import_jsx_runtime.jsx)(Divider, {}, `divider-${resourceOption.value}`));
					if (resourceOption.isGroupRoot) items.push(/*#__PURE__*/ (0, import_jsx_runtime.jsx)(ResourceMenuListSubheader, {
						className: classes.eventDialogResourceMenuListSubheader,
						children: resourceOption.label.toUpperCase()
					}, `header-${resourceOption.value}`));
					items.push(/*#__PURE__*/ (0, import_jsx_runtime.jsxs)(ResourceMenuItem, {
						value: resourceOption.value ?? NO_RESOURCE_VALUE,
						"aria-label": resourceOption.label,
						className: classes.eventDialogResourceMenuItem,
						style: _extends({ "--resource-indent": resourceOption.indentLevel }, resourceOption.hidden && { display: "none" }),
						children: [/*#__PURE__*/ (0, import_jsx_runtime.jsx)(ListItemIcon, { children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ResourceMenuColorDot, {
							className: classes.eventDialogResourceMenuColorDot,
							"data-palette": resourceOption.eventColor,
							"data-no-resource": Boolean(resourceOption.value === null)
						}) }), /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ListItemText, { children: resourceOption.label })]
					}, resourceOption.value ?? NO_RESOURCE_VALUE));
					return items;
				})
			}),
			error && /*#__PURE__*/ (0, import_jsx_runtime.jsx)(FormHelperText, {
				id: errorId,
				role: "alert",
				children: error
			})
		]
	}), /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ResourceMenuColorToggleGroup, {
		value: color ? [color] : [],
		onValueChange: (values) => {
			const next = values[values.length - 1];
			onColorChange(next ?? null);
		},
		"aria-label": localeText.colorPickerLabel,
		disabled: readOnly,
		className: classes.eventDialogResourceMenuColorToggleGroup,
		children: EVENT_COLORS.map((colorOption) => /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ResourceMenuColorToggle, {
			value: colorOption,
			"aria-label": localeText.selectColorAriaLabel(colorOption),
			"data-palette": colorOption,
			className: classes.eventDialogResourceMenuColorToggle,
			children: color === colorOption && /*#__PURE__*/ (0, import_jsx_runtime.jsx)(Check_default, { fontSize: "small" })
		}, colorOption))
	})] });
}
//#endregion
//#region node_modules/@mui/x-scheduler/internals/components/event-dialog/EventDialogTabPanel.mjs
var EventDialogTabPanel = styled("div", {
	name: "MuiEventDialog",
	slot: "TabPanel"
})({
	display: "flex",
	flexDirection: "column",
	flex: 1,
	minHeight: 0,
	overflow: "hidden",
	"&[hidden]": { display: "none" }
});
var EventDialogTabContent = styled("div", {
	name: "MuiEventDialog",
	slot: "TabContent"
})(({ theme }) => ({
	padding: theme.spacing(3),
	display: "flex",
	flexDirection: "column",
	gap: theme.spacing(2),
	flex: 1,
	overflow: "auto",
	scrollbarWidth: "thin",
	maxHeight: 450,
	height: "fit-content"
}));
//#endregion
//#region node_modules/@mui/x-scheduler/internals/components/event-dialog/GeneralTab.mjs
var SectionFieldset = styled("fieldset", {
	name: "MuiEventDialog",
	slot: "SectionFieldset"
})(({ theme }) => ({
	display: "flex",
	flexDirection: "column",
	gap: theme.spacing(2),
	border: 0,
	margin: 0,
	padding: 0,
	minInlineSize: "min-content"
}));
var SectionHeaderTitle = styled("legend", {
	name: "MuiEventDialog",
	slot: "SectionHeaderTitle"
})(({ theme }) => _extends({}, theme.typography.subtitle2, {
	padding: 0,
	marginBlockEnd: theme.spacing(2),
	textTransform: "uppercase",
	color: (theme.vars || theme).palette.text.secondary
}));
var DateTimeFieldsContainer = styled("div", {
	name: "MuiEventDialog",
	slot: "DateTimeFieldsContainer"
})(({ theme }) => ({
	display: "flex",
	flexDirection: "column",
	gap: theme.spacing(3)
}));
var DateTimeFieldsRow = styled("div", {
	name: "MuiEventDialog",
	slot: "DateTimeFieldsRow"
})(({ theme }) => ({
	display: "flex",
	gap: theme.spacing(1),
	"& > :first-of-type": { flex: "1.5 1 0%" },
	"& > :nth-of-type(2)": { flex: "1 1 0%" }
}));
var AllDayFormControlLabel = styled(FormControlLabel, {
	name: "MuiEventDialog",
	slot: "AllDayFormControlLabel"
})({
	width: "100%",
	justifyContent: "space-between",
	[`&.${formControlLabelClasses.root}`]: { marginLeft: 0 }
});
function GeneralTab(props) {
	const { occurrence, errors, setErrors, controlled, setControlled, value } = props;
	const adapter = useAdapterContext();
	const { schedulerId, classes, localeText } = useEventDialogStyledContext();
	const store = useSchedulerStoreContext();
	const displayTimezone = useStore(store, schedulerOtherSelectors.displayTimezone);
	const shouldEventRequireResource = useStore(store, schedulerOtherSelectors.shouldEventRequireResource);
	const isPropertyReadOnly = useStore(store, schedulerEventSelectors.isPropertyReadOnly, occurrence.id);
	const rawPlaceholder = useStore(store, schedulerOccurrencePlaceholderSelectors.value);
	function pushPlaceholder(next) {
		if (rawPlaceholder?.type !== "creation") return;
		const { start, end, surfaceType } = computeRange(adapter, next, displayTimezone);
		const surfaceTypeToUse = rawPlaceholder.lockSurfaceType ? rawPlaceholder.surfaceType : surfaceType;
		store.setOccurrencePlaceholder({
			type: "creation",
			surfaceType: surfaceTypeToUse,
			resourceId: next.resourceId,
			start,
			end,
			lockSurfaceType: rawPlaceholder.lockSurfaceType
		});
	}
	const createHandleChangeDateOrTimeField = (field) => (event) => {
		const inputValue = event.currentTarget.value;
		setErrors({});
		const newState = _extends({}, controlled, { [field]: inputValue });
		pushPlaceholder(newState);
		setControlled(newState);
	};
	const handleToggleAllDay = (checked) => {
		const newState = _extends({}, controlled, { allDay: checked });
		pushPlaceholder(newState);
		setControlled(newState);
	};
	const handleResourceChange = (newResource) => {
		const nextErrors = _extends({}, errors);
		delete nextErrors.resource;
		setErrors(nextErrors);
		const newState = _extends({}, controlled, { resourceId: newResource });
		pushPlaceholder(newState);
		setControlled(newState);
	};
	const handleColorChange = (newColor) => {
		const newState = _extends({}, controlled, { color: newColor });
		pushPlaceholder(newState);
		setControlled(newState);
	};
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(EventDialogTabPanel, {
		role: "tabpanel",
		id: `${schedulerId}-general-tabpanel`,
		"aria-labelledby": `${schedulerId}-general-tab`,
		className: classes.eventDialogTabPanel,
		hidden: value !== "general",
		children: /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(EventDialogTabContent, {
			className: classes.eventDialogTabContent,
			children: [
				/*#__PURE__*/ (0, import_jsx_runtime.jsxs)(SectionFieldset, {
					className: classes.eventDialogSectionFieldset,
					children: [/*#__PURE__*/ (0, import_jsx_runtime.jsx)(SectionHeaderTitle, {
						className: classes.eventDialogSectionHeaderTitle,
						children: localeText.dateTimeSectionLabel
					}), /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(DateTimeFieldsContainer, {
						className: classes.eventDialogDateTimeFieldsContainer,
						children: [
							/*#__PURE__*/ (0, import_jsx_runtime.jsxs)(DateTimeFieldsRow, {
								className: classes.eventDialogDateTimeFieldsRow,
								children: [/*#__PURE__*/ (0, import_jsx_runtime.jsx)(TextField, {
									name: "startDate",
									label: localeText.startDateLabel,
									type: "date",
									value: controlled.startDate,
									onChange: createHandleChangeDateOrTimeField("startDate"),
									required: true,
									slotProps: {
										inputLabel: { shrink: true },
										input: { readOnly: isPropertyReadOnly("start") },
										formHelperText: { role: "alert" }
									},
									error: !!errors.startDate,
									helperText: errors.startDate,
									size: "small"
								}), !controlled.allDay && /*#__PURE__*/ (0, import_jsx_runtime.jsx)(TextField, {
									name: "startTime",
									label: localeText.startTimeLabel,
									type: "time",
									value: controlled.startTime,
									onChange: createHandleChangeDateOrTimeField("startTime"),
									required: true,
									slotProps: {
										inputLabel: { shrink: true },
										input: { readOnly: isPropertyReadOnly("start") }
									},
									size: "small"
								})]
							}),
							/*#__PURE__*/ (0, import_jsx_runtime.jsxs)(DateTimeFieldsRow, {
								className: classes.eventDialogDateTimeFieldsRow,
								children: [/*#__PURE__*/ (0, import_jsx_runtime.jsx)(TextField, {
									name: "endDate",
									label: localeText.endDateLabel,
									type: "date",
									value: controlled.endDate,
									onChange: createHandleChangeDateOrTimeField("endDate"),
									required: true,
									slotProps: {
										inputLabel: { shrink: true },
										input: { readOnly: isPropertyReadOnly("end") }
									},
									size: "small"
								}), !controlled.allDay && /*#__PURE__*/ (0, import_jsx_runtime.jsx)(TextField, {
									name: "endTime",
									label: localeText.endTimeLabel,
									type: "time",
									value: controlled.endTime,
									onChange: createHandleChangeDateOrTimeField("endTime"),
									required: true,
									slotProps: {
										inputLabel: { shrink: true },
										input: { readOnly: isPropertyReadOnly("end") }
									},
									size: "small"
								})]
							}),
							/*#__PURE__*/ (0, import_jsx_runtime.jsx)(AllDayFormControlLabel, {
								control: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(Switch, {
									id: `${schedulerId}-enable-all-day-switch`,
									checked: controlled.allDay,
									onChange: (event) => handleToggleAllDay(event.target.checked),
									disabled: isPropertyReadOnly("allDay")
								}),
								label: localeText.allDayLabel,
								labelPlacement: "start"
							})
						]
					})]
				}),
				/*#__PURE__*/ (0, import_jsx_runtime.jsx)(Divider, {}),
				/*#__PURE__*/ (0, import_jsx_runtime.jsxs)(SectionFieldset, {
					className: classes.eventDialogSectionFieldset,
					children: [/*#__PURE__*/ (0, import_jsx_runtime.jsx)(SectionHeaderTitle, {
						className: classes.eventDialogSectionHeaderTitle,
						children: localeText.resourceColorSectionLabel
					}), /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ResourceAndColorSection, {
						readOnly: isPropertyReadOnly("resource"),
						resourceId: controlled.resourceId,
						onResourceChange: handleResourceChange,
						onColorChange: handleColorChange,
						color: controlled.color,
						error: shouldEventRequireResource && typeof errors.resource === "string" ? errors.resource : void 0
					})]
				}),
				/*#__PURE__*/ (0, import_jsx_runtime.jsx)(Divider, {}),
				/*#__PURE__*/ (0, import_jsx_runtime.jsx)(TextField, {
					name: "description",
					label: localeText.descriptionLabel,
					defaultValue: hasProp(occurrence, "description") ? occurrence.description : "",
					multiline: true,
					rows: 5,
					fullWidth: true,
					slotProps: { input: { readOnly: isPropertyReadOnly("description") } }
				})
			]
		})
	});
}
//#endregion
//#region node_modules/@mui/x-scheduler/internals/components/event-dialog/FormContent.mjs
var FormActions = styled(DialogActions, {
	name: "MuiEventDialog",
	slot: "FormActions"
})(({ theme }) => ({
	padding: theme.spacing(3),
	gap: theme.spacing(2)
}));
var DialogContent = styled(DialogContent$1, {
	name: "MuiEventDialog",
	slot: "DialogContent"
})({
	cursor: "default",
	userSelect: "text",
	padding: 0,
	minWidth: 360,
	width: 450,
	display: "flex",
	flexDirection: "column",
	overflow: "hidden"
});
var EventDialogTitleTextField = styled(TextField, {
	name: "MuiEventDialog",
	slot: "TitleTextField"
})(({ theme }) => ({
	flex: 1,
	[`& .${inputBaseClasses.root}`]: {
		fontSize: theme.typography.h6.fontSize,
		lineHeight: theme.typography.h6.lineHeight,
		fontWeight: theme.typography.h6.fontWeight
	}
}));
var EventDialogForm = styled("form", {
	name: "MuiEventDialog",
	slot: "Form"
})({
	display: "flex",
	flexDirection: "column",
	flex: 1,
	minHeight: 0
});
var EventDialogTabsContainer = styled("div", {
	name: "MuiEventDialog",
	slot: "TabsContainer"
})(({ theme }) => ({ borderBottom: `1px solid ${(theme.vars || theme).palette.divider}` }));
var EventDialogTabs = styled(Tabs, {
	name: "MuiEventDialog",
	slot: "Tabs"
})(({ theme }) => ({ padding: theme.spacing(0, 3) }));
function FormContent(props) {
	const { occurrence, onClose, dragHandlerRef } = props;
	const adapter = useAdapterContext();
	const { schedulerId, classes, localeText } = useEventDialogStyledContext();
	const store = useSchedulerStoreContext();
	const isPropertyReadOnly = useStore(store, schedulerEventSelectors.isPropertyReadOnly, occurrence.id);
	const rawPlaceholder = useStore(store, schedulerOccurrencePlaceholderSelectors.value);
	const recurringEventsPlugin = useStore(store, schedulerOtherSelectors.recurringEventsPlugin);
	const displayTimezone = useStore(store, schedulerOtherSelectors.displayTimezone);
	const showRecurrence = useStore(store, schedulerOtherSelectors.areRecurringEventsAvailable);
	const shouldEventRequireResource = useStore(store, schedulerOtherSelectors.shouldEventRequireResource);
	const { recurrenceTab: RecurrenceTabRenderer } = useEventDialogOptionalRenderers();
	const recurrencePresets = useStore(store, schedulerRecurringEventSelectors.presets, occurrence.displayTimezone.start);
	const defaultRecurrencePresetKey = useStore(store, schedulerRecurringEventSelectors.defaultPresetKey, occurrence.displayTimezone.rrule, occurrence.displayTimezone.start);
	const titleInputRef = import_react.useCallback((input) => input?.focus(), []);
	const [tabValue, setTabValue] = import_react.useState("general");
	const [errors, setErrors] = import_react.useState({});
	const [controlled, setControlled] = import_react.useState(() => {
		const fmtDate = (d) => adapter.formatByString(d.value, "yyyy-MM-dd");
		const fmtTime = (d) => adapter.formatByString(d.value, "HH:mm");
		const base = occurrence.displayTimezone.rrule;
		return {
			startDate: fmtDate(occurrence.displayTimezone.start),
			endDate: fmtDate(occurrence.displayTimezone.end),
			startTime: fmtTime(occurrence.displayTimezone.start),
			endTime: fmtTime(occurrence.displayTimezone.end),
			resourceId: getPrimaryResourceId(occurrence.resource),
			allDay: !!occurrence.allDay,
			color: hasProp(occurrence, "color") ? occurrence.color : null,
			recurrenceSelection: defaultRecurrencePresetKey,
			rruleDraft: _extends({
				freq: base?.freq ?? "WEEKLY",
				interval: base?.interval ?? 1,
				byDay: base?.byDay ?? [],
				byMonthDay: base?.byMonthDay ?? []
			}, base?.count ? { count: base.count } : {}, base?.until ? { until: base.until } : {})
		};
	});
	const handleSubmit = async (event) => {
		event.preventDefault();
		const { start, end } = computeRange(adapter, controlled, displayTimezone);
		const form = new FormData(event.currentTarget);
		setErrors({});
		const err = validateRange(adapter, start, end, controlled.allDay);
		if (err) {
			setErrors({ [err.field]: localeText.startDateAfterEndDateError });
			return;
		}
		if (shouldEventRequireResource && controlled.resourceId === null) {
			setErrors({ resource: localeText.requiredResourceError });
			return;
		}
		const metaChanges = {
			title: form.get("title").trim(),
			description: form.get("description").trim(),
			allDay: controlled.allDay,
			resource: controlled.resourceId === null ? void 0 : controlled.resourceId,
			color: controlled.color === null ? void 0 : controlled.color
		};
		let rruleToSubmit;
		if (!showRecurrence || !recurrencePresets) rruleToSubmit = void 0;
		else if (controlled.recurrenceSelection === null) rruleToSubmit = void 0;
		else if (controlled.recurrenceSelection === "custom") rruleToSubmit = controlled.rruleDraft;
		else rruleToSubmit = recurrencePresets[controlled.recurrenceSelection];
		if (rawPlaceholder?.type === "creation") store.createEvent(_extends({}, metaChanges, {
			start,
			end,
			rrule: rruleToSubmit
		}));
		else if (showRecurrence && recurringEventsPlugin && occurrence.displayTimezone.rrule) {
			const recurrenceModified = !schedulerRecurringEventSelectors.isSameRRule(store.state, occurrence.displayTimezone.rrule, rruleToSubmit);
			const changes = _extends({}, metaChanges, {
				id: occurrence.id,
				start,
				end
			}, recurrenceModified ? { rrule: rruleToSubmit } : {});
			await store.updateRecurringEvent({
				occurrenceStart: occurrence.displayTimezone.start.value,
				changes,
				onSubmit: onClose
			});
			return;
		} else store.updateEvent(_extends({ id: occurrence.id }, metaChanges, {
			start,
			end,
			rrule: rruleToSubmit
		}));
		onClose();
	};
	const handleDelete = () => {
		if (showRecurrence && recurringEventsPlugin && occurrence.displayTimezone.rrule) {
			store.deleteRecurringEvent({
				occurrenceStart: occurrence.displayTimezone.start.value,
				eventId: occurrence.id,
				onSubmit: onClose
			});
			return;
		}
		store.deleteEvent(occurrence.id);
		onClose();
	};
	const handleTabChange = (event, newValue) => {
		setTabValue(newValue);
	};
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(DialogContent, {
		className: classes.eventDialogContent,
		children: /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(EventDialogForm, {
			onSubmit: handleSubmit,
			className: classes.eventDialogForm,
			children: [
				/*#__PURE__*/ (0, import_jsx_runtime.jsxs)(EventDialogHeader, {
					onClose,
					dragHandlerRef,
					children: [/*#__PURE__*/ (0, import_jsx_runtime.jsx)("span", {
						id: `${schedulerId}-event-dialog-title`,
						style: {
							position: "absolute",
							width: 0,
							height: 0,
							overflow: "hidden"
						},
						children: occurrence.title
					}), /*#__PURE__*/ (0, import_jsx_runtime.jsx)(EventDialogTitleTextField, {
						name: "title",
						defaultValue: occurrence.title,
						required: true,
						inputRef: titleInputRef,
						slotProps: {
							input: {
								readOnly: isPropertyReadOnly("title"),
								"aria-label": localeText.eventTitleAriaLabel
							},
							formHelperText: { role: "alert" }
						},
						error: !!errors.title,
						helperText: errors.title,
						fullWidth: true,
						size: "small"
					})]
				}),
				showRecurrence && RecurrenceTabRenderer && /*#__PURE__*/ (0, import_jsx_runtime.jsx)(EventDialogTabsContainer, {
					className: classes.eventDialogTabsContainer,
					children: /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(EventDialogTabs, {
						value: tabValue,
						onChange: handleTabChange,
						children: [/*#__PURE__*/ (0, import_jsx_runtime.jsx)(Tab, {
							id: `${schedulerId}-general-tab`,
							className: classes.eventDialogTab,
							label: localeText.generalTabLabel,
							value: "general"
						}), /*#__PURE__*/ (0, import_jsx_runtime.jsx)(Tab, {
							id: `${schedulerId}-recurrence-tab`,
							className: classes.eventDialogTab,
							label: localeText.recurrenceTabLabel,
							value: "recurrence"
						})]
					})
				}),
				/*#__PURE__*/ (0, import_jsx_runtime.jsx)(GeneralTab, {
					occurrence,
					errors,
					setErrors,
					controlled,
					setControlled,
					value: showRecurrence && RecurrenceTabRenderer ? tabValue : "general"
				}),
				showRecurrence && RecurrenceTabRenderer && /*#__PURE__*/ (0, import_jsx_runtime.jsx)(RecurrenceTabRenderer, {
					occurrence,
					controlled,
					setControlled,
					tabValue
				}),
				/*#__PURE__*/ (0, import_jsx_runtime.jsx)(Divider, { className: classes.eventDialogFormDivider }),
				/*#__PURE__*/ (0, import_jsx_runtime.jsxs)(FormActions, {
					className: classes.eventDialogFormActions,
					children: [/*#__PURE__*/ (0, import_jsx_runtime.jsx)(Button, {
						className: classes.eventDialogDeleteButton,
						color: "error",
						type: "button",
						onClick: handleDelete,
						children: localeText.deleteEvent
					}), /*#__PURE__*/ (0, import_jsx_runtime.jsx)(Button, {
						className: classes.eventDialogSaveButton,
						variant: "contained",
						type: "submit",
						children: localeText.saveChanges
					})]
				})
			]
		})
	});
}
//#endregion
//#region node_modules/@mui/x-scheduler/internals/utils/dialog-utils.mjs
function calculatePosition(anchorElement, element, defaultPosition = "right") {
	if (!element || !anchorElement) return;
	const anchorRect = anchorElement.getBoundingClientRect();
	const elemRect = element.getBoundingClientRect();
	const margin = 16;
	const windowWidth = window.innerWidth;
	const windowHeight = window.innerHeight;
	const clamp = (value, min, max) => Math.max(min, Math.min(value, max));
	const checkFit = (pos) => {
		switch (pos) {
			case "right": return windowWidth - anchorRect.right >= elemRect.width + margin;
			case "left": return anchorRect.left >= elemRect.width + margin;
			case "bottom": return windowHeight - anchorRect.bottom >= elemRect.height + margin;
			case "top": return anchorRect.top >= elemRect.height + margin;
			default: return false;
		}
	};
	const getCoords = (pos) => {
		let top = 0;
		let left = 0;
		switch (pos) {
			case "right":
				left = anchorRect.right + margin;
				top = anchorRect.top;
				top = clamp(top, margin, windowHeight - elemRect.height - margin);
				break;
			case "left":
				left = anchorRect.left - elemRect.width - margin;
				top = anchorRect.top;
				top = clamp(top, margin, windowHeight - elemRect.height - margin);
				break;
			case "bottom":
				top = anchorRect.bottom + margin;
				left = anchorRect.left;
				left = clamp(left, margin, windowWidth - elemRect.width - margin);
				break;
			case "top":
				top = anchorRect.top - elemRect.height - margin;
				left = anchorRect.left;
				left = clamp(left, margin, windowWidth - elemRect.width - margin);
				break;
			default: break;
		}
		return {
			top,
			left
		};
	};
	const validPosition = [defaultPosition, ...[
		"right",
		"left",
		"bottom",
		"top"
	].filter((position) => position !== defaultPosition)].find(checkFit);
	if (validPosition) return getCoords(validPosition);
	return {
		left: (windowWidth - elemRect.width) / 2,
		top: (windowHeight - elemRect.height) / 2
	};
}
//#endregion
//#region node_modules/@mui/icons-material/CalendarMonthRounded.mjs
var CalendarMonthRounded_default = createSvgIcon(/*#__PURE__*/ (0, import_jsx_runtime.jsx)("path", { d: "M17 2c-.55 0-1 .45-1 1v1H8V3c0-.55-.45-1-1-1s-1 .45-1 1v1H5c-1.11 0-1.99.9-1.99 2L3 20c0 1.1.89 2 2 2h14c1.1 0 2-.9 2-2V6c0-1.1-.9-2-2-2h-1V3c0-.55-.45-1-1-1m2 18H5V10h14zm-8-7c0-.55.45-1 1-1s1 .45 1 1-.45 1-1 1-1-.45-1-1m-4 0c0-.55.45-1 1-1s1 .45 1 1-.45 1-1 1-1-.45-1-1m8 0c0-.55.45-1 1-1s1 .45 1 1-.45 1-1 1-1-.45-1-1m-4 4c0-.55.45-1 1-1s1 .45 1 1-.45 1-1 1-1-.45-1-1m-4 0c0-.55.45-1 1-1s1 .45 1 1-.45 1-1 1-1-.45-1-1m8 0c0-.55.45-1 1-1s1 .45 1 1-.45 1-1 1-1-.45-1-1" }), "CalendarMonthRounded");
//#endregion
//#region node_modules/@mui/x-scheduler/internals/components/event-dialog/ReadonlyContent.mjs
var ReadonlyContentDragContainer = styled("section", {
	name: "MuiEventDialog",
	slot: "ReadonlyContentDragContainer"
})({ cursor: "move" });
var ReadonlyContentRoot = styled("div", {
	name: "MuiEventDialog",
	slot: "ReadonlyContent"
})(({ theme }) => ({
	display: "flex",
	padding: theme.spacing(0, 3),
	flexDirection: "column",
	gap: theme.spacing(2)
}));
var RecurrenceLabelContainer = styled("div", {
	name: "MuiEventDialog",
	slot: "RecurrenceLabelContainer"
})(({ theme }) => ({
	display: "flex",
	gap: theme.spacing(1)
}));
var EventDialogActions = styled("div", {
	name: "MuiEventDialog",
	slot: "Actions"
})(({ theme }) => ({
	display: "flex",
	justifyContent: "space-between",
	padding: theme.spacing(3)
}));
var EventDialogDateTimeContainer = styled("div", {
	name: "MuiEventDialog",
	slot: "DateTimeContainer"
})(({ theme }) => ({
	display: "flex",
	alignItems: "center",
	gap: theme.spacing(1)
}));
var EventDialogTitle = styled(Typography, {
	name: "MuiEventDialog",
	slot: "Title"
})({
	margin: 0,
	color: "var(--event-on-surface-subtle-primary)"
});
var EventDialogResourceContainer = styled("div", {
	name: "MuiEventDialog",
	slot: "ResourceContainer"
})(({ theme }) => ({
	display: "flex",
	alignItems: "center",
	gap: theme.spacing(1)
}));
var EventDialogResourceLegendContainer = styled("div", {
	name: "MuiEventDialog",
	slot: "ResourceLegendContainer"
})(({ theme }) => ({
	display: "flex",
	alignItems: "center",
	gap: theme.spacing(1)
}));
var ResourceLegendColorDot = styled("span", {
	name: "MuiEventDialog",
	slot: "ResourceLegendColor"
})(({ theme }) => ({
	width: 14,
	height: 14,
	margin: theme.spacing(0, .2),
	borderRadius: 2,
	flexShrink: 0,
	backgroundColor: "var(--event-main)",
	variants: getPaletteVariants(theme)
}));
var EventDialogResourceTitle = styled(Typography, {
	name: "MuiEventDialog",
	slot: "ResourceTitle"
})(({ theme }) => ({
	margin: 0,
	fontSize: theme.typography.body2.fontSize,
	color: "var(--event-on-surface-subtle-secondary)",
	overflow: "hidden",
	textOverflow: "ellipsis",
	whiteSpace: "nowrap"
}));
function ReadonlyContent(props) {
	const { occurrence, onClose, dragHandlerRef } = props;
	const adapter = useAdapterContext();
	const { schedulerId, classes, localeText } = useEventDialogStyledContext();
	const store = useSchedulerStoreContext();
	const color = useStore(store, schedulerEventSelectors.color, occurrence.id);
	const resource = useStore(store, schedulerResourceSelectors.processedResource, getPrimaryResourceId(occurrence.resource));
	const defaultRecurrenceKey = useStore(store, schedulerRecurringEventSelectors.defaultPresetKey, occurrence.displayTimezone.rrule, occurrence.displayTimezone.start);
	const showRecurrence = useStore(store, schedulerOtherSelectors.areRecurringEventsAvailable);
	const formatTime = useFormatTime();
	const recurrenceLabel = getRecurrenceLabel(adapter, occurrence.displayTimezone.start, defaultRecurrenceKey, localeText);
	return /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(ReadonlyContentDragContainer, {
		ref: dragHandlerRef,
		children: [
			/*#__PURE__*/ (0, import_jsx_runtime.jsx)(EventDialogHeader, {
				onClose,
				children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(EventDialogTitle, {
					variant: "h6",
					id: `${schedulerId}-event-dialog-title`,
					className: classes.eventDialogTitle,
					children: occurrence.title
				})
			}),
			/*#__PURE__*/ (0, import_jsx_runtime.jsxs)(ReadonlyContentRoot, {
				className: classes.eventDialogReadonlyContent,
				children: [
					/*#__PURE__*/ (0, import_jsx_runtime.jsxs)(EventDialogResourceContainer, {
						className: classes.eventDialogResourceContainer,
						children: [/*#__PURE__*/ (0, import_jsx_runtime.jsxs)(EventDialogResourceLegendContainer, {
							className: classes.eventDialogResourceLegendContainer,
							children: [resource?.eventColor && resource.eventColor !== color && /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ResourceLegendColorDot, {
								className: classes.eventDialogResourceLegendColor,
								"data-palette": resource.eventColor
							}), /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ResourceLegendColorDot, {
								className: classes.eventDialogResourceLegendColor,
								"data-palette": color
							})]
						}), /*#__PURE__*/ (0, import_jsx_runtime.jsx)(EventDialogResourceTitle, {
							className: classes.eventDialogResourceTitle,
							children: resource?.title || localeText.noResourceAriaLabel
						})]
					}),
					/*#__PURE__*/ (0, import_jsx_runtime.jsxs)(EventDialogDateTimeContainer, {
						className: classes.eventDialogDateTimeContainer,
						children: [/*#__PURE__*/ (0, import_jsx_runtime.jsx)(CalendarMonthRounded_default, {
							className: classes.eventDialogDateTimeIcon,
							fontSize: "small"
						}), /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(Typography, {
							className: classes.eventDialogDateTimeLabel,
							variant: "body2",
							component: "p",
							noWrap: true,
							children: [/*#__PURE__*/ (0, import_jsx_runtime.jsx)("time", {
								dateTime: adapter.format(occurrence.displayTimezone.start.value, "localizedNumericDate"),
								children: /*#__PURE__*/ (0, import_jsx_runtime.jsxs)("span", { children: [
									adapter.format(occurrence.displayTimezone.start.value, "localizedDateWithFullMonthAndWeekDay"),
									",",
									" "
								] })
							}), occurrence.allDay ? /*#__PURE__*/ (0, import_jsx_runtime.jsxs)("span", { children: [" ", localeText.allDayLabel] }) : /*#__PURE__*/ (0, import_jsx_runtime.jsxs)("time", { children: [/*#__PURE__*/ (0, import_jsx_runtime.jsx)("span", { children: formatTime(occurrence.displayTimezone.start.value) }), /*#__PURE__*/ (0, import_jsx_runtime.jsxs)("span", { children: [" - ", formatTime(occurrence.displayTimezone.end.value)] })] })]
						})]
					}),
					showRecurrence && defaultRecurrenceKey != null && /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(RecurrenceLabelContainer, {
						className: classes.eventDialogRecurrenceLabelContainer,
						children: [/*#__PURE__*/ (0, import_jsx_runtime.jsx)(RepeatRounded_default, {
							className: classes.eventDialogRecurrenceIcon,
							fontSize: "small"
						}), /*#__PURE__*/ (0, import_jsx_runtime.jsx)(Typography, {
							className: classes.eventDialogRecurrenceLabel,
							variant: "body2",
							component: "em",
							sx: { color: "text.secondary" },
							children: recurrenceLabel
						})]
					}),
					hasProp(occurrence, "description") && !!occurrence.description ? /*#__PURE__*/ (0, import_jsx_runtime.jsx)(Typography, {
						className: classes.eventDialogDescriptionLabel,
						variant: "body2",
						children: occurrence.description
					}) : null
				]
			}),
			/*#__PURE__*/ (0, import_jsx_runtime.jsx)(EventDialogActions, {
				className: classes.eventDialogActions,
				children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(Button, {
					className: classes.eventDialogCloseAction,
					variant: "contained",
					type: "button",
					onClick: onClose,
					children: localeText.closeButtonLabel
				})
			})
		]
	});
}
//#endregion
//#region node_modules/@mui/x-scheduler/internals/components/event-dialog/EventDialog.mjs
var _excluded$21 = [
	"anchorRef",
	"dragHandlerRef",
	"className"
], _excluded2$1 = [
	"style",
	"anchorRef",
	"occurrence",
	"onClose",
	"open"
], _excluded3 = ["children", "optionalRenderers"], _excluded4 = ["occurrence"];
var EventDialogRoot = styled(Dialog, {
	name: "MuiEventDialog",
	slot: "Root"
})({
	[`& .${backdropClasses.root}`]: { backgroundColor: "transparent" },
	[`& .${dialogClasses.container}`]: {
		width: "100%",
		justifyContent: "unset",
		alignItems: "unset"
	},
	[`& .${dialogClasses.paper}`]: { margin: 0 }
});
var EventDialogPaper = styled(Paper, {
	name: "MuiEventDialog",
	slot: "Paper"
})(({ theme }) => ({
	borderWidth: 0,
	borderTopWidth: 1,
	height: "fit-content",
	overflow: "hidden",
	"&[data-dragging]": { outline: `1px solid ${(theme.vars || theme).palette.primary.light}` }
}));
var PaperComponent = function PaperComponent(props) {
	const nodeRef = import_react.useRef(null);
	const mutateStyle = import_react.useCallback((style) => {
		if (nodeRef.current) nodeRef.current.style.transform = style;
	}, [nodeRef]);
	const { anchorRef, dragHandlerRef, className } = props, other = _objectWithoutPropertiesLoose(props, _excluded$21);
	const resetDrag = useDraggableDialog(nodeRef, dragHandlerRef, mutateStyle);
	const updatePosition = import_react.useCallback((shouldResetDrag = false) => {
		const position = calculatePosition(anchorRef.current, nodeRef.current, "left");
		if (position && nodeRef.current) {
			nodeRef.current.style.top = `${position.top}px`;
			nodeRef.current.style.left = `${position.left}px`;
			if (shouldResetDrag) resetDrag();
		}
	}, [
		anchorRef,
		resetDrag,
		nodeRef
	]);
	import_react.useLayoutEffect(() => {
		updatePosition(true);
	}, [updatePosition]);
	import_react.useEffect(() => {
		const handleResize = () => {
			updatePosition(false);
			resetDrag();
		};
		window.addEventListener("resize", handleResize);
		return () => {
			window.removeEventListener("resize", handleResize);
		};
	}, [updatePosition, resetDrag]);
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(EventDialogPaper, _extends({}, other, {
		ref: nodeRef,
		className
	}));
};
PaperComponent.displayName = "PaperComponent";
var EventDialog = createModal({ contextName: "EventDialogContext" });
EventDialog.Context;
var useEventDialogContext = EventDialog.useContext;
var EventDialogContent = /*#__PURE__*/ import_react.forwardRef(function EventDialogContent(inProps, forwardedRef) {
	const props = useThemeProps({
		props: inProps,
		name: "MuiEventDialog"
	});
	const { anchorRef, occurrence, onClose, open } = props, other = _objectWithoutPropertiesLoose(props, _excluded2$1);
	const store = useSchedulerStoreContext();
	const { schedulerId, classes } = useEventDialogStyledContext();
	const isEventReadOnly = useStore(store, schedulerEventSelectors.isReadOnly, occurrence.id);
	const dragHandlerRef = import_react.useRef(null);
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(EventDialogRoot, _extends({
		ref: forwardedRef,
		open,
		onClose,
		PaperComponent,
		"aria-labelledby": `${schedulerId}-event-dialog-title`,
		"aria-modal": "false",
		className: classes.eventDialog,
		slotProps: { paper: {
			className: classes.eventDialogPaper,
			anchorRef,
			dragHandlerRef
		} }
	}, other, { children: isEventReadOnly ? /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ReadonlyContent, {
		occurrence,
		onClose,
		dragHandlerRef
	}) : /*#__PURE__*/ (0, import_jsx_runtime.jsx)(FormContent, {
		occurrence,
		onClose,
		dragHandlerRef
	}) }));
});
EventDialogContent.displayName = "EventDialogContent";
function EventDialogProvider(props) {
	const { children, optionalRenderers } = props, other = _objectWithoutPropertiesLoose(props, _excluded3);
	const store = useSchedulerStoreContext();
	const isRecurringScopeDialogOpen = useStore(store, schedulerOtherSelectors.isRecurringScopeDialogOpen);
	const showRecurrence = useStore(store, schedulerOtherSelectors.areRecurringEventsAvailable);
	const RecurringScopeDialogRenderer = optionalRenderers?.recurringScopeDialog;
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(EventDialogOptionalRenderersContext.Provider, {
		value: optionalRenderers ?? EMPTY_OBJECT,
		children: /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(EventDialog.Provider, {
			render: ({ isOpen, anchorRef, data: occurrence, onClose }) => /*#__PURE__*/ (0, import_jsx_runtime.jsx)(EventDialogContent, _extends({
				open: isOpen,
				anchorRef,
				occurrence,
				onClose
			}, other)),
			onOpen: (occurrence) => {
				store.setEditedOccurrenceKey(occurrence.key);
			},
			onClose: () => {
				store.setEditedOccurrenceKey(null);
				store.setOccurrencePlaceholder(null);
			},
			children: [children, showRecurrence && isRecurringScopeDialogOpen && RecurringScopeDialogRenderer && /*#__PURE__*/ (0, import_jsx_runtime.jsx)(RecurringScopeDialogRenderer, {})]
		})
	});
}
function EventDialogTrigger(props) {
	const { occurrence } = props, other = _objectWithoutPropertiesLoose(props, _excluded4);
	const ref = import_react.useRef(null);
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(EventDialog.Trigger, _extends({
		ref,
		data: occurrence
	}, other));
}
//#endregion
//#region node_modules/@mui/x-scheduler/internals/components/event-dialog/eventDialogClasses.mjs
var eventDialogClassKeys = [
	"eventDialog",
	"eventDialogCloseButton",
	"eventDialogHeader",
	"eventDialogReadonlyContent",
	"eventDialogActions",
	"eventDialogDateTimeContainer",
	"eventDialogTitle",
	"eventDialogResourceContainer",
	"eventDialogResourceLegendContainer",
	"eventDialogResourceLegendColor",
	"eventDialogResourceTitle",
	"eventDialogPaper",
	"eventDialogForm",
	"eventDialogFormActions",
	"eventDialogContent",
	"eventDialogTabPanel",
	"eventDialogTabContent",
	"eventDialogDateTimeFieldsContainer",
	"eventDialogDateTimeFieldsRow",
	"eventDialogResourceMenuItem",
	"eventDialogResourceMenuListSubheader",
	"eventDialogResourceMenuColorDot",
	"eventDialogResourceMenuColorToggleGroup",
	"eventDialogResourceMenuColorToggle",
	"eventDialogRecurrenceLabelContainer",
	"eventDialogRecurrenceSelectorContainer",
	"eventDialogInlineRow",
	"eventDialogSectionHeaderTitle",
	"eventDialogSectionFieldset",
	"eventDialogRadioButtonLabel",
	"eventDialogRepeatSectionLabel",
	"eventDialogEndsRadioGroup",
	"eventDialogRepeatSectionFieldset",
	"eventDialogRepeatSectionContent",
	"eventDialogRecurrenceSelectorToggleGroup",
	"eventDialogWeekDaySelectorCheckbox",
	"eventDialogFrequencySelect",
	"eventDialogSmallNumberField",
	"eventDialogTabsContainer",
	"eventDialogTab",
	"eventDialogFormDivider",
	"eventDialogDeleteButton",
	"eventDialogSaveButton",
	"eventDialogCloseAction",
	"eventDialogDateTimeIcon",
	"eventDialogDateTimeLabel",
	"eventDialogRecurrenceIcon",
	"eventDialogRecurrenceLabel",
	"eventDialogDescriptionLabel"
];
var eventDialogSlots = {
	eventDialog: ["eventDialog"],
	eventDialogCloseButton: ["eventDialogCloseButton"],
	eventDialogHeader: ["eventDialogHeader"],
	eventDialogReadonlyContent: ["eventDialogReadonlyContent"],
	eventDialogActions: ["eventDialogActions"],
	eventDialogDateTimeContainer: ["eventDialogDateTimeContainer"],
	eventDialogTitle: ["eventDialogTitle"],
	eventDialogResourceContainer: ["eventDialogResourceContainer"],
	eventDialogResourceLegendContainer: ["eventDialogResourceLegendContainer"],
	eventDialogResourceLegendColor: ["eventDialogResourceLegendColor"],
	eventDialogResourceTitle: ["eventDialogResourceTitle"],
	eventDialogPaper: ["eventDialogPaper"],
	eventDialogForm: ["eventDialogForm"],
	eventDialogFormActions: ["eventDialogFormActions"],
	eventDialogContent: ["eventDialogContent"],
	eventDialogTabPanel: ["eventDialogTabPanel"],
	eventDialogTabContent: ["eventDialogTabContent"],
	eventDialogDateTimeFieldsContainer: ["eventDialogDateTimeFieldsContainer"],
	eventDialogDateTimeFieldsRow: ["eventDialogDateTimeFieldsRow"],
	eventDialogResourceMenuItem: ["eventDialogResourceMenuItem"],
	eventDialogResourceMenuListSubheader: ["eventDialogResourceMenuListSubheader"],
	eventDialogResourceMenuColorDot: ["eventDialogResourceMenuColorDot"],
	eventDialogResourceMenuColorToggleGroup: ["eventDialogResourceMenuColorToggleGroup"],
	eventDialogResourceMenuColorToggle: ["eventDialogResourceMenuColorToggle"],
	eventDialogRecurrenceLabelContainer: ["eventDialogRecurrenceLabelContainer"],
	eventDialogRecurrenceSelectorContainer: ["eventDialogRecurrenceSelectorContainer"],
	eventDialogInlineRow: ["eventDialogInlineRow"],
	eventDialogSectionHeaderTitle: ["eventDialogSectionHeaderTitle"],
	eventDialogSectionFieldset: ["eventDialogSectionFieldset"],
	eventDialogRadioButtonLabel: ["eventDialogRadioButtonLabel"],
	eventDialogRepeatSectionLabel: ["eventDialogRepeatSectionLabel"],
	eventDialogEndsRadioGroup: ["eventDialogEndsRadioGroup"],
	eventDialogRepeatSectionFieldset: ["eventDialogRepeatSectionFieldset"],
	eventDialogRepeatSectionContent: ["eventDialogRepeatSectionContent"],
	eventDialogRecurrenceSelectorToggleGroup: ["eventDialogRecurrenceSelectorToggleGroup"],
	eventDialogWeekDaySelectorCheckbox: ["eventDialogWeekDaySelectorCheckbox"],
	eventDialogFrequencySelect: ["eventDialogFrequencySelect"],
	eventDialogSmallNumberField: ["eventDialogSmallNumberField"],
	eventDialogTabsContainer: ["eventDialogTabsContainer"],
	eventDialogTab: ["eventDialogTab"],
	eventDialogFormDivider: ["eventDialogFormDivider"],
	eventDialogDeleteButton: ["eventDialogDeleteButton"],
	eventDialogSaveButton: ["eventDialogSaveButton"],
	eventDialogCloseAction: ["eventDialogCloseAction"],
	eventDialogDateTimeIcon: ["eventDialogDateTimeIcon"],
	eventDialogDateTimeLabel: ["eventDialogDateTimeLabel"],
	eventDialogRecurrenceIcon: ["eventDialogRecurrenceIcon"],
	eventDialogRecurrenceLabel: ["eventDialogRecurrenceLabel"],
	eventDialogDescriptionLabel: ["eventDialogDescriptionLabel"]
};
//#endregion
//#region node_modules/@mui/x-internals/useDisposable/useDisposable.mjs
var STRICT_MODE_BITS = 16;
var SafeReactInternals = SafeReact;
var ReactInternals = SafeReactInternals.__CLIENT_INTERNALS_DO_NOT_USE_OR_WARN_USERS_THEY_CANNOT_UPGRADE ?? SafeReactInternals.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED;
function isInStrictMode() {
	try {
		const owner = ReactInternals?.A?.getOwner?.() ?? ReactInternals?.ReactCurrentOwner?.current ?? null;
		if (owner == null || typeof owner.mode !== "number") return false;
		return (owner.mode & STRICT_MODE_BITS) !== 0;
	} catch {
		return false;
	}
}
var MOUNT = Symbol("useDisposable.mount");
var DEV_STATE = Symbol("useDisposable.devState");
var UNINITIALIZED = Symbol();
/**
* Development variant: detects StrictMode by reading the currently-rendering
* fiber's `mode` bits from React shared internals so the same instance
* survives StrictMode's mount→unmount→mount cycle.
*
* The mount callback counts mounts to keep the detection honest:
* - it skips dispose on StrictMode's *first* (simulated) unmount only, and
*   disposes on every real unmount — including the final one inside
*   `<StrictMode>`, so no instance is leaked in dev;
* - if the instance is mounted a second time but was already disposed, our
*   render-time detection produced a false negative. Rather than silently
*   hand back a dead instance (which is the failure that motivated this hook),
*   it throws so the bug surfaces immediately.
* @returns {T} the lazily-created instance.
*/
function useDisposableDevelopment(factory) {
	const ref = import_react.useRef(UNINITIALIZED);
	if (ref.current === UNINITIALIZED) {
		const inst = factory();
		const state = {
			detectedStrict: isInStrictMode(),
			mountCount: 0,
			disposed: false
		};
		inst[DEV_STATE] = state;
		inst[MOUNT] = () => {
			state.mountCount += 1;
			if (state.mountCount > 1 && state.disposed) throw new Error("MUI X: useDisposable failed to detect React StrictMode.\nThe instance was disposed on StrictMode's simulated unmount and is about to be reused while torn down.\nThis is an internal invariant violation — please report it at https://github.com/mui/mui-x/issues.");
			return () => {
				if (state.detectedStrict && state.mountCount < 2) return;
				state.disposed = true;
				inst[disposeSymbol]();
				ref.current = UNINITIALIZED;
			};
		};
		ref.current = inst;
	}
	useOnMount(ref.current[MOUNT]);
	return ref.current;
}
/**
* Lazily creates an instance on first render and runs its `[disposeSymbol]`
* once on unmount. The cleanup runs synchronously; in development StrictMode's
* simulated unmount is detected and skipped so the same instance survives the
* double mount. The development variant degrades to production behaviour on
* runtimes that don't replay the double mount (Preact, React 17) because no
* readable StrictEffectsMode bit is found, so no runtime branching is needed.
*/
var useDisposable = useDisposableDevelopment;
//#endregion
//#region node_modules/@mui/x-scheduler-internals/use-event-calendar/useEventCalendar.mjs
function useEventCalendar(parameters, StoreClass) {
	const adapter = useAdapter(parameters.dateLocale);
	const ResolvedStoreClass = StoreClass ?? EventCalendarStore;
	const store = useDisposable(() => new ResolvedStoreClass(parameters, adapter));
	useIsoLayoutEffect(() => store.updateStateFromParameters(parameters, adapter), [
		store,
		adapter,
		parameters
	]);
	return store;
}
//#endregion
//#region node_modules/@mui/x-scheduler-internals/use-event-calendar/useExtractEventCalendarParameters.mjs
var _excluded$20 = [
	"areEventsDraggable",
	"areEventsResizable",
	"canDragEventsFromTheOutside",
	"canDropEventsToTheOutside",
	"collapsedResources",
	"dateLocale",
	"defaultCollapsedResources",
	"defaultPreferences",
	"defaultView",
	"defaultVisibleDate",
	"defaultVisibleResources",
	"displayTimezone",
	"eventColor",
	"eventCreation",
	"eventModelStructure",
	"events",
	"onCollapsedResourcesChange",
	"onEventsChange",
	"onViewChange",
	"onVisibleDateChange",
	"onVisibleResourcesChange",
	"preferences",
	"preferencesMenuConfig",
	"readOnly",
	"dataSource",
	"shouldEventRequireResource",
	"resourceModelStructure",
	"resources",
	"showCurrentTimeIndicator",
	"view",
	"viewConfig",
	"views",
	"visibleDate",
	"visibleResources"
];
function useExtractEventCalendarParameters(props) {
	const { areEventsDraggable, areEventsResizable, canDragEventsFromTheOutside, canDropEventsToTheOutside, collapsedResources, dateLocale, defaultCollapsedResources, defaultPreferences, defaultView, defaultVisibleDate, defaultVisibleResources, displayTimezone, eventColor, eventCreation, eventModelStructure, events, onCollapsedResourcesChange, onEventsChange, onViewChange, onVisibleDateChange, onVisibleResourcesChange, preferences, preferencesMenuConfig, readOnly, dataSource, shouldEventRequireResource, resourceModelStructure, resources, showCurrentTimeIndicator, view, viewConfig, views, visibleDate, visibleResources } = props, forwardedProps = _objectWithoutPropertiesLoose(props, _excluded$20);
	return {
		parameters: import_react.useMemo(() => ({
			areEventsDraggable,
			areEventsResizable,
			canDragEventsFromTheOutside,
			canDropEventsToTheOutside,
			collapsedResources,
			dateLocale,
			defaultCollapsedResources,
			defaultPreferences,
			defaultView,
			defaultVisibleDate,
			defaultVisibleResources,
			displayTimezone,
			eventColor,
			eventCreation,
			eventModelStructure,
			events,
			onCollapsedResourcesChange,
			onEventsChange,
			onViewChange,
			onVisibleDateChange,
			onVisibleResourcesChange,
			preferences,
			preferencesMenuConfig,
			readOnly,
			dataSource,
			shouldEventRequireResource,
			resourceModelStructure,
			resources,
			showCurrentTimeIndicator,
			view,
			viewConfig,
			views,
			visibleDate,
			visibleResources
		}), [
			areEventsDraggable,
			areEventsResizable,
			canDragEventsFromTheOutside,
			canDropEventsToTheOutside,
			collapsedResources,
			dateLocale,
			defaultCollapsedResources,
			defaultPreferences,
			defaultView,
			defaultVisibleDate,
			defaultVisibleResources,
			displayTimezone,
			eventColor,
			eventCreation,
			eventModelStructure,
			events,
			onCollapsedResourcesChange,
			onEventsChange,
			onViewChange,
			onVisibleDateChange,
			onVisibleResourcesChange,
			preferences,
			preferencesMenuConfig,
			readOnly,
			shouldEventRequireResource,
			resourceModelStructure,
			resources,
			showCurrentTimeIndicator,
			view,
			viewConfig,
			views,
			visibleDate,
			visibleResources
		]),
		forwardedProps
	};
}
//#endregion
//#region node_modules/@mui/x-scheduler-internals/event-calendar-provider/EventCalendarProvider.mjs
var _excluded$19 = ["children", "storeClass"];
function EventCalendarProvider$1(props) {
	const { children, storeClass } = props;
	const store = useEventCalendar(_objectWithoutPropertiesLoose(props, _excluded$19), storeClass);
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(SchedulerStoreContext.Provider, {
		value: store,
		children
	});
}
//#endregion
//#region node_modules/@mui/x-scheduler/internals/components/error-container/errorContainerClasses.mjs
var errorContainerClassKeys = [
	"errorContainer",
	"errorAlert",
	"errorMessage"
];
var errorContainerSlots = {
	errorContainer: ["errorContainer"],
	errorAlert: ["errorAlert"],
	errorMessage: ["errorMessage"]
};
//#endregion
//#region node_modules/@mui/x-scheduler/event-calendar/eventCalendarClasses.mjs
function getEventCalendarUtilityClass(slot) {
	return generateUtilityClass("MuiEventCalendar", slot);
}
var eventCalendarClasses = generateUtilityClasses("MuiEventCalendar", [
	"root",
	"sidePanel",
	"mainPanel",
	"content",
	"miniCalendar",
	"miniCalendarHeader",
	"miniCalendarNavigation",
	"miniCalendarMonthLabel",
	"miniCalendarWeekdayHeader",
	"miniCalendarWeekdayCell",
	"miniCalendarGrid",
	"miniCalendarWeekRow",
	"miniCalendarDayCell",
	"miniCalendarDayButton",
	"headerToolbar",
	"headerToolbarActions",
	"headerToolbarLeftElement",
	"headerToolbarLabel",
	"headerToolbarDateNavigator",
	"viewSwitcher",
	"preferencesMenu",
	"resourcesTree",
	"resourcesTreeLabel",
	"resourcesTreeItem",
	"resourcesTreeItemLabel",
	"resourcesTreeItemCheckbox",
	"agendaView",
	"agendaViewRow",
	"agendaViewDayHeaderCell",
	"agendaViewDayNumberCell",
	"agendaViewWeekDayCell",
	"agendaViewWeekDayNameLabel",
	"agendaViewYearAndMonthLabel",
	"agendaViewEventsList",
	"agendaViewWeekNumberLabel",
	"agendaViewWeekNumberRow",
	"monthView",
	"monthViewGrid",
	"monthViewHeader",
	"monthViewHeaderCell",
	"monthViewWeekHeaderCell",
	"monthViewBody",
	"monthViewRow",
	"monthViewWeekNumberCell",
	"monthViewCell",
	"monthViewCellNumber",
	"monthViewCellNumberButton",
	"monthViewCellEvents",
	"monthViewMoreEvents",
	"monthViewPlaceholderContainer",
	"dayTimeGridContainer",
	"dayTimeGrid",
	"dayTimeGridHeader",
	"dayTimeGridHeaderCell",
	"dayTimeGridAllDayEventsGrid",
	"dayTimeGridAllDayEventsRow",
	"dayTimeGridAllDayEventsCell",
	"dayTimeGridAllDayEventsHeaderCell",
	"dayTimeGridHeaderContent",
	"dayTimeGridHeaderButton",
	"dayTimeGridHeaderDayName",
	"dayTimeGridHeaderDayNumber",
	"dayTimeGridBody",
	"dayTimeGridScrollableContent",
	"dayTimeGridTimeAxis",
	"dayTimeGridTimeAxisCell",
	"dayTimeGridTimeAxisText",
	"dayTimeGridGrid",
	"dayTimeGridColumn",
	"dayTimeGridColumnInteractiveLayer",
	"dayTimeGridCurrentTimeIndicator",
	"dayTimeGridCurrentTimeIndicatorCircle",
	"dayTimeGridCurrentTimeLabel",
	"dayTimeGridAllDayEventsCellEvents",
	"dayTimeGridAllDayEventContainer",
	"dayTimeGridScrollablePlaceholder",
	"dayGridEvent",
	"dayGridEventPlaceholder",
	"dayGridEventTitle",
	"dayGridEventTime",
	"dayGridEventRecurringIcon",
	"dayGridEventResizeHandler",
	"dayGridEventCardWrapper",
	"dayGridEventCardContent",
	"dayGridEventLinesClamp",
	"eventColorIndicator",
	"timeGridEvent",
	"timeGridEventPlaceholder",
	"timeGridEventSkeleton",
	"timeGridEventTitle",
	"timeGridEventTime",
	"timeGridEventRecurringIcon",
	"timeGridEventResizeHandler",
	"eventItemCard",
	"eventItemCardWrapper",
	"eventItemTitle",
	"eventItemTime",
	"eventItemRecurringIcon",
	"resourceLegendColor",
	"eventItemCardContent",
	"eventItemLinesClamp",
	"moreEventsPopoverHeader",
	"moreEventsPopoverTitle",
	"moreEventsPopoverBody",
	"eventDragPreview",
	"standaloneEvent",
	"agendaViewEventListItem",
	"sidePanelCollapse",
	"sidePanelDivider",
	"headerToolbarSidePanelToggle",
	"headerToolbarPreviousButton",
	"headerToolbarTodayButton",
	"headerToolbarNextButton",
	"headerToolbarWeekNumber",
	"preferencesMenuButton",
	"preferencesMenuList",
	"preferencesMenuListItemIcon",
	"preferencesMenuListSubheader",
	"preferencesMenuItem",
	"preferencesMenuDivider",
	"preferencesMenuListItemText",
	"viewSwitcherButton",
	"viewSwitcherMenu",
	"miniCalendarPreviousButton",
	"miniCalendarNextButton",
	"moreEventsPopover",
	"headerToolbarCompactMenuButton",
	"headerToolbarCompactTodayButton",
	"sidePanelDrawer",
	"sidePanelDrawerBackdrop",
	"sidePanelDrawerViewport",
	"sidePanelDrawerHeader",
	"sidePanelDrawerTitle",
	"sidePanelDrawerCloseButton",
	"sidePanelDrawerViewList",
	"sidePanelDrawerViewItem",
	"sidePanelDrawerPreferencesButton",
	"sidePanelDrawerPreferences",
	"sidePanelDrawerPreferencesList",
	...errorContainerClassKeys,
	...eventSkeletonClassKeys,
	...eventDialogClassKeys
]);
var slots = _extends({
	root: ["root"],
	sidePanel: ["sidePanel"],
	mainPanel: ["mainPanel"],
	content: ["content"],
	miniCalendar: ["miniCalendar"],
	miniCalendarHeader: ["miniCalendarHeader"],
	miniCalendarNavigation: ["miniCalendarNavigation"],
	miniCalendarMonthLabel: ["miniCalendarMonthLabel"],
	miniCalendarWeekdayHeader: ["miniCalendarWeekdayHeader"],
	miniCalendarWeekdayCell: ["miniCalendarWeekdayCell"],
	miniCalendarGrid: ["miniCalendarGrid"],
	miniCalendarWeekRow: ["miniCalendarWeekRow"],
	miniCalendarDayCell: ["miniCalendarDayCell"],
	miniCalendarDayButton: ["miniCalendarDayButton"],
	headerToolbar: ["headerToolbar"],
	headerToolbarActions: ["headerToolbarActions"],
	headerToolbarLeftElement: ["headerToolbarLeftElement"],
	headerToolbarLabel: ["headerToolbarLabel"],
	headerToolbarDateNavigator: ["headerToolbarDateNavigator"],
	viewSwitcher: ["viewSwitcher"],
	preferencesMenu: ["preferencesMenu"],
	resourcesTree: ["resourcesTree"],
	resourcesTreeLabel: ["resourcesTreeLabel"],
	resourcesTreeItem: ["resourcesTreeItem"],
	resourcesTreeItemLabel: ["resourcesTreeItemLabel"],
	resourcesTreeItemCheckbox: ["resourcesTreeItemCheckbox"],
	agendaView: ["agendaView"],
	agendaViewRow: ["agendaViewRow"],
	agendaViewDayHeaderCell: ["agendaViewDayHeaderCell"],
	agendaViewDayNumberCell: ["agendaViewDayNumberCell"],
	agendaViewWeekDayCell: ["agendaViewWeekDayCell"],
	agendaViewWeekDayNameLabel: ["agendaViewWeekDayNameLabel"],
	agendaViewYearAndMonthLabel: ["agendaViewYearAndMonthLabel"],
	agendaViewEventsList: ["agendaViewEventsList"],
	agendaViewWeekNumberLabel: ["agendaViewWeekNumberLabel"],
	agendaViewWeekNumberRow: ["agendaViewWeekNumberRow"],
	monthView: ["monthView"],
	monthViewGrid: ["monthViewGrid"],
	monthViewHeader: ["monthViewHeader"],
	monthViewHeaderCell: ["monthViewHeaderCell"],
	monthViewWeekHeaderCell: ["monthViewWeekHeaderCell"],
	monthViewBody: ["monthViewBody"],
	monthViewRow: ["monthViewRow"],
	monthViewWeekNumberCell: ["monthViewWeekNumberCell"],
	monthViewCell: ["monthViewCell"],
	monthViewCellNumber: ["monthViewCellNumber"],
	monthViewCellNumberButton: ["monthViewCellNumberButton"],
	monthViewCellEvents: ["monthViewCellEvents"],
	monthViewMoreEvents: ["monthViewMoreEvents"],
	monthViewPlaceholderContainer: ["monthViewPlaceholderContainer"],
	dayTimeGridContainer: ["dayTimeGridContainer"],
	dayTimeGrid: ["dayTimeGrid"],
	dayTimeGridHeader: ["dayTimeGridHeader"],
	dayTimeGridHeaderCell: ["dayTimeGridHeaderCell"],
	dayTimeGridAllDayEventsGrid: ["dayTimeGridAllDayEventsGrid"],
	dayTimeGridAllDayEventsRow: ["dayTimeGridAllDayEventsRow"],
	dayTimeGridAllDayEventsCell: ["dayTimeGridAllDayEventsCell"],
	dayTimeGridAllDayEventsHeaderCell: ["dayTimeGridAllDayEventsHeaderCell"],
	dayTimeGridHeaderContent: ["dayTimeGridHeaderContent"],
	dayTimeGridHeaderButton: ["dayTimeGridHeaderButton"],
	dayTimeGridHeaderDayName: ["dayTimeGridHeaderDayName"],
	dayTimeGridHeaderDayNumber: ["dayTimeGridHeaderDayNumber"],
	dayTimeGridBody: ["dayTimeGridBody"],
	dayTimeGridScrollableContent: ["dayTimeGridScrollableContent"],
	dayTimeGridTimeAxis: ["dayTimeGridTimeAxis"],
	dayTimeGridTimeAxisCell: ["dayTimeGridTimeAxisCell"],
	dayTimeGridTimeAxisText: ["dayTimeGridTimeAxisText"],
	dayTimeGridGrid: ["dayTimeGridGrid"],
	dayTimeGridColumn: ["dayTimeGridColumn"],
	dayTimeGridColumnInteractiveLayer: ["dayTimeGridColumnInteractiveLayer"],
	dayTimeGridCurrentTimeIndicator: ["dayTimeGridCurrentTimeIndicator"],
	dayTimeGridCurrentTimeIndicatorCircle: ["dayTimeGridCurrentTimeIndicatorCircle"],
	dayTimeGridCurrentTimeLabel: ["dayTimeGridCurrentTimeLabel"],
	dayTimeGridAllDayEventsCellEvents: ["dayTimeGridAllDayEventsCellEvents"],
	dayTimeGridAllDayEventContainer: ["dayTimeGridAllDayEventContainer"],
	dayTimeGridScrollablePlaceholder: ["dayTimeGridScrollablePlaceholder"],
	dayGridEvent: ["dayGridEvent"],
	dayGridEventPlaceholder: ["dayGridEventPlaceholder"],
	dayGridEventTitle: ["dayGridEventTitle"],
	dayGridEventTime: ["dayGridEventTime"],
	dayGridEventRecurringIcon: ["dayGridEventRecurringIcon"],
	dayGridEventResizeHandler: ["dayGridEventResizeHandler"],
	dayGridEventCardWrapper: ["dayGridEventCardWrapper"],
	dayGridEventCardContent: ["dayGridEventCardContent"],
	dayGridEventLinesClamp: ["dayGridEventLinesClamp"],
	eventColorIndicator: ["eventColorIndicator"],
	timeGridEvent: ["timeGridEvent"],
	timeGridEventPlaceholder: ["timeGridEventPlaceholder"],
	timeGridEventSkeleton: ["timeGridEventSkeleton"],
	timeGridEventTitle: ["timeGridEventTitle"],
	timeGridEventTime: ["timeGridEventTime"],
	timeGridEventRecurringIcon: ["timeGridEventRecurringIcon"],
	timeGridEventResizeHandler: ["timeGridEventResizeHandler"],
	eventItemCard: ["eventItemCard"],
	eventItemCardWrapper: ["eventItemCardWrapper"],
	eventItemTitle: ["eventItemTitle"],
	eventItemTime: ["eventItemTime"],
	eventItemRecurringIcon: ["eventItemRecurringIcon"],
	resourceLegendColor: ["resourceLegendColor"],
	eventItemCardContent: ["eventItemCardContent"],
	eventItemLinesClamp: ["eventItemLinesClamp"],
	moreEventsPopoverHeader: ["moreEventsPopoverHeader"],
	moreEventsPopoverTitle: ["moreEventsPopoverTitle"],
	moreEventsPopoverBody: ["moreEventsPopoverBody"],
	eventDragPreview: ["eventDragPreview"],
	standaloneEvent: ["standaloneEvent"],
	agendaViewEventListItem: ["agendaViewEventListItem"],
	sidePanelCollapse: ["sidePanelCollapse"],
	sidePanelDivider: ["sidePanelDivider"],
	headerToolbarSidePanelToggle: ["headerToolbarSidePanelToggle"],
	headerToolbarPreviousButton: ["headerToolbarPreviousButton"],
	headerToolbarTodayButton: ["headerToolbarTodayButton"],
	headerToolbarNextButton: ["headerToolbarNextButton"],
	headerToolbarWeekNumber: ["headerToolbarWeekNumber"],
	preferencesMenuButton: ["preferencesMenuButton"],
	preferencesMenuList: ["preferencesMenuList"],
	preferencesMenuListItemIcon: ["preferencesMenuListItemIcon"],
	preferencesMenuListSubheader: ["preferencesMenuListSubheader"],
	preferencesMenuItem: ["preferencesMenuItem"],
	preferencesMenuDivider: ["preferencesMenuDivider"],
	preferencesMenuListItemText: ["preferencesMenuListItemText"],
	viewSwitcherButton: ["viewSwitcherButton"],
	viewSwitcherMenu: ["viewSwitcherMenu"],
	miniCalendarPreviousButton: ["miniCalendarPreviousButton"],
	miniCalendarNextButton: ["miniCalendarNextButton"],
	moreEventsPopover: ["moreEventsPopover"],
	headerToolbarCompactMenuButton: ["headerToolbarCompactMenuButton"],
	headerToolbarCompactTodayButton: ["headerToolbarCompactTodayButton"],
	sidePanelDrawer: ["sidePanelDrawer"],
	sidePanelDrawerBackdrop: ["sidePanelDrawerBackdrop"],
	sidePanelDrawerViewport: ["sidePanelDrawerViewport"],
	sidePanelDrawerHeader: ["sidePanelDrawerHeader"],
	sidePanelDrawerTitle: ["sidePanelDrawerTitle"],
	sidePanelDrawerCloseButton: ["sidePanelDrawerCloseButton"],
	sidePanelDrawerViewList: ["sidePanelDrawerViewList"],
	sidePanelDrawerViewItem: ["sidePanelDrawerViewItem"],
	sidePanelDrawerPreferencesButton: ["sidePanelDrawerPreferencesButton"],
	sidePanelDrawerPreferences: ["sidePanelDrawerPreferences"],
	sidePanelDrawerPreferencesList: ["sidePanelDrawerPreferencesList"]
}, errorContainerSlots, eventSkeletonSlots, eventDialogSlots);
/**
* Computes the utility classes for EventCalendar components.
* Exported so both EventCalendar and EventCalendarPremium can use it.
*/
var useEventCalendarUtilityClasses = (classes) => {
	return composeClasses(slots, getEventCalendarUtilityClass, classes);
};
//#endregion
//#region node_modules/@mui/x-scheduler/utils/getSchedulerLocalization.mjs
var import_prop_types = /* @__PURE__ */ __toESM(require_prop_types(), 1);
var getSchedulerLocalization = (translations) => ({ components: {
	MuiEventCalendar: { defaultProps: { localeText: _extends({}, translations.dialog, translations.calendar) } },
	MuiEventTimeline: { defaultProps: { localeText: _extends({}, translations.dialog, translations.timeline) } }
} });
var enUS = getSchedulerLocalization({
	dialog: {
		colorPickerLabel: "Event color",
		dateTimeSectionLabel: "Date & time",
		resourceColorSectionLabel: "Resource & color",
		allDayLabel: "All Day",
		closeButtonAriaLabel: "Close",
		closeButtonLabel: "Close",
		deleteEvent: "Delete event",
		descriptionLabel: "Description",
		endDateLabel: "End date",
		endTimeLabel: "End time",
		eventTitleAriaLabel: "Event title",
		generalTabLabel: "General",
		labelNoResource: "No resource",
		labelInvalidResource: "Invalid resource",
		recurrenceLabel: "Recurrence",
		recurrenceNoRepeat: "Don't repeat",
		recurrenceCustomRepeat: "Custom repeat rule",
		recurrenceDailyPresetLabel: "Repeats daily",
		recurrenceDailyFrequencyLabel: "days",
		recurrenceEndsLabel: "Ends",
		recurrenceEndsAfterLabel: "After",
		recurrenceEndsNeverLabel: "Never",
		recurrenceEndsUntilLabel: "Until",
		recurrenceEndsTimesLabel: "times",
		recurrenceEveryLabel: "Every",
		recurrenceRepeatLabel: "Repeat",
		recurrenceTabLabel: "Recurrence",
		recurrenceMainSelectCustomLabel: "Recurrence",
		recurrenceWeeklyFrequencyLabel: "weeks",
		recurrenceWeeklyPresetLabel: ({ weekdayName }) => `Repeats weekly on ${weekdayName}`,
		recurrenceMonthlyFrequencyLabel: "months",
		recurrenceMonthlyDayOfMonthLabel: (dayNumber) => `Day ${dayNumber}`,
		recurrenceMonthlyLastWeekAriaLabel: (weekDay) => `${weekDay} of the last week of the month`,
		recurrenceMonthlyLastWeekLabel: (weekDay) => `${weekDay} last week`,
		recurrenceMonthlyPresetLabel: (dayNumber) => `Repeats monthly on day ${dayNumber}`,
		recurrenceMonthlyWeekNumberAriaLabel: (ord, weekDay) => `${weekDay} week ${ord} of the month`,
		recurrenceMonthlyWeekNumberLabel: (ord, weekDay) => `${weekDay} week ${ord}`,
		recurrenceWeeklyMonthlySpecificInputsLabel: "On",
		recurrenceYearlyFrequencyLabel: "years",
		recurrenceYearlyPresetLabel: (date) => `Repeats annually on ${date}`,
		noResourceAriaLabel: "No specific resource",
		selectColorAriaLabel: (color) => `Select ${color} as event color`,
		resourceLabel: "Resource",
		requiredResourceError: "A resource is required.",
		saveChanges: "Save",
		startDateAfterEndDateError: "Start date/time must be before end date/time.",
		startDateLabel: "Start date",
		startTimeLabel: "Start time",
		all: "All events",
		cancel: "Cancel",
		confirm: "Confirm",
		onlyThis: "Only this event",
		radioGroupAriaLabel: "Editing recurring events scope",
		thisAndFollowing: "This and following events",
		title: "Apply this change to:"
	},
	calendar: {
		resourcesLabel: "Resources",
		agenda: "Agenda",
		day: "Day",
		month: "Month",
		other: "Other",
		today: "Today",
		week: "Week",
		time: "Time",
		days: "Days",
		months: "Months",
		weeks: "Weeks",
		years: "Years",
		closeSidePanel: "Close side panel",
		openSidePanel: "Open side panel",
		openMenu: "Open menu",
		amPm12h: "12-hour (1:00PM)",
		hour24h: "24-hour (13:00)",
		preferencesMenu: "Preferences",
		showWeekends: "Show weekends",
		showEmptyDaysInAgenda: "Show empty days",
		showWeekNumber: "Show week number",
		timeFormat: "Time format",
		viewSpecificOptions: (view) => `${view} view options`,
		startWeekOn: "Start week on",
		weekdaySunday: "Sunday",
		weekdayMonday: "Monday",
		weekdaySaturday: "Saturday",
		allDay: "All day",
		hiddenEvents: (hiddenEventsCount) => `${hiddenEventsCount} more..`,
		nextTimeSpan: (timeSpan) => `Next ${timeSpan}`,
		previousTimeSpan: (timeSpan) => `Previous ${timeSpan}`,
		resourceAriaLabel: (resourceName) => `Resource: ${resourceName}`,
		weekAbbreviation: "W",
		weekNumberAriaLabel: (weekNumber) => `Week ${weekNumber}`,
		eventItemMultiDayLabel: (endDate) => `Ends ${endDate}`,
		miniCalendarLabel: "Calendar",
		miniCalendarGoToPreviousMonth: "Show previous month in calendar",
		miniCalendarGoToNextMonth: "Show next month in calendar",
		calendarContentAriaLabel: "Calendar content",
		timelineResourceTitleHeader: "Resource title"
	},
	timeline: { timelineResourceTitleHeader: "Resource title" }
});
//#endregion
//#region node_modules/@mui/x-scheduler/internals/constants/defaultLocaleText.mjs
var EVENT_CALENDAR_DEFAULT_LOCALE_TEXT = enUS.components.MuiEventCalendar.defaultProps.localeText;
enUS.components.MuiEventTimeline.defaultProps.localeText;
//#endregion
//#region node_modules/@mui/x-scheduler/internals/constants/responsiveTypography.mjs
var EVENT_CALENDAR_CONTAINER_NAME = "mui-event-calendar-content";
var EVENT_CALENDAR_ROOT_CONTAINER_NAME = "mui-event-calendar-root";
var containerWidthBelow = (containerName, widthPx) => `@container ${containerName} (width < ${widthPx}px)`;
var containerWidthFrom = (containerName, widthPx) => `@container ${containerName} (width >= ${widthPx}px)`;
var containerWidthBetween = (containerName, minWidthPx, maxWidthPx) => `@container ${containerName} (${minWidthPx}px <= width < ${maxWidthPx}px)`;
var eventCalendarRootCompactQuery = containerWidthBelow(EVENT_CALENDAR_ROOT_CONTAINER_NAME, 550);
var eventCalendarRootExpandedQuery = containerWidthFrom(EVENT_CALENDAR_ROOT_CONTAINER_NAME, 550);
var eventCalendarContentCompactQuery = containerWidthBelow(EVENT_CALENDAR_CONTAINER_NAME, 550);
var responsiveTokens = _extends({ "--EventCalendar-size-fixedCellWidth": "68px" }, {
	"--EventCalendar-fontSize-eventTitle-sm": "0.6rem",
	"--EventCalendar-fontSize-eventTitle-md": "0.7rem",
	"--EventCalendar-fontSize-eventTitle-lg": "0.75rem",
	"--EventCalendar-fontSize-dayNumber-sm": "1rem",
	"--EventCalendar-fontSize-dayNumber-md": "1.2rem",
	"--EventCalendar-fontSize-dayNumber-lg": "1.5rem",
	"--EventCalendar-fontSize-agendaDayNumber-sm": "1rem",
	"--EventCalendar-fontSize-agendaDayNumber-md": "1.2rem",
	"--EventCalendar-fontSize-agendaDayNumber-lg": "1.5rem",
	"--EventCalendar-fontSize-timeText-sm": "0.6rem",
	"--EventCalendar-fontSize-timeText-md": "0.7rem",
	"--EventCalendar-fontSize-timeText-lg": "0.75rem",
	"--EventCalendar-fontSize-recurringIcon-sm": "1rem",
	"--EventCalendar-fontSize-recurringIcon-md": "1.125rem",
	"--EventCalendar-fontSize-recurringIcon-lg": "1.25rem",
	"--EventCalendar-fontSize-eventTitle": "var(--EventCalendar-fontSize-eventTitle-lg)",
	"--EventCalendar-fontSize-dayNumber": "var(--EventCalendar-fontSize-dayNumber-lg)",
	"--EventCalendar-fontSize-agendaDayNumber": "var(--EventCalendar-fontSize-agendaDayNumber-lg)",
	"--EventCalendar-fontSize-timeText": "var(--EventCalendar-fontSize-timeText-lg)",
	"--EventCalendar-fontSize-recurringIcon": "var(--EventCalendar-fontSize-recurringIcon-lg)"
});
var responsiveTypographyContainerQueries = {
	[eventCalendarContentCompactQuery]: {
		"--EventCalendar-size-fixedCellWidth": "54px",
		"--EventCalendar-fontSize-eventTitle": "var(--EventCalendar-fontSize-eventTitle-sm)",
		"--EventCalendar-fontSize-dayNumber": "var(--EventCalendar-fontSize-dayNumber-sm)",
		"--EventCalendar-fontSize-agendaDayNumber": "var(--EventCalendar-fontSize-agendaDayNumber-sm)",
		"--EventCalendar-fontSize-timeText": "var(--EventCalendar-fontSize-timeText-sm)",
		"--EventCalendar-fontSize-recurringIcon": "var(--EventCalendar-fontSize-recurringIcon-sm)"
	},
	[containerWidthBetween(EVENT_CALENDAR_CONTAINER_NAME, 550, 800)]: {
		"--EventCalendar-fontSize-eventTitle": "var(--EventCalendar-fontSize-eventTitle-md)",
		"--EventCalendar-fontSize-dayNumber": "var(--EventCalendar-fontSize-dayNumber-md)",
		"--EventCalendar-fontSize-agendaDayNumber": "var(--EventCalendar-fontSize-agendaDayNumber-md)",
		"--EventCalendar-fontSize-timeText": "var(--EventCalendar-fontSize-timeText-md)",
		"--EventCalendar-fontSize-recurringIcon": "var(--EventCalendar-fontSize-recurringIcon-md)"
	}
};
//#endregion
//#region node_modules/@mui/x-scheduler/internals/components/EventCalendarProvider.mjs
/**
* Root wrapper for standalone views that provides CSS variable tokens.
* This ensures event colors work correctly outside of EventCalendar.
*
* The @container queries spread below redefine the effective typography
* vars on this slot when the surrounding ResponsiveTypographyContainer
* (which Standalone*View components wrap their tree in) crosses a tier
* threshold. display:contents doesn't block custom-property inheritance,
* so descendants pick up the retargeted values.
*/
var _excluded$18 = ["children"];
var StandaloneViewRoot = styled("div", {
	name: "MuiEventCalendar",
	slot: "StandaloneViewRoot"
})(({ theme }) => _extends({
	display: "contents",
	fontFamily: theme.typography.fontFamily,
	boxSizing: "border-box",
	"*, *::before, *::after": { boxSizing: "inherit" }
}, responsiveTypographyContainerQueries));
function EventCalendarProvider(props) {
	const { children } = props, other = _objectWithoutPropertiesLoose(props, _excluded$18);
	const schedulerId = useId();
	const calendarStyledValue = import_react.useMemo(() => ({
		schedulerId,
		classes: eventCalendarClasses,
		localeText: EVENT_CALENDAR_DEFAULT_LOCALE_TEXT
	}), [schedulerId]);
	const dialogStyledValue = import_react.useMemo(() => ({
		schedulerId,
		classes: eventCalendarClasses,
		localeText: EVENT_CALENDAR_DEFAULT_LOCALE_TEXT
	}), [schedulerId]);
	const sharedComponentsStyledValue = import_react.useMemo(() => ({ classes: eventCalendarClasses }), []);
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(EventCalendarProvider$1, _extends({}, other, { children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(EventCalendarStyledContext.Provider, {
		value: calendarStyledValue,
		children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(EventDialogStyledContext.Provider, {
			value: dialogStyledValue,
			children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(SharedComponentsStyledContext.Provider, {
				value: sharedComponentsStyledValue,
				children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(StandaloneViewRoot, { children })
			})
		})
	}) }));
}
//#endregion
//#region node_modules/@mui/x-scheduler/internals/components/ResponsiveTypographyContainer.mjs
var ResponsiveTypographyContainer = styled("div", {
	name: "MuiEventCalendar",
	slot: "ResponsiveTypographyContainer"
})(_extends({
	display: "flex",
	flexDirection: "column",
	flex: 1,
	width: "100%",
	height: "100%",
	minHeight: 0,
	minWidth: 0,
	containerType: "inline-size",
	containerName: EVENT_CALENDAR_CONTAINER_NAME
}, responsiveTokens));
//#endregion
//#region node_modules/@mui/x-internals/useResizeObserver/useResizeObserver.mjs
var noop = () => {};
function useResizeObserver(ref, fn, enabled) {
	const fnRef = import_react.useRef(null);
	fnRef.current = fn;
	useEnhancedEffect(() => {
		if (enabled === false || typeof ResizeObserver === "undefined") return noop;
		let frameID = 0;
		const target = ref.current;
		const observer = new ResizeObserver((entries) => {
			frameID = requestAnimationFrame(() => {
				fnRef.current(entries);
			});
		});
		if (target) observer.observe(target);
		return () => {
			if (frameID) cancelAnimationFrame(frameID);
			observer.disconnect();
		};
	}, [ref, enabled]);
}
//#endregion
//#region node_modules/@mui/x-scheduler-internals/use-event-occurrences-with-day-grid-position/useEventOccurrencesWithDayGridPosition.mjs
/**
* Places event occurrences for a list of days, where if an event is rendered in a day, it fills the entire day cell (no notion of time).
*/
function useEventOccurrencesWithDayGridPosition(parameters) {
	const { days, occurrencesMap, shouldAddPosition, maxEvents } = parameters;
	const adapter = useAdapterContext();
	return import_react.useMemo(() => {
		const dayListSize = days.length;
		const usedIndexesByDay = [];
		const activeSegments = {};
		const processedDays = days.map((day, dayIndex) => {
			const usedIndexes = /* @__PURE__ */ new Set();
			usedIndexesByDay.push(usedIndexes);
			const needsPosition = [];
			const withoutPosition = [];
			for (const occurrence of occurrencesMap.get(day.key) ?? []) if (shouldAddPosition ? shouldAddPosition(occurrence) : true) needsPosition.push(occurrence);
			else withoutPosition.push(occurrence);
			const baseSorted = sortEventOccurrences(needsPosition);
			const sortedNeedsPosition = maxEvents == null ? baseSorted : baseSorted.sort((a, b) => {
				const aIsActive = activeSegments[a.key] != null ? 0 : 1;
				const bIsActive = activeSegments[b.key] != null ? 0 : 1;
				if (aIsActive !== bIsActive) return aIsActive - bIsActive;
				const aSpan = adapter.differenceInDays(a.displayTimezone.end.value, a.displayTimezone.start.value);
				return adapter.differenceInDays(b.displayTimezone.end.value, b.displayTimezone.start.value) - aSpan;
			});
			let effectiveMax = Infinity;
			if (maxEvents != null) effectiveMax = needsPosition.length > maxEvents ? maxEvents - 1 : maxEvents;
			for (const occurrence of sortedNeedsPosition) {
				const active = activeSegments[occurrence.key];
				if (active != null && !active.hidden && active.position.index <= effectiveMax) usedIndexes.add(active.position.index);
			}
			const withPosition = [];
			for (const occurrence of sortedNeedsPosition) {
				let smallestAvailableIndex = 1;
				while (usedIndexes.has(smallestAvailableIndex)) smallestAvailableIndex += 1;
				const active = activeSegments[occurrence.key];
				let position;
				if (active != null && !active.hidden && active.position.index <= effectiveMax) position = {
					index: active.position.index,
					daySpan: 1,
					isInvisible: true
				};
				else {
					if (active != null && !active.hidden) active.position.daySpan = dayIndex - active.startDayIndex;
					if (smallestAvailableIndex <= effectiveMax) {
						position = {
							index: smallestAvailableIndex,
							daySpan: Math.min(adapter.differenceInDays(occurrence.displayTimezone.end.value, day.value) + 1, dayListSize - dayIndex)
						};
						activeSegments[occurrence.key] = {
							position,
							startDayIndex: dayIndex,
							hidden: false
						};
					} else {
						position = {
							index: active != null ? active.position.index : smallestAvailableIndex,
							daySpan: 1,
							isInvisible: true
						};
						activeSegments[occurrence.key] = {
							position,
							startDayIndex: dayIndex,
							hidden: true
						};
					}
				}
				usedIndexes.add(position.index);
				withPosition.push(_extends({}, occurrence, { position }));
			}
			withPosition.sort((a, b) => a.position.index - b.position.index);
			return _extends({}, day, {
				withPosition,
				withoutPosition
			});
		});
		const usedIndexesFlat = usedIndexesByDay.flatMap((set) => Array.from(set));
		return {
			days: processedDays,
			maxIndex: usedIndexesFlat.length === 0 ? 1 : Math.max(...usedIndexesFlat)
		};
	}, [
		adapter,
		days,
		maxEvents,
		occurrencesMap,
		shouldAddPosition
	]);
}
//#endregion
//#region node_modules/@mui/x-scheduler-internals/base-ui-copy/utils/getStateAttributesProps.mjs
function getStateAttributesProps(state, customMapping) {
	const props = {};
	for (const key in state) {
		const value = state[key];
		if (customMapping?.hasOwnProperty(key)) {
			const customProps = customMapping[key](value);
			if (customProps != null) Object.assign(props, customProps);
			continue;
		}
		if (value === true) props[`data-${key.toLowerCase()}`] = "";
		else if (value) props[`data-${key.toLowerCase()}`] = value.toString();
	}
	return props;
}
//#endregion
//#region node_modules/@mui/x-scheduler-internals/base-ui-copy/utils/resolveClassName.mjs
/**
* If the provided className is a string, it will be returned as is.
* Otherwise, the function will call the className function with the state as the first argument.
*
* @param className
* @param state
*/
function resolveClassName(className, state) {
	return typeof className === "function" ? className(state) : className;
}
//#endregion
//#region node_modules/@mui/x-scheduler-internals/base-ui-copy/utils/resolveStyle.mjs
/**
* If the provided style is an object, it will be returned as is.
* Otherwise, the function will call the style function with the state as the first argument.
*
* @param style
* @param state
*/
function resolveStyle(style, state) {
	return typeof style === "function" ? style(state) : style;
}
//#endregion
//#region node_modules/@mui/x-scheduler-internals/base-ui-copy/utils/useRenderElement.mjs
/**
* Renders a Base UI element.
*
* @param element The default HTML element to render. Can be overridden by the `render` prop.
* @param componentProps An object containing the `render` and `className` props to be used for element customization. Other props are ignored.
* @param params Additional parameters for rendering the element.
*/
function useRenderElement(element, componentProps, params = {}) {
	const renderProp = componentProps.render;
	const outProps = useRenderElementProps(componentProps, params);
	if (params.enabled === false) return null;
	return evaluateRenderProp(element, renderProp, outProps, params.state ?? EMPTY_OBJECT);
}
/**
* Computes render element final props.
*/
function useRenderElementProps(componentProps, params = {}) {
	const { className: classNameProp, style: styleProp, render: renderProp } = componentProps;
	const { state = EMPTY_OBJECT, ref, props, stateAttributesMapping, enabled = true } = params;
	const className = enabled ? resolveClassName(classNameProp, state) : void 0;
	const style = enabled ? resolveStyle(styleProp, state) : void 0;
	const stateProps = enabled ? getStateAttributesProps(state, stateAttributesMapping) : EMPTY_OBJECT;
	const resolvedProps = enabled && props ? resolveRenderFunctionProps(props) : void 0;
	const outProps = enabled ? mergeObjects(stateProps, resolvedProps) ?? {} : EMPTY_OBJECT;
	if (typeof document !== "undefined") if (!enabled) useMergedRefs(null, null);
	else if (Array.isArray(ref)) outProps.ref = useMergedRefsN([
		outProps.ref,
		getReactElementRef(renderProp),
		...ref
	]);
	else outProps.ref = useMergedRefs(outProps.ref, getReactElementRef(renderProp), ref);
	if (!enabled) return EMPTY_OBJECT;
	if (className !== void 0) outProps.className = mergeClassNames(outProps.className, className);
	if (style !== void 0) outProps.style = mergeObjects(outProps.style, style);
	return outProps;
}
function resolveRenderFunctionProps(props) {
	if (Array.isArray(props)) return mergePropsN(props);
	return mergeProps(void 0, props);
}
var REACT_LAZY_TYPE = Symbol.for("react.lazy");
var COMPONENT_IDENTIFIER_PATTERN = /^[A-Z][A-Za-z0-9$]*$/;
var LOWERCASE_CHARACTER_PATTERN = /[a-z]/;
function evaluateRenderProp(element, render, props, state) {
	if (render) {
		if (typeof render === "function") {
			warnIfRenderPropLooksLikeComponent(render);
			return render(props, state);
		}
		const mergedProps = mergeProps(props, render.props);
		mergedProps.ref = props.ref;
		let newElement = render;
		if (newElement?.$$typeof === REACT_LAZY_TYPE) newElement = import_react.Children.toArray(render)[0];
		if (!/*#__PURE__*/ import_react.isValidElement(newElement)) throw new Error([
			"Base UI: The `render` prop was provided an invalid React element as `React.isValidElement(render)` is `false`.",
			"A valid React element must be provided to the `render` prop because it is cloned with props to replace the default element.",
			"https://base-ui.com/r/invalid-render-prop"
		].join("\n"));
		return /*#__PURE__*/ import_react.cloneElement(newElement, mergedProps);
	}
	if (element) {
		if (typeof element === "string") return renderTag(element, props);
	}
	throw new Error("Base UI: Render element or function are not defined.");
}
function warnIfRenderPropLooksLikeComponent(renderFn) {
	const functionName = renderFn.name;
	if (functionName.length === 0) return;
	if (!COMPONENT_IDENTIFIER_PATTERN.test(functionName)) return;
	if (!LOWERCASE_CHARACTER_PATTERN.test(functionName)) return;
	warn(`The \`render\` prop received a function named \`${functionName}\` that starts with an uppercase letter.`, "This usually means a React component was passed directly as `render={Component}`.", "Base UI calls `render` as a plain function, which can break the Rules of Hooks during reconciliation.", "If this is an intentional render callback, rename it to start with a lowercase letter.", "Use `render={<Component />}` or `render={(props) => <Component {...props} />}` instead.", "https://base-ui.com/r/invalid-render-prop");
}
function renderTag(Tag, props) {
	if (Tag === "button") return /*#__PURE__*/ (0, import_react.createElement)("button", _extends({ type: "button" }, props, { key: props.key }));
	if (Tag === "img") return /*#__PURE__*/ (0, import_react.createElement)("img", _extends({ alt: "" }, props, { key: props.key }));
	return /*#__PURE__*/ import_react.createElement(Tag, props);
}
//#endregion
//#region node_modules/@mui/x-scheduler-internals/calendar-grid/root/CalendarGridRootContext.mjs
var CalendarGridRootContext = /*#__PURE__*/ import_react.createContext(void 0);
CalendarGridRootContext.displayName = "CalendarGridRootContext";
function useCalendarGridRootContext() {
	const context = import_react.useContext(CalendarGridRootContext);
	if (context === void 0) throw new Error("MUI X Scheduler: `CalendarGridRootContext` is missing. CalendarGrid parts must be placed within <CalendarGrid.Root />.");
	return context;
}
//#endregion
//#region node_modules/@mui/x-scheduler-internals/calendar-grid/root/CalendarGridRoot.mjs
var _excluded$17 = [
	"className",
	"render",
	"style",
	"id",
	"rowTypes",
	"rowsPerType"
];
var DEFAULT_ROWS_PER_TYPE = {};
var CalendarGridRoot = /*#__PURE__*/ import_react.forwardRef(function CalendarGridRoot(componentProps, forwardedRef) {
	const { id: idProp, rowTypes = DEFAULT_ROW_TYPES, rowsPerType: rowsPerTypeProp = DEFAULT_ROWS_PER_TYPE } = componentProps, elementProps = _objectWithoutPropertiesLoose(componentProps, _excluded$17);
	const id = useId(idProp);
	const rowsPerType = rowsPerTypeProp;
	const rootRef = import_react.useRef(null);
	const [focusedCell, setFocusedCellState] = import_react.useState(null);
	const setFocusedCell = import_react.useCallback((coordinates) => {
		setFocusedCellState((prev) => prev?.rowType === coordinates.rowType && prev.rowIndex === coordinates.rowIndex && prev.columnIndex === coordinates.columnIndex ? prev : coordinates);
	}, []);
	const handleBlur = import_react.useCallback((event) => {
		if (!rootRef.current?.contains(event.relatedTarget)) setFocusedCellState(null);
	}, []);
	const contextValue = import_react.useMemo(() => ({
		id,
		focusedCell,
		setFocusedCell,
		rowTypes,
		rowsPerType
	}), [
		id,
		focusedCell,
		setFocusedCell,
		rowTypes,
		rowsPerType
	]);
	const element = useRenderElement("div", componentProps, {
		ref: [forwardedRef, rootRef],
		props: [elementProps, {
			role: "grid",
			id,
			onBlur: handleBlur
		}]
	});
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(CalendarGridRootContext.Provider, {
		value: contextValue,
		children: element
	});
});
CalendarGridRoot.displayName = "CalendarGridRoot";
//#endregion
//#region node_modules/@mui/x-scheduler-internals/base-ui-copy/composite/list/CompositeListContext.mjs
var CompositeListContext = /*#__PURE__*/ import_react.createContext({
	register: () => {},
	unregister: () => {},
	subscribeMapChange: () => {
		return () => {};
	},
	elementsRef: { current: [] },
	nextIndexRef: { current: 0 }
});
CompositeListContext.displayName = "CompositeListContext";
function useCompositeListContext() {
	return import_react.useContext(CompositeListContext);
}
//#endregion
//#region node_modules/@mui/x-scheduler-internals/base-ui-copy/composite/list/CompositeList.mjs
/**
* Provides context for a list of items in a composite component.
* @internal
*/
function CompositeList(props) {
	const { children, elementsRef, labelsRef, onMapChange: onMapChangeProp } = props;
	const onMapChange = useStableCallback(onMapChangeProp);
	const nextIndexRef = import_react.useRef(0);
	const listeners = useRefWithInit(createListeners).current;
	const map = useRefWithInit(createMap).current;
	const [mapTick, setMapTick] = import_react.useState(0);
	const lastTickRef = import_react.useRef(mapTick);
	const register = useStableCallback((node, metadata) => {
		map.set(node, metadata ?? null);
		lastTickRef.current += 1;
		setMapTick(lastTickRef.current);
	});
	const unregister = useStableCallback((node) => {
		map.delete(node);
		lastTickRef.current += 1;
		setMapTick(lastTickRef.current);
	});
	const sortedMap = import_react.useMemo(() => {
		const newMap = /* @__PURE__ */ new Map();
		Array.from(map.keys()).filter((node) => node.isConnected).sort(sortByDocumentPosition).forEach((node, index) => {
			const metadata = map.get(node) ?? {};
			newMap.set(node, _extends({}, metadata, { index }));
		});
		return newMap;
	}, [map, mapTick]);
	useIsoLayoutEffect(() => {
		if (typeof MutationObserver !== "function" || sortedMap.size === 0) return;
		const mutationObserver = new MutationObserver((entries) => {
			const diff = /* @__PURE__ */ new Set();
			const updateDiff = (node) => diff.has(node) ? diff.delete(node) : diff.add(node);
			entries.forEach((entry) => {
				entry.removedNodes.forEach(updateDiff);
				entry.addedNodes.forEach(updateDiff);
			});
			if (diff.size === 0) {
				lastTickRef.current += 1;
				setMapTick(lastTickRef.current);
			}
		});
		sortedMap.forEach((_, node) => {
			if (node.parentElement) mutationObserver.observe(node.parentElement, { childList: true });
		});
		return () => {
			mutationObserver.disconnect();
		};
	}, [sortedMap]);
	useIsoLayoutEffect(() => {
		if (lastTickRef.current === mapTick) {
			if (elementsRef.current.length !== sortedMap.size) elementsRef.current.length = sortedMap.size;
			if (labelsRef && labelsRef.current.length !== sortedMap.size) labelsRef.current.length = sortedMap.size;
			nextIndexRef.current = sortedMap.size;
		}
		onMapChange(sortedMap);
	}, [
		onMapChange,
		sortedMap,
		elementsRef,
		labelsRef,
		mapTick
	]);
	useIsoLayoutEffect(() => {
		return () => {
			elementsRef.current = [];
		};
	}, [elementsRef]);
	useIsoLayoutEffect(() => {
		return () => {
			if (labelsRef) labelsRef.current = [];
		};
	}, [labelsRef]);
	const subscribeMapChange = useStableCallback((fn) => {
		listeners.add(fn);
		return () => {
			listeners.delete(fn);
		};
	});
	useIsoLayoutEffect(() => {
		listeners.forEach((l) => l(sortedMap));
	}, [listeners, sortedMap]);
	const contextValue = import_react.useMemo(() => ({
		register,
		unregister,
		subscribeMapChange,
		elementsRef,
		labelsRef,
		nextIndexRef
	}), [
		register,
		unregister,
		subscribeMapChange,
		elementsRef,
		labelsRef,
		nextIndexRef
	]);
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(CompositeListContext.Provider, {
		value: contextValue,
		children
	});
}
function createMap() {
	return /* @__PURE__ */ new Map();
}
function createListeners() {
	return /* @__PURE__ */ new Set();
}
function sortByDocumentPosition(a, b) {
	const position = a.compareDocumentPosition(b);
	if (position & Node.DOCUMENT_POSITION_FOLLOWING || position & Node.DOCUMENT_POSITION_CONTAINED_BY) return -1;
	if (position & Node.DOCUMENT_POSITION_PRECEDING || position & Node.DOCUMENT_POSITION_CONTAINS) return 1;
	return 0;
}
//#endregion
//#region node_modules/@mui/x-scheduler-internals/calendar-grid/header-row/CalendarGridHeaderRow.mjs
var _excluded$16 = [
	"className",
	"render",
	"style"
];
var CalendarGridHeaderRow = /*#__PURE__*/ import_react.forwardRef(function CalendarGridHeaderRow(componentProps, forwardedRef) {
	const elementProps = _objectWithoutPropertiesLoose(componentProps, _excluded$16);
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(CompositeList, {
		elementsRef: import_react.useRef([]),
		children: useRenderElement("div", componentProps, {
			ref: [forwardedRef],
			props: [{
				role: "row",
				"aria-rowindex": 1
			}, elementProps]
		})
	});
});
CalendarGridHeaderRow.displayName = "CalendarGridHeaderRow";
//#endregion
//#region node_modules/@mui/x-scheduler-internals/base-ui-copy/composite/list/useCompositeListItem.mjs
var IndexGuessBehavior = /*#__PURE__*/ function(IndexGuessBehavior) {
	IndexGuessBehavior[IndexGuessBehavior["None"] = 0] = "None";
	IndexGuessBehavior[IndexGuessBehavior["GuessFromOrder"] = 1] = "GuessFromOrder";
	return IndexGuessBehavior;
}({});
/**
* Used to register a list item and its index (DOM position) in the `CompositeList`.
*/
function useCompositeListItem(params = {}) {
	const { label, metadata, textRef, indexGuessBehavior, index: externalIndex } = params;
	const { register, unregister, subscribeMapChange, elementsRef, labelsRef, nextIndexRef } = useCompositeListContext();
	const indexRef = import_react.useRef(-1);
	const [index, setIndex] = import_react.useState(externalIndex ?? (indexGuessBehavior === IndexGuessBehavior.GuessFromOrder ? () => {
		if (indexRef.current === -1) {
			const newIndex = nextIndexRef.current;
			nextIndexRef.current += 1;
			indexRef.current = newIndex;
		}
		return indexRef.current;
	} : -1));
	const componentRef = import_react.useRef(null);
	const ref = import_react.useCallback((node) => {
		componentRef.current = node;
		if (index !== -1 && node !== null) {
			elementsRef.current[index] = node;
			if (labelsRef) {
				const isLabelDefined = label !== void 0;
				labelsRef.current[index] = isLabelDefined ? label : textRef?.current?.textContent ?? node.textContent;
			}
		}
	}, [
		index,
		elementsRef,
		labelsRef,
		label,
		textRef
	]);
	useIsoLayoutEffect(() => {
		if (externalIndex != null) return;
		const node = componentRef.current;
		if (node) {
			register(node, metadata);
			return () => {
				unregister(node);
			};
		}
	}, [
		externalIndex,
		register,
		unregister,
		metadata
	]);
	useIsoLayoutEffect(() => {
		if (externalIndex != null) return;
		return subscribeMapChange((map) => {
			const i = componentRef.current ? map.get(componentRef.current)?.index : null;
			if (i != null) setIndex(i);
		});
	}, [
		externalIndex,
		subscribeMapChange,
		setIndex
	]);
	return import_react.useMemo(() => ({
		ref,
		index
	}), [index, ref]);
}
//#endregion
//#region node_modules/@mui/x-scheduler-internals/internals/utils/accessibility-utils.mjs
function getCalendarGridHeaderCellId(rootId, columnIndex) {
	if (rootId === void 0) return;
	return `${rootId}-header-cell-${columnIndex}`;
}
//#endregion
//#region node_modules/@mui/x-scheduler-internals/calendar-grid/header-cell/CalendarGridHeaderCell.mjs
var _excluded$15 = [
	"className",
	"render",
	"style",
	"date",
	"skipDataCurrent",
	"ariaLabelFormat"
];
var selectorIsCurrentDate = createSelector((state, date, skipDataCurrent) => !skipDataCurrent && schedulerNowSelectors.isCurrentDay(state, date));
var CalendarGridHeaderCell = /*#__PURE__*/ import_react.forwardRef(function CalendarGridHeaderCell(componentProps, forwardedRef) {
	const adapter = useAdapterContext();
	const { date, skipDataCurrent, ariaLabelFormat = adapter.formats.weekday } = componentProps, elementProps = _objectWithoutPropertiesLoose(componentProps, _excluded$15);
	const store = useEventCalendarStoreContext();
	const { id: rootId, focusedCell, setFocusedCell, rowTypes, rowsPerType } = useCalendarGridRootContext();
	const isCurrentDay = useStore(store, selectorIsCurrentDate, date.value, skipDataCurrent);
	const { ref: listItemRef, index } = useCompositeListItem();
	const { elementsRef } = useCompositeListContext();
	const id = getCalendarGridHeaderCellId(rootId, index);
	const cellRef = import_react.useRef(null);
	const hasFocus = focusedCell?.rowType === "header" && focusedCell?.rowIndex === 0 && focusedCell?.columnIndex === index;
	import_react.useEffect(() => {
		if (hasFocus && cellRef.current && !cellRef.current.contains(document.activeElement)) cellRef.current.focus({ preventScroll: true });
	}, [hasFocus]);
	const handleKeyDown = (event) => {
		const target = getNavigationTarget(event.key, "header", 0, index, {
			columnCount: elementsRef.current.length,
			rowTypes,
			rowsPerType
		});
		if (target) {
			event.preventDefault();
			setFocusedCell(target);
			return;
		}
		if (event.key === "Enter" && event.target === event.currentTarget) {
			const button = event.currentTarget.querySelector("button");
			if (button) button.click();
		}
	};
	const handleFocus = (event) => {
		if (event.target === event.currentTarget) setFocusedCell({
			rowType: "header",
			rowIndex: 0,
			columnIndex: index
		});
	};
	const state = import_react.useMemo(() => ({ current: isCurrentDay }), [isCurrentDay]);
	const keyboardProps = {
		tabIndex: 0,
		onKeyDown: handleKeyDown,
		onFocus: handleFocus
	};
	return useRenderElement("div", componentProps, {
		state,
		ref: [
			forwardedRef,
			listItemRef,
			cellRef
		],
		props: [
			{
				role: "columnheader",
				id,
				"aria-label": `${adapter.formatByString(date.value, ariaLabelFormat)}`,
				"aria-colindex": index + 1
			},
			keyboardProps,
			elementProps
		]
	});
});
CalendarGridHeaderCell.displayName = "CalendarGridHeaderCell";
//#endregion
//#region node_modules/@mui/x-scheduler-internals/calendar-grid/day-row/CalendarGridDayRowContext.mjs
var CalendarGridDayRowContext = /*#__PURE__*/ import_react.createContext(void 0);
CalendarGridDayRowContext.displayName = "CalendarGridDayRowContext";
function useCalendarGridDayRowContext() {
	const context = import_react.useContext(CalendarGridDayRowContext);
	if (context === void 0) throw new Error("MUI X Scheduler: CalendarGridDayRowContext is missing. <CalendarGrid.DayCell /> and <CalendarGrid.DayEvent /> must be placed within <CalendarGrid.DayRow />. Ensure the component hierarchy is correct.");
	return context;
}
//#endregion
//#region node_modules/@mui/x-scheduler-internals/calendar-grid/day-row/CalendarGridDayRow.mjs
var _excluded$14 = [
	"className",
	"render",
	"style",
	"start",
	"end",
	"rowIndex"
];
var CalendarGridDayRow = /*#__PURE__*/ import_react.forwardRef(function CalendarGridDayRow(componentProps, forwardedRef) {
	const { start, end, rowIndex = 0 } = componentProps, elementProps = _objectWithoutPropertiesLoose(componentProps, _excluded$14);
	const cellsRefs = import_react.useRef([]);
	const contextValue = import_react.useMemo(() => ({
		start,
		end,
		rowIndex
	}), [
		start,
		end,
		rowIndex
	]);
	const element = useRenderElement("div", componentProps, {
		ref: [forwardedRef],
		props: [{
			role: "row",
			"aria-rowindex": rowIndex + 2
		}, elementProps]
	});
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(CompositeList, {
		elementsRef: cellsRefs,
		children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(CalendarGridDayRowContext.Provider, {
			value: contextValue,
			children: element
		})
	});
});
CalendarGridDayRow.displayName = "CalendarGridDayRow";
//#endregion
//#region node_modules/@mui/x-scheduler-internals/calendar-grid/day-cell/useDayCellDropTarget.mjs
var isValidDropTarget$1 = buildIsValidDropTarget([
	"CalendarGridDayEvent",
	"CalendarGridDayEventResizeHandler",
	"CalendarGridTimeEvent",
	"StandaloneEvent"
]);
function useDayCellDropTarget(parameters) {
	const { value, addPropertiesToDroppedEvent } = parameters;
	const adapter = useAdapterContext();
	const ref = import_react.useRef(null);
	useDropTarget({
		surfaceType: "day-grid",
		ref,
		getEventDropData: useStableCallback(({ data, getDataFromInside, getDataFromOutside }) => {
			if (!isValidDropTarget$1(data)) return;
			if (data.source === "CalendarGridDayEvent") {
				const offset = adapter.differenceInDays(value, data.draggedDay);
				return getDataFromInside(data, offset === 0 ? data.start : adapter.addDays(data.start, offset), offset === 0 ? data.end : adapter.addDays(data.end, offset));
			}
			if (data.source === "CalendarGridDayEventResizeHandler") {
				if (data.side === "start") {
					if (adapter.isAfter(value, adapter.endOfDay(data.end))) return;
					let newStart;
					if (adapter.isSameDay(value, data.end)) newStart = adapter.startOfDay(data.end);
					else newStart = mergeDateAndTime(adapter, value, data.start);
					return getDataFromInside(data, newStart, data.end);
				}
				if (data.side === "end") {
					if (adapter.isBefore(value, adapter.startOfDay(data.start))) return;
					let draggedDay;
					if (adapter.isSameDay(value, data.start)) draggedDay = adapter.endOfDay(data.start);
					else draggedDay = mergeDateAndTime(adapter, value, data.end);
					return getDataFromInside(data, data.start, draggedDay);
				}
			}
			if (data.source === "CalendarGridTimeEvent") {
				const cursorDate = adapter.startOfDay(adapter.addMilliseconds(data.start, data.initialCursorPositionInEventMs));
				const offset = adapter.differenceInDays(value, cursorDate);
				return getDataFromInside(data, offset === 0 ? data.start : adapter.addDays(data.start, offset), offset === 0 ? data.end : adapter.addDays(data.end, offset));
			}
			if (data.source === "StandaloneEvent") return getDataFromOutside(data, value);
		}),
		isValidDropTarget: isValidDropTarget$1,
		addPropertiesToDroppedEvent
	});
	return ref;
}
//#endregion
//#region node_modules/@mui/x-scheduler-internals/calendar-grid/day-cell/CalendarGridDayCellContext.mjs
var CalendarGridDayCellContext = /*#__PURE__*/ import_react.createContext(void 0);
CalendarGridDayCellContext.displayName = "CalendarGridDayCellContext";
function useCalendarGridDayCellContext() {
	const context = import_react.useContext(CalendarGridDayCellContext);
	if (context === void 0) throw new Error("MUI X Scheduler: `CalendarGridDayCellContext` is missing. <CalendarGrid.DayEvent /> must be placed within <CalendarGrid.DayCell />.");
	return context;
}
//#endregion
//#region node_modules/@mui/x-scheduler-internals/calendar-grid/day-cell/CalendarGridDayCell.mjs
var _excluded$13 = [
	"className",
	"render",
	"style",
	"value",
	"addPropertiesToDroppedEvent",
	"lockSurfaceType"
], _excluded2 = ["aria-labelledby"];
var CalendarGridDayCell = /*#__PURE__*/ import_react.forwardRef(function CalendarGridDayCell(componentProps, forwardedRef) {
	const { value, addPropertiesToDroppedEvent, lockSurfaceType } = componentProps, elementProps = _objectWithoutPropertiesLoose(componentProps, _excluded$13);
	const adapter = useAdapterContext();
	const { id: rootId, focusedCell, setFocusedCell, rowTypes, rowsPerType } = useCalendarGridRootContext();
	const { rowIndex } = useCalendarGridDayRowContext();
	const { ref: listItemRef, index } = useCompositeListItem();
	const { elementsRef } = useCompositeListContext();
	const dropTargetRef = useDayCellDropTarget({
		value,
		addPropertiesToDroppedEvent
	});
	const columnHeaderId = getCalendarGridHeaderCellId(rootId, index);
	const cellRef = import_react.useRef(null);
	const hasFocus = focusedCell?.rowType === "day-grid" && focusedCell?.rowIndex === rowIndex && focusedCell?.columnIndex === index;
	const getPlaceholderFields = () => ({
		surfaceType: "day-grid",
		start: adapter.startOfDay(value),
		end: adapter.endOfDay(value),
		lockSurfaceType,
		resourceId: null
	});
	const eventCreationProps = useEventCreation(getPlaceholderFields);
	const triggerKeyboardCreation = useKeyboardEventCreation(getPlaceholderFields);
	import_react.useEffect(() => {
		if (hasFocus && cellRef.current && !cellRef.current.contains(document.activeElement)) cellRef.current.focus({ preventScroll: true });
	}, [hasFocus]);
	const handleKeyDown = (event) => {
		const target = getNavigationTarget(event.key, "day-grid", rowIndex, index, {
			columnCount: elementsRef.current.length,
			rowTypes,
			rowsPerType
		});
		if (target) {
			event.preventDefault();
			setFocusedCell(target);
			return;
		}
		if (event.key === "Enter" && event.target === event.currentTarget && triggerKeyboardCreation) {
			event.preventDefault();
			triggerKeyboardCreation();
		}
	};
	const handleFocus = (event) => {
		if (event.target === event.currentTarget) setFocusedCell({
			rowType: "day-grid",
			rowIndex,
			columnIndex: index
		});
	};
	const contextValue = import_react.useMemo(() => ({
		index,
		hasFocus
	}), [index, hasFocus]);
	const { "aria-labelledby": elementAriaLabelledBy } = elementProps, restElementProps = _objectWithoutPropertiesLoose(elementProps, _excluded2);
	const ariaLabelledBy = [columnHeaderId, elementAriaLabelledBy].filter(Boolean).join(" ");
	const keyboardProps = {
		tabIndex: 0,
		onKeyDown: handleKeyDown,
		onFocus: handleFocus
	};
	const element = useRenderElement("div", componentProps, {
		ref: [
			forwardedRef,
			dropTargetRef,
			listItemRef,
			cellRef
		],
		props: [
			{
				role: "gridcell",
				"aria-labelledby": ariaLabelledBy || void 0,
				"aria-colindex": index + 1
			},
			keyboardProps,
			eventCreationProps,
			restElementProps
		]
	});
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(CalendarGridDayCellContext.Provider, {
		value: contextValue,
		children: element
	});
});
CalendarGridDayCell.displayName = "CalendarGridDayCell";
//#endregion
//#region node_modules/@mui/x-scheduler-internals/base-ui-copy/composite/root/CompositeRootContext.mjs
var CompositeRootContext = /*#__PURE__*/ import_react.createContext(void 0);
CompositeRootContext.displayName = "CompositeRootContext";
function useCompositeRootContext(optional = false) {
	const context = import_react.useContext(CompositeRootContext);
	if (context === void 0 && !optional) throw new Error("Base UI: CompositeRootContext is missing. Composite parts must be placed within <Composite.Root>.");
	return context;
}
//#endregion
//#region node_modules/@mui/x-scheduler-internals/base-ui-copy/utils/useFocusableWhenDisabled.mjs
function useFocusableWhenDisabled(parameters) {
	const { focusableWhenDisabled, disabled, composite = false, tabIndex: tabIndexProp = 0, isNativeButton } = parameters;
	const isFocusableComposite = composite && focusableWhenDisabled !== false;
	const isNonFocusableComposite = composite && focusableWhenDisabled === false;
	return { props: import_react.useMemo(() => {
		const additionalProps = { onKeyDown(event) {
			if (disabled && focusableWhenDisabled && event.key !== "Tab") event.preventDefault();
		} };
		if (!composite) {
			additionalProps.tabIndex = tabIndexProp;
			if (!isNativeButton && disabled) additionalProps.tabIndex = focusableWhenDisabled ? tabIndexProp : -1;
		}
		if (isNativeButton && (focusableWhenDisabled || isFocusableComposite) || !isNativeButton && disabled) additionalProps["aria-disabled"] = disabled;
		if (isNativeButton && (!focusableWhenDisabled || isNonFocusableComposite)) additionalProps.disabled = disabled;
		return additionalProps;
	}, [
		composite,
		disabled,
		focusableWhenDisabled,
		isFocusableComposite,
		isNonFocusableComposite,
		isNativeButton,
		tabIndexProp
	]) };
}
//#endregion
//#region node_modules/@mui/x-scheduler-internals/base-ui-copy/utils/useButton.mjs
var _excluded$12 = [
	"onClick",
	"onMouseDown",
	"onKeyUp",
	"onKeyDown",
	"onPointerDown"
];
function useButton(parameters = {}) {
	const { disabled = false, focusableWhenDisabled, tabIndex = 0, native: isNativeButton = true, composite: compositeProp } = parameters;
	const elementRef = import_react.useRef(null);
	const compositeRootContext = useCompositeRootContext(true);
	const isCompositeItem = compositeProp ?? compositeRootContext !== void 0;
	const { props: focusableWhenDisabledProps } = useFocusableWhenDisabled({
		focusableWhenDisabled,
		disabled,
		composite: isCompositeItem,
		tabIndex,
		isNativeButton
	});
	import_react.useEffect(() => {
		if (!elementRef.current) return;
		const isButtonTag = isButtonElement(elementRef.current);
		if (isNativeButton) {
			if (!isButtonTag) error(`A component that acts as a button expected a native <button> because the \`nativeButton\` prop is true. Rendering a non-<button> removes native button semantics, which can impact forms and accessibility. Use a real <button> in the \`render\` prop, or set \`nativeButton\` to \`false\`.${SafeReact.captureOwnerStack?.() || ""}`);
		} else if (isButtonTag) error(`A component that acts as a button expected a non-<button> because the \`nativeButton\` prop is false. Rendering a <button> keeps native behavior while Base UI applies non-native attributes and handlers, which can add unintended extra attributes (such as \`role\` or \`aria-disabled\`). Use a non-<button> in the \`render\` prop, or set \`nativeButton\` to \`true\`.${SafeReact.captureOwnerStack?.() || ""}`);
	}, [isNativeButton]);
	const updateDisabled = import_react.useCallback(() => {
		const element = elementRef.current;
		if (!isButtonElement(element)) return;
		if (isCompositeItem && disabled && focusableWhenDisabledProps.disabled === void 0 && element.disabled) element.disabled = false;
	}, [
		disabled,
		focusableWhenDisabledProps.disabled,
		isCompositeItem
	]);
	useIsoLayoutEffect(updateDisabled, [updateDisabled]);
	return {
		getButtonProps: import_react.useCallback((externalProps = {}) => {
			const { onClick: externalOnClick, onMouseDown: externalOnMouseDown, onKeyUp: externalOnKeyUp, onKeyDown: externalOnKeyDown, onPointerDown: externalOnPointerDown } = externalProps, otherExternalProps = _objectWithoutPropertiesLoose(externalProps, _excluded$12);
			return mergeProps({
				type: isNativeButton ? "button" : void 0,
				onClick(event) {
					if (disabled) {
						event.preventDefault();
						return;
					}
					externalOnClick?.(event);
				},
				onMouseDown(event) {
					if (!disabled) externalOnMouseDown?.(event);
				},
				onKeyDown(event) {
					if (disabled) return;
					makeEventPreventable(event);
					externalOnKeyDown?.(event);
					if (event.baseUIHandlerPrevented) return;
					const isCurrentTarget = event.target === event.currentTarget;
					const currentTarget = event.currentTarget;
					const isButton = isButtonElement(currentTarget);
					const isLink = !isNativeButton && isValidLinkElement(currentTarget);
					const shouldClick = isCurrentTarget && (isNativeButton ? isButton : !isLink);
					const isEnterKey = event.key === "Enter";
					const isSpaceKey = event.key === " ";
					const role = currentTarget.getAttribute("role");
					const isTextNavigationRole = role?.startsWith("menuitem") || role === "option" || role === "gridcell";
					if (isCurrentTarget && isCompositeItem && isSpaceKey) {
						if (event.defaultPrevented && isTextNavigationRole) return;
						event.preventDefault();
						if (isLink || isNativeButton && isButton) {
							currentTarget.click();
							event.preventBaseUIHandler();
						} else if (shouldClick) {
							externalOnClick?.(event);
							event.preventBaseUIHandler();
						}
						return;
					}
					if (shouldClick) {
						if (!isNativeButton && (isSpaceKey || isEnterKey)) event.preventDefault();
						if (!isNativeButton && isEnterKey) externalOnClick?.(event);
					}
				},
				onKeyUp(event) {
					if (disabled) return;
					makeEventPreventable(event);
					externalOnKeyUp?.(event);
					if (event.target === event.currentTarget && isNativeButton && isCompositeItem && isButtonElement(event.currentTarget) && event.key === " ") {
						event.preventDefault();
						return;
					}
					if (event.baseUIHandlerPrevented) return;
					if (event.target === event.currentTarget && !isNativeButton && !isCompositeItem && event.key === " ") externalOnClick?.(event);
				},
				onPointerDown(event) {
					if (disabled) {
						event.preventDefault();
						return;
					}
					externalOnPointerDown?.(event);
				}
			}, !isNativeButton ? { role: "button" } : void 0, focusableWhenDisabledProps, otherExternalProps);
		}, [
			disabled,
			focusableWhenDisabledProps,
			isCompositeItem,
			isNativeButton
		]),
		buttonRef: useStableCallback((element) => {
			elementRef.current = element;
			updateDisabled();
		})
	};
}
function isButtonElement(elem) {
	return isHTMLElement(elem) && elem.tagName === "BUTTON";
}
function isValidLinkElement(elem) {
	return Boolean(elem?.tagName === "A" && elem?.href);
}
//#endregion
//#region node_modules/@mui/x-scheduler-internals/calendar-grid/day-event/CalendarGridDayEventContext.mjs
var CalendarGridDayEventContext = /*#__PURE__*/ import_react.createContext(void 0);
CalendarGridDayEventContext.displayName = "CalendarGridDayEventContext";
function useCalendarGridDayEventContext() {
	const context = import_react.useContext(CalendarGridDayEventContext);
	if (context === void 0) throw new Error("MUI X Scheduler: `CalendarGridDayEventContext` is missing. CalendarGrid DayEvent parts must be placed within <CalendarGrid.DayEvent />.");
	return context;
}
//#endregion
//#region node_modules/@mui/x-scheduler-internals/calendar-grid/day-event/CalendarGridDayEvent.mjs
var _excluded$11 = [
	"className",
	"render",
	"style",
	"start",
	"end",
	"eventId",
	"occurrenceKey",
	"renderDragPreview",
	"id",
	"isDraggable",
	"nativeButton"
];
var overflowStateAttributesMapping = {
	startingBeforeEdge: (value) => value ? { "data-starting-before-edge": "" } : null,
	endingAfterEdge: (value) => value ? { "data-ending-after-edge": "" } : null
};
var CalendarGridDayEvent = /*#__PURE__*/ import_react.forwardRef(function CalendarGridDayEvent(componentProps, forwardedRef) {
	const { start, end, eventId, occurrenceKey, renderDragPreview, id: idProp, isDraggable = false, nativeButton = false } = componentProps, elementProps = _objectWithoutPropertiesLoose(componentProps, _excluded$11);
	const adapter = useAdapterContext();
	const store = useEventCalendarStoreContext();
	const { id: rootId } = useCalendarGridRootContext();
	const { start: rowStart, end: rowEnd } = useCalendarGridDayRowContext();
	const { index: cellIndex, hasFocus: cellHasFocus } = useCalendarGridDayCellContext();
	const ref = import_react.useRef(null);
	const hasPlaceholder = useStore(store, schedulerOccurrencePlaceholderSelectors.isDefined);
	const id = useId(idProp);
	const getDraggedDay = useStableCallback((input) => {
		if (!ref.current) return start.value;
		const eventStartInRow = adapter.isBefore(start.value, rowStart) ? rowStart : start.value;
		const eventEndInRow = adapter.isAfter(end.value, rowEnd) ? rowEnd : end.value;
		const eventDayLengthInRow = adapter.differenceInDays(eventEndInRow, eventStartInRow) + 1;
		const positionX = (input.clientX - ref.current.getBoundingClientRect().x) / ref.current.offsetWidth;
		return adapter.startOfDay(adapter.addDays(eventStartInRow, Math.ceil(positionX * eventDayLengthInRow) - 1));
	});
	const originalOccurrence = generateOccurrenceFromEvent({
		event: schedulerEventSelectors.processedEvent(store.state, eventId),
		eventId,
		occurrenceKey,
		start,
		end
	});
	const getSharedDragData = useStableCallback(() => ({
		eventId,
		occurrenceKey,
		originalOccurrence,
		start: start.value,
		end: end.value
	}));
	const { state, preview, contextValue: draggableEventContextValue } = useDraggableEvent({
		ref,
		start,
		end,
		occurrenceKey,
		eventId,
		isDraggable,
		renderDragPreview,
		getDragData: useStableCallback((input) => _extends({}, getSharedDragData(input), {
			source: "CalendarGridDayEvent",
			draggedDay: getDraggedDay(input)
		})),
		collectionStart: rowStart,
		collectionEnd: rowEnd
	});
	const startingBeforeEdge = draggableEventContextValue.doesEventStartBeforeCollectionStart;
	const endingAfterEdge = draggableEventContextValue.doesEventEndAfterCollectionEnd;
	const mergedState = _extends({}, state, {
		startingBeforeEdge,
		endingAfterEdge
	});
	const { getButtonProps, buttonRef } = useButton({
		disabled: false,
		native: nativeButton,
		tabIndex: cellHasFocus ? 0 : -1
	});
	const columnHeaderId = getCalendarGridHeaderCellId(rootId, cellIndex);
	const contextValue = import_react.useMemo(() => _extends({}, draggableEventContextValue, { getSharedDragData }), [draggableEventContextValue, getSharedDragData]);
	const element = useRenderElement("div", componentProps, {
		state: mergedState,
		ref: [
			forwardedRef,
			buttonRef,
			ref
		],
		props: [
			elementProps,
			{
				id,
				"aria-labelledby": `${columnHeaderId} ${id}`,
				style: hasPlaceholder ? { pointerEvents: "none" } : void 0
			},
			getButtonProps
		],
		stateAttributesMapping: overflowStateAttributesMapping
	});
	return /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(CalendarGridDayEventContext.Provider, {
		value: contextValue,
		children: [element, preview.element]
	});
});
CalendarGridDayEvent.displayName = "CalendarGridDayEvent";
//#endregion
//#region node_modules/@mui/x-scheduler-internals/calendar-grid/day-event-placeholder/CalendarGridDayEventPlaceholder.mjs
var _excluded$10 = [
	"className",
	"render",
	"style",
	"start",
	"end"
];
var CalendarGridDayEventPlaceholder = /*#__PURE__*/ import_react.forwardRef(function CalendarGridDayEventPlaceholder(componentProps, forwardedRef) {
	const { start, end } = componentProps, elementProps = _objectWithoutPropertiesLoose(componentProps, _excluded$10);
	const { state } = useEvent({
		start,
		end
	});
	return useRenderElement("div", componentProps, {
		state,
		ref: [forwardedRef],
		props: [elementProps, { style: { pointerEvents: "none" } }]
	});
});
CalendarGridDayEventPlaceholder.displayName = "CalendarGridDayEventPlaceholder";
//#endregion
//#region node_modules/@mui/x-scheduler-internals/calendar-grid/day-event-resize-handler/CalendarGridDayEventResizeHandler.mjs
var _excluded$9 = [
	"className",
	"render",
	"style",
	"side"
];
var CalendarGridDayEventResizeHandler = /*#__PURE__*/ import_react.forwardRef(function CalendarGridDayEventResizeHandler(componentProps, forwardedRef) {
	const { side } = componentProps, elementProps = _objectWithoutPropertiesLoose(componentProps, _excluded$9);
	const contextValue = useCalendarGridDayEventContext();
	const ref = import_react.useRef(null);
	const { state, enabled } = useEventResizeHandler({
		ref,
		side,
		contextValue,
		getDragData: useStableCallback((input) => _extends({}, contextValue.getSharedDragData(input), {
			source: "CalendarGridDayEventResizeHandler",
			side
		}))
	});
	return useRenderElement("div", componentProps, {
		enabled,
		state,
		ref: [forwardedRef, ref],
		props: [elementProps]
	});
});
CalendarGridDayEventResizeHandler.displayName = "CalendarGridDayEventResizeHandler";
//#endregion
//#region node_modules/@mui/x-scheduler-internals/calendar-grid/time-scrollable-content/CalendarGridTimeScrollableContent.mjs
var _excluded$8 = [
	"className",
	"render",
	"style"
];
var CalendarGridTimeScrollableContent = /*#__PURE__*/ import_react.forwardRef(function CalendarGridScrollableContent(componentProps, forwardedRef) {
	const elementProps = _objectWithoutPropertiesLoose(componentProps, _excluded$8);
	const ref = import_react.useRef(null);
	const columnsRefs = import_react.useRef([]);
	import_react.useEffect(() => {
		if (!ref.current || false) return;
		return autoScrollForElements({ element: ref.current });
	}, []);
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(CompositeList, {
		elementsRef: columnsRefs,
		children: useRenderElement("div", componentProps, {
			ref: [forwardedRef, ref],
			props: [elementProps]
		})
	});
});
CalendarGridTimeScrollableContent.displayName = "CalendarGridTimeScrollableContent";
//#endregion
//#region node_modules/@mui/x-scheduler-internals/calendar-grid/time-column/CalendarGridTimeColumnContext.mjs
var CalendarGridTimeColumnContext = /*#__PURE__*/ import_react.createContext(void 0);
CalendarGridTimeColumnContext.displayName = "CalendarGridTimeColumnContext";
function useCalendarGridTimeColumnContext() {
	const context = import_react.useContext(CalendarGridTimeColumnContext);
	if (context === void 0) throw new Error("MUI X Scheduler: `CalendarGridTimeColumnContext` is missing. <CalendarGrid.TimeEvent /> must be placed within <CalendarGrid.TimeColumn />.");
	return context;
}
//#endregion
//#region node_modules/@mui/x-scheduler-internals/calendar-grid/time-column/useTimeDropTarget.mjs
var isValidDropTarget = buildIsValidDropTarget([
	"CalendarGridTimeEvent",
	"CalendarGridTimeEventResizeHandler",
	"CalendarGridDayEvent",
	"StandaloneEvent"
]);
function useTimeDropTarget(parameters) {
	const { start, end, addPropertiesToDroppedEvent } = parameters;
	const adapter = useAdapterContext();
	const store = useEventCalendarStoreContext();
	const ref = import_react.useRef(null);
	const collectionStartTimestamp = adapter.getTime(start);
	const collectionDurationMs = adapter.getTime(end) - collectionStartTimestamp;
	const getCursorPositionInElementMs = useStableCallback(({ input, elementRef }) => {
		if (!ref.current || !elementRef.current) return 0;
		const positionY = (input.clientY - elementRef.current.getBoundingClientRect().y) / ref.current.offsetHeight;
		return Math.round(collectionDurationMs * Math.max(0, Math.min(1, positionY)));
	});
	useDropTarget({
		ref,
		surfaceType: "time-grid",
		getEventDropData: useStableCallback(({ data, getDataFromInside, getDataFromOutside, input }) => {
			if (!isValidDropTarget(data)) return;
			const cursorOffsetMs = getCursorPositionInElementMs({
				input,
				elementRef: ref
			});
			const addOffsetToDate = (date, offsetMs) => {
				const roundedOffset = Math.round(offsetMs / EVENT_DRAG_PRECISION_MS) * EVENT_DRAG_PRECISION_MS;
				return adapter.addMilliseconds(date, roundedOffset);
			};
			if (data.source === "CalendarGridTimeEvent") {
				const eventDurationMs = adapter.getTime(data.end) - adapter.getTime(data.start);
				let newStartDate = addOffsetToDate(start, cursorOffsetMs - data.initialCursorPositionInEventMs);
				if (adapter.isBefore(newStartDate, start)) newStartDate = start;
				const maxStartDate = adapter.addMilliseconds(end, -eventDurationMs);
				if (adapter.isAfter(newStartDate, maxStartDate)) newStartDate = maxStartDate;
				const newEndDate = adapter.addMilliseconds(newStartDate, eventDurationMs);
				return getDataFromInside(data, newStartDate, newEndDate);
			}
			if (data.source === "CalendarGridTimeEventResizeHandler") {
				if (data.side === "start") {
					let cursorDate = addOffsetToDate(start, cursorOffsetMs - data.initialCursorPositionInEventMs);
					if (adapter.isBefore(cursorDate, start)) cursorDate = start;
					const maxStartDate = adapter.addMinutes(data.end, -15);
					return getDataFromInside(data, adapter.isBefore(cursorDate, maxStartDate) ? cursorDate : maxStartDate, data.end);
				}
				if (data.side === "end") {
					const eventDurationMs = adapter.getTime(data.end) - adapter.getTime(data.start);
					let cursorDate = addOffsetToDate(start, cursorOffsetMs - data.initialCursorPositionInEventMs + eventDurationMs);
					if (adapter.isAfter(cursorDate, end)) cursorDate = end;
					const minEndDate = adapter.addMinutes(data.start, 15);
					const newEndDate = adapter.isAfter(cursorDate, minEndDate) ? cursorDate : minEndDate;
					return getDataFromInside(data, data.start, newEndDate);
				}
			}
			if (data.source === "CalendarGridDayEvent") {
				const newStartDate = addOffsetToDate(start, cursorOffsetMs);
				return getDataFromInside(data, newStartDate, adapter.addMinutes(newStartDate, schedulerEventSelectors.defaultEventDuration(store.state)));
			}
			if (data.source === "StandaloneEvent") return getDataFromOutside(data, addOffsetToDate(start, cursorOffsetMs));
		}),
		isValidDropTarget,
		addPropertiesToDroppedEvent
	});
	return {
		getCursorPositionInElementMs,
		ref
	};
}
//#endregion
//#region node_modules/@mui/x-scheduler-internals/calendar-grid/time-column/CalendarGridTimeColumn.mjs
var _excluded$7 = [
	"className",
	"render",
	"style",
	"start",
	"end",
	"dayStartMinute",
	"dayEndMinute",
	"addPropertiesToDroppedEvent"
];
var CalendarGridTimeColumn = /*#__PURE__*/ import_react.forwardRef(function CalendarGridTimeColumn(componentProps, forwardedRef) {
	const { start, end, dayStartMinute = 0, dayEndMinute = 1440, addPropertiesToDroppedEvent } = componentProps, elementProps = _objectWithoutPropertiesLoose(componentProps, _excluded$7);
	const adapter = useAdapterContext();
	const store = useEventCalendarStoreContext();
	const { focusedCell, setFocusedCell, rowTypes, rowsPerType } = useCalendarGridRootContext();
	const isCurrentDay = useStore(store, schedulerNowSelectors.isCurrentDay, start);
	const { ref: listItemRef, index } = useCompositeListItem();
	const { elementsRef } = useCompositeListContext();
	const cellRef = import_react.useRef(null);
	const hasFocus = focusedCell?.rowType === "time-grid" && focusedCell?.rowIndex === 0 && focusedCell?.columnIndex === index;
	const { getCursorPositionInElementMs, ref: dropTargetRef } = useTimeDropTarget({
		start,
		end,
		addPropertiesToDroppedEvent
	});
	const eventCreationProps = useEventCreation(({ event, creationConfig }) => {
		const offsetMs = getCursorPositionInElementMs({
			input: { clientY: event.clientY },
			elementRef: dropTargetRef
		});
		const anchor = adapter.addMilliseconds(start, offsetMs);
		const startDate = adapter.addMinutes(anchor, -(adapter.getMinutes(anchor) % 30));
		return {
			surfaceType: "time-grid",
			start: startDate,
			end: adapter.addMinutes(startDate, creationConfig.duration),
			resourceId: null
		};
	});
	const triggerKeyboardCreation = useKeyboardEventCreation(({ creationConfig }) => {
		const noon = adapter.setHours(adapter.setMinutes(start, 0), 12);
		const anchor = adapter.isBefore(noon, start) || !adapter.isBefore(noon, end) ? start : noon;
		return {
			surfaceType: "time-grid",
			start: anchor,
			end: adapter.addMinutes(anchor, creationConfig.duration),
			resourceId: null
		};
	});
	import_react.useEffect(() => {
		if (hasFocus && cellRef.current && !cellRef.current.contains(document.activeElement)) cellRef.current.focus({ preventScroll: true });
	}, [hasFocus]);
	const handleKeyDown = (event) => {
		const target = getNavigationTarget(event.key, "time-grid", 0, index, {
			columnCount: elementsRef.current.length,
			rowTypes,
			rowsPerType
		});
		if (target) {
			event.preventDefault();
			setFocusedCell(target);
			return;
		}
		if (event.key === "Enter" && event.target === event.currentTarget && triggerKeyboardCreation) {
			event.preventDefault();
			triggerKeyboardCreation();
		}
	};
	const handleFocus = (event) => {
		if (event.target === event.currentTarget) setFocusedCell({
			rowType: "time-grid",
			rowIndex: 0,
			columnIndex: index
		});
	};
	const state = import_react.useMemo(() => ({ current: isCurrentDay }), [isCurrentDay]);
	const contextValue = import_react.useMemo(() => ({
		start,
		end,
		dayStartMinute,
		dayEndMinute,
		index,
		hasFocus,
		getCursorPositionInElementMs
	}), [
		start,
		end,
		dayStartMinute,
		dayEndMinute,
		index,
		hasFocus,
		getCursorPositionInElementMs
	]);
	const keyboardProps = {
		tabIndex: 0,
		onKeyDown: handleKeyDown,
		onFocus: handleFocus
	};
	const element = useRenderElement("div", componentProps, {
		state,
		ref: [
			forwardedRef,
			dropTargetRef,
			listItemRef,
			cellRef
		],
		props: [
			{
				role: "gridcell",
				"aria-colindex": index + 1
			},
			keyboardProps,
			eventCreationProps,
			elementProps
		]
	});
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(CalendarGridTimeColumnContext.Provider, {
		value: contextValue,
		children: element
	});
});
CalendarGridTimeColumn.displayName = "CalendarGridTimeColumn";
//#endregion
//#region node_modules/@mui/x-scheduler-internals/calendar-grid/time-event/CalendarGridTimeEventCssVars.mjs
var CalendarGridTimeEventCssVars = /*#__PURE__*/ function(CalendarGridTimeEventCssVars) {
	CalendarGridTimeEventCssVars["yPosition"] = "--y-position";
	CalendarGridTimeEventCssVars["height"] = "--height";
	return CalendarGridTimeEventCssVars;
}({});
//#endregion
//#region node_modules/@mui/x-scheduler-internals/calendar-grid/time-event/CalendarGridTimeEventContext.mjs
var CalendarGridTimeEventContext = /*#__PURE__*/ import_react.createContext(void 0);
CalendarGridTimeEventContext.displayName = "CalendarGridTimeEventContext";
function useCalendarGridTimeEventContext() {
	const context = import_react.useContext(CalendarGridTimeEventContext);
	if (context === void 0) throw new Error("MUI X Scheduler: `CalendarGridTimeEventContext` is missing. CalendarGrid TimeEvent parts must be placed within <CalendarGrid.TimeEvent />.");
	return context;
}
//#endregion
//#region node_modules/@mui/x-scheduler-internals/calendar-grid/time-event/CalendarGridTimeEvent.mjs
var _excluded$6 = [
	"className",
	"render",
	"style",
	"start",
	"end",
	"eventId",
	"occurrenceKey",
	"renderDragPreview",
	"id",
	"isDraggable",
	"nativeButton"
];
var CalendarGridTimeEvent = /*#__PURE__*/ import_react.forwardRef(function CalendarGridTimeEvent(componentProps, forwardedRef) {
	const { start, end, eventId, occurrenceKey, renderDragPreview, id: idProp, isDraggable = false, nativeButton = false } = componentProps, elementProps = _objectWithoutPropertiesLoose(componentProps, _excluded$6);
	const adapter = useAdapterContext();
	const store = useEventCalendarStoreContext();
	const { id: rootId } = useCalendarGridRootContext();
	const { start: columnStart, end: columnEnd, dayStartMinute, dayEndMinute, index: columnIndex, hasFocus: columnHasFocus, getCursorPositionInElementMs } = useCalendarGridTimeColumnContext();
	const ref = import_react.useRef(null);
	const id = useId(idProp);
	const getSharedDragData = useStableCallback((input) => {
		const offsetBeforeColumnStart = Math.max(adapter.getTime(columnStart) - start.timestamp, 0);
		const originalOccurrence = generateOccurrenceFromEvent({
			event: schedulerEventSelectors.processedEvent(store.state, eventId),
			eventId,
			occurrenceKey,
			start,
			end
		});
		const offsetInsideColumn = getCursorPositionInElementMs({
			input,
			elementRef: ref
		});
		return {
			eventId,
			occurrenceKey,
			originalOccurrence,
			start: start.value,
			end: end.value,
			initialCursorPositionInEventMs: offsetBeforeColumnStart + offsetInsideColumn
		};
	});
	const { state, preview, contextValue: draggableEventContextValue } = useDraggableEvent({
		ref,
		start,
		end,
		occurrenceKey,
		eventId,
		isDraggable,
		renderDragPreview,
		getDragData: useStableCallback((input) => _extends({}, getSharedDragData(input), { source: "CalendarGridTimeEvent" })),
		collectionStart: columnStart,
		collectionEnd: columnEnd
	});
	const { getButtonProps, buttonRef } = useButton({
		disabled: false,
		native: nativeButton,
		tabIndex: columnHasFocus ? 0 : -1
	});
	const { position, duration } = useElementPositionInCollection({
		start,
		end,
		collectionStart: columnStart,
		collectionEnd: columnEnd,
		dayStartMinute,
		dayEndMinute
	});
	const columnHeaderId = getCalendarGridHeaderCellId(rootId, columnIndex);
	const contextValue = import_react.useMemo(() => _extends({}, draggableEventContextValue, { getSharedDragData }), [draggableEventContextValue, getSharedDragData]);
	const element = useRenderElement("div", componentProps, {
		state,
		ref: [
			forwardedRef,
			buttonRef,
			ref
		],
		props: [
			elementProps,
			{
				id,
				"aria-labelledby": `${columnHeaderId} ${id}`,
				style: {
					[CalendarGridTimeEventCssVars.yPosition]: `${position * 100}%`,
					[CalendarGridTimeEventCssVars.height]: `${duration * 100}%`
				}
			},
			getButtonProps
		]
	});
	return /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(CalendarGridTimeEventContext.Provider, {
		value: contextValue,
		children: [element, preview.element]
	});
});
CalendarGridTimeEvent.displayName = "CalendarGridTimeEvent";
//#endregion
//#region node_modules/@mui/x-scheduler-internals/calendar-grid/time-event-placeholder/CalendarGridTimeEventPlaceholderCssVars.mjs
var CalendarGridTimeEventPlaceholderCssVars = /*#__PURE__*/ function(CalendarGridTimeEventPlaceholderCssVars) {
	CalendarGridTimeEventPlaceholderCssVars["yPosition"] = "--y-position";
	CalendarGridTimeEventPlaceholderCssVars["height"] = "--height";
	return CalendarGridTimeEventPlaceholderCssVars;
}({});
//#endregion
//#region node_modules/@mui/x-scheduler-internals/calendar-grid/time-event-placeholder/CalendarGridTimeEventPlaceholder.mjs
var _excluded$5 = [
	"className",
	"render",
	"style",
	"start",
	"end"
];
var CalendarGridTimeEventPlaceholder = /*#__PURE__*/ import_react.forwardRef(function CalendarGridTimeEventPlaceholder(componentProps, forwardedRef) {
	const { start, end } = componentProps, elementProps = _objectWithoutPropertiesLoose(componentProps, _excluded$5);
	const { start: columnStart, end: columnEnd, dayStartMinute, dayEndMinute } = useCalendarGridTimeColumnContext();
	const { position, duration } = useElementPositionInCollection({
		start,
		end,
		collectionStart: columnStart,
		collectionEnd: columnEnd,
		dayStartMinute,
		dayEndMinute
	});
	const { state } = useEvent({
		start,
		end
	});
	return useRenderElement("div", componentProps, {
		state,
		ref: [forwardedRef],
		props: [elementProps, { style: {
			[CalendarGridTimeEventPlaceholderCssVars.yPosition]: `${position * 100}%`,
			[CalendarGridTimeEventPlaceholderCssVars.height]: `${duration * 100}%`
		} }]
	});
});
CalendarGridTimeEventPlaceholder.displayName = "CalendarGridTimeEventPlaceholder";
//#endregion
//#region node_modules/@mui/x-scheduler-internals/calendar-grid/time-event-resize-handler/CalendarGridTimeEventResizeHandler.mjs
var _excluded$4 = [
	"className",
	"render",
	"style",
	"side"
];
var CalendarGridTimeEventResizeHandler = /*#__PURE__*/ import_react.forwardRef(function CalendarGridTimeEventResizeHandler(componentProps, forwardedRef) {
	const { side } = componentProps, elementProps = _objectWithoutPropertiesLoose(componentProps, _excluded$4);
	const contextValue = useCalendarGridTimeEventContext();
	const ref = import_react.useRef(null);
	const { state, enabled } = useEventResizeHandler({
		ref,
		side,
		contextValue,
		getDragData: useStableCallback((input) => _extends({}, contextValue.getSharedDragData(input), {
			source: "CalendarGridTimeEventResizeHandler",
			side
		}))
	});
	return useRenderElement("div", componentProps, {
		enabled,
		state,
		ref: [forwardedRef, ref],
		props: [elementProps]
	});
});
CalendarGridTimeEventResizeHandler.displayName = "CalendarGridTimeEventResizeHandler";
//#endregion
//#region node_modules/@mui/x-scheduler-internals/calendar-grid/current-time-indicator/CalendarGridCurrentTimeIndicatorCssVars.mjs
var CalendarGridCurrentTimeIndicatorCssVars = /*#__PURE__*/ function(CalendarGridCurrentTimeIndicatorCssVars) {
	CalendarGridCurrentTimeIndicatorCssVars["yPosition"] = "--y-position";
	return CalendarGridCurrentTimeIndicatorCssVars;
}({});
//#endregion
//#region node_modules/@mui/x-scheduler-internals/calendar-grid/current-time-indicator/CalendarGridCurrentTimeIndicator.mjs
var _excluded$3 = [
	"className",
	"render",
	"style"
];
var CalendarGridCurrentTimeIndicator = /*#__PURE__*/ import_react.forwardRef(function CalendarGridCurrentTimeIndicator(componentProps, forwardedRef) {
	const adapter = useAdapterContext();
	const elementProps = _objectWithoutPropertiesLoose(componentProps, _excluded$3);
	const { start: columnStart, end: columnEnd, dayStartMinute, dayEndMinute } = useCalendarGridTimeColumnContext();
	const now = useStore(useEventCalendarStoreContext(), schedulerNowSelectors.nowUpdatedEveryMinute);
	const nowForColumn = import_react.useMemo(() => processDate(mergeDateAndTime(adapter, columnStart, now), adapter), [
		adapter,
		columnStart,
		now
	]);
	const { position } = useElementPositionInCollection({
		start: nowForColumn,
		end: import_react.useMemo(() => processDate(adapter.addMinutes(nowForColumn.value, 1), adapter), [adapter, nowForColumn]),
		collectionStart: columnStart,
		collectionEnd: columnEnd,
		dayStartMinute,
		dayEndMinute
	});
	const isOutOfRange = adapter.isBefore(nowForColumn.value, columnStart) || adapter.isAfter(nowForColumn.value, columnEnd);
	return useRenderElement("div", componentProps, {
		ref: [forwardedRef],
		props: [elementProps, { style: { [CalendarGridCurrentTimeIndicatorCssVars.yPosition]: `${position * 100}%` } }],
		enabled: !isOutOfRange
	});
});
CalendarGridCurrentTimeIndicator.displayName = "CalendarGridCurrentTimeIndicator";
//#endregion
//#region node_modules/@mui/x-scheduler-internals/calendar-grid/use-placeholder-in-day/useCalendarGridPlaceholderInDay.mjs
function useCalendarGridPlaceholderInDay(day, row, maxEvents) {
	const adapter = useAdapterContext();
	const store = useEventCalendarStoreContext();
	const { start: rowStart, end: rowEnd } = useCalendarGridDayRowContext();
	const findAvailableIndex = import_react.useCallback((excludeKey) => {
		let positionIndex = 1;
		const targetDay = row.days.find((rowDay) => adapter.isSameDay(rowDay.value, day));
		if (targetDay) {
			const usedIndexes = new Set(targetDay.withPosition.filter((occ) => occ.key !== excludeKey).map((occ) => occ.position.index));
			while (usedIndexes.has(positionIndex)) positionIndex += 1;
		}
		if (maxEvents != null && positionIndex > maxEvents) positionIndex = maxEvents;
		return positionIndex;
	}, [
		adapter,
		day,
		maxEvents,
		row.days
	]);
	const rawPlaceholder = useStore(store, eventCalendarOccurrencePlaceholderSelectors.placeholderInDayCell, day, rowStart);
	const originalEventId = isInternalDragOrResizePlaceholder(rawPlaceholder) ? rawPlaceholder.eventId : null;
	const originalEvent = useStore(store, schedulerEventSelectors.processedEvent, originalEventId);
	return import_react.useMemo(() => {
		if (!rawPlaceholder) return null;
		const sharedProperties = {
			key: "occurrence-placeholder",
			id: originalEventId ?? "occurrence-placeholder",
			title: originalEvent ? originalEvent.title : ""
		};
		if (rawPlaceholder.type === "creation") return _extends({}, sharedProperties, {
			title: "",
			allDay: true,
			displayTimezone: {
				start: processDate(day, adapter),
				end: processDate(adapter.isAfter(rawPlaceholder.end, rowEnd) ? rowEnd : rawPlaceholder.end, adapter),
				timezone: adapter.getTimezone(day)
			},
			position: {
				index: findAvailableIndex(),
				daySpan: adapter.differenceInDays(rawPlaceholder.end, day) + 1
			}
		});
		if (rawPlaceholder.type === "external-drag") {
			const startProcessed = processDate(rawPlaceholder.start, adapter);
			const endProcessed = processDate(rawPlaceholder.end, adapter);
			const timezone = adapter.getTimezone(rawPlaceholder.start);
			return _extends({}, sharedProperties, {
				title: rawPlaceholder.eventData.title ?? "",
				displayTimezone: {
					start: startProcessed,
					end: endProcessed,
					timezone
				},
				position: {
					index: findAvailableIndex(),
					daySpan: adapter.differenceInDays(rawPlaceholder.end, day) + 1
				}
			});
		}
		return _extends({}, sharedProperties, {
			start: processDate(rawPlaceholder.start, adapter),
			end: processDate(rawPlaceholder.end, adapter),
			displayTimezone: _extends({}, originalEvent.displayTimezone),
			position: {
				index: findAvailableIndex(rawPlaceholder.occurrenceKey),
				daySpan: adapter.differenceInDays(rawPlaceholder.end, day) + 1
			}
		});
	}, [
		adapter,
		day,
		originalEvent,
		originalEventId,
		rawPlaceholder,
		rowEnd,
		findAvailableIndex
	]);
}
//#endregion
//#region node_modules/@mui/x-scheduler-internals/calendar-grid/use-placeholder-in-range/useCalendarGridPlaceholderInRange.mjs
function useCalendarGridPlaceholderInRange(parameters) {
	const { start, end, occurrences, maxIndex } = parameters;
	const adapter = useAdapterContext();
	const store = useEventCalendarStoreContext();
	const rawPlaceholder = useStore(store, eventCalendarOccurrencePlaceholderSelectors.placeholderInTimeRange, start, end);
	const originalEventId = isInternalDragOrResizePlaceholder(rawPlaceholder) ? rawPlaceholder.eventId : null;
	const originalEvent = useStore(store, schedulerEventSelectors.processedEvent, originalEventId);
	return import_react.useMemo(() => {
		if (!rawPlaceholder) return null;
		const startProcessed = processDate(rawPlaceholder.start, adapter);
		const endProcessed = processDate(rawPlaceholder.end, adapter);
		const timezone = adapter.getTimezone(rawPlaceholder.start);
		const sharedProperties = {
			key: "occurrence-placeholder",
			id: originalEventId ?? "occurrence-placeholder",
			title: originalEvent ? originalEvent.title : "",
			displayTimezone: {
				start: startProcessed,
				end: endProcessed,
				timezone
			}
		};
		if (rawPlaceholder.type === "creation") return _extends({}, sharedProperties, {
			title: "",
			position: {
				firstIndex: 1,
				lastIndex: maxIndex
			}
		});
		if (rawPlaceholder.type === "external-drag") return _extends({}, sharedProperties, {
			title: rawPlaceholder.eventData.title ?? "",
			position: {
				firstIndex: 1,
				lastIndex: maxIndex
			}
		});
		return _extends({}, sharedProperties, { position: occurrences.find((occurrence) => occurrence.key === rawPlaceholder.occurrenceKey)?.position ?? {
			firstIndex: 1,
			lastIndex: maxIndex
		} });
	}, [
		rawPlaceholder,
		adapter,
		originalEvent,
		originalEventId,
		occurrences,
		maxIndex
	]);
}
//#endregion
//#region node_modules/@mui/x-scheduler-internals/use-event-occurrences-with-timeline-position/useEventOccurrencesWithTimelinePosition.mjs
/**
* Places event occurrences for a timeline UI.
*/
function useEventOccurrencesWithTimelinePosition(parameters) {
	const { occurrences, maxSpan } = parameters;
	const adapter = useAdapterContext();
	return import_react.useMemo(() => {
		const sortedOccurrences = sortEventOccurrences(occurrences);
		const conflicts = buildOccurrenceConflicts(adapter, sortedOccurrences);
		const { firstIndexLookup, maxIndex } = buildFirstIndexLookup(conflicts);
		const lastIndexLookup = buildLastIndexLookup(conflicts, firstIndexLookup, maxIndex, maxSpan);
		return {
			occurrences: sortedOccurrences.map((occurrence) => _extends({}, occurrence, { position: {
				firstIndex: firstIndexLookup[occurrence.key],
				lastIndex: lastIndexLookup[occurrence.key]
			} })),
			maxIndex
		};
	}, [
		adapter,
		occurrences,
		maxSpan
	]);
}
/**
* Looks for conflicts between occurrences and build a list of conflicts for each occurrence.
* The provided occurrences need to be sorted by starting date-time.
*/
function buildOccurrenceConflicts(adapter, occurrences) {
	const getEmptyBlock = () => ({
		occurrences: [],
		longestDurationMs: 0
	});
	const occurrencesBlocks = [];
	let currentBlock = getEmptyBlock();
	let lastEndTimestamp = 0;
	for (const occurrence of occurrences) {
		const startTimestamp = occurrence.displayTimezone.start.timestamp;
		const endTimestamp = occurrence.displayTimezone.end.timestamp;
		const occurrenceDurationMs = endTimestamp - startTimestamp;
		if (startTimestamp >= lastEndTimestamp) {
			if (currentBlock.occurrences.length > 0) occurrencesBlocks.push(currentBlock);
			currentBlock = getEmptyBlock();
			lastEndTimestamp = 0;
		}
		currentBlock.occurrences.push({
			key: occurrence.key,
			startTimestamp,
			endTimestamp
		});
		if (occurrenceDurationMs > currentBlock.longestDurationMs) currentBlock.longestDurationMs = occurrenceDurationMs;
		if (endTimestamp > lastEndTimestamp) lastEndTimestamp = endTimestamp;
	}
	if (currentBlock.occurrences.length > 0) occurrencesBlocks.push(currentBlock);
	const conflicts = [];
	for (const block of occurrencesBlocks) for (let i = 0; i < block.occurrences.length; i += 1) {
		const occurrence = block.occurrences[i];
		const conflictsBefore = /* @__PURE__ */ new Set();
		const conflictsAfter = /* @__PURE__ */ new Set();
		for (let j = i + 1; j < block.occurrences.length; j += 1) {
			const occurrenceA = block.occurrences[j];
			if (occurrenceA.startTimestamp < occurrence.endTimestamp) conflictsAfter.add(occurrenceA.key);
			else break;
		}
		for (let j = i - 1; j >= 0; j -= 1) {
			const occurrenceB = block.occurrences[j];
			if (occurrence.startTimestamp - occurrenceB.startTimestamp > block.longestDurationMs) break;
			if (occurrenceB.endTimestamp > occurrence.startTimestamp) conflictsBefore.add(occurrenceB.key);
		}
		conflicts.push({
			key: occurrence.key,
			before: conflictsBefore,
			after: conflictsAfter
		});
	}
	return conflicts;
}
function buildFirstIndexLookup(conflicts) {
	let maxIndex = 1;
	const firstIndexLookup = {};
	for (const occurrence of conflicts) if (occurrence.before.size === 0) firstIndexLookup[occurrence.key] = 1;
	else {
		const usedIndexes = new Set(Array.from(occurrence.before).map((conflictingOccurrence) => firstIndexLookup[conflictingOccurrence]));
		let i = 1;
		while (usedIndexes.has(i)) i += 1;
		firstIndexLookup[occurrence.key] = i;
		if (i > maxIndex) maxIndex = i;
	}
	return {
		firstIndexLookup,
		maxIndex
	};
}
function buildLastIndexLookup(conflicts, firstIndexLookup, maxIndex, maxSpan) {
	if (maxSpan < 2) return firstIndexLookup;
	const lastIndexLookup = {};
	for (const occurrence of conflicts) {
		const usedIndexes = new Set([...Array.from(occurrence.before), ...Array.from(occurrence.after)].map((conflictingOccurrence) => firstIndexLookup[conflictingOccurrence]));
		const firstIndex = firstIndexLookup[occurrence.key];
		let lastIndex = firstIndex;
		while (!usedIndexes.has(lastIndex + 1) && lastIndex < maxIndex && lastIndex - firstIndex < maxSpan - 1) lastIndex += 1;
		lastIndexLookup[occurrence.key] = lastIndex;
	}
	return lastIndexLookup;
}
/**
* A block of occurrences that overlap in time.
* The occurrences of two distinct blocks never overlap in time, their conflicts can thus be computed independently.
*/
//#endregion
//#region node_modules/@mui/x-scheduler/internals/components/event-drag-preview/EventDragPreview.mjs
var EventDragPreviewRoot = styled("div", {
	name: "MuiEventCalendar",
	slot: "EventDragPreview"
})(({ theme }) => ({
	padding: theme.spacing(.5, 1),
	borderRadius: theme.spacing(.5),
	fontSize: theme.typography.body2.fontSize,
	backgroundColor: "var(--event-surface-bold)",
	color: "var(--event-on-surface-bold)",
	variants: getPaletteVariants(theme)
}));
var fakeStore = {
	subscribe: () => {},
	getSnapshot: () => ({})
};
function EventDragPreview(props) {
	const store = useSchedulerStoreContext(true);
	const styledContext = import_react.useContext(EventCalendarStyledContext);
	const color = useStore(store ?? fakeStore, store ? schedulerEventSelectors.color : () => "teal", props.data.id);
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(EventDragPreviewRoot, {
		className: styledContext?.classes.eventDragPreview,
		"data-palette": color,
		children: props.data.title
	});
}
//#endregion
//#region node_modules/@mui/x-scheduler/internals/components/event/time-grid-event/useTimeGridEvent.mjs
function useTimeGridEvent(occurrence) {
	const store = useEventCalendarStoreContext();
	const isRecurring = useStore(store, schedulerEventSelectors.isRecurring, occurrence.id);
	const isDraggable = useStore(store, schedulerEventSelectors.isDraggable, occurrence.id);
	const isStartResizable = useStore(store, schedulerEventSelectors.isResizable, occurrence.id, "start");
	const isEndResizable = useStore(store, schedulerEventSelectors.isResizable, occurrence.id, "end");
	const palette = useStore(store, schedulerEventSelectors.color, occurrence.id);
	const durationMinutes = (occurrence.displayTimezone.end.timestamp - occurrence.displayTimezone.start.timestamp) / 6e4;
	const isBetween30and60Minutes = durationMinutes >= 30 && durationMinutes < 60;
	const isLessThan30Minutes = durationMinutes < 30;
	const isLessThan15Minutes = durationMinutes <= 15;
	return {
		isRecurring,
		isDraggable,
		isStartResizable,
		isEndResizable,
		palette,
		isLessThan15Minutes,
		isLessThan30Minutes,
		isBetween30and60Minutes,
		rootDataAttributes: import_react.useMemo(() => ({
			"data-under-hour": isLessThan30Minutes || isBetween30and60Minutes || void 0,
			"data-under-fifteen-minutes": isLessThan15Minutes || void 0,
			"data-recurrent": isRecurring || void 0,
			"data-palette": palette
		}), [
			isLessThan30Minutes,
			isBetween30and60Minutes,
			isLessThan15Minutes,
			isRecurring,
			palette
		]),
		rootPositionProps: import_react.useMemo(() => ({
			start: occurrence.displayTimezone.start,
			end: occurrence.displayTimezone.end,
			style: {
				"--first-index": occurrence.position.firstIndex,
				"--last-index": occurrence.position.lastIndex
			}
		}), [
			occurrence.displayTimezone.start,
			occurrence.displayTimezone.end,
			occurrence.position.firstIndex,
			occurrence.position.lastIndex
		])
	};
}
//#endregion
//#region node_modules/@mui/x-scheduler/internals/components/event/time-grid-event/TimeGridEventShared.mjs
var linesClampStyles = (maximumLines = 1) => ({
	display: "-webkit-box",
	WebkitLineClamp: maximumLines,
	WebkitBoxOrient: "vertical",
	overflow: "hidden",
	textOverflow: "ellipsis",
	wordBreak: "break-word",
	overflowWrap: "break-word"
});
var getTimeGridEventRootStyles = (theme) => ({
	"--time-grid-event-column-gap": "12px",
	borderRadius: theme.shape.borderRadius,
	backgroundColor: "var(--event-surface-subtle)",
	position: "absolute",
	left: "calc( ((100% - var(--time-grid-event-column-gap)) / var(--columns-count)) * (var(--first-index) - 1))",
	right: "calc(((100% - var(--time-grid-event-column-gap)) * (var(--columns-count) - var(--last-index))) / var(--columns-count) + var(--time-grid-event-column-gap))",
	top: "calc(var(--y-position) + 1px)",
	bottom: "calc(100% - var(--y-position) - var(--height))",
	zIndex: 2,
	display: "flex",
	flexDirection: "column",
	gap: theme.spacing(.25),
	justifyContent: "flex-start",
	alignContent: "flex-start",
	minHeight: 11.5,
	containerType: "size",
	"&[data-dragging], &[data-resizing]": { opacity: .5 },
	"&[data-under-hour=\"true\"]": { flexDirection: "row" },
	"&:focus-visible": {
		outline: "2px solid var(--event-surface-accent)",
		outlineOffset: 2
	},
	variants: getPaletteVariants(theme),
	[eventCalendarContentCompactQuery]: { "--time-grid-event-column-gap": "0px" }
});
var TimeGridEventPlaceholder = styled(CalendarGridTimeEventPlaceholder, {
	name: "MuiEventCalendar",
	slot: "TimeGridEventPlaceholder"
})(({ theme }) => ({
	"--time-grid-event-column-gap": "12px",
	borderRadius: theme.shape.borderRadius,
	backgroundColor: "var(--event-surface-subtle-hover)",
	border: `1px dashed var(--event-on-surface-subtle-secondary)`,
	color: "var(--event-on-surface-subtle-primary)",
	position: "absolute",
	left: "calc(((100% - var(--time-grid-event-column-gap)) / var(--columns-count)) * (var(--first-index) - 1))",
	right: "calc(((100% - var(--time-grid-event-column-gap)) * (var(--columns-count) - var(--last-index))) / var(--columns-count) + var(--time-grid-event-column-gap))",
	top: "var(--y-position)",
	bottom: "calc(100% - var(--y-position) - var(--height))",
	zIndex: 2,
	padding: theme.spacing(.5, 2, .5, 1.5),
	containerType: "size",
	minHeight: 11.5,
	"&[data-under-fifteen-minutes=\"true\"]": { padding: theme.spacing(0, 1) },
	variants: getPaletteVariants(theme),
	"&::before": {
		content: "\"\"",
		position: "absolute"
	},
	[eventCalendarContentCompactQuery]: { "--time-grid-event-column-gap": "0px" }
}));
//#endregion
//#region node_modules/@mui/x-scheduler/internals/components/event/time-grid-event/TimeGridEvent.mjs
var _excluded$2 = [
	"occurrence",
	"variant",
	"className"
];
var TimeGridEventRoot = styled(CalendarGridTimeEvent, {
	name: "MuiEventCalendar",
	slot: "TimeGridEventRoot"
})(({ theme }) => _extends({}, getTimeGridEventRootStyles(theme), {
	padding: theme.spacing(.5, 1, .5, 1),
	"&[data-under-fifteen-minutes=\"true\"]": { padding: theme.spacing(0, 1) },
	"&:hover": { backgroundColor: "var(--event-surface-subtle-hover)" },
	"&[data-editing]": {
		backgroundColor: "var(--event-surface-selected)",
		color: "var(--event-on-surface-selected)",
		"&:hover": { backgroundColor: "var(--event-surface-selected-hover)" }
	},
	"&[role=\"button\"]": { cursor: "pointer" },
	"&::before": {
		content: "\"\"",
		position: "absolute",
		top: 0,
		bottom: 0,
		left: 0,
		width: 3,
		borderRadius: "4px 0 0 4px",
		background: "var(--event-surface-accent)",
		pointerEvents: "none"
	},
	"&[data-editing]::before": { background: "var(--event-surface-selected)" }
}));
var TimeGridEventTitle = styled(Typography, {
	name: "MuiEventCalendar",
	slot: "TimeGridEventTitle"
})(({ theme }) => _extends({
	margin: 0,
	color: "var(--event-on-surface-subtle-primary)",
	fontWeight: theme.typography.fontWeightMedium,
	fontSize: "var(--EventCalendar-fontSize-eventTitle, 0.75rem)",
	lineHeight: 1.43,
	paddingRight: theme.spacing(1.5),
	height: "fit-content",
	"@container (max-height: 20px)": {
		fontSize: "11px",
		lineHeight: "11px"
	},
	"[data-editing] &": { color: "var(--event-on-surface-selected)" }
}, linesClampStyles(1)));
var TimeGridEventTime = styled("time", {
	name: "MuiEventCalendar",
	slot: "TimeGridEventTime"
})(({ theme }) => ({
	color: "var(--event-on-surface-subtle-secondary)",
	fontWeight: theme.typography.fontWeightRegular,
	fontSize: "var(--EventCalendar-fontSize-timeText, 0.75rem)",
	lineHeight: 1.43,
	"[data-editing] &": { color: "var(--event-on-surface-selected)" },
	"&[data-lines-clamp]": _extends({}, linesClampStyles(1), { paddingInlineEnd: theme.spacing(1.5) }),
	"@container (max-width: 50px & max-height: 50px)": { display: "none" },
	"@container (max-height: 20px)": {
		fontSize: "11px",
		lineHeight: "11px"
	}
}));
var TimeGridEventRecurringIcon = styled(RepeatRounded_default, {
	name: "MuiEventCalendar",
	slot: "TimeGridEventRecurringIcon"
})(({ theme }) => ({
	position: "absolute",
	right: 3,
	bottom: 3,
	padding: theme.spacing(.25),
	fontSize: "var(--EventCalendar-fontSize-recurringIcon, 1.25rem)",
	color: "var(--event-on-surface-subtle-secondary)",
	"[data-editing] &": { color: "var(--event-on-surface-selected)" },
	"@container (max-width: 50px)": { display: "none" },
	"@container (max-height: 12px)": {
		fontSize: 14,
		lineHeight: 14,
		padding: 0,
		bottom: 0
	}
}));
var TimeGridEventResizeHandler = styled(CalendarGridTimeEventResizeHandler, {
	name: "MuiEventCalendar",
	slot: "TimeGridEventResizeHandler"
})({
	position: "absolute",
	height: 4,
	left: 0,
	right: 0,
	zIndex: 3,
	cursor: "ns-resize",
	opacity: 0,
	"*:hover > &": { opacity: 1 },
	"&[data-start]": { top: 0 },
	"&[data-end]": { bottom: 0 }
});
var TimeGridEvent = /*#__PURE__*/ import_react.forwardRef(function TimeGridEvent(props, forwardedRef) {
	const { occurrence, variant, className } = props, other = _objectWithoutPropertiesLoose(props, _excluded$2);
	const { classes } = useEventCalendarStyledContext();
	const { isRecurring, isDraggable, isStartResizable, isEndResizable, isLessThan30Minutes, isBetween30and60Minutes, rootDataAttributes, rootPositionProps } = useTimeGridEvent(occurrence);
	const formatTime = useFormatTime();
	const content = import_react.useMemo(() => {
		return /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(import_react.Fragment, { children: [isLessThan30Minutes || isBetween30and60Minutes ? /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(TimeGridEventTitle, {
			className: classes.timeGridEventTitle,
			children: [
				occurrence.title,
				" ",
				/*#__PURE__*/ (0, import_jsx_runtime.jsx)(TimeGridEventTime, {
					className: classes.timeGridEventTime,
					children: formatTime(occurrence.displayTimezone.start.value)
				})
			]
		}) : /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(import_react.Fragment, { children: [/*#__PURE__*/ (0, import_jsx_runtime.jsx)(TimeGridEventTitle, {
			className: classes.timeGridEventTitle,
			children: occurrence.title
		}), /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(TimeGridEventTime, {
			className: classes.timeGridEventTime,
			"data-lines-clamp": true,
			children: [
				formatTime(occurrence.displayTimezone.start.value),
				" -",
				" ",
				formatTime(occurrence.displayTimezone.end.value)
			]
		})] }), isRecurring && /*#__PURE__*/ (0, import_jsx_runtime.jsx)(TimeGridEventRecurringIcon, {
			className: classes.timeGridEventRecurringIcon,
			"aria-hidden": "true"
		})] });
	}, [
		isBetween30and60Minutes,
		isLessThan30Minutes,
		occurrence.title,
		occurrence.displayTimezone.start.value,
		occurrence.displayTimezone.end.value,
		formatTime,
		isRecurring,
		classes
	]);
	const sharedProps = _extends({}, rootPositionProps, { ref: forwardedRef }, other, { className: clsx(className, occurrence.className) });
	if (variant === "placeholder") return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(TimeGridEventPlaceholder, _extends({ "aria-hidden": true }, rootDataAttributes, sharedProps, {
		className: clsx(classes.timeGridEventPlaceholder, sharedProps.className),
		children: content
	}));
	return /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(TimeGridEventRoot, _extends({
		isDraggable,
		eventId: occurrence.id,
		occurrenceKey: occurrence.key,
		renderDragPreview: (parameters) => /*#__PURE__*/ (0, import_jsx_runtime.jsx)(EventDragPreview, _extends({}, parameters))
	}, rootDataAttributes, sharedProps, {
		className: clsx(classes.timeGridEvent, sharedProps.className),
		children: [
			isStartResizable && /*#__PURE__*/ (0, import_jsx_runtime.jsx)(TimeGridEventResizeHandler, {
				className: classes.timeGridEventResizeHandler,
				side: "start"
			}),
			content,
			isEndResizable && /*#__PURE__*/ (0, import_jsx_runtime.jsx)(TimeGridEventResizeHandler, {
				className: classes.timeGridEventResizeHandler,
				side: "end"
			})
		]
	}));
});
TimeGridEvent.displayName = "TimeGridEvent";
//#endregion
//#region node_modules/@mui/x-scheduler/internals/components/day-time-grid/DayTimeGridInternalRenderersContext.mjs
/**
* Components internally overridable inside `DayTimeGrid`. The context is seeded with the
* desktop renderers as its default value, so consumers always receive a usable renderer
* without wrapping `DayTimeGrid` in a provider. The compact views (`CompactDayView`,
* `CompactThreeDayView`, `CompactWeekView`) wrap `DayTimeGrid` with this provider to swap
* in the touch-optimized variant.
*/
var DayTimeGridInternalRenderersContext = /*#__PURE__*/ import_react.createContext({ timeGridEvent: TimeGridEvent });
DayTimeGridInternalRenderersContext.displayName = "DayTimeGridInternalRenderersContext";
function useDayTimeGridInternalRenderers() {
	return import_react.useContext(DayTimeGridInternalRenderersContext);
}
//#endregion
//#region node_modules/@mui/x-scheduler/internals/components/day-time-grid/TimeGridColumn.mjs
var DayTimeGridColumn = styled(CalendarGridTimeColumn, {
	name: "MuiEventCalendar",
	slot: "DayTimeGridColumn"
})(({ theme }) => ({
	borderInlineStart: `1px solid ${(theme.vars || theme).palette.divider}`,
	flexGrow: 1,
	flexShrink: 0,
	flexBasis: 0,
	minWidth: 0,
	position: "relative",
	"&[data-weekend]": { backgroundColor: (theme.vars || theme).palette.action.hover },
	"&:focus-visible": {
		outline: "none",
		backgroundColor: getCellFocusBackground(theme)
	}
}));
var DayTimeGridColumnInteractiveLayer = styled("div", {
	name: "MuiEventCalendar",
	slot: "DayTimeGridColumnInteractiveLayer"
})({
	position: "absolute",
	top: 0,
	bottom: 0,
	left: 0,
	right: 0
});
var DayTimeGridCurrentTimeIndicator = styled(CalendarGridCurrentTimeIndicator, {
	name: "MuiEventCalendar",
	slot: "DayTimeGridCurrentTimeIndicator"
})(({ theme }) => ({
	position: "absolute",
	zIndex: 2,
	top: "var(--y-position)",
	left: 0,
	right: -1,
	height: 0,
	borderTop: `2px solid ${(theme.vars || theme).palette.primary.main}`
}));
var DayTimeGridCurrentTimeIndicatorCircle = styled("span", {
	name: "MuiEventCalendar",
	slot: "DayTimeGridCurrentTimeIndicatorCircle"
})(({ theme }) => ({
	position: "absolute",
	zIndex: 1,
	left: -5,
	top: -5,
	width: 8,
	height: 8,
	borderRadius: "50%",
	backgroundColor: (theme.vars || theme).palette.primary.main
}));
function TimeGridColumn(props) {
	const { day, showCurrentTimeIndicator, index, colIndex, startTime, endTime } = props;
	const adapter = useAdapterContext();
	const { classes } = useEventCalendarStyledContext();
	const start = import_react.useMemo(() => adapter.setHours(adapter.startOfDay(day.value), startTime), [
		adapter,
		day,
		startTime
	]);
	const end = import_react.useMemo(() => endTime === 24 ? adapter.endOfDay(day.value) : adapter.setHours(adapter.startOfDay(day.value), endTime), [
		adapter,
		day,
		endTime
	]);
	const { occurrences, maxIndex } = useEventOccurrencesWithTimelinePosition({
		occurrences: import_react.useMemo(() => {
			const startTimestamp = adapter.getTime(start);
			const endTimestamp = adapter.getTime(end);
			return day.withoutPosition.filter((occurrence) => occurrence.displayTimezone.end.timestamp > startTimestamp && occurrence.displayTimezone.start.timestamp < endTimestamp);
		}, [
			adapter,
			day.withoutPosition,
			start,
			end
		]),
		maxSpan: Infinity
	});
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(DayTimeGridColumn, {
		className: classes.dayTimeGridColumn,
		start,
		end,
		dayStartMinute: startTime * 60,
		dayEndMinute: endTime * 60,
		addPropertiesToDroppedEvent: addPropertiesToDroppedEvent$1,
		"aria-colindex": colIndex,
		"data-weekend": isWeekend(adapter, day.value) || void 0,
		style: { "--columns-count": maxIndex },
		children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ColumnInteractiveLayer, {
			start,
			end,
			showCurrentTimeIndicator,
			index,
			occurrences,
			maxIndex
		})
	});
}
function ColumnInteractiveLayer({ start, end, showCurrentTimeIndicator, index, occurrences, maxIndex }) {
	const store = useEventCalendarStoreContext();
	const { onOpen: startEditing } = useEventDialogContext();
	const { classes } = useEventCalendarStyledContext();
	const { timeGridEvent: TimeGridEvent } = useDayTimeGridInternalRenderers();
	const columnRef = import_react.useRef(null);
	const isCreatingAnEvent = useStore(store, eventCalendarOccurrencePlaceholderSelectors.isCreatingInTimeRange, start, end);
	const placeholder = useCalendarGridPlaceholderInRange({
		start,
		end,
		occurrences,
		maxIndex
	});
	const isLoading = useStore(store, schedulerOtherSelectors.isLoading);
	import_react.useEffect(() => {
		if (!isCreatingAnEvent || !placeholder || !columnRef.current) return;
		startEditing(columnRef, placeholder);
	}, [
		isCreatingAnEvent,
		placeholder,
		startEditing
	]);
	return /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(DayTimeGridColumnInteractiveLayer, {
		className: classes.dayTimeGridColumnInteractiveLayer,
		ref: columnRef,
		children: [
			isLoading && /*#__PURE__*/ (0, import_jsx_runtime.jsx)(EventSkeleton, { "data-variant": "time-column" }),
			!isLoading && occurrences.map((occurrence) => /*#__PURE__*/ (0, import_jsx_runtime.jsx)(EventDialogTrigger, {
				occurrence,
				children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(TimeGridEvent, {
					occurrence,
					variant: "regular"
				})
			}, occurrence.key)),
			placeholder != null && /*#__PURE__*/ (0, import_jsx_runtime.jsx)(TimeGridEvent, {
				occurrence: placeholder,
				variant: "placeholder"
			}),
			showCurrentTimeIndicator ? /*#__PURE__*/ (0, import_jsx_runtime.jsx)(DayTimeGridCurrentTimeIndicator, {
				className: classes.dayTimeGridCurrentTimeIndicator,
				"aria-hidden": true,
				children: index === 0 && /*#__PURE__*/ (0, import_jsx_runtime.jsx)(DayTimeGridCurrentTimeIndicatorCircle, { className: classes.dayTimeGridCurrentTimeIndicatorCircle })
			}) : null
		]
	});
}
/**
* Makes sure any event dropped in the time grid column is turned into a non all-day event.
*/
function addPropertiesToDroppedEvent$1() {
	return { allDay: false };
}
//#endregion
//#region node_modules/@mui/x-scheduler/internals/utils/event-utils.mjs
function isOccurrenceAllDayOrMultipleDay(occurrence, adapter) {
	if (occurrence.allDay) return true;
	return !adapter.isSameDay(occurrence.displayTimezone.start.value, occurrence.displayTimezone.end.value);
}
//#endregion
//#region node_modules/@mui/x-scheduler/internals/components/event/day-grid-event/DayGridEvent.mjs
var _excluded$1 = [
	"occurrence",
	"variant",
	"style",
	"className"
];
var DayGridEventBaseStyles = (theme) => ({
	containerType: "inline-size",
	borderRadius: theme.shape.borderRadius * .75,
	minWidth: 18,
	height: 18,
	cursor: "pointer",
	position: "relative",
	zIndex: 1,
	gridRow: "var(--grid-row)",
	gridColumn: 1,
	padding: `0 ${theme.spacing(.5)}`,
	display: "flex",
	alignItems: "center",
	gap: theme.spacing(1),
	width: `calc(var(--grid-column-span) * 100% + (var(--grid-column-span) - 1) * (2 * ${theme.spacing(.5)} + 1px))`,
	"&[data-dragging], &[data-resizing]": { opacity: .5 },
	variants: getPaletteVariants(theme)
});
var DayGridEventRoot = styled(CalendarGridDayEvent, {
	name: "MuiEventCalendar",
	slot: "DayGridEvent"
})(({ theme }) => _extends({}, DayGridEventBaseStyles(theme), {
	"&:focus-visible": {
		outline: "2px solid var(--event-surface-accent)",
		outlineOffset: 1
	},
	"&[data-variant=\"filled\"]": {
		backgroundColor: "var(--event-surface-bold)",
		"&:active": {},
		"&:hover": { backgroundColor: "var(--event-surface-bold-hover)" },
		"&[data-editing]": {
			backgroundColor: "var(--event-surface-selected)",
			"&:hover": { backgroundColor: "var(--event-surface-selected-hover)" }
		},
		[`& .${eventCalendarClasses.dayGridEventRecurringIcon}`]: { color: "var(--event-on-surface-bold)" },
		"&[data-starting-before-edge]": {
			borderTopLeftRadius: 0,
			borderBottomLeftRadius: 0,
			clipPath: LEFT_ARROW_CLIP,
			paddingLeft: 16
		},
		"&[data-ending-after-edge]": {
			borderTopRightRadius: 0,
			borderBottomRightRadius: 0,
			clipPath: RIGHT_ARROW_CLIP,
			paddingRight: 16
		},
		"&[data-starting-before-edge][data-ending-after-edge]": { clipPath: BOTH_ARROWS_CLIP },
		"&[data-starting-before-edge]:focus-visible, &[data-ending-after-edge]:focus-visible": {
			clipPath: "none",
			borderRadius: theme.shape.borderRadius * .75
		}
	},
	"&[data-variant=\"invisible\"]": {
		width: "100%",
		visibility: "hidden",
		height: 18
	},
	"&[data-variant=\"compact\"]": {
		height: "fit-content",
		"&:active": {},
		"&:hover": { backgroundColor: (theme.vars || theme).palette.action.hover },
		"&[data-editing]": {
			backgroundColor: "var(--event-surface-subtle)",
			"&:hover": { backgroundColor: "var(--event-surface-subtle-hover)" }
		}
	}
}));
var DayGridEventPlaceholder = styled(CalendarGridDayEventPlaceholder, {
	name: "MuiEventCalendar",
	slot: "DayGridEventPlaceholder"
})(({ theme }) => _extends({}, DayGridEventBaseStyles(theme), {
	minHeight: 18,
	zIndex: 2,
	backgroundColor: "var(--event-surface-subtle-hover)",
	color: "var(--event-on-surface-subtle-primary)",
	border: `1px dashed var(--event-on-surface-subtle-secondary)`
}));
var DayGridEventTitle = styled("p", {
	name: "MuiEventCalendar",
	slot: "DayGridEventTitle"
})(({ theme }) => ({
	margin: 0,
	fontWeight: theme.typography.fontWeightMedium,
	fontSize: "var(--EventCalendar-fontSize-eventTitle, 0.75rem)",
	lineHeight: 1.43,
	flexGrow: 1,
	"[data-variant=\"filled\"] &": { color: "var(--event-on-surface-bold)" },
	"[data-variant=\"compact\"] &": { color: (theme.vars || theme).palette.text.primary },
	"[data-editing] &": { color: "var(--event-on-surface-selected)" },
	"[data-variant=\"compact\"][data-editing] &": { color: "var(--event-on-surface-subtle-primary)" }
}));
var DayGridEventTime = styled("time", {
	name: "MuiEventCalendar",
	slot: "DayGridEventTime"
})(({ theme }) => ({
	display: "inline-block",
	color: "var(--event-on-surface-subtle-secondary)",
	fontWeight: theme.typography.fontWeightRegular,
	fontSize: theme.typography.caption.fontSize,
	lineHeight: 1.43,
	whiteSpace: "nowrap",
	paddingInlineEnd: theme.spacing(.5),
	"@container (width < 300px)": {
		paddingInlineEnd: 0,
		display: "inline",
		"& > span:last-of-type": { display: "none" }
	},
	"[data-variant=\"compact\"] &": { color: (theme.vars || theme).palette.text.secondary },
	"[data-editing] &": { color: "var(--event-on-surface-selected)" },
	"[data-variant=\"compact\"][data-editing] &": { color: "var(--event-on-surface-subtle-secondary)" }
}));
var DayGridEventRecurringIcon = styled(RepeatRounded_default, {
	name: "MuiEventCalendar",
	slot: "DayGridEventRecurringIcon"
})(({ theme }) => ({
	color: (theme.vars || theme).palette.text.primary,
	fontSize: "1rem",
	justifySelf: "flex-end",
	"[data-editing] &": { color: "var(--event-on-surface-selected)" },
	"[data-variant=\"compact\"][data-editing] &": { color: "var(--event-on-surface-bold)" }
}));
var DayGridEventResizeHandler = styled(CalendarGridDayEventResizeHandler, {
	name: "MuiEventCalendar",
	slot: "DayGridEventResizeHandler"
})({
	position: "absolute",
	width: 4,
	top: 0,
	bottom: 0,
	zIndex: 3,
	cursor: "ew-resize",
	opacity: 0,
	"*:hover > &": { opacity: 1 },
	"&[data-start]": { left: 0 },
	"&[data-end]": { right: 0 }
});
var DayGridEventCardWrapper = styled("div", {
	name: "MuiEventCalendar",
	slot: "DayGridEventCardWrapper"
})(({ theme }) => ({
	display: "flex",
	alignItems: "center",
	gap: theme.spacing(1),
	flexGrow: 1,
	"@container (width < 300px)": {
		gap: theme.spacing(.5),
		[`& .${eventCalendarClasses.dayGridEventTitle}`]: {
			marginInlineStart: theme.spacing(.5),
			paddingInlineEnd: theme.spacing(1.5)
		}
	}
}));
var DayGridEventCardContent = styled("p", {
	name: "MuiEventCalendar",
	slot: "DayGridEventCardContent"
})({
	margin: 0,
	height: 20,
	lineHeight: "20px"
});
var EventColorIndicator = styled("span", {
	name: "MuiEventCalendar",
	slot: "EventColorIndicator"
})({
	width: 8,
	height: 8,
	borderRadius: "50%",
	flexShrink: 0,
	backgroundColor: "var(--event-main)",
	marginTop: 2
});
var DayGridEventLinesClamp = styled("span", {
	name: "MuiEventCalendar",
	slot: "DayGridEventLinesClamp"
})({
	display: "-webkit-box",
	WebkitLineClamp: "var(--number-of-lines)",
	WebkitBoxOrient: "vertical",
	overflow: "hidden",
	textOverflow: "ellipsis",
	wordBreak: "break-word",
	overflowWrap: "break-word",
	flexGrow: 1
});
var isResizableSelector = createSelector((state, side, occurrence) => {
	if (!schedulerEventSelectors.isResizable(state, occurrence.id, side)) return false;
	const view = eventCalendarViewSelectors.view(state);
	if (view === "day") return false;
	if (view === "month") return isOccurrenceAllDayOrMultipleDay(occurrence, state.adapter);
	return true;
});
var DayGridEvent = /*#__PURE__*/ import_react.forwardRef(function DayGridEvent(props, forwardedRef) {
	const { occurrence, variant, style: styleProp, className } = props, other = _objectWithoutPropertiesLoose(props, _excluded$1);
	const { classes, localeText } = useEventCalendarStyledContext();
	const store = useEventCalendarStoreContext();
	const isDraggable = useStore(store, schedulerEventSelectors.isDraggable, occurrence.id);
	const isStartResizable = useStore(store, isResizableSelector, "start", occurrence);
	const isEndResizable = useStore(store, isResizableSelector, "end", occurrence);
	const isRecurring = useStore(store, schedulerEventSelectors.isRecurring, occurrence.id);
	const resource = useStore(store, schedulerResourceSelectors.processedResource, getPrimaryResourceId(occurrence.resource));
	const color = useStore(store, schedulerEventSelectors.color, occurrence.id);
	const formatTime = useFormatTime();
	const content = import_react.useMemo(() => {
		switch (variant) {
			case "invisible": return null;
			case "filled":
			case "placeholder": return /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(import_react.Fragment, { children: [/*#__PURE__*/ (0, import_jsx_runtime.jsx)(DayGridEventLinesClamp, {
				className: classes.dayGridEventLinesClamp,
				style: { "--number-of-lines": 1 },
				children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(DayGridEventTitle, {
					className: classes.dayGridEventTitle,
					children: occurrence.title
				})
			}), isRecurring && /*#__PURE__*/ (0, import_jsx_runtime.jsx)(DayGridEventRecurringIcon, {
				className: classes.dayGridEventRecurringIcon,
				"aria-hidden": "true",
				fontSize: "small"
			})] });
			case "compact": return /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(DayGridEventCardWrapper, {
				className: classes.dayGridEventCardWrapper,
				children: [
					/*#__PURE__*/ (0, import_jsx_runtime.jsx)(EventColorIndicator, {
						className: classes.eventColorIndicator,
						role: "img",
						"aria-label": resource?.title ? localeText.resourceAriaLabel(resource.title) : localeText.noResourceAriaLabel
					}),
					/*#__PURE__*/ (0, import_jsx_runtime.jsx)(DayGridEventCardContent, {
						className: classes.dayGridEventCardContent,
						children: /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(DayGridEventLinesClamp, {
							className: classes.dayGridEventLinesClamp,
							style: { "--number-of-lines": 1 },
							children: [/*#__PURE__*/ (0, import_jsx_runtime.jsxs)(DayGridEventTime, {
								className: classes.dayGridEventTime,
								children: [/*#__PURE__*/ (0, import_jsx_runtime.jsx)("span", { children: formatTime(occurrence.displayTimezone.start.value) }), /*#__PURE__*/ (0, import_jsx_runtime.jsxs)("span", { children: [" - ", formatTime(occurrence.displayTimezone.end.value)] })]
							}), /*#__PURE__*/ (0, import_jsx_runtime.jsx)(DayGridEventTitle, {
								className: classes.dayGridEventTitle,
								as: "span",
								children: occurrence.title
							})]
						})
					}),
					isRecurring && /*#__PURE__*/ (0, import_jsx_runtime.jsx)(DayGridEventRecurringIcon, {
						className: classes.dayGridEventRecurringIcon,
						"aria-hidden": "true",
						fontSize: "small"
					})
				]
			});
			default: throw new Error("MUI X Scheduler: Unsupported variant provided to DayGridEvent component. The DayGridEvent component only supports specific variant values. Check the component documentation for supported variants.");
		}
	}, [
		variant,
		occurrence.title,
		occurrence.displayTimezone.start.value,
		occurrence.displayTimezone.end.value,
		isRecurring,
		resource?.title,
		localeText,
		formatTime,
		classes
	]);
	const sharedProps = _extends({
		start: occurrence.displayTimezone.start,
		end: occurrence.displayTimezone.end,
		ref: forwardedRef,
		"data-variant": variant,
		"data-palette": color,
		style: _extends({
			"--grid-row": occurrence.position.index,
			"--grid-column-span": occurrence.position.daySpan
		}, styleProp)
	}, other, { className: clsx(className, occurrence.className) });
	if (variant === "placeholder") return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(DayGridEventPlaceholder, _extends({ "aria-hidden": true }, sharedProps, {
		className: clsx(classes.dayGridEventPlaceholder, sharedProps.className),
		children: content
	}));
	return /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(DayGridEventRoot, _extends({
		eventId: occurrence.id,
		occurrenceKey: occurrence.key,
		isDraggable,
		renderDragPreview: (parameters) => /*#__PURE__*/ (0, import_jsx_runtime.jsx)(EventDragPreview, _extends({}, parameters)),
		"aria-hidden": variant === "invisible"
	}, sharedProps, {
		className: clsx(classes.dayGridEvent, sharedProps.className),
		children: [
			isStartResizable && /*#__PURE__*/ (0, import_jsx_runtime.jsx)(DayGridEventResizeHandler, {
				className: classes.dayGridEventResizeHandler,
				side: "start"
			}),
			content,
			isEndResizable && /*#__PURE__*/ (0, import_jsx_runtime.jsx)(DayGridEventResizeHandler, {
				className: classes.dayGridEventResizeHandler,
				side: "end"
			})
		]
	}));
});
DayGridEvent.displayName = "DayGridEvent";
//#endregion
//#region node_modules/@mui/x-scheduler/internals/components/day-time-grid/DayGridCell.mjs
var EVENT_HEIGHT = 22;
var DayTimeGridAllDayEventsCell$1 = styled(CalendarGridDayCell, {
	name: "MuiEventCalendar",
	slot: "DayTimeGridAllDayEventsCell"
})(({ theme }) => ({
	flexGrow: 1,
	flexShrink: 0,
	flexBasis: 0,
	minWidth: 0,
	padding: theme.spacing(.5),
	display: "grid",
	gridTemplateRows: "repeat(var(--row-count), minmax(auto, 18px))",
	gap: theme.spacing(.5),
	lineHeight: "18px",
	minHeight: `calc(var(--row-count, 0) * ${EVENT_HEIGHT}px + ${theme.spacing(.5)})`,
	"&[data-weekend]": { backgroundColor: (theme.vars || theme).palette.action.hover },
	"&:focus-visible": {
		outline: "none",
		backgroundColor: getCellFocusBackground(theme)
	}
}));
var DayTimeGridAllDayEventsCellEvents = styled("div", {
	name: "MuiEventCalendar",
	slot: "DayTimeGridAllDayEventsCellEvents"
})(({ theme }) => ({
	position: "relative",
	display: "grid",
	gap: theme.spacing(.5)
}));
var DayTimeGridAllDayEventContainer = styled("div", {
	name: "MuiEventCalendar",
	slot: "DayTimeGridAllDayEventContainer"
})({ display: "contents" });
function DayGridCell(props) {
	const { day, row, colIndex } = props;
	const adapter = useAdapterContext();
	const store = useEventCalendarStoreContext();
	const { onOpen: startEditing } = useEventDialogContext();
	const { schedulerId, classes } = useEventCalendarStyledContext();
	const cellRef = import_react.useRef(null);
	const isCreatingAnEvent = useStore(store, eventCalendarOccurrencePlaceholderSelectors.isCreatingInDayCell, day.value);
	const placeholder = useCalendarGridPlaceholderInDay(day.value, row);
	const isLoading = useStore(store, schedulerOtherSelectors.isLoading);
	const rowCount = Math.max(row.maxIndex, placeholder?.position.index ?? 0);
	import_react.useEffect(() => {
		if (!isCreatingAnEvent || !placeholder || !cellRef.current) return;
		startEditing(cellRef, placeholder);
	}, [
		isCreatingAnEvent,
		placeholder,
		startEditing
	]);
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(DayTimeGridAllDayEventsCell$1, {
		className: classes.dayTimeGridAllDayEventsCell,
		ref: cellRef,
		value: day.value,
		addPropertiesToDroppedEvent,
		style: { "--row-count": rowCount },
		"aria-labelledby": `${schedulerId}-DayTimeGridAllDayEventsHeaderCell`,
		"aria-colindex": colIndex,
		role: "gridcell",
		"data-weekend": isWeekend(adapter, day.value) || void 0,
		children: /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(DayTimeGridAllDayEventsCellEvents, {
			className: classes.dayTimeGridAllDayEventsCellEvents,
			children: [
				isLoading && /*#__PURE__*/ (0, import_jsx_runtime.jsx)(EventSkeleton, { "data-variant": "day-grid" }),
				day.withPosition.map((occurrence) => {
					if (occurrence.position.isInvisible) return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(DayGridEvent, {
						occurrence,
						variant: "invisible"
					}, occurrence.key);
					return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(EventDialogTrigger, {
						occurrence,
						children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(DayGridEvent, {
							occurrence,
							variant: "filled"
						})
					}, occurrence.key);
				}),
				placeholder != null && /*#__PURE__*/ (0, import_jsx_runtime.jsx)(DayTimeGridAllDayEventContainer, {
					className: classes.dayTimeGridAllDayEventContainer,
					children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(DayGridEvent, {
						occurrence: placeholder,
						variant: "placeholder"
					})
				})
			]
		})
	});
}
/**
* Makes sure any event dropped in the day cell is turned into an all-day event.
*/
function addPropertiesToDroppedEvent() {
	return { allDay: true };
}
//#endregion
//#region node_modules/@mui/x-scheduler/internals/utils/getTimeGridHourRange.mjs
var DEFAULT_START_TIME = 0;
var DEFAULT_END_TIME = 24;
/**
* Resolves and validates the `startTime` / `endTime` hour range of a time-grid based view.
*
* Both values must be whole hours between 0 and 24 with `startTime < endTime`. When the range is
* invalid (non-integer, out of bounds, or inverted), it falls back to the full day (`0`–`24`) and
* warns in development.
*/
function getTimeGridHourRange(startTime, endTime) {
	const resolvedStartTime = startTime ?? DEFAULT_START_TIME;
	const resolvedEndTime = endTime ?? DEFAULT_END_TIME;
	if (!(Number.isInteger(resolvedStartTime) && Number.isInteger(resolvedEndTime) && resolvedStartTime >= 0 && resolvedEndTime <= 24 && resolvedStartTime < resolvedEndTime)) {
		warnOnce([
			`MUI X Scheduler: The time-grid view received an invalid hour range (startTime: ${resolvedStartTime}, endTime: ${resolvedEndTime}).`,
			"`startTime` and `endTime` must be whole hours between 0 and 24 with `startTime` lower than `endTime`.",
			"Falling back to the full day (0–24)."
		]);
		return {
			startTime: DEFAULT_START_TIME,
			endTime: DEFAULT_END_TIME
		};
	}
	return {
		startTime: resolvedStartTime,
		endTime: resolvedEndTime
	};
}
//#endregion
//#region node_modules/@mui/x-scheduler/internals/components/day-time-grid/DayTimeGrid.mjs
var _excluded = [
	"days",
	"className",
	"startTime",
	"endTime"
];
var HOUR_HEIGHT = 46;
var DEFAULT_HOURS_IN_DAY = 24;
var DayTimeGridContainer = styled(CalendarGridRoot, {
	name: "MuiEventCalendar",
	slot: "DayTimeGridContainer"
})(({ theme }) => ({
	"--fixed-cell-width": "var(--EventCalendar-size-fixedCellWidth)",
	"--hour-height": `${HOUR_HEIGHT}px`,
	width: "100%",
	height: "100%",
	display: "flex",
	flexDirection: "column",
	overflow: "hidden",
	border: `1px solid ${(theme.vars || theme).palette.divider}`,
	borderRadius: theme.shape.borderRadius,
	maxHeight: "100%"
}));
var DayTimeGridRoot = styled("div", {
	name: "MuiEventCalendar",
	slot: "DayTimeGrid"
})({
	display: "flex",
	flexDirection: "column",
	flex: 1,
	overflowY: "auto"
});
var DayTimeGridHeader = styled(CalendarGridHeaderRow, {
	name: "MuiEventCalendar",
	slot: "DayTimeGridHeader"
})(({ theme }) => ({
	display: "grid",
	gridTemplateColumns: "minmax(var(--fixed-cell-width), auto) repeat(var(--column-count), minmax(0, 1fr)) fit-content(100%)",
	width: "100%",
	borderBlockEnd: `1px solid ${(theme.vars || theme).palette.divider}`
}));
var DayTimeGridAllDayEventsGrid = styled("div", {
	name: "MuiEventCalendar",
	slot: "DayTimeGridAllDayEventsGrid"
})(({ theme }) => ({
	display: "grid",
	gridTemplateColumns: "var(--fixed-cell-width) repeat(var(--column-count), 1fr) fit-content(100%)",
	width: "100%",
	borderBlockEnd: `1px solid ${(theme.vars || theme).palette.divider}`,
	[`&[data-has-scroll] .${eventCalendarClasses.dayTimeGridScrollablePlaceholder}`]: {
		overflowY: "scroll",
		scrollbarGutter: "stable",
		height: "100%",
		gridColumn: "-1",
		scrollbarColor: "transparent transparent",
		background: "transparent",
		"&::-webkit-scrollbar": { background: "transparent" },
		"&::-webkit-scrollbar-thumb": { background: "transparent" },
		"&::-webkit-scrollbar-track": { background: "transparent" }
	},
	[`&[data-weekend][data-has-scroll] .${eventCalendarClasses.dayTimeGridScrollablePlaceholder}`]: {
		background: (theme.vars || theme).palette.action.hover,
		"&::-webkit-scrollbar": { background: (theme.vars || theme).palette.action.hover }
	}
}));
var DayTimeGridAllDayEventsRow = styled(CalendarGridDayRow, {
	name: "MuiEventCalendar",
	slot: "DayTimeGridAllDayEventsRow"
})(({ theme }) => ({
	position: "relative",
	backgroundColor: "transparent",
	display: "grid",
	gridTemplateColumns: "subgrid",
	gridColumn: "2 / -1",
	gridRow: 1,
	width: "100%",
	height: "100%",
	"& > *": { borderInlineStart: `1px solid ${(theme.vars || theme).palette.divider}` }
}));
var DayTimeGridAllDayEventsCell = styled("div", {
	name: "MuiEventCalendar",
	slot: "DayTimeGridAllDayEventsCell"
})(({ theme }) => ({
	padding: theme.spacing(.5),
	backgroundColor: (theme.vars || theme).palette.background.paper
}));
var DayTimeGridAllDayEventsHeaderCell = styled("div", {
	name: "MuiEventCalendar",
	slot: "DayTimeGridAllDayEventsHeaderCell"
})(({ theme }) => ({
	gridColumn: "1",
	gridRow: "1",
	fontSize: "var(--EventCalendar-fontSize-timeText, 0.75rem)",
	fontStyle: "italic",
	padding: theme.spacing(1),
	textAlign: "end",
	color: (theme.vars || theme).palette.text.secondary,
	overflowWrap: "break-word",
	wordBreak: "break-word",
	hyphens: "auto"
}));
var DayTimeGridHeaderContent = styled("span", {
	name: "MuiEventCalendar",
	slot: "DayTimeGridHeaderContent"
})(({ theme }) => ({
	display: "flex",
	flexDirection: "column",
	alignItems: "center",
	gap: theme.spacing(1),
	padding: theme.spacing(1.25)
}));
var DayTimeGridHeaderCell = styled(CalendarGridHeaderCell, {
	name: "MuiEventCalendar",
	slot: "DayTimeGridHeaderCell"
})(({ theme }) => ({
	display: "flex",
	"&:focus-visible": {
		outline: `2px solid ${(theme.vars || theme).palette.primary.main}`,
		outlineOffset: -2,
		borderRadius: theme.shape.borderRadius
	},
	"&:last-of-type": { gridColumn: "span 2" },
	"&[data-has-scroll]": {
		display: "grid",
		gridTemplateColumns: "subgrid"
	},
	[`&[data-has-scroll] .${eventCalendarClasses.dayTimeGridScrollablePlaceholder}`]: {
		overflowY: "scroll",
		scrollbarGutter: "stable",
		scrollbarColor: "transparent transparent",
		background: "transparent",
		"&::-webkit-scrollbar": { background: "transparent" },
		"&::-webkit-scrollbar-thumb": { background: "transparent" },
		"&::-webkit-scrollbar-track": { background: "transparent" }
	}
}));
var DayTimeGridHeaderButton = styled("button", {
	name: "MuiEventCalendar",
	slot: "DayTimeGridHeaderButton"
})(({ theme }) => ({
	display: "flex",
	flexDirection: "column",
	alignItems: "center",
	gap: theme.spacing(1),
	background: "none",
	border: "none",
	cursor: "pointer",
	font: "inherit",
	color: "inherit",
	padding: theme.spacing(.25),
	flexGrow: 1,
	"&:focus-visible": {
		outline: `2px solid ${(theme.vars || theme).palette.primary.main}`,
		outlineOffset: -2,
		borderRadius: theme.shape.borderRadius
	}
}));
var DayTimeGridHeaderDayName = styled("span", {
	name: "MuiEventCalendar",
	slot: "DayTimeGridHeaderDayName"
})(({ theme }) => ({
	fontSize: theme.typography.body2.fontSize,
	color: (theme.vars || theme).palette.text.secondary,
	lineHeight: 1,
	"[data-current] &": { color: (theme.vars || theme).palette.primary.main }
}));
var DayTimeGridHeaderDayNumber = styled("span", {
	name: "MuiEventCalendar",
	slot: "DayTimeGridHeaderDayNumber"
})(({ theme }) => ({
	fontSize: "var(--EventCalendar-fontSize-dayNumber, 1.5rem)",
	lineHeight: 1,
	width: 46,
	height: 46,
	display: "flex",
	alignItems: "center",
	justifyContent: "center",
	borderRadius: "50%",
	"button:hover &": { backgroundColor: (theme.vars || theme).palette.action.hover },
	"[data-current] &": {
		backgroundColor: (theme.vars || theme).palette.primary.main,
		color: (theme.vars || theme).palette.primary.contrastText
	},
	"[data-current] button:hover &": { backgroundColor: (theme.vars || theme).palette.primary.dark },
	[eventCalendarContentCompactQuery]: {
		width: 32,
		height: 32
	}
}));
var DayTimeGridBody = styled("div", {
	name: "MuiEventCalendar",
	slot: "DayTimeGridBody"
})({
	flex: 1,
	height: "100%"
});
var DayTimeGridScrollableContent = styled(CalendarGridTimeScrollableContent, {
	name: "MuiEventCalendar",
	slot: "DayTimeGridScrollableContent"
})({
	display: "flex",
	height: `calc(var(--hour-height) * var(--hours-count, ${DEFAULT_HOURS_IN_DAY}))`,
	position: "relative",
	overflowY: "auto",
	overflowX: "clip"
});
var DayTimeGridTimeAxis = styled("div", {
	name: "MuiEventCalendar",
	slot: "DayTimeGridTimeAxis"
})({
	display: "flex",
	flexDirection: "column",
	flexShrink: 0,
	width: "var(--fixed-cell-width)"
});
var DayTimeGridTimeAxisCell = styled("div", {
	name: "MuiEventCalendar",
	slot: "DayTimeGridTimeAxisCell"
})(({ theme }) => ({
	height: `calc(100% / var(--hours-count, ${DEFAULT_HOURS_IN_DAY}))`,
	lineHeight: `calc(100% / var(--hours-count, ${DEFAULT_HOURS_IN_DAY}))`,
	paddingInline: theme.spacing(1),
	textAlign: "end",
	"&:not(:first-of-type)::after": {
		content: "\"\"",
		position: "absolute",
		left: "var(--fixed-cell-width)",
		right: 0,
		borderBlockEnd: `1px solid ${(theme.vars || theme).palette.divider}`,
		top: "calc(var(--hour) * var(--hour-height))",
		zIndex: 1
	}
}));
var DayTimeGridTimeAxisText = styled("time", {
	name: "MuiEventCalendar",
	slot: "DayTimeGridTimeAxisText"
})(({ theme }) => ({
	fontSize: "var(--EventCalendar-fontSize-timeText, 0.75rem)",
	lineHeight: `calc(100% / var(--hours-count, ${DEFAULT_HOURS_IN_DAY}))`,
	color: (theme.vars || theme).palette.text.secondary,
	whiteSpace: "nowrap"
}));
var DayTimeGridGrid = styled("div", {
	name: "MuiEventCalendar",
	slot: "DayTimeGridGrid"
})(({ theme }) => ({
	display: "grid",
	gridTemplateColumns: "repeat(auto-fit, minmax(0, 1fr))",
	width: "100%",
	position: "relative",
	[`&[data-has-scroll] .${eventCalendarClasses.dayTimeGridColumn}:last-of-type`]: { borderInlineEnd: `1px solid ${(theme.vars || theme).palette.divider}` }
}));
var DayTimeGrid = /*#__PURE__*/ import_react.forwardRef(function DayTimeGrid(props, forwardedRef) {
	const { days, className, startTime: startTimeProp, endTime: endTimeProp } = props, other = _objectWithoutPropertiesLoose(props, _excluded);
	const { startTime, endTime } = getTimeGridHourRange(startTimeProp, endTimeProp);
	const hoursCount = endTime - startTime;
	const adapter = useAdapterContext();
	const { schedulerId, classes, localeText } = useEventCalendarStyledContext();
	const store = useEventCalendarStoreContext();
	const bodyRef = import_react.useRef(null);
	const allDayHeaderWrapperRef = import_react.useRef(null);
	const handleRef = useMergedRefs(forwardedRef, import_react.useRef(null));
	const hasDayView = useStore(store, eventCalendarViewSelectors.hasDayView);
	const now = useStore(store, schedulerNowSelectors.nowUpdatedEveryMinute);
	const showCurrentTimeIndicator = useStore(store, schedulerNowSelectors.showCurrentTimeIndicator);
	const occurrencesMap = useEventOccurrencesGroupedByDay({ days });
	const occurrences = useEventOccurrencesWithDayGridPosition({
		days,
		occurrencesMap,
		shouldAddPosition: import_react.useCallback((occurrence) => isOccurrenceAllDayOrMultipleDay(occurrence, adapter), [adapter])
	});
	const formatTime = useFormatTime();
	const [hasScroll, setHasScroll] = import_react.useState(false);
	const { start, end } = import_react.useMemo(() => ({
		start: days[0].value,
		end: adapter.endOfDay(days[days.length - 1].value)
	}), [adapter, days]);
	const isTodayInView = import_react.useMemo(() => adapter.isWithinRange(now, [adapter.startOfDay(days[0].value), adapter.endOfDay(days[days.length - 1].value)]), [
		adapter,
		days,
		now
	]);
	const updateHasScroll = import_react.useCallback(() => {
		const body = bodyRef.current;
		const allDayHeader = allDayHeaderWrapperRef.current;
		if (!body || !allDayHeader) return;
		setHasScroll(body.scrollHeight > body.clientHeight);
	}, []);
	useIsoLayoutEffect(updateHasScroll, [occurrencesMap, updateHasScroll]);
	useResizeObserver(bodyRef, updateHasScroll);
	const lastIsWeekend = isWeekend(adapter, days[days.length - 1].value);
	const template = adapter.date("2020-01-01T00:00:00", "default");
	const renderHeaderContent = (day) => /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(DayTimeGridHeaderContent, {
		className: classes.dayTimeGridHeaderContent,
		children: [/*#__PURE__*/ (0, import_jsx_runtime.jsx)(DayTimeGridHeaderDayName, {
			className: classes.dayTimeGridHeaderDayName,
			children: adapter.format(day.value, "weekday3Letters")
		}), /*#__PURE__*/ (0, import_jsx_runtime.jsx)(DayTimeGridHeaderDayNumber, {
			className: classes.dayTimeGridHeaderDayNumber,
			children: adapter.format(day.value, "dayOfMonth")
		})]
	});
	renderHeaderContent.displayName = "renderHeaderContent";
	return /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(DayTimeGridContainer, _extends({ ref: handleRef }, other, {
		className: clsx(className, classes.dayTimeGridContainer),
		"aria-rowcount": 3,
		"aria-colcount": days.length,
		children: [
			/*#__PURE__*/ (0, import_jsx_runtime.jsxs)(DayTimeGridHeader, {
				className: classes.dayTimeGridHeader,
				as: CalendarGridHeaderRow,
				"aria-rowindex": 1,
				style: { "--column-count": days.length },
				children: [/*#__PURE__*/ (0, import_jsx_runtime.jsx)(DayTimeGridAllDayEventsCell, {
					className: classes.dayTimeGridAllDayEventsCell,
					"aria-hidden": "true"
				}), days.map((day, index) => /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(DayTimeGridHeaderCell, {
					className: classes.dayTimeGridHeaderCell,
					date: day,
					ariaLabelFormat: `${adapter.formats.weekday} ${adapter.formats.dayOfMonth}`,
					"aria-colindex": index + 1,
					"data-has-scroll": index === days.length - 1 && hasScroll || void 0,
					children: [hasDayView ? /*#__PURE__*/ (0, import_jsx_runtime.jsx)(DayTimeGridHeaderButton, {
						className: classes.dayTimeGridHeaderButton,
						type: "button",
						onClick: (event) => store.switchToDay(day.value, event),
						tabIndex: -1,
						children: renderHeaderContent(day)
					}) : renderHeaderContent(day), index === days.length - 1 && /*#__PURE__*/ (0, import_jsx_runtime.jsx)("div", { className: classes.dayTimeGridScrollablePlaceholder })]
				}, day.key))]
			}),
			/*#__PURE__*/ (0, import_jsx_runtime.jsxs)(DayTimeGridAllDayEventsGrid, {
				className: classes.dayTimeGridAllDayEventsGrid,
				ref: allDayHeaderWrapperRef,
				"data-weekend": lastIsWeekend || void 0,
				"data-has-scroll": hasScroll || void 0,
				style: { "--column-count": days.length },
				children: [
					/*#__PURE__*/ (0, import_jsx_runtime.jsx)(DayTimeGridAllDayEventsHeaderCell, {
						className: classes.dayTimeGridAllDayEventsHeaderCell,
						id: `${schedulerId}-DayTimeGridAllDayEventsHeaderCell`,
						"aria-hidden": "true",
						children: localeText.allDay
					}),
					/*#__PURE__*/ (0, import_jsx_runtime.jsx)(DayTimeGridAllDayEventsRow, {
						as: CalendarGridDayRow,
						start,
						end,
						className: classes.dayTimeGridAllDayEventsRow,
						children: occurrences.days.map((day, index) => /*#__PURE__*/ (0, import_jsx_runtime.jsx)(DayGridCell, {
							day,
							row: occurrences,
							colIndex: index + 1
						}, day.key))
					}),
					/*#__PURE__*/ (0, import_jsx_runtime.jsx)("div", { className: classes.dayTimeGridScrollablePlaceholder })
				]
			}),
			/*#__PURE__*/ (0, import_jsx_runtime.jsx)(DayTimeGridRoot, {
				className: classes.dayTimeGrid,
				children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(DayTimeGridBody, {
					className: classes.dayTimeGridBody,
					ref: bodyRef,
					children: /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(DayTimeGridScrollableContent, {
						className: classes.dayTimeGridScrollableContent,
						as: CalendarGridTimeScrollableContent,
						style: { "--hours-count": hoursCount },
						children: [/*#__PURE__*/ (0, import_jsx_runtime.jsx)(DayTimeGridTimeAxis, {
							className: classes.dayTimeGridTimeAxis,
							"aria-hidden": "true",
							children: Array.from({ length: hoursCount }, (_, index) => {
								const hour = startTime + index;
								return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(DayTimeGridTimeAxisCell, {
									className: classes.dayTimeGridTimeAxisCell,
									style: { "--hour": index },
									children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(DayTimeGridTimeAxisText, {
										className: classes.dayTimeGridTimeAxisText,
										as: "time",
										children: index === 0 ? null : formatTime(adapter.setHours(template, hour))
									})
								}, hour);
							})
						}), /*#__PURE__*/ (0, import_jsx_runtime.jsx)(DayTimeGridGrid, {
							className: classes.dayTimeGridGrid,
							role: "row",
							"aria-rowindex": 3,
							"data-has-scroll": hasScroll || void 0,
							children: occurrences.days.map((day, index) => /*#__PURE__*/ (0, import_jsx_runtime.jsx)(TimeGridColumn, {
								day,
								index,
								colIndex: index + 1,
								startTime,
								endTime,
								showCurrentTimeIndicator: showCurrentTimeIndicator && isTodayInView
							}, day.key))
						})]
					})
				})
			})
		]
	}));
});
DayTimeGrid.displayName = "DayTimeGrid";
//#endregion
//#region node_modules/@mui/x-scheduler/internals/utils/day-time-grid-view-definition.mjs
var DAYS_IN_WEEK = 7;
/**
* Number of consecutive days a day-time-grid based view can render. Restricted to the
* values the views actually use so the `=== 7` (week-aligned) branch below cannot be
* reached with an out-of-domain count (`0`, negative, or fractional) that would silently
* produce an empty or garbage day list.
*/
/**
* Builds an `EventCalendarViewDefinition` for a day-time-grid based view.
*
* When `dayCount === 7` the definition is week-aligned: it snaps the visible range to
* `startOfWeek` and honors the `showWeekends` and `weekStartsOn` preferences. For any
* other value, the definition shows `dayCount` consecutive days starting from the current
* visible date.
*/
function createDayTimeGridViewDefinition(dayCount) {
	if (dayCount === DAYS_IN_WEEK) return {
		siblingVisibleDateGetter: ({ state, delta }) => state.adapter.addWeeks(getStartOfWeek(state.adapter, schedulerOtherSelectors.visibleDate(state), eventCalendarPreferenceSelectors.weekStartsOn(state)), delta),
		visibleDaysSelector: createSelectorMemoized((state) => state.adapter, schedulerOtherSelectors.visibleDate, eventCalendarPreferenceSelectors.showWeekends, eventCalendarPreferenceSelectors.weekStartsOn, (adapter, visibleDate, showWeekends, weekStartsOn) => getDayList({
			adapter,
			start: getStartOfWeek(adapter, visibleDate, weekStartsOn),
			end: getEndOfWeek(adapter, visibleDate, weekStartsOn),
			excludeWeekends: !showWeekends
		}))
	};
	return {
		siblingVisibleDateGetter: ({ state, delta }) => state.adapter.addDays(schedulerOtherSelectors.visibleDate(state), delta * dayCount),
		visibleDaysSelector: createSelectorMemoized((state) => state.adapter, schedulerOtherSelectors.visibleDate, (adapter, visibleDate) => Array.from({ length: dayCount }, (_, index) => processDate(adapter.addDays(visibleDate, index), adapter)))
	};
}
//#endregion
//#region node_modules/@mui/x-scheduler/week-view/WeekView.mjs
var WEEK_VIEW_DEFINITION = createDayTimeGridViewDefinition(7);
/**
* A Week View to use inside the Event Calendar.
*
* Renders the desktop event variant, which `DayTimeGrid` resolves from the default value of
* `DayTimeGridInternalRenderersContext` — no provider is needed here.
*/
var WeekView = /*#__PURE__*/ import_react.memo(/*#__PURE__*/ import_react.forwardRef(function WeekView(props, forwardedRef) {
	const store = useEventCalendarStoreContext();
	const { days } = useEventCalendarView(WEEK_VIEW_DEFINITION);
	const config = useStore(store, eventCalendarViewSelectors.timeGridConfig, "week");
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(DayTimeGrid, _extends({
		ref: forwardedRef,
		days,
		startTime: config?.startTime,
		endTime: config?.endTime
	}, props));
}));
WeekView.displayName = "WeekView";
//#endregion
//#region node_modules/@mui/x-scheduler/week-view/StandaloneWeekView.mjs
/**
* A Week View that can be used outside of the Event Calendar.
*/
var StandaloneWeekView = /*#__PURE__*/ import_react.forwardRef(function StandaloneWeekView(props, forwardedRef) {
	const { parameters, forwardedProps } = useExtractEventCalendarParameters(props);
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ResponsiveTypographyContainer, { children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(EventCalendarProvider, _extends({}, parameters, { children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(EventDialogProvider, { children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(WeekView, _extends({ ref: forwardedRef }, forwardedProps)) }) })) });
});
StandaloneWeekView.displayName = "StandaloneWeekView";
StandaloneWeekView.propTypes = {
	/**
	* Whether the event can be dragged to change its start and end dates without changing the duration.
	* @default true
	*/
	areEventsDraggable: import_prop_types.default.bool,
	/**
	* Whether the event start or end can be dragged to change its duration without changing its other date.
	* If `true`, both start and end can be resized.
	* If `false`, the events are not resizable.
	* If `"start"`, only the start can be resized.
	* If `"end"`, only the end can be resized.
	* @default true
	*/
	areEventsResizable: import_prop_types.default.oneOfType([import_prop_types.default.oneOf(["end", "start"]), import_prop_types.default.bool]),
	/**
	* Whether events can be dragged from outside of the calendar and dropped into it.
	* @default false
	*/
	canDragEventsFromTheOutside: import_prop_types.default.bool,
	/**
	* Whether events can be dragged from inside of the calendar and dropped outside of it.
	* If true, when the mouse leaves the calendar, the event won't be rendered inside the calendar anymore.
	* If false, when the mouse leaves the calendar, the event will be rendered in its last valid position inside the calendar.
	* @default false
	*/
	canDropEventsToTheOutside: import_prop_types.default.bool,
	/**
	* Data source for fetching events asynchronously.
	* When provided, events are fetched through the data source instead of the `events` prop.
	*/
	dataSource: import_prop_types.default.shape({
		getEvents: import_prop_types.default.func.isRequired,
		persistEvents: import_prop_types.default.func.isRequired
	}),
	/**
	* The locale object from `date-fns` used to format dates.
	* This affects day names, month names, week start day, and other locale-dependent formatting.
	* Import a locale from `date-fns/locale` and pass it to this prop.
	* @default enUS (English)
	*/
	dateLocale: import_prop_types.default.object,
	/**
	* The default preferences for the calendar.
	* To use controlled preferences, use the `preferences` prop.
	* @default { showWeekends: true, showWeekNumber: false, isSidePanelOpen: true, showEmptyDaysInAgenda: true, ampm: true }
	*/
	defaultPreferences: import_prop_types.default.shape({
		ampm: import_prop_types.default.bool,
		isSidePanelOpen: import_prop_types.default.bool,
		showEmptyDaysInAgenda: import_prop_types.default.bool,
		showWeekends: import_prop_types.default.bool,
		showWeekNumber: import_prop_types.default.bool,
		weekStartsOn: import_prop_types.default.oneOf([
			0,
			1,
			2,
			3,
			4,
			5,
			6
		])
	}),
	/**
	* The view initially displayed in the calendar.
	* To render a controlled calendar, use the `view` prop.
	* @default "week"
	*/
	defaultView: import_prop_types.default.oneOf([
		"agenda",
		"day",
		"month",
		"week"
	]),
	/**
	* The date initially used to determine the visible date range.
	* To render a controlled component, use the `visibleDate` prop.
	* @default today
	*/
	defaultVisibleDate: import_prop_types.default.instanceOf(Date),
	/**
	* The IDs of the resources initially visible.
	* To render a controlled scheduler, use the `visibleResources` prop.
	* @default {} - all resources are visible
	*/
	defaultVisibleResources: import_prop_types.default.object,
	/**
	* The timezone used to display events in the scheduler.
	*
	* Accepts any valid IANA timezone name
	* (for example "America/New_York", "Europe/Paris", "Asia/Tokyo"),
	* or keywords understood by the adapter, such as
	* "default" (use the adapter's default timezone),
	* "locale" (use the user's current locale timezone),
	* or "UTC".
	*
	* This timezone only affects rendering, events keep their original data timezone.
	* @default "default"
	*/
	displayTimezone: import_prop_types.default.string,
	/**
	* The color palette used for all events.
	* Can be overridden per resource using the `eventColor` property on the resource model.
	* Can be overridden per event using the `color` property on the event model.
	* @default "teal"
	*/
	eventColor: import_prop_types.default.oneOf([
		"amber",
		"blue",
		"green",
		"grey",
		"indigo",
		"lime",
		"orange",
		"pink",
		"purple",
		"red",
		"teal"
	]),
	/**
	* Configures how events are created.
	* If `false`, event creation is disabled.
	* If `true`, event creation is enabled with default configuration.
	* If an object, event creation is enabled with the provided configuration.
	*/
	eventCreation: import_prop_types.default.oneOfType([import_prop_types.default.shape({
		duration: import_prop_types.default.number,
		interaction: import_prop_types.default.oneOf(["click", "double-click"])
	}), import_prop_types.default.bool]),
	/**
	* The structure of the event model.
	* It defines how to read and write the properties of the event model.
	* If not provided, the event model is assumed to match the `CalendarEvent` interface.
	*/
	eventModelStructure: import_prop_types.default.object,
	/**
	* The events currently available in the calendar.
	* @default []
	*/
	events: import_prop_types.default.arrayOf(import_prop_types.default.object),
	/**
	* Callback fired when some event of the calendar change.
	*/
	onEventsChange: import_prop_types.default.func,
	/**
	* Event handler called when the preferences change.
	*/
	onPreferencesChange: import_prop_types.default.func,
	/**
	* Event handler called when the view changes.
	*/
	onViewChange: import_prop_types.default.func,
	/**
	* Event handler called when the visible date changes.
	*/
	onVisibleDateChange: import_prop_types.default.func,
	/**
	* Event handler called when the visible resources change.
	*/
	onVisibleResourcesChange: import_prop_types.default.func,
	/**
	* Preferences currently displayed in the calendar.
	*/
	preferences: import_prop_types.default.shape({
		ampm: import_prop_types.default.bool,
		isSidePanelOpen: import_prop_types.default.bool,
		showEmptyDaysInAgenda: import_prop_types.default.bool,
		showWeekends: import_prop_types.default.bool,
		showWeekNumber: import_prop_types.default.bool,
		weekStartsOn: import_prop_types.default.oneOf([
			0,
			1,
			2,
			3,
			4,
			5,
			6
		])
	}),
	/**
	* Config of the preferences menu.
	* Defines which options are visible in the menu.
	* If `false`, the menu will be entirely hidden.
	* @default { toggleWeekendVisibility: true, toggleWeekNumberVisibility: true, toggleAmpm: true, toggleEmptyDaysInAgenda: true, toggleWeekStartsOn: false }
	*/
	preferencesMenuConfig: import_prop_types.default.oneOfType([import_prop_types.default.oneOf([false]), import_prop_types.default.shape({
		toggleAmpm: import_prop_types.default.bool,
		toggleEmptyDaysInAgenda: import_prop_types.default.bool,
		toggleWeekendVisibility: import_prop_types.default.bool,
		toggleWeekNumberVisibility: import_prop_types.default.bool,
		toggleWeekStartsOn: import_prop_types.default.bool
	})]),
	/**
	* Whether the calendar is in read-only mode.
	* @default false
	*/
	readOnly: import_prop_types.default.bool,
	/**
	* The structure of the resource model.
	* It defines how to read and write the properties of the resource model.
	* If not provided, the resource model is assumed to match the `CalendarResource` interface.
	*/
	resourceModelStructure: import_prop_types.default.object,
	/**
	* The resources the events can be assigned to.
	*/
	resources: import_prop_types.default.arrayOf(import_prop_types.default.object),
	/**
	* Whether each event must be assigned to a resource. When true, the resource cannot be cleared in the edit dialog and the form cannot be submitted without one.
	* @default false
	*/
	shouldEventRequireResource: import_prop_types.default.bool,
	/**
	* Whether the component should display the current time indicator.
	* @default true
	*/
	showCurrentTimeIndicator: import_prop_types.default.bool,
	/**
	* The system prop that allows defining system overrides as well as additional CSS styles.
	*/
	sx: import_prop_types.default.oneOfType([
		import_prop_types.default.arrayOf(import_prop_types.default.oneOfType([
			import_prop_types.default.func,
			import_prop_types.default.object,
			import_prop_types.default.bool
		])),
		import_prop_types.default.func,
		import_prop_types.default.object
	]),
	/**
	* The view currently displayed in the calendar.
	*/
	view: import_prop_types.default.oneOf([
		"agenda",
		"day",
		"month",
		"week"
	]),
	/**
	* Configuration applied to the view, keyed by the view name.
	* For the `week` view, `startTime` and `endTime` (whole hours between 0 and 24)
	* limit the hours displayed in the time grid.
	* @example { week: { startTime: 8, endTime: 20 } }
	*/
	viewConfig: import_prop_types.default.shape({ week: import_prop_types.default.shape({
		endTime: import_prop_types.default.number,
		startTime: import_prop_types.default.number
	}) }),
	/**
	* The views available in the calendar.
	* @default ["day", "week", "month", "agenda"]
	*/
	views: import_prop_types.default.arrayOf(import_prop_types.default.oneOf([
		"agenda",
		"day",
		"month",
		"week"
	]).isRequired),
	/**
	* The date currently used to determine the visible date range.
	*/
	visibleDate: import_prop_types.default.instanceOf(Date),
	/**
	* The IDs of the resources currently visible.
	* A resource is visible if it is not included in this object or if it is included with `true` value.
	*/
	visibleResources: import_prop_types.default.object
};
//#endregion
export { useEventCalendarStyledContext as $, responsiveTypographyContainerQueries as A, useEventCalendarView as At, useEventDialogContext as B, useStableCallback as Bt, useEventOccurrencesWithDayGridPosition as C, schedulerEventSelectors as Ct, EVENT_CALENDAR_ROOT_CONTAINER_NAME as D, disposeSymbol as Dt, EventCalendarProvider as E, DisposableStack as Et, useExtractEventCalendarParameters as F, useMergedRefs as Ft, SharedComponentsStyledContext as G, EventDialogStyledContext as H, useRefWithInit as Ht, useEventCalendar as I, useOnMount as It, LEFT_ARROW_CLIP as J, useSharedComponentsStyledContext as K, useDisposable as L, EMPTY_ARRAY$1 as Lt, eventCalendarClasses as M, useAdapterContext as Mt, getEventCalendarUtilityClass as N, SchedulerStoreContext as Nt, eventCalendarRootCompactQuery as O, unwrapSuppressedErrors as Ot, useEventCalendarUtilityClasses as P, useSchedulerStoreContext as Pt, EventCalendarStyledContext as Q, EventDialogProvider as R, EMPTY_OBJECT as Rt, useRenderElement as S, schedulerOtherSelectors as St, ResponsiveTypographyContainer as T, schedulerResourceSelectors as Tt, createModal as U, createSelectorMemoized as Ut, Check_default as V, useStore as Vt, EventSkeleton as W, getCellFocusBackground as X, RIGHT_ARROW_CLIP as Y, getPaletteVariants as Z, CalendarGridDayCell as _, eventCalendarPreferenceSelectors as _t, DayGridEvent as a, useButton$1 as at, CalendarGridHeaderRow as b, getWeekNumber as bt, TimeGridEventPlaceholder as c, useDragPreview as ct, useTimeGridEvent as d, getDayList as dt, useFormatTime as et, EventDragPreview as f, isWeekend as ft, useButton as g, eventCalendarViewSelectors as gt, CalendarGridTimeEvent as h, eventCalendarOccurrencePlaceholderSelectors as ht, DayTimeGrid as i, useRenderElement$1 as it, EVENT_CALENDAR_DEFAULT_LOCALE_TEXT as j, useEventCalendarStoreContext as jt, eventCalendarRootExpandedQuery as k, sortEventOccurrences as kt, getTimeGridEventRootStyles as l, disableNativeDragPreview as lt, CalendarGridTimeEventResizeHandler as m, useEventOccurrencesGroupedByDay as mt, WeekView as n, formatMonthFullLetterAndYear as nt, isOccurrenceAllDayOrMultipleDay as o, RepeatRounded_default as ot, useCalendarGridPlaceholderInDay as p, innerGetEventOccurrencesGroupedByDay as pt, BOTH_ARROWS_CLIP as q, createDayTimeGridViewDefinition as r, formatWeekDayMonthAndDayOfMonth as rt, DayTimeGridInternalRenderersContext as s, useId as st, StandaloneWeekView as t, formatMonthAndDayOfMonth as tt, linesClampStyles as u, draggable as ut, CalendarGridDayRow as v, getEndOfWeek as vt, useResizeObserver as w, getPrimaryResourceId as wt, CalendarGridRoot as x, schedulerNowSelectors as xt, CalendarGridHeaderCell as y, getStartOfWeek as yt, EventDialogTrigger as z, useIsoLayoutEffect as zt };

//# sourceMappingURL=week-view-DzUc-2pO.js.map