import { r as __toESM } from "./chunk-B-1-B7_t.js";
import { t as require_react } from "./react.js";
import { t as require_jsx_runtime } from "./react_jsx-runtime.js";
import { $ as generateUtilityClass, Ct as composeClasses, N as useRtl, Q as shouldForwardProp, t as styled, wt as require_prop_types } from "./styled-CNP9jUFH.js";
import { t as clsx } from "./clsx-DQoRUQAf.js";
import { t as _extends } from "./extends-DrH2PCIy.js";
import { D as checkboxClasses, f as useThemeProps$2 } from "./styles-BeNPniuI.js";
import { t as generateUtilityClasses } from "./generateUtilityClasses-BLzP2xdp.js";
import { t as createSvgIcon } from "./createSvgIcon-CeHHFvOi.js";
import Menu_default from "./@mui_icons-material_Menu.js";
import { n as extractEventHandlers, r as resolveComponentProps } from "./mergeSlotProps-CVk7QiMt.js";
import { a as unsupportedProp } from "./utils-Cq148YLK.js";
import { t as Collapse } from "./Collapse-CfPlTiIV.js";
import { t as Alert } from "./Alert-UoOgs5ag.js";
import { t as CircularProgress } from "./CircularProgress-BBMG5rst.js";
import { t as IconButton } from "./IconButton-A5Pl-d0P.js";
import { t as Typography } from "./Typography-DYKLC8_d.js";
import { t as useSlotProps } from "./useSlotProps-on91qybo.js";
import { x as ListSubheader, y as Divider } from "./Tabs-OtxUtgYr.js";
import { t as Button$1 } from "./Button-CH_Ul6xi.js";
import { t as Checkbox } from "./Checkbox-BUt3ZH2H.js";
import { t as MenuItem } from "./MenuItem-DH6O5v0p.js";
import { t as Drawer } from "./Drawer-ZIarVvss.js";
import { t as ListItemButton } from "./ListItemButton-SoRwtIoy.js";
import { t as ListItemIcon } from "./ListItemIcon-DbXSSp_K.js";
import { t as ListItemText } from "./ListItemText-DrL2R2f5.js";
import { d as MenuList, i as Popover, t as Menu } from "./Menu-BVvtch31.js";
import { a as createSelector, n as Store, o as createSelectorMemoized, r as useStore, t as fastObjectShallowCompare } from "./fastObjectShallowCompare-BusicMYR.js";
import { t as _objectWithoutPropertiesLoose } from "./objectWithoutPropertiesLoose-BwcsosU7.js";
import { t as warnOnce } from "./warning-ci_nFDcC.js";
import { t as EventManager } from "./EventManager-B_87hSpQ.js";
import { $ as useEventCalendarStyledContext, A as responsiveTypographyContainerQueries, At as useEventCalendarView, B as useEventDialogContext, Bt as useStableCallback, C as useEventOccurrencesWithDayGridPosition, Ct as schedulerEventSelectors, D as EVENT_CALENDAR_ROOT_CONTAINER_NAME, Dt as disposeSymbol, E as EventCalendarProvider, Et as DisposableStack, F as useExtractEventCalendarParameters, Ft as useMergedRefs, G as SharedComponentsStyledContext, H as EventDialogStyledContext, Ht as useRefWithInit, I as useEventCalendar, It as useOnMount, J as LEFT_ARROW_CLIP, K as useSharedComponentsStyledContext, L as useDisposable, Lt as EMPTY_ARRAY, M as eventCalendarClasses, Mt as useAdapterContext, N as getEventCalendarUtilityClass, Nt as SchedulerStoreContext, O as eventCalendarRootCompactQuery, Ot as unwrapSuppressedErrors, P as useEventCalendarUtilityClasses, Pt as useSchedulerStoreContext, Q as EventCalendarStyledContext, R as EventDialogProvider, Rt as EMPTY_OBJECT, S as useRenderElement$1, St as schedulerOtherSelectors, T as ResponsiveTypographyContainer, Tt as schedulerResourceSelectors, U as createModal, Ut as createSelectorMemoized$1, V as Check_default, Vt as useStore$1, W as EventSkeleton, X as getCellFocusBackground, Y as RIGHT_ARROW_CLIP, Z as getPaletteVariants, _ as CalendarGridDayCell, _t as eventCalendarPreferenceSelectors, a as DayGridEvent, at as useButton, b as CalendarGridHeaderRow, bt as getWeekNumber, c as TimeGridEventPlaceholder, ct as useDragPreview, d as useTimeGridEvent, dt as getDayList, et as useFormatTime, f as EventDragPreview, ft as isWeekend, g as useButton$1, gt as eventCalendarViewSelectors, h as CalendarGridTimeEvent, ht as eventCalendarOccurrencePlaceholderSelectors, i as DayTimeGrid, it as useRenderElement, j as EVENT_CALENDAR_DEFAULT_LOCALE_TEXT, jt as useEventCalendarStoreContext, k as eventCalendarRootExpandedQuery, kt as sortEventOccurrences, l as getTimeGridEventRootStyles, lt as disableNativeDragPreview, m as CalendarGridTimeEventResizeHandler, mt as useEventOccurrencesGroupedByDay, n as WeekView, nt as formatMonthFullLetterAndYear, o as isOccurrenceAllDayOrMultipleDay, ot as RepeatRounded_default, p as useCalendarGridPlaceholderInDay, pt as innerGetEventOccurrencesGroupedByDay, q as BOTH_ARROWS_CLIP, r as createDayTimeGridViewDefinition, rt as formatWeekDayMonthAndDayOfMonth, s as DayTimeGridInternalRenderersContext, st as useId, t as StandaloneWeekView, tt as formatMonthAndDayOfMonth, u as linesClampStyles, ut as draggable, v as CalendarGridDayRow, vt as getEndOfWeek, w as useResizeObserver, wt as getPrimaryResourceId, x as CalendarGridRoot, xt as schedulerNowSelectors, y as CalendarGridHeaderCell, yt as getStartOfWeek, z as EventDialogTrigger, zt as useIsoLayoutEffect } from "./week-view-DzUc-2pO.js";
//#region node_modules/@mui/x-scheduler-internals/event-calendar-selectors/eventCalendarAgendaSelectors.mjs
var eventCalendarAgendaSelectors = { visibleDays: createSelectorMemoized$1((state) => state.adapter, schedulerOtherSelectors.visibleDate, schedulerOtherSelectors.displayTimezone, eventCalendarPreferenceSelectors.showWeekends, eventCalendarPreferenceSelectors.showEmptyDaysInAgenda, schedulerEventSelectors.processedEventList, schedulerResourceSelectors.visibleMap, schedulerOtherSelectors.recurringEventsPlugin, (adapter, visibleDate, displayTimezone, showWeekends, showEmptyDaysInAgenda, events, visibleResources, recurringEventsPlugin) => {
	const amount = 12;
	let accumulatedDays = getDayList({
		adapter,
		start: visibleDate,
		end: adapter.addDays(visibleDate, amount - 1),
		excludeWeekends: !showWeekends
	});
	let occurrenceMap = innerGetEventOccurrencesGroupedByDay({
		adapter,
		days: accumulatedDays,
		events,
		visibleResources,
		displayTimezone,
		recurringEventsPlugin
	});
	const hasEvents = (day) => (occurrenceMap.get(day.key)?.length ?? 0) > 0;
	if (showEmptyDaysInAgenda) return accumulatedDays;
	let daysWithEvents = accumulatedDays.filter(hasEvents).slice(0, amount);
	while (daysWithEvents.length < amount) {
		const first = accumulatedDays[0]?.value;
		const last = accumulatedDays[accumulatedDays.length - 1]?.value;
		if (first && last) {
			if (adapter.differenceInDays(adapter.startOfDay(last), adapter.startOfDay(first)) + 1 >= 180) break;
		}
		const nextStart = adapter.addDays(last ?? visibleDate, 1);
		const more = getDayList({
			adapter,
			start: nextStart,
			end: adapter.addDays(nextStart, amount),
			excludeWeekends: !showWeekends
		});
		accumulatedDays = accumulatedDays.concat(more);
		occurrenceMap = innerGetEventOccurrencesGroupedByDay({
			adapter,
			days: accumulatedDays,
			events,
			visibleResources,
			displayTimezone,
			recurringEventsPlugin
		});
		daysWithEvents = accumulatedDays.filter(hasEvents).slice(0, amount);
	}
	return daysWithEvents;
}) };
//#endregion
//#region node_modules/@mui/x-scheduler-internals/internals/utils/useInitializeApiRef.mjs
function initializeInputApiRef$1(publicAPI, apiRef) {
	if (apiRef != null && apiRef.current == null) apiRef.current = publicAPI;
}
function useInitializeApiRef(store, apiRef) {
	const publicAPI = useRefWithInit(() => store.buildPublicAPI()).current;
	initializeInputApiRef$1(publicAPI, apiRef);
}
//#endregion
//#region node_modules/@base-ui/react/button/Button.mjs
var import_react = /* @__PURE__ */ __toESM(require_react(), 1);
/**
* A button component that can be used to trigger actions.
* Renders a `<button>` element.
*
* Documentation: [Base UI Button](https://base-ui.com/react/components/button)
*/
var Button = /*#__PURE__*/ import_react.forwardRef(function Button(componentProps, forwardedRef) {
	const { render, className, disabled = false, focusableWhenDisabled = false, nativeButton = true, style, ...elementProps } = componentProps;
	const { getButtonProps, buttonRef } = useButton({
		disabled,
		focusableWhenDisabled,
		native: nativeButton
	});
	return useRenderElement("button", componentProps, {
		state: { disabled },
		ref: [forwardedRef, buttonRef],
		props: [elementProps, getButtonProps]
	});
});
Button.displayName = "Button";
//#endregion
//#region node_modules/@mui/x-scheduler/internals/components/event/event-item/EventItem.mjs
var import_jsx_runtime = require_jsx_runtime();
var _excluded$14 = [
	"occurrence",
	"date",
	"ariaLabelledBy",
	"id",
	"variant",
	"className",
	"onClick"
];
var EventItemCard = styled("div", {
	name: "MuiEventCalendar",
	slot: "EventItemCard"
})(({ theme }) => ({
	padding: 0,
	borderRadius: theme.shape.borderRadius,
	"&:hover": { backgroundColor: (theme.vars || theme).palette.action.hover },
	"&:focus-visible": {
		outline: "2px solid var(--event-surface-accent)",
		outlineOffset: 1
	},
	"&[data-variant=\"compact\"], &[data-variant=\"regular\"]": {
		containerType: "inline-size",
		cursor: "pointer",
		height: "fit-content"
	},
	"&[data-variant=\"filled\"]": {
		backgroundColor: "var(--event-surface-bold)",
		color: "var(--event-on-surface-bold)",
		"&:hover": { backgroundColor: "var(--event-surface-bold-hover)" },
		"&[data-editing]": {
			backgroundColor: "var(--event-surface-selected)",
			color: "var(--event-on-surface-selected)",
			"&:hover": { backgroundColor: "var(--event-surface-selected-hover)" }
		},
		"&[data-starting-before-edge]": {
			borderTopLeftRadius: 0,
			borderBottomLeftRadius: 0,
			clipPath: LEFT_ARROW_CLIP,
			paddingLeft: 8
		},
		"&[data-ending-after-edge]": {
			borderTopRightRadius: 0,
			borderBottomRightRadius: 0,
			clipPath: RIGHT_ARROW_CLIP,
			paddingRight: 8
		},
		"&[data-starting-before-edge][data-ending-after-edge]": { clipPath: BOTH_ARROWS_CLIP }
	},
	"&[data-variant=\"regular\"]": { cursor: "pointer" },
	"&[data-editing]": {
		backgroundColor: "var(--event-surface-selected)",
		"&:hover": { backgroundColor: "var(--event-surface-selected-hover)" }
	},
	variants: getPaletteVariants(theme)
}));
var EventItemCardWrapper = styled("div", {
	name: "MuiEventCalendar",
	slot: "EventItemCardWrapper"
})(({ theme }) => ({
	display: "flex",
	alignItems: "center",
	gap: theme.spacing(1),
	padding: theme.spacing(.5, 1),
	"&[data-variant=\"compact\"], &[data-variant=\"filled\"]": { padding: `0 ${theme.spacing(.5)}` }
}));
var EventItemTitle = styled("span", {
	name: "MuiEventCalendar",
	slot: "EventItemTitle"
})(({ theme }) => ({
	margin: 0,
	color: (theme.vars || theme).palette.text.primary,
	fontWeight: theme.typography.fontWeightMedium,
	fontSize: "var(--EventCalendar-fontSize-eventTitle, 0.75rem)",
	lineHeight: 1.43,
	"[data-editing] &": { color: "var(--event-on-surface-selected)" }
}));
var EventItemTime = styled("time", {
	name: "MuiEventCalendar",
	slot: "EventItemTime"
})(({ theme }) => ({
	display: "inline-block",
	color: (theme.vars || theme).palette.text.secondary,
	fontWeight: theme.typography.fontWeightRegular,
	fontSize: theme.typography.caption.fontSize,
	lineHeight: 1.43,
	whiteSpace: "nowrap",
	width: 150,
	"&[data-compact]": {
		width: "fit-content",
		marginInlineEnd: theme.spacing(.5)
	},
	"[data-editing] &": { color: "var(--event-on-surface-selected)" }
}));
var EventItemRecurringIcon = styled(RepeatRounded_default, {
	name: "MuiEventCalendar",
	slot: "EventItemRecurringIcon"
})(({ theme }) => ({
	color: (theme.vars || theme).palette.text.primary,
	"[data-editing] &": { color: "var(--event-on-surface-selected)" }
}));
var ResourceLegendColor = styled("span", {
	name: "MuiEventCalendar",
	slot: "ResourceLegendColor"
})({
	width: 8,
	height: 8,
	borderRadius: "50%",
	flexShrink: 0,
	backgroundColor: "var(--event-main)",
	"[data-editing] &": { backgroundColor: "var(--event-on-surface-selected)" }
});
var EventItemCardContent = styled("p", {
	name: "MuiEventCalendar",
	slot: "EventItemCardContent"
})({
	margin: 0,
	height: 20,
	lineHeight: "20px"
});
var EventItemLinesClamp = styled("span", {
	name: "MuiEventCalendar",
	slot: "EventItemLinesClamp"
})({
	display: "-webkit-box",
	WebkitLineClamp: "var(--number-of-lines)",
	WebkitBoxOrient: "vertical",
	overflow: "hidden",
	textOverflow: "ellipsis",
	wordBreak: "break-word",
	overflowWrap: "break-word"
});
/**
* Component used to display an event occurrence, without any positioning capabilities
* Used in <AgendaView /> and in the event popover of <MonthView /> to display the list of events for a specific day.
*/
var EventItem = /*#__PURE__*/ import_react.forwardRef(function EventItem(props, forwardedRef) {
	const { occurrence, date, ariaLabelledBy, id: idProp, variant = "regular", className, onClick } = props, other = _objectWithoutPropertiesLoose(props, _excluded$14);
	const { classes, localeText } = useEventCalendarStyledContext();
	const store = useEventCalendarStoreContext();
	const isEditing = useStore$1(store, schedulerOtherSelectors.isEditedOccurrence, occurrence.key);
	const id = useId(idProp);
	const resource = useStore$1(store, schedulerResourceSelectors.processedResource, getPrimaryResourceId(occurrence.resource));
	const color = useStore$1(store, schedulerEventSelectors.color, occurrence.id);
	const isRecurring = useStore$1(store, schedulerEventSelectors.isRecurring, occurrence.id);
	const formatTime = useFormatTime();
	const adapter = useAdapterContext();
	const startsBeforeDay = !adapter.isSameDay(occurrence.displayTimezone.start.value, date.value) && adapter.isBefore(occurrence.displayTimezone.start.value, date.value);
	const endsAfterDay = !adapter.isSameDay(occurrence.displayTimezone.end.value, date.value) && adapter.isAfter(occurrence.displayTimezone.end.value, date.value);
	const content = import_react.useMemo(() => {
		switch (variant) {
			case "compact": return /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(import_react.Fragment, { children: [
				/*#__PURE__*/ (0, import_jsx_runtime.jsx)(ResourceLegendColor, {
					className: classes.resourceLegendColor,
					role: "img",
					"aria-label": resource?.title ? localeText.resourceAriaLabel(resource.title) : localeText.noResourceAriaLabel
				}),
				/*#__PURE__*/ (0, import_jsx_runtime.jsx)(EventItemLinesClamp, {
					className: classes.eventItemLinesClamp,
					style: { "--number-of-lines": 1 },
					children: /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(EventItemCardContent, {
						className: classes.eventItemCardContent,
						children: [/*#__PURE__*/ (0, import_jsx_runtime.jsx)(EventItemTime, {
							className: classes.eventItemTime,
							"data-compact": true,
							children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)("span", { children: formatTime(occurrence.displayTimezone.start.value) })
						}), /*#__PURE__*/ (0, import_jsx_runtime.jsx)(EventItemTitle, {
							className: classes.eventItemTitle,
							children: occurrence.title
						})]
					})
				}),
				isRecurring && /*#__PURE__*/ (0, import_jsx_runtime.jsx)(EventItemRecurringIcon, {
					className: classes.eventItemRecurringIcon,
					"aria-hidden": "true",
					fontSize: "small"
				})
			] });
			case "filled": return /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(import_react.Fragment, { children: [/*#__PURE__*/ (0, import_jsx_runtime.jsx)(EventItemLinesClamp, {
				className: classes.eventItemLinesClamp,
				style: { "--number-of-lines": 1 },
				children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(EventItemTitle, {
					className: classes.eventItemTitle,
					children: occurrence.title
				})
			}), isRecurring && /*#__PURE__*/ (0, import_jsx_runtime.jsx)(EventItemRecurringIcon, {
				className: classes.eventItemRecurringIcon,
				"aria-hidden": "true",
				fontSize: "small"
			})] });
			case "regular": return /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(import_react.Fragment, { children: [
				/*#__PURE__*/ (0, import_jsx_runtime.jsx)(ResourceLegendColor, {
					className: classes.resourceLegendColor,
					role: "img",
					"aria-label": resource?.title ? localeText.resourceAriaLabel(resource.title) : localeText.noResourceAriaLabel
				}),
				/*#__PURE__*/ (0, import_jsx_runtime.jsx)(EventItemLinesClamp, {
					className: classes.eventItemLinesClamp,
					style: { "--number-of-lines": 1 },
					children: /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(EventItemCardContent, {
						className: classes.eventItemCardContent,
						children: [/*#__PURE__*/ (0, import_jsx_runtime.jsx)(MultiDayDateLabel, {
							occurrence,
							formatTime
						}), /*#__PURE__*/ (0, import_jsx_runtime.jsx)(EventItemTitle, {
							className: classes.eventItemTitle,
							children: occurrence.title
						})]
					})
				}),
				isRecurring && /*#__PURE__*/ (0, import_jsx_runtime.jsx)(EventItemRecurringIcon, {
					className: classes.eventItemRecurringIcon,
					"aria-hidden": "true",
					fontSize: "small"
				})
			] });
			default: throw new Error("MUI X Scheduler: Unsupported variant provided to EventItem component. The EventItem component only supports specific variant values. Check the component documentation for supported variants.");
		}
	}, [
		variant,
		resource?.title,
		localeText,
		formatTime,
		occurrence,
		isRecurring,
		classes
	]);
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(Button, {
		nativeButton: false,
		onClick,
		render: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(EventItemCard, _extends({
			ref: forwardedRef,
			id,
			"data-variant": variant,
			"data-palette": color,
			"data-editing": isEditing || void 0,
			"aria-labelledby": `${ariaLabelledBy} ${id}`
		}, startsBeforeDay ? { "data-starting-before-edge": "" } : {}, endsAfterDay ? { "data-ending-after-edge": "" } : {}, other, { className: clsx(className, classes.eventItemCard, occurrence.className) })),
		children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(EventItemCardWrapper, {
			className: classes.eventItemCardWrapper,
			"data-variant": variant,
			children: content
		})
	});
});
EventItem.displayName = "EventItem";
function MultiDayDateLabel(props) {
	const { occurrence, formatTime } = props;
	const adapter = useAdapterContext();
	const { classes, localeText } = useEventCalendarStyledContext();
	if (!adapter.isSameDay(occurrence.displayTimezone.start.value, occurrence.displayTimezone.end.value)) {
		const format = `${adapter.formats.dayOfMonth} ${adapter.formats.month3Letters}`;
		return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(EventItemTime, {
			className: classes.eventItemTime,
			as: "span",
			children: localeText.eventItemMultiDayLabel(adapter.formatByString(occurrence.displayTimezone.end.value, format))
		});
	}
	if (occurrence.allDay) return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(EventItemTime, {
		className: classes.eventItemTime,
		as: "span",
		children: localeText.allDay
	});
	return /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(EventItemTime, {
		className: classes.eventItemTime,
		children: [/*#__PURE__*/ (0, import_jsx_runtime.jsx)("span", { children: formatTime(occurrence.displayTimezone.start.value) }), /*#__PURE__*/ (0, import_jsx_runtime.jsxs)("span", { children: [" - ", formatTime(occurrence.displayTimezone.end.value)] })]
	});
}
//#endregion
//#region node_modules/@mui/x-scheduler/agenda-view/AgendaView.mjs
var AgendaViewRoot = styled("div", {
	name: "MuiEventCalendar",
	slot: "AgendaView"
})(({ theme }) => ({
	width: "100%",
	maxHeight: "100%",
	border: `1px solid ${(theme.vars || theme).palette.divider}`,
	borderRadius: theme.shape.borderRadius,
	overflowY: "auto",
	position: "relative"
}));
var AgendaViewRow = styled("section", {
	name: "MuiEventCalendar",
	slot: "AgendaViewRow"
})(({ theme }) => ({
	display: "grid",
	gridTemplateColumns: "190px 1fr",
	"&:not(:last-child)": { borderBottom: `1px solid ${(theme.vars || theme).palette.divider}` }
}));
var DayHeaderCell = styled("header", {
	name: "MuiEventCalendar",
	slot: "AgendaViewDayHeaderCell"
})(({ theme }) => ({
	display: "flex",
	alignItems: "flex-start",
	borderRight: `1px solid ${(theme.vars || theme).palette.divider}`,
	padding: theme.spacing(2),
	gap: theme.spacing(.5),
	"&[data-current]": {
		backgroundColor: theme.alpha((theme.vars || theme).palette.primary.light, .05),
		color: (theme.vars || theme).palette.primary.main
	}
}));
var DayNumberCell = styled("span", {
	name: "MuiEventCalendar",
	slot: "AgendaViewDayNumberCell"
})(({ theme }) => ({
	fontSize: "var(--EventCalendar-fontSize-agendaDayNumber, 1.5rem)",
	fontWeight: theme.typography.fontWeightMedium,
	lineHeight: 1,
	minWidth: "4ch",
	textAlign: "center",
	color: (theme.vars || theme).palette.text.primary,
	"&[data-current]": { color: (theme.vars || theme).palette.primary.main }
}));
var WeekDayCell = styled("div", {
	name: "MuiEventCalendar",
	slot: "AgendaViewWeekDayCell"
})(({ theme }) => ({
	display: "flex",
	flexDirection: "column",
	gap: theme.spacing(1)
}));
var AgendaWeekDayNameLabel = styled("span", {
	name: "MuiEventCalendar",
	slot: "AgendaViewWeekDayNameLabel"
})(({ theme }) => ({
	fontSize: theme.typography.body2.fontSize,
	lineHeight: 1,
	display: "-webkit-box",
	WebkitLineClamp: "var(--number-of-lines)",
	WebkitBoxOrient: "vertical",
	overflow: "hidden",
	textOverflow: "ellipsis",
	wordBreak: "break-word",
	overflowWrap: "break-word"
}));
var AgendaYearAndMonthLabel = styled("span", {
	name: "MuiEventCalendar",
	slot: "AgendaViewYearAndMonthLabel"
})(({ theme }) => ({
	fontSize: theme.typography.caption.fontSize,
	lineHeight: 1,
	color: (theme.vars || theme).palette.text.secondary,
	display: "-webkit-box",
	WebkitLineClamp: "var(--number-of-lines)",
	WebkitBoxOrient: "vertical",
	overflow: "hidden",
	textOverflow: "ellipsis",
	wordBreak: "break-word",
	overflowWrap: "break-word",
	"[data-current] &": { color: "inherit" }
}));
var EventsList = styled("ul", {
	name: "MuiEventCalendar",
	slot: "AgendaViewEventsList"
})(({ theme }) => ({
	display: "flex",
	flexDirection: "column",
	padding: theme.spacing(1),
	margin: 0,
	listStyle: "none",
	flexGrow: 1
}));
var AgendaViewWeekNumberLabel = styled("span", {
	name: "MuiEventCalendar",
	slot: "AgendaViewWeekNumberLabel"
})(({ theme }) => ({
	fontSize: "0.875rem",
	fontWeight: theme.typography.fontWeightRegular,
	lineHeight: 1,
	color: (theme.vars || theme).palette.text.primary
}));
var AgendaViewWeekNumberRow = styled("div", {
	name: "MuiEventCalendar",
	slot: "AgendaViewWeekNumberRow"
})(({ theme }) => _extends({
	padding: theme.spacing(.5, 2),
	backgroundColor: (theme.vars || theme).palette.grey[200],
	borderBottom: `1px solid ${(theme.vars || theme).palette.divider}`
}, theme.applyStyles("dark", { backgroundColor: (theme.vars || theme).palette.grey[900] })));
var AGENDA_VIEW_DEFINITION = {
	siblingVisibleDateGetter: ({ state, delta }) => state.adapter.addDays(schedulerOtherSelectors.visibleDate(state), 12 * delta),
	visibleDaysSelector: eventCalendarAgendaSelectors.visibleDays
};
/**
* An Agenda View to use inside the Event Calendar.
*/
var AgendaView = /*#__PURE__*/ import_react.memo(/*#__PURE__*/ import_react.forwardRef(function AgendaView(props, forwardedRef) {
	const adapter = useAdapterContext();
	const { schedulerId, classes, localeText } = useEventCalendarStyledContext();
	const store = useEventCalendarStoreContext();
	const handleRef = useMergedRefs(forwardedRef, import_react.useRef(null));
	const now = useStore$1(store, schedulerNowSelectors.nowUpdatedEveryMinute);
	const showWeekNumber = useStore$1(store, eventCalendarPreferenceSelectors.showWeekNumber);
	const weekStartsOn = useStore$1(store, eventCalendarPreferenceSelectors.weekStartsOn);
	const { days } = useEventCalendarView(AGENDA_VIEW_DEFINITION);
	const occurrencesMap = useEventOccurrencesGroupedByDay({ days });
	const isLoading = useStore$1(store, schedulerOtherSelectors.isLoading);
	const daysWithOccurrences = import_react.useMemo(() => days.map((date, index) => {
		return {
			date,
			occurrences: sortEventOccurrences(occurrencesMap.get(date.key) || []),
			weekNumber: getWeekNumber(adapter, date.value, weekStartsOn),
			isFirstDayOfWeek: index === 0 || !adapter.isSameDay(getStartOfWeek(adapter, date.value, weekStartsOn), getStartOfWeek(adapter, days[index - 1].value, weekStartsOn))
		};
	}), [
		adapter,
		days,
		occurrencesMap,
		weekStartsOn
	]);
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(AgendaViewRoot, _extends({}, props, {
		ref: handleRef,
		className: clsx(props.className, classes.agendaView),
		children: daysWithOccurrences.map(({ date, occurrences, isFirstDayOfWeek, weekNumber }) => /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(import_react.Fragment, { children: [showWeekNumber && isFirstDayOfWeek && /*#__PURE__*/ (0, import_jsx_runtime.jsx)(AgendaViewWeekNumberRow, {
			className: classes.agendaViewWeekNumberRow,
			children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(AgendaViewWeekNumberLabel, {
				className: classes.agendaViewWeekNumberLabel,
				"aria-label": localeText.weekNumberAriaLabel(weekNumber),
				children: `${localeText.week} ${weekNumber}`
			})
		}), /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(AgendaViewRow, {
			className: classes.agendaViewRow,
			id: `${schedulerId}-AgendaViewRow-${date.key}`,
			"aria-labelledby": `${schedulerId}-DayHeaderCell-${date.key}`,
			children: [/*#__PURE__*/ (0, import_jsx_runtime.jsxs)(DayHeaderCell, {
				className: classes.agendaViewDayHeaderCell,
				id: `${schedulerId}-DayHeaderCell-${date.key}`,
				"aria-label": `${adapter.format(date.value, "weekday")} ${adapter.format(date.value, "dayOfMonth")}`,
				"data-current": adapter.isSameDay(date.value, now) ? "" : void 0,
				children: [/*#__PURE__*/ (0, import_jsx_runtime.jsx)(DayNumberCell, {
					className: classes.agendaViewDayNumberCell,
					"data-current": adapter.isSameDay(date.value, now) ? "" : void 0,
					children: adapter.format(date.value, "dayOfMonth")
				}), /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(WeekDayCell, {
					className: classes.agendaViewWeekDayCell,
					children: [/*#__PURE__*/ (0, import_jsx_runtime.jsx)(AgendaWeekDayNameLabel, {
						className: classes.agendaViewWeekDayNameLabel,
						style: { "--number-of-lines": 1 },
						children: adapter.format(date.value, "weekday")
					}), /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(AgendaYearAndMonthLabel, {
						className: classes.agendaViewYearAndMonthLabel,
						style: { "--number-of-lines": 1 },
						children: [
							adapter.format(date.value, "monthFullLetter"),
							",",
							" ",
							adapter.format(date.value, "yearPadded")
						]
					})]
				})]
			}), /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(EventsList, {
				className: classes.agendaViewEventsList,
				children: [isLoading && /*#__PURE__*/ (0, import_jsx_runtime.jsx)("li", {
					className: classes.agendaViewEventListItem,
					children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(EventSkeleton, { "data-variant": "agenda" })
				}), !isLoading && occurrences.map((occurrence) => /*#__PURE__*/ (0, import_jsx_runtime.jsx)("li", {
					className: classes.agendaViewEventListItem,
					children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(EventDialogTrigger, {
						occurrence,
						children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(EventItem, {
							occurrence,
							date,
							variant: "regular",
							ariaLabelledBy: `${schedulerId}-DayHeaderCell-${date.key}`
						})
					})
				}, occurrence.key))]
			})]
		})] }, date.key))
	}));
}));
AgendaView.displayName = "AgendaView";
//#endregion
//#region node_modules/@mui/x-scheduler/agenda-view/StandaloneAgendaView.mjs
/**
* An Agenda View that can be used outside of the Event Calendar.
*/
var import_prop_types = /* @__PURE__ */ __toESM(require_prop_types(), 1);
var StandaloneAgendaView = /*#__PURE__*/ import_react.forwardRef(function StandaloneAgendaView(props, forwardedRef) {
	const { parameters, forwardedProps } = useExtractEventCalendarParameters(props);
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ResponsiveTypographyContainer, { children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(EventCalendarProvider, _extends({}, parameters, { children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(EventDialogProvider, { children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(AgendaView, _extends({ ref: forwardedRef }, forwardedProps)) }) })) });
});
StandaloneAgendaView.displayName = "StandaloneAgendaView";
StandaloneAgendaView.propTypes = {
	/**
	* Whether the event can be dragged to change its start and end dates without changing the duration.
	* @default true
	*/
	areEventsDraggable: import_prop_types.default.bool,
	/**
	* Whether the event start or end can be dragged to change its duration without changing its other date.
	* If `true`, both start and end can be resized.
	* If `false`, the events are not resizable.
	* If `"start"`, only the start can be resized.
	* If `"end"`, only the end can be resized.
	* @default true
	*/
	areEventsResizable: import_prop_types.default.oneOfType([import_prop_types.default.oneOf(["end", "start"]), import_prop_types.default.bool]),
	/**
	* Whether events can be dragged from outside of the calendar and dropped into it.
	* @default false
	*/
	canDragEventsFromTheOutside: import_prop_types.default.bool,
	/**
	* Whether events can be dragged from inside of the calendar and dropped outside of it.
	* If true, when the mouse leaves the calendar, the event won't be rendered inside the calendar anymore.
	* If false, when the mouse leaves the calendar, the event will be rendered in its last valid position inside the calendar.
	* @default false
	*/
	canDropEventsToTheOutside: import_prop_types.default.bool,
	/**
	* Data source for fetching events asynchronously.
	* When provided, events are fetched through the data source instead of the `events` prop.
	*/
	dataSource: import_prop_types.default.shape({
		getEvents: import_prop_types.default.func.isRequired,
		persistEvents: import_prop_types.default.func.isRequired
	}),
	/**
	* The locale object from `date-fns` used to format dates.
	* This affects day names, month names, week start day, and other locale-dependent formatting.
	* Import a locale from `date-fns/locale` and pass it to this prop.
	* @default enUS (English)
	*/
	dateLocale: import_prop_types.default.object,
	/**
	* The default preferences for the calendar.
	* To use controlled preferences, use the `preferences` prop.
	* @default { showWeekends: true, showWeekNumber: false, isSidePanelOpen: true, showEmptyDaysInAgenda: true, ampm: true }
	*/
	defaultPreferences: import_prop_types.default.shape({
		ampm: import_prop_types.default.bool,
		isSidePanelOpen: import_prop_types.default.bool,
		showEmptyDaysInAgenda: import_prop_types.default.bool,
		showWeekends: import_prop_types.default.bool,
		showWeekNumber: import_prop_types.default.bool,
		weekStartsOn: import_prop_types.default.oneOf([
			0,
			1,
			2,
			3,
			4,
			5,
			6
		])
	}),
	/**
	* The view initially displayed in the calendar.
	* To render a controlled calendar, use the `view` prop.
	* @default "week"
	*/
	defaultView: import_prop_types.default.oneOf([
		"agenda",
		"day",
		"month",
		"week"
	]),
	/**
	* The date initially used to determine the visible date range.
	* To render a controlled component, use the `visibleDate` prop.
	* @default today
	*/
	defaultVisibleDate: import_prop_types.default.instanceOf(Date),
	/**
	* The IDs of the resources initially visible.
	* To render a controlled scheduler, use the `visibleResources` prop.
	* @default {} - all resources are visible
	*/
	defaultVisibleResources: import_prop_types.default.object,
	/**
	* The timezone used to display events in the scheduler.
	*
	* Accepts any valid IANA timezone name
	* (for example "America/New_York", "Europe/Paris", "Asia/Tokyo"),
	* or keywords understood by the adapter, such as
	* "default" (use the adapter's default timezone),
	* "locale" (use the user's current locale timezone),
	* or "UTC".
	*
	* This timezone only affects rendering, events keep their original data timezone.
	* @default "default"
	*/
	displayTimezone: import_prop_types.default.string,
	/**
	* The color palette used for all events.
	* Can be overridden per resource using the `eventColor` property on the resource model.
	* Can be overridden per event using the `color` property on the event model.
	* @default "teal"
	*/
	eventColor: import_prop_types.default.oneOf([
		"amber",
		"blue",
		"green",
		"grey",
		"indigo",
		"lime",
		"orange",
		"pink",
		"purple",
		"red",
		"teal"
	]),
	/**
	* Configures how events are created.
	* If `false`, event creation is disabled.
	* If `true`, event creation is enabled with default configuration.
	* If an object, event creation is enabled with the provided configuration.
	*/
	eventCreation: import_prop_types.default.oneOfType([import_prop_types.default.shape({
		duration: import_prop_types.default.number,
		interaction: import_prop_types.default.oneOf(["click", "double-click"])
	}), import_prop_types.default.bool]),
	/**
	* The structure of the event model.
	* It defines how to read and write the properties of the event model.
	* If not provided, the event model is assumed to match the `CalendarEvent` interface.
	*/
	eventModelStructure: import_prop_types.default.object,
	/**
	* The events currently available in the calendar.
	* @default []
	*/
	events: import_prop_types.default.arrayOf(import_prop_types.default.object),
	/**
	* Callback fired when some event of the calendar change.
	*/
	onEventsChange: import_prop_types.default.func,
	/**
	* Event handler called when the preferences change.
	*/
	onPreferencesChange: import_prop_types.default.func,
	/**
	* Event handler called when the view changes.
	*/
	onViewChange: import_prop_types.default.func,
	/**
	* Event handler called when the visible date changes.
	*/
	onVisibleDateChange: import_prop_types.default.func,
	/**
	* Event handler called when the visible resources change.
	*/
	onVisibleResourcesChange: import_prop_types.default.func,
	/**
	* Preferences currently displayed in the calendar.
	*/
	preferences: import_prop_types.default.shape({
		ampm: import_prop_types.default.bool,
		isSidePanelOpen: import_prop_types.default.bool,
		showEmptyDaysInAgenda: import_prop_types.default.bool,
		showWeekends: import_prop_types.default.bool,
		showWeekNumber: import_prop_types.default.bool,
		weekStartsOn: import_prop_types.default.oneOf([
			0,
			1,
			2,
			3,
			4,
			5,
			6
		])
	}),
	/**
	* Config of the preferences menu.
	* Defines which options are visible in the menu.
	* If `false`, the menu will be entirely hidden.
	* @default { toggleWeekendVisibility: true, toggleWeekNumberVisibility: true, toggleAmpm: true, toggleEmptyDaysInAgenda: true, toggleWeekStartsOn: false }
	*/
	preferencesMenuConfig: import_prop_types.default.oneOfType([import_prop_types.default.oneOf([false]), import_prop_types.default.shape({
		toggleAmpm: import_prop_types.default.bool,
		toggleEmptyDaysInAgenda: import_prop_types.default.bool,
		toggleWeekendVisibility: import_prop_types.default.bool,
		toggleWeekNumberVisibility: import_prop_types.default.bool,
		toggleWeekStartsOn: import_prop_types.default.bool
	})]),
	/**
	* Whether the calendar is in read-only mode.
	* @default false
	*/
	readOnly: import_prop_types.default.bool,
	/**
	* The structure of the resource model.
	* It defines how to read and write the properties of the resource model.
	* If not provided, the resource model is assumed to match the `CalendarResource` interface.
	*/
	resourceModelStructure: import_prop_types.default.object,
	/**
	* The resources the events can be assigned to.
	*/
	resources: import_prop_types.default.arrayOf(import_prop_types.default.object),
	/**
	* Whether each event must be assigned to a resource. When true, the resource cannot be cleared in the edit dialog and the form cannot be submitted without one.
	* @default false
	*/
	shouldEventRequireResource: import_prop_types.default.bool,
	/**
	* Whether the component should display the current time indicator.
	* @default true
	*/
	showCurrentTimeIndicator: import_prop_types.default.bool,
	/**
	* The system prop that allows defining system overrides as well as additional CSS styles.
	*/
	sx: import_prop_types.default.oneOfType([
		import_prop_types.default.arrayOf(import_prop_types.default.oneOfType([
			import_prop_types.default.func,
			import_prop_types.default.object,
			import_prop_types.default.bool
		])),
		import_prop_types.default.func,
		import_prop_types.default.object
	]),
	/**
	* The view currently displayed in the calendar.
	*/
	view: import_prop_types.default.oneOf([
		"agenda",
		"day",
		"month",
		"week"
	]),
	/**
	* Configuration applied to the view, keyed by the view name.
	* The `agenda` view does not support any configuration keys yet.
	*/
	viewConfig: import_prop_types.default.object,
	/**
	* The views available in the calendar.
	* @default ["day", "week", "month", "agenda"]
	*/
	views: import_prop_types.default.arrayOf(import_prop_types.default.oneOf([
		"agenda",
		"day",
		"month",
		"week"
	]).isRequired),
	/**
	* The date currently used to determine the visible date range.
	*/
	visibleDate: import_prop_types.default.instanceOf(Date),
	/**
	* The IDs of the resources currently visible.
	* A resource is visible if it is not included in this object or if it is included with `true` value.
	*/
	visibleResources: import_prop_types.default.object
};
//#endregion
//#region node_modules/@mui/x-scheduler/internals/components/event/time-grid-event/TimeGridEventTouch.mjs
var _excluded$13 = [
	"occurrence",
	"variant",
	"className"
];
var TimeGridEventTouchRoot = styled(CalendarGridTimeEvent, {
	name: "MuiEventCalendar",
	slot: "TimeGridEventTouchRoot"
})(({ theme }) => _extends({}, getTimeGridEventRootStyles(theme), {
	padding: theme.spacing(.5, .7, .5, .7),
	"&[data-under-fifteen-minutes=\"true\"]": { padding: theme.spacing(0, .5) },
	"&[data-editing]": {
		backgroundColor: "var(--event-surface-selected)",
		color: "var(--event-on-surface-selected)"
	}
}));
var TOUCH_TITLE_LINE_HEIGHT_PX = 12;
var TOUCH_TITLE_MAX_LINES = 24;
var touchTitleLineClampSteps = {};
for (let lines = 2; lines <= TOUCH_TITLE_MAX_LINES; lines += 1) {
	const minHeight = lines * TOUCH_TITLE_LINE_HEIGHT_PX;
	touchTitleLineClampSteps[`@container (min-height: ${minHeight}px)`] = {
		WebkitLineClamp: lines,
		maxHeight: `${lines * TOUCH_TITLE_LINE_HEIGHT_PX}px`
	};
}
var TimeGridEventTouchTitle = styled(Typography, {
	name: "MuiEventCalendar",
	slot: "TimeGridEventTouchTitle"
})(({ theme }) => _extends({
	margin: 0,
	color: "var(--event-on-surface-subtle-primary)",
	fontWeight: theme.typography.fontWeightMedium,
	fontSize: "var(--EventCalendar-fontSize-eventTitle, 0.75rem)",
	lineHeight: `${TOUCH_TITLE_LINE_HEIGHT_PX}px`,
	"[data-editing] &": { color: "var(--event-on-surface-selected)" }
}, linesClampStyles(1), { maxHeight: `${TOUCH_TITLE_LINE_HEIGHT_PX}px` }, touchTitleLineClampSteps));
var TimeGridEventTouchResizeHandler = styled(CalendarGridTimeEventResizeHandler, {
	name: "MuiEventCalendar",
	slot: "TimeGridEventTouchResizeHandler"
})({
	position: "absolute",
	height: 4,
	left: 0,
	right: 0,
	zIndex: 3,
	cursor: "ns-resize",
	"&[data-start]": { top: 0 },
	"&[data-end]": { bottom: 0 }
});
var TimeGridEventTouch = /*#__PURE__*/ import_react.forwardRef(function TimeGridEventTouch(props, forwardedRef) {
	const { occurrence, variant, className } = props, other = _objectWithoutPropertiesLoose(props, _excluded$13);
	const { classes } = useEventCalendarStyledContext();
	const { isDraggable, isStartResizable, isEndResizable, rootDataAttributes, rootPositionProps } = useTimeGridEvent(occurrence);
	const content = /*#__PURE__*/ (0, import_jsx_runtime.jsx)(TimeGridEventTouchTitle, {
		className: classes.timeGridEventTitle,
		children: occurrence.title
	});
	const sharedProps = _extends({}, rootPositionProps, { ref: forwardedRef }, other, { className: clsx(className, occurrence.className) });
	if (variant === "placeholder") return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(TimeGridEventPlaceholder, _extends({ "aria-hidden": true }, rootDataAttributes, sharedProps, {
		className: clsx(classes.timeGridEventPlaceholder, sharedProps.className),
		children: content
	}));
	return /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(TimeGridEventTouchRoot, _extends({
		isDraggable,
		eventId: occurrence.id,
		occurrenceKey: occurrence.key,
		renderDragPreview: (parameters) => /*#__PURE__*/ (0, import_jsx_runtime.jsx)(EventDragPreview, _extends({}, parameters))
	}, rootDataAttributes, sharedProps, {
		className: clsx(classes.timeGridEvent, sharedProps.className),
		children: [
			isStartResizable && /*#__PURE__*/ (0, import_jsx_runtime.jsx)(TimeGridEventTouchResizeHandler, {
				className: classes.timeGridEventResizeHandler,
				side: "start"
			}),
			content,
			isEndResizable && /*#__PURE__*/ (0, import_jsx_runtime.jsx)(TimeGridEventTouchResizeHandler, {
				className: classes.timeGridEventResizeHandler,
				side: "end"
			})
		]
	}));
});
TimeGridEventTouch.displayName = "TimeGridEventTouch";
//#endregion
//#region node_modules/@mui/x-scheduler/compact-day-view/CompactDayView.mjs
var COMPACT_DAY_VIEW_DEFINITION = createDayTimeGridViewDefinition(1);
var COMPACT_DAY_VIEW_RENDERERS = { timeGridEvent: TimeGridEventTouch };
/**
* A touch-optimized Day View (1 day) for narrow widths, to use inside the Event Calendar.
*/
var CompactDayView = /*#__PURE__*/ import_react.memo(/*#__PURE__*/ import_react.forwardRef(function CompactDayView(props, forwardedRef) {
	const { days } = useEventCalendarView(COMPACT_DAY_VIEW_DEFINITION);
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(DayTimeGridInternalRenderersContext.Provider, {
		value: COMPACT_DAY_VIEW_RENDERERS,
		children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(DayTimeGrid, _extends({
			ref: forwardedRef,
			days
		}, props))
	});
}));
CompactDayView.displayName = "CompactDayView";
//#endregion
//#region node_modules/@mui/x-scheduler/compact-day-view/StandaloneCompactDayView.mjs
/**
* A touch-optimized Day View (1 day) for narrow widths that can be used outside of the
* Event Calendar.
*/
var StandaloneCompactDayView = /*#__PURE__*/ import_react.forwardRef(function StandaloneCompactDayView(props, forwardedRef) {
	const { parameters, forwardedProps } = useExtractEventCalendarParameters(props);
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ResponsiveTypographyContainer, { children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(EventCalendarProvider, _extends({}, parameters, { children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(EventDialogProvider, { children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(CompactDayView, _extends({ ref: forwardedRef }, forwardedProps)) }) })) });
});
StandaloneCompactDayView.displayName = "StandaloneCompactDayView";
//#endregion
//#region node_modules/@mui/x-scheduler/compact-three-day-view/CompactThreeDayView.mjs
var COMPACT_THREE_DAY_VIEW_DEFINITION = createDayTimeGridViewDefinition(3);
var COMPACT_THREE_DAY_VIEW_RENDERERS = { timeGridEvent: TimeGridEventTouch };
/**
* A touch-optimized 3-Day View (3 days) for narrow widths, to use inside the Event Calendar.
*/
var CompactThreeDayView = /*#__PURE__*/ import_react.memo(/*#__PURE__*/ import_react.forwardRef(function CompactThreeDayView(props, forwardedRef) {
	const { days } = useEventCalendarView(COMPACT_THREE_DAY_VIEW_DEFINITION);
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(DayTimeGridInternalRenderersContext.Provider, {
		value: COMPACT_THREE_DAY_VIEW_RENDERERS,
		children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(DayTimeGrid, _extends({
			ref: forwardedRef,
			days
		}, props))
	});
}));
CompactThreeDayView.displayName = "CompactThreeDayView";
//#endregion
//#region node_modules/@mui/x-scheduler/compact-three-day-view/StandaloneCompactThreeDayView.mjs
/**
* A touch-optimized 3-Day View (3 days) for narrow widths that can be used outside of the
* Event Calendar.
*/
var StandaloneCompactThreeDayView = /*#__PURE__*/ import_react.forwardRef(function StandaloneCompactThreeDayView(props, forwardedRef) {
	const { parameters, forwardedProps } = useExtractEventCalendarParameters(props);
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ResponsiveTypographyContainer, { children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(EventCalendarProvider, _extends({}, parameters, { children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(EventDialogProvider, { children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(CompactThreeDayView, _extends({ ref: forwardedRef }, forwardedProps)) }) })) });
});
StandaloneCompactThreeDayView.displayName = "StandaloneCompactThreeDayView";
//#endregion
//#region node_modules/@mui/x-scheduler/compact-week-view/CompactWeekView.mjs
var COMPACT_WEEK_VIEW_DEFINITION = createDayTimeGridViewDefinition(7);
var COMPACT_WEEK_VIEW_RENDERERS = { timeGridEvent: TimeGridEventTouch };
/**
* A touch-optimized Week View (7 days) for narrow widths, to use inside the Event Calendar.
*/
var CompactWeekView = /*#__PURE__*/ import_react.memo(/*#__PURE__*/ import_react.forwardRef(function CompactWeekView(props, forwardedRef) {
	const { days } = useEventCalendarView(COMPACT_WEEK_VIEW_DEFINITION);
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(DayTimeGridInternalRenderersContext.Provider, {
		value: COMPACT_WEEK_VIEW_RENDERERS,
		children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(DayTimeGrid, _extends({
			ref: forwardedRef,
			days
		}, props))
	});
}));
CompactWeekView.displayName = "CompactWeekView";
//#endregion
//#region node_modules/@mui/x-scheduler/compact-week-view/StandaloneCompactWeekView.mjs
/**
* A touch-optimized Week View (7 days) for narrow widths that can be used outside of the
* Event Calendar.
*/
var StandaloneCompactWeekView = /*#__PURE__*/ import_react.forwardRef(function StandaloneCompactWeekView(props, forwardedRef) {
	const { parameters, forwardedProps } = useExtractEventCalendarParameters(props);
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ResponsiveTypographyContainer, { children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(EventCalendarProvider, _extends({}, parameters, { children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(EventDialogProvider, { children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(CompactWeekView, _extends({ ref: forwardedRef }, forwardedProps)) }) })) });
});
StandaloneCompactWeekView.displayName = "StandaloneCompactWeekView";
//#endregion
//#region node_modules/@mui/x-scheduler/day-view/DayView.mjs
var DAY_VIEW_DEFINITION = createDayTimeGridViewDefinition(1);
/**
* A Day View to use inside the Event Calendar.
*
* Renders the desktop event variant, which `DayTimeGrid` resolves from the default value of
* `DayTimeGridInternalRenderersContext` — no provider is needed here.
*/
var DayView = /*#__PURE__*/ import_react.memo(/*#__PURE__*/ import_react.forwardRef(function DayView(props, forwardedRef) {
	const store = useEventCalendarStoreContext();
	const { days } = useEventCalendarView(DAY_VIEW_DEFINITION);
	const config = useStore$1(store, eventCalendarViewSelectors.timeGridConfig, "day");
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(DayTimeGrid, _extends({
		ref: forwardedRef,
		days,
		startTime: config?.startTime,
		endTime: config?.endTime
	}, props));
}));
DayView.displayName = "DayView";
//#endregion
//#region node_modules/@mui/x-scheduler/day-view/StandaloneDayView.mjs
/**
* A Day View that can be used outside of the Event Calendar.
*/
var StandaloneDayView = /*#__PURE__*/ import_react.forwardRef(function StandaloneDayView(props, forwardedRef) {
	const { parameters, forwardedProps } = useExtractEventCalendarParameters(props);
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ResponsiveTypographyContainer, { children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(EventCalendarProvider, _extends({}, parameters, { children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(EventDialogProvider, { children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(DayView, _extends({ ref: forwardedRef }, forwardedProps)) }) })) });
});
StandaloneDayView.displayName = "StandaloneDayView";
StandaloneDayView.propTypes = {
	/**
	* Whether the event can be dragged to change its start and end dates without changing the duration.
	* @default true
	*/
	areEventsDraggable: import_prop_types.default.bool,
	/**
	* Whether the event start or end can be dragged to change its duration without changing its other date.
	* If `true`, both start and end can be resized.
	* If `false`, the events are not resizable.
	* If `"start"`, only the start can be resized.
	* If `"end"`, only the end can be resized.
	* @default true
	*/
	areEventsResizable: import_prop_types.default.oneOfType([import_prop_types.default.oneOf(["end", "start"]), import_prop_types.default.bool]),
	/**
	* Whether events can be dragged from outside of the calendar and dropped into it.
	* @default false
	*/
	canDragEventsFromTheOutside: import_prop_types.default.bool,
	/**
	* Whether events can be dragged from inside of the calendar and dropped outside of it.
	* If true, when the mouse leaves the calendar, the event won't be rendered inside the calendar anymore.
	* If false, when the mouse leaves the calendar, the event will be rendered in its last valid position inside the calendar.
	* @default false
	*/
	canDropEventsToTheOutside: import_prop_types.default.bool,
	/**
	* Data source for fetching events asynchronously.
	* When provided, events are fetched through the data source instead of the `events` prop.
	*/
	dataSource: import_prop_types.default.shape({
		getEvents: import_prop_types.default.func.isRequired,
		persistEvents: import_prop_types.default.func.isRequired
	}),
	/**
	* The locale object from `date-fns` used to format dates.
	* This affects day names, month names, week start day, and other locale-dependent formatting.
	* Import a locale from `date-fns/locale` and pass it to this prop.
	* @default enUS (English)
	*/
	dateLocale: import_prop_types.default.object,
	/**
	* The default preferences for the calendar.
	* To use controlled preferences, use the `preferences` prop.
	* @default { showWeekends: true, showWeekNumber: false, isSidePanelOpen: true, showEmptyDaysInAgenda: true, ampm: true }
	*/
	defaultPreferences: import_prop_types.default.shape({
		ampm: import_prop_types.default.bool,
		isSidePanelOpen: import_prop_types.default.bool,
		showEmptyDaysInAgenda: import_prop_types.default.bool,
		showWeekends: import_prop_types.default.bool,
		showWeekNumber: import_prop_types.default.bool,
		weekStartsOn: import_prop_types.default.oneOf([
			0,
			1,
			2,
			3,
			4,
			5,
			6
		])
	}),
	/**
	* The view initially displayed in the calendar.
	* To render a controlled calendar, use the `view` prop.
	* @default "week"
	*/
	defaultView: import_prop_types.default.oneOf([
		"agenda",
		"day",
		"month",
		"week"
	]),
	/**
	* The date initially used to determine the visible date range.
	* To render a controlled component, use the `visibleDate` prop.
	* @default today
	*/
	defaultVisibleDate: import_prop_types.default.instanceOf(Date),
	/**
	* The IDs of the resources initially visible.
	* To render a controlled scheduler, use the `visibleResources` prop.
	* @default {} - all resources are visible
	*/
	defaultVisibleResources: import_prop_types.default.object,
	/**
	* The timezone used to display events in the scheduler.
	*
	* Accepts any valid IANA timezone name
	* (for example "America/New_York", "Europe/Paris", "Asia/Tokyo"),
	* or keywords understood by the adapter, such as
	* "default" (use the adapter's default timezone),
	* "locale" (use the user's current locale timezone),
	* or "UTC".
	*
	* This timezone only affects rendering, events keep their original data timezone.
	* @default "default"
	*/
	displayTimezone: import_prop_types.default.string,
	/**
	* The color palette used for all events.
	* Can be overridden per resource using the `eventColor` property on the resource model.
	* Can be overridden per event using the `color` property on the event model.
	* @default "teal"
	*/
	eventColor: import_prop_types.default.oneOf([
		"amber",
		"blue",
		"green",
		"grey",
		"indigo",
		"lime",
		"orange",
		"pink",
		"purple",
		"red",
		"teal"
	]),
	/**
	* Configures how events are created.
	* If `false`, event creation is disabled.
	* If `true`, event creation is enabled with default configuration.
	* If an object, event creation is enabled with the provided configuration.
	*/
	eventCreation: import_prop_types.default.oneOfType([import_prop_types.default.shape({
		duration: import_prop_types.default.number,
		interaction: import_prop_types.default.oneOf(["click", "double-click"])
	}), import_prop_types.default.bool]),
	/**
	* The structure of the event model.
	* It defines how to read and write the properties of the event model.
	* If not provided, the event model is assumed to match the `CalendarEvent` interface.
	*/
	eventModelStructure: import_prop_types.default.object,
	/**
	* The events currently available in the calendar.
	* @default []
	*/
	events: import_prop_types.default.arrayOf(import_prop_types.default.object),
	/**
	* Callback fired when some event of the calendar change.
	*/
	onEventsChange: import_prop_types.default.func,
	/**
	* Event handler called when the preferences change.
	*/
	onPreferencesChange: import_prop_types.default.func,
	/**
	* Event handler called when the view changes.
	*/
	onViewChange: import_prop_types.default.func,
	/**
	* Event handler called when the visible date changes.
	*/
	onVisibleDateChange: import_prop_types.default.func,
	/**
	* Event handler called when the visible resources change.
	*/
	onVisibleResourcesChange: import_prop_types.default.func,
	/**
	* Preferences currently displayed in the calendar.
	*/
	preferences: import_prop_types.default.shape({
		ampm: import_prop_types.default.bool,
		isSidePanelOpen: import_prop_types.default.bool,
		showEmptyDaysInAgenda: import_prop_types.default.bool,
		showWeekends: import_prop_types.default.bool,
		showWeekNumber: import_prop_types.default.bool,
		weekStartsOn: import_prop_types.default.oneOf([
			0,
			1,
			2,
			3,
			4,
			5,
			6
		])
	}),
	/**
	* Config of the preferences menu.
	* Defines which options are visible in the menu.
	* If `false`, the menu will be entirely hidden.
	* @default { toggleWeekendVisibility: true, toggleWeekNumberVisibility: true, toggleAmpm: true, toggleEmptyDaysInAgenda: true, toggleWeekStartsOn: false }
	*/
	preferencesMenuConfig: import_prop_types.default.oneOfType([import_prop_types.default.oneOf([false]), import_prop_types.default.shape({
		toggleAmpm: import_prop_types.default.bool,
		toggleEmptyDaysInAgenda: import_prop_types.default.bool,
		toggleWeekendVisibility: import_prop_types.default.bool,
		toggleWeekNumberVisibility: import_prop_types.default.bool,
		toggleWeekStartsOn: import_prop_types.default.bool
	})]),
	/**
	* Whether the calendar is in read-only mode.
	* @default false
	*/
	readOnly: import_prop_types.default.bool,
	/**
	* The structure of the resource model.
	* It defines how to read and write the properties of the resource model.
	* If not provided, the resource model is assumed to match the `CalendarResource` interface.
	*/
	resourceModelStructure: import_prop_types.default.object,
	/**
	* The resources the events can be assigned to.
	*/
	resources: import_prop_types.default.arrayOf(import_prop_types.default.object),
	/**
	* Whether each event must be assigned to a resource. When true, the resource cannot be cleared in the edit dialog and the form cannot be submitted without one.
	* @default false
	*/
	shouldEventRequireResource: import_prop_types.default.bool,
	/**
	* Whether the component should display the current time indicator.
	* @default true
	*/
	showCurrentTimeIndicator: import_prop_types.default.bool,
	/**
	* The system prop that allows defining system overrides as well as additional CSS styles.
	*/
	sx: import_prop_types.default.oneOfType([
		import_prop_types.default.arrayOf(import_prop_types.default.oneOfType([
			import_prop_types.default.func,
			import_prop_types.default.object,
			import_prop_types.default.bool
		])),
		import_prop_types.default.func,
		import_prop_types.default.object
	]),
	/**
	* The view currently displayed in the calendar.
	*/
	view: import_prop_types.default.oneOf([
		"agenda",
		"day",
		"month",
		"week"
	]),
	/**
	* Configuration applied to the view, keyed by the view name.
	* For the `day` view, `startTime` and `endTime` (whole hours between 0 and 24)
	* limit the hours displayed in the time grid.
	* @example { day: { startTime: 8, endTime: 20 } }
	*/
	viewConfig: import_prop_types.default.shape({ day: import_prop_types.default.shape({
		endTime: import_prop_types.default.number,
		startTime: import_prop_types.default.number
	}) }),
	/**
	* The views available in the calendar.
	* @default ["day", "week", "month", "agenda"]
	*/
	views: import_prop_types.default.arrayOf(import_prop_types.default.oneOf([
		"agenda",
		"day",
		"month",
		"week"
	]).isRequired),
	/**
	* The date currently used to determine the visible date range.
	*/
	visibleDate: import_prop_types.default.instanceOf(Date),
	/**
	* The IDs of the resources currently visible.
	* A resource is visible if it is not included in this object or if it is included with `true` value.
	*/
	visibleResources: import_prop_types.default.object
};
//#endregion
//#region node_modules/@mui/x-scheduler/internals/components/error-container/ErrorContainer.mjs
var ErrorContainerRoot = styled("div", {
	name: "MuiEventErrorContainer",
	slot: "Root"
})({
	position: "absolute",
	zIndex: 1300,
	display: "flex",
	flexDirection: "column",
	alignItems: "flex-end",
	gap: 16,
	bottom: 16,
	right: 16
});
var ErrorAlert = styled(Alert, {
	name: "MuiEventErrorContainer",
	slot: "Alert"
})({ maxWidth: 400 });
var ErrorMessage = styled(Typography, {
	name: "MuiEventErrorContainer",
	slot: "Message"
})({
	wordBreak: "break-word",
	display: "-webkit-box",
	WebkitLineClamp: 3,
	WebkitBoxOrient: "vertical",
	overflow: "hidden",
	textOverflow: "ellipsis",
	overflowWrap: "break-word"
});
function ErrorContainer(props) {
	const { className } = props;
	const store = useSchedulerStoreContext();
	const { classes } = useSharedComponentsStyledContext();
	const errors = useStore$1(store, schedulerOtherSelectors.errors);
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ErrorContainerRoot, {
		className: clsx(classes.errorContainer, className),
		children: errors.map(({ error, key }) => /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ErrorAlert, {
			className: classes.errorAlert,
			severity: "error",
			onClose: () => store.dismissError(key),
			children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ErrorMessage, {
				className: classes.errorMessage,
				variant: "body2",
				children: error.message
			})
		}, key))
	});
}
//#endregion
//#region node_modules/@mui/x-scheduler/internals/components/more-events-popover/MoreEventsPopover.mjs
var _excluded$12 = ["occurrences", "day"];
var MoreEventsPopoverHeader = styled("div", {
	name: "MuiEventCalendar",
	slot: "MoreEventsPopoverHeader"
})(({ theme }) => ({
	display: "flex",
	alignItems: "center",
	justifyContent: "space-between",
	padding: theme.spacing(1),
	borderBottom: `1px solid ${(theme.vars || theme).palette.divider}`
}));
var MoreEventsPopoverTitle = styled(Typography, {
	name: "MuiEventCalendar",
	slot: "MoreEventsPopoverTitle"
})(({ theme }) => ({
	fontSize: theme.typography.body2.fontSize,
	fontWeight: theme.typography.fontWeightMedium,
	color: (theme.vars || theme).palette.text.primary,
	lineHeight: 1.5,
	margin: 0
}));
var MoreEventsPopoverBody = styled("div", {
	name: "MuiEventCalendar",
	slot: "MoreEventsPopoverBody"
})(({ theme }) => ({
	padding: theme.spacing(1),
	display: "flex",
	flexDirection: "column",
	gap: theme.spacing(1),
	width: "fit-content",
	minWidth: 200
}));
var MoreEventsPopover = createModal({ contextName: "MoreEventsPopoverContext" });
MoreEventsPopover.Context;
MoreEventsPopover.useContext;
function MoreEventsPopoverContent(props) {
	const { open, anchor, occurrences, day, onClose } = props;
	const adapter = useAdapterContext();
	const { schedulerId, classes } = useEventCalendarStyledContext();
	const { subscribeCloseHandler } = useEventDialogContext();
	import_react.useEffect(() => {
		return subscribeCloseHandler(() => {
			onClose();
		});
	}, [subscribeCloseHandler, onClose]);
	return /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(Popover, {
		className: classes.moreEventsPopover,
		open,
		anchorEl: anchor,
		onClose,
		children: [/*#__PURE__*/ (0, import_jsx_runtime.jsx)(MoreEventsPopoverHeader, {
			className: classes.moreEventsPopoverHeader,
			id: `${schedulerId}-PopoverHeader-${day.key}`,
			"aria-label": `${formatWeekDayMonthAndDayOfMonth(day.value, adapter)}`,
			children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(MoreEventsPopoverTitle, {
				className: classes.moreEventsPopoverTitle,
				children: formatWeekDayMonthAndDayOfMonth(day.value, adapter)
			})
		}), /*#__PURE__*/ (0, import_jsx_runtime.jsx)(MoreEventsPopoverBody, {
			className: classes.moreEventsPopoverBody,
			children: occurrences.map((occurrence) => /*#__PURE__*/ (0, import_jsx_runtime.jsx)(EventDialogTrigger, {
				occurrence,
				children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(EventItem, {
					variant: isOccurrenceAllDayOrMultipleDay(occurrence, adapter) ? "filled" : "compact",
					occurrence,
					date: day,
					ariaLabelledBy: `${schedulerId}-PopoverHeader-${day.key}`
				})
			}, occurrence.key))
		})]
	});
}
function MoreEventsPopoverProvider(props) {
	const { children } = props;
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(MoreEventsPopover.Provider, {
		render: ({ isOpen, anchorRef, data, onClose }) => /*#__PURE__*/ (0, import_jsx_runtime.jsx)(MoreEventsPopoverContent, {
			open: isOpen,
			anchor: anchorRef.current,
			occurrences: data.occurrences,
			count: data.count,
			day: data.day,
			onClose
		}),
		children
	});
}
function MoreEventsPopoverTrigger(props) {
	const { occurrences, day } = props, other = _objectWithoutPropertiesLoose(props, _excluded$12);
	const ref = import_react.useRef(null);
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(MoreEventsPopover.Trigger, _extends({
		ref,
		data: {
			occurrences,
			count: occurrences.length,
			day
		}
	}, other));
}
//#endregion
//#region node_modules/@mui/x-scheduler/month-view/month-view-row/MonthViewCell.mjs
var MonthViewCellRoot = styled(CalendarGridDayCell, {
	name: "MuiEventCalendar",
	slot: "MonthViewCell"
})(({ theme }) => ({
	display: "grid",
	gridTemplateRows: "repeat(var(--row-count), minmax(auto, 18px))",
	gap: theme.spacing(.5),
	padding: theme.spacing(.5),
	fontSize: theme.typography.body2.fontSize,
	lineHeight: "18px",
	borderInlineStart: `1px solid transparent`,
	"&:not(:first-of-type)": { borderInlineStartColor: (theme.vars || theme).palette.divider },
	"&[data-weekend]": {
		backgroundColor: (theme.vars || theme).palette.action.hover,
		color: (theme.vars || theme).palette.text.primary
	},
	"&[data-current]": { backgroundColor: theme.alpha((theme.vars || theme).palette.primary.light, .05) },
	[`&[data-current] .${eventCalendarClasses.monthViewCellNumber}`]: {
		backgroundColor: (theme.vars || theme).palette.primary.main,
		color: (theme.vars || theme).palette.primary.contrastText
	},
	"&[data-other-month]": { color: (theme.vars || theme).palette.text.disabled },
	"&:focus-visible": {
		outline: "none",
		backgroundColor: getCellFocusBackground(theme)
	},
	[`&[data-current] > .${eventCalendarClasses.monthViewCellNumberButton} > .${eventCalendarClasses.monthViewCellNumber}`]: { backgroundColor: "transparent" },
	[`&[data-current] > .${eventCalendarClasses.monthViewCellNumberButton}:hover > .${eventCalendarClasses.monthViewCellNumber}`]: { backgroundColor: "transparent" },
	[`&[data-current] > .${eventCalendarClasses.monthViewCellNumberButton}:active > .${eventCalendarClasses.monthViewCellNumber}`]: { backgroundColor: "transparent" },
	[`&[data-current] > .${eventCalendarClasses.monthViewCellNumberButton}`]: {
		backgroundColor: (theme.vars || theme).palette.primary.main,
		color: (theme.vars || theme).palette.primary.contrastText,
		marginTop: 1
	},
	[`&[data-current] > .${eventCalendarClasses.monthViewCellNumberButton}:hover`]: { backgroundColor: (theme.vars || theme).palette.primary.dark },
	[`&[data-current] > .${eventCalendarClasses.monthViewCellNumberButton}:active`]: { backgroundColor: (theme.vars || theme).palette.primary.dark }
}));
var MonthViewCellNumber = styled("span", {
	name: "MuiEventCalendar",
	slot: "MonthViewCellNumber"
})(({ theme }) => ({
	gridRow: 1,
	justifySelf: "end",
	alignSelf: "flex-end",
	padding: theme.spacing(0, .5),
	borderRadius: theme.shape.borderRadius
}));
var MonthViewCellNumberButton = styled("button", {
	name: "MuiEventCalendar",
	slot: "MonthViewCellNumberButton"
})(({ theme }) => ({
	gridRow: 1,
	justifySelf: "end",
	width: "fit-content",
	alignSelf: "flex-end",
	borderRadius: theme.shape.borderRadius,
	border: "none",
	background: "none",
	padding: 0,
	cursor: "pointer",
	font: "inherit",
	color: "inherit",
	"&:hover": { backgroundColor: (theme.vars || theme).palette.action.hover },
	"&:active": { backgroundColor: (theme.vars || theme).palette.action.selected },
	"&:focus-visible": {
		backgroundColor: getCellFocusBackground(theme),
		outline: `2px solid ${(theme.vars || theme).palette.primary.main}`,
		outlineOffset: 2
	},
	"&:focus-visible:hover": { backgroundColor: (theme.vars || theme).palette.action.hover }
}));
var MonthViewCellEvents = styled("div", {
	name: "MuiEventCalendar",
	slot: "MonthViewCellEvents"
})(({ theme }) => ({
	position: "relative",
	display: "grid",
	gap: theme.spacing(.5)
}));
var MonthViewMoreEvents = styled(Button$1, {
	name: "MuiEventCalendar",
	slot: "MonthViewMoreEvents"
})(({ theme }) => ({
	margin: 0,
	color: (theme.vars || theme).palette.text.secondary,
	fontSize: theme.typography.caption.fontSize,
	lineHeight: "18px",
	paddingInlineStart: theme.spacing(.5),
	justifyContent: "flex-start",
	textTransform: "none"
}));
var MonthViewPlaceholderEventContainer = styled("div", {
	name: "MuiEventCalendar",
	slot: "MonthViewPlaceholderContainer"
})(({ theme }) => ({
	position: "absolute",
	top: 0,
	left: 0,
	right: 0,
	display: "grid",
	gridTemplateColumns: "1fr",
	gridTemplateRows: "repeat(var(--row-count), minmax(auto, 18px))",
	gap: theme.spacing(.5)
}));
var MonthViewCell = /*#__PURE__*/ import_react.forwardRef(function MonthViewCell(props, ref) {
	const { day, row, maxEvents, colIndex, ariaLabelledBy } = props;
	const adapter = useAdapterContext();
	const store = useEventCalendarStoreContext();
	const { classes, localeText } = useEventCalendarStyledContext();
	const { onOpen: startEditing } = useEventDialogContext();
	const hasDayView = useStore$1(store, eventCalendarViewSelectors.hasDayView);
	const visibleDate = useStore$1(store, schedulerOtherSelectors.visibleDate);
	const isCreatingAnEvent = useStore$1(store, eventCalendarOccurrencePlaceholderSelectors.isCreatingInDayCell, day.value);
	const isToday = useStore$1(store, schedulerNowSelectors.isCurrentDay, day.value);
	const isLoading = useStore$1(store, schedulerOtherSelectors.isLoading);
	const placeholder = useCalendarGridPlaceholderInDay(day.value, row, maxEvents);
	const cellRef = import_react.useRef(null);
	const handleRef = useMergedRefs(ref, cellRef);
	const isCurrentMonth = adapter.isSameMonth(day.value, visibleDate);
	const isFirstDayOfMonth = adapter.isSameDay(day.value, adapter.startOfMonth(day.value));
	const inBoundOccurrences = day.withPosition.filter((o) => o.position.index <= maxEvents);
	const overflowOccurrences = day.withPosition.filter((o) => o.position.index > maxEvents);
	const visibleOccurrences = overflowOccurrences.length > 0 ? inBoundOccurrences.slice(0, maxEvents - 1) : inBoundOccurrences;
	const hiddenCount = overflowOccurrences.length + (inBoundOccurrences.length - visibleOccurrences.length);
	const cellNumberContent = /*#__PURE__*/ (0, import_jsx_runtime.jsx)(MonthViewCellNumber, {
		className: classes.monthViewCellNumber,
		children: isFirstDayOfMonth ? formatMonthAndDayOfMonth(day.value, adapter) : adapter.format(day.value, "dayOfMonth")
	});
	const rowCount = 1 + maxEvents;
	import_react.useEffect(() => {
		if (!isCreatingAnEvent || !placeholder || !cellRef.current) return;
		startEditing(cellRef, placeholder);
	}, [
		isCreatingAnEvent,
		placeholder,
		startEditing
	]);
	return /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(MonthViewCellRoot, {
		className: classes.monthViewCell,
		ref: handleRef,
		value: day.value,
		"aria-colindex": colIndex,
		"aria-labelledby": ariaLabelledBy,
		"data-current": isToday || void 0,
		"data-other-month": !isCurrentMonth || void 0,
		"data-weekend": isWeekend(adapter, day.value) || void 0,
		lockSurfaceType: true,
		style: { "--row-count": rowCount },
		children: [hasDayView ? /*#__PURE__*/ (0, import_jsx_runtime.jsx)(MonthViewCellNumberButton, {
			type: "button",
			className: classes.monthViewCellNumberButton,
			onClick: (event) => store.switchToDay(day.value, event),
			tabIndex: -1,
			children: cellNumberContent
		}) : cellNumberContent, /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(MonthViewCellEvents, {
			className: classes.monthViewCellEvents,
			children: [
				isLoading && /*#__PURE__*/ (0, import_jsx_runtime.jsx)(EventSkeleton, { "data-variant": "day-grid" }),
				!isLoading && visibleOccurrences.map((occurrence) => {
					if (occurrence.position.isInvisible) return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(DayGridEvent, {
						occurrence,
						variant: "invisible"
					}, occurrence.key);
					const startsBeforeThisDay = !adapter.isSameDay(occurrence.displayTimezone.start.value, day.value) && adapter.isBefore(occurrence.displayTimezone.start.value, day.value);
					return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(EventDialogTrigger, {
						occurrence,
						children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(DayGridEvent, _extends({
							occurrence,
							variant: isOccurrenceAllDayOrMultipleDay(occurrence, adapter) ? "filled" : "compact"
						}, startsBeforeThisDay ? { "data-starting-before-edge": "" } : {}))
					}, occurrence.key);
				}),
				hiddenCount > 0 && /*#__PURE__*/ (0, import_jsx_runtime.jsx)(MoreEventsPopoverTrigger, {
					occurrences: day.withPosition,
					day,
					children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(MonthViewMoreEvents, {
						size: "small",
						"aria-label": localeText.hiddenEvents(hiddenCount),
						className: classes.monthViewMoreEvents,
						children: localeText.hiddenEvents(hiddenCount)
					})
				}),
				placeholder != null && /*#__PURE__*/ (0, import_jsx_runtime.jsx)(MonthViewPlaceholderEventContainer, {
					className: classes.monthViewPlaceholderContainer,
					children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(DayGridEvent, {
						occurrence: placeholder,
						variant: "placeholder"
					})
				})
			]
		})]
	}, day.key);
});
MonthViewCell.displayName = "MonthViewCell";
//#endregion
//#region node_modules/@mui/x-scheduler/month-view/month-view-row/MonthViewWeekRow.mjs
var FIXED_CELL_WIDTH$1 = 28;
var MonthViewRow = styled(CalendarGridDayRow, {
	name: "MuiEventCalendar",
	slot: "MonthViewRow"
})(({ theme }) => ({
	display: "grid",
	gridTemplateColumns: "repeat(auto-fit, minmax(0, 1fr))",
	"&[data-show-week-number]": { gridTemplateColumns: `${FIXED_CELL_WIDTH$1}px repeat(auto-fit, minmax(0, 1fr))` },
	"&:not(:last-child)": { borderBlockEnd: `1px solid ${(theme.vars || theme).palette.divider}` }
}));
var MonthViewWeekNumberCell = styled("div", {
	name: "MuiEventCalendar",
	slot: "MonthViewWeekNumberCell"
})(({ theme }) => ({
	padding: theme.spacing(1, 0),
	textAlign: "center",
	fontSize: theme.typography.caption.fontSize,
	lineHeight: "18px",
	color: (theme.vars || theme).palette.text.secondary
}));
function MonthViewWeekRow(props) {
	const { rowIndex, maxEvents, days, occurrencesMap, firstDayRef } = props;
	const adapter = useAdapterContext();
	const store = useEventCalendarStoreContext();
	const showWeekNumber = useStore$1(store, eventCalendarPreferenceSelectors.showWeekNumber);
	const weekStartsOn = useStore$1(store, eventCalendarPreferenceSelectors.weekStartsOn);
	const { schedulerId, classes, localeText } = useEventCalendarStyledContext();
	const occurrences = useEventOccurrencesWithDayGridPosition({
		days,
		occurrencesMap,
		maxEvents
	});
	const weekNumber = getWeekNumber(adapter, days[0].value, weekStartsOn);
	const { start, end } = import_react.useMemo(() => ({
		start: days[0].value,
		end: adapter.endOfDay(days[days.length - 1].value)
	}), [adapter, days]);
	const weekNumberId = showWeekNumber ? `${schedulerId}-MonthViewWeekNumber-${weekNumber}` : void 0;
	return /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(MonthViewRow, {
		className: classes.monthViewRow,
		start,
		end,
		rowIndex,
		"data-show-week-number": showWeekNumber || void 0,
		children: [showWeekNumber && /*#__PURE__*/ (0, import_jsx_runtime.jsx)(MonthViewWeekNumberCell, {
			className: classes.monthViewWeekNumberCell,
			id: weekNumberId,
			"aria-label": localeText.weekNumberAriaLabel(weekNumber),
			"aria-hidden": "true",
			children: weekNumber
		}), occurrences.days.map((day, dayIdx) => /*#__PURE__*/ (0, import_jsx_runtime.jsx)(MonthViewCell, {
			ref: dayIdx === 0 ? firstDayRef : void 0,
			day,
			maxEvents,
			row: occurrences,
			colIndex: dayIdx + 1,
			ariaLabelledBy: weekNumberId
		}, day.key))]
	});
}
//#endregion
//#region node_modules/@mui/x-scheduler/month-view/MonthView.mjs
var FIXED_CELL_WIDTH = 28;
var MonthViewRoot = styled("div", {
	name: "MuiEventCalendar",
	slot: "MonthView"
})(({ theme }) => ({
	width: "100%",
	height: "100%",
	borderRadius: theme.shape.borderRadius,
	border: `1px solid ${(theme.vars || theme).palette.divider}`,
	overflow: "hidden",
	display: "flex",
	flexDirection: "column",
	maxHeight: "100%",
	overflowY: "auto"
}));
var MonthViewGrid = styled(CalendarGridRoot, {
	name: "MuiEventCalendar",
	slot: "MonthViewGrid"
})({
	flex: 1,
	display: "flex",
	flexDirection: "column",
	minHeight: 0
});
var MonthViewHeader = styled(CalendarGridHeaderRow, {
	name: "MuiEventCalendar",
	slot: "MonthViewHeader"
})(({ theme, ownerState }) => ({
	display: "grid",
	gridTemplateColumns: ownerState.showWeekNumber ? `${FIXED_CELL_WIDTH}px repeat(auto-fit, minmax(0, 1fr))` : "repeat(auto-fit, minmax(0, 1fr))",
	borderBlockEnd: `1px solid ${(theme.vars || theme).palette.divider}`
}));
var MonthViewHeaderCell = styled(CalendarGridHeaderCell, {
	name: "MuiEventCalendar",
	slot: "MonthViewHeaderCell"
})(({ theme }) => ({
	padding: theme.spacing(1),
	textAlign: "center",
	fontSize: theme.typography.body2.fontSize,
	lineHeight: "18px",
	"&:not(:first-of-type)": { borderInlineStart: `1px solid ${(theme.vars || theme).palette.divider}` },
	"&:focus-visible": {
		outline: "none",
		borderRadius: theme.shape.borderRadius,
		boxShadow: `inset 0 0 0 2px ${(theme.vars || theme).palette.primary.main}`
	}
}));
var MonthViewWeekHeaderCell = styled("div", {
	name: "MuiEventCalendar",
	slot: "MonthViewWeekHeaderCell"
})(({ theme }) => ({
	padding: theme.spacing(1, 0),
	textAlign: "center",
	fontSize: theme.typography.caption.fontSize,
	lineHeight: "18px",
	color: (theme.vars || theme).palette.text.secondary,
	fontStyle: "italic"
}));
var MonthViewBody = styled("div", {
	name: "MuiEventCalendar",
	slot: "MonthViewBody"
})({
	flex: 1,
	display: "grid",
	gridAutoRows: "minmax(0, 1fr)",
	position: "relative",
	flexGrow: 1,
	overflow: "hidden"
});
var MONTH_VIEW_ROW_TYPES = ["header", "day-grid"];
var CELL_PADDING = 5;
var DAY_NUMBER_HEADER_HEIGHT = 22;
var MONTH_VIEW_DEFINITION = {
	siblingVisibleDateGetter: ({ state, delta }) => state.adapter.addMonths(state.adapter.startOfMonth(schedulerOtherSelectors.visibleDate(state)), delta),
	visibleDaysSelector: createSelectorMemoized$1((state) => state.adapter, schedulerOtherSelectors.visibleDate, eventCalendarPreferenceSelectors.showWeekends, eventCalendarPreferenceSelectors.weekStartsOn, (adapter, visibleDate, showWeekends, weekStartsOn) => getDayList({
		adapter,
		start: getStartOfWeek(adapter, adapter.startOfMonth(visibleDate), weekStartsOn),
		end: getEndOfWeek(adapter, adapter.endOfMonth(visibleDate), weekStartsOn),
		excludeWeekends: !showWeekends
	}))
};
/**
* A Month View to use inside the Event Calendar.
*/
var MonthView = /*#__PURE__*/ import_react.memo(/*#__PURE__*/ import_react.forwardRef(function MonthView(props, forwardedRef) {
	const adapter = useAdapterContext();
	const { classes, localeText } = useEventCalendarStyledContext();
	const store = useEventCalendarStoreContext();
	const handleRef = useMergedRefs(forwardedRef, import_react.useRef(null));
	const cellRef = import_react.useRef(null);
	const showWeekNumber = useStore$1(store, eventCalendarPreferenceSelectors.showWeekNumber);
	const showWeekends = useStore$1(store, eventCalendarPreferenceSelectors.showWeekends);
	const [maxEvents, setMaxEvents] = import_react.useState(2);
	const { days } = useEventCalendarView(MONTH_VIEW_DEFINITION);
	const weeks = import_react.useMemo(() => {
		const chunkSize = showWeekends ? 7 : 5;
		const result = [];
		for (let i = 0; i < days.length; i += chunkSize) result.push(days.slice(i, i + chunkSize));
		return result;
	}, [days, showWeekends]);
	const monthViewRowsPerType = import_react.useMemo(() => ({ "day-grid": weeks.length }), [weeks.length]);
	const occurrencesMap = useEventOccurrencesGroupedByDay({ days });
	useResizeObserver(cellRef, () => {
		const eventContainerHeight = cellRef.current.clientHeight - CELL_PADDING - DAY_NUMBER_HEADER_HEIGHT;
		const maxEventsCount = Math.floor(eventContainerHeight / 22);
		setMaxEvents(Math.max(1, maxEventsCount));
	}, true);
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(MonthViewRoot, _extends({}, props, {
		ref: handleRef,
		className: clsx(props.className, classes.monthView),
		children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(MoreEventsPopoverProvider, { children: /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(MonthViewGrid, {
			className: classes.monthViewGrid,
			rowTypes: MONTH_VIEW_ROW_TYPES,
			rowsPerType: monthViewRowsPerType,
			"aria-rowcount": 1 + weeks.length,
			"aria-colcount": weeks[0].length,
			children: [/*#__PURE__*/ (0, import_jsx_runtime.jsxs)(MonthViewHeader, {
				className: classes.monthViewHeader,
				ownerState: { showWeekNumber },
				"aria-rowindex": 1,
				children: [showWeekNumber && /*#__PURE__*/ (0, import_jsx_runtime.jsx)(MonthViewWeekHeaderCell, {
					className: classes.monthViewWeekHeaderCell,
					"aria-hidden": "true",
					children: localeText.weekAbbreviation
				}), weeks[0].map((weekDay, dayIdx) => /*#__PURE__*/ (0, import_jsx_runtime.jsx)(MonthViewHeaderCell, {
					className: classes.monthViewHeaderCell,
					date: weekDay,
					"aria-colindex": dayIdx + 1,
					skipDataCurrent: true,
					children: adapter.formatByString(weekDay.value, "ccc")
				}, weekDay.key))]
			}), /*#__PURE__*/ (0, import_jsx_runtime.jsx)(MonthViewBody, {
				className: classes.monthViewBody,
				children: weeks.map((week, weekIdx) => /*#__PURE__*/ (0, import_jsx_runtime.jsx)(MonthViewWeekRow, {
					rowIndex: weekIdx,
					maxEvents,
					days: week,
					occurrencesMap,
					firstDayRef: weekIdx === 0 ? cellRef : void 0
				}, weekIdx))
			})]
		}) })
	}));
}));
MonthView.displayName = "MonthView";
//#endregion
//#region node_modules/@mui/x-scheduler/month-view/StandaloneMonthView.mjs
/**
* A Month View that can be used outside of the Event Calendar.
*/
var StandaloneMonthView = /*#__PURE__*/ import_react.forwardRef(function StandaloneMonthView(props, forwardedRef) {
	const { parameters, forwardedProps } = useExtractEventCalendarParameters(props);
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ResponsiveTypographyContainer, { children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(EventCalendarProvider, _extends({}, parameters, { children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(EventDialogProvider, { children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(MonthView, _extends({ ref: forwardedRef }, forwardedProps)) }) })) });
});
StandaloneMonthView.displayName = "StandaloneMonthView";
StandaloneMonthView.propTypes = {
	/**
	* Whether the event can be dragged to change its start and end dates without changing the duration.
	* @default true
	*/
	areEventsDraggable: import_prop_types.default.bool,
	/**
	* Whether the event start or end can be dragged to change its duration without changing its other date.
	* If `true`, both start and end can be resized.
	* If `false`, the events are not resizable.
	* If `"start"`, only the start can be resized.
	* If `"end"`, only the end can be resized.
	* @default true
	*/
	areEventsResizable: import_prop_types.default.oneOfType([import_prop_types.default.oneOf(["end", "start"]), import_prop_types.default.bool]),
	/**
	* Whether events can be dragged from outside of the calendar and dropped into it.
	* @default false
	*/
	canDragEventsFromTheOutside: import_prop_types.default.bool,
	/**
	* Whether events can be dragged from inside of the calendar and dropped outside of it.
	* If true, when the mouse leaves the calendar, the event won't be rendered inside the calendar anymore.
	* If false, when the mouse leaves the calendar, the event will be rendered in its last valid position inside the calendar.
	* @default false
	*/
	canDropEventsToTheOutside: import_prop_types.default.bool,
	/**
	* Data source for fetching events asynchronously.
	* When provided, events are fetched through the data source instead of the `events` prop.
	*/
	dataSource: import_prop_types.default.shape({
		getEvents: import_prop_types.default.func.isRequired,
		persistEvents: import_prop_types.default.func.isRequired
	}),
	/**
	* The locale object from `date-fns` used to format dates.
	* This affects day names, month names, week start day, and other locale-dependent formatting.
	* Import a locale from `date-fns/locale` and pass it to this prop.
	* @default enUS (English)
	*/
	dateLocale: import_prop_types.default.object,
	/**
	* The default preferences for the calendar.
	* To use controlled preferences, use the `preferences` prop.
	* @default { showWeekends: true, showWeekNumber: false, isSidePanelOpen: true, showEmptyDaysInAgenda: true, ampm: true }
	*/
	defaultPreferences: import_prop_types.default.shape({
		ampm: import_prop_types.default.bool,
		isSidePanelOpen: import_prop_types.default.bool,
		showEmptyDaysInAgenda: import_prop_types.default.bool,
		showWeekends: import_prop_types.default.bool,
		showWeekNumber: import_prop_types.default.bool,
		weekStartsOn: import_prop_types.default.oneOf([
			0,
			1,
			2,
			3,
			4,
			5,
			6
		])
	}),
	/**
	* The view initially displayed in the calendar.
	* To render a controlled calendar, use the `view` prop.
	* @default "week"
	*/
	defaultView: import_prop_types.default.oneOf([
		"agenda",
		"day",
		"month",
		"week"
	]),
	/**
	* The date initially used to determine the visible date range.
	* To render a controlled component, use the `visibleDate` prop.
	* @default today
	*/
	defaultVisibleDate: import_prop_types.default.instanceOf(Date),
	/**
	* The IDs of the resources initially visible.
	* To render a controlled scheduler, use the `visibleResources` prop.
	* @default {} - all resources are visible
	*/
	defaultVisibleResources: import_prop_types.default.object,
	/**
	* The timezone used to display events in the scheduler.
	*
	* Accepts any valid IANA timezone name
	* (for example "America/New_York", "Europe/Paris", "Asia/Tokyo"),
	* or keywords understood by the adapter, such as
	* "default" (use the adapter's default timezone),
	* "locale" (use the user's current locale timezone),
	* or "UTC".
	*
	* This timezone only affects rendering, events keep their original data timezone.
	* @default "default"
	*/
	displayTimezone: import_prop_types.default.string,
	/**
	* The color palette used for all events.
	* Can be overridden per resource using the `eventColor` property on the resource model.
	* Can be overridden per event using the `color` property on the event model.
	* @default "teal"
	*/
	eventColor: import_prop_types.default.oneOf([
		"amber",
		"blue",
		"green",
		"grey",
		"indigo",
		"lime",
		"orange",
		"pink",
		"purple",
		"red",
		"teal"
	]),
	/**
	* Configures how events are created.
	* If `false`, event creation is disabled.
	* If `true`, event creation is enabled with default configuration.
	* If an object, event creation is enabled with the provided configuration.
	*/
	eventCreation: import_prop_types.default.oneOfType([import_prop_types.default.shape({
		duration: import_prop_types.default.number,
		interaction: import_prop_types.default.oneOf(["click", "double-click"])
	}), import_prop_types.default.bool]),
	/**
	* The structure of the event model.
	* It defines how to read and write the properties of the event model.
	* If not provided, the event model is assumed to match the `CalendarEvent` interface.
	*/
	eventModelStructure: import_prop_types.default.object,
	/**
	* The events currently available in the calendar.
	* @default []
	*/
	events: import_prop_types.default.arrayOf(import_prop_types.default.object),
	/**
	* Callback fired when some event of the calendar change.
	*/
	onEventsChange: import_prop_types.default.func,
	/**
	* Event handler called when the preferences change.
	*/
	onPreferencesChange: import_prop_types.default.func,
	/**
	* Event handler called when the view changes.
	*/
	onViewChange: import_prop_types.default.func,
	/**
	* Event handler called when the visible date changes.
	*/
	onVisibleDateChange: import_prop_types.default.func,
	/**
	* Event handler called when the visible resources change.
	*/
	onVisibleResourcesChange: import_prop_types.default.func,
	/**
	* Preferences currently displayed in the calendar.
	*/
	preferences: import_prop_types.default.shape({
		ampm: import_prop_types.default.bool,
		isSidePanelOpen: import_prop_types.default.bool,
		showEmptyDaysInAgenda: import_prop_types.default.bool,
		showWeekends: import_prop_types.default.bool,
		showWeekNumber: import_prop_types.default.bool,
		weekStartsOn: import_prop_types.default.oneOf([
			0,
			1,
			2,
			3,
			4,
			5,
			6
		])
	}),
	/**
	* Config of the preferences menu.
	* Defines which options are visible in the menu.
	* If `false`, the menu will be entirely hidden.
	* @default { toggleWeekendVisibility: true, toggleWeekNumberVisibility: true, toggleAmpm: true, toggleEmptyDaysInAgenda: true, toggleWeekStartsOn: false }
	*/
	preferencesMenuConfig: import_prop_types.default.oneOfType([import_prop_types.default.oneOf([false]), import_prop_types.default.shape({
		toggleAmpm: import_prop_types.default.bool,
		toggleEmptyDaysInAgenda: import_prop_types.default.bool,
		toggleWeekendVisibility: import_prop_types.default.bool,
		toggleWeekNumberVisibility: import_prop_types.default.bool,
		toggleWeekStartsOn: import_prop_types.default.bool
	})]),
	/**
	* Whether the calendar is in read-only mode.
	* @default false
	*/
	readOnly: import_prop_types.default.bool,
	/**
	* The structure of the resource model.
	* It defines how to read and write the properties of the resource model.
	* If not provided, the resource model is assumed to match the `CalendarResource` interface.
	*/
	resourceModelStructure: import_prop_types.default.object,
	/**
	* The resources the events can be assigned to.
	*/
	resources: import_prop_types.default.arrayOf(import_prop_types.default.object),
	/**
	* Whether each event must be assigned to a resource. When true, the resource cannot be cleared in the edit dialog and the form cannot be submitted without one.
	* @default false
	*/
	shouldEventRequireResource: import_prop_types.default.bool,
	/**
	* Whether the component should display the current time indicator.
	* @default true
	*/
	showCurrentTimeIndicator: import_prop_types.default.bool,
	/**
	* The system prop that allows defining system overrides as well as additional CSS styles.
	*/
	sx: import_prop_types.default.oneOfType([
		import_prop_types.default.arrayOf(import_prop_types.default.oneOfType([
			import_prop_types.default.func,
			import_prop_types.default.object,
			import_prop_types.default.bool
		])),
		import_prop_types.default.func,
		import_prop_types.default.object
	]),
	/**
	* The view currently displayed in the calendar.
	*/
	view: import_prop_types.default.oneOf([
		"agenda",
		"day",
		"month",
		"week"
	]),
	/**
	* Configuration applied to the view, keyed by the view name.
	* The `month` view does not support any configuration keys yet.
	*/
	viewConfig: import_prop_types.default.object,
	/**
	* The views available in the calendar.
	* @default ["day", "week", "month", "agenda"]
	*/
	views: import_prop_types.default.arrayOf(import_prop_types.default.oneOf([
		"agenda",
		"day",
		"month",
		"week"
	]).isRequired),
	/**
	* The date currently used to determine the visible date range.
	*/
	visibleDate: import_prop_types.default.instanceOf(Date),
	/**
	* The IDs of the resources currently visible.
	* A resource is visible if it is not included in this object or if it is included with `true` value.
	*/
	visibleResources: import_prop_types.default.object
};
//#endregion
//#region node_modules/@mui/icons-material/ChevronLeft.mjs
var ChevronLeft_default = createSvgIcon(/*#__PURE__*/ (0, import_jsx_runtime.jsx)("path", { d: "M15.41 7.41 14 6l-6 6 6 6 1.41-1.41L10.83 12z" }), "ChevronLeft");
//#endregion
//#region node_modules/@mui/icons-material/ChevronRight.mjs
var ChevronRight_default = createSvgIcon(/*#__PURE__*/ (0, import_jsx_runtime.jsx)("path", { d: "M10 6 8.59 7.41 13.17 12l-4.58 4.59L10 18l6-6z" }), "ChevronRight");
//#endregion
//#region node_modules/@mui/icons-material/MenuOpen.mjs
var MenuOpen_default = createSvgIcon(/*#__PURE__*/ (0, import_jsx_runtime.jsx)("path", { d: "M3 18h13v-2H3zm0-5h10v-2H3zm0-7v2h13V6zm18 9.59L17.42 12 21 8.41 19.59 7l-5 5 5 5z" }), "MenuOpen");
//#endregion
//#region node_modules/@mui/icons-material/Today.mjs
var Today_default = createSvgIcon(/*#__PURE__*/ (0, import_jsx_runtime.jsx)("path", { d: "M19 3h-1V1h-2v2H8V1H6v2H5c-1.11 0-1.99.9-1.99 2L3 19c0 1.1.89 2 2 2h14c1.1 0 2-.9 2-2V5c0-1.1-.9-2-2-2m0 16H5V8h14zM7 10h5v5H7z" }), "Today");
//#endregion
//#region node_modules/@mui/icons-material/ExpandMoreOutlined.mjs
var ExpandMoreOutlined_default = createSvgIcon(/*#__PURE__*/ (0, import_jsx_runtime.jsx)("path", { d: "M16.59 8.59 12 13.17 7.41 8.59 6 10l6 6 6-6z" }), "ExpandMoreOutlined");
//#endregion
//#region node_modules/@mui/x-scheduler/event-calendar/header-toolbar/view-switcher/ViewSwitcher.mjs
var _excluded$11 = [
	"views",
	"onViewChange",
	"view",
	"className"
];
var ViewSwitcherRoot = styled("div", {
	name: "MuiEventCalendar",
	slot: "ViewSwitcher"
})({ display: "flex" });
var ViewSwitcher = /*#__PURE__*/ import_react.forwardRef(function ViewSwitcher(props, forwardedRef) {
	const { views, onViewChange, view, className } = props, other = _objectWithoutPropertiesLoose(props, _excluded$11);
	const handleRef = useMergedRefs(forwardedRef, import_react.useRef(null));
	const { schedulerId, classes, localeText } = useEventCalendarStyledContext();
	const [anchorEl, setAnchorEl] = import_react.useState(null);
	const open = Boolean(anchorEl);
	const handleMenuOpen = (event) => {
		setAnchorEl(event.currentTarget);
	};
	const handleMenuClose = () => {
		setAnchorEl(null);
	};
	const handleMenuItemClick = (event, newView) => {
		onViewChange(newView, event.nativeEvent);
		handleMenuClose();
	};
	return /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(ViewSwitcherRoot, _extends({ ref: handleRef }, other, {
		className: clsx(className, classes.viewSwitcher),
		children: [/*#__PURE__*/ (0, import_jsx_runtime.jsx)(Button$1, {
			className: classes.viewSwitcherButton,
			size: "medium",
			id: `${schedulerId}-view-switcher-button`,
			"aria-controls": open ? `${schedulerId}-view-switcher-menu` : void 0,
			"aria-haspopup": "true",
			"aria-expanded": open ? "true" : void 0,
			onClick: handleMenuOpen,
			endIcon: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ExpandMoreOutlined_default, {}),
			children: localeText[view]
		}), /*#__PURE__*/ (0, import_jsx_runtime.jsx)(Menu, {
			className: classes.viewSwitcherMenu,
			id: `${schedulerId}-view-switcher-menu`,
			anchorEl,
			open,
			onClose: handleMenuClose,
			slotProps: { list: {
				"aria-labelledby": `${schedulerId}-view-switcher-button`,
				role: "listbox"
			} },
			children: views.map((viewItem) => /*#__PURE__*/ (0, import_jsx_runtime.jsx)(MenuItem, {
				selected: view === viewItem,
				"aria-selected": view === viewItem,
				onClick: (event) => handleMenuItemClick(event, viewItem),
				children: localeText[viewItem]
			}, viewItem))
		})]
	}));
});
ViewSwitcher.displayName = "ViewSwitcher";
//#endregion
//#region node_modules/@mui/icons-material/SettingsOutlined.mjs
var SettingsOutlined_default = createSvgIcon(/*#__PURE__*/ (0, import_jsx_runtime.jsx)("path", { d: "M19.43 12.98c.04-.32.07-.64.07-.98s-.03-.66-.07-.98l2.11-1.65c.19-.15.24-.42.12-.64l-2-3.46c-.09-.16-.26-.25-.44-.25-.06 0-.12.01-.17.03l-2.49 1c-.52-.4-1.08-.73-1.69-.98l-.38-2.65C14.46 2.18 14.25 2 14 2h-4c-.25 0-.46.18-.49.42l-.38 2.65c-.61.25-1.17.59-1.69.98l-2.49-1q-.09-.03-.18-.03c-.17 0-.34.09-.43.25l-2 3.46c-.13.22-.07.49.12.64l2.11 1.65c-.04.32-.07.65-.07.98s.03.66.07.98l-2.11 1.65c-.19.15-.24.42-.12.64l2 3.46c.09.16.26.25.44.25.06 0 .12-.01.17-.03l2.49-1c.52.4 1.08.73 1.69.98l.38 2.65c.03.24.24.42.49.42h4c.25 0 .46-.18.49-.42l.38-2.65c.61-.25 1.17-.59 1.69-.98l2.49 1q.09.03.18.03c.17 0 .34-.09.43-.25l2-3.46c.12-.22.07-.49-.12-.64zm-1.98-1.71c.04.31.05.52.05.73s-.02.43-.05.73l-.14 1.13.89.7 1.08.84-.7 1.21-1.27-.51-1.04-.42-.9.68c-.43.32-.84.56-1.25.73l-1.06.43-.16 1.13-.2 1.35h-1.4l-.19-1.35-.16-1.13-1.06-.43c-.43-.18-.83-.41-1.23-.71l-.91-.7-1.06.43-1.27.51-.7-1.21 1.08-.84.89-.7-.14-1.13c-.03-.31-.05-.54-.05-.74s.02-.43.05-.73l.14-1.13-.89-.7-1.08-.84.7-1.21 1.27.51 1.04.42.9-.68c.43-.32.84-.56 1.25-.73l1.06-.43.16-1.13.2-1.35h1.39l.19 1.35.16 1.13 1.06.43c.43.18.83.41 1.23.71l.91.7 1.06-.43 1.27-.51.7 1.21-1.07.85-.89.7zM12 8c-2.21 0-4 1.79-4 4s1.79 4 4 4 4-1.79 4-4-1.79-4-4-4m0 6c-1.1 0-2-.9-2-2s.9-2 2-2 2 .9 2 2-.9 2-2 2" }), "SettingsOutlined");
//#endregion
//#region node_modules/@mui/x-scheduler/event-calendar/header-toolbar/preferences-menu/PreferencesMenuItems.mjs
var PreferencesMenuListItemIcon = styled(ListItemIcon, {
	name: "MuiEventCalendar",
	slot: "PreferencesMenuListItemIcon"
})({
	justifyContent: "flex-end",
	"&[data-checked=\"false\"]": { visibility: "hidden" }
});
var PreferencesMenuListSubheader = styled(ListSubheader, {
	name: "MuiEventCalendar",
	slot: "PreferencesMenuListSubheader"
})(({ theme }) => _extends({}, theme.typography.button, {
	color: (theme.vars || theme).palette.text.disabled,
	paddingBottom: theme.spacing(1),
	backgroundColor: "transparent"
}));
/**
* Computes which preference options are visible from the menu config and current view.
* Shared by the expanded-layout `PreferencesMenu` and the compact-layout `SidePanelDrawer`.
*/
function usePreferencesMenuModel() {
	const store = useEventCalendarStoreContext();
	const { localeText } = useEventCalendarStyledContext();
	const currentView = useStore$1(store, eventCalendarViewSelectors.view);
	const preferencesMenuConfig = useStore$1(store, eventCalendarPreferenceSelectors.menuConfig);
	const preferenceOptions = [{
		configKey: "toggleWeekendVisibility",
		preferenceKey: "showWeekends",
		label: localeText.showWeekends
	}, {
		configKey: "toggleWeekNumberVisibility",
		preferenceKey: "showWeekNumber",
		label: localeText.showWeekNumber
	}];
	const viewSpecificPreferenceOptions = [{
		configKey: "toggleEmptyDaysInAgenda",
		preferenceKey: "showEmptyDaysInAgenda",
		view: "agenda",
		label: localeText.showEmptyDaysInAgenda
	}];
	if (preferencesMenuConfig === false) return {
		currentView,
		visibleOptions: [],
		visibleViewSpecificOptions: [],
		showTimeFormatSubmenu: false,
		showWeekStartsOnSubmenu: false,
		showSpecificOptions: false,
		hasAnyOption: false
	};
	const visibleOptions = preferenceOptions.filter((option) => preferencesMenuConfig?.[option.configKey] !== false);
	const visibleViewSpecificOptions = viewSpecificPreferenceOptions.filter((option) => preferencesMenuConfig?.[option.configKey] !== false && option.view === currentView);
	const showSpecificOptions = visibleViewSpecificOptions.length > 0;
	const showTimeFormatSubmenu = preferencesMenuConfig?.toggleAmpm !== false;
	const showWeekStartsOnSubmenu = preferencesMenuConfig?.toggleWeekStartsOn !== false;
	return {
		currentView,
		visibleOptions,
		visibleViewSpecificOptions,
		showTimeFormatSubmenu,
		showWeekStartsOnSubmenu,
		showSpecificOptions,
		hasAnyOption: showTimeFormatSubmenu || showWeekStartsOnSubmenu || visibleOptions.length > 0 || showSpecificOptions
	};
}
/**
* Builds the preference options as a flat array of elements, so they spread into a
* `Menu` (expanded layout) or `MenuList` (drawer). `hasAnyOption` is `false` when none show.
*/
function usePreferencesMenuItems() {
	const store = useEventCalendarStoreContext();
	const { classes, localeText } = useEventCalendarStyledContext();
	const preferences = useStore$1(store, eventCalendarPreferenceSelectors.all);
	const { currentView, visibleOptions, visibleViewSpecificOptions, showTimeFormatSubmenu, showWeekStartsOnSubmenu, showSpecificOptions, hasAnyOption } = usePreferencesMenuModel();
	const handleToggle = (key, checked, event) => {
		store.setPreferences({ [key]: checked }, event);
	};
	const handleTimeFormatChange = (value, event) => {
		store.setPreferences({ ampm: value === "12" }, event);
	};
	const handleWeekStartsOnChange = (value, event) => {
		store.setPreferences({ weekStartsOn: value }, event);
	};
	const items = [];
	if (!hasAnyOption) return {
		hasAnyOption,
		items
	};
	const showDividerBeforeTimeFormat = showTimeFormatSubmenu && visibleOptions.length > 0;
	const showDividerBeforeWeekStartsOn = showWeekStartsOnSubmenu && (visibleOptions.length > 0 || showTimeFormatSubmenu);
	const showDividerBeforeSpecificOptions = showSpecificOptions && (visibleOptions.length > 0 || showTimeFormatSubmenu || showWeekStartsOnSubmenu);
	const renderCheckableItem = (key, role, checked, label, onClick) => /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(MenuItem, {
		className: classes.preferencesMenuItem,
		role,
		"aria-checked": checked,
		onClick,
		children: [/*#__PURE__*/ (0, import_jsx_runtime.jsx)(ListItemText, {
			className: classes.preferencesMenuListItemText,
			children: label
		}), /*#__PURE__*/ (0, import_jsx_runtime.jsx)(PreferencesMenuListItemIcon, {
			className: classes.preferencesMenuListItemIcon,
			"data-checked": checked,
			children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(Check_default, { fontSize: "small" })
		})]
	}, key);
	renderCheckableItem.displayName = "renderCheckableItem";
	visibleOptions.forEach((option) => {
		items.push(renderCheckableItem(option.configKey, "menuitemcheckbox", !!preferences[option.preferenceKey], option.label, (event) => handleToggle(option.preferenceKey, !preferences[option.preferenceKey], event.nativeEvent)));
	});
	if (showDividerBeforeTimeFormat) items.push(/*#__PURE__*/ (0, import_jsx_runtime.jsx)(Divider, { className: classes.preferencesMenuDivider }, "divider-time-format"));
	if (showTimeFormatSubmenu) items.push(/*#__PURE__*/ (0, import_jsx_runtime.jsx)(PreferencesMenuListSubheader, {
		className: classes.preferencesMenuListSubheader,
		children: localeText.timeFormat
	}, "subheader-time-format"), renderCheckableItem("ampm-12", "menuitemradio", !!preferences.ampm, localeText.amPm12h, (event) => handleTimeFormatChange("12", event.nativeEvent)), renderCheckableItem("ampm-24", "menuitemradio", !preferences.ampm, localeText.hour24h, (event) => handleTimeFormatChange("24", event.nativeEvent)));
	if (showDividerBeforeWeekStartsOn) items.push(/*#__PURE__*/ (0, import_jsx_runtime.jsx)(Divider, { className: classes.preferencesMenuDivider }, "divider-week-start"));
	if (showWeekStartsOnSubmenu) {
		items.push(/*#__PURE__*/ (0, import_jsx_runtime.jsx)(PreferencesMenuListSubheader, {
			className: classes.preferencesMenuListSubheader,
			children: localeText.startWeekOn
		}, "subheader-week-start"));
		[
			{
				value: 0,
				label: localeText.weekdaySunday
			},
			{
				value: 1,
				label: localeText.weekdayMonday
			},
			{
				value: 6,
				label: localeText.weekdaySaturday
			}
		].forEach(({ value, label }) => {
			items.push(renderCheckableItem(`week-start-${value}`, "menuitemradio", preferences.weekStartsOn === value, label, (event) => handleWeekStartsOnChange(value, event.nativeEvent)));
		});
	}
	if (showDividerBeforeSpecificOptions) items.push(/*#__PURE__*/ (0, import_jsx_runtime.jsx)(Divider, { className: classes.preferencesMenuDivider }, "divider-specific"));
	if (showSpecificOptions) {
		items.push(/*#__PURE__*/ (0, import_jsx_runtime.jsx)(PreferencesMenuListSubheader, {
			className: classes.preferencesMenuListSubheader,
			children: localeText.viewSpecificOptions(currentView)
		}, "subheader-specific"));
		visibleViewSpecificOptions.forEach((option) => {
			items.push(renderCheckableItem(option.configKey, "menuitemcheckbox", !!preferences[option.preferenceKey], option.label, (event) => handleToggle(option.preferenceKey, !preferences[option.preferenceKey], event.nativeEvent)));
		});
	}
	return {
		hasAnyOption,
		items
	};
}
//#endregion
//#region node_modules/@mui/x-scheduler/event-calendar/header-toolbar/preferences-menu/PreferencesMenu.mjs
var PreferencesMenuRoot = styled("div", {
	name: "MuiEventCalendar",
	slot: "PreferencesMenu"
})({});
var PreferencesMenu = /*#__PURE__*/ import_react.forwardRef(function PreferencesMenu(props, forwardedRef) {
	const { schedulerId, classes, localeText } = useEventCalendarStyledContext();
	const containerRef = import_react.useRef(null);
	const handleRef = useMergedRefs(forwardedRef, containerRef);
	const { hasAnyOption, items } = usePreferencesMenuItems();
	const [anchorEl, setAnchorEl] = import_react.useState(null);
	const open = Boolean(anchorEl);
	const handleClick = (event) => {
		setAnchorEl(event.currentTarget);
	};
	const handleClose = () => {
		setAnchorEl(null);
	};
	if (!hasAnyOption) return null;
	return /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(PreferencesMenuRoot, _extends({ ref: handleRef }, props, {
		className: clsx(props.className, classes.preferencesMenu),
		children: [/*#__PURE__*/ (0, import_jsx_runtime.jsx)(IconButton, {
			className: classes.preferencesMenuButton,
			"aria-label": localeText.preferencesMenu,
			onClick: handleClick,
			"aria-controls": open ? `${schedulerId}-preferences-menu` : void 0,
			"aria-haspopup": "true",
			"aria-expanded": open ? "true" : void 0,
			children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(SettingsOutlined_default, {})
		}), /*#__PURE__*/ (0, import_jsx_runtime.jsx)(Menu, {
			className: classes.preferencesMenuList,
			id: `${schedulerId}-preferences-menu`,
			anchorEl,
			open,
			onClose: handleClose,
			container: containerRef.current,
			anchorOrigin: {
				vertical: "bottom",
				horizontal: "right"
			},
			transformOrigin: {
				vertical: "top",
				horizontal: "right"
			},
			slotProps: {
				list: { "aria-label": localeText.preferencesMenu },
				paper: { sx: { minWidth: 220 } }
			},
			children: items
		})]
	}));
});
PreferencesMenu.displayName = "PreferencesMenu";
//#endregion
//#region node_modules/@mui/x-scheduler/event-calendar/header-toolbar/HeaderToolbar.mjs
var _excluded$10 = ["onCompactMenuClick", "className"];
var HeaderToolbarRoot = styled("header", {
	name: "MuiEventCalendar",
	slot: "HeaderToolbar"
})(({ theme }) => ({
	display: "flex",
	gap: theme.spacing(2),
	justifyContent: "space-between",
	alignItems: "center",
	width: "100%",
	flexWrap: "wrap"
}));
var HeaderToolbarActions = styled("div", {
	name: "MuiEventCalendar",
	slot: "HeaderToolbarActions"
})(({ theme }) => ({
	display: "flex",
	gap: theme.spacing(2)
}));
var HeaderToolbarDateNavigator = styled("div", {
	name: "MuiEventCalendar",
	slot: "HeaderToolbarDateNavigator"
})(({ theme }) => ({
	display: "flex",
	gap: theme.spacing(2)
}));
var HeaderToolbarLeftElement = styled("div", {
	name: "MuiEventCalendar",
	slot: "HeaderToolbarLeftElement"
})(({ theme }) => ({
	display: "flex",
	alignItems: "center",
	justifyContent: "flex-start",
	gap: theme.spacing(2)
}));
var HeaderToolbarLabel = styled("p", {
	name: "MuiEventCalendar",
	slot: "HeaderToolbarLabel"
})(({ theme }) => _extends({ margin: 0 }, theme.typography.h6, {
	fontWeight: theme.typography.fontWeightBold,
	lineHeight: 1.4,
	display: "flex",
	alignItems: "center",
	gap: theme.spacing(1)
}));
var HeaderToolbarWeekNumber = styled("span", {
	name: "MuiEventCalendar",
	slot: "HeaderToolbarWeekNumber"
})(({ theme }) => _extends({
	padding: theme.spacing(.1, .7),
	borderRadius: theme.shape.borderRadius,
	fontSize: "0.875rem",
	fontWeight: theme.typography.fontWeightRegular,
	color: (theme.vars || theme).palette.text.primary,
	textAlign: "center",
	backgroundColor: (theme.vars || theme).palette.grey[200]
}, theme.applyStyles("dark", { backgroundColor: (theme.vars || theme).palette.grey[900] })));
var HeaderToolbar = /*#__PURE__*/ import_react.forwardRef(function HeaderToolbar(props, forwardedRef) {
	const store = useEventCalendarStoreContext();
	const { classes, localeText } = useEventCalendarStyledContext();
	const adapter = useAdapterContext();
	const views = useStore$1(store, eventCalendarViewSelectors.views);
	const view = useStore$1(store, eventCalendarViewSelectors.view);
	const visibleDate = useStore$1(store, schedulerOtherSelectors.visibleDate);
	const isSidePanelOpen = useStore$1(store, eventCalendarPreferenceSelectors.isSidePanelOpen);
	const showWeekNumber = useStore$1(store, eventCalendarPreferenceSelectors.showWeekNumber);
	const weekStartsOn = useStore$1(store, eventCalendarPreferenceSelectors.weekStartsOn);
	const { onCompactMenuClick, className } = props, other = _objectWithoutPropertiesLoose(props, _excluded$10);
	const weekNumber = getWeekNumber(adapter, visibleDate, weekStartsOn);
	const showViewSwitcher = views.length > 1;
	const showWeekLabel = showWeekNumber && (view === "week" || view === "day");
	return /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(HeaderToolbarRoot, _extends({
		ref: forwardedRef,
		"data-single-primary-action": !showViewSwitcher
	}, other, {
		className: clsx(className, classes.headerToolbar),
		children: [/*#__PURE__*/ (0, import_jsx_runtime.jsxs)(HeaderToolbarLeftElement, {
			className: classes.headerToolbarLeftElement,
			children: [
				/*#__PURE__*/ (0, import_jsx_runtime.jsx)(IconButton, {
					"data-compact-only": true,
					className: classes.headerToolbarCompactMenuButton,
					"aria-label": localeText.openMenu,
					onClick: onCompactMenuClick,
					children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(Menu_default, {})
				}),
				/*#__PURE__*/ (0, import_jsx_runtime.jsx)(IconButton, {
					"data-expanded-only": true,
					className: classes.headerToolbarSidePanelToggle,
					"aria-label": isSidePanelOpen ? localeText.closeSidePanel : localeText.openSidePanel,
					onClick: (event) => store.setPreferences({ isSidePanelOpen: !isSidePanelOpen }, event.nativeEvent),
					children: isSidePanelOpen ? /*#__PURE__*/ (0, import_jsx_runtime.jsx)(MenuOpen_default, {}) : /*#__PURE__*/ (0, import_jsx_runtime.jsx)(Menu_default, {})
				}),
				/*#__PURE__*/ (0, import_jsx_runtime.jsxs)(HeaderToolbarLabel, {
					"data-expanded-only": true,
					"aria-live": "polite",
					children: [
						adapter.format(visibleDate, "monthFullLetter"),
						" ",
						adapter.format(visibleDate, "yearPadded"),
						showWeekLabel && /*#__PURE__*/ (0, import_jsx_runtime.jsx)(HeaderToolbarWeekNumber, {
							className: classes.headerToolbarWeekNumber,
							children: `${localeText.week} ${weekNumber}`
						})
					]
				})
			]
		}), /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(HeaderToolbarActions, {
			className: classes.headerToolbarActions,
			children: [
				/*#__PURE__*/ (0, import_jsx_runtime.jsxs)(HeaderToolbarDateNavigator, {
					role: "navigation",
					className: classes.headerToolbarDateNavigator,
					children: [/*#__PURE__*/ (0, import_jsx_runtime.jsx)(IconButton, {
						className: classes.headerToolbarPreviousButton,
						onClick: store.goToPreviousVisibleDate,
						"aria-label": localeText.previousTimeSpan(view),
						children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ChevronLeft_default, {})
					}), /*#__PURE__*/ (0, import_jsx_runtime.jsx)(IconButton, {
						className: classes.headerToolbarNextButton,
						onClick: store.goToNextVisibleDate,
						"aria-label": localeText.nextTimeSpan(view),
						children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ChevronRight_default, {})
					})]
				}),
				/*#__PURE__*/ (0, import_jsx_runtime.jsx)(IconButton, {
					"data-compact-only": true,
					className: classes.headerToolbarCompactTodayButton,
					onClick: store.goToToday,
					"aria-label": localeText.today,
					children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(Today_default, {})
				}),
				/*#__PURE__*/ (0, import_jsx_runtime.jsx)(Button$1, {
					"data-expanded-only": true,
					className: classes.headerToolbarTodayButton,
					onClick: store.goToToday,
					children: localeText.today
				}),
				showViewSwitcher && /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ViewSwitcher, {
					"data-expanded-only": true,
					views,
					view,
					onViewChange: store.setView
				}),
				/*#__PURE__*/ (0, import_jsx_runtime.jsx)(PreferencesMenu, { "data-expanded-only": true })
			]
		})]
	}));
});
HeaderToolbar.displayName = "HeaderToolbar";
//#endregion
//#region node_modules/@mui/x-tree-view/RichTreeView/richTreeViewClasses.mjs
function getRichTreeViewUtilityClass(slot) {
	return generateUtilityClass("MuiRichTreeView", slot);
}
generateUtilityClasses("MuiRichTreeView", [
	"root",
	"item",
	"itemContent",
	"itemGroupTransition",
	"itemIconContainer",
	"itemLabel",
	"itemCheckbox",
	"itemLabelInput"
]);
//#endregion
//#region node_modules/@mui/x-tree-view/internals/zero-styled/index.mjs
function createUseThemeProps(name) {
	return useThemeProps$2;
}
//#endregion
//#region node_modules/@mui/x-tree-view/internals/TreeViewProvider/TreeViewContext.mjs
/**
* @ignore - internal component.
*/
var TreeViewContext = /*#__PURE__*/ import_react.createContext(null);
TreeViewContext.displayName = "TreeViewContext";
var useTreeViewContext = () => {
	const context = import_react.useContext(TreeViewContext);
	if (context == null) throw new Error("MUI X Tree View: Could not find the Tree View context. This happens when a component is rendered outside of a SimpleTreeView or RichTreeView parent component. Ensure your component is a child of a Tree View component. This can also happen if you are bundling multiple versions of the Tree View.");
	return context;
};
//#endregion
//#region node_modules/@mui/x-tree-view/internals/TreeViewProvider/TreeViewStyleContext.mjs
/**
* @ignore - internal component.
*/
var TreeViewStyleContext = /*#__PURE__*/ import_react.createContext({
	classes: {},
	slots: {},
	slotProps: {}
});
TreeViewStyleContext.displayName = "TreeViewStyleContext";
var useTreeViewStyleContext = () => {
	return import_react.useContext(TreeViewStyleContext);
};
//#endregion
//#region node_modules/@mui/x-tree-view/internals/TreeViewProvider/useTreeViewBuildContext.mjs
var useTreeViewBuildContext = (parameters) => {
	const { store, apiRef, rootRef } = parameters;
	const publicAPI = useRefWithInit(() => store.buildPublicAPI()).current;
	initializeInputApiRef(publicAPI, apiRef);
	const runItemPlugins = import_react.useCallback((itemPluginProps) => {
		let finalRootRef = null;
		let finalContentRef = null;
		const pluginPropEnhancers = [];
		const pluginPropEnhancersNames = {};
		store.itemPluginManager.listPlugins().forEach((itemPlugin) => {
			const itemPluginResponse = itemPlugin({
				props: itemPluginProps,
				rootRef: finalRootRef,
				contentRef: finalContentRef
			});
			if (itemPluginResponse?.rootRef) finalRootRef = itemPluginResponse.rootRef;
			if (itemPluginResponse?.contentRef) finalContentRef = itemPluginResponse.contentRef;
			if (itemPluginResponse?.propsEnhancers) {
				pluginPropEnhancers.push(itemPluginResponse.propsEnhancers);
				Object.keys(itemPluginResponse.propsEnhancers).forEach((propsEnhancerName) => {
					pluginPropEnhancersNames[propsEnhancerName] = true;
				});
			}
		});
		const resolvePropsEnhancer = (currentSlotName) => (currentSlotParams) => {
			const enhancedProps = {};
			pluginPropEnhancers.forEach((propsEnhancersForCurrentPlugin) => {
				const propsEnhancerForCurrentPluginAndSlot = propsEnhancersForCurrentPlugin[currentSlotName];
				if (propsEnhancerForCurrentPluginAndSlot != null) Object.assign(enhancedProps, propsEnhancerForCurrentPluginAndSlot(currentSlotParams));
			});
			return enhancedProps;
		};
		const propsEnhancers = Object.fromEntries(Object.keys(pluginPropEnhancersNames).map((propEnhancerName) => [propEnhancerName, resolvePropsEnhancer(propEnhancerName)]));
		return {
			contentRef: finalContentRef,
			rootRef: finalRootRef,
			propsEnhancers
		};
	}, [store]);
	const wrapItem = import_react.useCallback(({ itemId, children, idAttribute }) => {
		let finalChildren = children;
		const itemsWrapper = store.itemPluginManager.listWrappers();
		for (let i = itemsWrapper.length - 1; i >= 0; i -= 1) {
			const itemWrapper = itemsWrapper[i];
			finalChildren = itemWrapper({
				store,
				itemId,
				children: finalChildren,
				idAttribute
			});
		}
		return finalChildren;
	}, [store]);
	return import_react.useMemo(() => ({
		runItemPlugins,
		wrapItem,
		publicAPI,
		store,
		rootRef
	}), [
		runItemPlugins,
		wrapItem,
		publicAPI,
		store,
		rootRef
	]);
};
function initializeInputApiRef(publicAPI, apiRef) {
	if (apiRef != null && apiRef.current !== publicAPI) apiRef.current = publicAPI;
}
//#endregion
//#region node_modules/@mui/x-tree-view/internals/TreeViewProvider/TreeViewProvider.mjs
/**
* Sets up the contexts for the underlying Tree Item components.
*
* @ignore - do not document.
*/
function TreeViewProvider(props) {
	const { store, apiRef, rootRef, classes = EMPTY_OBJECT, slots = EMPTY_OBJECT, slotProps = EMPTY_OBJECT, children } = props;
	const contextValue = useTreeViewBuildContext({
		store,
		apiRef,
		rootRef
	});
	const styleContextValue = import_react.useMemo(() => ({
		classes,
		slots: {
			collapseIcon: slots.collapseIcon,
			expandIcon: slots.expandIcon,
			endIcon: slots.endIcon
		},
		slotProps: {
			collapseIcon: slotProps.collapseIcon,
			expandIcon: slotProps.expandIcon,
			endIcon: slotProps.endIcon
		}
	}), [
		classes,
		slots.collapseIcon,
		slots.expandIcon,
		slots.endIcon,
		slotProps.collapseIcon,
		slotProps.expandIcon,
		slotProps.endIcon
	]);
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(TreeViewContext.Provider, {
		value: contextValue,
		children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(TreeViewStyleContext.Provider, {
			value: styleContextValue,
			children
		})
	});
}
var buildSiblingIndexes = (siblings) => {
	const siblingsIndexLookup = {};
	siblings.forEach((childId, index) => {
		siblingsIndexLookup[childId] = index;
	});
	return siblingsIndexLookup;
};
/**
* Check if an item is disabled.
* This method should only be used in selectors that are checking if several items are disabled.
* Otherwise, use the `itemsSelector.isItemDisabled` selector.
* @returns
*/
var isItemDisabled = (itemMetaLookup, itemId) => {
	if (itemId == null) return false;
	let itemMeta = itemMetaLookup[itemId];
	if (!itemMeta) return false;
	if (itemMeta.disabled) return true;
	while (itemMeta.parentId != null) {
		itemMeta = itemMetaLookup[itemMeta.parentId];
		if (!itemMeta) return false;
		if (itemMeta.disabled) return true;
	}
	return false;
};
function buildItemsLookups(parameters) {
	const { storeParameters, items, parentId, depth, isItemExpandable, otherItemsMetaLookup } = parameters;
	const metaLookup = {};
	const modelLookup = {};
	const orderedChildrenIds = [];
	const itemsChildren = [];
	const processItem = (item) => {
		const id = storeParameters.getItemId ? storeParameters.getItemId(item) : item.id;
		checkId({
			id,
			parentId,
			item,
			itemMetaLookup: otherItemsMetaLookup,
			siblingsMetaLookup: metaLookup
		});
		const label = storeParameters.getItemLabel ? storeParameters.getItemLabel(item) : item.label;
		if (label == null) throw new Error(`MUI X Tree View: All items must have a \`label\` property. You can use the \`getItemLabel\` prop to specify a custom label for each item. An item was provided without a label: ${JSON.stringify(item)}`);
		const children = (storeParameters.getItemChildren ? storeParameters.getItemChildren(item) : item.children) || [];
		itemsChildren.push({
			id,
			children
		});
		modelLookup[id] = item;
		metaLookup[id] = {
			id,
			label,
			parentId,
			idAttribute: void 0,
			expandable: isItemExpandable(item, children),
			disabled: storeParameters.isItemDisabled ? storeParameters.isItemDisabled(item) : false,
			selectable: storeParameters.isItemSelectionDisabled ? !storeParameters.isItemSelectionDisabled(item) : true,
			depth
		};
		orderedChildrenIds.push(id);
	};
	for (const item of items) processItem(item);
	return {
		metaLookup,
		modelLookup,
		orderedChildrenIds,
		childrenIndexes: buildSiblingIndexes(orderedChildrenIds),
		itemsChildren
	};
}
function checkId({ id, parentId, item, itemMetaLookup, siblingsMetaLookup }) {
	if (id == null) throw new Error(`MUI X Tree View: All items must have a unique \`id\` property. You can use the \`getItemId\` prop to specify a custom id for each item. An item was provided without an id: ${JSON.stringify(item)}`);
	if (siblingsMetaLookup[id] != null || itemMetaLookup[id] != null && itemMetaLookup[id].parentId !== parentId) throw new Error(`MUI X Tree View: All items must have a unique \`id\` property. The id "${id}" is used by multiple items. Use the \`getItemId\` prop to specify a custom id for each item if needed.`);
}
//#endregion
//#region node_modules/@mui/x-tree-view/internals/plugins/items/selectors.mjs
var EMPTY_CHILDREN = [];
var itemsSelectors = {
	/**
	* Gets the DOM structure of the Tree View.
	*/
	domStructure: createSelector((state) => state.domStructure),
	/**
	* Checks whether the disabled items are focusable.
	*/
	disabledItemFocusable: createSelector((state) => state.disabledItemsFocusable),
	/**
	* Gets the meta-information of all items.
	*/
	itemMetaLookup: createSelector((state) => state.itemMetaLookup),
	/**
	* Gets the ordered children ids of all items.
	*/
	itemOrderedChildrenIdsLookup: createSelector((state) => state.itemOrderedChildrenIdsLookup),
	/**
	* Gets the meta-information of an item.
	*/
	itemMeta: createSelector((state, itemId) => state.itemMetaLookup[itemId ?? "__TREE_VIEW_ROOT_PARENT_ID__"] ?? null),
	/**
	* Gets the ordered children ids of an item.
	*/
	itemOrderedChildrenIds: createSelector((state, itemId) => state.itemOrderedChildrenIdsLookup[itemId ?? "__TREE_VIEW_ROOT_PARENT_ID__"] ?? EMPTY_CHILDREN),
	/**
	* Gets the model of an item.
	*/
	itemModel: createSelector((state, itemId) => state.itemModelLookup[itemId]),
	/**
	* Checks whether an item is disabled.
	*/
	isItemDisabled: createSelector((state, itemId) => isItemDisabled(state.itemMetaLookup, itemId)),
	/**
	* Gets the index of an item in its parent's children.
	*/
	itemIndex: createSelector((state, itemId) => {
		const itemMeta = state.itemMetaLookup[itemId];
		if (itemMeta == null) return -1;
		return state.itemChildrenIndexesLookup[itemMeta.parentId ?? "__TREE_VIEW_ROOT_PARENT_ID__"][itemMeta.id];
	}),
	/**
	* Gets the id of an item's parent.
	*/
	itemParentId: createSelector((state, itemId) => state.itemMetaLookup[itemId]?.parentId ?? null),
	/**
	* Gets the depth of an item (items at the root level have a depth of 0).
	*/
	itemDepth: createSelector((state, itemId) => state.itemMetaLookup[itemId]?.depth ?? 0),
	/**
	* Checks whether an item can be focused.
	*/
	canItemBeFocused: createSelector((state, itemId) => state.disabledItemsFocusable || state.itemModelLookup[itemId] != null && !isItemDisabled(state.itemMetaLookup, itemId)),
	/**
	* Gets the identation between an item and its children.
	*/
	itemChildrenIndentation: createSelector((state) => state.itemChildrenIndentation),
	/**
	* Gets the height of an individual item.
	*/
	itemHeight: createSelector((state) => state.itemHeight)
};
//#endregion
//#region node_modules/@mui/x-tree-view/internals/plugins/id/selectors.mjs
var treeIdSelector = createSelector((state) => state.providedTreeId ?? state.treeId);
var idSelectors = {
	/**
	* Get the id attribute of the tree view.
	*/
	treeId: treeIdSelector,
	/**
	* Generate the id attribute (i.e.: the `id` attribute passed to the DOM element) of a Tree Item.
	* If the user explicitly defined an id attribute, it will be returned.
	* Otherwise, the method creates a unique id for the item based on the Tree View id attribute and the item `itemId`
	*/
	treeItemIdAttribute: createSelector(treeIdSelector, (treeId, itemId, providedIdAttribute) => {
		if (providedIdAttribute != null) return providedIdAttribute;
		return `${treeId ?? ""}-${itemId}`;
	})
};
//#endregion
//#region node_modules/@mui/x-tree-view/internals/plugins/items/TreeViewItemsPlugin.mjs
var TreeViewItemsPlugin = class {
	constructor(store) {
		this.store = store;
	}
	/**
	* Determines if the items state should be rebuilt based on the new and previous parameters.
	*/
	static shouldRebuildItemsState = (newParameters, previousParameters) => {
		return [
			"items",
			"isItemDisabled",
			"isItemSelectionDisabled",
			"getItemId",
			"getItemLabel",
			"getItemChildren"
		].some((key) => {
			const typedKey = key;
			return newParameters[typedKey] !== previousParameters[typedKey];
		});
	};
	/**
	* Builds the state properties derived from the `items` prop.
	*/
	static buildItemsStateIfNeeded = (parameters) => {
		const itemMetaLookup = {};
		const itemModelLookup = {};
		const itemOrderedChildrenIdsLookup = {};
		const itemChildrenIndexesLookup = {};
		function processSiblings(items, parentId, depth) {
			const parentIdWithDefault = parentId ?? "__TREE_VIEW_ROOT_PARENT_ID__";
			const { metaLookup, modelLookup, orderedChildrenIds, childrenIndexes, itemsChildren } = buildItemsLookups({
				storeParameters: parameters,
				items,
				parentId,
				depth,
				isItemExpandable: (item, children) => !!children && children.length > 0,
				otherItemsMetaLookup: itemMetaLookup
			});
			Object.assign(itemMetaLookup, metaLookup);
			Object.assign(itemModelLookup, modelLookup);
			itemOrderedChildrenIdsLookup[parentIdWithDefault] = orderedChildrenIds;
			itemChildrenIndexesLookup[parentIdWithDefault] = childrenIndexes;
			for (const item of itemsChildren) processSiblings(item.children || [], item.id, depth + 1);
		}
		processSiblings(parameters.items, null, 0);
		return {
			itemMetaLookup,
			itemModelLookup,
			itemOrderedChildrenIdsLookup,
			itemChildrenIndexesLookup
		};
	};
	/**
	* Get the item with the given id.
	* When used in the Simple Tree View, it returns an object with the `id` and `label` properties.
	* @param {TreeViewItemId} itemId The id of the item to retrieve.
	* @returns {R} The item with the given id.
	*/
	getItem = (itemId) => itemsSelectors.itemModel(this.store.state, itemId);
	/**
	* Get all the items in the same format as provided by `props.items`.
	* @returns {R[]} The items in the tree.
	*/
	getItemTree = () => {
		const getItemFromItemId = (itemId) => {
			const itemToMutate = _extends({}, itemsSelectors.itemModel(this.store.state, itemId));
			const newChildren = itemsSelectors.itemOrderedChildrenIds(this.store.state, itemId);
			if (newChildren.length > 0) itemToMutate.children = newChildren.map(getItemFromItemId);
			else delete itemToMutate.children;
			return itemToMutate;
		};
		return itemsSelectors.itemOrderedChildrenIds(this.store.state, null).map(getItemFromItemId);
	};
	/**
	* Get the ids of a given item's children.
	* Those ids are returned in the order they should be rendered.
	* To get the root items, pass `null` as the `itemId`.
	* @param {TreeViewItemId | null} itemId The id of the item to get the children of.
	* @returns {TreeViewItemId[]} The ids of the item's children.
	*/
	getItemOrderedChildrenIds = (itemId) => itemsSelectors.itemOrderedChildrenIds(this.store.state, itemId);
	/** * Get the id of the parent item.
	* @param {TreeViewItemId} itemId The id of the item to whose parentId we want to retrieve.
	* @returns {TreeViewItemId | null} The id of the parent item.
	*/
	getParentId = (itemId) => {
		return itemsSelectors.itemMeta(this.store.state, itemId)?.parentId || null;
	};
	/**
	* Toggle the disabled state of the item with the given id.
	* @param {object} parameters The params of the method.
	* @param {TreeViewItemId } parameters.itemId The id of the item to get the children of.
	* @param {boolean } parameters.shouldBeDisabled true if the item should be disabled.
	*/
	setIsItemDisabled = ({ itemId, shouldBeDisabled }) => {
		if (!this.store.state.itemMetaLookup[itemId]) return;
		const itemMetaLookup = _extends({}, this.store.state.itemMetaLookup);
		itemMetaLookup[itemId] = _extends({}, itemMetaLookup[itemId], { disabled: shouldBeDisabled ?? !itemMetaLookup[itemId].disabled });
		this.store.set("itemMetaLookup", itemMetaLookup);
	};
	buildPublicAPI = () => {
		return {
			getItem: this.getItem,
			getItemDOMElement: this.getItemDOMElement,
			getItemOrderedChildrenIds: this.getItemOrderedChildrenIds,
			getItemTree: this.getItemTree,
			getParentId: this.getParentId,
			setIsItemDisabled: this.setIsItemDisabled
		};
	};
	/**
	* Get the DOM element of the item with the given id.
	* @param {TreeViewItemId} itemId The id of the item to get the DOM element of.
	* @returns {HTMLElement | null} The DOM element of the item with the given id.
	*/
	getItemDOMElement = (itemId) => {
		const itemMeta = itemsSelectors.itemMeta(this.store.state, itemId);
		if (itemMeta == null) return null;
		const idAttribute = idSelectors.treeItemIdAttribute(this.store.state, itemId, itemMeta.idAttribute);
		return document.getElementById(idAttribute);
	};
	/**
	* Add an array of items to the tree.
	* @param {SetItemChildrenParameters<R>} args The items to add to the tree and information about their ancestors.
	*/
	setItemChildren = ({ items, parentId, getChildrenCount }) => {
		const parentIdWithDefault = parentId ?? "__TREE_VIEW_ROOT_PARENT_ID__";
		const parentDepth = parentId == null ? -1 : itemsSelectors.itemDepth(this.store.state, parentId);
		const { metaLookup, modelLookup, orderedChildrenIds, childrenIndexes } = buildItemsLookups({
			storeParameters: this.store.parameters,
			items,
			parentId,
			depth: parentDepth + 1,
			isItemExpandable: getChildrenCount ? (item) => getChildrenCount(item) !== 0 : () => false,
			otherItemsMetaLookup: itemsSelectors.itemMetaLookup(this.store.state)
		});
		this.store.update({
			itemModelLookup: _extends({}, this.store.state.itemModelLookup, modelLookup),
			itemMetaLookup: _extends({}, this.store.state.itemMetaLookup, metaLookup),
			itemOrderedChildrenIdsLookup: _extends({}, this.store.state.itemOrderedChildrenIdsLookup, { [parentIdWithDefault]: orderedChildrenIds }),
			itemChildrenIndexesLookup: _extends({}, this.store.state.itemChildrenIndexesLookup, { [parentIdWithDefault]: childrenIndexes })
		});
	};
	/**
	* Remove the children of an item.
	* @param {TreeViewItemId | null} parentId The id of the item to remove the children of.
	*/
	removeChildren = (parentId) => {
		const itemMetaLookup = this.store.state.itemMetaLookup;
		const newMetaMap = Object.keys(itemMetaLookup).reduce((acc, key) => {
			const item = itemMetaLookup[key];
			if (item.parentId === parentId) return acc;
			return _extends({}, acc, { [item.id]: item });
		}, {});
		const newItemOrderedChildrenIdsLookup = _extends({}, this.store.state.itemOrderedChildrenIdsLookup);
		const newItemChildrenIndexesLookup = _extends({}, this.store.state.itemChildrenIndexesLookup);
		const cleanId = parentId ?? "__TREE_VIEW_ROOT_PARENT_ID__";
		delete newItemChildrenIndexesLookup[cleanId];
		delete newItemOrderedChildrenIdsLookup[cleanId];
		this.store.update({
			itemMetaLookup: newMetaMap,
			itemOrderedChildrenIdsLookup: newItemOrderedChildrenIdsLookup,
			itemChildrenIndexesLookup: newItemChildrenIndexesLookup
		});
	};
	/**
	* Callback fired when the `content` slot of a given Tree Item is clicked.
	* @param {React.MouseEvent} event The DOM event that triggered the change.
	* @param {TreeViewItemId} itemId The id of the item being clicked.
	*/
	handleItemClick = (event, itemId) => {
		this.store.parameters.onItemClick?.(event, itemId);
	};
};
//#endregion
//#region node_modules/@mui/x-tree-view/internals/plugins/expansion/selectors.mjs
var expandedItemMapSelector = createSelectorMemoized((state) => state.expandedItems, (expandedItems) => {
	const expandedItemsMap = /* @__PURE__ */ new Map();
	expandedItems.forEach((id) => {
		expandedItemsMap.set(id, true);
	});
	return expandedItemsMap;
});
var expansionSelectors = {
	/**
	* Gets the expanded items as provided to the component.
	*/
	expandedItemsRaw: createSelector((state) => state.expandedItems),
	/**
	* Gets the expanded items as a Map.
	*/
	expandedItemsMap: expandedItemMapSelector,
	/**
	* Gets the items to render as a flat list (the descendants of an expanded item are listed as siblings of the item).
	*/
	flatList: createSelectorMemoized(itemsSelectors.itemOrderedChildrenIdsLookup, expandedItemMapSelector, (itemOrderedChildrenIds, expandedItemsMap) => {
		function appendChildren(itemId) {
			if (!expandedItemsMap.has(itemId)) return [itemId];
			const itemsWithDescendants = [itemId];
			const children = itemOrderedChildrenIds[itemId] || [];
			for (const childId of children) itemsWithDescendants.push(...appendChildren(childId));
			return itemsWithDescendants;
		}
		return (itemOrderedChildrenIds["__TREE_VIEW_ROOT_PARENT_ID__"] ?? []).flatMap(appendChildren);
	}),
	/**
	* Gets the slot that triggers the item's expansion when clicked.
	*/
	triggerSlot: createSelector((state) => state.expansionTrigger),
	/**
	* Checks whether an item is expanded.
	*/
	isItemExpanded: createSelector(expandedItemMapSelector, (expandedItemsMap, itemId) => expandedItemsMap.has(itemId)),
	/**
	* Checks whether an item is expandable.
	*/
	isItemExpandable: createSelector(itemsSelectors.itemMeta, (itemMeta, _itemId) => itemMeta?.expandable ?? false)
};
//#endregion
//#region node_modules/@mui/x-tree-view/internals/plugins/selection/selectors.mjs
var selectedItemsSelector = createSelectorMemoized((state) => state.selectedItems, (selectedItemsRaw) => {
	if (Array.isArray(selectedItemsRaw)) return selectedItemsRaw;
	if (selectedItemsRaw != null) return [selectedItemsRaw];
	return [];
});
var selectedItemsMapSelector = createSelectorMemoized(selectedItemsSelector, (selectedItems) => {
	const selectedItemsMap = /* @__PURE__ */ new Map();
	selectedItems.forEach((id) => {
		selectedItemsMap.set(id, true);
	});
	return selectedItemsMap;
});
var isItemSelectableSelector = createSelector((state, itemId) => state.itemMetaLookup[itemId]?.selectable ?? true);
var selectionSelectors = {
	/**
	* Gets the selected items as provided to the component.
	*/
	selectedItemsRaw: createSelector((state) => state.selectedItems),
	/**
	* Gets the selected items as an array.
	*/
	selectedItems: selectedItemsSelector,
	/**
	* Gets the selected items as a Map.
	*/
	selectedItemsMap: selectedItemsMapSelector,
	/**
	* Checks whether selection is enabled.
	*/
	enabled: createSelector((state) => !state.disableSelection),
	/**
	* Checks whether multi selection is enabled.
	*/
	isMultiSelectEnabled: createSelector((state) => state.multiSelect),
	/**
	* Checks whether checkbox selection is enabled.
	*/
	isCheckboxSelectionEnabled: createSelector((state) => state.checkboxSelection),
	/**
	* Gets the selection propagation rules.
	*/
	propagationRules: createSelector((state) => state.selectionPropagation),
	/**
	* Checks whether an item is selected.
	*/
	isItemSelected: createSelector(selectedItemsMapSelector, (selectedItemsMap, itemId) => selectedItemsMap.has(itemId)),
	/**
	* Checks whether the selection feature is enabled for an item.
	* Returns `true` when selection is enabled on the Tree View and the item is selectable (even if the item is disabled).
	*/
	isFeatureEnabledForItem: createSelector(isItemSelectableSelector, (state) => !state.disableSelection, (isItemSelectable, isSelectionEnabled, _itemId) => isSelectionEnabled && isItemSelectable),
	/**
	* Checks whether an item can be selected (if selection is enabled, if the item is not disabled, and if the item is selectable).
	*/
	canItemBeSelected: createSelector(itemsSelectors.isItemDisabled, isItemSelectableSelector, (state) => !state.disableSelection, (isItemDisabled, isItemSelectable, isSelectionEnabled, _itemId) => isSelectionEnabled && !isItemDisabled && isItemSelectable),
	/**
	* Checks whether an item is selectable based on the `isItemSelectionDisabled` prop.
	*/
	isItemSelectable: isItemSelectableSelector
};
//#endregion
//#region node_modules/@mui/x-tree-view/internals/plugins/focus/selectors.mjs
var defaultFocusableItemIdSelector = createSelectorMemoized(selectionSelectors.selectedItems, expansionSelectors.expandedItemsMap, itemsSelectors.itemMetaLookup, itemsSelectors.disabledItemFocusable, (state) => itemsSelectors.itemOrderedChildrenIds(state, null), (selectedItems, expandedItemsMap, itemMetaLookup, disabledItemsFocusable, orderedRootItemIds) => {
	const firstSelectedItem = selectedItems.find((itemId) => {
		if (!disabledItemsFocusable && isItemDisabled(itemMetaLookup, itemId)) return false;
		const itemMeta = itemMetaLookup[itemId];
		return itemMeta && (itemMeta.parentId == null || expandedItemsMap.has(itemMeta.parentId));
	});
	if (firstSelectedItem != null) return firstSelectedItem;
	const firstNavigableItem = orderedRootItemIds.find((itemId) => disabledItemsFocusable || !isItemDisabled(itemMetaLookup, itemId));
	if (firstNavigableItem != null) return firstNavigableItem;
	return null;
});
var focusSelectors = {
	/**
	* Gets the item that should be sequentially focusable (usually with the Tab key).
	* At any point in time, there is a single item that can be sequentially focused in the Tree View.
	* This item is the first selected item (that is both visible and navigable), if any, or the first navigable item if no item is selected.
	*/
	defaultFocusableItemId: defaultFocusableItemIdSelector,
	/**
	* Checks whether an item is the default focusable item.
	*/
	isItemTheDefaultFocusableItem: createSelector(defaultFocusableItemIdSelector, (defaultFocusableItemId, itemId) => defaultFocusableItemId === itemId),
	/**
	* Gets the id of the item that is currently focused.
	*/
	focusedItemId: createSelector((state) => state.focusedItemId),
	/**
	* Checks whether an item is focused.
	*/
	isItemFocused: createSelector((state, itemId) => state.focusedItemId === itemId)
};
//#endregion
//#region node_modules/@mui/x-tree-view/internals/plugins/lazyLoading/selectors.mjs
var lazyLoadingSelectors = {
	/**
	* Checks if the lazy loaded state is empty.
	*/
	isEmpty: createSelector((state) => {
		if (state.lazyLoadedItems == null) return true;
		return Object.keys(state.lazyLoadedItems.loading).length === 0 && Object.keys(state.lazyLoadedItems.errors).length === 0;
	}),
	/**
	* Checks whether an item is loading.
	*/
	isItemLoading: createSelector((state, itemId) => state.lazyLoadedItems?.loading[itemId ?? "__TREE_VIEW_ROOT_PARENT_ID__"] ?? false),
	/**
	* Checks whether an item has errors.
	*/
	itemHasError: createSelector((state, itemId) => !!state.lazyLoadedItems?.errors[itemId ?? "__TREE_VIEW_ROOT_PARENT_ID__"]),
	/**
	* Get an item error.
	*/
	itemError: createSelector((state, itemId) => state.lazyLoadedItems?.errors[itemId ?? "__TREE_VIEW_ROOT_PARENT_ID__"])
};
//#endregion
//#region node_modules/@mui/x-tree-view/internals/plugins/labelEditing/selectors.mjs
var labelSelectors = {
	/**
	* Checks whether an item is editable.
	*/
	isItemEditable: createSelector((state) => state.isItemEditable, itemsSelectors.itemModel, (isItemEditable, itemModel, _itemId) => {
		if (!itemModel || isItemEditable == null) return false;
		if (typeof isItemEditable === "boolean") return isItemEditable;
		return isItemEditable(itemModel);
	}),
	/**
	* Checks whether an item is being edited.
	*/
	isItemBeingEdited: createSelector((state, itemId) => itemId == null ? false : state.editedItemId === itemId),
	/**
	* Checks whether any item is being edited.
	*/
	isAnyItemBeingEdited: createSelector((state) => !!state.editedItemId)
};
//#endregion
//#region node_modules/@mui/x-tree-view/hooks/useTreeItemUtils/useTreeItemUtils.mjs
var itemHasChildren = (reactChildren) => {
	if (Array.isArray(reactChildren)) return reactChildren.length > 0 && reactChildren.some(itemHasChildren);
	return Boolean(reactChildren);
};
var useTreeItemUtils = ({ itemId, children }) => {
	const { store, publicAPI } = useTreeViewContext();
	const isItemExpandable = useStore(store, expansionSelectors.isItemExpandable, itemId);
	const isLoading = useStore(store, lazyLoadingSelectors.isItemLoading, itemId);
	const hasError = useStore(store, lazyLoadingSelectors.itemHasError, itemId);
	const isExpandable = itemHasChildren(children) || isItemExpandable;
	const isExpanded = useStore(store, expansionSelectors.isItemExpanded, itemId);
	const isFocused = useStore(store, focusSelectors.isItemFocused, itemId);
	const isSelected = useStore(store, selectionSelectors.isItemSelected, itemId);
	const isDisabled = useStore(store, itemsSelectors.isItemDisabled, itemId);
	const isEditing = useStore(store, labelSelectors.isItemBeingEdited, itemId);
	const status = {
		expandable: isExpandable,
		expanded: isExpanded,
		focused: isFocused,
		selected: isSelected,
		disabled: isDisabled,
		editing: isEditing,
		editable: useStore(store, labelSelectors.isItemEditable, itemId),
		loading: isLoading,
		error: hasError
	};
	const handleExpansion = (event) => {
		if (status.disabled) return;
		if (!status.focused) store.focus.focusItem(event, itemId);
		const multiple = selectionSelectors.isMultiSelectEnabled(store.state) && (event.shiftKey || event.ctrlKey || event.metaKey);
		if (status.expandable && !(multiple && expansionSelectors.isItemExpanded(store.state, itemId))) store.expansion.setItemExpansion({
			event,
			itemId
		});
	};
	const handleSelection = (event) => {
		if (!selectionSelectors.canItemBeSelected(store.state, itemId)) return;
		if (!status.focused && !status.editing) store.focus.focusItem(event, itemId);
		if (selectionSelectors.isMultiSelectEnabled(store.state) && (event.shiftKey || event.ctrlKey || event.metaKey)) if (event.shiftKey) store.selection.expandSelectionRange(event, itemId);
		else store.selection.setItemSelection({
			event,
			itemId,
			keepExistingSelection: true
		});
		else store.selection.setItemSelection({
			event,
			itemId,
			shouldBeSelected: true
		});
	};
	const handleCheckboxSelection = (event) => {
		const hasShift = event.nativeEvent.shiftKey;
		const isMultiSelectEnabled = selectionSelectors.isMultiSelectEnabled(store.state);
		if (isMultiSelectEnabled && hasShift) store.selection.expandSelectionRange(event, itemId);
		else store.selection.setItemSelection({
			event,
			itemId,
			keepExistingSelection: isMultiSelectEnabled,
			shouldBeSelected: event.target.checked
		});
	};
	const toggleItemEditing = () => {
		if (!store.labelEditing) return;
		if (isEditing) store.labelEditing.setEditedItem(null);
		else store.labelEditing.setEditedItem(itemId);
	};
	const handleSaveItemLabel = (event, newLabel) => {
		if (!store.labelEditing) return;
		if (labelSelectors.isItemBeingEdited(store.state, itemId)) {
			store.labelEditing.updateItemLabel(itemId, newLabel);
			toggleItemEditing();
			store.focus.focusItem(event, itemId);
		}
	};
	const handleCancelItemLabelEditing = (event) => {
		if (!store.labelEditing) return;
		if (labelSelectors.isItemBeingEdited(store.state, itemId)) {
			toggleItemEditing();
			store.focus.focusItem(event, itemId);
		}
	};
	return {
		interactions: {
			handleExpansion,
			handleSelection,
			handleCheckboxSelection,
			toggleItemEditing,
			handleSaveItemLabel,
			handleCancelItemLabelEditing
		},
		status,
		publicAPI
	};
};
//#endregion
//#region node_modules/@mui/x-tree-view/internals/TreeViewItemDepthContext/TreeViewItemDepthContext.mjs
var TreeViewItemDepthContext = /*#__PURE__*/ import_react.createContext(() => -1);
TreeViewItemDepthContext.displayName = "TreeViewItemDepthContext";
//#endregion
//#region node_modules/@mui/x-tree-view/internals/utils/tree.mjs
var getLastNavigableItemInArray = (state, items) => {
	let itemIndex = items.length - 1;
	while (itemIndex >= 0 && !itemsSelectors.canItemBeFocused(state, items[itemIndex])) itemIndex -= 1;
	if (itemIndex === -1) return;
	return items[itemIndex];
};
var getPreviousNavigableItem = (state, itemId) => {
	const itemMeta = itemsSelectors.itemMeta(state, itemId);
	if (!itemMeta) return null;
	const siblings = itemsSelectors.itemOrderedChildrenIds(state, itemMeta.parentId);
	const itemIndex = itemsSelectors.itemIndex(state, itemId);
	if (itemIndex === 0) return itemMeta.parentId;
	let previousNavigableSiblingIndex = itemIndex - 1;
	while (!itemsSelectors.canItemBeFocused(state, siblings[previousNavigableSiblingIndex]) && previousNavigableSiblingIndex >= 0) previousNavigableSiblingIndex -= 1;
	if (previousNavigableSiblingIndex === -1) {
		if (itemMeta.parentId == null) return null;
		return getPreviousNavigableItem(state, itemMeta.parentId);
	}
	let currentItemId = siblings[previousNavigableSiblingIndex];
	let lastNavigableChild = getLastNavigableItemInArray(state, itemsSelectors.itemOrderedChildrenIds(state, currentItemId));
	while (expansionSelectors.isItemExpanded(state, currentItemId) && lastNavigableChild != null) {
		currentItemId = lastNavigableChild;
		lastNavigableChild = getLastNavigableItemInArray(state, itemsSelectors.itemOrderedChildrenIds(state, currentItemId));
	}
	return currentItemId;
};
var getNextNavigableItem = (state, itemId) => {
	if (expansionSelectors.isItemExpanded(state, itemId)) {
		const firstNavigableChild = itemsSelectors.itemOrderedChildrenIds(state, itemId).find((childId) => itemsSelectors.canItemBeFocused(state, childId));
		if (firstNavigableChild != null) return firstNavigableChild;
	}
	let itemMeta = itemsSelectors.itemMeta(state, itemId);
	while (itemMeta != null) {
		const siblings = itemsSelectors.itemOrderedChildrenIds(state, itemMeta.parentId);
		const currentItemIndex = itemsSelectors.itemIndex(state, itemMeta.id);
		if (currentItemIndex < siblings.length - 1) {
			let nextItemIndex = currentItemIndex + 1;
			while (!itemsSelectors.canItemBeFocused(state, siblings[nextItemIndex]) && nextItemIndex < siblings.length - 1) nextItemIndex += 1;
			if (itemsSelectors.canItemBeFocused(state, siblings[nextItemIndex])) return siblings[nextItemIndex];
		}
		itemMeta = itemsSelectors.itemMeta(state, itemMeta.parentId);
	}
	return null;
};
var getLastNavigableItem = (state) => {
	let itemId = null;
	while (itemId == null || expansionSelectors.isItemExpanded(state, itemId)) {
		const lastNavigableChild = getLastNavigableItemInArray(state, itemsSelectors.itemOrderedChildrenIds(state, itemId));
		if (lastNavigableChild == null) return itemId;
		itemId = lastNavigableChild;
	}
	return itemId;
};
var getFirstNavigableItem = (state) => itemsSelectors.itemOrderedChildrenIds(state, null).find((itemId) => itemsSelectors.canItemBeFocused(state, itemId));
/**
* This is used to determine the start and end of a selection range so
* we can get the items between the two border items.
*
* It finds the items' common ancestor using
* a naive implementation of a lowest common ancestor algorithm
* (https://en.wikipedia.org/wiki/Lowest_common_ancestor).
* Then compares the ancestor's 2 children that are ancestors of itemA and ItemB
* so we can compare their indexes to work out which item comes first in a depth first search.
* (https://en.wikipedia.org/wiki/Depth-first_search)
*
* Another way to put it is which item is shallower in a trémaux tree
* https://en.wikipedia.org/wiki/Tr%C3%A9maux_tree
*/
var findOrderInTremauxTree = (state, itemAId, itemBId) => {
	if (itemAId === itemBId) return [itemAId, itemBId];
	const itemMetaA = itemsSelectors.itemMeta(state, itemAId);
	const itemMetaB = itemsSelectors.itemMeta(state, itemBId);
	if (!itemMetaA || !itemMetaB) return [itemAId, itemBId];
	if (itemMetaA.parentId === itemMetaB.id || itemMetaB.parentId === itemMetaA.id) return itemMetaB.parentId === itemMetaA.id ? [itemMetaA.id, itemMetaB.id] : [itemMetaB.id, itemMetaA.id];
	const aFamily = [itemMetaA.id];
	const bFamily = [itemMetaB.id];
	let aAncestor = itemMetaA.parentId;
	let bAncestor = itemMetaB.parentId;
	let aAncestorIsCommon = bFamily.indexOf(aAncestor) !== -1;
	let bAncestorIsCommon = aFamily.indexOf(bAncestor) !== -1;
	let continueA = true;
	let continueB = true;
	while (!bAncestorIsCommon && !aAncestorIsCommon) {
		if (continueA) {
			aFamily.push(aAncestor);
			aAncestorIsCommon = bFamily.indexOf(aAncestor) !== -1;
			continueA = aAncestor !== null;
			if (!aAncestorIsCommon && continueA) aAncestor = itemsSelectors.itemParentId(state, aAncestor);
		}
		if (continueB && !aAncestorIsCommon) {
			bFamily.push(bAncestor);
			bAncestorIsCommon = aFamily.indexOf(bAncestor) !== -1;
			continueB = bAncestor !== null;
			if (!bAncestorIsCommon && continueB) bAncestor = itemsSelectors.itemParentId(state, bAncestor);
		}
	}
	const commonAncestor = aAncestorIsCommon ? aAncestor : bAncestor;
	const ancestorFamily = itemsSelectors.itemOrderedChildrenIds(state, commonAncestor);
	const aSide = aFamily[aFamily.indexOf(commonAncestor) - 1];
	const bSide = bFamily[bFamily.indexOf(commonAncestor) - 1];
	return ancestorFamily.indexOf(aSide) < ancestorFamily.indexOf(bSide) ? [itemAId, itemBId] : [itemBId, itemAId];
};
var getNonDisabledItemsInRange = (state, itemAId, itemBId) => {
	const getNextItem = (itemId) => {
		if (expansionSelectors.isItemExpandable(state, itemId) && expansionSelectors.isItemExpanded(state, itemId)) return itemsSelectors.itemOrderedChildrenIds(state, itemId)[0];
		let itemMeta = itemsSelectors.itemMeta(state, itemId);
		while (itemMeta != null) {
			const siblings = itemsSelectors.itemOrderedChildrenIds(state, itemMeta.parentId);
			const currentItemIndex = itemsSelectors.itemIndex(state, itemMeta.id);
			if (currentItemIndex < siblings.length - 1) return siblings[currentItemIndex + 1];
			itemMeta = itemMeta.parentId ? itemsSelectors.itemMeta(state, itemMeta.parentId) : null;
		}
		throw new Error("MUI X Tree View: Invalid range - unable to find a path between the two items. This may occur if the items are not in the same tree or the tree structure is invalid. Verify that both items exist in the tree.");
	};
	const [first, last] = findOrderInTremauxTree(state, itemAId, itemBId);
	const items = [first];
	let current = first;
	while (current !== last) {
		current = getNextItem(current);
		if (!itemsSelectors.isItemDisabled(state, current)) items.push(current);
	}
	return items;
};
var getAllNavigableItems = (state) => {
	let item = getFirstNavigableItem(state);
	const navigableItems = [];
	while (item != null) {
		navigableItems.push(item);
		item = getNextNavigableItem(state, item);
	}
	return navigableItems;
};
/**
* Checks if the target is in a descendant of this item.
* This can prevent from firing some logic on the ancestors on the interacted item when the event handler is on the root.
* @param {HTMLElement} target The target to check
* @param {HTMLElement | null} itemRoot The root of the item to check if the event target is in its descendants
* @returns {boolean} Whether the target is in a descendant of this item
*/
var isTargetInDescendants = (target, itemRoot) => {
	return itemRoot !== target.closest("*[role=\"treeitem\"]");
};
//#endregion
//#region node_modules/@mui/x-tree-view/internals/plugins/expansion/TreeViewExpansionPlugin.mjs
var TreeViewExpansionPlugin = class {
	constructor(store) {
		this.store = store;
	}
	setExpandedItems = (event, value) => {
		if (this.store.parameters.expandedItems === void 0) this.store.set("expandedItems", value);
		this.store.parameters.onExpandedItemsChange?.(event, value);
	};
	/**
	* Check if an item is expanded.
	* @param {TreeViewItemId} itemId The id of the item to check.
	* @returns {boolean} `true` if the item is expanded, `false` otherwise.
	*/
	isItemExpanded = (itemId) => expansionSelectors.isItemExpanded(this.store.state, itemId);
	buildPublicAPI = () => {
		return {
			isItemExpanded: this.isItemExpanded,
			setItemExpansion: this.setItemExpansion
		};
	};
	/**
	* Change the expansion status of a given item.
	* @param {object} parameters The parameters of the method.
	* @param {TreeViewItemId} parameters.itemId The id of the item to expand of collapse.
	* @param {React.SyntheticEvent} parameters.event The DOM event that triggered the change.
	* @param {boolean} parameters.shouldBeExpanded If `true` the item will be expanded. If `false` the item will be collapsed. If not defined, the item's expansion status will be the toggled.
	*/
	setItemExpansion = ({ itemId, event = null, shouldBeExpanded }) => {
		const isExpandedBefore = expansionSelectors.isItemExpanded(this.store.state, itemId);
		const cleanShouldBeExpanded = shouldBeExpanded ?? !isExpandedBefore;
		if (isExpandedBefore === cleanShouldBeExpanded) return;
		const eventParameters = {
			isExpansionPrevented: false,
			shouldBeExpanded: cleanShouldBeExpanded,
			itemId
		};
		this.store.publishEvent("beforeItemToggleExpansion", eventParameters, event);
		if (eventParameters.isExpansionPrevented) return;
		this.applyItemExpansion({
			itemId,
			event,
			shouldBeExpanded: cleanShouldBeExpanded
		});
	};
	/**
	* Apply the new expansion status of a given item.
	* Is used by the `setItemExpansion` method and by the `useTreeViewLazyLoading` plugin.
	* Unlike `setItemExpansion`, this method does not trigger the lazy loading.
	* @param {object} parameters The parameters of the method.
	* @param {TreeViewItemId} parameters.itemId The id of the item to expand of collapse.
	* @param {React.SyntheticEvent | null} parameters.event The DOM event that triggered the change.
	* @param {boolean} parameters.shouldBeExpanded If `true` the item will be expanded. If `false` the item will be collapsed.
	*/
	applyItemExpansion = ({ itemId, event, shouldBeExpanded }) => {
		const oldExpanded = expansionSelectors.expandedItemsRaw(this.store.state);
		let newExpanded;
		if (shouldBeExpanded) newExpanded = [itemId].concat(oldExpanded);
		else newExpanded = oldExpanded.filter((id) => id !== itemId);
		this.store.parameters.onItemExpansionToggle?.(event, itemId, shouldBeExpanded);
		this.setExpandedItems(event, newExpanded);
	};
	/**
	* Expand all the siblings (i.e.: the items that have the same parent) of a given item.
	* @param {React.SyntheticEvent} event The DOM event that triggered the change.
	* @param {TreeViewItemId} itemId The id of the item whose siblings will be expanded.
	*/
	expandAllSiblings = (event, itemId) => {
		const itemMeta = itemsSelectors.itemMeta(this.store.state, itemId);
		if (itemMeta == null) return;
		const diff = itemsSelectors.itemOrderedChildrenIds(this.store.state, itemMeta.parentId).filter((child) => expansionSelectors.isItemExpandable(this.store.state, child) && !expansionSelectors.isItemExpanded(this.store.state, child));
		const newExpanded = expansionSelectors.expandedItemsRaw(this.store.state).concat(diff);
		if (diff.length > 0) {
			if (this.store.parameters.onItemExpansionToggle) diff.forEach((newlyExpandedItemId) => {
				this.store.parameters.onItemExpansionToggle(event, newlyExpandedItemId, true);
			});
			this.setExpandedItems(event, newExpanded);
		}
	};
	/**
	* Mark a list of items as expandable.
	* @param {TreeViewItemId[]} items The ids of the items to mark as expandable.
	*/
	addExpandableItems = (items) => {
		const newItemMetaLookup = _extends({}, this.store.state.itemMetaLookup);
		for (const itemId of items) newItemMetaLookup[itemId] = _extends({}, newItemMetaLookup[itemId], { expandable: true });
		this.store.set("itemMetaLookup", newItemMetaLookup);
	};
};
//#endregion
//#region node_modules/@mui/x-tree-view/internals/plugins/focus/TreeViewFocusPlugin.mjs
var TreeViewFocusPlugin = class {
	constructor(store) {
		this.store = store;
		let previousState = store.state;
		this.store.disposables.defer(this.store.subscribe((newState) => {
			if (newState.itemMetaLookup === previousState.itemMetaLookup) {
				previousState = newState;
				return;
			}
			const focusedItemId = focusSelectors.focusedItemId(newState);
			if (focusedItemId == null || itemsSelectors.itemMeta(newState, focusedItemId)) {
				previousState = newState;
				return;
			}
			const checkItemInNewTree = (itemId) => itemId == null || !itemsSelectors.itemMeta(newState, itemId) ? null : itemId;
			const itemToFocusId = checkItemInNewTree(getNextNavigableItem(previousState, focusedItemId)) ?? checkItemInNewTree(getPreviousNavigableItem(previousState, focusedItemId)) ?? getFirstNavigableItem(newState);
			if (itemToFocusId == null) this.setFocusedItemId(null);
			else this.applyItemFocus(null, itemToFocusId);
			previousState = newState;
		}));
	}
	setFocusedItemId = (itemId) => {
		if (focusSelectors.focusedItemId(this.store.state) === itemId) return;
		this.store.set("focusedItemId", itemId);
	};
	applyItemFocus = (event, itemId) => {
		this.store.items.getItemDOMElement(itemId)?.focus();
		this.setFocusedItemId(itemId);
		this.store.parameters.onItemFocus?.(event, itemId);
	};
	buildPublicAPI = () => {
		return { focusItem: this.focusItem };
	};
	/**
	* Focus the item with the given id.
	*
	* If the item is the child of a collapsed item, then this method will do nothing.
	* Make sure to expand the ancestors of the item before calling this method if needed.
	* @param {React.SyntheticEvent | null} event The DOM event that triggered the change.
	* @param {TreeViewItemId} itemId The id of the item to focus.
	*/
	focusItem = (event, itemId) => {
		const itemMeta = itemsSelectors.itemMeta(this.store.state, itemId);
		if (itemMeta && (itemMeta.parentId == null || expansionSelectors.isItemExpanded(this.store.state, itemMeta.parentId))) this.applyItemFocus(event, itemId);
	};
	/**
	* Remove the focus from the currently focused item (both from the internal state and the DOM).
	*/
	removeFocusedItem = () => {
		const focusedItemId = focusSelectors.focusedItemId(this.store.state);
		if (focusedItemId == null) return;
		if (itemsSelectors.itemMeta(this.store.state, focusedItemId)) {
			const itemElement = this.store.items.getItemDOMElement(focusedItemId);
			if (itemElement) itemElement.blur();
		}
		this.setFocusedItemId(null);
	};
	/**
	* Event handler to fire when the `root` slot of the Tree View is focused.
	* @param {React.MouseEvent} event The DOM event that triggered the change.
	*/
	handleRootFocus = (event) => {
		if (event.defaultMuiPrevented) return;
		const defaultFocusableItemId = focusSelectors.defaultFocusableItemId(this.store.state);
		if (event.target === event.currentTarget && defaultFocusableItemId != null) this.applyItemFocus(event, defaultFocusableItemId);
	};
	/**
	* Event handler to fire when the `root` slot of the Tree View is blurred.
	* @param {React.MouseEvent} event The DOM event that triggered the change.
	*/
	handleRootBlur = (event) => {
		if (event.defaultMuiPrevented) return;
		this.setFocusedItemId(null);
	};
};
//#endregion
//#region node_modules/@mui/x-tree-view/internals/plugins/selection/itemPlugin.mjs
var selectorCheckboxSelectionStatus = createSelector((state, itemId) => {
	if (selectionSelectors.isItemSelected(state, itemId)) return "checked";
	let hasSelectedDescendant = false;
	let hasUnSelectedDescendant = false;
	const traverseDescendants = (itemToTraverseId) => {
		if (itemToTraverseId !== itemId) {
			if (selectionSelectors.canItemBeSelected(state, itemToTraverseId)) if (selectionSelectors.isItemSelected(state, itemToTraverseId)) hasSelectedDescendant = true;
			else hasUnSelectedDescendant = true;
		}
		itemsSelectors.itemOrderedChildrenIds(state, itemToTraverseId).forEach(traverseDescendants);
	};
	traverseDescendants(itemId);
	if (selectionSelectors.propagationRules(state).parents) {
		if (hasSelectedDescendant && hasUnSelectedDescendant) return "indeterminate";
		if (hasSelectedDescendant && !hasUnSelectedDescendant) return "checked";
		return "empty";
	}
	if (hasSelectedDescendant) return "indeterminate";
	return "empty";
});
var useSelectionItemPlugin = ({ props }) => {
	const { itemId } = props;
	const { store } = useTreeViewContext();
	const isCheckboxSelectionEnabled = useStore(store, selectionSelectors.isCheckboxSelectionEnabled);
	const isFeatureEnabledForItem = useStore(store, selectionSelectors.isFeatureEnabledForItem, itemId);
	const canItemBeSelected = useStore(store, selectionSelectors.canItemBeSelected, itemId);
	const isItemDisabled = useStore(store, itemsSelectors.isItemDisabled, itemId);
	const isItemSelectable = useStore(store, selectionSelectors.isItemSelectable, itemId);
	const selectionStatus = useStore(store, selectorCheckboxSelectionStatus, itemId);
	const isItemInherentlyNotSelectable = isItemDisabled || !isItemSelectable;
	return { propsEnhancers: {
		root: () => {
			let ariaChecked;
			if (isItemInherentlyNotSelectable) ariaChecked = void 0;
			else if (selectionStatus === "checked") ariaChecked = true;
			else if (selectionStatus === "indeterminate") ariaChecked = "mixed";
			else if (!canItemBeSelected) ariaChecked = void 0;
			else ariaChecked = false;
			return { "aria-checked": ariaChecked };
		},
		checkbox: ({ externalEventHandlers, interactions }) => {
			const handleChange = (event) => {
				externalEventHandlers.onChange?.(event);
				if (event.defaultMuiPrevented) return;
				if (!selectionSelectors.canItemBeSelected(store.state, itemId)) return;
				interactions.handleCheckboxSelection(event);
			};
			return {
				tabIndex: -1,
				onChange: handleChange,
				visible: isCheckboxSelectionEnabled && isFeatureEnabledForItem,
				disabled: !canItemBeSelected,
				checked: selectionStatus === "checked",
				indeterminate: selectionStatus === "indeterminate"
			};
		}
	} };
};
//#endregion
//#region node_modules/@mui/x-tree-view/internals/plugins/selection/TreeViewSelectionPlugin.mjs
var TreeViewSelectionPlugin = class {
	lastSelectedItem = null;
	lastSelectedRange = {};
	constructor(store) {
		this.store = store;
		store.itemPluginManager.register(useSelectionItemPlugin, null);
	}
	setSelectedItems = (event, newModel, additionalItemsToPropagate) => {
		const { selectionPropagation = EMPTY_OBJECT, selectedItems, onItemSelectionToggle, onSelectedItemsChange } = this.store.parameters;
		const oldModel = selectionSelectors.selectedItemsRaw(this.store.state);
		let cleanModel;
		const isMultiSelectEnabled = selectionSelectors.isMultiSelectEnabled(this.store.state);
		if (isMultiSelectEnabled && (selectionPropagation.descendants || selectionPropagation.parents)) cleanModel = propagateSelection({
			store: this.store,
			selectionPropagation,
			newModel,
			oldModel,
			additionalItemsToPropagate
		});
		else cleanModel = newModel;
		if (onItemSelectionToggle) {
			if (isMultiSelectEnabled) {
				const changes = getAddedAndRemovedItems({
					store: this.store,
					newModel: cleanModel,
					oldModel
				});
				if (onItemSelectionToggle) {
					changes.added.forEach((itemId) => {
						onItemSelectionToggle(event, itemId, true);
					});
					changes.removed.forEach((itemId) => {
						onItemSelectionToggle(event, itemId, false);
					});
				}
			} else if (cleanModel !== oldModel) {
				if (oldModel != null) onItemSelectionToggle(event, oldModel, false);
				if (cleanModel != null) onItemSelectionToggle(event, cleanModel, true);
			}
		}
		if (selectedItems === void 0) this.store.set("selectedItems", cleanModel);
		onSelectedItemsChange?.(event, cleanModel);
	};
	selectRange = (event, [start, end]) => {
		if (!selectionSelectors.isMultiSelectEnabled(this.store.state)) return;
		let newSelectedItems = selectionSelectors.selectedItems(this.store.state).slice();
		if (Object.keys(this.lastSelectedRange).length > 0) newSelectedItems = newSelectedItems.filter((id) => !this.lastSelectedRange[id]);
		const selectedItemsLookup = getLookupFromArray(newSelectedItems);
		const range = getNonDisabledItemsInRange(this.store.state, start, end).filter((id) => selectionSelectors.isItemSelectable(this.store.state, id));
		const itemsToAddToModel = range.filter((id) => !selectedItemsLookup[id]);
		newSelectedItems = newSelectedItems.concat(itemsToAddToModel);
		this.setSelectedItems(event, newSelectedItems);
		this.lastSelectedRange = getLookupFromArray(range);
	};
	buildPublicAPI = () => {
		return { setItemSelection: this.setItemSelection };
	};
	/**
	* Select or deselect an item.
	* @param {object} parameters The parameters of the method.
	* @param {TreeViewItemId} parameters.itemId The id of the item to select or deselect.
	* @param {React.SyntheticEvent} parameters.event The DOM event that triggered the change.
	* @param {boolean} parameters.keepExistingSelection If `true`, the other already selected items will remain selected, otherwise, they will be deselected. This parameter is only relevant when `multiSelect` is `true`
	* @param {boolean | undefined} parameters.shouldBeSelected If `true` the item will be selected. If `false` the item will be deselected. If not defined, the item's selection status will be toggled.
	*/
	setItemSelection = ({ itemId, event = null, keepExistingSelection = false, shouldBeSelected }) => {
		if (!selectionSelectors.enabled(this.store.state)) return;
		let newSelected;
		const isMultiSelectEnabled = selectionSelectors.isMultiSelectEnabled(this.store.state);
		if (keepExistingSelection) {
			const oldSelected = selectionSelectors.selectedItems(this.store.state);
			const isSelectedBefore = selectionSelectors.isItemSelected(this.store.state, itemId);
			if (isSelectedBefore && (shouldBeSelected === false || shouldBeSelected == null)) newSelected = oldSelected.filter((id) => id !== itemId);
			else if (!isSelectedBefore && (shouldBeSelected === true || shouldBeSelected == null)) newSelected = [itemId].concat(oldSelected);
			else newSelected = oldSelected;
		} else if (shouldBeSelected === false || shouldBeSelected == null && selectionSelectors.isItemSelected(this.store.state, itemId)) newSelected = isMultiSelectEnabled ? [] : null;
		else newSelected = isMultiSelectEnabled ? [itemId] : itemId;
		this.setSelectedItems(event, newSelected, [itemId]);
		this.lastSelectedItem = itemId;
		this.lastSelectedRange = {};
	};
	/**
	* Select all the navigable items in the tree.
	* @param {React.SyntheticEvent} event The DOM event that triggered the change.
	*/
	selectAllNavigableItems = (event) => {
		if (!selectionSelectors.isMultiSelectEnabled(this.store.state)) return;
		const navigableItems = getAllNavigableItems(this.store.state);
		this.setSelectedItems(event, navigableItems);
		this.lastSelectedRange = getLookupFromArray(navigableItems);
	};
	/**
	* Expand the current selection range up to the given item.
	* @param {React.SyntheticEvent} event The DOM event that triggered the change.
	* @param {TreeViewItemId} itemId The id of the item to expand the selection to.
	*/
	expandSelectionRange = (event, itemId) => {
		if (this.lastSelectedItem != null) {
			const [start, end] = findOrderInTremauxTree(this.store.state, itemId, this.lastSelectedItem);
			this.selectRange(event, [start, end]);
		}
	};
	/**
	* Expand the current selection range from the first navigable item to the given item.
	* @param {React.SyntheticEvent} event The DOM event that triggered the change.
	* @param {TreeViewItemId} itemId The id of the item up to which the selection range should be expanded.
	*/
	selectRangeFromStartToItem = (event, itemId) => {
		this.selectRange(event, [getFirstNavigableItem(this.store.state), itemId]);
	};
	/**
	* Expand the current selection range from the given item to the last navigable item.
	* @param {React.SyntheticEvent} event The DOM event that triggered the change.
	* @param {TreeViewItemId} itemId The id of the item from which the selection range should be expanded.
	*/
	selectRangeFromItemToEnd = (event, itemId) => {
		this.selectRange(event, [itemId, getLastNavigableItem(this.store.state)]);
	};
	/**
	* Update the selection when navigating with ArrowUp / ArrowDown keys.
	* @param {React.SyntheticEvent} event The DOM event that triggered the change.
	* @param {TreeViewItemId} currentItemId The id of the active item before the keyboard navigation.
	* @param {TreeViewItemId} nextItemId The id of the active item after the keyboard navigation.
	*/
	selectItemFromArrowNavigation = (event, currentItem, nextItem) => {
		if (!selectionSelectors.isMultiSelectEnabled(this.store.state)) return;
		let newSelectedItems = selectionSelectors.selectedItems(this.store.state).slice();
		if (Object.keys(this.lastSelectedRange).length === 0) {
			newSelectedItems.push(nextItem);
			this.lastSelectedRange = {
				[currentItem]: true,
				[nextItem]: true
			};
		} else {
			if (!this.lastSelectedRange[currentItem]) this.lastSelectedRange = {};
			if (this.lastSelectedRange[nextItem]) {
				newSelectedItems = newSelectedItems.filter((id) => id !== currentItem);
				delete this.lastSelectedRange[currentItem];
			} else {
				newSelectedItems.push(nextItem);
				this.lastSelectedRange[nextItem] = true;
			}
		}
		this.setSelectedItems(event, newSelectedItems);
	};
};
function propagateSelection({ store, selectionPropagation, newModel, oldModel, additionalItemsToPropagate }) {
	if (!selectionPropagation.descendants && !selectionPropagation.parents) return newModel;
	let shouldRegenerateModel = false;
	const newModelLookup = getLookupFromArray(newModel);
	const changes = getAddedAndRemovedItems({
		store,
		newModel,
		oldModel
	});
	additionalItemsToPropagate?.forEach((itemId) => {
		if (newModelLookup[itemId]) {
			if (!changes.added.includes(itemId)) changes.added.push(itemId);
		} else if (!changes.removed.includes(itemId)) changes.removed.push(itemId);
	});
	changes.added.forEach((addedItemId) => {
		if (selectionPropagation.descendants) {
			const selectDescendants = (itemId) => {
				if (itemId !== addedItemId) {
					if (selectionSelectors.canItemBeSelected(store.state, itemId)) {
						shouldRegenerateModel = true;
						newModelLookup[itemId] = true;
					}
				}
				itemsSelectors.itemOrderedChildrenIds(store.state, itemId).forEach(selectDescendants);
			};
			selectDescendants(addedItemId);
		}
		if (selectionPropagation.parents) {
			const checkAllSelectableDescendantsSelected = (itemId) => {
				if (!selectionSelectors.canItemBeSelected(store.state, itemId)) return itemsSelectors.itemOrderedChildrenIds(store.state, itemId).every(checkAllSelectableDescendantsSelected);
				if (!newModelLookup[itemId]) return false;
				return itemsSelectors.itemOrderedChildrenIds(store.state, itemId).every(checkAllSelectableDescendantsSelected);
			};
			const selectParents = (itemId) => {
				const parentId = itemsSelectors.itemParentId(store.state, itemId);
				if (parentId == null) return;
				if (itemsSelectors.itemOrderedChildrenIds(store.state, parentId).every(checkAllSelectableDescendantsSelected)) {
					if (selectionSelectors.canItemBeSelected(store.state, parentId)) {
						shouldRegenerateModel = true;
						newModelLookup[parentId] = true;
					}
					selectParents(parentId);
				}
			};
			selectParents(addedItemId);
		}
	});
	changes.removed.forEach((removedItemId) => {
		if (selectionPropagation.parents) {
			let parentId = itemsSelectors.itemParentId(store.state, removedItemId);
			while (parentId != null) {
				if (newModelLookup[parentId]) {
					shouldRegenerateModel = true;
					delete newModelLookup[parentId];
				}
				parentId = itemsSelectors.itemParentId(store.state, parentId);
			}
		}
		if (selectionPropagation.descendants) {
			const deSelectDescendants = (itemId) => {
				if (itemId !== removedItemId) {
					shouldRegenerateModel = true;
					delete newModelLookup[itemId];
				}
				itemsSelectors.itemOrderedChildrenIds(store.state, itemId).forEach(deSelectDescendants);
			};
			deSelectDescendants(removedItemId);
		}
	});
	return shouldRegenerateModel ? Object.keys(newModelLookup) : newModel;
}
function getAddedAndRemovedItems({ store, oldModel, newModel }) {
	const newModelMap = /* @__PURE__ */ new Map();
	newModel.forEach((id) => {
		newModelMap.set(id, true);
	});
	return {
		added: newModel.filter((itemId) => !selectionSelectors.isItemSelected(store.state, itemId)),
		removed: oldModel.filter((itemId) => !newModelMap.has(itemId))
	};
}
function getLookupFromArray(array) {
	const lookup = {};
	array.forEach((itemId) => {
		lookup[itemId] = true;
	});
	return lookup;
}
//#endregion
//#region node_modules/@mui/x-tree-view/useTreeItem/useTreeItem.mjs
var depthSelector = (state, itemId, depthContext) => {
	if (typeof depthContext === "function") return depthContext(state, itemId);
	return depthContext;
};
var useTreeItem = (parameters) => {
	const { runItemPlugins, publicAPI, store } = useTreeViewContext();
	const depthContext = import_react.useContext(TreeViewItemDepthContext);
	const depth = useStore(store, depthSelector, parameters.itemId, depthContext);
	const { id, itemId, label, children, rootRef } = parameters;
	const { rootRef: pluginRootRef, contentRef, propsEnhancers } = runItemPlugins(parameters);
	const { interactions, status } = useTreeItemUtils({
		itemId,
		children
	});
	const rootRefObject = import_react.useRef(null);
	const contentRefObject = import_react.useRef(null);
	const handleRootRef = useMergedRefs(rootRef, pluginRootRef, rootRefObject);
	const handleContentRef = useMergedRefs(contentRef, contentRefObject);
	const checkboxRef = import_react.useRef(null);
	const isCheckboxSelectionEnabled = useStore(store, selectionSelectors.isCheckboxSelectionEnabled);
	const idAttribute = useStore(store, idSelectors.treeItemIdAttribute, itemId, id);
	const shouldBeAccessibleWithTab = useStore(store, focusSelectors.isItemTheDefaultFocusableItem, itemId);
	const itemHeight = useStore(store, itemsSelectors.itemHeight);
	const sharedPropsEnhancerParams = {
		rootRefObject,
		contentRefObject,
		interactions
	};
	const createRootHandleFocus = (otherHandlers) => (event) => {
		otherHandlers.onFocus?.(event);
		if (event.defaultMuiPrevented) return;
		if (!status.focused && itemsSelectors.canItemBeFocused(store.state, itemId) && event.currentTarget === event.target) store.focus.focusItem(event, itemId);
	};
	const createRootHandleBlur = (otherHandlers) => (event) => {
		otherHandlers.onBlur?.(event);
		if (event.defaultMuiPrevented) return;
		const rootElement = store.items.getItemDOMElement(itemId);
		if (status.editing || event.relatedTarget && isTargetInDescendants(event.relatedTarget, rootElement) && (event.target && event.target?.dataset?.element === "labelInput" && isTargetInDescendants(event.target, rootElement) || event.relatedTarget?.dataset?.element === "labelInput")) return;
		store.focus.removeFocusedItem();
	};
	const createRootHandleKeyDown = (otherHandlers) => (event) => {
		otherHandlers.onKeyDown?.(event);
		if (event.defaultMuiPrevented || event.target?.dataset?.element === "labelInput") return;
		store.keyboardNavigation.handleItemKeyDown(event, itemId);
	};
	const createLabelHandleDoubleClick = (otherHandlers) => (event) => {
		otherHandlers.onDoubleClick?.(event);
		if (event.defaultMuiPrevented) return;
		interactions.toggleItemEditing();
	};
	const createContentHandleClick = (otherHandlers) => (event) => {
		otherHandlers.onClick?.(event);
		store.items.handleItemClick(event, itemId);
		if (event.defaultMuiPrevented || checkboxRef.current?.contains(event.target)) return;
		if (expansionSelectors.triggerSlot(store.state) === "content") interactions.handleExpansion(event);
		if (!isCheckboxSelectionEnabled) interactions.handleSelection(event);
	};
	const createContentHandleMouseDown = (otherHandlers) => (event) => {
		otherHandlers.onMouseDown?.(event);
		if (event.defaultMuiPrevented) return;
		if (event.shiftKey || event.ctrlKey || event.metaKey || status.disabled) event.preventDefault();
	};
	const createIconContainerHandleClick = (otherHandlers) => (event) => {
		otherHandlers.onClick?.(event);
		if (event.defaultMuiPrevented) return;
		if (expansionSelectors.triggerSlot(store.state) === "iconContainer") interactions.handleExpansion(event);
	};
	const getContextProviderProps = () => ({
		itemId,
		id
	});
	const getRootProps = (externalProps = {}) => {
		const externalEventHandlers = _extends({}, extractEventHandlers(parameters), extractEventHandlers(externalProps));
		return _extends({}, _extends({}, externalEventHandlers, {
			ref: handleRootRef,
			role: "treeitem",
			tabIndex: shouldBeAccessibleWithTab ? 0 : -1,
			id: idAttribute,
			"aria-expanded": status.expandable ? status.expanded : void 0,
			"aria-disabled": status.disabled || void 0
		}, externalProps, {
			style: _extends({}, externalProps.style ?? {}, { "--TreeView-itemDepth": depth }, itemHeight == null ? {} : { "--TreeView-itemHeight": `${itemHeight}px` }),
			onFocus: createRootHandleFocus(externalEventHandlers),
			onBlur: createRootHandleBlur(externalEventHandlers),
			onKeyDown: createRootHandleKeyDown(externalEventHandlers)
		}), propsEnhancers.root?.(_extends({}, sharedPropsEnhancerParams, { externalEventHandlers })) ?? {});
	};
	const getContentProps = (externalProps = {}) => {
		const externalEventHandlers = extractEventHandlers(externalProps);
		const props = _extends({}, externalEventHandlers, externalProps, {
			ref: handleContentRef,
			onClick: createContentHandleClick(externalEventHandlers),
			onMouseDown: createContentHandleMouseDown(externalEventHandlers)
		});
		[
			"expanded",
			"selected",
			"focused",
			"disabled",
			"editing",
			"editable"
		].forEach((key) => {
			if (status[key]) props[`data-${key}`] = "";
		});
		return _extends({}, props, propsEnhancers.content?.(_extends({}, sharedPropsEnhancerParams, { externalEventHandlers })) ?? {});
	};
	const getCheckboxProps = (externalProps = {}) => {
		const externalEventHandlers = extractEventHandlers(externalProps);
		return _extends({}, _extends({}, externalEventHandlers, {
			ref: checkboxRef,
			"aria-hidden": true
		}, externalProps), propsEnhancers.checkbox?.(_extends({}, sharedPropsEnhancerParams, { externalEventHandlers })) ?? {});
	};
	const getLabelProps = (externalProps = {}) => {
		const externalEventHandlers = _extends({}, extractEventHandlers(externalProps));
		const props = _extends({}, externalEventHandlers, { children: label }, externalProps, { onDoubleClick: createLabelHandleDoubleClick(externalEventHandlers) });
		return _extends({}, propsEnhancers.label?.(_extends({}, sharedPropsEnhancerParams, { externalEventHandlers })) ?? {}, props);
	};
	const getLabelInputProps = (externalProps = {}) => {
		const externalEventHandlers = extractEventHandlers(externalProps);
		return _extends({}, externalProps, propsEnhancers.labelInput?.(_extends({}, sharedPropsEnhancerParams, { externalEventHandlers })) ?? {});
	};
	const getIconContainerProps = (externalProps = {}) => {
		const externalEventHandlers = extractEventHandlers(externalProps);
		return _extends({}, externalEventHandlers, externalProps, { onClick: createIconContainerHandleClick(externalEventHandlers) });
	};
	const getErrorContainerProps = (externalProps = {}) => {
		return _extends({}, extractEventHandlers(externalProps), externalProps);
	};
	const getLoadingContainerProps = (externalProps = {}) => {
		return _extends({
			size: "12px",
			thickness: 6
		}, extractEventHandlers(externalProps), externalProps);
	};
	const getGroupTransitionProps = (externalProps = {}) => {
		return _extends({}, extractEventHandlers(externalProps), {
			unmountOnExit: true,
			component: "ul",
			role: "group",
			in: status.expanded,
			children
		}, externalProps);
	};
	const getDragAndDropOverlayProps = (externalProps = {}) => {
		const externalEventHandlers = extractEventHandlers(externalProps);
		return _extends({}, externalProps, propsEnhancers.dragAndDropOverlay?.(_extends({}, sharedPropsEnhancerParams, { externalEventHandlers })) ?? {});
	};
	return {
		getContextProviderProps,
		getRootProps,
		getContentProps,
		getGroupTransitionProps,
		getIconContainerProps,
		getCheckboxProps,
		getLabelProps,
		getLabelInputProps,
		getDragAndDropOverlayProps,
		getErrorContainerProps,
		getLoadingContainerProps,
		rootRef: handleRootRef,
		status,
		publicAPI
	};
};
//#endregion
//#region node_modules/@mui/x-tree-view/TreeItem/treeItemClasses.mjs
function getTreeItemUtilityClass(slot) {
	return generateUtilityClass("MuiTreeItem", slot);
}
var treeItemClasses = generateUtilityClasses("MuiTreeItem", [
	"root",
	"content",
	"groupTransition",
	"iconContainer",
	"label",
	"checkbox",
	"labelInput",
	"dragAndDropOverlay",
	"errorIcon",
	"loadingIcon"
]);
//#endregion
//#region node_modules/@mui/x-tree-view/icons/icons.mjs
var TreeViewExpandIcon = createSvgIcon(/*#__PURE__*/ (0, import_jsx_runtime.jsx)("path", { d: "M10 6 8.59 7.41 13.17 12l-4.58 4.59L10 18l6-6z" }), "TreeViewExpandIcon");
var TreeViewCollapseIcon = createSvgIcon(/*#__PURE__*/ (0, import_jsx_runtime.jsx)("path", { d: "M16.59 8.59 12 13.17 7.41 8.59 6 10l6 6 6-6z" }), "TreeViewCollapseIcon");
//#endregion
//#region node_modules/@mui/x-tree-view/TreeItemIcon/TreeItemIcon.mjs
var _excluded$9 = ["ownerState"];
function pickIcon(treeItemIcon, treeViewIcon, fallback) {
	if (treeItemIcon !== void 0) return treeItemIcon;
	if (treeViewIcon !== void 0) return treeViewIcon;
	return fallback;
}
function TreeItemIcon(props) {
	const { slots: slotsFromTreeItem, slotProps: slotPropsFromTreeItem, status } = props;
	const { slots: slotsFromTreeView, slotProps: slotPropsFromTreeView } = useTreeViewStyleContext();
	const slots = {
		collapseIcon: pickIcon(slotsFromTreeItem?.collapseIcon, slotsFromTreeView.collapseIcon, TreeViewCollapseIcon),
		expandIcon: pickIcon(slotsFromTreeItem?.expandIcon, slotsFromTreeView.expandIcon, TreeViewExpandIcon),
		endIcon: pickIcon(slotsFromTreeItem?.endIcon, slotsFromTreeView.endIcon),
		icon: slotsFromTreeItem?.icon
	};
	let iconName;
	if (slots?.icon) iconName = "icon";
	else if (status.expandable) if (status.expanded) iconName = "collapseIcon";
	else iconName = "expandIcon";
	else iconName = "endIcon";
	const Icon = slots[iconName];
	const iconProps = _objectWithoutPropertiesLoose(useSlotProps({
		elementType: Icon,
		externalSlotProps: (tempOwnerState) => _extends({}, resolveComponentProps(slotPropsFromTreeView[iconName], tempOwnerState), resolveComponentProps(slotPropsFromTreeItem?.[iconName], tempOwnerState)),
		ownerState: {}
	}), _excluded$9);
	if (!Icon) return null;
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(Icon, _extends({}, iconProps));
}
TreeItemIcon.propTypes = {
	/**
	* The props used for each component slot.
	* @default {}
	*/
	slotProps: import_prop_types.default.object,
	/**
	* Overridable component slots.
	* @default {}
	*/
	slots: import_prop_types.default.object,
	status: import_prop_types.default.shape({
		disabled: import_prop_types.default.bool.isRequired,
		editable: import_prop_types.default.bool.isRequired,
		editing: import_prop_types.default.bool.isRequired,
		error: import_prop_types.default.bool.isRequired,
		expandable: import_prop_types.default.bool.isRequired,
		expanded: import_prop_types.default.bool.isRequired,
		focused: import_prop_types.default.bool.isRequired,
		loading: import_prop_types.default.bool.isRequired,
		selected: import_prop_types.default.bool.isRequired
	}).isRequired
};
//#endregion
//#region node_modules/@mui/x-tree-view/TreeItemDragAndDropOverlay/TreeItemDragAndDropOverlay.mjs
var TreeItemDragAndDropOverlayRoot = styled("div", {
	name: "MuiTreeItemDragAndDropOverlay",
	slot: "Root",
	shouldForwardProp: (prop) => shouldForwardProp(prop) && prop !== "action"
})(({ theme }) => ({
	position: "absolute",
	left: 0,
	display: "flex",
	top: 0,
	bottom: 0,
	right: 0,
	pointerEvents: "none",
	variants: [
		{
			props: { action: "make-child" },
			style: {
				marginLeft: "calc(var(--TreeView-indentMultiplier) * var(--TreeView-itemDepth))",
				borderRadius: theme.shape.borderRadius,
				backgroundColor: theme.alpha((theme.vars || theme).palette.primary.dark, (theme.vars || theme).palette.action.focusOpacity)
			}
		},
		{
			props: { action: "reorder-above" },
			style: {
				marginLeft: "calc(var(--TreeView-indentMultiplier) * var(--TreeView-itemDepth))",
				borderTop: `1px solid ${(theme.vars || theme).palette.action.active}`
			}
		},
		{
			props: { action: "reorder-below" },
			style: {
				marginLeft: "calc(var(--TreeView-indentMultiplier) * var(--TreeView-itemDepth))",
				borderBottom: `1px solid ${(theme.vars || theme).palette.action.active}`
			}
		},
		{
			props: { action: "move-to-parent" },
			style: {
				marginLeft: "calc(var(--TreeView-indentMultiplier) * calc(var(--TreeView-itemDepth) - 1))",
				borderBottom: `1px solid ${(theme.vars || theme).palette.action.active}`
			}
		}
	]
}));
function TreeItemDragAndDropOverlay(props) {
	if (props.action == null) return null;
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(TreeItemDragAndDropOverlayRoot, _extends({}, props));
}
TreeItemDragAndDropOverlay.propTypes = {
	action: import_prop_types.default.oneOf([
		"make-child",
		"move-to-parent",
		"reorder-above",
		"reorder-below"
	]),
	style: import_prop_types.default.object
};
//#endregion
//#region node_modules/@mui/x-tree-view/TreeItemProvider/TreeItemProvider.mjs
function TreeItemProvider(props) {
	const { children, itemId, id } = props;
	const { wrapItem, store } = useTreeViewContext();
	const idAttribute = useStore(store, idSelectors.treeItemIdAttribute, itemId, id);
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(import_react.Fragment, { children: wrapItem({
		children,
		itemId,
		store,
		idAttribute
	}) });
}
TreeItemProvider.propTypes = {
	children: import_prop_types.default.node,
	id: import_prop_types.default.string,
	itemId: import_prop_types.default.string.isRequired
};
//#endregion
//#region node_modules/@mui/x-tree-view/TreeItemLabelInput/TreeItemLabelInput.mjs
/**
* @ignore - internal component.
*/
var TreeItemLabelInput = styled("input", {
	name: "MuiTreeItem",
	slot: "LabelInput"
})(({ theme }) => _extends({}, theme.typography.body1, {
	width: "100%",
	backgroundColor: (theme.vars || theme).palette.background.paper,
	borderRadius: theme.shape.borderRadius,
	border: "none",
	padding: "0 2px",
	boxSizing: "border-box",
	"&:focus": { outline: `1px solid ${(theme.vars || theme).palette.primary.main}` }
}));
//#endregion
//#region node_modules/@mui/x-tree-view/TreeItem/TreeItem.mjs
var _excluded$8 = ["visible"], _excluded2$1 = [
	"id",
	"itemId",
	"label",
	"disabled",
	"disableSelection",
	"children",
	"slots",
	"slotProps",
	"classes"
];
var useThemeProps$1 = createUseThemeProps("MuiTreeItem");
var TreeItemRoot = styled("li", {
	name: "MuiTreeItem",
	slot: "Root"
})({
	listStyle: "none",
	margin: 0,
	padding: 0,
	outline: 0
});
var TreeItemContent = styled("div", {
	name: "MuiTreeItem",
	slot: "Content",
	shouldForwardProp: (prop) => shouldForwardProp(prop) && prop !== "status"
})(({ theme }) => ({
	padding: theme.spacing(.5, 1),
	paddingLeft: `calc(${theme.spacing(1)} + var(--TreeView-itemChildrenIndentation) * var(--TreeView-itemDepth))`,
	borderRadius: theme.shape.borderRadius,
	width: "100%",
	height: "var(--TreeView-itemHeight, unset)",
	boxSizing: "border-box",
	position: "relative",
	display: "flex",
	alignItems: "center",
	gap: theme.spacing(1),
	cursor: "pointer",
	WebkitTapHighlightColor: "transparent",
	"&:hover": {
		backgroundColor: (theme.vars || theme).palette.action.hover,
		"@media (hover: none)": { backgroundColor: "transparent" }
	},
	"&[data-disabled]": {
		opacity: (theme.vars || theme).palette.action.disabledOpacity,
		backgroundColor: "transparent",
		cursor: "auto"
	},
	"&[data-focused]": { backgroundColor: (theme.vars || theme).palette.action.focus },
	"&[data-selected]": {
		backgroundColor: theme.alpha((theme.vars || theme).palette.primary.main, (theme.vars || theme).palette.action.selectedOpacity),
		"&:hover": {
			backgroundColor: theme.alpha((theme.vars || theme).palette.primary.main, `${(theme.vars || theme).palette.action.selectedOpacity} + ${(theme.vars || theme).palette.action.hoverOpacity}`),
			"@media (hover: none)": { backgroundColor: theme.alpha((theme.vars || theme).palette.primary.main, (theme.vars || theme).palette.action.selectedOpacity) }
		}
	},
	"&[data-selected][data-focused]": { backgroundColor: theme.alpha((theme.vars || theme).palette.primary.main, `${(theme.vars || theme).palette.action.selectedOpacity} + ${(theme.vars || theme).palette.action.focusOpacity}`) }
}));
var TreeItemLabel = styled("div", {
	name: "MuiTreeItem",
	slot: "Label",
	shouldForwardProp: (prop) => shouldForwardProp(prop) && prop !== "editable"
})(({ theme }) => _extends({
	width: "100%",
	boxSizing: "border-box",
	minWidth: 0,
	position: "relative",
	overflow: "hidden"
}, theme.typography.body1, { variants: [{
	props: ({ editable }) => editable,
	style: { paddingLeft: "2px" }
}] }));
var TreeItemIconContainer = styled("div", {
	name: "MuiTreeItem",
	slot: "IconContainer"
})({
	width: 16,
	display: "flex",
	flexShrink: 0,
	justifyContent: "center",
	position: "relative",
	cursor: "inherit",
	"& svg": { fontSize: 18 }
});
var TreeItemGroupTransition = styled(Collapse, {
	name: "MuiTreeItem",
	slot: "GroupTransition",
	overridesResolver: (props, styles) => styles.groupTransition
})({
	margin: 0,
	padding: 0
});
var TreeItemErrorContainer = styled("div", {
	name: "MuiTreeItem",
	slot: "ErrorIcon"
})({
	position: "absolute",
	right: -3,
	width: 7,
	height: 7,
	borderRadius: "50%",
	backgroundColor: "red"
});
var TreeItemLoadingContainer = styled(CircularProgress, {
	name: "MuiTreeItem",
	slot: "LoadingIcon"
})({ color: "text.primary" });
var TreeItemCheckbox = styled(/*#__PURE__*/ import_react.forwardRef((props, ref) => {
	const { visible } = props, other = _objectWithoutPropertiesLoose(props, _excluded$8);
	if (!visible) return null;
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(Checkbox, _extends({}, other, { ref }));
}), {
	name: "MuiTreeItem",
	slot: "Checkbox"
})({ padding: 0 });
TreeItemCheckbox.displayName = "TreeItemCheckbox";
var useUtilityClasses$1 = (classesProp) => {
	const { classes: classesFromTreeView } = useTreeViewStyleContext();
	return composeClasses({
		root: ["root"],
		content: ["content"],
		iconContainer: ["iconContainer"],
		checkbox: ["checkbox"],
		label: ["label"],
		groupTransition: ["groupTransition"],
		labelInput: ["labelInput"],
		dragAndDropOverlay: ["dragAndDropOverlay"],
		errorIcon: ["errorIcon"],
		loadingIcon: ["loadingIcon"]
	}, getTreeItemUtilityClass, _extends({}, classesProp, {
		root: clsx(classesProp?.root, classesFromTreeView.root),
		content: clsx(classesProp?.content, classesFromTreeView.itemContent),
		iconContainer: clsx(classesProp?.iconContainer, classesFromTreeView.itemIconContainer),
		checkbox: clsx(classesProp?.checkbox, classesFromTreeView.itemCheckbox),
		label: clsx(classesProp?.label, classesFromTreeView.itemLabel),
		groupTransition: clsx(classesProp?.groupTransition, classesFromTreeView.itemGroupTransition),
		labelInput: clsx(classesProp?.labelInput, classesFromTreeView.itemLabelInput),
		dragAndDropOverlay: clsx(classesProp?.dragAndDropOverlay, classesFromTreeView.itemDragAndDropOverlay),
		errorIcon: clsx(classesProp?.errorIcon, classesFromTreeView.itemErrorIcon),
		loadingIcon: clsx(classesProp?.loadingIcon, classesFromTreeView.itemLoadingIcon)
	}));
};
/**
*
* Demos:
*
* - [Tree View](https://mui.com/x/react-tree-view/)
*
* API:
*
* - [TreeItem API](https://mui.com/x/api/tree-view/tree-item-2/)
*/
var TreeItem = /*#__PURE__*/ import_react.forwardRef(function TreeItem(inProps, forwardedRef) {
	const props = useThemeProps$1({
		props: inProps,
		name: "MuiTreeItem"
	});
	const { id, itemId, label, disabled, disableSelection, children, slots = {}, slotProps = {}, classes: classesProp } = props, other = _objectWithoutPropertiesLoose(props, _excluded2$1);
	const { getContextProviderProps, getRootProps, getContentProps, getIconContainerProps, getCheckboxProps, getLabelProps, getGroupTransitionProps, getLabelInputProps, getDragAndDropOverlayProps, getErrorContainerProps, getLoadingContainerProps, status } = useTreeItem({
		id,
		itemId,
		children,
		label,
		disabled,
		disableSelection
	});
	const classes = useUtilityClasses$1(classesProp);
	const Root = slots.root ?? TreeItemRoot;
	const rootProps = useSlotProps({
		elementType: Root,
		getSlotProps: getRootProps,
		externalForwardedProps: other,
		externalSlotProps: slotProps.root,
		additionalProps: { ref: forwardedRef },
		ownerState: {},
		className: classes.root
	});
	const Content = slots.content ?? TreeItemContent;
	const contentProps = useSlotProps({
		elementType: Content,
		getSlotProps: getContentProps,
		externalSlotProps: slotProps.content,
		ownerState: {},
		className: classes.content
	});
	const IconContainer = slots.iconContainer ?? TreeItemIconContainer;
	const iconContainerProps = useSlotProps({
		elementType: IconContainer,
		getSlotProps: getIconContainerProps,
		externalSlotProps: slotProps.iconContainer,
		ownerState: {},
		className: classes.iconContainer
	});
	const Label = slots.label ?? TreeItemLabel;
	const labelProps = useSlotProps({
		elementType: Label,
		getSlotProps: getLabelProps,
		externalSlotProps: slotProps.label,
		ownerState: {},
		className: classes.label
	});
	const Checkbox = slots.checkbox ?? TreeItemCheckbox;
	const checkboxProps = useSlotProps({
		elementType: Checkbox,
		getSlotProps: getCheckboxProps,
		externalSlotProps: slotProps.checkbox,
		ownerState: {},
		className: classes.checkbox
	});
	const GroupTransition = slots.groupTransition ?? void 0;
	const groupTransitionProps = useSlotProps({
		elementType: GroupTransition,
		getSlotProps: getGroupTransitionProps,
		externalSlotProps: slotProps.groupTransition,
		ownerState: {},
		className: classes.groupTransition
	});
	const LabelInput = slots.labelInput ?? TreeItemLabelInput;
	const labelInputProps = useSlotProps({
		elementType: LabelInput,
		getSlotProps: getLabelInputProps,
		externalSlotProps: slotProps.labelInput,
		ownerState: {},
		className: classes.labelInput
	});
	const DragAndDropOverlay = slots.dragAndDropOverlay ?? TreeItemDragAndDropOverlay;
	const dragAndDropOverlayProps = useSlotProps({
		elementType: DragAndDropOverlay,
		getSlotProps: getDragAndDropOverlayProps,
		externalSlotProps: slotProps.dragAndDropOverlay,
		ownerState: {},
		className: classes.dragAndDropOverlay
	});
	const ErrorIcon = slots.errorIcon ?? TreeItemErrorContainer;
	const errorContainerProps = useSlotProps({
		elementType: ErrorIcon,
		getSlotProps: getErrorContainerProps,
		externalSlotProps: slotProps.errorIcon,
		ownerState: {},
		className: classes.errorIcon
	});
	const LoadingIcon = slots.loadingIcon ?? TreeItemLoadingContainer;
	const loadingContainerProps = useSlotProps({
		elementType: LoadingIcon,
		getSlotProps: getLoadingContainerProps,
		externalSlotProps: slotProps.loadingIcon,
		ownerState: {},
		className: classes.loadingIcon
	});
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(TreeItemProvider, _extends({}, getContextProviderProps(), { children: /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(Root, _extends({}, rootProps, { children: [/*#__PURE__*/ (0, import_jsx_runtime.jsxs)(Content, _extends({}, contentProps, { children: [
		/*#__PURE__*/ (0, import_jsx_runtime.jsxs)(IconContainer, _extends({}, iconContainerProps, { children: [status.error && /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ErrorIcon, _extends({}, errorContainerProps)), status.loading ? /*#__PURE__*/ (0, import_jsx_runtime.jsx)(LoadingIcon, _extends({}, loadingContainerProps)) : /*#__PURE__*/ (0, import_jsx_runtime.jsx)(TreeItemIcon, {
			status,
			slots,
			slotProps
		})] })),
		/*#__PURE__*/ (0, import_jsx_runtime.jsx)(Checkbox, _extends({}, checkboxProps)),
		status.editing ? /*#__PURE__*/ (0, import_jsx_runtime.jsx)(LabelInput, _extends({}, labelInputProps)) : /*#__PURE__*/ (0, import_jsx_runtime.jsx)(Label, _extends({}, labelProps)),
		/*#__PURE__*/ (0, import_jsx_runtime.jsx)(DragAndDropOverlay, _extends({}, dragAndDropOverlayProps))
	] })), children && /*#__PURE__*/ (0, import_jsx_runtime.jsx)(TreeItemGroupTransition, _extends({ as: GroupTransition }, groupTransitionProps))] })) }));
});
TreeItem.displayName = "TreeItem";
TreeItem.propTypes = {
	/**
	* The content of the component.
	*/
	children: import_prop_types.default.any,
	/**
	* Override or extend the styles applied to the component.
	*/
	classes: import_prop_types.default.object,
	className: import_prop_types.default.string,
	/**
	* If `true`, the item is disabled.
	* @default false
	*/
	disabled: import_prop_types.default.bool,
	/**
	* If `true`, the item cannot be selected.
	* @default false
	*/
	disableSelection: import_prop_types.default.bool,
	/**
	* The id attribute of the item. If not provided, it will be generated.
	*/
	id: import_prop_types.default.string,
	/**
	* The id of the item.
	* Must be unique.
	*/
	itemId: import_prop_types.default.string.isRequired,
	/**
	* The label of the item.
	*/
	label: import_prop_types.default.node,
	/**
	* Callback fired when the item root is blurred.
	*/
	onBlur: import_prop_types.default.func,
	/**
	* This prop isn't supported.
	* Use the `onItemFocus` callback on the tree if you need to monitor an item's focus.
	*/
	onFocus: unsupportedProp,
	/**
	* Callback fired when a key is pressed on the keyboard and the tree is in focus.
	*/
	onKeyDown: import_prop_types.default.func,
	/**
	* The props used for each component slot.
	* @default {}
	*/
	slotProps: import_prop_types.default.object,
	/**
	* Overridable component slots.
	* @default {}
	*/
	slots: import_prop_types.default.object,
	/**
	* The system prop that allows defining system overrides as well as additional CSS styles.
	*/
	sx: import_prop_types.default.oneOfType([
		import_prop_types.default.arrayOf(import_prop_types.default.oneOfType([
			import_prop_types.default.func,
			import_prop_types.default.object,
			import_prop_types.default.bool
		])),
		import_prop_types.default.func,
		import_prop_types.default.object
	])
};
//#endregion
//#region node_modules/@mui/x-tree-view/internals/hooks/useTreeViewRootProps.mjs
function useTreeViewRootProps(store, forwardedProps, ref) {
	const treeId = useStore(store, idSelectors.treeId);
	const itemChildrenIndentation = useStore(store, itemsSelectors.itemChildrenIndentation);
	const isMultiSelectEnabled = useStore(store, selectionSelectors.isMultiSelectEnabled);
	return (otherHandlers) => _extends({
		ref,
		role: "tree",
		id: treeId,
		"aria-multiselectable": isMultiSelectEnabled
	}, forwardedProps, otherHandlers, {
		style: _extends({}, forwardedProps.style, { "--TreeView-itemChildrenIndentation": typeof itemChildrenIndentation === "number" ? `${itemChildrenIndentation}px` : itemChildrenIndentation }),
		onFocus: (event) => {
			otherHandlers.onFocus?.(event);
			store.focus.handleRootFocus(event);
		},
		onBlur: (event) => {
			otherHandlers.onBlur?.(event);
			store.focus.handleRootBlur(event);
		}
	});
}
//#endregion
//#region node_modules/@mui/x-tree-view/internals/components/RichTreeViewItems.mjs
var _excluded$7 = ["ownerState"];
var RichTreeViewItemsContext = /*#__PURE__*/ import_react.createContext(null);
RichTreeViewItemsContext.displayName = "RichTreeViewItemsContext";
var selectorNoChildren = () => EMPTY_ARRAY;
var selectorChildrenIdsNull = (state) => itemsSelectors.itemOrderedChildrenIds(state, null);
var RichTreeViewItem = /*#__PURE__*/ import_react.memo(function RichTreeViewItem({ itemSlot, itemSlotProps, itemId, skipChildren }) {
	const renderItemForRichTreeView = import_react.useContext(RichTreeViewItemsContext);
	const { store } = useTreeViewContext();
	const itemMeta = useStore(store, itemsSelectors.itemMeta, itemId);
	const children = useStore(store, skipChildren ? selectorNoChildren : itemsSelectors.itemOrderedChildrenIds, itemId);
	const Item = itemSlot ?? TreeItem;
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(Item, _extends({}, _objectWithoutPropertiesLoose(useSlotProps({
		elementType: Item,
		externalSlotProps: itemSlotProps,
		additionalProps: {
			label: itemMeta?.label,
			id: itemMeta?.idAttribute,
			itemId
		},
		ownerState: {
			itemId,
			label: itemMeta?.label
		}
	}), _excluded$7), { children: renderItemForRichTreeView ? children?.map(renderItemForRichTreeView) : null }));
}, fastObjectShallowCompare);
RichTreeViewItem.displayName = "RichTreeViewItem";
function RichTreeViewItems(props) {
	const { slots, slotProps, ownerState, forwardedProps, rootRef } = props;
	const { store } = useTreeViewContext();
	const { classes } = useTreeViewStyleContext();
	const itemSlot = slots?.item;
	const itemSlotProps = slotProps?.item;
	const domStructure = useStore(store, itemsSelectors.domStructure);
	const items = useStore(store, domStructure === "flat" ? expansionSelectors.flatList : selectorChildrenIdsNull);
	const getRootProps = useTreeViewRootProps(store, forwardedProps, rootRef);
	const Root = slots.root;
	const rootProps = useSlotProps({
		elementType: Root,
		externalSlotProps: slotProps?.root,
		className: classes.root,
		getSlotProps: getRootProps,
		ownerState
	});
	const skipChildren = domStructure === "flat";
	const renderItem = import_react.useCallback((itemId) => {
		return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(RichTreeViewItem, {
			itemSlot,
			itemSlotProps,
			itemId,
			skipChildren
		}, itemId);
	}, [
		itemSlot,
		itemSlotProps,
		skipChildren
	]);
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(RichTreeViewItemsContext.Provider, {
		value: renderItem,
		children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(Root, _extends({}, rootProps, { children: items.map(renderItem) }))
	});
}
//#endregion
//#region node_modules/@mui/x-tree-view/RichTreeView/useExtractRichTreeViewParameters.mjs
var _excluded$6 = [
	"apiRef",
	"slots",
	"slotProps",
	"disabledItemsFocusable",
	"items",
	"isItemDisabled",
	"isItemSelectionDisabled",
	"getItemLabel",
	"getItemChildren",
	"getItemId",
	"onItemClick",
	"itemChildrenIndentation",
	"id",
	"expandedItems",
	"defaultExpandedItems",
	"onExpandedItemsChange",
	"onItemExpansionToggle",
	"expansionTrigger",
	"disableSelection",
	"selectedItems",
	"defaultSelectedItems",
	"multiSelect",
	"checkboxSelection",
	"selectionPropagation",
	"onSelectedItemsChange",
	"onItemSelectionToggle",
	"onItemFocus",
	"itemHeight",
	"onItemLabelChange",
	"isItemEditable",
	"domStructure"
];
function useExtractRichTreeViewParameters(props) {
	const { apiRef, slots, slotProps, disabledItemsFocusable, items, isItemDisabled, isItemSelectionDisabled, getItemLabel, getItemChildren, getItemId, onItemClick, itemChildrenIndentation, id, expandedItems, defaultExpandedItems, onExpandedItemsChange, onItemExpansionToggle, expansionTrigger, disableSelection, selectedItems, defaultSelectedItems, multiSelect, checkboxSelection, selectionPropagation, onSelectedItemsChange, onItemSelectionToggle, onItemFocus, itemHeight, onItemLabelChange, isItemEditable, domStructure } = props, forwardedProps = _objectWithoutPropertiesLoose(props, _excluded$6);
	return {
		apiRef,
		slots,
		slotProps,
		parameters: import_react.useMemo(() => ({
			disabledItemsFocusable,
			items,
			isItemDisabled,
			isItemSelectionDisabled,
			getItemLabel,
			getItemChildren,
			getItemId,
			onItemClick,
			itemChildrenIndentation,
			id,
			expandedItems,
			defaultExpandedItems,
			onExpandedItemsChange,
			onItemExpansionToggle,
			expansionTrigger,
			disableSelection,
			selectedItems,
			defaultSelectedItems,
			multiSelect,
			checkboxSelection,
			selectionPropagation,
			onSelectedItemsChange,
			onItemSelectionToggle,
			onItemFocus,
			itemHeight,
			onItemLabelChange,
			isItemEditable,
			domStructure
		}), [
			disabledItemsFocusable,
			items,
			isItemDisabled,
			isItemSelectionDisabled,
			getItemLabel,
			getItemChildren,
			getItemId,
			onItemClick,
			itemChildrenIndentation,
			id,
			expandedItems,
			defaultExpandedItems,
			onExpandedItemsChange,
			onItemExpansionToggle,
			expansionTrigger,
			disableSelection,
			selectedItems,
			defaultSelectedItems,
			multiSelect,
			checkboxSelection,
			selectionPropagation,
			onSelectedItemsChange,
			onItemSelectionToggle,
			onItemFocus,
			itemHeight,
			onItemLabelChange,
			isItemEditable,
			domStructure
		]),
		forwardedProps
	};
}
//#endregion
//#region node_modules/@mui/x-tree-view/internals/hooks/useTreeViewStore.mjs
/**
* Creates a Tree View store and keep it in sync with the provided parameters.
*/
function useTreeViewStore(StoreClass, parameters) {
	const isRtl = useRtl();
	const store = useDisposable(() => new StoreClass(_extends({}, parameters, { isRtl })));
	useIsoLayoutEffect(() => store.updateStateFromParameters(_extends({}, parameters, { isRtl })), [
		store,
		isRtl,
		parameters
	]);
	useOnMount(store.mountEffect);
	return store;
}
//#endregion
//#region node_modules/@mui/x-tree-view/internals/plugins/labelEditing/itemPlugin.mjs
var useLabelEditingItemPlugin = ({ props }) => {
	const { store } = useTreeViewContext();
	const { label, itemId } = props;
	const [labelInputValue, setLabelInputValue] = import_react.useState(label);
	const isItemEditable = useStore(store, labelSelectors.isItemEditable, itemId);
	if (!useStore(store, labelSelectors.isItemBeingEdited, itemId) && labelInputValue !== label) setLabelInputValue(label);
	return { propsEnhancers: {
		label: () => ({ editable: isItemEditable }),
		labelInput: ({ externalEventHandlers, interactions }) => {
			if (!isItemEditable) return {};
			const handleKeydown = (event) => {
				externalEventHandlers.onKeyDown?.(event);
				if (event.defaultMuiPrevented) return;
				const target = event.target;
				if (event.key === "Enter" && target.value) interactions.handleSaveItemLabel(event, target.value);
				else if (event.key === "Escape") interactions.handleCancelItemLabelEditing(event);
			};
			const handleBlur = (event) => {
				externalEventHandlers.onBlur?.(event);
				if (event.defaultMuiPrevented) return;
				if (event.target.value) interactions.handleSaveItemLabel(event, event.target.value);
			};
			const handleInputChange = (event) => {
				externalEventHandlers.onChange?.(event);
				setLabelInputValue(event.target.value);
			};
			return {
				value: labelInputValue ?? "",
				"data-element": "labelInput",
				onChange: handleInputChange,
				onKeyDown: handleKeydown,
				onBlur: handleBlur,
				autoFocus: true,
				type: "text"
			};
		}
	} };
};
//#endregion
//#region node_modules/@mui/x-tree-view/internals/plugins/labelEditing/TreeViewLabelEditingPlugin.mjs
var TreeViewLabelEditingPlugin = class {
	constructor(store) {
		this.store = store;
		store.itemPluginManager.register(useLabelEditingItemPlugin, null);
	}
	buildPublicAPI = () => {
		return {
			setEditedItem: this.setEditedItem,
			updateItemLabel: this.updateItemLabel
		};
	};
	/**
	* Set which item is currently being edited.
	* You can pass `null` to exit editing mode.
	* @param {TreeViewItemId | null} itemId The id of the item to edit, or `null` to exit editing mode.
	*/
	setEditedItem = (itemId) => {
		if (itemId !== null && !labelSelectors.isItemEditable(this.store.state, itemId)) return;
		this.store.set("editedItemId", itemId);
	};
	/**
	* Used to update the label of an item.
	* @param {TreeViewItemId} itemId The id of the item to update the label of.
	* @param {string} label The new label of the item.
	*/
	updateItemLabel = (itemId, label) => {
		if (!label) throw new Error(`MUI X: The Tree View component requires all items to have a \`label\` property.
The label of an item cannot be empty.
Item ID: ${itemId}`);
		const item = this.store.state.itemMetaLookup[itemId];
		if (item.label === label) return;
		this.store.set("itemMetaLookup", _extends({}, this.store.state.itemMetaLookup, { [itemId]: _extends({}, item, { label }) }));
		if (this.store.parameters.onItemLabelChange) this.store.parameters.onItemLabelChange(itemId, label);
	};
};
//#endregion
//#region node_modules/@mui/x-tree-view/internals/plugins/expansion/utils.mjs
var getExpansionTrigger = ({ isItemEditable, expansionTrigger }) => {
	if (expansionTrigger) return expansionTrigger;
	if (isItemEditable) return "iconContainer";
	return "content";
};
//#endregion
//#region node_modules/@mui/x-tree-view/internals/MinimalTreeViewStore/MinimalTreeViewStore.utils.mjs
/**
* Returns the properties of the state that are derived from the parameters.
* This do not contain state properties that don't update whenever the parameters update.
*/
function deriveStateFromParameters$1(parameters) {
	return {
		disabledItemsFocusable: parameters.disabledItemsFocusable ?? false,
		itemChildrenIndentation: parameters.itemChildrenIndentation ?? "12px",
		providedTreeId: parameters.id,
		expansionTrigger: getExpansionTrigger({
			isItemEditable: parameters.isItemEditable,
			expansionTrigger: parameters.expansionTrigger
		}),
		disableSelection: parameters.disableSelection ?? false,
		multiSelect: parameters.multiSelect ?? false,
		checkboxSelection: parameters.checkboxSelection ?? false,
		selectionPropagation: parameters.selectionPropagation ?? EMPTY_OBJECT,
		itemHeight: parameters.itemHeight ?? null
	};
}
function applyModelInitialValue(controlledValue, defaultValue, fallback) {
	if (controlledValue !== void 0) return controlledValue;
	if (defaultValue !== void 0) return defaultValue;
	return fallback;
}
function createMinimalInitialState(parameters) {
	return _extends({
		treeId: void 0,
		focusedItemId: null
	}, deriveStateFromParameters$1(parameters), TreeViewItemsPlugin.buildItemsStateIfNeeded(parameters), {
		expandedItems: applyModelInitialValue(parameters.expandedItems, parameters.defaultExpandedItems, []),
		selectedItems: applyModelInitialValue(parameters.selectedItems, parameters.defaultSelectedItems, parameters.multiSelect ? EMPTY_ARRAY : null)
	});
}
var globalTreeViewDefaultId = 0;
var createTreeViewDefaultId = () => {
	globalTreeViewDefaultId += 1;
	return `mui-tree-view-${globalTreeViewDefaultId}`;
};
//#endregion
//#region node_modules/@mui/x-tree-view/internals/MinimalTreeViewStore/TimeoutManager.mjs
var TimeoutManager = class {
	timeoutIds = /* @__PURE__ */ new Map();
	intervalIds = /* @__PURE__ */ new Map();
	startTimeout = (key, delay, fn) => {
		this.clearTimeout(key);
		const id = setTimeout(() => {
			this.timeoutIds.delete(key);
			fn();
		}, delay);
		this.timeoutIds.set(key, id);
	};
	startInterval = (key, delay, fn) => {
		this.clearInterval(key);
		const id = setInterval(fn, delay);
		this.intervalIds.set(key, id);
	};
	clearTimeout = (key) => {
		const id = this.timeoutIds.get(key);
		if (id != null) {
			clearTimeout(id);
			this.timeoutIds.delete(key);
		}
	};
	clearInterval = (key) => {
		const id = this.intervalIds.get(key);
		if (id != null) {
			clearInterval(id);
			this.intervalIds.delete(key);
		}
	};
	clearAll = () => {
		this.timeoutIds.forEach(clearTimeout);
		this.timeoutIds.clear();
		this.intervalIds.forEach(clearInterval);
		this.intervalIds.clear();
	};
};
//#endregion
//#region node_modules/@mui/x-tree-view/internals/plugins/keyboardNavigation/TreeViewKeyboardNavigationPlugin.mjs
var TYPEAHEAD_TIMEOUT = 500;
var TreeViewKeyboardNavigationPlugin = class {
	typeaheadQuery = "";
	constructor(store) {
		this.store = store;
		this.labelMap = createLabelMapFromItemMetaLookup(itemsSelectors.itemMetaLookup(this.store.state));
		this.store.registerStoreEffect(itemsSelectors.itemMetaLookup, (_, itemMetaLookup) => {
			if (this.store.shouldIgnoreItemsStateUpdate()) return;
			this.labelMap = createLabelMapFromItemMetaLookup(itemMetaLookup);
		});
	}
	canToggleItemSelection = (itemId) => selectionSelectors.canItemBeSelected(this.store.state, itemId);
	canToggleItemExpansion = (itemId) => {
		return !itemsSelectors.isItemDisabled(this.store.state, itemId) && expansionSelectors.isItemExpandable(this.store.state, itemId);
	};
	getFirstItemMatchingTypeaheadQuery = (itemId, newKey) => {
		const getNextItem = (itemIdToCheck) => {
			const nextItemId = getNextNavigableItem(this.store.state, itemIdToCheck);
			if (nextItemId === null) return getFirstNavigableItem(this.store.state);
			return nextItemId;
		};
		const getNextMatchingItemId = (query) => {
			let matchingItemId = null;
			const checkedItems = {};
			let currentItemId = query.length > 1 ? itemId : getNextItem(itemId);
			while (matchingItemId == null && !checkedItems[currentItemId]) if (this.labelMap[currentItemId]?.startsWith(query)) matchingItemId = currentItemId;
			else {
				checkedItems[currentItemId] = true;
				currentItemId = getNextItem(currentItemId);
			}
			return matchingItemId;
		};
		const cleanNewKey = newKey.toLowerCase();
		const concatenatedQuery = `${this.typeaheadQuery}${cleanNewKey}`;
		const concatenatedQueryMatchingItemId = getNextMatchingItemId(concatenatedQuery);
		if (concatenatedQueryMatchingItemId != null) {
			this.typeaheadQuery = concatenatedQuery;
			return concatenatedQueryMatchingItemId;
		}
		const newKeyMatchingItemId = getNextMatchingItemId(cleanNewKey);
		if (newKeyMatchingItemId != null) {
			this.typeaheadQuery = cleanNewKey;
			return newKeyMatchingItemId;
		}
		this.typeaheadQuery = "";
		return null;
	};
	/**
	* Updates the `labelMap` to add/remove the first character of some item's labels.
	* This map is used to navigate the tree using type-ahead search.
	* This method is only used by the `useTreeViewJSXItems` plugin, otherwise the updates are handled internally.
	* @param {(map: TreeViewLabelMap) => TreeViewLabelMap} updater The function to update the map.
	*/
	updateLabelMap = (callback) => {
		this.labelMap = callback(this.labelMap);
	};
	/**
	* Callback fired when a key is pressed on an item.
	* Handles all the keyboard navigation logic.
	* @param {React.KeyboardEvent<HTMLElement> & TreeViewCancellableEvent} event The keyboard event that triggered the callback.
	* @param {TreeViewItemId} itemId The id of the item that the event was triggered on.
	*/
	handleItemKeyDown = async (event, itemId) => {
		if (event.defaultMuiPrevented) return;
		if (event.altKey || isTargetInDescendants(event.target, event.currentTarget)) return;
		const ctrlPressed = event.ctrlKey || event.metaKey;
		const key = event.key;
		const isMultiSelectEnabled = selectionSelectors.isMultiSelectEnabled(this.store.state);
		switch (true) {
			case key === " " && this.canToggleItemSelection(itemId):
				event.preventDefault();
				if (isMultiSelectEnabled && event.shiftKey) this.store.selection.expandSelectionRange(event, itemId);
				else this.store.selection.setItemSelection({
					event,
					itemId,
					keepExistingSelection: isMultiSelectEnabled,
					shouldBeSelected: void 0
				});
				break;
			case key === "Enter":
				if (this.store.labelEditing?.setEditedItem && labelSelectors.isItemEditable(this.store.state, itemId) && !labelSelectors.isItemBeingEdited(this.store.state, itemId)) this.store.labelEditing.setEditedItem(itemId);
				else if (this.canToggleItemExpansion(itemId)) {
					this.store.expansion.setItemExpansion({
						event,
						itemId
					});
					event.preventDefault();
				} else if (this.canToggleItemSelection(itemId)) {
					if (isMultiSelectEnabled) {
						event.preventDefault();
						this.store.selection.setItemSelection({
							event,
							itemId,
							keepExistingSelection: true
						});
					} else if (!selectionSelectors.isItemSelected(this.store.state, itemId)) {
						this.store.selection.setItemSelection({
							event,
							itemId
						});
						event.preventDefault();
					}
				}
				break;
			case key === "ArrowDown": {
				const nextItem = getNextNavigableItem(this.store.state, itemId);
				if (nextItem) {
					event.preventDefault();
					this.store.focus.focusItem(event, nextItem);
					if (isMultiSelectEnabled && event.shiftKey && this.canToggleItemSelection(nextItem)) this.store.selection.selectItemFromArrowNavigation(event, itemId, nextItem);
				}
				break;
			}
			case key === "ArrowUp": {
				const previousItem = getPreviousNavigableItem(this.store.state, itemId);
				if (previousItem) {
					event.preventDefault();
					this.store.focus.focusItem(event, previousItem);
					if (isMultiSelectEnabled && event.shiftKey && this.canToggleItemSelection(previousItem)) this.store.selection.selectItemFromArrowNavigation(event, itemId, previousItem);
				}
				break;
			}
			case key === "ArrowRight" && !this.store.parameters.isRtl || key === "ArrowLeft" && this.store.parameters.isRtl:
				if (ctrlPressed) return;
				if (expansionSelectors.isItemExpanded(this.store.state, itemId)) {
					const nextItemId = getNextNavigableItem(this.store.state, itemId);
					if (nextItemId) {
						this.store.focus.focusItem(event, nextItemId);
						event.preventDefault();
					}
				} else if (this.canToggleItemExpansion(itemId)) {
					this.store.expansion.setItemExpansion({
						event,
						itemId
					});
					event.preventDefault();
				}
				break;
			case key === "ArrowLeft" && !this.store.parameters.isRtl || key === "ArrowRight" && this.store.parameters.isRtl:
				if (ctrlPressed) return;
				if (this.canToggleItemExpansion(itemId) && expansionSelectors.isItemExpanded(this.store.state, itemId)) {
					this.store.expansion.setItemExpansion({
						event,
						itemId
					});
					event.preventDefault();
				} else {
					const parent = itemsSelectors.itemParentId(this.store.state, itemId);
					if (parent) {
						this.store.focus.focusItem(event, parent);
						event.preventDefault();
					}
				}
				break;
			case key === "Home":
				if (this.canToggleItemSelection(itemId) && isMultiSelectEnabled && ctrlPressed && event.shiftKey) this.store.selection.selectRangeFromStartToItem(event, itemId);
				else this.store.focus.focusItem(event, getFirstNavigableItem(this.store.state));
				event.preventDefault();
				break;
			case key === "End":
				if (this.canToggleItemSelection(itemId) && isMultiSelectEnabled && ctrlPressed && event.shiftKey) this.store.selection.selectRangeFromItemToEnd(event, itemId);
				else this.store.focus.focusItem(event, getLastNavigableItem(this.store.state));
				event.preventDefault();
				break;
			case key === "*":
				this.store.expansion.expandAllSiblings(event, itemId);
				event.preventDefault();
				break;
			case String.fromCharCode(event.keyCode) === "A" && ctrlPressed && isMultiSelectEnabled && selectionSelectors.enabled(this.store.state):
				this.store.selection.selectAllNavigableItems(event);
				event.preventDefault();
				break;
			case !ctrlPressed && !event.shiftKey && isPrintableKey(key): {
				this.store.timeoutManager.clearTimeout("typeahead");
				const matchingItem = this.getFirstItemMatchingTypeaheadQuery(itemId, key);
				if (matchingItem != null) {
					this.store.focus.focusItem(event, matchingItem);
					event.preventDefault();
				} else this.typeaheadQuery = "";
				this.store.timeoutManager.startTimeout("typeahead", TYPEAHEAD_TIMEOUT, () => {
					this.typeaheadQuery = "";
				});
				break;
			}
		}
	};
};
function isPrintableKey(string) {
	return !!string && string.length === 1 && !!string.match(/\S/);
}
function createLabelMapFromItemMetaLookup(itemMetaLookup) {
	const labelMap = {};
	const processItem = (item) => {
		labelMap[item.id] = item.label.toLowerCase();
	};
	Object.values(itemMetaLookup).forEach(processItem);
	return labelMap;
}
//#endregion
//#region node_modules/@mui/x-tree-view/internals/MinimalTreeViewStore/TreeViewItemPluginManager.mjs
/**
* Manages the registration and application of plugins for the Tree Item.
* This will be replaced with a proper implementation in the future.
*/
var TreeViewItemPluginManager = class {
	itemPlugins = [];
	itemWrappers = [];
	register = (plugin, wrapItem) => {
		this.itemPlugins.push(plugin);
		if (wrapItem) this.itemWrappers.push(wrapItem);
	};
	listPlugins = () => this.itemPlugins;
	listWrappers = () => this.itemWrappers;
};
//#endregion
//#region node_modules/@mui/x-tree-view/internals/MinimalTreeViewStore/MinimalTreeViewStore.mjs
var MinimalTreeViewStore = class extends Store {
	initialParameters = null;
	disposables = new DisposableStack();
	eventManager = this.disposables.adopt(new EventManager(), (manager) => manager.removeAllListeners());
	timeoutManager = this.disposables.adopt(new TimeoutManager(), (manager) => manager.clearAll());
	itemPluginManager = new TreeViewItemPluginManager();
	constructor(parameters, instanceName, mapper) {
		const minimalInitialState = createMinimalInitialState(parameters);
		const initialState = mapper.getInitialState(minimalInitialState, parameters);
		super(initialState);
		this.parameters = parameters;
		this.instanceName = instanceName;
		this.mapper = mapper;
		this.items = new TreeViewItemsPlugin(this);
		this.focus = new TreeViewFocusPlugin(this);
		this.expansion = new TreeViewExpansionPlugin(this);
		this.selection = new TreeViewSelectionPlugin(this);
		this.keyboardNavigation = new TreeViewKeyboardNavigationPlugin(this);
		this.initialParameters = parameters;
	}
	/**
	* Builds an object containing the method that should be exposed publicly by the Tree View components.
	*/
	buildPublicAPI() {
		return _extends({}, this.items.buildPublicAPI(), this.focus.buildPublicAPI(), this.expansion.buildPublicAPI(), this.selection.buildPublicAPI());
	}
	/**
	* Updates the state of the Tree View based on the new parameters provided to the root component.
	*/
	updateStateFromParameters(parameters) {
		const updateModel = (mutableNewState, controlledProp, defaultProp) => {
			if (parameters[controlledProp] !== void 0) mutableNewState[controlledProp] = parameters[controlledProp];
			{
				const defaultValue = parameters[defaultProp];
				const isControlled = parameters[controlledProp] !== void 0;
				const initialDefaultValue = this.initialParameters?.[defaultProp];
				const initialIsControlled = this.initialParameters?.[controlledProp] !== void 0;
				if (initialIsControlled !== isControlled) warnOnce([
					`MUI X Tree View: A component is changing the ${initialIsControlled ? "" : "un"}controlled ${controlledProp} state of ${this.instanceName} to be ${initialIsControlled ? "un" : ""}controlled.`,
					"Elements should not switch from uncontrolled to controlled (or vice versa).",
					`Decide between using a controlled or uncontrolled ${controlledProp} element for the lifetime of the component.`,
					"The nature of the state is determined during the first render. It's considered controlled if the value is not `undefined`.",
					"More info: https://fb.me/react-controlled-components"
				], "error");
				else if (JSON.stringify(initialDefaultValue) !== JSON.stringify(defaultValue)) warnOnce([`MUI X Tree View: A component is changing the default ${controlledProp} state of an uncontrolled ${this.instanceName} after being initialized. `, `To suppress this warning opt to use a controlled ${this.instanceName}.`], "error");
			}
		};
		const newMinimalState = deriveStateFromParameters$1(parameters);
		updateModel(newMinimalState, "expandedItems", "defaultExpandedItems");
		updateModel(newMinimalState, "selectedItems", "defaultSelectedItems");
		if (this.state.providedTreeId !== parameters.id || this.state.treeId === void 0) newMinimalState.treeId = createTreeViewDefaultId();
		if (!this.mapper.shouldIgnoreItemsStateUpdate(parameters) && TreeViewItemsPlugin.shouldRebuildItemsState(parameters, this.parameters)) Object.assign(newMinimalState, TreeViewItemsPlugin.buildItemsStateIfNeeded(parameters));
		const newState = this.mapper.updateStateFromParameters(newMinimalState, parameters, updateModel);
		this.update(newState);
		this.parameters = parameters;
	}
	/**
	* Runs mount-time side effects that must not happen during render (the store
	* is created during render by `useDisposable`). No-op by default; overridden
	* by stores that kick off work on mount (e.g. lazy-loading fetches). Safe to
	* call more than once (StrictMode replays mount effects).
	*/
	mountEffect = () => {};
	/**
	* Disposes the store synchronously when the component unmounts. `useDisposable`
	* handles React StrictMode's simulated unmount, so this runs once on real unmount.
	*/
	[disposeSymbol]() {
		if (this.disposables.disposed) return;
		try {
			this.disposables.dispose();
		} catch (error) {
			console.error("MUI X Tree View: error while disposing the store.", ...unwrapSuppressedErrors(error));
		}
	}
	/**
	* Whether updates based on `props.items` change should be ignored.
	*/
	shouldIgnoreItemsStateUpdate = () => {
		return this.mapper.shouldIgnoreItemsStateUpdate(this.parameters);
	};
	/**
	* Registers an effect to be run when the value returned by the selector changes.
	*/
	registerStoreEffect = (selector, effect) => {
		let previousValue = selector(this.state);
		this.disposables.defer(this.subscribe((state) => {
			const nextValue = selector(state);
			if (nextValue !== previousValue) {
				effect(previousValue, nextValue);
				previousValue = nextValue;
			}
		}));
	};
	/**
	* Publishes an event to all its subscribers.
	*/
	publishEvent = (name, params, event) => {
		if (isSyntheticEvent(event) && event.isPropagationStopped()) return;
		this.eventManager.emit(name, params, event);
	};
	/**
	* Subscribe to an event emitted by the store.
	* For now, the subscription is only removed when the store is destroyed.
	*/
	subscribeEvent = (eventName, handler) => {
		this.eventManager.on(eventName, handler);
	};
};
function isSyntheticEvent(event) {
	return event?.isPropagationStopped !== void 0;
}
//#endregion
//#region node_modules/@mui/x-tree-view/internals/RichTreeViewStore/RichTreeViewStore.utils.mjs
var deriveStateFromParameters = (parameters) => ({ isItemEditable: parameters.isItemEditable ?? false });
var parametersToStateMapper = {
	getInitialState: (minimalInitialState, parameters) => _extends({}, minimalInitialState, deriveStateFromParameters(parameters), {
		editedItemId: null,
		lazyLoadedItems: null,
		domStructure: parameters.domStructure ?? "nested"
	}),
	updateStateFromParameters: (newMinimalState, parameters) => {
		return _extends({}, newMinimalState, deriveStateFromParameters(parameters));
	},
	shouldIgnoreItemsStateUpdate: () => false
};
//#endregion
//#region node_modules/@mui/x-tree-view/internals/RichTreeViewStore/RichTreeViewStore.mjs
var ExtendableRichTreeViewStore = class extends MinimalTreeViewStore {
	labelEditing = new TreeViewLabelEditingPlugin(this);
	/**
	* Mapper of the RichTreeViewStore.
	* Can be used by classes extending the RichTreeViewStore to create their own mapper.
	*/
	static rawMapper = parametersToStateMapper;
	buildPublicAPI() {
		return _extends({}, super.buildPublicAPI(), this.labelEditing.buildPublicAPI());
	}
};
var RichTreeViewStore = class extends ExtendableRichTreeViewStore {
	constructor(parameters) {
		super(parameters, "RichTreeView", parametersToStateMapper);
	}
};
//#endregion
//#region node_modules/@mui/x-tree-view/RichTreeView/RichTreeView.mjs
var useThemeProps = createUseThemeProps("MuiRichTreeView");
var useUtilityClasses = (ownerState) => {
	const { classes } = ownerState;
	return import_react.useMemo(() => {
		return composeClasses({
			root: ["root"],
			item: ["item"],
			itemContent: ["itemContent"],
			itemGroupTransition: ["itemGroupTransition"],
			itemIconContainer: ["itemIconContainer"],
			itemLabel: ["itemLabel"],
			itemLabelInput: ["itemLabelInput"],
			itemCheckbox: ["itemCheckbox"]
		}, getRichTreeViewUtilityClass, classes);
	}, [classes]);
};
var RichTreeViewRoot = styled("ul", {
	name: "MuiRichTreeView",
	slot: "Root"
})({
	padding: 0,
	margin: 0,
	listStyle: "none",
	outline: 0,
	position: "relative"
});
/**
*
* Demos:
*
* - [Tree View](https://mui.com/x/react-tree-view/)
*
* API:
*
* - [RichTreeView API](https://mui.com/x/api/tree-view/rich-tree-view/)
*/
var RichTreeView = /*#__PURE__*/ import_react.forwardRef(function RichTreeView(inProps, forwardedRef) {
	const props = useThemeProps({
		props: inProps,
		name: "MuiRichTreeView"
	});
	if (props.children != null) warnOnce([
		"MUI X: The Rich Tree View component does not support JSX children.",
		"If you want to add items, you need to use the `items` prop.",
		"Check the documentation for more details: https://mui.com/x/react-tree-view/rich-tree-view/items/."
	]);
	const { slots: inSlots, slotProps, apiRef, parameters, forwardedProps } = useExtractRichTreeViewParameters(props);
	const store = useTreeViewStore(RichTreeViewStore, parameters);
	const ref = import_react.useRef(null);
	const handleRef = useMergedRefs(forwardedRef, ref);
	const isLoading = useStore(store, lazyLoadingSelectors.isItemLoading, null);
	const error = useStore(store, lazyLoadingSelectors.itemError, null);
	const classes = useUtilityClasses(props);
	const slots = import_react.useMemo(() => _extends({ root: RichTreeViewRoot }, inSlots), [inSlots]);
	if (isLoading) return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(Typography, { children: "Loading…" });
	if (error) return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(Alert, {
		severity: "error",
		children: error.message
	});
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(TreeViewProvider, {
		store,
		classes,
		slots,
		slotProps,
		apiRef,
		rootRef: ref,
		children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(TreeViewItemDepthContext.Provider, {
			value: itemsSelectors.itemDepth,
			children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(RichTreeViewItems, {
				slots,
				slotProps,
				forwardedProps,
				ownerState: props,
				rootRef: handleRef
			})
		})
	});
});
RichTreeView.displayName = "RichTreeView";
RichTreeView.propTypes = {
	/**
	* The ref object that allows Tree View manipulation. Can be instantiated with `useRichTreeViewApiRef()`.
	*/
	apiRef: import_prop_types.default.shape({ current: import_prop_types.default.shape({
		focusItem: import_prop_types.default.func,
		getItem: import_prop_types.default.func,
		getItemDOMElement: import_prop_types.default.func,
		getItemOrderedChildrenIds: import_prop_types.default.func,
		getItemTree: import_prop_types.default.func,
		getParentId: import_prop_types.default.func,
		isItemExpanded: import_prop_types.default.func,
		setEditedItem: import_prop_types.default.func,
		setIsItemDisabled: import_prop_types.default.func,
		setItemExpansion: import_prop_types.default.func,
		setItemSelection: import_prop_types.default.func,
		updateItemLabel: import_prop_types.default.func
	}) }),
	/**
	* Whether the Tree View renders a checkbox at the left of its label that allows selecting it.
	* @default false
	*/
	checkboxSelection: import_prop_types.default.bool,
	/**
	* Override or extend the styles applied to the component.
	*/
	classes: import_prop_types.default.object,
	className: import_prop_types.default.string,
	/**
	* Expanded item ids.
	* Used when the item's expansion is not controlled.
	* @default []
	*/
	defaultExpandedItems: import_prop_types.default.arrayOf(import_prop_types.default.string),
	/**
	* Selected item ids. (Uncontrolled)
	* When `multiSelect` is true this takes an array of strings; when false (default) a string.
	* @default []
	*/
	defaultSelectedItems: import_prop_types.default.any,
	/**
	* Whether the items should be focusable when disabled.
	* @default false
	*/
	disabledItemsFocusable: import_prop_types.default.bool,
	/**
	* Whether selection is disabled.
	* @default false
	*/
	disableSelection: import_prop_types.default.bool,
	/**
	* When `'flat'`, the tree is rendered as a flat list (children are rendered as siblings of their parent).
	* When `'nested'`, children are rendered inside their parent's groupTransition slot.
	* Collapse/expansion animations that rely on the groupTransition slot only work with the nested DOM structure.
	* @default 'nested'
	*/
	domStructure: import_prop_types.default.oneOf(["flat", "nested"]),
	/**
	* Expanded item ids.
	* Used when the item's expansion is controlled.
	*/
	expandedItems: import_prop_types.default.arrayOf(import_prop_types.default.string),
	/**
	* The slot that triggers the item's expansion when clicked.
	* @default 'content'
	*/
	expansionTrigger: import_prop_types.default.oneOf(["content", "iconContainer"]),
	/**
	* Used to determine the children of a given item.
	*
	* @template R
	* @param {R} item The item to check.
	* @returns {R[]} The children of the item.
	* @default (item) => item.children
	*/
	getItemChildren: import_prop_types.default.func,
	/**
	* Used to determine the id of a given item.
	*
	* @template R
	* @param {R} item The item to check.
	* @returns {TreeViewItemId} The id of the item.
	* @default (item) => item.id
	*/
	getItemId: import_prop_types.default.func,
	/**
	* Used to determine the string label for a given item.
	*
	* @template R
	* @param {R} item The item to check.
	* @returns {string} The label of the item.
	* @default (item) => item.label
	*/
	getItemLabel: import_prop_types.default.func,
	/**
	* This prop is used to help implement the accessibility logic.
	* If you don't provide this prop. It falls back to a randomly generated id.
	*/
	id: import_prop_types.default.string,
	/**
	* Used to determine if a given item should be disabled.
	* @template R
	* @param {R} item The item to check.
	* @returns {boolean} `true` if the item should be disabled.
	*/
	isItemDisabled: import_prop_types.default.func,
	/**
	* Determine if a given item can be edited.
	* @template R
	* @param {R} item The item to check.
	* @returns {boolean} `true` if the item can be edited.
	* @default () => false
	*/
	isItemEditable: import_prop_types.default.oneOfType([import_prop_types.default.func, import_prop_types.default.bool]),
	/**
	* Used to determine if a given item should have selection disabled.
	* @template R
	* @param {R} item The item to check.
	* @returns {boolean} `true` if the item should have selection disabled.
	*/
	isItemSelectionDisabled: import_prop_types.default.func,
	/**
	* Horizontal indentation between an item and its children.
	* Examples: 24, "24px", "2rem", "2em".
	* @default 12px
	*/
	itemChildrenIndentation: import_prop_types.default.oneOfType([import_prop_types.default.number, import_prop_types.default.string]),
	/**
	* Sets the height in pixel of an item.
	* Set to `null` to explicitly remove any item height restriction when items have different heights.
	* @default null
	*/
	itemHeight: import_prop_types.default.number,
	items: import_prop_types.default.array.isRequired,
	/**
	* Whether multiple items can be selected.
	* @default false
	*/
	multiSelect: import_prop_types.default.bool,
	/**
	* Callback fired when Tree Items are expanded/collapsed.
	* @param {React.SyntheticEvent} event The DOM event that triggered the change. Can be null when the change is caused by the `publicAPI.setItemExpansion()` method.
	* @param {TreeViewItemId[]} itemIds The ids of the expanded items.
	*/
	onExpandedItemsChange: import_prop_types.default.func,
	/**
	* Callback fired when the `content` slot of a given Tree Item is clicked.
	* @param {React.MouseEvent} event The DOM event that triggered the change.
	* @param {TreeViewItemId} itemId The id of the focused item.
	*/
	onItemClick: import_prop_types.default.func,
	/**
	* Callback fired when a Tree Item is expanded or collapsed.
	* @param {React.SyntheticEvent | null} event The DOM event that triggered the change. Can be null when the change is caused by the `publicAPI.setItemExpansion()` method.
	* @param {TreeViewItemId} itemId The itemId of the modified item.
	* @param {boolean} isExpanded `true` if the item has just been expanded, `false` if it has just been collapsed.
	*/
	onItemExpansionToggle: import_prop_types.default.func,
	/**
	* Callback fired when a given Tree Item is focused.
	* @param {React.SyntheticEvent | null} event The DOM event that triggered the change. **Warning**: This is a generic event not a focus event.
	* @param {TreeViewItemId} itemId The id of the focused item.
	*/
	onItemFocus: import_prop_types.default.func,
	/**
	* Callback fired when the label of an item changes.
	* @param {TreeViewItemId} itemId The id of the item that was edited.
	* @param {string} newLabel The new label of the items.
	*/
	onItemLabelChange: import_prop_types.default.func,
	/**
	* Callback fired when a Tree Item is selected or deselected.
	* @param {React.SyntheticEvent} event The DOM event that triggered the change. Can be null when the change is caused by the `publicAPI.setItemSelection()` method.
	* @param {TreeViewItemId} itemId The itemId of the modified item.
	* @param {boolean} isSelected `true` if the item has just been selected, `false` if it has just been deselected.
	*/
	onItemSelectionToggle: import_prop_types.default.func,
	/**
	* Callback fired when Tree Items are selected/deselected.
	* @param {React.SyntheticEvent} event The DOM event that triggered the change. Can be null when the change is caused by the `publicAPI.setItemSelection()` method.
	* @param {TreeViewItemId[] | TreeViewItemId} itemIds The ids of the selected items.
	* When `multiSelect` is `true`, this is an array of strings; when false (default) a string.
	*/
	onSelectedItemsChange: import_prop_types.default.func,
	/**
	* Selected item ids. (Controlled)
	* When `multiSelect` is true this takes an array of strings; when false (default) a string.
	*/
	selectedItems: import_prop_types.default.any,
	/**
	* When `selectionPropagation.descendants` is set to `true`.
	*
	* - Selecting a parent selects all its descendants automatically.
	* - Deselecting a parent deselects all its descendants automatically.
	*
	* When `selectionPropagation.parents` is set to `true`.
	*
	* - Selecting all the descendants of a parent selects the parent automatically.
	* - Deselecting a descendant of a selected parent deselects the parent automatically.
	*
	* Only works when `multiSelect` is `true`.
	* On the <SimpleTreeView />, only the expanded items are considered (since the collapsed item are not passed to the Tree View component at all)
	*
	* @default { parents: false, descendants: false }
	*/
	selectionPropagation: import_prop_types.default.shape({
		descendants: import_prop_types.default.bool,
		parents: import_prop_types.default.bool
	}),
	/**
	* The props used for each component slot.
	* @default {}
	*/
	slotProps: import_prop_types.default.object,
	/**
	* Overridable component slots.
	* @default {}
	*/
	slots: import_prop_types.default.object,
	/**
	* The system prop that allows defining system overrides as well as additional CSS styles.
	*/
	sx: import_prop_types.default.oneOfType([
		import_prop_types.default.arrayOf(import_prop_types.default.oneOfType([
			import_prop_types.default.func,
			import_prop_types.default.object,
			import_prop_types.default.bool
		])),
		import_prop_types.default.func,
		import_prop_types.default.object
	])
};
//#endregion
//#region node_modules/@mui/x-tree-view/hooks/useRichTreeViewApiRef.mjs
/**
* Creates the ref to pass to the `apiRef` prop of the `RichTreeView` component.
*/
function useRichTreeViewApiRef() {
	return import_react.useRef(void 0);
}
//#endregion
//#region node_modules/@mui/x-scheduler/event-calendar/resources-tree/ResourcesTree.mjs
var _excluded$5 = ["itemId", "label"];
var ResourcesTreeRoot = styled("section", {
	name: "MuiEventCalendar",
	slot: "ResourcesTree"
})(({ theme }) => ({
	display: "flex",
	flexDirection: "column",
	padding: theme.spacing(1),
	overflowY: "auto",
	scrollbarWidth: "thin",
	[`&[data-flat] .${treeItemClasses.iconContainer}`]: { display: "none" }
}));
var ResourcesTreeLabel = styled(Typography, {
	name: "MuiEventCalendar",
	slot: "ResourcesTreeLabel"
})(({ theme }) => _extends({}, theme.typography.subtitle2, {
	fontWeight: theme.typography.fontWeightMedium,
	paddingLeft: theme.spacing(1.25),
	paddingBottom: theme.spacing(1)
}));
var ResourcesTreeItemRoot = styled(TreeItem, {
	name: "MuiEventCalendar",
	slot: "ResourcesTreeItem"
})(({ theme }) => ({
	[`& .${treeItemClasses.content}[data-selected]`]: {
		backgroundColor: "transparent",
		"&:hover": {
			backgroundColor: (theme.vars || theme).palette.action.hover,
			"@media (hover: none)": { backgroundColor: "transparent" }
		},
		"&[data-focused]": { backgroundColor: (theme.vars || theme).palette.action.focus }
	},
	[`& .${checkboxClasses.root}`]: {
		color: "var(--event-main)",
		[`&.${checkboxClasses.checked}`]: { color: "var(--event-main)" }
	},
	variants: getPaletteVariants(theme)
}));
var ResourcesTreeItemLabel = styled(Typography, {
	name: "MuiEventCalendar",
	slot: "ResourcesTreeItemLabel"
})(({ theme }) => ({ fontSize: theme.typography.body2.fontSize }));
var getItemLabel = (item) => item.title;
function ResourcesTreeItem(props) {
	const { itemId, label } = props, other = _objectWithoutPropertiesLoose(props, _excluded$5);
	const { classes } = useEventCalendarStyledContext();
	const eventColor = useStore$1(useEventCalendarStoreContext(), schedulerResourceSelectors.defaultEventColor, itemId);
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ResourcesTreeItemRoot, _extends({
		itemId,
		label: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ResourcesTreeItemLabel, {
			className: classes.resourcesTreeItemLabel,
			children: label
		}),
		"data-palette": eventColor,
		className: classes.resourcesTreeItem,
		slotProps: { checkbox: {
			className: classes.resourcesTreeItemCheckbox,
			size: "small",
			slotProps: { input: { tabIndex: -1 } }
		} }
	}, other));
}
var ResourcesTree = /*#__PURE__*/ import_react.forwardRef(function ResourcesTree(props, forwardedRef) {
	const { classes, localeText } = useEventCalendarStyledContext();
	const store = useEventCalendarStoreContext();
	const apiRef = useRichTreeViewApiRef();
	const headingId = import_react.useId();
	const resources = useStore$1(store, schedulerResourceSelectors.processedResourceList);
	const childrenLookup = useStore$1(store, schedulerResourceSelectors.processedResourceChildrenLookup);
	const flatList = useStore$1(store, schedulerResourceSelectors.processedResourceFlatList);
	const visibleMap = useStore$1(store, schedulerResourceSelectors.visibleMap);
	const collapsedResources = useStore$1(store, schedulerResourceSelectors.collapsedResources);
	const getItemChildren = import_react.useCallback((item) => childrenLookup.get(item.id) ?? [], [childrenLookup]);
	const selectedItems = import_react.useMemo(() => {
		const fullyVisible = /* @__PURE__ */ new Set();
		for (let i = flatList.length - 1; i >= 0; i -= 1) {
			const resource = flatList[i];
			if (visibleMap[resource.id] === false) continue;
			const children = childrenLookup.get(resource.id);
			if (!children || children.every((child) => fullyVisible.has(child.id))) fullyVisible.add(resource.id);
		}
		return flatList.filter((r) => fullyVisible.has(r.id)).map((r) => r.id);
	}, [
		flatList,
		visibleMap,
		childrenLookup
	]);
	const expandedItems = import_react.useMemo(() => Array.from(childrenLookup.keys()).filter((id) => collapsedResources[id] !== true), [childrenLookup, collapsedResources]);
	const handleExpandedItemsChange = useStableCallback((event, newExpandedIds) => {
		const expanded = new Set(newExpandedIds);
		const newCollapsedResources = {};
		for (const parentId of childrenLookup.keys()) if (!expanded.has(parentId)) newCollapsedResources[parentId] = true;
		store.setCollapsedResources(newCollapsedResources, event?.nativeEvent);
	});
	const handleSelectedItemsChange = useStableCallback((event, newSelectedIds) => {
		const newSelected = new Set(newSelectedIds);
		const newVisibleResources = {};
		for (let i = flatList.length - 1; i >= 0; i -= 1) {
			const resource = flatList[i];
			if (newSelected.has(resource.id)) {
				newVisibleResources[resource.id] = true;
				continue;
			}
			const hasVisibleDescendant = childrenLookup.get(resource.id)?.some((child) => newVisibleResources[child.id] === true);
			newVisibleResources[resource.id] = hasVisibleDescendant ?? false;
		}
		store.setVisibleResources(newVisibleResources, event?.nativeEvent);
	});
	const handleItemClick = useStableCallback((event, itemId) => {
		const target = event.target;
		if (target.closest(`.${treeItemClasses.checkbox}`) || target.closest(`.${treeItemClasses.iconContainer}`)) return;
		apiRef.current?.setItemSelection({
			event,
			itemId,
			keepExistingSelection: true
		});
	});
	if (resources.length === 0) return null;
	const isFlat = childrenLookup.size === 0;
	return /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(ResourcesTreeRoot, _extends({ ref: forwardedRef }, props, {
		className: clsx(props.className, classes.resourcesTree),
		"data-flat": isFlat || void 0,
		children: [/*#__PURE__*/ (0, import_jsx_runtime.jsx)(ResourcesTreeLabel, {
			id: headingId,
			className: classes.resourcesTreeLabel,
			children: localeText.resourcesLabel
		}), /*#__PURE__*/ (0, import_jsx_runtime.jsx)(RichTreeView, {
			items: resources,
			apiRef,
			getItemChildren,
			getItemLabel,
			multiSelect: true,
			checkboxSelection: true,
			selectionPropagation: {
				parents: true,
				descendants: true
			},
			expansionTrigger: "iconContainer",
			selectedItems,
			expandedItems,
			onExpandedItemsChange: handleExpandedItemsChange,
			onSelectedItemsChange: handleSelectedItemsChange,
			onItemClick: handleItemClick,
			slots: { item: ResourcesTreeItem },
			"aria-labelledby": headingId
		})]
	}));
});
ResourcesTree.displayName = "ResourcesTree";
//#endregion
//#region node_modules/@mui/x-scheduler/event-calendar/mini-calendar/MiniCalendar.mjs
var _excluded$4 = ["className"];
var MiniCalendarRoot = styled("div", {
	name: "MuiEventCalendar",
	slot: "MiniCalendar"
})(({ theme }) => ({
	userSelect: "none",
	padding: theme.spacing(1)
}));
var MiniCalendarHeader = styled("div", {
	name: "MuiEventCalendar",
	slot: "MiniCalendarHeader"
})(({ theme }) => ({
	display: "flex",
	alignItems: "center",
	justifyContent: "space-between",
	marginBottom: theme.spacing(1)
}));
var MiniCalendarNavigation = styled("div", {
	name: "MuiEventCalendar",
	slot: "MiniCalendarNavigation"
})(({ theme }) => ({
	display: "flex",
	alignItems: "center",
	gap: theme.spacing(.5)
}));
var MiniCalendarMonthLabel = styled("span", {
	name: "MuiEventCalendar",
	slot: "MiniCalendarMonthLabel"
})(({ theme }) => _extends({}, theme.typography.subtitle2, {
	fontWeight: theme.typography.fontWeightMedium,
	paddingLeft: theme.spacing(1.25)
}));
var MiniCalendarWeekdayHeader = styled("div", {
	name: "MuiEventCalendar",
	slot: "MiniCalendarWeekdayHeader"
})(({ theme }) => ({
	display: "grid",
	gridTemplateColumns: "repeat(7, 1fr)",
	textAlign: "center",
	marginBottom: theme.spacing(.5)
}));
var MiniCalendarWeekdayCell = styled("span", {
	name: "MuiEventCalendar",
	slot: "MiniCalendarWeekdayCell"
})(({ theme }) => ({
	fontSize: theme.typography.caption.fontSize,
	color: (theme.vars || theme).palette.text.secondary,
	padding: theme.spacing(.5, 0),
	"&[data-weekend]": { color: (theme.vars || theme).palette.error.main }
}));
var MiniCalendarGrid = styled("div", {
	name: "MuiEventCalendar",
	slot: "MiniCalendarGrid"
})({
	display: "flex",
	flexDirection: "column"
});
var MiniCalendarWeekRow = styled("div", {
	name: "MuiEventCalendar",
	slot: "MiniCalendarWeekRow"
})({
	display: "grid",
	gridTemplateColumns: "repeat(7, 1fr)"
});
var MiniCalendarDayCell = styled("div", {
	name: "MuiEventCalendar",
	slot: "MiniCalendarDayCell"
})({
	display: "flex",
	justifyContent: "center",
	alignItems: "center",
	aspectRatio: "1"
});
var MiniCalendarDayButton = styled("button", {
	name: "MuiEventCalendar",
	slot: "MiniCalendarDayButton"
})(({ theme }) => ({
	width: 28,
	height: 28,
	display: "flex",
	alignItems: "center",
	justifyContent: "center",
	border: "none",
	borderRadius: "50%",
	background: "none",
	cursor: "pointer",
	fontSize: theme.typography.caption.fontSize,
	color: (theme.vars || theme).palette.text.primary,
	padding: 0,
	"&:hover": { backgroundColor: theme.alpha((theme.vars || theme).palette.primary.main, .08) },
	"&:focus-visible": {
		outline: `2px solid ${(theme.vars || theme).palette.primary.main}`,
		outlineOffset: 2
	},
	"&[data-other-month]": { color: (theme.vars || theme).palette.text.disabled },
	"&[data-today]:not([data-active])": {
		fontWeight: theme.typography.fontWeightBold,
		color: (theme.vars || theme).palette.primary.main,
		backgroundColor: theme.alpha((theme.vars || theme).palette.primary.main, .15)
	},
	"&[data-active]": {
		backgroundColor: (theme.vars || theme).palette.primary.main,
		color: (theme.vars || theme).palette.primary.contrastText,
		"&:hover": { backgroundColor: (theme.vars || theme).palette.primary.dark }
	},
	"&[data-today][data-active]": { fontWeight: theme.typography.fontWeightBold }
}));
/**
* A compact month calendar for navigating to specific days.
*/
var MiniCalendar = /*#__PURE__*/ import_react.forwardRef(function MiniCalendar(props, forwardedRef) {
	const { className } = props, other = _objectWithoutPropertiesLoose(props, _excluded$4);
	const adapter = useAdapterContext();
	const store = useEventCalendarStoreContext();
	const { classes, localeText } = useEventCalendarStyledContext();
	const visibleDate = useStore$1(store, schedulerOtherSelectors.visibleDate);
	const now = useStore$1(store, schedulerNowSelectors.nowUpdatedEveryMinute);
	const weekStartsOn = useStore$1(store, eventCalendarPreferenceSelectors.weekStartsOn);
	const [displayedMonth, setDisplayedMonth] = import_react.useState(() => adapter.startOfMonth(visibleDate));
	import_react.useEffect(() => {
		setDisplayedMonth(adapter.startOfMonth(visibleDate));
	}, [adapter, visibleDate]);
	const days = import_react.useMemo(() => {
		const gridStart = getStartOfWeek(adapter, adapter.startOfMonth(displayedMonth), weekStartsOn);
		return getDayList({
			adapter,
			start: gridStart,
			end: adapter.addDays(gridStart, 41)
		});
	}, [
		adapter,
		displayedMonth,
		weekStartsOn
	]);
	const weeks = import_react.useMemo(() => {
		const result = [];
		for (let i = 0; i < days.length; i += 7) result.push(days.slice(i, i + 7));
		return result;
	}, [days]);
	const weekdays = weeks[0] ?? [];
	const monthYearLabel = import_react.useMemo(() => formatMonthFullLetterAndYear(displayedMonth, adapter), [adapter, displayedMonth]);
	return /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(MiniCalendarRoot, _extends({
		ref: forwardedRef,
		role: "grid",
		"aria-label": localeText.miniCalendarLabel,
		"aria-rowcount": 1 + weeks.length,
		"aria-colcount": 7,
		className: clsx(classes.miniCalendar, className)
	}, other, { children: [
		/*#__PURE__*/ (0, import_jsx_runtime.jsxs)(MiniCalendarHeader, {
			className: classes.miniCalendarHeader,
			children: [/*#__PURE__*/ (0, import_jsx_runtime.jsx)(MiniCalendarMonthLabel, {
				className: classes.miniCalendarMonthLabel,
				children: monthYearLabel
			}), /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(MiniCalendarNavigation, {
				className: classes.miniCalendarNavigation,
				children: [/*#__PURE__*/ (0, import_jsx_runtime.jsx)(IconButton, {
					className: classes.miniCalendarPreviousButton,
					size: "small",
					"aria-label": localeText.miniCalendarGoToPreviousMonth,
					onClick: () => setDisplayedMonth((prev) => adapter.addMonths(prev, -1)),
					children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ChevronLeft_default, { fontSize: "small" })
				}), /*#__PURE__*/ (0, import_jsx_runtime.jsx)(IconButton, {
					className: classes.miniCalendarNextButton,
					size: "small",
					"aria-label": localeText.miniCalendarGoToNextMonth,
					onClick: () => setDisplayedMonth((prev) => adapter.addMonths(prev, 1)),
					children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ChevronRight_default, { fontSize: "small" })
				})]
			})]
		}),
		/*#__PURE__*/ (0, import_jsx_runtime.jsx)(MiniCalendarWeekdayHeader, {
			role: "row",
			"aria-rowindex": 1,
			className: classes.miniCalendarWeekdayHeader,
			children: weekdays.map((day, dayIndex) => /*#__PURE__*/ (0, import_jsx_runtime.jsx)(MiniCalendarWeekdayCell, {
				role: "columnheader",
				"aria-label": adapter.formatByString(day.value, adapter.formats.weekday),
				"aria-colindex": dayIndex + 1,
				className: classes.miniCalendarWeekdayCell,
				"data-weekend": isWeekend(adapter, day.value) || void 0,
				children: adapter.formatByString(day.value, adapter.formats.weekday1Letter)
			}, day.key))
		}),
		/*#__PURE__*/ (0, import_jsx_runtime.jsx)(MiniCalendarGrid, {
			role: "rowgroup",
			className: classes.miniCalendarGrid,
			children: weeks.map((week, weekIndex) => /*#__PURE__*/ (0, import_jsx_runtime.jsx)(MiniCalendarWeekRow, {
				role: "row",
				"aria-rowindex": weekIndex + 2,
				className: classes.miniCalendarWeekRow,
				children: week.map((day, dayIndex) => {
					const isToday = adapter.isSameDay(day.value, now);
					const isActive = adapter.isSameDay(day.value, visibleDate);
					const isOtherMonth = !adapter.isSameMonth(day.value, displayedMonth);
					const fullDateLabel = adapter.formatByString(day.value, adapter.formats.localizedDateWithFullMonthAndWeekDay);
					return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(MiniCalendarDayCell, {
						role: "gridcell",
						"aria-colindex": dayIndex + 1,
						className: classes.miniCalendarDayCell,
						children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(MiniCalendarDayButton, {
							type: "button",
							className: classes.miniCalendarDayButton,
							"data-other-month": isOtherMonth || void 0,
							"data-today": isToday || void 0,
							"data-active": isActive || void 0,
							"aria-label": fullDateLabel,
							"aria-current": isToday ? "date" : void 0,
							"aria-selected": isActive,
							tabIndex: isActive ? 0 : -1,
							onClick: (event) => store.goToDate(day.value, event),
							children: adapter.formatByString(day.value, adapter.formats.dayOfMonth)
						})
					}, day.key);
				})
			}, weekIndex))
		})
	] }));
});
MiniCalendar.displayName = "MiniCalendar";
//#endregion
//#region node_modules/@mui/icons-material/Close.mjs
var Close_default = createSvgIcon(/*#__PURE__*/ (0, import_jsx_runtime.jsx)("path", { d: "M19 6.41 17.59 5 12 10.59 6.41 5 5 6.41 10.59 12 5 17.59 6.41 19 12 13.41 17.59 19 19 17.59 13.41 12z" }), "Close");
//#endregion
//#region node_modules/@mui/icons-material/ExpandLessOutlined.mjs
var ExpandLessOutlined_default = createSvgIcon(/*#__PURE__*/ (0, import_jsx_runtime.jsx)("path", { d: "m12 8-6 6 1.41 1.41L12 10.83l4.59 4.58L18 14z" }), "ExpandLessOutlined");
//#endregion
//#region node_modules/@mui/icons-material/CalendarViewMonthOutlined.mjs
var CalendarViewMonthOutlined_default = createSvgIcon(/*#__PURE__*/ (0, import_jsx_runtime.jsx)("path", { d: "M20 4H4c-1.1 0-2 .9-2 2v12c0 1.1.9 2 2 2h16c1.1 0 2-.9 2-2V6c0-1.1-.9-2-2-2M8 11H4V6h4zm6 0h-4V6h4zm6 0h-4V6h4zM8 18H4v-5h4zm6 0h-4v-5h4zm6 0h-4v-5h4z" }), "CalendarViewMonthOutlined");
//#endregion
//#region node_modules/@mui/icons-material/CalendarViewWeekOutlined.mjs
var CalendarViewWeekOutlined_default = createSvgIcon(/*#__PURE__*/ (0, import_jsx_runtime.jsx)("path", { d: "M20 4H4c-1.1 0-2 .9-2 2v12c0 1.1.9 2 2 2h16c1.1 0 2-.9 2-2V6c0-1.1-.9-2-2-2m-7 2h2.5v12H13zm-2 12H8.5V6H11zM4 6h2.5v12H4zm16 12h-2.5V6H20z" }), "CalendarViewWeekOutlined");
//#endregion
//#region node_modules/@mui/icons-material/CalendarViewDayOutlined.mjs
var CalendarViewDayOutlined_default = createSvgIcon(/*#__PURE__*/ (0, import_jsx_runtime.jsx)("path", { d: "M3 17h18v2H3zm16-5v1H5v-1zm2-2H3v5h18zM3 6h18v2H3z" }), "CalendarViewDayOutlined");
//#endregion
//#region node_modules/@mui/icons-material/ViewAgendaOutlined.mjs
var ViewAgendaOutlined_default = createSvgIcon(/*#__PURE__*/ (0, import_jsx_runtime.jsx)("path", { d: "M19 13H5c-1.1 0-2 .9-2 2v4c0 1.1.9 2 2 2h14c1.1 0 2-.9 2-2v-4c0-1.1-.9-2-2-2m0 6H5v-4h14zm0-16H5c-1.1 0-2 .9-2 2v4c0 1.1.9 2 2 2h14c1.1 0 2-.9 2-2V5c0-1.1-.9-2-2-2m0 6H5V5h14z" }), "ViewAgendaOutlined");
//#endregion
//#region node_modules/@mui/x-scheduler/event-calendar/side-panel-drawer/SidePanelDrawer.mjs
var _excluded$3 = [
	"open",
	"onClose",
	"container",
	"className"
];
var SidePanelDrawerRoot = styled(Drawer, {
	name: "MuiEventCalendar",
	slot: "SidePanelDrawerViewport"
})(({ theme }) => ({
	position: "absolute",
	zIndex: theme.zIndex.drawer,
	"& .MuiBackdrop-root": {
		position: "absolute",
		backgroundColor: "rgba(0, 0, 0, 0.5)"
	},
	"& .MuiDrawer-paper": {
		position: "absolute",
		boxSizing: "border-box",
		width: "min(20rem, 85%)",
		height: "100%",
		display: "flex",
		flexDirection: "column",
		backgroundColor: (theme.vars || theme).palette.background.paper,
		borderRight: "1px solid",
		borderColor: (theme.vars || theme).palette.divider,
		overflowY: "auto",
		overscrollBehavior: "contain"
	}
}));
var SidePanelDrawerHeader = styled("div", {
	name: "MuiEventCalendar",
	slot: "SidePanelDrawerHeader"
})(({ theme }) => ({
	display: "flex",
	alignItems: "center",
	gap: theme.spacing(1),
	borderBottom: "1px solid",
	borderColor: (theme.vars || theme).palette.divider,
	padding: theme.spacing(2)
}));
var SidePanelDrawerTitle = styled("h2", {
	name: "MuiEventCalendar",
	slot: "SidePanelDrawerTitle"
})(({ theme }) => _extends({
	flex: 1,
	margin: 0
}, theme.typography.h6, { fontWeight: theme.typography.fontWeightBold }));
var SidePanelDrawerViewList = styled(MenuList, {
	name: "MuiEventCalendar",
	slot: "SidePanelDrawerViewList"
})(({ theme }) => ({
	borderBottom: "1px solid",
	borderColor: (theme.vars || theme).palette.divider
}));
var SidePanelDrawerResources = styled("div", {
	name: "MuiEventCalendar",
	slot: "SidePanelDrawerResources"
})(({ theme }) => ({
	flexShrink: 0,
	display: "flex",
	flexDirection: "column",
	borderBottom: "1px solid",
	borderColor: (theme.vars || theme).palette.divider
}));
var VIEW_ICONS = {
	day: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(CalendarViewDayOutlined_default, { fontSize: "small" }),
	week: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(CalendarViewWeekOutlined_default, { fontSize: "small" }),
	month: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(CalendarViewMonthOutlined_default, { fontSize: "small" }),
	agenda: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ViewAgendaOutlined_default, { fontSize: "small" })
};
/**
* Compact-layout side panel (MUI temporary `Drawer`) with view switching, resources, and
* preferences (inline `Collapse`). Portaled into the calendar root to stay container-scoped.
*/
var SidePanelDrawer = /*#__PURE__*/ import_react.forwardRef(function SidePanelDrawer(props, forwardedRef) {
	const { open, onClose, container, className } = props, other = _objectWithoutPropertiesLoose(props, _excluded$3);
	const store = useEventCalendarStoreContext();
	const adapter = useAdapterContext();
	const { schedulerId, classes, localeText } = useEventCalendarStyledContext();
	const views = useStore$1(store, eventCalendarViewSelectors.views);
	const view = useStore$1(store, eventCalendarViewSelectors.view);
	const visibleDate = useStore$1(store, schedulerOtherSelectors.visibleDate);
	const { hasAnyOption, items: preferencesItems } = usePreferencesMenuItems();
	const [preferencesOpen, setPreferencesOpen] = import_react.useState(false);
	const showViewSwitcher = views.length > 1;
	const titleId = `${schedulerId}-side-panel-drawer-title`;
	const portalContainer = container?.current ?? void 0;
	import_react.useEffect(() => {
		if (!open) setPreferencesOpen(false);
	}, [open]);
	return /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(SidePanelDrawerRoot, {
		anchor: "left",
		open,
		onClose,
		className: classes.sidePanelDrawerViewport,
		"data-compact-only": "",
		slotProps: {
			root: {
				container: portalContainer,
				disableScrollLock: true
			},
			backdrop: { className: classes.sidePanelDrawerBackdrop },
			paper: _extends({ ref: forwardedRef }, other, {
				className: clsx(className, classes.sidePanelDrawer),
				role: "dialog",
				"aria-modal": true,
				"aria-labelledby": titleId
			})
		},
		children: [
			/*#__PURE__*/ (0, import_jsx_runtime.jsxs)(SidePanelDrawerHeader, {
				className: classes.sidePanelDrawerHeader,
				children: [/*#__PURE__*/ (0, import_jsx_runtime.jsxs)(SidePanelDrawerTitle, {
					id: titleId,
					className: classes.sidePanelDrawerTitle,
					children: [
						adapter.format(visibleDate, "monthFullLetter"),
						" ",
						adapter.format(visibleDate, "yearPadded")
					]
				}), /*#__PURE__*/ (0, import_jsx_runtime.jsx)(IconButton, {
					className: classes.sidePanelDrawerCloseButton,
					"aria-label": localeText.closeSidePanel,
					onClick: onClose,
					children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(Close_default, {})
				})]
			}),
			showViewSwitcher && /*#__PURE__*/ (0, import_jsx_runtime.jsx)(SidePanelDrawerViewList, {
				className: classes.sidePanelDrawerViewList,
				role: "listbox",
				children: views.map((viewItem) => /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(MenuItem, {
					className: classes.sidePanelDrawerViewItem,
					role: "option",
					selected: view === viewItem,
					"aria-selected": view === viewItem,
					onClick: (event) => store.setView(viewItem, event.nativeEvent),
					children: [/*#__PURE__*/ (0, import_jsx_runtime.jsx)(ListItemIcon, { children: VIEW_ICONS[viewItem] }), /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ListItemText, { children: localeText[viewItem] })]
				}, viewItem))
			}),
			/*#__PURE__*/ (0, import_jsx_runtime.jsx)(SidePanelDrawerResources, { children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ResourcesTree, {}) }),
			hasAnyOption && /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(import_react.Fragment, { children: [/*#__PURE__*/ (0, import_jsx_runtime.jsx)("div", {
				className: classes.sidePanelDrawerPreferences,
				children: /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(ListItemButton, {
					className: classes.sidePanelDrawerPreferencesButton,
					"aria-expanded": preferencesOpen,
					onClick: () => setPreferencesOpen((prev) => !prev),
					children: [
						/*#__PURE__*/ (0, import_jsx_runtime.jsx)(ListItemIcon, { children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(SettingsOutlined_default, {}) }),
						/*#__PURE__*/ (0, import_jsx_runtime.jsx)(ListItemText, { children: localeText.preferencesMenu }),
						/*#__PURE__*/ (0, import_jsx_runtime.jsx)(ListItemIcon, { children: preferencesOpen ? /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ExpandLessOutlined_default, {}) : /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ExpandMoreOutlined_default, {}) })
					]
				})
			}), /*#__PURE__*/ (0, import_jsx_runtime.jsx)(Collapse, {
				in: preferencesOpen,
				unmountOnExit: true,
				children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(MenuList, {
					className: classes.sidePanelDrawerPreferencesList,
					"aria-label": localeText.preferencesMenu,
					children: preferencesItems
				})
			})] })
		]
	});
});
SidePanelDrawer.displayName = "SidePanelDrawer";
//#endregion
//#region node_modules/@mui/x-scheduler/event-calendar/EventCalendarRoot.mjs
var _excluded$2 = ["children", "className"];
var EventCalendarRootStyled = styled("div", {
	name: "MuiEventCalendar",
	slot: "Root"
})(({ theme }) => ({
	boxSizing: "border-box",
	"*, *::before, *::after": { boxSizing: "inherit" },
	position: "relative",
	width: "100%",
	display: "flex",
	flexDirection: "column",
	gap: theme.spacing(1),
	height: "100%",
	minHeight: 0,
	overflow: "hidden",
	fontFamily: theme.typography.fontFamily,
	containerType: "inline-size",
	containerName: EVENT_CALENDAR_ROOT_CONTAINER_NAME,
	[eventCalendarRootCompactQuery]: { "& [data-expanded-only]": { display: "none" } },
	[eventCalendarRootExpandedQuery]: { "& [data-compact-only]": { display: "none" } }
}));
var EventCalendarSidePanel = styled("aside", {
	name: "MuiEventCalendar",
	slot: "SidePanel"
})(({ theme }) => ({
	minWidth: 250,
	width: "fit-content",
	display: "flex",
	flexDirection: "column",
	gap: theme.spacing(2),
	border: "1px solid",
	borderColor: (theme.vars || theme).palette.divider,
	borderRadius: theme.shape.borderRadius,
	maxHeight: "100%",
	overflowY: "hidden"
}));
var EventCalendarSidePanelCollapse = styled(Collapse, {
	name: "MuiEventCalendar",
	slot: "SidePanelCollapse"
})({ [eventCalendarRootCompactQuery]: { display: "none" } });
var EventCalendarMainPanel = styled("div", {
	name: "MuiEventCalendar",
	slot: "MainPanel"
})(({ theme }) => ({
	display: "flex",
	flexGrow: 1,
	gap: theme.spacing(1),
	minHeight: 0,
	"&[data-view=\"month\"]": {
		maxHeight: "100%",
		overflow: "hidden"
	}
}));
var EventCalendarContent = styled("section", {
	name: "MuiEventCalendar",
	slot: "Content"
})(() => _extends({
	display: "flex",
	flex: 1,
	overflow: "auto",
	height: "100%",
	maxHeight: "100%"
}, responsiveTypographyContainerQueries, { "&[data-side-panel-open=\"false\"]": { gridColumn: "1 / -1" } }));
/**
* Internal component that renders the EventCalendar UI.
* Used by both EventCalendar and EventCalendarPremium.
* Expects the store to be provided via context.
*/
var EventCalendarRoot = /*#__PURE__*/ import_react.forwardRef(function EventCalendarRoot(props, forwardedRef) {
	const { children, className } = props, other = _objectWithoutPropertiesLoose(props, _excluded$2);
	const store = useEventCalendarStoreContext();
	const { classes, localeText } = useEventCalendarStyledContext();
	const view = useStore$1(store, eventCalendarViewSelectors.view);
	const isSidePanelOpen = useStore$1(store, eventCalendarPreferenceSelectors.isSidePanelOpen);
	const [isCompactDrawerOpen, setIsCompactDrawerOpen] = import_react.useState(false);
	let content;
	switch (view) {
		case "week":
			content = /*#__PURE__*/ (0, import_jsx_runtime.jsx)(WeekView, {});
			break;
		case "day":
			content = /*#__PURE__*/ (0, import_jsx_runtime.jsx)(DayView, {});
			break;
		case "month":
			content = /*#__PURE__*/ (0, import_jsx_runtime.jsx)(MonthView, {});
			break;
		case "agenda":
			content = /*#__PURE__*/ (0, import_jsx_runtime.jsx)(AgendaView, {});
			break;
		default: content = null;
	}
	const rootRef = import_react.useRef(null);
	const handleRootRef = useMergedRefs(forwardedRef, rootRef);
	useResizeObserver(rootRef, (entries) => {
		if (entries[0].contentRect.width >= 550) setIsCompactDrawerOpen(false);
	}, isCompactDrawerOpen);
	return /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(EventCalendarRootStyled, _extends({ className: clsx(classes.root, className) }, other, {
		ref: handleRootRef,
		children: [
			/*#__PURE__*/ (0, import_jsx_runtime.jsx)(HeaderToolbar, { onCompactMenuClick: () => setIsCompactDrawerOpen(true) }),
			/*#__PURE__*/ (0, import_jsx_runtime.jsxs)(EventCalendarMainPanel, {
				className: classes.mainPanel,
				"data-view": view,
				children: [/*#__PURE__*/ (0, import_jsx_runtime.jsx)(EventCalendarSidePanelCollapse, {
					in: isSidePanelOpen,
					orientation: "horizontal",
					className: classes.sidePanelCollapse,
					children: /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(EventCalendarSidePanel, {
						className: classes.sidePanel,
						children: [
							/*#__PURE__*/ (0, import_jsx_runtime.jsx)(MiniCalendar, {}),
							/*#__PURE__*/ (0, import_jsx_runtime.jsx)(Divider, { className: classes.sidePanelDivider }),
							/*#__PURE__*/ (0, import_jsx_runtime.jsx)(ResourcesTree, {})
						]
					})
				}), /*#__PURE__*/ (0, import_jsx_runtime.jsx)(ResponsiveTypographyContainer, { children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(EventCalendarContent, {
					className: classes.content,
					"data-view": view,
					"data-side-panel-open": isSidePanelOpen,
					"aria-label": localeText.calendarContentAriaLabel,
					children: content
				}) })]
			}),
			/*#__PURE__*/ (0, import_jsx_runtime.jsx)(SidePanelDrawer, {
				open: isCompactDrawerOpen,
				onClose: () => setIsCompactDrawerOpen(false),
				container: rootRef
			}),
			/*#__PURE__*/ (0, import_jsx_runtime.jsx)(ErrorContainer, {}),
			children
		]
	}));
});
EventCalendarRoot.displayName = "EventCalendarRoot";
//#endregion
//#region node_modules/@mui/x-scheduler/event-calendar/EventCalendar.mjs
var _excluded$1 = ["className", "classes"], _excluded2 = ["localeText", "apiRef"];
var EventCalendar = /*#__PURE__*/ import_react.forwardRef(function EventCalendar(inProps, forwardedRef) {
	const _useExtractEventCalen = useExtractEventCalendarParameters(useThemeProps$2({
		props: inProps,
		name: "MuiEventCalendar"
	})), { parameters, forwardedProps: { className, classes: classesProp } } = _useExtractEventCalen, forwardedProps = _objectWithoutPropertiesLoose(_useExtractEventCalen.forwardedProps, _excluded$1);
	const store = useEventCalendar(parameters);
	const classes = useEventCalendarUtilityClasses(classesProp);
	const { localeText, apiRef } = forwardedProps, other = _objectWithoutPropertiesLoose(forwardedProps, _excluded2);
	useInitializeApiRef(store, apiRef);
	const schedulerId = useId();
	const mergedLocaleText = import_react.useMemo(() => _extends({}, EVENT_CALENDAR_DEFAULT_LOCALE_TEXT, localeText), [localeText]);
	const calendarStyledContextValue = import_react.useMemo(() => ({
		schedulerId,
		classes,
		localeText: mergedLocaleText
	}), [
		schedulerId,
		classes,
		mergedLocaleText
	]);
	const dialogStyledContextValue = import_react.useMemo(() => ({
		schedulerId,
		classes,
		localeText: mergedLocaleText
	}), [
		schedulerId,
		classes,
		mergedLocaleText
	]);
	const sharedComponentsStyledContextValue = import_react.useMemo(() => ({ classes }), [classes]);
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(SchedulerStoreContext.Provider, {
		value: store,
		children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(EventCalendarStyledContext.Provider, {
			value: calendarStyledContextValue,
			children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(EventDialogStyledContext.Provider, {
				value: dialogStyledContextValue,
				children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(SharedComponentsStyledContext.Provider, {
					value: sharedComponentsStyledContextValue,
					children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(EventDialogProvider, { children: /*#__PURE__*/ (0, import_jsx_runtime.jsx)(EventCalendarRoot, _extends({ className }, other, { ref: forwardedRef })) })
				})
			})
		})
	});
});
EventCalendar.displayName = "EventCalendar";
EventCalendar.propTypes = {
	/**
	* The ref object that allows Event Calendar manipulation.
	* Can be instantiated with `useEventCalendarApiRef()`.
	*/
	apiRef: import_prop_types.default.shape({ current: import_prop_types.default.shape({ setVisibleDate: import_prop_types.default.func }) }),
	/**
	* Whether the event can be dragged to change its start and end dates without changing the duration.
	* @default true
	*/
	areEventsDraggable: import_prop_types.default.bool,
	/**
	* Whether the event start or end can be dragged to change its duration without changing its other date.
	* If `true`, both start and end can be resized.
	* If `false`, the events are not resizable.
	* If `"start"`, only the start can be resized.
	* If `"end"`, only the end can be resized.
	* @default true
	*/
	areEventsResizable: import_prop_types.default.oneOfType([import_prop_types.default.oneOf(["end", "start"]), import_prop_types.default.bool]),
	/**
	* Whether events can be dragged from outside of the calendar and dropped into it.
	* @default false
	*/
	canDragEventsFromTheOutside: import_prop_types.default.bool,
	/**
	* Whether events can be dragged from inside of the calendar and dropped outside of it.
	* If true, when the mouse leaves the calendar, the event won't be rendered inside the calendar anymore.
	* If false, when the mouse leaves the calendar, the event will be rendered in its last valid position inside the calendar.
	* @default false
	*/
	canDropEventsToTheOutside: import_prop_types.default.bool,
	/**
	* Override or extend the styles applied to the component.
	*/
	classes: import_prop_types.default.object,
	/**
	* The collapsed resources. A resource is expanded unless included here with a `true` value.
	*/
	collapsedResources: import_prop_types.default.object,
	/**
	* Data source for fetching events asynchronously.
	* When provided, events are fetched through the data source instead of the `events` prop.
	*/
	dataSource: import_prop_types.default.shape({
		getEvents: import_prop_types.default.func.isRequired,
		persistEvents: import_prop_types.default.func.isRequired
	}),
	/**
	* The locale object from `date-fns` used to format dates.
	* This affects day names, month names, week start day, and other locale-dependent formatting.
	* Import a locale from `date-fns/locale` and pass it to this prop.
	* @default enUS (English)
	*/
	dateLocale: import_prop_types.default.object,
	/**
	* The resources initially collapsed.
	* To render a controlled scheduler, use the `collapsedResources` prop.
	* @default {} - all resources are expanded
	*/
	defaultCollapsedResources: import_prop_types.default.object,
	/**
	* The default preferences for the calendar.
	* To use controlled preferences, use the `preferences` prop.
	* @default { showWeekends: true, showWeekNumber: false, isSidePanelOpen: true, showEmptyDaysInAgenda: true, ampm: true }
	*/
	defaultPreferences: import_prop_types.default.shape({
		ampm: import_prop_types.default.bool,
		isSidePanelOpen: import_prop_types.default.bool,
		showEmptyDaysInAgenda: import_prop_types.default.bool,
		showWeekends: import_prop_types.default.bool,
		showWeekNumber: import_prop_types.default.bool,
		weekStartsOn: import_prop_types.default.oneOf([
			0,
			1,
			2,
			3,
			4,
			5,
			6
		])
	}),
	/**
	* The view initially displayed in the calendar.
	* To render a controlled calendar, use the `view` prop.
	* @default "week"
	*/
	defaultView: import_prop_types.default.oneOf([
		"agenda",
		"day",
		"month",
		"week"
	]),
	/**
	* The date initially used to determine the visible date range.
	* To render a controlled component, use the `visibleDate` prop.
	* @default today
	*/
	defaultVisibleDate: import_prop_types.default.instanceOf(Date),
	/**
	* The IDs of the resources initially visible.
	* To render a controlled scheduler, use the `visibleResources` prop.
	* @default {} - all resources are visible
	*/
	defaultVisibleResources: import_prop_types.default.object,
	/**
	* The timezone used to display events in the scheduler.
	*
	* Accepts any valid IANA timezone name
	* (for example "America/New_York", "Europe/Paris", "Asia/Tokyo"),
	* or keywords understood by the adapter, such as
	* "default" (use the adapter's default timezone),
	* "locale" (use the user's current locale timezone),
	* or "UTC".
	*
	* This timezone only affects rendering, events keep their original data timezone.
	* @default "default"
	*/
	displayTimezone: import_prop_types.default.string,
	/**
	* The color palette used for all events.
	* Can be overridden per resource using the `eventColor` property on the resource model.
	* Can be overridden per event using the `color` property on the event model.
	* @default "teal"
	*/
	eventColor: import_prop_types.default.oneOf([
		"amber",
		"blue",
		"green",
		"grey",
		"indigo",
		"lime",
		"orange",
		"pink",
		"purple",
		"red",
		"teal"
	]),
	/**
	* Configures how events are created.
	* If `false`, event creation is disabled.
	* If `true`, event creation is enabled with default configuration.
	* If an object, event creation is enabled with the provided configuration.
	*/
	eventCreation: import_prop_types.default.oneOfType([import_prop_types.default.shape({
		duration: import_prop_types.default.number,
		interaction: import_prop_types.default.oneOf(["click", "double-click"])
	}), import_prop_types.default.bool]),
	/**
	* The structure of the event model.
	* It defines how to read and write the properties of the event model.
	* If not provided, the event model is assumed to match the `CalendarEvent` interface.
	*/
	eventModelStructure: import_prop_types.default.object,
	/**
	* The events currently available in the calendar.
	* @default []
	*/
	events: import_prop_types.default.arrayOf(import_prop_types.default.object),
	/**
	* Set the locale text of the Event Calendar.
	* You can find all the translation keys supported in [the source](https://github.com/mui/mui-x/blob/HEAD/packages/x-scheduler/src/models/translations.ts)
	* in the GitHub repository.
	*/
	localeText: import_prop_types.default.object,
	/**
	* Event handler called when the collapsed resources change.
	*/
	onCollapsedResourcesChange: import_prop_types.default.func,
	/**
	* Callback fired when some event of the calendar change.
	*/
	onEventsChange: import_prop_types.default.func,
	/**
	* Event handler called when the preferences change.
	*/
	onPreferencesChange: import_prop_types.default.func,
	/**
	* Event handler called when the view changes.
	*/
	onViewChange: import_prop_types.default.func,
	/**
	* Event handler called when the visible date changes.
	*/
	onVisibleDateChange: import_prop_types.default.func,
	/**
	* Event handler called when the visible resources change.
	*/
	onVisibleResourcesChange: import_prop_types.default.func,
	/**
	* Preferences currently displayed in the calendar.
	*/
	preferences: import_prop_types.default.shape({
		ampm: import_prop_types.default.bool,
		isSidePanelOpen: import_prop_types.default.bool,
		showEmptyDaysInAgenda: import_prop_types.default.bool,
		showWeekends: import_prop_types.default.bool,
		showWeekNumber: import_prop_types.default.bool,
		weekStartsOn: import_prop_types.default.oneOf([
			0,
			1,
			2,
			3,
			4,
			5,
			6
		])
	}),
	/**
	* Config of the preferences menu.
	* Defines which options are visible in the menu.
	* If `false`, the menu will be entirely hidden.
	* @default { toggleWeekendVisibility: true, toggleWeekNumberVisibility: true, toggleAmpm: true, toggleEmptyDaysInAgenda: true, toggleWeekStartsOn: false }
	*/
	preferencesMenuConfig: import_prop_types.default.oneOfType([import_prop_types.default.oneOf([false]), import_prop_types.default.shape({
		toggleAmpm: import_prop_types.default.bool,
		toggleEmptyDaysInAgenda: import_prop_types.default.bool,
		toggleWeekendVisibility: import_prop_types.default.bool,
		toggleWeekNumberVisibility: import_prop_types.default.bool,
		toggleWeekStartsOn: import_prop_types.default.bool
	})]),
	/**
	* Whether the calendar is in read-only mode.
	* @default false
	*/
	readOnly: import_prop_types.default.bool,
	/**
	* The structure of the resource model.
	* It defines how to read and write the properties of the resource model.
	* If not provided, the resource model is assumed to match the `CalendarResource` interface.
	*/
	resourceModelStructure: import_prop_types.default.object,
	/**
	* The resources the events can be assigned to.
	*/
	resources: import_prop_types.default.arrayOf(import_prop_types.default.object),
	/**
	* Whether each event must be assigned to a resource. When true, the resource cannot be cleared in the edit dialog and the form cannot be submitted without one.
	* @default false
	*/
	shouldEventRequireResource: import_prop_types.default.bool,
	/**
	* Whether the component should display the current time indicator.
	* @default true
	*/
	showCurrentTimeIndicator: import_prop_types.default.bool,
	/**
	* The system prop that allows defining system overrides as well as additional CSS styles.
	*/
	sx: import_prop_types.default.oneOfType([
		import_prop_types.default.arrayOf(import_prop_types.default.oneOfType([
			import_prop_types.default.func,
			import_prop_types.default.object,
			import_prop_types.default.bool
		])),
		import_prop_types.default.func,
		import_prop_types.default.object
	]),
	/**
	* The view currently displayed in the calendar.
	*/
	view: import_prop_types.default.oneOf([
		"agenda",
		"day",
		"month",
		"week"
	]),
	/**
	* Configuration applied to each view, keyed by the view name.
	* For the `day` and `week` views, `startTime` and `endTime` (whole hours between 0 and 24)
	* limit the hours displayed in the time grid.
	* @example { week: { startTime: 8, endTime: 20 } }
	*/
	viewConfig: import_prop_types.default.shape({
		day: import_prop_types.default.shape({
			endTime: import_prop_types.default.number,
			startTime: import_prop_types.default.number
		}),
		week: import_prop_types.default.shape({
			endTime: import_prop_types.default.number,
			startTime: import_prop_types.default.number
		})
	}),
	/**
	* The views available in the calendar.
	* @default ["day", "week", "month", "agenda"]
	*/
	views: import_prop_types.default.arrayOf(import_prop_types.default.oneOf([
		"agenda",
		"day",
		"month",
		"week"
	]).isRequired),
	/**
	* The date currently used to determine the visible date range.
	*/
	visibleDate: import_prop_types.default.instanceOf(Date),
	/**
	* The IDs of the resources currently visible.
	* A resource is visible if it is not included in this object or if it is included with `true` value.
	*/
	visibleResources: import_prop_types.default.object
};
//#endregion
//#region node_modules/@mui/x-scheduler-internals/standalone-event/StandaloneEvent.mjs
var _excluded = [
	"className",
	"render",
	"style",
	"data",
	"onEventDrop",
	"renderDragPreview"
];
var StandaloneEvent$1 = /*#__PURE__*/ import_react.forwardRef(function StandaloneEvent(componentProps, forwardedRef) {
	const { data, onEventDrop, renderDragPreview } = componentProps, elementProps = _objectWithoutPropertiesLoose(componentProps, _excluded);
	const ref = import_react.useRef(null);
	const { getButtonProps, buttonRef } = useButton$1({
		disabled: false,
		native: false
	});
	const preview = useDragPreview({
		type: "standalone-event",
		data,
		renderDragPreview,
		showPreviewOnDragStart: true
	});
	const state = import_react.useMemo(() => ({ dragging: preview.state.isDragging }), [preview.state.isDragging]);
	const getDragData = useStableCallback(() => ({
		source: "StandaloneEvent",
		eventData: data,
		onEventDrop,
		eventId: data.id,
		occurrenceKey: `external-${data.id}`
	}));
	import_react.useEffect(() => {
		return draggable({
			element: ref.current,
			getInitialData: getDragData,
			onGenerateDragPreview: ({ nativeSetDragImage }) => {
				disableNativeDragPreview({ nativeSetDragImage });
			},
			onDragStart: ({ location }) => {
				preview.actions.onDragStart(location);
			},
			onDrag: ({ location }) => {
				preview.actions.onDrag(location);
			},
			onDrop: () => {
				preview.actions.onDrop();
			}
		});
	}, [getDragData, preview.actions]);
	const element = useRenderElement$1("div", componentProps, {
		state,
		ref: [
			forwardedRef,
			buttonRef,
			ref
		],
		props: [elementProps, getButtonProps]
	});
	return /*#__PURE__*/ (0, import_jsx_runtime.jsxs)(import_react.Fragment, { children: [element, preview.element] });
});
StandaloneEvent$1.displayName = "StandaloneEvent";
//#endregion
//#region node_modules/@mui/x-scheduler/standalone-event/StandaloneEvent.mjs
var StandaloneEventRoot = styled(StandaloneEvent$1, {
	name: "MuiEventCalendar",
	slot: "StandaloneEvent"
})({});
var StandaloneEvent = /*#__PURE__*/ import_react.forwardRef(function StandaloneEvent(props, forwardedRef) {
	return /*#__PURE__*/ (0, import_jsx_runtime.jsx)(StandaloneEventRoot, _extends({
		ref: forwardedRef,
		className: import_react.useContext(EventCalendarStyledContext)?.classes.standaloneEvent
	}, props, { renderDragPreview: (parameters) => /*#__PURE__*/ (0, import_jsx_runtime.jsx)(EventDragPreview, _extends({}, parameters)) }));
});
StandaloneEvent.displayName = "StandaloneEvent";
StandaloneEvent.propTypes = {
	/**
	* CSS class applied to the element, or a function that
	* returns a class based on the component's state.
	*/
	className: import_prop_types.default.oneOfType([import_prop_types.default.func, import_prop_types.default.string]),
	data: import_prop_types.default.shape({
		allDay: import_prop_types.default.bool,
		className: import_prop_types.default.string,
		color: import_prop_types.default.oneOf([
			"amber",
			"blue",
			"green",
			"grey",
			"indigo",
			"lime",
			"orange",
			"pink",
			"purple",
			"red",
			"teal"
		]),
		description: import_prop_types.default.string,
		draggable: import_prop_types.default.bool,
		duration: import_prop_types.default.number,
		exDates: import_prop_types.default.arrayOf(import_prop_types.default.string),
		extractedFromId: import_prop_types.default.oneOfType([import_prop_types.default.number, import_prop_types.default.string]),
		id: import_prop_types.default.oneOfType([import_prop_types.default.number, import_prop_types.default.string]).isRequired,
		readOnly: import_prop_types.default.bool,
		resizable: import_prop_types.default.oneOfType([import_prop_types.default.oneOf(["end", "start"]), import_prop_types.default.bool]),
		resource: import_prop_types.default.oneOfType([import_prop_types.default.arrayOf(import_prop_types.default.string), import_prop_types.default.string]),
		rrule: import_prop_types.default.oneOfType([import_prop_types.default.shape({
			byDay: import_prop_types.default.arrayOf(import_prop_types.default.oneOf([
				"-1FR",
				"-1MO",
				"-1SA",
				"-1SU",
				"-1TH",
				"-1TU",
				"-1WE",
				"-2FR",
				"-2MO",
				"-2SA",
				"-2SU",
				"-2TH",
				"-2TU",
				"-2WE",
				"-3FR",
				"-3MO",
				"-3SA",
				"-3SU",
				"-3TH",
				"-3TU",
				"-3WE",
				"-4FR",
				"-4MO",
				"-4SA",
				"-4SU",
				"-4TH",
				"-4TU",
				"-4WE",
				"-5FR",
				"-5MO",
				"-5SA",
				"-5SU",
				"-5TH",
				"-5TU",
				"-5WE",
				"1FR",
				"1MO",
				"1SA",
				"1SU",
				"1TH",
				"1TU",
				"1WE",
				"2FR",
				"2MO",
				"2SA",
				"2SU",
				"2TH",
				"2TU",
				"2WE",
				"3FR",
				"3MO",
				"3SA",
				"3SU",
				"3TH",
				"3TU",
				"3WE",
				"4FR",
				"4MO",
				"4SA",
				"4SU",
				"4TH",
				"4TU",
				"4WE",
				"5FR",
				"5MO",
				"5SA",
				"5SU",
				"5TH",
				"5TU",
				"5WE",
				"FR",
				"MO",
				"SA",
				"SU",
				"TH",
				"TU",
				"WE"
			]).isRequired),
			byMonth: import_prop_types.default.arrayOf(import_prop_types.default.number),
			byMonthDay: import_prop_types.default.arrayOf(import_prop_types.default.number),
			count: import_prop_types.default.number,
			freq: import_prop_types.default.oneOf([
				"DAILY",
				"MONTHLY",
				"WEEKLY",
				"YEARLY"
			]).isRequired,
			interval: import_prop_types.default.number,
			until: import_prop_types.default.string
		}), import_prop_types.default.string]),
		timezone: import_prop_types.default.string,
		title: import_prop_types.default.string.isRequired
	}).isRequired,
	/**
	* Whether the component renders a native `<button>` element when replacing it
	* via the `render` prop.
	* Set to `true` if the rendered element is a native button.
	* @default false
	*/
	nativeButton: import_prop_types.default.bool,
	/**
	* Callback fired when the event is dropped into the Event Calendar.
	*/
	onEventDrop: import_prop_types.default.func,
	/**
	* Allows you to replace the component's HTML element
	* with a different tag, or compose it with another component.
	*
	* Accepts a `ReactElement` or a function that returns the element to render.
	*/
	render: import_prop_types.default.oneOfType([import_prop_types.default.element, import_prop_types.default.func]),
	/**
	* Style applied to the element, or a function that
	* returns a style object based on the component's state.
	*/
	style: import_prop_types.default.oneOfType([import_prop_types.default.func, import_prop_types.default.object]),
	/**
	* The system prop that allows defining system overrides as well as additional CSS styles.
	*/
	sx: import_prop_types.default.oneOfType([
		import_prop_types.default.arrayOf(import_prop_types.default.oneOfType([
			import_prop_types.default.func,
			import_prop_types.default.object,
			import_prop_types.default.bool
		])),
		import_prop_types.default.func,
		import_prop_types.default.object
	])
};
//#endregion
//#region node_modules/@mui/x-scheduler/index.mjs
/**
* @mui/x-scheduler v9.0.0-beta.7
*
* @license MIT
* This source code is licensed under the MIT license found in the
* LICENSE file in the root directory of this source tree.
*/
//#endregion
export { AgendaView, CompactDayView, CompactThreeDayView, CompactWeekView, DayView, EventCalendar, MonthView, StandaloneAgendaView, StandaloneCompactDayView, StandaloneCompactThreeDayView, StandaloneCompactWeekView, StandaloneDayView, StandaloneEvent, StandaloneMonthView, StandaloneWeekView, WeekView, eventCalendarClasses, getEventCalendarUtilityClass, useEventCalendarUtilityClasses };

//# sourceMappingURL=@mui_x-scheduler.js.map