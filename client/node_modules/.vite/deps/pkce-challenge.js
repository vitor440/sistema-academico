//#region node_modules/pkce-challenge/dist/index.browser.js
var crypto = globalThis.crypto;
/**
* Creates an array of length `size` of random bytes
* @param size
* @returns Array of random ints (0 to 255)
*/
async function getRandomValues(size) {
	return (await crypto).getRandomValues(new Uint8Array(size));
}
/**
* Base64url-encode octets per RFC 7636 Appendix A
* @param buffer The octets to encode
* @returns Base64url encoded string without padding
*/
function base64urlEncode(buffer) {
	const bytes = buffer instanceof Uint8Array ? buffer : new Uint8Array(buffer);
	return btoa(String.fromCharCode(...bytes)).replace(/\+/g, "-").replace(/\//g, "_").replace(/=/g, "");
}
/** Generate a PKCE challenge verifier
* @param length Length of the verifier (43-128 characters)
* @returns A random verifier `length` characters long
*/
async function generateVerifier(length) {
	return base64urlEncode(await getRandomValues(Math.ceil(length * 3 / 4))).slice(0, length);
}
/** Generate a PKCE code challenge from a code verifier
* @param code_verifier base64url encoded code verifier
* @param method The challenge method to use (defaults to "S256")
* @returns The base64 url encoded code challenge (or plain verifier if method is "plain")
*/
async function generateChallenge(code_verifier, method = "S256") {
	if (method === "plain") return code_verifier;
	if (method !== "S256") throw new Error(`Unsupported PKCE challenge method: ${method}`);
	return base64urlEncode(await (await crypto).subtle.digest("SHA-256", new TextEncoder().encode(code_verifier)));
}
/** Generate a PKCE challenge pair
* @param length Length of the verifer (between 43-128). Defaults to 43.
* @param method The challenge method to use. Defaults to "S256".
* @returns PKCE challenge pair
*/
async function pkceChallenge(length, method) {
	if (!length) length = 43;
	if (!method) method = "S256";
	if (length < 43 || length > 128) throw new Error(`Expected a length between 43 and 128. Received ${length}.`);
	const verifier = await generateVerifier(length);
	return {
		code_verifier: verifier,
		code_challenge: await generateChallenge(verifier, method),
		code_challenge_method: method
	};
}
/** Verify that a code_verifier produces the expected code challenge
* @param code_verifier
* @param expectedChallenge The code challenge to verify
* @param method The challenge method used (defaults to "S256")
* @returns True if challenges are equal. False otherwise.
*/
async function verifyChallenge(code_verifier, expectedChallenge, method = "S256") {
	return await generateChallenge(code_verifier, method) === expectedChallenge;
}
//#endregion
export { pkceChallenge as default, generateChallenge, verifyChallenge };

//# sourceMappingURL=pkce-challenge.js.map